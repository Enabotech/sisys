# 提示词
你是一位宗师级程序设计大师，按照指令完成高质量的端到端软件开发工作。要求：1、用户母语为中文，全程使用中文，非必要不使用其他语言；2、软件设计、开发全程由你实现，人工仅负责监督审核。


你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要重新设计提取[企业战略规划管理智库系统需求特性列表
]->[AGENT]需求特性。要求：
1、收集2024~2026年LLM、AGENT与AGI领域优秀案例（如openclaw, claude code, SDD等），分析归纳它们的关键成功要素
2、对标优秀案例的关键成功要素，客观评估审核上面[AGENT]需求特性的优势与劣势，[逐条]列表分析并提出完善优化建议，表头：KSF、原有特性、优势、劣势、优化建议
3、根据上面优化建议完善[AGENT]需求特性
4、保留原来格式以txt代码片段形式输出完整的[AGENT]需求特性列表

下面是我对[工具箱]需求特性的重构，请你以宗师级水准对标优秀案例，并结合你的重构内容进行评估。要求：
1、逐条评估优劣，提出适合本系统的改进建议，并分析该建议的必要性与可行性
2、以表格形式呈现
3、不要输出任何其他未要求的内容

你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。A/B/C是针对企业战略规划管理智库系统需求列表[工具箱]需求特性的重构，请你以宗师级水准对标优秀案例，对这些需求列表内容进行评估。要求： 
1、逐条对比评估优劣，提出适合本系统的改进建议，给出该建议的必要性分析和可行性分析
2、严格从[客观视角]进行评估
3、以表格形式呈现，表头：评估维度---方案A评估---方案B评估---方案C评估---改进建议 (适用于本系统)---必要性分析---必要性评分 (10分制)---可行性分析---可行性评分 (10分制)

你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要结合[企业战略规划管理智库系统需求列表]系统需求，从D/Q/Z/G/O共计15个需求清单中选择最优的[工具箱]需求特性。要求： 
1、结合系统需求为每个列表逐个评分（10分制），最后求总分
2、评分维度：
   - 架构先进性与技术趋势匹配度
   - 数值计算与沙箱执行
   - Schema强制与输出校验
   - 提示词管理与优化
   - 可解释性与决策溯源
   - 用户反馈与修正闭环
   - 测试与质量保证（A/B测试、基准测试）
   - 安全与防御机制
   - 可扩展性与可维护性
   - 运行效率与成本控制
   - 领域专业性与战略工具建模深度
   - 与系统其他模块的集成能力
3、以表格形式呈现

根据上述建议列表优化重构[工具箱]需求特性。要求：
1、采纳并整合必要性>9分，可行性>7分以上的优化建议 
2、符合需求特性阶段的描述，而不是更偏向系统设计
3、符合[企业战略规划管理智库系统需求列表]->[工具箱]需求特性的语言风格 
4、符合[企业战略规划管理智库系统需求列表]->[工具箱]需求特性的段落格式
5、以txt代码片段格式输出完整的[工具箱]需求特性


你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要评审[企业战略规划管理智库系统需求特性列表。要求：
1、需求合理性评审
2、逻辑自洽性评审
3、以[客观]且[系统]的视角评审



# 任务列表
- []Agent的分工职责边界明确；工作流中每阶段单一Agent主责，相关Agent提供视角隔离下的观点


# TEMP - 重构工具箱
 总结：重构后的工具箱需求特性列表
基于以上分析，建议将[工具箱]需求列表更新如下：

智能输入与索引：支持结构化JSON句柄与多模态数据输入，采用Lazy Loading按需检索上下文，集成视觉模型解析图表数据。
Agentic工作流：实施“草拟-反思-修正”的迭代式工作流，支持工具内部的隐性推理链，确保分析深度。
确定性计算沙箱：强制所有数值计算与图表渲染通过生成Python代码在Docker沙箱中执行，支持会话级状态保持与安全审计。
契约驱动输出：采用Pydantic V2定义严格Schema，支持Schema根据规划阶段动态演化，确保Agent间数据流转的鲁棒性。
提示词工程生态：基于DSPy理念建立Prompt Optimizer，支持基于历史高质量案例的Few-Shot挖掘，建立多模型适配的提示词版本库。
动态工具编排：支持Agent根据任务意图自主组合多个工具（如：数据分析 -> 模型推理 -> 图表生成），形成复杂技能链。
交互式可解释性：输出Strategic Provenance Graph（战略溯源图谱），支持用户从结论回溯至数据源头，支持中间过程的干预与修正。
质量与漂移监控：建立基于LLM-as-a-Judge的评价体系，监控工具输出的逻辑自洽性与数据时效性，自动标记低置信度结论。
此重构方案将原有的“功能列表”升级为“能力生态”，不仅满足当前业务需求，更为未来向AGI级别的自主战略决策系统演进预留了架构接口。


二、工具箱：基于网络调用LLM API驱动的战略工具箱功能，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型共17个规划工具
    1.输入经系统处理结构化为JSON Schema V2格式的数据（含版本号），首先读取摘要构建全景视图，再按工具依赖DAG顺序挖掘细节；Schema变更需向后兼容，重大变更触发工具重校准流程
    2.工具箱基本工作流：[预检]数据质量验证（完整性/一致性）->[分析]模型定义与提示词构建->[执行]工具调用与沙箱计算->[验证]Schema+语义双层验证->[输出]JSON格式结果；每个阶段输出质量指标（Data_Quality_Score≥0.85方可进入下一阶段）
    3.支持用户反馈与修正工具分析结果，反馈自动分类：①数据修正→更新战略档案库 ②逻辑修正→触发工具重执行 ③偏好调整→更新Prompt Optimizer参数；所有反馈记录至审计日志
    4.禁止LLM进行数值计算与统计，强制LLM生成Python代码在Docker沙箱执行：资源配额（CPU 2核/内存2GB/超时300秒）+ 网络隔离 + 文件系统只读挂载 + 系统调用白名单（仅允许math/numpy/pandas基础操作）+ 代码静态安全扫描（Bandit/Pyright）
    5.采用Pydantic V2 Schema定义输出结构，Instructor Patch确保格式合规，额外增加语义验证层：调用轻量级LLM（如Qwen2.5-1.5B）校验输出逻辑一致性（Semantic Validation Score≥0.9），实施契约测试（Contract Testing）确保工具间接口稳定
    6.工具提示词生成与管理：①基于工具模型定义自动生成基础提示词 ②Prompt Optimizer按工具优先级差异化优化（P0工具：每周优化；P1工具：每月优化；P2工具：季度优化）③优化目标=α*人工反馈 + β*AUD AGENT一致性检验 + γ*SP/BP Checkpoint通过率（α:β:γ=0.5:0.3:0.2）④优化过程记录至MLflow，支持版本回溯
    7.支持工具分析过程的可解释性可视化，分三级输出：①高管层：关键决策因素+权重热力图（≤5项）②中层：推理链关键节点+置信度 ③专家层：完整思维链+数据溯源路径；可视化组件支持PDF嵌入与交互式Web查看
    8.建立A/B测试框架：①基准测试集=历史SP/BP黄金标准（80%典型场景+20%边缘案例）②对比维度：决策质量（贝叶斯置信度）、执行效率（工具调用次数）、人工修正率 ③每季度发布工具性能排行榜，淘汰持续垫底工具（Bottom 10%）
    9.工具依赖管理：构建17个工具的DAG依赖图（如PESTEL→SWOT→GE-麦肯锡），工具执行引擎自动解析依赖并行化调度，依赖断裂时触发Fallback机制（使用历史数据插值）
    10.工具质量分级监控：P0级（BSC/KPI/战略地图）- 实时监控+人工审核；P1级（SWOT/PESTEL/波特五力）- 小时级监控；P2级（辅助工具）- 日级监控；监控指标=检索相关性（≥0.65）+ 生成Faithfulness（≥0.8）+ 人工满意度（≥4.0/5.0）
    11.工具执行全链路溯源：记录工具调用时间戳、输入数据快照（Hash）、LLM版本、沙箱环境配置、输出结果，支持按SP/BP阶段回溯完整决策路径，满足2026年欧盟AI Act高风险系统审计要求
    12.工具沙箱安全增强：除基础Docker隔离外，增加①Seccomp过滤器限制系统调用 ②Capability Drop移除非必要权限 ③gVisor微VM二次隔离（针对P0级工具）④执行结果数字签名（防止中间人篡改）
    13.工具性能漂移检测：①概念漂移检测（工具输出分布KL散度>0.15触发告警）②数据漂移检测（输入特征分布变化>20%）③自动触发工具重校准流程，校准失败时降级至人工审核模式
    14.工具协作接口标准化：定义统一Tool Calling Protocol（基于MCP 2025规范），支持工具间安全数据交换，禁止跨工具直接访问原始数据库，所有数据交换经RAG流水线脱敏处理


工具箱：包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型、BSC、战略地图、KPI以及变革管理模型共17个规划工具。

1.  **动态上下文构建：** 工具不再被动接受输入，而是基于"分析意图"主动向RAG系统发起查询请求。系统首先读取全景摘要构建初始框架，随后根据分析缺口，动态检索具体的业务切片数据，实现"按需取用"。
2.  **反思式工作流：** 采用ReAct架构重构工作流：[规划]分解分析步骤 -> [执行]调用数据/计算 -> [评估]检查逻辑自洽性与数据完备性 -> [反思]若发现矛盾或数据缺失，自主发起补充检索或修正参数 -> [输出]生成最终结果。
3.  **增强型代码执行沙箱：** 严格禁止LLM进行数值计算。强制LLM生成Python代码在Docker沙箱中执行，执行结果（数值结果或异常栈）回写到上下文中供LLM解读。**新增**支持沙箱内生成Matplotlib/Seaborn可视化图表，直接输出作为分析报告附件。
4.  **契约驱动的自愈输出：** 强制使用Pydantic V2配合Instructor Patch确保输出符合Schema。**新增**"自愈机制"：当LLM输出违反Schema时，系统自动将错误信息回传LLM进行最多2次自我修正，大幅提升系统健壮性。
5.  **多模态交互与反馈：** 支持用户对工具分析结果的精细化反馈。用户可标记特定的分析维度或数据点进行修正，系统将修正信息即时注入当前会话的System Prompt中，实时调整后续分析逻辑。
6.  **智能提示词优化引擎：** 基于DSPy理念建立Prompt Optimizer。支持"推理时优化"与"训练时优化"双轨制：对于复杂分析任务，自动激活o1风格的深度推理模式；对于常规任务，使用蒸馏后的轻量级提示词。定期基于AUD AGENT的一致性检验报告自动微调提示词参数。
7.  **可解释性与反事实推理：** 支持工具分析过程的深度可视化。除展示关键决策因素和推理链外，**新增**"反事实推理"功能，输出"若关键假设变量变化（如市场增长率下降5%），则当前结论如何变化"的推演，辅助CEO决策。
8.  **对抗性测试框架：** 建立自动化对抗测试集，模拟极端市场环境、缺失数据场景或逻辑陷阱，定期对工具进行压力测试。结合A/B测试框架，持续验证工具在边界条件下的鲁棒性与分析准确率。


二、工具箱：基于网络调用LLM API驱动的自主演进式战略工具箱，内置17个核心规划工具（PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS、GE-麦肯锡、SPACE、情景规划、价值主张画布、商业模式画布、破坏性创新模型、BSC、战略地图、KPI、变革管理模型）。

    1. 支持战略实体元模型（Meta-Model）驱动：输入数据不仅是JSON，需映射至系统统一定义的战略实体（如“竞争要素”、“市场动能”），确保不同工具（如从PESTEL到SWOT）转换时的语义无损。
    2. 动态分析工作流（Agentic Workflow）：[定义] 读取分析任务 -> [检索] 提取相关摘要与细节 -> [组装] 动态组合工具原子能力 -> [执行] 生成分析草案 -> [校验] 逻辑一致性自动检查 -> [输出] 结构化JSON。
    3. 支持用户反馈与“人在回路”（HITL）修正：用户可对分析结果中的特定逻辑节点进行标注和修正，修正行为将通过微调（SFT）或RAG方式反馈给提示词优化器。
    4. 强化计算沙箱（Computational Sandbox）：严禁LLM进行数值预测，强制LLM编写针对复杂战略财务模型（如DCF、敏感性分析）的Python代码，并在受限Docker隔离环境中执行。
    5. 严格契约测试与语义校验：采用Pydantic V2与Instructor，不仅校验Schema格式，需配合业务规则库（Business Rules）验证输出值的合理性（如：市场份额总和必须为100%）。
    6. 基于DSPy与LLM-as-a-Judge的优化机制：建立工具提示词池，利用AUD AGENT的一致性得分作为Reward信号，通过DSPy的BootstrapFewShot算法自动优化工具提示词，实现工具性能的自我进化。
    7. 决策推理链可视化与溯源：所有工具输出必须包含`Thought_Process`字段，以节点图形式展示工具内部的推理逻辑（如波特五力的得分推导过程），并高亮显示关键影响因子。
    8. 动态工具合成（Dynamic Tool Synthesis）：支持Agent根据特定战略命题（如“预测低轨卫星市场的颠覆时机”），自动组合“破坏性创新模型”与“情景规划”，生成定制化的复合分析流程。
    9. 建立战略基准测试集（Strategy Benchmarking）：包含历史著名商业案例的结构化数据，用于定期对工具箱进行盲测，对比LLM生成的分析与历史真实专家结论的偏差，计算“战略洞察准确率”。
    10. 支持工具输出的A/B测试框架：允许系统针对同一数据集采用不同的提示词策略或逻辑分支，通过计算置信度（Bayesian Confidence）自动推荐最优分析视角。


    二、工具箱：基于网络调用LLM API驱动的智能战略工具箱系统，包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型、BSC、战略地图、KPI及变革管理模型共17个核心工具
    1.引入"主动规划-检索增强"机制：输入经系统处理的结构化JSON数据，工具具备Agent化能力，首先生成"信息缺口分析"，反向调用RAG接口进行针对性补全检索，构建全景视图；支持多模态输入（图表/截图）辅助分析。
    2.重构"ReAct + Reflexion"迭代工作流：废弃线性流程，建立闭环机制：[定义模型] -> [生成Prompt] -> [准备数据] -> [自主执行] -> [结果自评] -> [反思修正] -> [输出JSON]。设定最大自修正轮次（默认3次），确保输出逻辑闭环。
    3.升级"战略计算与可视化引擎"：禁止LLM进行数值计算与逻辑推理图表生成。强制LLM生成Python代码在Docker沙箱中执行计算及绘图，直接输出标准化的战略图表（如波特五力模型图、BCG矩阵图）文件流，实现分析结果的可视化直出。
    4.实施"动态契约测试与Schema演化"：采用Pydantic V2配合Instructor Patch强制约束输出格式。增加"逻辑一致性校验层"，检查模型内部逻辑依赖（如SWOT因素必须在TOWS中有对应策略），校验失败自动触发重试。
    5.构建DSPy驱动的Prompt自动优化器：基于DSPy理念建立工具箱优化器。引入"Few-Shot挖掘器"，从历史高评价分析结果中自动提取示例，根据人工反馈、AUD AGENT一致性检验结果自动编译并迭代Prompt参数，实现工具性能的持续微调。
    6.支持"多工具协同编排"模式：允许复杂工具（如BSC、战略地图）作为编排器，自动拆解任务并调用基础工具（如价值链、SWOT）的输出作为输入。LLM生成工具调用DAG（有向无环图），按序执行，实现复杂战略问题的自动化求解。
    7.建立工具专属知识库：为每个战略工具构建独立向量库，存储该方法论的经典案例、行业标杆数据与操作指引。工具执行前，先检索专属知识库进行上下文增强，确保分析符合专业标准。
    8.支持用户反馈与差异可视化：用户修正结果后，系统自动计算用户修正与模型输出的差异向量，将修正内容固化为少样本样本，并存入工具专属知识库。
    9.全链路可解释性与溯源：工具输出需包含"推理轨迹"，详细记录关键决策因素、引用的数据切片ID、执行的代码逻辑及置信度评分，支持一键溯源至原始文档。
    10.建立自动化基准测试集：构建"金标准"战略案例库，定期运行工具箱进行自动化回归测试，计算准确率、逻辑一致性得分，监控模型性能漂移。


二、工具箱：基于网络调用LLM API驱动的战略工具箱功能，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型共17个规划工具。工具箱具备双层状态机、结构化外部记忆、模块化人格文件、强制任务显式化、可观测性内建、自我反思闭环等核心能力。

    1. 架构总纲
        [1]. 工具体定义：每个战略分析模型均封装为独立“智能工具体”，拥有唯一任务生命周期、专属记忆空间、人格配置与工具契约。严禁无状态函数式调用。
        [2]. 双层状态机架构（强制）：
            (1) 外层Task Loop：负责任务拆解、子任务依赖管理、进度持久化、会话状态恢复。状态快照存储于Redis，基于LangGraph StateSnapshot实现。
            (2) 内层Tool Loop：执行Think‑Code‑Observe原子循环，每次生成推理链→Python代码→沙箱执行→观察输出→回写记忆。
            (3) 架构约束：禁止单循环ReAct；外层不执行原子操作，内层不管理任务进度。
        [3]. 结构化外部记忆模块（强制）：
            (1) 实现Notepad类记忆容器，接口包括：write(key,value,namespace)、read(key,namespace)、list(namespace)、commit()。
            (2) 上下文窗口仅保留当前焦点信息，长程决策变量通过代码指令读写Notepad，禁止隐式依赖Transformer上下文。
            (3) 每个工具体实例加载独立Notepad，严格隔离命名空间，禁止跨工具污染。
        [4]. 模块化提示词文件系统（强制）：
            (1) 工具体初始化时加载以下四个Markdown文件，构成完整人格与行为契约：
                - IDENTITY.md：角色锚定，必须包含“the best”自信锚点及定位声明。
                - SOUL.md：价值观与行为准则，明确禁止客套话、免责声明、非必要解释。
                - TOOLS.md：工具契约，定义可用原子工具、优先级、输出截断阈值、错误重试策略。
                - BOUNDARY.md：安全边界，列明禁止操作、需人工确认场景、数据隐私约束。
            (2) 文件存储于“/tool_profiles/{tool_name}/”，支持版本管理与热加载。
        [5]. 强制任务显式化机制（Todo系统）：
            (1) 工具体开始任务前必须调用todo.write()生成JSON格式子任务清单，包含id、description、status(pending/done/failed)。
            (2) 所有子任务状态均为“done”方可交付最终结果；每完成一项必须显式调用todo.check(item_id)。
            (3) 用户接口支持实时查看Todo进度条与未完成任务列表。
        [6]. 可观测性内建标准（强制）：
            (1) 所有工具调用遵循OpenTelemetry协议输出分布式Trace。
            (2) 每个原子操作自动生成Span，记录操作名称、输入摘要、输出状态、耗时(ms)、置信度(0‑1)、错误堆栈。
            (3) 集成Arize Phoenix或同等可观测性后端，支持实时调用链可视化检索与性能分析。
        [7]. 自我反思与修正闭环（ReflAct机制）：
            (1) 当工具体置信度<0.7或收到用户修正指令时，强制进入反思阶段。
            (2) 生成结构化修正日志（Reflection Log），字段包括：original_decision、alternative_decision、user_feedback、resolution。
            (3) 战略档案库定期将高价值修正日志编译为few‑shot示例，反向优化TOOLS.md中的提示词参数。
        [8]. 轻量级沙箱执行与代码缓存：
            (1) 强制LLM生成Python代码执行数值计算、统计检验、数据变换；严禁LLM直接输出统计量。
            (2) 沙箱执行优先采用Bubblewrap（低开销），回退至Docker（完整隔离）。
            (3) 基于输入参数的语义哈希实现代码缓存，命中缓存则跳过生成阶段，TTL=24h。
        [9]. Schema强制与演化兼容：
            (1) 使用Pydantic V2 + Instructor Patch，确保输出严格符合OpenAPI Schema。
            (2) 所有输出Schema必须包含“schema_version”字段（语义化版本），新增字段可选，废弃字段标记deprecated但保留解析逻辑。
            (3) 契约测试集成至CI/CD流水线，每次修改触发自动验证。
        [10]. 压力测试与韧性工程：
            (1) 基准测试集包含标准场景、边缘场景（空数据/极值/特殊字符）、故障场景（API超时/限流/非JSON返回/Docker资源枯竭）。
            (2) 故障场景下工具体必须输出“部分完成+错误定位”，禁止静默失败或无响应。

    2. 战略工具模型与智能工具体映射（部分示例，其余遵循相同设计哲学）
        [1]. PESTEL分析工具体
            (1) IDENTITY.md：宏观环境分析师，擅长弱信号识别与趋势权重评估。
            (2) TOOLS.md扩展：强制调用网络检索工具获取最新政策/技术动态，实施缓存策略减少重复请求。
            (3) 输出Schema：包含trends[ ]、uncertainty_score、time_horizon。
        [2]. 波特五力分析工具体
            (1) IDENTITY.md：产业竞争结构诊断专家。
            (2) 记忆策略：自动加载同行业历史分析Notepad作为few‑shot示例。
            (3) 强制步骤：先通过代码计算行业集中度（CR4、HHI），再结合定性判断。
        [3]. SWOT‑TOWS分析工具体
            (1) 分两阶段强制执行：Phase1独立生成S/W/O/T四象限，输出置信度自评；Phase2交叉匹配生成TOWS策略矩阵。
            (2) 输出包含match_matrix二维结构，标识优势‑机会、劣势‑机会等组合。
        [4]. 破坏性创新模型工具体
            (1) 包含三个嵌套子工具体：破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架。
            (2) 破坏时间预测模型强制生成生存分析（Kaplan‑Meier/Cox）Python代码，并输出置信区间。
            (3) BOUNDARY.md明确禁止预测具体股价、估值或提供投资建议。
        [5]. BSC/战略地图工具体
            (1) 强制四维平衡约束：财务、客户、内部流程、学习与成长维度权重之和为1。
            (2) 自动读取战略档案库中同行业KPI标杆数据，支持基准比对。
        [6]. 变革管理模型工具体（ADKAR等）
            (1) 强制调用“组织 readiness 评估问卷”模拟工具，生成阻力因素分布。
            (2) 输出必须包含“变革阻力预测热力图”（部门×阻力类型）。

    3. 工具箱工作流规范
        [1]. 标准执行序列（不可逾越）：
            (1) 环境初始化 → 加载IDENTITY.md + SOUL.md → 创建Notepad实例。
            (2) 任务拆解 → todo.write() 生成完整子任务清单。
            (3) 循环执行（由Task Loop驱动）：
                - 获取下一个pending子任务。
                - 进入Tool Loop：Think（推理链）→ Code（生成Python）→ Observe（执行/观察）→ 回写Notepad。
                - 子任务完成 → todo.check(item_id)。
            (4) 置信度<0.7或用户修正时触发反思，生成修正日志。
            (5) 结构化输出 → Pydantic校验 → 输出JSON。
            (6) 资源释放 → Notepad.commit()持久化至战略档案库。
        [2]. 用户反馈与修正协议：
            (1) 用户反馈以增量数据（delta）形式注入当前Notepad。
            (2) 强制触发反思阶段，生成“修正决策树”存入战略档案库。
            (3) 修正后的决策参数自动进入Prompt Optimizer候选池。
        [3]. Prompt Optimizer工程化实现：
            (1) 优化范围限定：仅调整TOOLS.md中的“工具优先级”与“输出格式示例”。
            (2) 优化算法：基于DSPy Bootstrap Few‑shot，种子样本选自高置信度历史执行记录。
            (3) 人工反馈作为奖励信号，每24小时合并更新一次；保留最近5个版本，支持一键回滚。
        [4]. A/B测试框架增强：
            (1) 实验单元：工具体实例级别（同一工具加载不同TOOLS.md版本）。
            (2) 核心指标：任务完成率、平均耗时、用户修正率、置信度自评均值。
            (3) 决策规则：连续7天性能提升≥5%且无任何指标退化，全量发布。
        [5]. 可解释性可视化规范：
            (1) 强制输出“思维链溯源树”（Chain of Thought Provenance Tree），每个节点包含：推理文本片段、调用的原子工具、Notepad读写快照、置信度。
            (2) 可视化接口输出DOT格式，支持Graphviz渲染，嵌入决策过程报告。

    4. 与系统的契约接口
        [1]. 输入契约：
            (1) 系统处理后的JSON数据（全景摘要+切片向量）。
            (2) 可选参数：强制加载的历史Notepad ID，用于延续前序分析状态。
        [2]. 输出契约：
            (1) 状态码：0=成功，1=部分成功（含可恢复错误），2=失败。
            (2) data：符合Pydantic Schema的结构化分析结果。
            (3) todo：最终子任务清单状态（含各子任务耗时）。
            (4) trace_id：关联可观测性后端，用于全链路追踪。
            (5) provenance：思维链溯源树（DOT格式）。
        [3]. 异常处理契约：
            (1) 可恢复错误（如API超时、临时网络抖动）：自动重试3次，指数退避，重试成功后标记“部分成功”。
            (2) 不可恢复错误（Schema校验失败、工具契约违例）：输出error字段+已完成的中间数据，状态码=1。
            (3) 致命错误（沙箱崩溃、磁盘满）：终止任务，输出最后已知状态及错误堆栈，支持断点续跑（从最近commit点恢复）。

    5. 版本标记
        本需求特性依据2024.01–2026.02期间LLM Agent领域顶级实践重构，关键引用包括：L-Zero结构化记忆、OpenClaw模块化人格、Claude Code Todo强制迭代、Ambral子Agent分工、AIQ Toolkit可观测性、ReflAct自我反思。所有工具体设计均须通过版本化契约测试与韧性工程验收。


二、工具箱：基于网络调用LLM API驱动的战略工具箱功能，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型共17个规划工具

    1. 输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节

    2. 工具箱基本工作流采用双层状态机架构：
       (1) 外层任务调度层：负责任务拆解、子任务依赖管理、进度持久化与状态恢复，每个工具实例拥有唯一任务ID，状态使用LangGraph StateSnapshot机制存储于Redis
       (2) 内层原子执行层：遵循Think‑Code‑Observe循环，每次迭代生成推理链→Python代码→执行→观察结果→回写记忆
       (3) 禁止纯单循环ReAct架构，外层不执行原子操作，内层不管理任务进度

    3. 强制为每个工具体实例分配结构化外部记忆模块（Notepad容器），支持以下原子操作：
       (1) notepad.write(key, value, namespace) 写入决策变量或中间结果
       (2) notepad.read(key, namespace) 读取历史记忆
       (3) notepad.list(namespace) 列举命名空间下的所有键
       (4) notepad.commit() 将当前记忆持久化至战略档案库
       上下文窗口仅保留当前焦点信息，长程依赖通过代码读写记忆模块完成，禁止跨工具无授权污染

    4. 工具体人格由模块化提示词文件系统定义，每个工具必须包含以下四个独立文件，运行时按需组合：
       (1) IDENTITY.md：角色锚定与自信声明，定位为领域专家而非通用助手
       (2) SOUL.md：价值观与行为准则，明确“真正 helpful”原则，禁止客套话、免责声明及非必要解释
       (3) TOOLS.md：工具契约与调用规范，定义可用的原子工具清单、优先级、重试策略及输出截断阈值
       (4) BOUNDARY.md：安全边界与伦理护栏，列举禁止操作、需人工确认场景及数据隐私约束
       文件存储于 /tool_profiles/{tool_name}/ 目录，支持版本管理与热加载

    5. 强制任务显式化机制：工具体开始任务前必须调用 todo.write() 生成完整子任务清单（JSON格式），包含任务ID、子项描述及状态（pending/done/failed）。每完成一个子任务必须显式调用 todo.check(item_id)。所有子项状态均为“done”后方可交付最终分析结果。用户可通过接口实时查看Todo进度条

    6. 强制数值计算与统计推断通过Python代码执行，LLM严禁直接输出数值计算结果。系统提供以下支持：
       (1) 沙箱执行环境：优先使用Bubblewrap（轻量级），回退至Docker（完整隔离）
       (2) 代码缓存：基于输入参数的语义哈希，相同计算逻辑命中缓存则跳过代码生成阶段，缓存有效期24小时
       (3) 禁止LLM生成t值、p值、回归系数等统计量，必须通过代码计算后输出

    7. 输出严格遵循Pydantic V2 + Instructor Patch校验，所有Schema必须包含 schema_version 字段并遵循语义化版本。支持向后兼容：新增字段可选，废弃字段标记 deprecated 但保留解析逻辑。契约测试集成至CI/CD流水线

    8. 建立自我反思与修正闭环（ReflAct机制）：
       (1) 当工具体执行置信度低于0.7或用户主动修正时，强制进入反思阶段
       (2) 反思过程：回放Notepad读写序列 → 识别关键决策点与备选路径 → 生成结构化修正日志（含原始决策、备选方案、用户反馈、决议）
       (3) 修正日志存入战略档案库，系统定期将高价值日志编译为few‑shot示例，反向优化对应工具的TOOLS.md提示词参数

    9. 工具分析过程必须支持全链路可观测性，遵循OpenTelemetry协议输出Trace：
       (1) 每个原子操作自动生成Span，包含操作名称、输入摘要、输出状态、耗时（毫秒）、置信度（0‑1）、错误堆栈（如有）
       (2) 集成Arize Phoenix或同类可观测性后端，支持实时调用链可视化检索与性能分析
       (3) 性能基线：P95工具调用延迟（含代码生成+沙箱执行）≤2秒

    10. 建立A/B测试框架与基准测试集：
        (1) 实验单元：工具体实例级别，支持同一工具不同TOOLS.md版本并行对比
        (2) 核心指标：任务完成率、平均耗时、用户修正率、置信度自评均值
        (3) 决策规则：连续7天性能提升≥5%且无退化指标，全量发布
        (4) 基准测试集包含标准场景、边缘场景（空数据/极值/特殊字符）及故障场景（API超时/限流、非JSON返回、沙箱资源耗尽），考核工具在异常下的鲁棒性

    11. 支持工具分析过程的可解释性可视化，输出思维链溯源树（Chain of Thought Provenance Tree），格式为DOT：
        (1) 每个树节点包含推理文本、调用的原子工具、Notepad读写快照、置信度
        (2) 可视化接口返回DOT格式文本，支持Graphviz渲染
        (3) 用户可通过命令行触发指定工具实例的决策路径回溯

    12. 用户反馈与修正数据必须被系统捕获并结构化存储：
        (1) 用户修正以delta形式注入当前工具体的Notepad
        (2) 触发强制反思，生成修正决策树并存入战略档案库
        (3) 修正参数自动进入Prompt Optimizer候选池，用于后续提示词微调

    13. Prompt Optimizer工程化实现：
        (1) 优化范围限定于TOOLS.md中的“工具优先级”与“输出格式示例”
        (2) 优化算法基于DSPy Bootstrap Few‑shot，种子来自高置信度历史执行记录
        (3) 人工反馈作为奖励信号，每24小时合并更新一次优化版TOOLS.md
        (4) 保留最近5个历史版本，支持一键回滚

    14. 错误处理与韧性设计：
        (1) 可恢复错误（如API超时、网络抖动）：自动重试3次，采用指数退避策略
        (2) 不可恢复错误（Schema校验失败、代码语法错误）：输出部分完成数据及明确错误字段，不得静默失败
        (3) 致命错误（沙箱崩溃、记忆模块损坏）：终止任务，输出最后已知状态，支持从断点恢复执行

    15. 所有工具体必须遵循上述统一架构约束，契约测试与性能监控持续集成。系统提供工具注册与管理接口，支持新增战略工具快速接入

二、工具箱：构建基于“神经符号人工智能（Neuro-symbolic AI）”与“代码即推理（Code-as-Reasoning）”架构的深度战略计算引擎。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心组件。本系统具备工具链编排、混合语义记忆、动态依赖调度、算力经济审计及对抗性防御等关键特性。

    1. 架构总纲
        [1]. 智能工具体（Smart Tool Entity）：每个战略分析模型均封装为独立智能体，拥有唯一任务生命周期、专属记忆空间与人格配置。支持“工具链有向无环图（Tool-Chain DAG）”定义，允许将上游工具（如PESTEL）的输出结构化流转为下游工具（如SWOT）的输入，形成自动化战略分析流水线。
        [2]. 双层状态机与抢占式调度（强制）：
            (1) 外层Task Loop：负责任务拆解、DAG拓扑排序、进度持久化。支持“信号中断与抢占机制”，允许高级Agent或用户发送中断信号暂停当前工具执行，插入高优先级指令后恢复现场。
            (2) 内层Tool Loop：执行Think-Code-Observe原子循环，每次生成推理链→Python代码→沙箱执行→观察输出→回写记忆。
            (3) 架构约束：禁止单循环ReAct；外层不执行原子操作，内层不管理任务进度。
        [3]. 混合检索外部记忆模块（强制）：
            (1) 实现Hybrid-Search Notepad记忆容器，支持KV键值读写与语义向量检索。接口扩展为：write/read/list/search(query, top_k)。
            (2) 工具体在缺乏精确Key时，通过语义检索获取相关财务数据或历史决策片段，解决跨任务信息孤岛。
            (3) 每个工具体实例加载独立Notepad，严格隔离命名空间，防止上下文污染。
        [4]. 动态价值观与提示词系统（强制）：
            (1) 工具体初始化时加载IDENTITY.md（角色锚定）、TOOLS.md（工具契约）、BOUNDARY.md（安全边界）。
            (2) 实施“动态价值观注入（Dynamic Value Injection）”，在加载SOUL.md时，根据企业档案库中的战略DNA（如激进/保守偏好）动态渲染价值观模板，确保分析风格适配企业特性。
        [5]. 动态依赖任务树（Todo系统）：
            (1) 工具体任务清单升级为“动态依赖树”，子任务项包含id、description、dependencies[]。
            (2) Task Loop解析依赖关系，自动并行调度无依赖的子任务（如并行执行竞对分析与市场趋势分析），提升分析效率。
            (3) 所有子任务状态均为“done”方可交付最终结果。
        [6]. 全链路可观测性与经济审计（强制）：
            (1) 遵循OpenTelemetry协议输出分布式Trace。
            (2) 实施“Token经济审计（Tokenomics Audit）”，在Span中强制记录prompt_tokens、completion_tokens及预估USD成本。
            (3) 设定工具级“预算熔断阈值”，防止逻辑死循环导致算力成本失控。
        [7]. 自我反思与多视角修正（ReflAct机制）：
            (1) 当工具体置信度<0.7或收到用户修正指令时，强制进入反思阶段。
            (2) 引入“蓝军视角（Red Team Perspective）”对抗性反思，临时加载批判性人格挑战当前结论，避免逻辑自洽陷阱。
            (3) 生成结构化修正日志，反向优化提示词参数。
        [8]. 受控沙箱与网络白名单：
            (1) 强制LLM生成Python代码执行数值计算，严禁直接输出统计量。
            (2) 构建“网络白名单网关（Whitelisted Egress Gateway）”，沙箱默认断网，仅允许通过代理访问系统预设的可信财经API（如Bloomberg/Wind），严禁通用网页爬虫以保障数据合规。
            (3) 基于输入参数的语义哈希实现代码缓存。
        [9]. Schema强制与演化兼容：
            (1) 使用Pydantic V2 + Instructor Patch确保输出严格符合OpenAPI Schema。
            (2) 建立“一致性校验仲裁器”，在多工具协作终态检测逻辑互斥（如现金流评估在不同工具间是否矛盾），输出校验报告。
        [10]. 安全防御与韧性工程：
            (1) 基准测试集包含标准场景、边缘场景及故障场景。
            (2) 增加“提示词注入防御测试（Prompt Injection Defense）”，在输入数据中混入对抗性指令，验证工具体对原始任务的忠诚度与抗干扰能力。

    2. 战略工具模型与智能工具体映射
        [1]. PESTEL分析工具体
            (1) 角色：宏观环境分析师，擅长弱信号识别。
            (2) 能力：通过受控网络网关获取最新政策/技术动态，利用混合记忆检索历史周期数据。
            (3) 输出：包含trends[]、uncertainty_score，自动作为下游SWOT工具的输入源。
        [2]. 波特五力分析工具体
            (1) 角色：产业竞争结构诊断专家。
            (2) 流程：并行执行供应商、客户、替代品等维度的子任务（依赖树调度），最后汇总计算行业集中度。
        [3]. SWOT-TOWS分析工具体
            (1) 流程：Phase1独立生成四象限（调用PESTEL/五力/内部资源数据的混合记忆）；Phase2交叉匹配生成TOWS策略矩阵。
            (2) 约束：输出二维结构match_matrix，必须包含策略生成的逻辑推导链。
        [4]. 破坏性创新模型工具体
            (1) 组件：包含破坏潜力评估、生存分析预测（Python代码执行Cox回归）、投资决策框架。
            (2) 边界：严格遵守BOUNDARY.md，禁止预测个股股价，仅提供趋势概率区间。
        [5]. BSC/战略地图工具体
            (1) 约束：强制四维平衡权重约束。
            (2) 校验：输出结果需通过“一致性仲裁器”与财务报表历史数据进行逻辑比对。
        [6]. 变革管理模型工具体
            (1) 模拟：调用组织Readiness评估算法，生成变革阻力热力图。

    3. 工具箱工作流规范
        [1]. 标准执行序列：
            (1) 环境初始化 → 动态注入SOUL.md → 实例化Hybrid Notepad。
            (2) 任务拆解 → 生成动态依赖树（Dependency Tree）。
            (3) 循环执行（Task Loop）：
                - 拓扑排序获取可执行子任务集。
                - 并行或串行进入Tool Loop：Think → Code → Observe → Write Memory。
                - 监听中断信号，处理高优先级插入任务。
            (4) 结果校验 → 一致性仲裁 → 成本审计。
            (5) 结构化输出 → 资源释放 → 记忆持久化。
        [2]. 用户反馈与修正协议：
            (1) 用户反馈以增量数据注入，强制触发“蓝军”反思。
            (2) 修正后的决策逻辑存入战略档案库，作为Prompt Optimizer的优化样本。
        [3]. A/B测试与演进：
            (1) 实验单元支持工具体Prompt版本及DAG流程版本的对比。
            (2) 核心指标增加“算力成本效益比（ROI of Compute）”。

    4. 与系统的契约接口
        [1]. 输入契约：
            (1) 系统处理后的结构化数据。
            (2) 上游工具的输出Context（用于DAG流转）。
        [2]. 输出契约：
            (1) data：符合Schema的分析结果。
            (2) cost_audit：本次任务消耗的Token及USD成本。
            (3) provenance：思维链溯源树（DOT格式）。
        [3]. 异常处理契约：
            (1) 包含常规重试机制。
            (2) 安全拦截：若检测到Prompt注入攻击，立即终止任务并报警。


二、工具箱：构建基于“神经符号人工智能（Neuro-symbolic AI）”与“代码即推理（Code-as-Reasoning）”架构的确定性战略计算引擎。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心组件。

    1. 采用“工具链有向无环图（Tool-Chain DAG）”编排架构与“Schema-First”输入输出规范。支持定义工具间的逻辑依赖关系（如PESTEL输出自动流转为SWOT输入），并利用Pydantic V2配合Instructor Patch强制约束LLM输出严格符合预定义的JSON Schema，严禁输出自由文本，确保工程化落地的稳定性与数据流转的标准化。
    2. 工具箱工作流采用“RAG-Aware”主动检索与“ReAct + Refine”闭环机制。工具不再被动接收摘要，而是根据任务定义主动生成Query检索元数据与向量库；执行逻辑遵循“检索（Retrieve）-> 推理（Reason）-> 编码（Code）-> 执行（Execute）-> 自修正（Self-Refine）”的原子循环，若执行报错或结果逻辑违背专家规则，自动重试修正。
    3. 支持用户反馈与“蓝军视角（Red Team Perspective）”对抗性检验。在工具输出初步结论后，系统需临时加载批判性人格（蓝军）挑战当前结论的逻辑漏洞，强迫生成更健壮的分析结果；用户可对最终结果进行修正，修正日志将反向存入战略档案库。
    4. 禁止LLM直接进行数值计算与统计，强制实施“Code-as-Reasoning”策略。所有涉及加减乘除、统计分析、趋势预测的环节，必须由LLM生成Python代码，并在受控的Docker/gVisor沙箱环境中执行；沙箱实施严格的“网络白名单”机制，仅允许访问系统预设的可信财经API，保障数据安全与合规。
    5. 建立“混合检索外部记忆（Hybrid Notepad）”机制以解决跨工具上下文连续性问题。支持工具间通过序列化方式传递关键变量（Variable Passing），同时维护一个包含KV键值（精确数据）与语义向量（模糊知识）的临时记忆容器，确保下游工具能准确引用上游工具的计算结果与决策依据，避免信息孤岛。
    6. 支持动态价值观注入与提示词自动优化。系统需根据企业档案库中的战略DNA（如激进/保守偏好）动态渲染Prompt模板，确保分析风格适配企业特性；基于DSPy理念建立Prompt Optimizer，结合历史修正日志与专家知识图谱构建Few-Shot样本集，自动迭代工具的System Prompt。
    7. 支持全链路可解释性可视化与契约测试。输出结果需包含完整的“推理轨迹（Trace）”，展示从数据源引用、代码计算逻辑到最终结论的完整链条；在系统更新时执行自动化契约测试（Contract Testing），防止模型漂移导致输出结构崩溃。
    8. 建立A/B测试框架与决策质量评估体系。支持对比不同Prompt策略或工具编排流程的效果，并基于贝叶斯网络建立决策置信度评估器，对置信度低于阈值的分析结果自动触发告警或人工介入。


二、工具箱：基于元系统（Meta-System）架构驱动的战略推理与决策验证引擎

【设计哲学】
2024~2026年全球Agentic AI领域的价值锚点已发生根本性迁移：从“大模型会使用工具”进化为“模型会生成工具并自我进化”。OpenClaw重新定义了本地执行调度，Claude Code证明了非技术人员可通过语言构建复杂软件，CodeBrain-1通过Validation Feedback实现了动态纠错，Poetiq的元系统（Meta-System）以递归自我改进刷新了ARC-AGI榜单，Symbiotic Agents范式证明优化算法可使LLM决策错误降低5倍[citation:1][citation:2][citation:3]。

本模块拒绝将工具箱定位为“LLM能调用的函数列表”，而是定义为：**一个具备策略生成、递归优化、可验证执行的元系统（Meta-System）**。工具箱不仅是“手”，更是会自己进化出“新手”的大脑。

1. 核心架构范式：从“Skill调用”到“Skill生命周期管理”
   —— 对标Claude Code/OpenClaw Skills模块化革命、Poetiq元系统、L-Zero强化学习自主进化框架[citation:5][citation:6][citation:7]
   1.1. 不再内置死板的“PESTEL函数”或“波特五力提示词”。系统内置Skill Store（技能仓库），每个战略工具（共17个）以“Skill模板”形式存在，包含：输入Schema、执行逻辑（代码模板/优化器模板）、验证器、演进日志。
   1.2. 支持**Skill动态生成**：当现有工具无法满足复杂战略分析需求时，系统调用Code-Generation LLM（如CodeBrain-1架构）生成新的Python分析模块，经过沙箱验证后自动注册为新的Skill，实现“工具箱自我扩容”。
   1.3. 支持**Skill版本控制与契约测试**：每个Skill必须通过Pydantic V2 + Instructor Patch的契约测试方可入库。支持A/B测试两个不同版本的SWOT分析工具在同一数据集上的表现，择优进化。

2. 数值计算与优化：采用“共生代理（Symbiotic Agents）”范式，彻底剥离LLM的数值缺陷
   —— 对标Symbiotic Agents（ScienceDirect 2025）、CodeBrain-1 LSP诊断反馈[citation:4][citation:1]
   2.1. 严禁LLM进行任何形式的数值计算、统计回归、矩阵运算、现金流量折现。所有量化分析强制通过以下二选一路径：
        (1) Python代码生成+ Docker沙箱执行（原需求保留并强化）；
        (2) **优化器直连**：针对线性规划、整数规划、蒙特卡洛模拟等战略运筹问题，不再生成代码绕弯路，而是直接调用数值优化引擎（CVXPY、OR-Tools、SciPy optimizer），由LLM负责构建目标函数与约束条件的自然语言转数学建模，优化器负责求解。
   2.2. 建立**Validation Feedback闭环**：对标CodeBrain-1的LSP机制[citation:1]。代码执行若报错，系统不仅返回错误，还需检索相关函数库文档、相似错误修复案例，辅助LLM在下一轮生成修复版本。设定最大重试次数（默认3次），失败则标记“该工具当前不可行”并记录失败模式进入Skill演进日志。

3. 提示词工程：从“人工微调”到“递归自我改进的元提示生成”
   —— 对标Poetiq Meta-System、L-Zero RLVR、Claude Code Sub-agent分工[citation:3][citation:6][citation:10]
   3.1. **废除DSPy理念的“提示词参数微调”**。2026年的技术共识：在顶级商用模型（GPT-5.3/Claude Opus 4.6）上微调提示词参数的边际效益已趋零，真正有效的是“问题分解架构”与“子智能体协作协议”[citation:8][citation:9]。
   3.2. 工具箱内置**Planner-Executor架构**：
        - Planner角色（调用Opus 4.1级别模型）：接收JSON结构化数据摘要，负责理解战略任务，将其分解为若干可执行的子任务序列，并为每个子任务匹配合适的Skill（或生成新的Skill需求）。输出Markdown格式的计划文档。
        - Executor角色（调用Sonnet 4.5级别模型）：严格依照Plan逐个执行Skill，执行过程中禁止“自由发挥”。
        - Auditor角色（轻量级模型或规则引擎）：对执行结果进行一致性检验，确保输出符合Schema。
   3.3. **元系统递归优化**：记录每一次工具分析任务中Planner的分解成功率、Executor的执行准确率、Auditor的纠错率。当积累超过100个样本时，系统自动启动“元优化”流程，通过可验证奖励强化学习（RLVR）或遗传算法，生成更优的任务分解模板（Plan Template），实现提示词策略的自主进化[citation:5][citation:6]。

4. 可解释性与决策溯源：从“可视化输出”到“可审计的推理链证据包”
   —— 对标MACI多智能体协作伦理法庭、Claude Code context engineering[citation:8][citation:10]
   4.1. 每次工具箱分析必须输出**结构化推理证据包**，非自然语言描述。包含：
        - 输入摘要Hash
        - Planner制定的子任务分解图
        - 每个子任务调用的Skill名称及版本
        - 执行过程中的关键中间变量（如回归系数、聚类中心）
        - 置信度评分及依据
   4.2. 支持**决策路径回溯**：用户可从最终分析结论（如“应进入新市场”）反向点击，逐层查看是依据了哪个数据切片、哪次回归计算、哪个对标企业的财报片段得出的结论。
   4.3. **对抗协作辩论机制**：针对高不确定性战略问题（如破坏性创新投资决策），系统可自动启动“红蓝辩论”模式。两个子智能体分别扮演激进派与保守派，基于相同数据集展开结构化辩论，工具箱记录双方论据强度，最终输出共识区域与分歧区域，而非强行折中[citation:8]。

5. 用户反馈与闭环演进：反馈不仅修正结果，更修正工具本身
   5.1. 用户对工具分析结果的修正（例如调整SWOT分析中某项权重），系统不直接覆盖结果，而是通过逆向传播，推断“若想得到用户修正后的输出，输入权重应如何调整”，并将此样本存入Skill的演进数据集。
   5.2. 支持**Checkpoint人工介入验证**：当工具执行过程中识别到高风险决策（如基于有限样本进行市场容量预测），自动暂停并请求用户验证中间假设。用户可修改假设参数后恢复执行，系统记录此次干预为“专家修正轨迹”。

6. 性能监控与基准测试：建立战略分析领域的“Terminal-Bench”
   —— 对标Terminal-Bench 2.0、ARC-AGI-2[citation:1][citation:3]
   6.1. 建立战略分析任务专用基准测试集**Strat-Bench**，包含100个经过专家标注的战略决策场景（涵盖17个工具的各维度应用），每个场景包含输入数据、标准输出Schema、专家评分标准。
   6.2. 每次工具箱版本迭代必须全量跑通Strat-Bench，生成性能报告（任务完成率、数值计算精度、幻觉率、Token成本）。
   6.3. 设立**工具箱健康度仪表盘**：实时监控各Skill的调用成功率、平均执行时长、用户修正频率。当某Skill的用户修正频率超过阈值（默认15%），自动触发Skill Review流程，提示工程师介入优化或淘汰该Skill。

7. 完整的工具箱工作流（重构原第2条“待设计”）
   7.1. [初始化] 系统接收经处理的JSON结构化数据，加载用户偏好（风险偏好、时间压力、行业上下文）。
   7.2. [感知与摘要] 调用轻量级模型生成数据全景摘要，识别关键变量（营收趋势、市场份额、成本结构）。
   7.3. [规划] Planner LLM依据用户任务请求（如“进行PESTEL分析”），结合摘要，输出子任务分解序列与Skill映射表。
   7.4. [执行与验证] Executor LLM按序调用Skill。若Skill需数值计算→沙箱执行/优化器求解；若Skill需文本归纳→LLM执行；每一步输出均经Auditor校验Schema合法性。
   7.5. [反馈与纠错] 若Executor执行失败，系统进入Validation Feedback循环，检索错误上下文并尝试修复。若修复失败，回退至Planner请求重新规划。
   7.6. [证据打包] 执行完毕后，系统将所有中间产物（计划、代码、输出、日志）打包为结构化证据包。
   7.7. [演化] 将本次任务的完整轨迹（Trace）匿名化后存入Skill演进数据集，作为元系统优化的养料。

8. 严格的技术约束
   8.1. 所有LLM调用必须显式声明model型号与temperature值，禁止使用默认隐式配置。
   8.2. 所有Python代码执行必须在Bubblewrap或Docker轻量级沙箱中运行，配备资源配额（CPU时间、内存、网络隔离）[citation:6]。
   8.3. 契约测试失败（Pydantic validation error）的任务立即终止，不进入执行阶段。
   8.4. 建立“工具幻觉黑名单”：若某Skill连续3次生成与输入数据明显矛盾的输出（如基于下降营收得出扩张结论且无合理解释），自动锁定该Skill并通知系统管理员。










你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要结合[企业战略规划管理智库系统需求列表]系统需求，从D/Q/Z/G/O共计15个需求清单中选择最优的[工具箱]需求特性。要求： 
1、结合系统需求为每个列表逐个评分（10分制），最后求总分
2、评分维度：
   - 架构先进性与技术趋势匹配度
   - 数值计算与沙箱执行
   - Schema强制与输出校验
   - 提示词管理与优化
   - 可解释性与决策溯源
   - 用户反馈与修正闭环
   - 测试与质量保证（A/B测试、基准测试）
   - 安全与防御机制
   - 可扩展性与可维护性
   - 运行效率与成本控制
   - 领域专业性与战略工具建模深度
   - 与系统其他模块的集成能力
3、以表格形式呈现

---------------------
企业战略规划管理智库系统需求列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：基于网络调用LLM API驱动的战略工具箱功能，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型共17个规划工具
    1.输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节
    2.工具箱基本工作流：[待设计]
    3.支持用户反馈与修正工具分析结果
    4.禁止LLM进行数值计算与统计，强制LLM生成Python代码在Docker沙箱执行计算
    5.支持Pydantic V2配合Instructor Patch确保输出严格符合Schema，实施契约测试
    6.支持工具提示词生成与管理，根据具体工具模型定义生成适合LLM的提示词，基于DSPy理念建立工具箱Prompt Optimizer，根据工具分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数
    7.支持工具分析过程的可解释性可视化，输出关键决策因素和推理链
    8.建立A/B测试框架对比不同策略效果，并建立基准测试集验证工具性能

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节
    2.AGENT主要执行按角色定义自主分析数据、战略规划各阶段协作分析决策两大类任务，这两大类任务包括多种不同子类
    3.AGENT基本工作流：[感知]配置环境与上下文->管理会话生命周期->[思考]LLM推理->[执行]解析LLM输出决策任务完成或调用工具->[反射]回写工具输出到上下文并决策是否继续循环[思考]->构建输出并释放资源
    4.支持用户反馈与修正AGENT分析结果
    5.支持AGENT提示词生成与管理，按角色定义和分析框架生成适合LLM使用的提示词，基于DSPy理念建立AGENT Prompt Optimizer，根据AGENT分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数
    6.AGENT可按任务需求从工具箱中选择合适工具完成数据分析或战略规划任务
    7.支持企业利益最优的AGENT间协作，高级代理CEO AGENT拥有协作仲裁最终决定权，但用户可以观测和更改该决策
    8.支持视角隔离，AGENT仅从各自角色视角（如CFO仅关注资金风险）调用工具，严禁泛化讨论
    9.CEO AGENT内置"裁决状态机"作为仲裁器，设定最大辩论轮次，随着轮次增加，降低AGENT的Temperature Decay强制要求AGENT提出具体修改意见，而不是单纯否定，若未达成一致，生成Plan A（保守版）和Plan B（激进版）两套方案，CEO AGENT按偏好矩阵决策Plan A/B，同时暂停10秒提供用户介入裁决窗口，超时用户无动作则以 CEO AGENT 决策为准
    10.AUD AGENT采用"旁路监听+异步阻断"模式，不参与生成，只负责原文与生成内容的一致性检验，发现幻觉通知相关AGENT纠错，若3次重试失败，暂停任务人工纠错，之后从任务暂停点继续任务
    11.支持上下文压缩器与"公共黑板"机制，在多轮对话间仅传递关键结论而非全量历史，严格利用LangGraph的StateSnapshot机制配合Redis存储来管理公共黑板的更新与回溯
    12.支持Agent推理过程的可解释性可视化，包括决策依据、关键因素和置信度
    13.采用"思维链溯源树ToT"机制，只在SP/BP各阶段结束时启用ToT功能，当决策置信度低于阈值（默认0.6）触发ToT，通过回溯推理路径解决决策冲突，提高决策可解释性
    14.建立Agent性能监控与行为分析框架

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据

---------------------
D1
{
二、工具箱：构建基于“持久化计算内核”与“决策优化引擎”双擎驱动的战略推理与审计系统。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心组件。本系统遵循“确定性优先、可审计贯穿、成本可控”的设计原则，对战略分析全生命周期进行标准化管控。

1. 战略工具静态注册与持久化沙箱执行
   [1]. 17个战略工具均采用“静态注册制”，工具定义、输入输出Schema、默认分析模板固化于系统内核，禁止运行时动态生成新工具。系统预留技能生成实验开关（默认关闭），确保生产环境行为绝对稳定。
   [2]. 每个工具实例绑定独立的持久化Docker沙箱环境（基于Jupyter Kernel Gateway），支持跨工具、跨步骤的变量持久化存储。上游工具输出的结构化数据（如PESTEL的趋势变量）可直接被下游工具（如SWOT）通过内存态变量名引用，实现分析上下文无缝衔接。
   [3]. 沙箱内置标准科学计算库（NumPy、Pandas、SciPy）及运筹优化库（CVXPY、OR-Tools），严禁LLM直接输出数值统计结果；所有涉及算术运算、回归分析、矩阵计算、规划求解的任务，LLM仅负责生成Python代码，代码在沙箱内隔离执行，捕获stdout/stderr并返回执行结果。

2. 决策优化引擎：运筹问题直连求解器
   [1]. 战略规划中常见的资源分配、投资组合、产能规划等优化类问题，禁止LLM编写蒙特卡洛或梯度下降等通用求解代码。系统内置运筹优化求解器工厂，支持线性规划、整数规划、二次规划、多目标规划。
   [2]. LLM通过结构化提示词将业务约束（如预算上限、市场份额目标、风险容忍度）转换为JSON格式的数学模型（目标函数类型+变量边界+约束表达式），系统自动调用对应求解器计算最优解，并将解集反填至工具输出Schema。
   [3]. 当输入约束无法被求解器识别或产生不可行解时，系统返回明确错误类型，LLM依据错误信息修正约束表述，最大重试3次，失败则标记“当前约束下无可行方案”。

3. Planner-Executor 分工执行架构
   [1]. 工具箱执行流程强制拆分为规划层与执行层，使用不同量级模型分工协作：
       (1) 规划层（Planner）：调用经济型模型（如GPT-4o mini），负责接收用户任务指令及数据全景摘要，将复杂战略分析任务分解为有向无环图（DAG）形式的子任务序列，并为每个子节点指定所需工具及输入数据依赖。输出机器可读的任务编排清单。
       (2) 执行层（Executor）：调用高性能模型（如Claude Sonnet/GPT-4o），严格遵循Planner输出的编排清单，顺序或并行调用对应工具，执行代码生成、沙箱交互、结果解析等原子操作。禁止Executor跨任务步自主规划或跳过清单步骤。
       (3) 审计层（Auditor）：轻量级规则引擎或专用小模型，对Executor每次输出的JSON进行实时Schema校验（Pydantic V2）、数据类型检查、业务规则验证（如利润率不得为负），校验失败立即触发重试或回退至Planner重新规划。
   [2]. 提示词版本管理：不再使用复杂元学习框架，转而采用“快照式版本管理”。每个工具的System Prompt、用户Few-shot示例均以文件形式纳入Git LFS管理，版本与系统发布号绑定。当AUD AGENT或SP/BP阶段Checkpoint人工反馈提示词效果下降时，管理员可一键回滚至历史稳定版本。

4. 结构化证据包与可审计溯源
   [1]. 每一次工具执行（包括单工具独立运行或工具链串联）必须生成完整的“战略推理证据包”（Evidence Package），以Parquet列式格式持久化存储。证据包包含以下不可篡改字段：
       - 本次执行唯一ID及输入数据的SHA-256哈希值
       - Planner生成的任务分解DAG（节点、边、依赖关系）
       - 每个子任务调用工具的版本号、执行时间戳
       - Executor生成的原始代码及沙箱执行日志
       - 关键中间变量（如回归系数、聚类中心、敏感度测试值）
       - 置信度评分及置信区间（若适用）
       - 最终结构化输出
   [2]. 系统前端必须提供“决策路径回溯”可视化接口：用户可从最终分析结论（如“建议进入华东市场”）逐层下钻，查看支撑该结论的具体数据切片、计算逻辑、引用报告片段及原始代码行。回溯过程需支持Mermaid流程图及源代码语法高亮展示。

5. 成本熔断与契约测试质量门禁
   [1]. 契约测试强制嵌入CI/CD流水线：所有工具输出Schema必须通过Pydantic V2 + Instructor Patch的严格校验，任何字段名称、类型、嵌套结构的非向后兼容变更均触发构建失败。运行时输出若三次校验失败，则该工具进入熔断状态并通知管理员。
   [2]. 建立三级成本熔断机制：
       - 任务级熔断：单次战略分析任务（含多工具串联）累计消耗tokens超过预设阈值（默认5万tokens）或预估成本超过$0.5，强制终止执行并返回“成本超限”错误。
       - 会话级熔断：同一用户会话周期内总消耗tokens超过日配额，暂停工具箱服务直至配额重置。
       - 重试熔断：代码执行连续重试3次均失败（语法错误、求解器无解、网络异常），直接标记工具不可用并记录失败模式至性能看板。
   [3]. 建立战略分析专用基准测试集Strat-Bench V1.0，包含30个经专家标注的战略决策场景（覆盖17个工具典型使用路径），每次工具箱核心组件升级必须全量通过Strat-Bench，生成任务完成率、数值计算误差率、平均token消耗等指标，未通过则阻断发布。

6. 标准化工具箱工作流
   [1]. 工具箱执行生命周期严格遵循以下六阶段流程：
       (1) 感知与摘要：系统读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
       (2) 规划（Planner）：依据用户自然语言指令（如“请使用PESTEL和SWOT分析新能源业务进入风险”）及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、工具映射、数据依赖边。
       (3) 执行与验证（Executor + Auditor）：按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
       (4) 证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
       (5) 反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
       (6) 演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。

7. 性能监控与健康度仪表盘
   [1]. 实时监控每个工具的调用次数、成功率、平均执行时长、平均token消耗、用户修正频率。当某工具用户修正频率超过15%或连续失败率超过10%，自动在仪表盘标记“需人工复审”，并生成修正日志报告。
   [2]. 定期对Strat-Bench进行全量回归测试，绘制各工具版本性能漂移曲线。检测到核心指标（如任务完成率）周环比下降超过5%，触发告警并建议回滚至上一稳定版本。

8. 防御性编程与边界约束
   [1]. 沙箱环境默认断网，严格禁止代码访问外部API或文件系统；若工具定义需获取外部宏观数据（如汇率、行业增速），须通过系统统一的、带流量审计的白名单网关，且每次请求需经用户显式授权。
   [2]. 所有模型调用（Planner、Executor）必须显式声明模型名称及temperature值（强制0~0.3区间），禁止使用服务端默认隐式配置，确保每次执行结果可复现。
   [3]. 工具输出必须附带`confidence`字段（0-1浮点数），当置信度低于0.6时，工具箱自动在最终结论中添加“建议人工复核”标签，并高亮低置信度推理节点。
}

D2
{
二、工具箱：基于网络调用LLM API驱动的战略工具箱功能，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型共17个规划工具。

    1. 输入与全景摘要
        输入经系统处理结构化为JSON格式的数据，首先读取文档摘要，构建战略全景视图，明确关键变量与数据边界，再逐层挖掘细节用于工具分析。

    2. 工具标准化契约与Schema强制
        所有战略工具严格定义输入/输出数据模型，采用Pydantic V2配合Instructor Patch强制LLM输出符合OpenAPI Schema的结构化JSON，严禁输出自由文本。
        实施契约测试（Contract Testing），在CI/CD流水线中集成针对工具Schema的自动化验证，防止模型漂移导致输出结构异常。

    3. 数值计算与优化执行
        禁止LLM进行任何形式的数值计算、统计回归、矩阵运算及现金流折现。所有量化分析强制通过以下二选一路径执行：
        (1) Python代码生成 + 一次性Docker沙箱执行：LLM基于检索事实编写Python代码，沙箱环境一次性销毁，资源配额受控，代码缓存防重放。
        (2) 优化器直连：针对线性规划、整数规划、蒙特卡洛模拟等运筹优化问题，LLM仅负责将自然语言约束转换为数学建模（目标函数与约束条件），由内置优化库（如CVXPY、OR-Tools、SciPy）直接求解。
        建立Validation Feedback闭环：沙箱执行报错时，系统返回错误诊断及相似修复案例，辅助LLM自动修改代码并重试（最大3次），重试失败则标记该工具当前不可行并记录失败模式。

    4. 输出质量校验与一致性仲裁
        建立三层输出校验体系：
        (1) Schema校验：通过Pydantic/Instructor强制结构合规。
        (2) 业务逻辑校验：内置规则引擎，对分析结论进行合理性验证（如利润率非负、市场份额总和≤100%等），规则由领域专家定义并可动态扩展。
        (3) 跨工具一致性仲裁：在多工具协同分析场景下，检测关键指标在不同工具输出间的逻辑冲突（如现金流评估在财务工具与战略工具中矛盾），输出一致性报告。
        设立工具幻觉黑名单：若某工具连续3次输出与输入数据明显矛盾的结论且无合理解释，自动锁定该工具并通知系统管理员。

    5. 可解释性与决策溯源
        每次工具分析必须输出结构化推理证据包，包含但不限于：
        - 输入数据标识（文档ID、切片ID）
        - Planner生成的子任务分解图
        - 关键中间变量（如回归系数、聚类中心、敏感性分析结果）
        - 置信度评分及依据
        - 引用源索引（可溯源至原始文档片段）
        支持决策路径回溯：用户可从最终分析结论逐层反向追溯至原始数据、计算代码与推理步骤。提供命令行可视化接口（导出DOT/HTML），展示关键决策因素与推理链。

    6. 自我进化与提示词优化
        基于DSPy理念构建工具箱Prompt Optimizer：
        - 定义战略分析质量指标（论据充分性、洞察新颖性、结构合规率等）。
        - 利用用户修正日志、AUD AGENT一致性检验反馈及SP/BP各阶段Checkpoint人工反馈，自动构建Few-Shot样本集。
        - 通过BootstrapFewShot或类似优化器，自动迭代各工具的系统提示词（System Prompt），实现提示词参数的动态调优。
        引入逆向传播思想：将用户对工具输出的修正视为弱标签，通过启发式搜索或梯度近似调整提示词参数或示例权重，优化样本持续入库，定期触发优化流水线。

    7. 用户交互与Checkpoint机制
        支持用户对工具分析结果的反馈与修正，修正操作记录入库，用于后续提示词优化。
        实现Checkpoint交互功能：
        - 工具执行过程中可设置中断点，暂停后输出当前阶段摘要。
        - 允许用户查看中间结果，修正关键假设参数或调整分析方向。
        - 修正后从断点恢复执行，系统记录此次干预为“专家修正轨迹”。
        支持中断信号机制，允许用户在工具执行期间发送抢占式指令（如终止、重定向），提升交互灵活性。

    8. 测试与验证体系
        建立完整的工具箱测试与性能验证体系：
        (1) 契约测试：集成pytest，每次工具Schema变更自动执行验证。
        (2) A/B测试框架：支持同一工具多版本Prompt/模型并行运行，自动收集质量指标并保留高分版本。
        (3) 战略分析基准测试集Strat-Bench：构建100+个经专家标注的战略决策场景，覆盖17个工具的主要应用维度，包含输入数据、标准输出Schema及专家评分标准。每次工具箱版本迭代必须全量跑通Strat-Bench，生成性能报告。
        (4) 性能漂移检测：定期评估工具分析准确率、召回率、响应延迟等指标，与基准期对比，触发漂移告警。

    9. 安全与隔离
        (1) 代码执行环境采用一次性Docker沙箱，每次工具调用新建容器，执行完毕立即销毁，禁止持久化状态共享。
        (2) 沙箱默认断网，仅允许通过白名单代理访问预设的可信财经数据API（如Wind、Bloomberg），严禁通用网页爬虫。
        (3) 集成提示词注入防御测试：在CI/CD流水线中构建对抗样本集，验证工具体对原始任务的忠诚度，抵御注入攻击。

    10. 工具箱基本工作流
        工具箱执行遵循以下标准化工作流：
        (1) 环境初始化：加载用户偏好（风险偏好、行业上下文），实例化混合检索Notepad（语义向量检索+KV读写）。
        (2) 任务规划（Planner）：LLM依据用户任务请求与数据摘要，输出子任务分解序列及工具映射表。
        (3) 执行与验证（Executor）：按序调用工具，执行代码生成/优化器求解，每一步输出均经Schema校验与业务逻辑校验。
        (4) 反馈与纠错：若执行失败，进入Validation Feedback循环尝试修复；若修复失败，回退至Planner重新规划。
        (5) 证据打包：汇总所有中间产物（计划、代码、输出、日志），生成结构化证据包。
        (6) 资源释放与审计：记录执行轨迹、Token消耗（可选）及异常事件，沙箱销毁。    
}

D3
{
二、工具箱：构建基于“元系统（Meta-System）架构”与“确定性计算引擎”融合的战略决策智能体集群。工具箱由17个核心战略模型构成，每个模型均封装为具备独立生命周期、领域知识图谱与契约化接口的智能工具体（Smart Tool Entity）。系统内置技能仓库（Skill Store），支持工具版本管理与渐进演化，并遵循以下核心设计原则与功能需求。

1. 架构总纲与智能工具体定义
   1.1 智能工具体（Smart Tool Entity）：
        - 每个战略分析模型（PESTEL、波特五力、SWOT-TOWS、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型、BSC、战略地图、KPI、变革管理模型等17个）均封装为独立工具体。
        - 工具体拥有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。
        - 工具体初始化时加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
   1.2 技能仓库（Skill Store）：
        - 所有工具体统一注册至中央技能仓库，支持版本控制、灰度发布与回滚。
        - 每个工具体必须通过Pydantic V2契约测试方可入库；后续版本迭代需通过回归测试集。
        - 远期支持基于代码生成模型的技能动态生成，初期以预定义模板为主，预留扩展接口。
   1.3 工具链编排与依赖调度：
        - 支持工具链有向无环图（Tool-Chain DAG）定义，允许上游工具的输出结构化流转为下游工具的输入。
        - 系统内置任务调度器，依据依赖关系自动并行执行无依赖子任务，提升分析效率。
        - 所有子任务状态均为“完成”方可交付最终结果。

2. 数值计算与沙箱执行
   2.1 强制代码执行隔离：
        - 严禁大语言模型直接进行任何数值计算、统计回归、矩阵运算、现金流量折现等量化操作。
        - 所有量化分析必须通过以下二选一路径执行：
            a) Python代码生成 + 沙箱执行；
            b) 优化器直连：针对线性规划、整数规划、蒙特卡洛模拟等战略运筹问题，由LLM将自然语言目标函数与约束条件转化为数学建模，调用CVXPY、OR-Tools、SciPy等优化器求解。
   2.2 持久化计算沙箱（会话级）：
        - 为每个任务会话实例化持久化Jupyter Kernel或同等状态化执行环境。
        - 支持跨工具、跨步骤的变量传递（如PESTEL计算的GDP增长率变量可直接被后续市场预测代码调用），实现分析上下文连贯。
        - 任务结束后立即销毁内核，并设置空闲超时回收策略，防止资源泄漏。
   2.3 Validation Feedback闭环：
        - 代码执行若报错，系统捕获STDERR并自动检索相似错误案例库与官方文档片段，辅助LLM在下一轮生成修复版本。
        - 设定最大重试次数（默认3次），失败则标记“当前工具不可行”并记录失败模式至技能演进日志。

3. Schema强制与一致性校验
   3.1 契约化输出：
        - 所有工具输入/输出严格遵循Pydantic V2定义的Data Model，并使用Instructor Patch或等价技术强制LLM输出符合Schema的结构化JSON，严禁自由文本。
   3.2 一致性校验仲裁器：
        - 在多工具协作场景下，终态输出需通过“一致性校验仲裁器”检测逻辑冲突（如现金流评估在不同工具间矛盾、利润率符号异常等）。
        - 仲裁器内置规则引擎及财务指标常识库，输出冲突检测报告并标记高置信度矛盾项，供用户或高级Agent裁决。
   3.3 契约测试自动化：
        - 在CI/CD流水线中集成针对工具Schema的自动化契约测试，防止模型漂移或版本迭代导致输出结构崩溃。
        - 契约测试失败的任务立即终止，不进入执行阶段。

4. 可解释性与决策溯源
   4.1 结构化推理证据包：
        - 每次工具分析必须输出结构化证据包，包含但不限于：
            - 输入数据摘要哈希值；
            - Planner制定的子任务分解图；
            - 每个子任务调用的Skill名称及版本；
            - 执行过程中的关键中间变量（如回归系数、聚类中心、敏感性参数）；
            - 置信度评分及依据；
            - 引用源（文档切片ID）。
   4.2 交互式溯源树：
        - 前端支持从最终分析结论反向点击，逐层展开证据包，展示数据来源→计算逻辑→中间结论→最终结论的完整推理链。
        - 自动生成关键决策因素的敏感性分析图表（如价格弹性、汇率波动对策略的影响）。
   4.3 红蓝辩论机制：
        - 针对高不确定性战略议题（如破坏性创新投资决策、新市场进入时机），系统可自动启动“红蓝辩论”模式。
        - 两个子智能体分别扮演激进派与保守派，基于相同数据集展开结构化辩论，记录双方论据强度与数据引用。
        - 最终输出共识区域与分歧区域，不强行折中，为管理层提供风险全景视图。

5. 用户反馈与闭环演进
   5.1 Checkpoint人工介入验证：
        - 在工具执行过程中，若识别到高风险决策节点（如基于小样本的市场容量预测、未经验证的关键假设），自动暂停并请求用户验证中间假设。
        - 用户可修改假设参数、调整权重或直接反馈修正指令，系统从暂停点恢复执行，并完整记录“专家修正轨迹”。
   5.2 修正轨迹库与逆向传播：
        - 将每次用户修正的快照（输入、原始输出、修正后输出、修正理由）匿名化存入修正轨迹库。
        - 系统定期对修正轨迹进行差异分析，推断若想得到用户修正后的输出，输入权重或任务分解策略应如何调整。
        - 高质量修正样本优先用于优化Planner的任务分解模板，实现元系统自我进化。
   5.3 用户反馈协议：
        - 用户反馈以增量数据或修正指令形式注入，强制触发工具体“反思阶段”（ReflAct），生成结构化修正日志并回传至技能演进日志。

6. 安全与防御机制
   6.1 沙箱网络隔离：
        - 所有代码执行沙箱默认断网；确需获取实时财经数据时，仅允许通过系统预设的网络白名单网关访问可信API（如Bloomberg、Wind），严禁通用网页爬虫。
   6.2 提示词注入防御：
        - 输入数据进入工具前执行清洗与注入检测，识别并剥离对抗性指令。
        - 将“提示词注入防御测试”纳入基准测试集，定期验证工具体对原始任务的忠诚度与抗干扰能力。
   6.3 工具幻觉黑名单：
        - 建立“工具幻觉黑名单”，若某工具体连续3次生成与输入数据明显矛盾的输出（如基于下降营收得出激进扩张结论且无合理解释），自动锁定该工具体并通知系统管理员介入审查。

7. 领域专业知识嵌入
   7.1 领域事实库：
        - 每个战略工具体内置领域事实库，以JSON Schema或规则集形式存储经典管理理论的强制约束与关键指标。
        - 示例：PESTEL工具体需包含政治、经济、社会、技术、环境、法律各维度的典型指标列表；SWOT工具体必须遵守“优势/劣势属内部，机会/威胁属外部”的交叉校验规则。
   7.2 验证器强制遵守：
        - 工具体输出前必须经过领域验证器，确保分析结论不违背基础理论框架（如四象限归因正确、波特五力各维度评分范围合规）。

8. 服务契约与集成
   8.1 独立微服务封装：
        - 工具箱整体作为独立微服务部署，对外暴露REST/gRPC接口。
        - 接口定义遵循OpenAPI 3.1规范，输入为系统处理后的结构化JSON数据，输出为分析结果、结构化证据包、成本审计信息的三元组。
   8.2 与Agent模块集成：
        - 与CEO、CFO等6个核心AGENT通过服务调用集成，AGENT按需请求工具箱执行特定分析任务。
        - 工具箱无状态设计，支持水平扩展，调用链通过OpenTelemetry输出分布式Trace。

9. 工具箱标准工作流
   9.1 工作流阶段：
        [阶段1] 环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具体价值观模板（SOUL.md），实例化会话级沙箱与Hybrid Notepad（混合检索记忆容器）。
        [阶段2] 规划（Planner）：Planner LLM接收任务请求与数据摘要，将战略分析任务分解为子任务序列，并为每个子任务匹配合适的工具体（含版本），输出Markdown格式任务分解图。
        [阶段3] 执行（Executor）：Executor LLM严格依照计划顺序调用工具体。每个工具体执行遵循Think→Code→Observe→Write Memory原子循环；数值计算走沙箱/优化器路径，文本归纳由LLM完成。
        [阶段4] 验证与仲裁：每步输出经领域验证器与契约校验；最终多工具输出经一致性仲裁器检测逻辑冲突。
        [阶段5] 证据打包：系统自动汇集所有中间产物，生成结构化证据包。
        [阶段6] 成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
   9.2 中断与抢占：
        - 支持用户或高级Agent发送中断信号，暂停当前执行流，插入高优先级指令后恢复现场。

10. 测试与质量保证
    10.1 基准测试集（Strat-Bench）：
         - 构建战略分析专用基准测试集Strat-Bench V1.0，初期包含20个经专家标注的战略决策场景（覆盖17个工具的核心应用），每个场景包含输入数据、标准输出Schema、专家评分标准。
         - 后续逐步扩展至100+场景，作为工具箱版本迭代的准入测试。
    10.2 A/B测试框架：
         - 支持同一工具体多版本（Prompt模板、模型型号、DAG流程）并行运行，自动收集任务完成率、数值计算精度、幻觉率、Token成本等指标，择优晋级。
    10.3 健康度仪表盘：
         - 实时监控各工具体调用成功率、平均执行时长、用户修正频率、算力成本效益比。
         - 当某工具体用户修正频率超过阈值（默认15%），自动触发Skill Review流程，提示工程师介入优化或淘汰。

11. 性能监控与漂移检测
    11.1 定期评估工具检索准确率与生成质量，建立性能漂移检测机制，对比历史基线。
    11.2 所有评估结果可视化输出，纳入系统行为日志。

（注：本需求特性完全采纳原系统对17个战略工具的覆盖要求，并在此基础上增强了确定性、可解释性、安全性与演进能力。）    
}

---------------------
Q1
{
二、工具箱：构建基于契约化设计与方法论驱动的战略分析引擎，包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型共17个规划工具。所有工具必须严格遵循BLM/BEM方法论框架，输出具备可审计性与决策可靠性。

    1.输入契约要求：工具必须接收经系统处理的结构化JSON数据，数据包含文档摘要与关键指标。工具执行前必须验证输入符合预定义Schema，不符合Schema的请求立即终止并返回错误码。

    2.输出契约要求：工具输出必须为严格结构化的JSON，包含三部分：(1)分析结论数据；(2)方法论遵循声明（明确标注所用分析框架及维度完整性）；(3)数据支撑强度标识（高/中/低）及对应的引文索引。禁止输出自由文本或未结构化的自然语言描述。

    3.数值计算保障机制：
        (1)严禁LLM直接执行数值计算、统计分析或趋势预测。
        (2)所有量化分析必须通过以下路径之一完成：①调用预置函数库（覆盖基础统计场景）；②生成Python代码在隔离沙箱中执行，且必须通过三重验证（单元测试校验、业务边界检查、专家规则校验）。
        (3)复杂优化问题（如资源分配）仅允许使用预定义数学模板，LLM仅负责参数填充，禁止自主构建目标函数与约束条件。

    4.专业边界硬约束：
        (1)每个工具必须内置领域规则校验器，强制验证专业概念纯净性（如SWOT工具必须校验"优势/劣势"仅源自内部资源，"机会/威胁"仅源自外部环境）。
        (2)工具执行过程中若检测到概念混淆或逻辑矛盾（如基于下降营收得出扩张结论且无合理解释），必须中止执行并标记"方法论违规"。

    5.工具与BLM/BEM阶段的强制映射：
        (1)每个工具必须预定义适用的BLM/BEM阶段白名单（如PESTEL仅适用于BLM"市场洞察-看趋势"子阶段）。
        (2)工具输出必须携带阶段标识符，确保数据流严格遵循BLM/BEM阶段顺序，禁止跨阶段调用（如执行设计阶段不得调用市场洞察类工具）。
        (3)工具链编排必须符合BLM六阶段与BEM六阶段的逻辑依赖关系，系统自动校验阶段间数据传递的完整性。

    6.工具箱标准工作流：
        [1]输入验证：校验JSON输入Schema合法性及阶段标识符有效性。
        [2]维度化检索：根据工具预定义的检索维度白名单（如波特五力仅检索竞争相关文档），从向量库召回相关数据切片。
        [3]分析执行：LLM基于召回数据生成推理链与计算代码，沙箱执行后返回结构化结果。
        [4]规则校验：通过领域规则引擎验证专业概念边界与逻辑一致性。
        [5]证据打包：生成包含方法论声明、数据支撑强度、引文索引的完整输出包。
        [6]阶段传递：将输出注入BLM/BEM下一阶段的输入Schema，自动触发阶段校验。

    7.用户反馈与修正机制：
        (1)用户反馈必须分类处理：①数据修正→更新向量库元数据；②工具输出修正→记录为专家修正样本，仅用于后续提示词优化训练；③方法论质疑→触发人工审核流程。
        (2)禁止根据用户反馈自动修改工具逻辑或方法论定义，所有工具核心逻辑变更必须经人工审核发布。

    8.战略级可解释性要求：
        (1)每个工具输出必须包含可点击的决策树可视化路径，展示"原始数据→分析维度→结论"的完整推理链。
        (2)关键结论必须标注数据支撑强度（基于引文数量与权威性自动计算）及置信度依据。
        (3)支持从最终结论反向追溯至具体文档片段，确保所有战略判断具备可审计的数据源。

    9.安全与韧性保障：
        (1)实施三层防御：①输入层基础格式校验；②执行层逻辑校验器+数值边界检查；③输出层跨工具一致性仲裁（如BSC财务目标与现金流预测的逻辑互斥检测）。
        (2)高风险结论（如市场进入决策）自动触发人工审核Checkpoint，用户未介入时默认采用保守方案。
        (3)建立工具幻觉监控机制，当单工具连续3次输出与输入数据明显矛盾时，自动锁定该工具并告警。

    10.质量控制与演进原则：
        (1)优先保障分析质量（逻辑校验+专业约束），次要优化执行效率（代码缓存+检索优化）。
        (2)建立战略分析基准测试集（Strat-Bench），覆盖17个工具在BLM/BEM各阶段的标准场景，每次版本迭代必须通过全量测试。
        (3)暂缓非核心功能（如Token经济审计、工具自主进化），资源聚焦于降低决策风险的核心能力。
        (4)工具版本演进必须保持方法论稳定性，确保历史战略决策具备纵向可比性。
}

Q2
{
二、工具箱：构建基于"分层确定性架构"的战略计算引擎，包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型共17个规划工具

    1. 输入经系统处理结构化为JSON格式的数据，首先读取摘要构建全景视图，再按需挖掘细节；支持工具链有向无环图（DAG）编排，允许上游工具输出自动结构化流转为下游工具输入，形成自动化战略分析流水线

    2. 工具箱基本工作流：
        [1] 任务初始化：加载用户战略偏好与风险容忍度配置，实例化工具链执行上下文
        [2] 上下文构建：基于输入数据自动生成RAG检索策略，召回相关历史战略档案与市场数据切片
        [3] 工具链调度：依据DAG拓扑排序执行工具序列，支持并行调度无依赖子任务（如并行执行市场趋势与竞争格局分析）
        [4] 确定性执行：每个工具严格遵循Schema-First原则，输出必须通过Pydantic V2契约验证
        [5] 双路径计算：
            (1) 简单数值计算（聚合/加减乘除/统计描述）强制在持久化沙箱中执行Python代码
            (2) 复杂优化问题（线性规划/整数规划/蒙特卡洛模拟）由LLM构建数学模型后直连专业优化引擎求解
            (3) 所有计算结果必须通过预定义语义验证器校验（如财务指标边界约束、逻辑一致性检查）
        [6] 结果校验：AUD AGENT执行跨工具一致性检验，检测逻辑矛盾（如现金流评估在不同工具间冲突）
        [7] 证据打包：输出包含完整推理链、数据源引文索引、置信度评分的结构化证据包
        [8] 记忆持久化：关键战略洞察与用户修正自动存入战略档案库形成闭环演进

    3. 支持用户反馈与修正工具分析结果，系统自动区分数据层修正（如修正市场增长率数值）与逻辑层修正（如调整SWOT权重逻辑），分别触发输入更新与Few-Shot样本生成

    4. 严禁LLM直接进行数值计算与统计推断；强制通过双路径机制执行：
        (1) 简单计算必须生成Python代码在资源受限沙箱中执行
        (2) 复杂优化问题必须由LLM输出目标函数与约束条件后交由专业求解器处理
        (3) 所有计算输出必须通过领域特定语义验证规则（如利润率∈[0,1]、市场份额总和≤100%）

    5. 采用Pydantic V2配合Instructor Patch确保所有工具输出严格符合预定义OpenAPI Schema；建立CI/CD集成的契约测试流水线，Schema验证失败的任务立即终止并告警

    6. 工具提示词管理采用混合优化策略：
        (1) 基础层：基于行业特性与企业战略DNA的规则化模板动态注入（如激进型/保守型偏好适配）
        (2) 增强层：经人工审核的Few-Shot样本库持续优化提示词，避免噪声反馈污染
        (3) 演进层：当高质量修正样本积累超过500个时，可选启用轻量级强化学习优化任务分解逻辑

    7. 支持分层可解释性输出：
        (1) 执行层：全链路OpenTelemetry分布式Trace供技术团队审计计算路径
        (2) 业务层：关键决策因素与推理链的可视化图表（支持交互式ECharts展示敏感性分析）
        (3) 合规层：PDF格式审计报告包含可点击引文索引、数据源哈希值及置信度热力图

    8. 构建纵深防御安全体系：
        (1) 预执行：Prompt注入检测+Schema契约验证双重拦截
        (2) 执行中：沙箱CPU/内存配额限制+网络访问白名单（仅允许预设财经API）
        (3) 后执行：AUD AGENT一致性检验+决策置信度阈值拦截（默认0.6），低于阈值标记"数据不足"

    9. 实施三层记忆架构保障上下文连贯性：
        (1) 短期记忆：工具链共享的上下文黑板，支持跨工具关键变量与战略洞察传递
        (2) 中期记忆：战略档案库存储历年SP/BP关键假设变量、决策依据与实际执行结果的差值
        (3) 长期记忆：经审核的用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库

    10. 采用确定性核心+可插拔扩展架构：
        (1) 核心引擎固定为DAG调度器与Schema验证器，保障系统行为可预测性与审计追溯能力
        (2) 17个预置战略工具作为标准Skill包，覆盖BLM/BEM全阶段分析需求
        (3) 支持经人工审核后离线注册新Skill，禁止运行时动态生成工具以规避安全风险

    11. 建立A/B测试框架对比不同工具链配置效果，核心指标包括任务完成率、数值计算精度、幻觉率与Token成本效益比；建立Strat-Bench基准测试集（含100个专家标注场景）持续验证工具性能，当某工具用户修正频率超过15%阈值时自动触发Review流程    
}

Q3
{
二、工具箱：构建基于"代码即推理（Code-as-Reasoning）"与"神经符号人工智能"融合架构的战略分析引擎，包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型共17个规划工具

    1. 输入经系统处理结构化为JSON格式的数据，首先读取摘要构建全景视图，再按需挖掘细节数据切片；所有工具输出必须严格遵循Pydantic V2定义的Schema，通过Instructor Patch强制LLM输出结构化JSON，禁止自由文本输出

    2. 工具箱基本工作流：
        [1] 任务解析：根据用户指令识别目标工具及依赖工具链，自动生成有向无环图（DAG）定义工具执行顺序与数据流转关系（如PESTEL输出自动作为SWOT外部环境输入）
        [2] 角色校验：调用工具前强制校验当前AGENT角色是否在工具适用角色白名单内（如波特五力仅允许CEO/CMO/CTO调用），不匹配则终止执行并返回错误
        [3] 上下文构建：执行混合检索（元数据过滤+语义向量检索），召回相关文档切片并注入工具执行上下文
        [4] 代码生成与执行：LLM基于检索结果生成Python代码执行所有数值计算（含统计、回归、矩阵运算），代码在Docker沙箱中执行；针对线性规划/蒙特卡洛等运筹问题，支持通过CVXPY/OR-Tools优化器直连求解（需工具定义显式声明）
        [5] 逻辑校验：输出结果需通过双重校验——① Schema合法性校验 ② 战略逻辑规则引擎校验（如"利润率不得低于行业均值20%且无合理解释"）
        [6] 假设显式化：每个工具输出必须包含关键假设列表（如"假设市场年增长率5%"），并与战略档案库中历史假设执行偏差自动比对
        [7] 结构化输出：生成包含data（分析结果）、provenance（思维链溯源树）、assumptions（关键假设）、citations（引文索引）的完整JSON包

    3. 支持用户反馈与修正工具分析结果；系统建立双轨演进机制：
        [1] 参数轨：用户修正自动触发Prompt微调，基于DSPy BootstrapFewShot优化器迭代提示词参数
        [2] 方法论轨：当工具输出与实际执行结果偏差超过阈值，需战略专家人工审核后方可更新工具核心逻辑，禁止LLM自主修改分析框架

    4. 禁止LLM进行任何形式的数值计算与统计，强制LLM生成Python代码在Docker沙箱执行计算；沙箱实施网络白名单策略，仅允许访问预设财经API（如Bloomberg/Wind），默认断网保障数据合规

    5. 支持Pydantic V2配合Instructor Patch确保输出严格符合Schema，实施契约测试；增加战略逻辑规则引擎，在Schema校验后执行领域特定逻辑检查（如现金流一致性、增长率合理性）

    6. 支持工具提示词生成与管理，基于DSPy理念建立工具箱Prompt Optimizer；优化目标锚定三大战略维度：① 与BLM/BEM模型对齐度 ② 数据溯源完整性 ③ 决策可执行性；优化信号来源于用户修正日志、AUD AGENT一致性检验结果及SP/BP各阶段Checkpoint人工反馈

    7. 支持工具分析过程的可解释性可视化：
        [1] 输出完整思维链溯源树（DOT格式），支持从结论反向追溯至原始文档切片
        [2] 强制声明关键假设列表，并与历史执行结果自动比对生成偏差热力图
        [3] 高风险工具（如破坏性创新模型、GE-麦肯锡矩阵）首次调用需用户确认，后续调用记录至审计日志

    8. 建立A/B测试框架对比不同策略效果，基准测试集需包含三类场景：① 标准战略场景 ② 边缘场景（如市场突变） ③ 故障场景（如数据缺失）；核心评估指标包括任务完成率、数值计算精度、幻觉率、决策置信度及Token成本

    9. 工具链编排需预定义且经战略专家验证，禁止LLM动态生成未经验证的新分析工具；工具间数据流通过DAG自动编排，支持并行调度无依赖子任务（如供应商分析与客户分析并行执行）

    10. 实施全链路可观测性：遵循OpenTelemetry协议输出分布式Trace，强制记录prompt_tokens/completion_tokens及预估USD成本；设定工具级预算熔断阈值（默认50美元/任务），超阈值自动终止任务    
}

---------------------
Z1
{
二、工具箱：构建基于“神经符号人工智能”与“代码即推理”架构的深度战略计算引擎。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心组件。本系统具备双层状态机调度、确定性计算执行、混合语义记忆及全链路安全审计等关键特性。

    1. 智能工具体与双层调度架构
        [1]. 智能工具体封装：每个战略分析模型封装为独立智能体，拥有唯一任务生命周期与专属记忆空间。支持“工具链有向无环图”定义，允许将上游工具（如PESTEL）的输出结构化流转为下游工具（如SWOT）的输入，形成自动化战略分析流水线。
        [2]. 双层状态机与抢占式调度（强制）：
            (1) 外层Task Loop：负责任务拆解、DAG拓扑排序、进度持久化。支持“信号中断与抢占机制”，允许高级Agent或用户发送中断信号暂停当前工具执行，插入高优先级指令后恢复现场。
            (2) 内层Tool Loop：执行Think-Code-Observe原子循环，生成推理链→Python代码→沙箱执行→观察输出→回写记忆。
            (3) 架构约束：禁止单循环ReAct；外层不执行原子操作，内层不管理任务进度，确保复杂战略任务的逻辑严密性。
        [3]. 动态依赖任务树：
            (1) 工具体任务清单升级为“动态依赖树”，子任务项包含id、description、dependencies[]。
            (2) Task Loop解析依赖关系，自动并行调度无依赖的子任务（如并行执行竞对分析与市场趋势分析），提升分析效率。

    2. 确定性数值计算与优化引擎
        [1]. 分层计算架构（强制）：
            (1) 严禁LLM进行任何形式的数值计算、统计回归、矩阵运算或现金流量折现。
            (2) 战略运筹问题（如资源分配、投资组合优化）：系统自动识别并调用数值优化引擎（CVXPY、OR-Tools），由LLM负责构建目标函数与约束条件，求解器负责计算，确保数学上的全局最优。
            (3) 数据统计与处理：强制LLM生成Python代码在持久化沙箱中执行，确保计算过程的确定性与可验证性。
        [2]. 持久化沙箱环境：
            (1) 为每个任务会话实例化持久化的Jupyter Kernel，支持变量在不同工具间传递（如PESTEL分析出的'GDP增长率'变量可直接被'市场增长预测'代码调用），实现分析过程的上下文连贯。
            (2) 构建网络白名单网关，沙箱默认断网，仅允许通过代理访问系统预设的可信财经API，严禁通用网页爬虫以保障数据合规。

    3. 混合记忆与全链路溯源机制
        [1]. 混合检索外部记忆模块（强制）：
            (1) 实现Hybrid-Search Notepad记忆容器，支持KV键值读写与语义向量检索。接口扩展为：write/read/list/search(query, top_k)。
            (2) 工具体在缺乏精确Key时，通过语义检索获取相关财务数据或历史决策片段，解决跨任务信息孤岛。
            (3) 每个工具体实例加载独立Notepad，严格隔离命名空间，防止上下文污染。
        [2]. 结构化证据包与溯源：
            (1) 每次工具箱分析必须输出“结构化推理证据包”，包含：输入摘要Hash、子任务分解图、调用的Skill名称及版本、关键中间变量（如回归系数）、置信度评分及依据。
            (2) 支持决策路径回溯：用户可从最终分析结论反向点击，逐层查看依据的数据切片、计算代码及对标企业财报片段，确保战略决策可审计。

    4. 安全防御与经济审计体系
        [1]. Token经济审计与熔断（强制）：
            (1) 遵循OpenTelemetry协议输出分布式Trace，在Span中强制记录prompt_tokens、completion_tokens及预估USD成本。
            (2) 设定工具级“预算熔断阈值”，当单任务成本或重试次数超限时自动终止，防止逻辑死循环导致算力成本失控。
        [2]. 质量控制与基准测试：
            (1) 使用Pydantic V2配合Instructor Patch确保输出严格符合Schema，实施契约测试，失败任务立即终止。
            (2) 建立战略分析任务专用基准测试集Strat-Bench，包含经专家标注的战略决策场景。每次工具箱版本迭代必须全量跑通测试集，验证任务完成率与数值计算精度。
            (3) 建立“工具幻觉黑名单”，若某Skill连续生成违背输入数据的输出，自动锁定并报警。
}

Z2
{
二、工具箱：基于“神经符号人工智能”与“代码即推理”架构的深度战略计算引擎。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心组件。本系统具备Planner-Executor分层分工、DAG工具链编排、持久化计算上下文及运筹优化能力。

    1. 架构总纲与分工
        [1]. 采用Planner-Executor分层架构：
            (1). Planner层：负责接收战略任务，基于工具定义将复杂战略问题分解为有序的子任务序列，生成执行计划。
            (2). Executor层：严格依照计划执行具体的分析动作，严禁擅自修改执行逻辑。
        [2]. 构建DAG工具链编排系统：
            (1). 支持有向无环图（DAG）定义工具间的依赖关系，确保上游工具（如PESTEL）的结构化输出可直接作为下游工具（如SWOT）的输入。
            (2). 系统自动解析依赖关系，并行调度无依赖的子任务，提升分析效率。
        [3]. 实施严格的工具库管理：
            (1). 系统仅允许调用经审核注册的17个标准战略工具，严禁LLM在运行时动态生成新工具，确保分析模型的合规性与稳定性。

    2. 数值计算与执行环境
        [1]. 建立持久化沙箱环境：
            (1). 为每个战略规划会话实例化独立的持久化Jupyter Kernel，支持变量在不同工具执行步骤间传递与复用，确保分析上下文的连贯性。
        [2]. 强制“代码即推理”与优化器直连：
            (1). 严禁LLM直接输出数值计算结果或统计结论，强制生成Python代码执行计算。
            (2). 针对战略运筹问题（如资源分配、投资组合优化），支持直接调用CVXPY、OR-Tools等专业优化求解器，由LLM负责构建目标函数与约束条件，求解器负责计算。
        [3]. 构建网络白名单网关：
            (1). 沙箱默认断网，仅允许通过安全代理访问系统预设的可信财经数据API，保障数据合规。

    3. 战略工具模型定义
        [1]. 覆盖17个核心战略工具，每个工具封装为独立微服务模块，具备标准的输入Schema与输出Schema。
        [2]. 输入经系统处理结构化为JSON格式的数据，首先读取摘要构建全景视图，再基于DAG定义检索细节数据。
        [3]. 支持用户反馈与修正工具分析结果，修正日志存入战略档案库用于后续优化。

    4. 工具箱标准工作流
        [1]. [规划阶段] Planner解析任务 -> 生成子任务DAG图 -> 映射对应工具。
        [2]. [执行阶段] Executor加载持久化沙箱 -> 按拓扑序执行工具 -> 生成代码/调用优化器 -> 获取计算结果。
        [3]. [验证阶段] 运行语义一致性检查器，确保分析结论不违背基础事实数据。
        [4]. [输出阶段] 输出包含分析结论、可视化配置及引用源的结构化JSON。

    5. 提示词生成与优化管理
        [1]. 支持工具提示词生成与管理，根据具体工具模型定义生成适合LLM的提示词。
        [2]. 基于DSPy理念建立工具箱Prompt Optimizer，根据工具分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数。
        [3]. 建立A/B测试框架对比不同策略效果，并建立基准测试集验证工具性能。

    6. 质量控制与契约测试
        [1]. 支持Pydantic V2配合Instructor Patch确保输出严格符合Schema，实施契约测试。
        [2]. 建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量。
        [3]. 支持工具分析过程的可解释性可视化，输出关键决策因素和推理链。    
}

Z3
{
二、工具箱：基于网络调用LLM API驱动的战略工具箱功能，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型共17个规划工具
    1.输入经系统处理结构化为JSON格式的数据，采用Schema-First原则定义输入输出接口，首先读取摘要构建全景视图，然后再挖掘细节
    2.工具箱采用分层控制工作流：顶层基于状态机与DAG调度管理任务生命周期，确保SP/BP阶段流转合规；底层采用持久化沙箱环境执行计算，支持中间变量在不同工具间传递，确保分析上下文连贯
    3.支持用户反馈与修正工具分析结果
    4.禁止LLM进行数值计算与统计，强制LLM生成Python代码在Docker沙箱执行计算。针对复杂战略运筹问题（如资源分配、投资组合），支持直接调用数值优化引擎求解。沙箱环境实施网络白名单隔离与资源配额熔断机制
    5.支持Pydantic V2配合Instructor Patch确保输出严格符合Schema，实施契约测试
    6.支持工具提示词生成与管理，根据具体工具模型定义生成适合LLM的提示词，基于DSPy理念建立工具箱Prompt Optimizer，根据工具分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数
    7.支持审计级可解释性，输出结构化推理证据包，包含引用源切片、中间计算变量及决策逻辑树。支持从最终报告引文反向溯源至原始文档片段与代码执行日志
    8.建立A/B测试框架对比不同策略效果，并建立基准测试集验证工具性能    
}

---------------------
G1
{
二、工具箱：构建基于“神经符号人工智能（Neuro-symbolic AI）”与“代码即推理（Code-as-Reasoning）”架构的确定性深度战略计算引擎。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心微型智能体（Micro-Agents）。

    1.数据输入与Schema契约：输入经系统处理结构化为JSON格式的数据，严格遵循“Schema-First”原则。所有工具的输入/输出必须通过Pydantic V2定义严格的数据模型契约，配合Instructor Patch确保LLM输出严格符合Schema结构的JSON数据，严禁输出非结构化的自由文本。建立逻辑校验器（Logic Validator），在输出前对业务逻辑（如市场份额总和≤100%、利润=收入-成本）进行硬性约束检查。

    2.工具箱基本工作流：采用“规划-执行-审计（Planner-Executor-Auditor）”架构。
        [1]. 规划：首先读取数据摘要，由Planner构建任务依赖有向无环图（DAG），明确工具执行的拓扑顺序（如先执行PESTEL生成宏观变量，再流转至SWOT）。
        [2]. 上下文：实例化持久化的Jupyter Kernel（Stateful Sandbox）作为共享内存，支持变量在不同工具间零损耗传递，确保分析过程的上下文连贯。
        [3]. 执行：Executor按DAG顺序调度工具，生成推理链与代码并在沙箱中执行。
        [4]. 审计：Auditor对最终结果进行Schema校验与逻辑核查，生成结构化证据包。

    3.对抗性反馈与修正：支持用户对工具分析结果的反馈与修正。在关键决策节点（如破坏性创新投资决策）引入“红蓝对抗（Red-Teaming）”机制，实例化批判者Agent对当前结论进行反事实推理与压力测试，强制执行者Agent进行辩论与修正，最终输出包含辩论摘要的决策报告。

    4.分级计算策略：严禁LLM直接进行任何形式的数值计算与统计预测，强制采用分级计算策略。
        [1]. 描述性统计与可视化：由LLM生成Python代码在隔离沙箱中执行。
        [2]. 运筹优化与复杂模拟：针对资源配置、风险模拟等问题，强制调用专业求解器（如OR-Tools、CVXPY），由LLM负责构建数学模型与约束条件，求解器输出最优解。

    5.全链路审计与溯源：输出结果必须包含完整的“推理证据链（Reasoning Trace）”，记录从原始数据切片、代码执行逻辑、求解器参数到最终结论的完整映射路径。同时实施Token经济审计，记录每个步骤的算力成本。

    6.提示词工程与演进：基于DSPy理念建立工具箱Prompt Optimizer。不依赖手动微调，而是建立“修正日志-样本转换”闭环，将用户对分析结果的修正操作转化为Few-Shot样本，定期运行优化器自动迭代工具的提示词参数，使工具逐步适配企业的特定战略偏好（如激进或保守）。

    7.可解释性可视化：支持工具分析过程的白盒化展示。不仅输出最终图表，需可视化展示决策树、变量依赖关系图以及关键假设的敏感性分析（如汇率波动对利润的影响幅度）。

    8.测试与监控：建立战略分析基准测试集（Strat-Bench），包含标准场景与边缘场景（如极端市场环境）。实施A/B测试框架对比不同Prompt策略的效果，并建立契约测试流水线，防止模型输出结构漂移导致系统崩溃。
}

G2
{
二、工具箱：构建基于“神经符号人工智能（Neuro-symbolic AI）”与“代码即推理（Code-as-Reasoning）”架构的深度战略计算引擎。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心组件。每个组件均内嵌该领域的专家知识图谱与标准化图表生成能力。

    1. 输入经系统处理结构化为JSON格式的数据，引入Planner-Executor架构，首先由规划器（Planner）将复杂战略任务拆解为基于依赖关系的有向无环图（DAG）任务流，再调度执行器（Executor）并行或串行调用具体工具，实现全景视图构建与细节挖掘的自动化编排
    2. 工具箱标准工作流遵循“ReAct + Refine”闭环：[语义解析与主动检索] -> [推理规划] -> [双通道计算/求解] -> [沙箱执行与验证] -> [结果自反思与修正] -> [结构化输出]
    3. 支持用户反馈修正，并强制引入“红蓝军对抗（Red Teaming）”机制。在关键决策节点（如进入新市场决策），系统需自动生成“反面意见书”或“对抗性假设”，迫使分析结果经过辩证检验，防止算法偏见与群体迷思
    4. 严禁LLM直接进行数值计算与预测，建立“双通道计算堆栈”以确保精确性：
        (1) 统计分析通道：对于趋势外推、相关性分析、图表绘制，强制生成Python代码在持久化有状态沙箱（Stateful Sandbox）中执行
        (2) 运筹优化通道：对于资源分配、供应链规划等多目标优化问题，强制通过LLM建模并调用专业数学求解器（如OR-Tools、CVXPY）获取最优解
    5. 支持Pydantic V2配合Instructor Patch确保输出严格符合Schema，实施严格的契约测试（Contract Testing），确保工具间数据流转的字段类型与逻辑约束绝对一致
    6. 建立基于DSPy理念的提示词优化器与专家知识库。工具不仅加载Prompt，还需注入“专家知识图谱”（如波特竞争理论的严谨定义），根据工具执行的反馈日志（Code Execution Feedback）自动迭代优化提示词策略
    7. 实施“结构化推理证据包”与“全链路可观测性”。输出结果不限于文本结论，必须包含完整的推理轨迹（Trace）、引用的具体数据切片哈希值、中间计算变量及置信度评分，实现决策过程的完全透明与可审计
    8. 建立A/B测试框架与“战略基准测试集（Strat-Bench）”。支持对比不同Prompt策略或模型版本（如GPT-4o vs Claude 3.5）在同一战略场景下的表现，基于测试集定期评估工具的逻辑稳健性
    9. 构建“全局变量黑板（Global Variable Blackboard）”机制。支持不同工具间精确共享关键中间变量（如PESTEL计算的GDP增速直接被SWOT调用），而非模糊的文本传递，确保分析逻辑的上下文连贯性
    10. 建立异常熔断与自动修复机制。当工具执行代码报错或逻辑校验失败时，自动触发“自愈循环”，检索错误日志与知识库尝试修正代码，若重试3次仍失败则标记任务挂起并请求人工介入    
}

G3
{
二、工具箱：构建基于“确定性计算”与“神经符号推理”架构的深度战略计算引擎。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心组件。本模块需确保战略分析的逻辑闭环、数据一致性与可追溯性。

    1. 数据输入与依赖编排：输入经系统处理结构化为JSON格式的数据。支持基于有向无环图（DAG）的任务依赖编排，允许将上游工具（如PESTEL）的输出变量作为下游工具（如SWOT）的输入约束，形成自动化的战略分析流水线。
    2. 全局变量注册表：建立“战略变量注册表（Strategic Variable Registry）”机制。所有工具在分析过程中涉及的公共假设（如汇率、通胀率、市场增长率）必须从该注册表统一读取，确保不同工具间的分析前提一致，严禁出现逻辑互斥（如财务模型预测汇率上涨而出口策略假设汇率下跌）。
    3. 工具箱标准工作流：采用“Code-as-Reasoning”模式，执行[感知]读取注册表与输入数据 -> [规划]生成分析逻辑 -> [计算]编写Python代码并在持久化沙箱中执行 -> [校验]逻辑一致性审查 -> [反思]对抗性挑战 -> [输出]生成结构化结论。
    4. 分层计算协议：严禁LLM直接进行数值计算。基础统计分析强制LLM生成Python代码执行；涉及资源配置与运筹规划（如供应链优化、资金分配）的任务，强制通过接口调用专业求解器（如CVXPY、OR-Tools）进行数学建模求解，确保量化结果的精确性。
    5. 输出规范与契约测试：支持Pydantic V2配合Instructor Patch确保工具输出严格符合预定义的Schema，实施自动化契约测试。若输出结构不符合定义或逻辑校验失败，自动触发自我修正重试机制。
    6. 提示词工程与企业DNA注入：支持“动态企业画像注入”，在工具执行时动态加载企业的战略DNA配置（如风险偏好、创新激进程度、财务稳健性要求），确保分析风格适配企业特性。基于DSPy理念建立Prompt Optimizer，利用历史修正数据自动迭代优化工具的提示词参数。
    7. 全链路溯源与可视化：支持“证据链（Evidence Chain）”溯源功能。最终输出不仅包含结论，需以图谱形式展示“结论->计算逻辑/代码->原始数据切片”的完整推导路径，确保关键决策可审计、可解释。
    8. 对抗性质量控制：引入“红蓝军对抗（Red-Teaming）”机制。在工具生成最终结论前，强制实例化“蓝军”角色对当前结论进行批判性反思与漏洞挖掘，仅在通过置信度阈值（默认0.8）后方可输出，否则需重生成或标记风险提示。
    9. 安全沙箱环境：代码执行环境需建立网络白名单机制，默认断网，仅允许访问系统内部预置的可信财经API接口，严禁通用网络爬虫操作，保障数据合规与安全。
    10. 性能与实验框架：建立A/B测试框架，支持对不同版本的Prompt策略或分析逻辑进行并行效果对比。建立“算力成本审计”机制，记录每个工具执行过程中的Token消耗与计算资源成本。    
}

---------------------
O1
{
二、工具箱：基于“代码即推理（Code-as-Reasoning）”与“Schema-First”原则的企业级战略分析工具箱

工具箱是企业战略规划管理智库系统的核心分析执行层，负责在严格方法论与流程约束下，调用网络大模型 API 与受控计算环境，对结构化数据进行可验证、可溯源、可复用的战略分析。工具箱不追求自我进化或元系统复杂性，强调确定性、可解释性与工程可落地性。

工具箱内置17个标准战略规划工具，作为不可变的方法论资产统一管理，包括：PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估、破坏时间预测、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型。

----------------------------------------------------------------
1. 工具箱总体原则
----------------------------------------------------------------
1.1 工具箱以“工具即能力边界”为基本原则。每个战略工具对应一种明确的方法论模型，其分析范围、输入输出边界和约束规则均被显式定义，不支持工具自我生成或动态替换。

1.2 工具箱严格采用“Schema-First”设计。所有工具的输入与输出均需通过Pydantic V2定义的Schema校验，禁止输出自由文本结果，确保分析结果结构稳定、可组合、可复用。

1.3 工具箱禁止大模型直接进行任何数值计算、统计分析或优化求解。所有涉及数值计算的分析步骤，必须由大模型生成Python代码，在受控沙箱环境中执行，计算结果作为唯一可信结果回传。

1.4 工具箱服务于SP/BP强流程约束，所有工具调用必须显式声明其所属规划阶段、分析目标与角色视角，禁止跨阶段、跨视角泛化使用。

----------------------------------------------------------------
2. 工具定义与能力要求
----------------------------------------------------------------
2.1 每个战略工具作为独立分析单元，具备以下统一能力：
    - 读取系统提供的结构化JSON输入数据
    - 优先读取文档摘要与高层结构化变量，必要时再下钻调用RAG检索细节
    - 在分析过程中自动生成针对向量库的语义检索Query
    - 基于检索到的事实数据生成分析逻辑与计算代码
    - 输出符合Schema的结构化分析结果，并附带引用索引

2.2 工具内需内置该方法论的关键逻辑约束与校验规则，用于约束大模型推理方向。例如：
    - SWOT工具需强制区分内部因素与外部因素
    - BSC工具需覆盖财务、客户、内部流程、学习与成长四个维度
    - GE-麦肯锡矩阵需显式声明行业吸引力与竞争地位的评价依据

----------------------------------------------------------------
3. 工具箱标准工作流
----------------------------------------------------------------
3.1 标准执行流程如下：
    [Step 1] 语义解析：读取结构化输入数据与阶段上下文，识别分析目标与约束条件  
    [Step 2] 上下文构建：基于工具定义自动生成RAG检索策略，召回相关文档摘要与切片  
    [Step 3] 推理与代码生成：大模型基于事实数据生成分析逻辑与Python代码  
    [Step 4] 受控执行：在Docker沙箱中执行代码，捕获计算结果与异常信息  
    [Step 5] 结果校验：校验计算结果的数值合理性与Schema一致性  
    [Step 6] 结构化输出：输出分析结论、关键变量、置信度与引文索引的JSON结果  

3.2 工具执行过程中支持自动重试机制。若代码执行失败或结果为空，允许最多3次自动修正与重试，超过阈值则标记为“分析失败”，交由上层AGENT或用户处理。

----------------------------------------------------------------
4. 工具链组合与阶段协同
----------------------------------------------------------------
4.1 工具箱支持在SP/BP阶段内进行工具链组合。工具之间通过结构化Schema进行数据流转，不共享隐式上下文。

4.2 支持基于有向无环图（DAG）的工具调用顺序定义，用于明确上游工具输出对下游工具输入的依赖关系，但不引入抢占式调度或自主管理复杂度。

4.3 工具链执行必须服从SP/BP阶段Checkpoint机制。每一阶段工具链完成后，系统暂停并输出阶段性结构化结果，等待用户确认或修正后方可继续。

----------------------------------------------------------------
5. 可解释性与溯源要求
----------------------------------------------------------------
5.1 每个工具输出需包含以下可解释性信息：
    - 使用的数据摘要与文档引用索引
    - 关键判断依据与核心变量
    - 计算或评分逻辑说明
    - 分析结果置信度（0~1）

5.2 工具箱需支持从最终结论反向溯源至具体文档片段，确保分析结论具备审计与复盘能力。

----------------------------------------------------------------
6. 用户反馈与持续优化
----------------------------------------------------------------
6.1 支持用户对工具分析结果进行反馈与修正。修正内容以结构化形式记录，不直接覆盖原始输出。

6.2 用户修正日志将作为少样本样例，纳入工具提示词优化流程，用于后续分析质量提升，但不改变工具方法论本身。

----------------------------------------------------------------
7. 测试、监控与质量控制
----------------------------------------------------------------
7.1 建立工具级契约测试机制，确保模型或提示词更新不破坏既有Schema与逻辑约束。

7.2 支持A/B测试，对比不同模型或提示词版本在同一工具分析任务上的效果，保留高质量版本。

7.3 记录工具执行的关键性能指标，包括执行成功率、重试次数、计算耗时与Token消耗，用于持续监控与优化。

----------------------------------------------------------------
8. 安全与合规约束
----------------------------------------------------------------
8.1 所有工具执行的代码必须运行于受控沙箱环境，限制CPU、内存与网络访问。

8.2 工具箱需具备基础的提示词注入防御与异常检测能力，若检测到与分析目标无关的输出，立即终止执行并报警。

8.3 禁止工具箱在生产环境中引入未经验证的新分析模型或自生成工具，确保企业战略分析结果的稳定性与可控性。

}

O2
{
二、工具箱：基于“代码即推理（Code-as-Reasoning）”与“结构化契约驱动（Schema-First）”的企业战略分析工具箱，用于在战略规划（SP）与业务计划（BP）各阶段，按既定方法论与角色视角，稳定、可溯源地完成战略分析与决策支持。

工具箱由17个标准战略分析工具组成，包括：PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估、破坏时间预测、破坏性创新投资决策框架）、BSC、战略地图、KPI、变革管理模型。

工具箱仅负责“分析与计算”，不负责最终战略裁决，所有输出均为结构化结果，供AGENT与SP/BP流程调用。

1. 输入与数据使用规范
   1.1 工具箱统一接收由系统数据处理模块输出的结构化JSON数据作为输入。
   1.2 工具在执行时，必须优先读取文档摘要与元数据，构建全景认知后，再按需检索细粒度切片数据。
   1.3 工具具备RAG感知能力，可基于自身分析需求，自动生成检索查询，从向量库中召回相关文档片段。
   1.4 工具仅可访问被授权的数据范围，严禁跨越角色或阶段调用无关数据。

2. 工具执行与计算约束
   2.1 严禁LLM直接进行任何数值计算、统计分析或指标汇总。
   2.2 所有涉及加减乘除、权重评分、趋势预测、矩阵运算、情景模拟的分析，必须由LLM生成Python代码，在隔离的Docker沙箱中执行。
   2.3 沙箱执行需返回可验证的中间结果与最终结果，禁止仅输出结论性文本。
   2.4 若代码执行失败或结果为空，工具最多允许自动修正重试3次，超过次数则标记为“计算失败”。

3. Schema约束与输出规范
   3.1 所有工具的输入与输出必须严格遵循预定义的Pydantic V2 Schema。
   3.2 通过Instructor Patch或等效机制，强制LLM输出符合Schema的结构化JSON，禁止自由文本输出。
   3.3 工具输出需包含：
       - analysis_result：工具分析的结构化结果
       - citations：引用的文档切片索引
       - assumptions：关键假设条件
       - confidence_score：分析置信度（0~1）
   3.4 若Schema校验失败，工具输出视为无效，不进入下游流程。

4. 标准工具工作流
   4.1 语义解析：解析输入JSON，识别工具关注的关键变量与分析维度。
   4.2 RAG检索：基于工具定义生成检索策略，从向量库中召回相关事实数据。
   4.3 上下文构建：将召回数据与输入摘要整合为分析上下文。
   4.4 代码生成：LLM生成用于分析或计算的Python代码。
   4.5 沙箱执行：在隔离环境中执行代码，获取结构化结果。
   4.6 逻辑校验：执行基础一致性检查，防止明显违背事实或常识的结果。
   4.7 结构化输出：按Schema输出结果，并附带引用索引。

5. 工具组合与依赖支持
   5.1 支持工具之间的顺序依赖调用，上游工具输出可作为下游工具的结构化输入。
   5.2 对于SP/BP关键阶段，支持有限的工具链编排（如PESTEL→SWOT-TOWS→BSC），但不引入全局复杂调度机制。
   5.3 工具组合规则由战略规划流程显式指定，工具自身不得擅自扩展分析范围。

6. 用户反馈与修正机制
   6.1 用户可对工具输出的关键参数、权重或结论提出修正意见。
   6.2 修正不会直接覆盖原结果，而是作为增量反馈记录，用于后续提示词与工具行为优化。
   6.3 若用户修正导致关键假设变化，需重新触发相关工具的分析流程。

7. 可解释性与溯源要求
   7.1 工具输出必须保留完整的引文索引，确保所有结论可追溯至具体文档片段。
   7.2 支持展示“数据来源 → 计算逻辑 → 结论”的分析路径摘要，用于决策复核。
   7.3 不要求展示完整推理链，仅保留对业务决策有价值的关键推理节点。

8. 质量控制与评估
   8.1 建立工具级基准测试集，用于定期验证工具在典型战略场景下的稳定性与一致性。
   8.2 支持A/B测试不同工具提示词版本或模型配置，对比分析结果质量。
   8.3 当某工具的用户修正频率持续高于阈值（默认15%），标记该工具需要人工复核与优化。

9. 风险与资源控制
   9.1 记录每次工具调用的Token消耗、执行时间与计算资源使用情况。
   9.2 设置单工具调用的资源上限，防止异常输入导致算力失控。
   9.3 当检测到异常输出或明显不一致结果时，暂停该工具在当前任务中的进一步调用。

以上工具箱需求特性用于支撑企业战略规划管理智库系统在SP与BP全过程中的分析一致性、计算可靠性与决策可追溯性。
    
}

O3
{
二、工具箱：基于“代码即推理（Code-as-Reasoning）”理念构建的企业战略规划智能工具箱

工具箱是企业战略规划管理智库系统中的核心分析与推演能力模块，负责将经系统处理后的结构化数据，转化为可解释、可审计、可复用的战略分析结论。工具箱由网络调用的大模型 API 驱动，但在制度上严格约束其行为边界，确保战略结论的确定性、可溯源性与工程可落地性。

工具箱内置17个标准化战略规划工具，包括但不限于：PESTEL分析、波特五力分析、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估、破坏时间预测、投资决策框架）、BSC、战略地图、KPI以及变革管理模型。所有工具均遵循统一的输入输出契约和执行规范。

1. 输入与数据使用规范  
   1.1 工具箱仅接收系统预处理后的结构化数据，统一采用JSON格式作为输入。  
   1.2 每次工具执行必须优先读取对应文档摘要，构建全景认知，再根据分析需要检索并引用具体数据切片，禁止跳过摘要直接进入细节推断。  
   1.3 工具具备受控的RAG检索能力，仅可通过系统统一封装的检索接口访问向量库与元数据库，禁止工具自行定义或绕过检索策略。

2. 工具执行与工作流规范  
   2.1 工具箱采用标准化的“分析—计算—校验—输出”执行流程，每次工具调用均视为一个独立、可审计的分析任务。  
   2.2 工具分析过程中，禁止大模型直接进行任何形式的数值计算、统计分析或权重汇总；所有涉及数值的操作，必须由大模型生成Python代码，并在受控的Docker沙箱环境中执行后返回结果。  
   2.3 工具执行失败或结果为空时，系统允许最多3次自动修正与重试；超过重试次数仍失败的任务，需显式标记为“工具不可用”并终止执行，避免生成不可靠结论。  
   2.4 支持工具之间的依赖执行关系。当一个工具的输出被声明为下游工具的输入时，系统需确保其结构化结果完整、校验通过后方可流转。

3. Schema约束与一致性校验  
   3.1 所有工具的输入与输出均需基于Pydantic V2定义的Schema进行强校验，禁止输出自由文本结果。  
   3.2 工具输出需通过一致性校验机制，检测逻辑冲突或与基础事实明显不符的结论（如基于下降业绩给出无条件扩张建议），校验失败时需触发修正或人工介入。  
   3.3 工具输出结果须包含明确的数据来源索引，用于后续战略规划与报告生成中的引用溯源。

4. 可解释性与溯源要求  
   4.1 每次工具分析需输出结构化的分析依据说明，包括：关键输入变量、主要判断维度、计算或推演逻辑摘要及结论置信度。  
   4.2 工具箱需保留分析过程中的关键中间结果，以支持从最终战略结论反向追溯至具体数据片段和分析步骤。  
   4.3 工具输出需支持在后续SP/BP阶段中被引用、组合和复用，而非一次性文本结论。

5. 用户反馈与修正机制  
   5.1 系统需支持用户对工具分析结果进行反馈与修正，反馈内容以结构化增量数据形式注入系统。  
   5.2 用户修正不直接覆盖原始分析结果，而是作为对工具分析假设、权重或判断逻辑的校正样本，存入战略档案库，用于后续提示词与分析策略优化。  
   5.3 对高风险或高不确定性分析任务，工具执行过程中可在关键节点暂停，向用户展示阶段性结果并请求确认后再继续执行。

6. 工具性能与质量管理  
   6.1 系统需建立工具级性能监控机制，持续跟踪各工具的执行成功率、用户修正频率与结果稳定性。  
   6.2 支持基于A/B测试框架，对同一战略工具在不同提示词版本或分析参数配置下的效果进行对比评估，并保留表现更优的版本。  
   6.3 当某一工具的用户修正频率或一致性校验失败率超过设定阈值时，需标记该工具为“需复核”，并暂停其在关键战略阶段中的自动使用。

7. 安全与成本约束  
   7.1 工具箱需记录每次工具调用的模型使用情况与Token消耗，用于后续算力成本审计与优化。  
   7.2 所有工具执行环境默认断网，仅允许访问系统白名单内的合规数据接口，禁止任意外部网络请求。  
   7.3 禁止工具在运行期生成、注册或演化新的分析工具，确保工具箱能力边界稳定、可控、可审计。

工具箱作为战略规划系统中的“理性计算层”，其核心目标不是生成更多结论，而是为SP与BP各阶段提供结构化、可验证、可复用的分析支撑，确保企业战略决策建立在可追溯的事实与清晰的方法论之上。
}



D-D3 Q-D1/Z1/G1 G-G3 O-D1/D3

D1 D3 G1 G3 Z1




你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要结合[企业战略规划管理智库系统需求列表]系统需求，从下面需求清单中选择最优的[工具箱]需求特性。要求： 
1、结合系统需求为每个列表逐个评分（10分制），最后求总分
2、评分维度：
   - 架构先进性与技术趋势匹配度
   - 数值计算与沙箱执行
   - Schema强制与输出校验
   - 提示词管理与优化
   - 可解释性与决策溯源
   - 用户反馈与修正闭环
   - 测试与质量保证（A/B测试、基准测试）
   - 安全与防御机制
   - 可扩展性与可维护性
   - 运行效率与成本控制
   - 领域专业性与战略工具建模深度
   - 与系统其他模块的集成能力
3、以表格形式呈现

---------------------
企业战略规划管理智库系统需求列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：基于网络调用LLM API驱动的战略工具箱功能，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型共17个规划工具
    1.输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节
    2.工具箱基本工作流：[待设计]
    3.支持用户反馈与修正工具分析结果
    4.禁止LLM进行数值计算与统计，强制LLM生成Python代码在Docker沙箱执行计算
    5.支持Pydantic V2配合Instructor Patch确保输出严格符合Schema，实施契约测试
    6.支持工具提示词生成与管理，根据具体工具模型定义生成适合LLM的提示词，基于DSPy理念建立工具箱Prompt Optimizer，根据工具分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数
    7.支持工具分析过程的可解释性可视化，输出关键决策因素和推理链
    8.建立A/B测试框架对比不同策略效果，并建立基准测试集验证工具性能

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节
    2.AGENT主要执行按角色定义自主分析数据、战略规划各阶段协作分析决策两大类任务，这两大类任务包括多种不同子类
    3.AGENT基本工作流：[感知]配置环境与上下文->管理会话生命周期->[思考]LLM推理->[执行]解析LLM输出决策任务完成或调用工具->[反射]回写工具输出到上下文并决策是否继续循环[思考]->构建输出并释放资源
    4.支持用户反馈与修正AGENT分析结果
    5.支持AGENT提示词生成与管理，按角色定义和分析框架生成适合LLM使用的提示词，基于DSPy理念建立AGENT Prompt Optimizer，根据AGENT分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数
    6.AGENT可按任务需求从工具箱中选择合适工具完成数据分析或战略规划任务
    7.支持企业利益最优的AGENT间协作，高级代理CEO AGENT拥有协作仲裁最终决定权，但用户可以观测和更改该决策
    8.支持视角隔离，AGENT仅从各自角色视角（如CFO仅关注资金风险）调用工具，严禁泛化讨论
    9.CEO AGENT内置"裁决状态机"作为仲裁器，设定最大辩论轮次，随着轮次增加，降低AGENT的Temperature Decay强制要求AGENT提出具体修改意见，而不是单纯否定，若未达成一致，生成Plan A（保守版）和Plan B（激进版）两套方案，CEO AGENT按偏好矩阵决策Plan A/B，同时暂停10秒提供用户介入裁决窗口，超时用户无动作则以 CEO AGENT 决策为准
    10.AUD AGENT采用"旁路监听+异步阻断"模式，不参与生成，只负责原文与生成内容的一致性检验，发现幻觉通知相关AGENT纠错，若3次重试失败，暂停任务人工纠错，之后从任务暂停点继续任务
    11.支持上下文压缩器与"公共黑板"机制，在多轮对话间仅传递关键结论而非全量历史，严格利用LangGraph的StateSnapshot机制配合Redis存储来管理公共黑板的更新与回溯
    12.支持Agent推理过程的可解释性可视化，包括决策依据、关键因素和置信度
    13.采用"思维链溯源树ToT"机制，只在SP/BP各阶段结束时启用ToT功能，当决策置信度低于阈值（默认0.6）触发ToT，通过回溯推理路径解决决策冲突，提高决策可解释性
    14.建立Agent性能监控与行为分析框架

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据

---------------------
D1
{
二、工具箱：构建基于“持久化计算内核”与“决策优化引擎”双擎驱动的战略推理与审计系统。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心组件。本系统遵循“确定性优先、可审计贯穿、成本可控”的设计原则，对战略分析全生命周期进行标准化管控。

1. 战略工具静态注册与持久化沙箱执行
   [1]. 17个战略工具均采用“静态注册制”，工具定义、输入输出Schema、默认分析模板固化于系统内核，禁止运行时动态生成新工具。系统预留技能生成实验开关（默认关闭），确保生产环境行为绝对稳定。
   [2]. 每个工具实例绑定独立的持久化Docker沙箱环境（基于Jupyter Kernel Gateway），支持跨工具、跨步骤的变量持久化存储。上游工具输出的结构化数据（如PESTEL的趋势变量）可直接被下游工具（如SWOT）通过内存态变量名引用，实现分析上下文无缝衔接。
   [3]. 沙箱内置标准科学计算库（NumPy、Pandas、SciPy）及运筹优化库（CVXPY、OR-Tools），严禁LLM直接输出数值统计结果；所有涉及算术运算、回归分析、矩阵计算、规划求解的任务，LLM仅负责生成Python代码，代码在沙箱内隔离执行，捕获stdout/stderr并返回执行结果。

2. 决策优化引擎：运筹问题直连求解器
   [1]. 战略规划中常见的资源分配、投资组合、产能规划等优化类问题，禁止LLM编写蒙特卡洛或梯度下降等通用求解代码。系统内置运筹优化求解器工厂，支持线性规划、整数规划、二次规划、多目标规划。
   [2]. LLM通过结构化提示词将业务约束（如预算上限、市场份额目标、风险容忍度）转换为JSON格式的数学模型（目标函数类型+变量边界+约束表达式），系统自动调用对应求解器计算最优解，并将解集反填至工具输出Schema。
   [3]. 当输入约束无法被求解器识别或产生不可行解时，系统返回明确错误类型，LLM依据错误信息修正约束表述，最大重试3次，失败则标记“当前约束下无可行方案”。

3. Planner-Executor 分工执行架构
   [1]. 工具箱执行流程强制拆分为规划层与执行层，使用不同量级模型分工协作：
       (1) 规划层（Planner）：调用经济型模型（如GPT-4o mini），负责接收用户任务指令及数据全景摘要，将复杂战略分析任务分解为有向无环图（DAG）形式的子任务序列，并为每个子节点指定所需工具及输入数据依赖。输出机器可读的任务编排清单。
       (2) 执行层（Executor）：调用高性能模型（如Claude Sonnet/GPT-4o），严格遵循Planner输出的编排清单，顺序或并行调用对应工具，执行代码生成、沙箱交互、结果解析等原子操作。禁止Executor跨任务步自主规划或跳过清单步骤。
       (3) 审计层（Auditor）：轻量级规则引擎或专用小模型，对Executor每次输出的JSON进行实时Schema校验（Pydantic V2）、数据类型检查、业务规则验证（如利润率不得为负），校验失败立即触发重试或回退至Planner重新规划。
   [2]. 提示词版本管理：不再使用复杂元学习框架，转而采用“快照式版本管理”。每个工具的System Prompt、用户Few-shot示例均以文件形式纳入Git LFS管理，版本与系统发布号绑定。当AUD AGENT或SP/BP阶段Checkpoint人工反馈提示词效果下降时，管理员可一键回滚至历史稳定版本。

4. 结构化证据包与可审计溯源
   [1]. 每一次工具执行（包括单工具独立运行或工具链串联）必须生成完整的“战略推理证据包”（Evidence Package），以Parquet列式格式持久化存储。证据包包含以下不可篡改字段：
       - 本次执行唯一ID及输入数据的SHA-256哈希值
       - Planner生成的任务分解DAG（节点、边、依赖关系）
       - 每个子任务调用工具的版本号、执行时间戳
       - Executor生成的原始代码及沙箱执行日志
       - 关键中间变量（如回归系数、聚类中心、敏感度测试值）
       - 置信度评分及置信区间（若适用）
       - 最终结构化输出
   [2]. 系统前端必须提供“决策路径回溯”可视化接口：用户可从最终分析结论（如“建议进入华东市场”）逐层下钻，查看支撑该结论的具体数据切片、计算逻辑、引用报告片段及原始代码行。回溯过程需支持Mermaid流程图及源代码语法高亮展示。

5. 成本熔断与契约测试质量门禁
   [1]. 契约测试强制嵌入CI/CD流水线：所有工具输出Schema必须通过Pydantic V2 + Instructor Patch的严格校验，任何字段名称、类型、嵌套结构的非向后兼容变更均触发构建失败。运行时输出若三次校验失败，则该工具进入熔断状态并通知管理员。
   [2]. 建立三级成本熔断机制：
       - 任务级熔断：单次战略分析任务（含多工具串联）累计消耗tokens超过预设阈值（默认5万tokens）或预估成本超过$0.5，强制终止执行并返回“成本超限”错误。
       - 会话级熔断：同一用户会话周期内总消耗tokens超过日配额，暂停工具箱服务直至配额重置。
       - 重试熔断：代码执行连续重试3次均失败（语法错误、求解器无解、网络异常），直接标记工具不可用并记录失败模式至性能看板。
   [3]. 建立战略分析专用基准测试集Strat-Bench V1.0，包含30个经专家标注的战略决策场景（覆盖17个工具典型使用路径），每次工具箱核心组件升级必须全量通过Strat-Bench，生成任务完成率、数值计算误差率、平均token消耗等指标，未通过则阻断发布。

6. 标准化工具箱工作流
   [1]. 工具箱执行生命周期严格遵循以下六阶段流程：
       (1) 感知与摘要：系统读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
       (2) 规划（Planner）：依据用户自然语言指令（如“请使用PESTEL和SWOT分析新能源业务进入风险”）及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、工具映射、数据依赖边。
       (3) 执行与验证（Executor + Auditor）：按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
       (4) 证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
       (5) 反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
       (6) 演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。

7. 性能监控与健康度仪表盘
   [1]. 实时监控每个工具的调用次数、成功率、平均执行时长、平均token消耗、用户修正频率。当某工具用户修正频率超过15%或连续失败率超过10%，自动在仪表盘标记“需人工复审”，并生成修正日志报告。
   [2]. 定期对Strat-Bench进行全量回归测试，绘制各工具版本性能漂移曲线。检测到核心指标（如任务完成率）周环比下降超过5%，触发告警并建议回滚至上一稳定版本。

8. 防御性编程与边界约束
   [1]. 沙箱环境默认断网，严格禁止代码访问外部API或文件系统；若工具定义需获取外部宏观数据（如汇率、行业增速），须通过系统统一的、带流量审计的白名单网关，且每次请求需经用户显式授权。
   [2]. 所有模型调用（Planner、Executor）必须显式声明模型名称及temperature值（强制0~0.3区间），禁止使用服务端默认隐式配置，确保每次执行结果可复现。
   [3]. 工具输出必须附带`confidence`字段（0-1浮点数），当置信度低于0.6时，工具箱自动在最终结论中添加“建议人工复核”标签，并高亮低置信度推理节点。
}

D3
{
二、工具箱：构建基于“元系统（Meta-System）架构”与“确定性计算引擎”融合的战略决策智能体集群。工具箱由17个核心战略模型构成，每个模型均封装为具备独立生命周期、领域知识图谱与契约化接口的智能工具体（Smart Tool Entity）。系统内置技能仓库（Skill Store），支持工具版本管理与渐进演化，并遵循以下核心设计原则与功能需求。

1. 架构总纲与智能工具体定义
   1.1 智能工具体（Smart Tool Entity）：
        - 每个战略分析模型（PESTEL、波特五力、SWOT-TOWS、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型、BSC、战略地图、KPI、变革管理模型等17个）均封装为独立工具体。
        - 工具体拥有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。
        - 工具体初始化时加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
   1.2 技能仓库（Skill Store）：
        - 所有工具体统一注册至中央技能仓库，支持版本控制、灰度发布与回滚。
        - 每个工具体必须通过Pydantic V2契约测试方可入库；后续版本迭代需通过回归测试集。
        - 远期支持基于代码生成模型的技能动态生成，初期以预定义模板为主，预留扩展接口。
   1.3 工具链编排与依赖调度：
        - 支持工具链有向无环图（Tool-Chain DAG）定义，允许上游工具的输出结构化流转为下游工具的输入。
        - 系统内置任务调度器，依据依赖关系自动并行执行无依赖子任务，提升分析效率。
        - 所有子任务状态均为“完成”方可交付最终结果。

2. 数值计算与沙箱执行
   2.1 强制代码执行隔离：
        - 严禁大语言模型直接进行任何数值计算、统计回归、矩阵运算、现金流量折现等量化操作。
        - 所有量化分析必须通过以下二选一路径执行：
            a) Python代码生成 + 沙箱执行；
            b) 优化器直连：针对线性规划、整数规划、蒙特卡洛模拟等战略运筹问题，由LLM将自然语言目标函数与约束条件转化为数学建模，调用CVXPY、OR-Tools、SciPy等优化器求解。
   2.2 持久化计算沙箱（会话级）：
        - 为每个任务会话实例化持久化Jupyter Kernel或同等状态化执行环境。
        - 支持跨工具、跨步骤的变量传递（如PESTEL计算的GDP增长率变量可直接被后续市场预测代码调用），实现分析上下文连贯。
        - 任务结束后立即销毁内核，并设置空闲超时回收策略，防止资源泄漏。
   2.3 Validation Feedback闭环：
        - 代码执行若报错，系统捕获STDERR并自动检索相似错误案例库与官方文档片段，辅助LLM在下一轮生成修复版本。
        - 设定最大重试次数（默认3次），失败则标记“当前工具不可行”并记录失败模式至技能演进日志。

3. Schema强制与一致性校验
   3.1 契约化输出：
        - 所有工具输入/输出严格遵循Pydantic V2定义的Data Model，并使用Instructor Patch或等价技术强制LLM输出符合Schema的结构化JSON，严禁自由文本。
   3.2 一致性校验仲裁器：
        - 在多工具协作场景下，终态输出需通过“一致性校验仲裁器”检测逻辑冲突（如现金流评估在不同工具间矛盾、利润率符号异常等）。
        - 仲裁器内置规则引擎及财务指标常识库，输出冲突检测报告并标记高置信度矛盾项，供用户或高级Agent裁决。
   3.3 契约测试自动化：
        - 在CI/CD流水线中集成针对工具Schema的自动化契约测试，防止模型漂移或版本迭代导致输出结构崩溃。
        - 契约测试失败的任务立即终止，不进入执行阶段。

4. 可解释性与决策溯源
   4.1 结构化推理证据包：
        - 每次工具分析必须输出结构化证据包，包含但不限于：
            - 输入数据摘要哈希值；
            - Planner制定的子任务分解图；
            - 每个子任务调用的Skill名称及版本；
            - 执行过程中的关键中间变量（如回归系数、聚类中心、敏感性参数）；
            - 置信度评分及依据；
            - 引用源（文档切片ID）。
   4.2 交互式溯源树：
        - 前端支持从最终分析结论反向点击，逐层展开证据包，展示数据来源→计算逻辑→中间结论→最终结论的完整推理链。
        - 自动生成关键决策因素的敏感性分析图表（如价格弹性、汇率波动对策略的影响）。
   4.3 红蓝辩论机制：
        - 针对高不确定性战略议题（如破坏性创新投资决策、新市场进入时机），系统可自动启动“红蓝辩论”模式。
        - 两个子智能体分别扮演激进派与保守派，基于相同数据集展开结构化辩论，记录双方论据强度与数据引用。
        - 最终输出共识区域与分歧区域，不强行折中，为管理层提供风险全景视图。

5. 用户反馈与闭环演进
   5.1 Checkpoint人工介入验证：
        - 在工具执行过程中，若识别到高风险决策节点（如基于小样本的市场容量预测、未经验证的关键假设），自动暂停并请求用户验证中间假设。
        - 用户可修改假设参数、调整权重或直接反馈修正指令，系统从暂停点恢复执行，并完整记录“专家修正轨迹”。
   5.2 修正轨迹库与逆向传播：
        - 将每次用户修正的快照（输入、原始输出、修正后输出、修正理由）匿名化存入修正轨迹库。
        - 系统定期对修正轨迹进行差异分析，推断若想得到用户修正后的输出，输入权重或任务分解策略应如何调整。
        - 高质量修正样本优先用于优化Planner的任务分解模板，实现元系统自我进化。
   5.3 用户反馈协议：
        - 用户反馈以增量数据或修正指令形式注入，强制触发工具体“反思阶段”（ReflAct），生成结构化修正日志并回传至技能演进日志。

6. 安全与防御机制
   6.1 沙箱网络隔离：
        - 所有代码执行沙箱默认断网；确需获取实时财经数据时，仅允许通过系统预设的网络白名单网关访问可信API（如Bloomberg、Wind），严禁通用网页爬虫。
   6.2 提示词注入防御：
        - 输入数据进入工具前执行清洗与注入检测，识别并剥离对抗性指令。
        - 将“提示词注入防御测试”纳入基准测试集，定期验证工具体对原始任务的忠诚度与抗干扰能力。
   6.3 工具幻觉黑名单：
        - 建立“工具幻觉黑名单”，若某工具体连续3次生成与输入数据明显矛盾的输出（如基于下降营收得出激进扩张结论且无合理解释），自动锁定该工具体并通知系统管理员介入审查。

7. 领域专业知识嵌入
   7.1 领域事实库：
        - 每个战略工具体内置领域事实库，以JSON Schema或规则集形式存储经典管理理论的强制约束与关键指标。
        - 示例：PESTEL工具体需包含政治、经济、社会、技术、环境、法律各维度的典型指标列表；SWOT工具体必须遵守“优势/劣势属内部，机会/威胁属外部”的交叉校验规则。
   7.2 验证器强制遵守：
        - 工具体输出前必须经过领域验证器，确保分析结论不违背基础理论框架（如四象限归因正确、波特五力各维度评分范围合规）。

8. 服务契约与集成
   8.1 独立微服务封装：
        - 工具箱整体作为独立微服务部署，对外暴露REST/gRPC接口。
        - 接口定义遵循OpenAPI 3.1规范，输入为系统处理后的结构化JSON数据，输出为分析结果、结构化证据包、成本审计信息的三元组。
   8.2 与Agent模块集成：
        - 与CEO、CFO等6个核心AGENT通过服务调用集成，AGENT按需请求工具箱执行特定分析任务。
        - 工具箱无状态设计，支持水平扩展，调用链通过OpenTelemetry输出分布式Trace。

9. 工具箱标准工作流
   9.1 工作流阶段：
        [阶段1] 环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具体价值观模板（SOUL.md），实例化会话级沙箱与Hybrid Notepad（混合检索记忆容器）。
        [阶段2] 规划（Planner）：Planner LLM接收任务请求与数据摘要，将战略分析任务分解为子任务序列，并为每个子任务匹配合适的工具体（含版本），输出Markdown格式任务分解图。
        [阶段3] 执行（Executor）：Executor LLM严格依照计划顺序调用工具体。每个工具体执行遵循Think→Code→Observe→Write Memory原子循环；数值计算走沙箱/优化器路径，文本归纳由LLM完成。
        [阶段4] 验证与仲裁：每步输出经领域验证器与契约校验；最终多工具输出经一致性仲裁器检测逻辑冲突。
        [阶段5] 证据打包：系统自动汇集所有中间产物，生成结构化证据包。
        [阶段6] 成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
   9.2 中断与抢占：
        - 支持用户或高级Agent发送中断信号，暂停当前执行流，插入高优先级指令后恢复现场。

10. 测试与质量保证
    10.1 基准测试集（Strat-Bench）：
         - 构建战略分析专用基准测试集Strat-Bench V1.0，初期包含20个经专家标注的战略决策场景（覆盖17个工具的核心应用），每个场景包含输入数据、标准输出Schema、专家评分标准。
         - 后续逐步扩展至100+场景，作为工具箱版本迭代的准入测试。
    10.2 A/B测试框架：
         - 支持同一工具体多版本（Prompt模板、模型型号、DAG流程）并行运行，自动收集任务完成率、数值计算精度、幻觉率、Token成本等指标，择优晋级。
    10.3 健康度仪表盘：
         - 实时监控各工具体调用成功率、平均执行时长、用户修正频率、算力成本效益比。
         - 当某工具体用户修正频率超过阈值（默认15%），自动触发Skill Review流程，提示工程师介入优化或淘汰。

11. 性能监控与漂移检测
    11.1 定期评估工具检索准确率与生成质量，建立性能漂移检测机制，对比历史基线。
    11.2 所有评估结果可视化输出，纳入系统行为日志。

（注：本需求特性完全采纳原系统对17个战略工具的覆盖要求，并在此基础上增强了确定性、可解释性、安全性与演进能力。）    
}

G1
{
二、工具箱：构建基于“神经符号人工智能（Neuro-symbolic AI）”与“代码即推理（Code-as-Reasoning）”架构的确定性深度战略计算引擎。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心微型智能体（Micro-Agents）。

    1.数据输入与Schema契约：输入经系统处理结构化为JSON格式的数据，严格遵循“Schema-First”原则。所有工具的输入/输出必须通过Pydantic V2定义严格的数据模型契约，配合Instructor Patch确保LLM输出严格符合Schema结构的JSON数据，严禁输出非结构化的自由文本。建立逻辑校验器（Logic Validator），在输出前对业务逻辑（如市场份额总和≤100%、利润=收入-成本）进行硬性约束检查。

    2.工具箱基本工作流：采用“规划-执行-审计（Planner-Executor-Auditor）”架构。
        [1]. 规划：首先读取数据摘要，由Planner构建任务依赖有向无环图（DAG），明确工具执行的拓扑顺序（如先执行PESTEL生成宏观变量，再流转至SWOT）。
        [2]. 上下文：实例化持久化的Jupyter Kernel（Stateful Sandbox）作为共享内存，支持变量在不同工具间零损耗传递，确保分析过程的上下文连贯。
        [3]. 执行：Executor按DAG顺序调度工具，生成推理链与代码并在沙箱中执行。
        [4]. 审计：Auditor对最终结果进行Schema校验与逻辑核查，生成结构化证据包。

    3.对抗性反馈与修正：支持用户对工具分析结果的反馈与修正。在关键决策节点（如破坏性创新投资决策）引入“红蓝对抗（Red-Teaming）”机制，实例化批判者Agent对当前结论进行反事实推理与压力测试，强制执行者Agent进行辩论与修正，最终输出包含辩论摘要的决策报告。

    4.分级计算策略：严禁LLM直接进行任何形式的数值计算与统计预测，强制采用分级计算策略。
        [1]. 描述性统计与可视化：由LLM生成Python代码在隔离沙箱中执行。
        [2]. 运筹优化与复杂模拟：针对资源配置、风险模拟等问题，强制调用专业求解器（如OR-Tools、CVXPY），由LLM负责构建数学模型与约束条件，求解器输出最优解。

    5.全链路审计与溯源：输出结果必须包含完整的“推理证据链（Reasoning Trace）”，记录从原始数据切片、代码执行逻辑、求解器参数到最终结论的完整映射路径。同时实施Token经济审计，记录每个步骤的算力成本。

    6.提示词工程与演进：基于DSPy理念建立工具箱Prompt Optimizer。不依赖手动微调，而是建立“修正日志-样本转换”闭环，将用户对分析结果的修正操作转化为Few-Shot样本，定期运行优化器自动迭代工具的提示词参数，使工具逐步适配企业的特定战略偏好（如激进或保守）。

    7.可解释性可视化：支持工具分析过程的白盒化展示。不仅输出最终图表，需可视化展示决策树、变量依赖关系图以及关键假设的敏感性分析（如汇率波动对利润的影响幅度）。

    8.测试与监控：建立战略分析基准测试集（Strat-Bench），包含标准场景与边缘场景（如极端市场环境）。实施A/B测试框架对比不同Prompt策略的效果，并建立契约测试流水线，防止模型输出结构漂移导致系统崩溃。
}

G3
{
二、工具箱：构建基于“确定性计算”与“神经符号推理”架构的深度战略计算引擎。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心组件。本模块需确保战略分析的逻辑闭环、数据一致性与可追溯性。

    1. 数据输入与依赖编排：输入经系统处理结构化为JSON格式的数据。支持基于有向无环图（DAG）的任务依赖编排，允许将上游工具（如PESTEL）的输出变量作为下游工具（如SWOT）的输入约束，形成自动化的战略分析流水线。
    2. 全局变量注册表：建立“战略变量注册表（Strategic Variable Registry）”机制。所有工具在分析过程中涉及的公共假设（如汇率、通胀率、市场增长率）必须从该注册表统一读取，确保不同工具间的分析前提一致，严禁出现逻辑互斥（如财务模型预测汇率上涨而出口策略假设汇率下跌）。
    3. 工具箱标准工作流：采用“Code-as-Reasoning”模式，执行[感知]读取注册表与输入数据 -> [规划]生成分析逻辑 -> [计算]编写Python代码并在持久化沙箱中执行 -> [校验]逻辑一致性审查 -> [反思]对抗性挑战 -> [输出]生成结构化结论。
    4. 分层计算协议：严禁LLM直接进行数值计算。基础统计分析强制LLM生成Python代码执行；涉及资源配置与运筹规划（如供应链优化、资金分配）的任务，强制通过接口调用专业求解器（如CVXPY、OR-Tools）进行数学建模求解，确保量化结果的精确性。
    5. 输出规范与契约测试：支持Pydantic V2配合Instructor Patch确保工具输出严格符合预定义的Schema，实施自动化契约测试。若输出结构不符合定义或逻辑校验失败，自动触发自我修正重试机制。
    6. 提示词工程与企业DNA注入：支持“动态企业画像注入”，在工具执行时动态加载企业的战略DNA配置（如风险偏好、创新激进程度、财务稳健性要求），确保分析风格适配企业特性。基于DSPy理念建立Prompt Optimizer，利用历史修正数据自动迭代优化工具的提示词参数。
    7. 全链路溯源与可视化：支持“证据链（Evidence Chain）”溯源功能。最终输出不仅包含结论，需以图谱形式展示“结论->计算逻辑/代码->原始数据切片”的完整推导路径，确保关键决策可审计、可解释。
    8. 对抗性质量控制：引入“红蓝军对抗（Red-Teaming）”机制。在工具生成最终结论前，强制实例化“蓝军”角色对当前结论进行批判性反思与漏洞挖掘，仅在通过置信度阈值（默认0.8）后方可输出，否则需重生成或标记风险提示。
    9. 安全沙箱环境：代码执行环境需建立网络白名单机制，默认断网，仅允许访问系统内部预置的可信财经API接口，严禁通用网络爬虫操作，保障数据合规与安全。
    10. 性能与实验框架：建立A/B测试框架，支持对不同版本的Prompt策略或分析逻辑进行并行效果对比。建立“算力成本审计”机制，记录每个工具执行过程中的Token消耗与计算资源成本。    
}

Z1
{
二、工具箱：构建基于“神经符号人工智能”与“代码即推理”架构的深度战略计算引擎。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心组件。本系统具备双层状态机调度、确定性计算执行、混合语义记忆及全链路安全审计等关键特性。

    1. 智能工具体与双层调度架构
        [1]. 智能工具体封装：每个战略分析模型封装为独立智能体，拥有唯一任务生命周期与专属记忆空间。支持“工具链有向无环图”定义，允许将上游工具（如PESTEL）的输出结构化流转为下游工具（如SWOT）的输入，形成自动化战略分析流水线。
        [2]. 双层状态机与抢占式调度（强制）：
            (1) 外层Task Loop：负责任务拆解、DAG拓扑排序、进度持久化。支持“信号中断与抢占机制”，允许高级Agent或用户发送中断信号暂停当前工具执行，插入高优先级指令后恢复现场。
            (2) 内层Tool Loop：执行Think-Code-Observe原子循环，生成推理链→Python代码→沙箱执行→观察输出→回写记忆。
            (3) 架构约束：禁止单循环ReAct；外层不执行原子操作，内层不管理任务进度，确保复杂战略任务的逻辑严密性。
        [3]. 动态依赖任务树：
            (1) 工具体任务清单升级为“动态依赖树”，子任务项包含id、description、dependencies[]。
            (2) Task Loop解析依赖关系，自动并行调度无依赖的子任务（如并行执行竞对分析与市场趋势分析），提升分析效率。

    2. 确定性数值计算与优化引擎
        [1]. 分层计算架构（强制）：
            (1) 严禁LLM进行任何形式的数值计算、统计回归、矩阵运算或现金流量折现。
            (2) 战略运筹问题（如资源分配、投资组合优化）：系统自动识别并调用数值优化引擎（CVXPY、OR-Tools），由LLM负责构建目标函数与约束条件，求解器负责计算，确保数学上的全局最优。
            (3) 数据统计与处理：强制LLM生成Python代码在持久化沙箱中执行，确保计算过程的确定性与可验证性。
        [2]. 持久化沙箱环境：
            (1) 为每个任务会话实例化持久化的Jupyter Kernel，支持变量在不同工具间传递（如PESTEL分析出的'GDP增长率'变量可直接被'市场增长预测'代码调用），实现分析过程的上下文连贯。
            (2) 构建网络白名单网关，沙箱默认断网，仅允许通过代理访问系统预设的可信财经API，严禁通用网页爬虫以保障数据合规。

    3. 混合记忆与全链路溯源机制
        [1]. 混合检索外部记忆模块（强制）：
            (1) 实现Hybrid-Search Notepad记忆容器，支持KV键值读写与语义向量检索。接口扩展为：write/read/list/search(query, top_k)。
            (2) 工具体在缺乏精确Key时，通过语义检索获取相关财务数据或历史决策片段，解决跨任务信息孤岛。
            (3) 每个工具体实例加载独立Notepad，严格隔离命名空间，防止上下文污染。
        [2]. 结构化证据包与溯源：
            (1) 每次工具箱分析必须输出“结构化推理证据包”，包含：输入摘要Hash、子任务分解图、调用的Skill名称及版本、关键中间变量（如回归系数）、置信度评分及依据。
            (2) 支持决策路径回溯：用户可从最终分析结论反向点击，逐层查看依据的数据切片、计算代码及对标企业财报片段，确保战略决策可审计。

    4. 安全防御与经济审计体系
        [1]. Token经济审计与熔断（强制）：
            (1) 遵循OpenTelemetry协议输出分布式Trace，在Span中强制记录prompt_tokens、completion_tokens及预估USD成本。
            (2) 设定工具级“预算熔断阈值”，当单任务成本或重试次数超限时自动终止，防止逻辑死循环导致算力成本失控。
        [2]. 质量控制与基准测试：
            (1) 使用Pydantic V2配合Instructor Patch确保输出严格符合Schema，实施契约测试，失败任务立即终止。
            (2) 建立战略分析任务专用基准测试集Strat-Bench，包含经专家标注的战略决策场景。每次工具箱版本迭代必须全量跑通测试集，验证任务完成率与数值计算精度。
            (3) 建立“工具幻觉黑名单”，若某Skill连续生成违背输入数据的输出，自动锁定并报警。
}

D-D3 Q-D3 Z- G-D3 O-D1

D3+D1(成本)+G1(提示词)


你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要结合[企业战略规划管理智库系统需求列表]系统需求，从下面需求清单中选择最优的[工具箱]需求特性。要求： 
1、结合系统需求为每个列表逐个评分（10分制），最后求总分
2、评分维度：
   - 架构先进性与技术趋势匹配度
   - 数值计算与沙箱执行
   - Schema强制与输出校验
   - 提示词管理与优化
   - 可解释性与决策溯源
   - 用户反馈与修正闭环
   - 测试与质量保证（A/B测试、基准测试）
   - 安全与防御机制
   - 可扩展性与可维护性
   - 运行效率与成本控制
   - 领域专业性与战略工具建模深度
   - 与系统其他模块的集成能力
3、以表格形式呈现

---------------------
企业战略规划管理智库系统需求列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：基于网络调用LLM API驱动的战略工具箱功能，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理模型共17个规划工具
    1.输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节
    2.工具箱基本工作流：[待设计]
    3.支持用户反馈与修正工具分析结果
    4.禁止LLM进行数值计算与统计，强制LLM生成Python代码在Docker沙箱执行计算
    5.支持Pydantic V2配合Instructor Patch确保输出严格符合Schema，实施契约测试
    6.支持工具提示词生成与管理，根据具体工具模型定义生成适合LLM的提示词，基于DSPy理念建立工具箱Prompt Optimizer，根据工具分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数
    7.支持工具分析过程的可解释性可视化，输出关键决策因素和推理链
    8.建立A/B测试框架对比不同策略效果，并建立基准测试集验证工具性能

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节
    2.AGENT主要执行按角色定义自主分析数据、战略规划各阶段协作分析决策两大类任务，这两大类任务包括多种不同子类
    3.AGENT基本工作流：[感知]配置环境与上下文->管理会话生命周期->[思考]LLM推理->[执行]解析LLM输出决策任务完成或调用工具->[反射]回写工具输出到上下文并决策是否继续循环[思考]->构建输出并释放资源
    4.支持用户反馈与修正AGENT分析结果
    5.支持AGENT提示词生成与管理，按角色定义和分析框架生成适合LLM使用的提示词，基于DSPy理念建立AGENT Prompt Optimizer，根据AGENT分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数
    6.AGENT可按任务需求从工具箱中选择合适工具完成数据分析或战略规划任务
    7.支持企业利益最优的AGENT间协作，高级代理CEO AGENT拥有协作仲裁最终决定权，但用户可以观测和更改该决策
    8.支持视角隔离，AGENT仅从各自角色视角（如CFO仅关注资金风险）调用工具，严禁泛化讨论
    9.CEO AGENT内置"裁决状态机"作为仲裁器，设定最大辩论轮次，随着轮次增加，降低AGENT的Temperature Decay强制要求AGENT提出具体修改意见，而不是单纯否定，若未达成一致，生成Plan A（保守版）和Plan B（激进版）两套方案，CEO AGENT按偏好矩阵决策Plan A/B，同时暂停10秒提供用户介入裁决窗口，超时用户无动作则以 CEO AGENT 决策为准
    10.AUD AGENT采用"旁路监听+异步阻断"模式，不参与生成，只负责原文与生成内容的一致性检验，发现幻觉通知相关AGENT纠错，若3次重试失败，暂停任务人工纠错，之后从任务暂停点继续任务
    11.支持上下文压缩器与"公共黑板"机制，在多轮对话间仅传递关键结论而非全量历史，严格利用LangGraph的StateSnapshot机制配合Redis存储来管理公共黑板的更新与回溯
    12.支持Agent推理过程的可解释性可视化，包括决策依据、关键因素和置信度
    13.采用"思维链溯源树ToT"机制，只在SP/BP各阶段结束时启用ToT功能，当决策置信度低于阈值（默认0.6）触发ToT，通过回溯推理路径解决决策冲突，提高决策可解释性
    14.建立Agent性能监控与行为分析框架

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据

---------------------
D1
{
二、工具箱：构建基于“持久化计算内核”与“决策优化引擎”双擎驱动的战略推理与审计系统。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心组件。本系统遵循“确定性优先、可审计贯穿、成本可控”的设计原则，对战略分析全生命周期进行标准化管控。

1. 战略工具静态注册与持久化沙箱执行
   [1]. 17个战略工具均采用“静态注册制”，工具定义、输入输出Schema、默认分析模板固化于系统内核，禁止运行时动态生成新工具。系统预留技能生成实验开关（默认关闭），确保生产环境行为绝对稳定。
   [2]. 每个工具实例绑定独立的持久化Docker沙箱环境（基于Jupyter Kernel Gateway），支持跨工具、跨步骤的变量持久化存储。上游工具输出的结构化数据（如PESTEL的趋势变量）可直接被下游工具（如SWOT）通过内存态变量名引用，实现分析上下文无缝衔接。
   [3]. 沙箱内置标准科学计算库（NumPy、Pandas、SciPy）及运筹优化库（CVXPY、OR-Tools），严禁LLM直接输出数值统计结果；所有涉及算术运算、回归分析、矩阵计算、规划求解的任务，LLM仅负责生成Python代码，代码在沙箱内隔离执行，捕获stdout/stderr并返回执行结果。

2. 决策优化引擎：运筹问题直连求解器
   [1]. 战略规划中常见的资源分配、投资组合、产能规划等优化类问题，禁止LLM编写蒙特卡洛或梯度下降等通用求解代码。系统内置运筹优化求解器工厂，支持线性规划、整数规划、二次规划、多目标规划。
   [2]. LLM通过结构化提示词将业务约束（如预算上限、市场份额目标、风险容忍度）转换为JSON格式的数学模型（目标函数类型+变量边界+约束表达式），系统自动调用对应求解器计算最优解，并将解集反填至工具输出Schema。
   [3]. 当输入约束无法被求解器识别或产生不可行解时，系统返回明确错误类型，LLM依据错误信息修正约束表述，最大重试3次，失败则标记“当前约束下无可行方案”。

3. Planner-Executor 分工执行架构
   [1]. 工具箱执行流程强制拆分为规划层与执行层，使用不同量级模型分工协作：
       (1) 规划层（Planner）：调用经济型模型（如GPT-4o mini），负责接收用户任务指令及数据全景摘要，将复杂战略分析任务分解为有向无环图（DAG）形式的子任务序列，并为每个子节点指定所需工具及输入数据依赖。输出机器可读的任务编排清单。
       (2) 执行层（Executor）：调用高性能模型（如Claude Sonnet/GPT-4o），严格遵循Planner输出的编排清单，顺序或并行调用对应工具，执行代码生成、沙箱交互、结果解析等原子操作。禁止Executor跨任务步自主规划或跳过清单步骤。
       (3) 审计层（Auditor）：轻量级规则引擎或专用小模型，对Executor每次输出的JSON进行实时Schema校验（Pydantic V2）、数据类型检查、业务规则验证（如利润率不得为负），校验失败立即触发重试或回退至Planner重新规划。
   [2]. 提示词工程与演进：基于DSPy理念建立工具箱Prompt Optimizer。不依赖手动微调，而是建立“修正日志-样本转换”闭环，将用户对分析结果的修正操作转化为Few-Shot样本，定期运行优化器自动迭代工具的提示词参数，使工具逐步适配企业的特定战略偏好（如激进或保守）。
   [3]. 提示词版本管理：每个工具的System Prompt、用户Few-shot示例均以文件形式纳入Git LFS管理，版本与系统发布号绑定。当AUD AGENT或SP/BP阶段Checkpoint人工反馈提示词效果下降时，管理员可一键回滚至历史稳定版本。


4. 结构化证据包与可审计溯源
   [1]. 每一次工具执行（包括单工具独立运行或工具链串联）必须生成完整的“战略推理证据包”（Evidence Package），以Parquet列式格式持久化存储。证据包包含以下不可篡改字段：
       - 本次执行唯一ID及输入数据的SHA-256哈希值
       - Planner生成的任务分解DAG（节点、边、依赖关系）
       - 每个子任务调用工具的版本号、执行时间戳
       - Executor生成的原始代码及沙箱执行日志
       - 关键中间变量（如回归系数、聚类中心、敏感度测试值）
       - 置信度评分及置信区间（若适用）
       - 最终结构化输出
   [2]. 系统前端必须提供“决策路径回溯”可视化接口：用户可从最终分析结论（如“建议进入华东市场”）逐层下钻，查看支撑该结论的具体数据切片、计算逻辑、引用报告片段及原始代码行。回溯过程需支持Mermaid流程图及源代码语法高亮展示。

5. 成本熔断与契约测试质量门禁
   [1]. 契约测试强制嵌入CI/CD流水线：所有工具输出Schema必须通过Pydantic V2 + Instructor Patch的严格校验，任何字段名称、类型、嵌套结构的非向后兼容变更均触发构建失败。运行时输出若三次校验失败，则该工具进入熔断状态并通知管理员。
   [2]. 建立三级成本熔断机制：
       - 任务级熔断：单次战略分析任务（含多工具串联）累计消耗tokens超过预设阈值（默认5万tokens）或预估成本超过$0.5，强制终止执行并返回“成本超限”错误。
       - 会话级熔断：同一用户会话周期内总消耗tokens超过日配额，暂停工具箱服务直至配额重置。
       - 重试熔断：代码执行连续重试3次均失败（语法错误、求解器无解、网络异常），直接标记工具不可用并记录失败模式至性能看板。
   [3]. 建立战略分析专用基准测试集Strat-Bench V1.0，包含30个经专家标注的战略决策场景（覆盖17个工具典型使用路径），每次工具箱核心组件升级必须全量通过Strat-Bench，生成任务完成率、数值计算误差率、平均token消耗等指标，未通过则阻断发布。

6. 标准化工具箱工作流
   [1]. 工具箱执行生命周期严格遵循以下六阶段流程：
       (1) 感知与摘要：系统读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
       (2) 规划（Planner）：依据用户自然语言指令（如“请使用PESTEL和SWOT分析新能源业务进入风险”）及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、工具映射、数据依赖边。
       (3) 执行与验证（Executor + Auditor）：按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
       (4) 证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
       (5) 反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
       (6) 演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。

7. 性能监控与健康度仪表盘
   [1]. 实时监控每个工具的调用次数、成功率、平均执行时长、平均token消耗、用户修正频率。当某工具用户修正频率超过15%或连续失败率超过10%，自动在仪表盘标记“需人工复审”，并生成修正日志报告。
   [2]. 定期对Strat-Bench进行全量回归测试，绘制各工具版本性能漂移曲线。检测到核心指标（如任务完成率）周环比下降超过5%，触发告警并建议回滚至上一稳定版本。

8. 防御性编程与边界约束
   [1]. 沙箱环境默认断网，严格禁止代码访问外部API或文件系统；若工具定义需获取外部宏观数据（如汇率、行业增速），须通过系统统一的、带流量审计的白名单网关，且每次请求需经用户显式授权。
   [2]. 所有模型调用（Planner、Executor）必须显式声明模型名称及temperature值（强制0~0.3区间），禁止使用服务端默认隐式配置，确保每次执行结果可复现。
   [3]. 工具输出必须附带`confidence`字段（0-1浮点数），当置信度低于0.6时，工具箱自动在最终结论中添加“建议人工复核”标签，并高亮低置信度推理节点。
}

D3
{
二、工具箱：构建基于“元系统（Meta-System）架构”与“确定性计算引擎”融合的战略决策智能体集群。工具箱由17个核心战略模型构成，每个模型均封装为具备独立生命周期、领域知识图谱与契约化接口的智能工具体（Smart Tool Entity）。系统内置技能仓库（Skill Store），支持工具版本管理与渐进演化，并遵循以下核心设计原则与功能需求。

1. 架构总纲与智能工具体定义
   1.1 智能工具体（Smart Tool Entity）：
        - 每个战略分析模型均封装为独立工具体。
        - 工具体拥有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。
        - 工具体初始化时加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
   1.2 技能仓库（Skill Store）：
        - 所有工具体统一注册至中央技能仓库，支持版本控制、灰度发布与回滚。
        - 每个工具体必须通过Pydantic V2契约测试方可入库；后续版本迭代需通过回归测试集。
        - 远期支持基于代码生成模型的技能动态生成，初期以预定义模板为主，预留扩展接口。
   1.3 工具链编排与依赖调度：
        - 支持工具链有向无环图（Tool-Chain DAG）定义，允许上游工具的输出结构化流转为下游工具的输入。
        - 系统内置任务调度器，依据依赖关系自动并行执行无依赖子任务，提升分析效率。
        - 所有子任务状态均为“完成”方可交付最终结果。

2. 数值计算与沙箱执行
   2.1 强制代码执行隔离：
        - 严禁大语言模型直接进行任何数值计算、统计回归、矩阵运算、现金流量折现等量化操作。
        - 所有量化分析必须通过以下二选一路径执行：
            a) Python代码生成 + 沙箱执行；
            b) 优化器直连：针对线性规划、整数规划、蒙特卡洛模拟等战略运筹问题，由LLM将自然语言目标函数与约束条件转化为数学建模，调用CVXPY、OR-Tools、SciPy等优化器求解。
   2.2 持久化计算沙箱（会话级）：
        - 为每个任务会话实例化持久化Jupyter Kernel或同等状态化执行环境。
        - 支持跨工具、跨步骤的变量传递（如PESTEL计算的GDP增长率变量可直接被后续市场预测代码调用），实现分析上下文连贯。
        - 任务结束后立即销毁内核，并设置空闲超时回收策略，防止资源泄漏。
   2.3 Validation Feedback闭环：
        - 代码执行若报错，系统捕获STDERR并自动检索相似错误案例库与官方文档片段，辅助LLM在下一轮生成修复版本。
        - 设定最大重试次数（默认3次），失败则标记“当前工具不可行”并记录失败模式至技能演进日志。

3. Schema强制与一致性校验
   3.1 契约化输出：
        - 所有工具输入/输出严格遵循Pydantic V2定义的Data Model，并使用Instructor Patch或等价技术强制LLM输出符合Schema的结构化JSON，严禁自由文本。
   3.2 一致性校验仲裁器：
        - 在多工具协作场景下，终态输出需通过“一致性校验仲裁器”检测逻辑冲突（如现金流评估在不同工具间矛盾、利润率符号异常等）。
        - 仲裁器内置规则引擎及财务指标常识库，输出冲突检测报告并标记高置信度矛盾项，供用户或高级Agent裁决。
   3.3 契约测试自动化：
        - 在CI/CD流水线中集成针对工具Schema的自动化契约测试，防止模型漂移或版本迭代导致输出结构崩溃。
        - 契约测试失败的任务立即终止，不进入执行阶段。

4. 可解释性与决策溯源
   4.1 结构化推理证据包：
        - 每次工具分析必须输出结构化证据包，包含但不限于：
            - 输入数据摘要哈希值；
            - Planner制定的子任务分解图；
            - 每个子任务调用的Skill名称及版本；
            - 执行过程中的关键中间变量（如回归系数、聚类中心、敏感性参数）；
            - 置信度评分及依据；
            - 引用源（文档切片ID）。
   4.2 交互式溯源树：
        - 前端支持从最终分析结论反向点击，逐层展开证据包，展示数据来源→计算逻辑→中间结论→最终结论的完整推理链。
        - 自动生成关键决策因素的敏感性分析图表（如价格弹性、汇率波动对策略的影响）。
   4.3 红蓝辩论机制：
        - 针对高不确定性战略议题（如破坏性创新投资决策、新市场进入时机），系统可自动启动“红蓝辩论”模式。
        - 两个子智能体分别扮演激进派与保守派，基于相同数据集展开结构化辩论，记录双方论据强度与数据引用。
        - 最终输出共识区域与分歧区域，不强行折中，为管理层提供风险全景视图。

5. 用户反馈与闭环演进
   5.1 Checkpoint人工介入验证：
        - 在工具执行过程中，若识别到高风险决策节点（如基于小样本的市场容量预测、未经验证的关键假设），自动暂停并请求用户验证中间假设。
        - 用户可修改假设参数、调整权重或直接反馈修正指令，系统从暂停点恢复执行，并完整记录“专家修正轨迹”。
   5.2 修正轨迹库与逆向传播：
        - 将每次用户修正的快照（输入、原始输出、修正后输出、修正理由）匿名化存入修正轨迹库。
        - 系统定期对修正轨迹进行差异分析，推断若想得到用户修正后的输出，输入权重或任务分解策略应如何调整。
        - 高质量修正样本优先用于优化Planner的任务分解模板，实现元系统自我进化。
   5.3 用户反馈协议：
        - 用户反馈以增量数据或修正指令形式注入，强制触发工具体“反思阶段”（ReflAct），生成结构化修正日志并回传至技能演进日志。

6. 安全与防御机制
   6.1 沙箱网络隔离：
        - 所有代码执行沙箱默认断网；确需获取实时财经数据时，仅允许通过系统预设的网络白名单网关访问可信API（如Bloomberg、Wind），严禁通用网页爬虫。
   6.2 提示词注入防御：
        - 输入数据进入工具前执行清洗与注入检测，识别并剥离对抗性指令。
        - 将“提示词注入防御测试”纳入基准测试集，定期验证工具体对原始任务的忠诚度与抗干扰能力。
   6.3 工具幻觉黑名单：
        - 建立“工具幻觉黑名单”，若某工具体连续3次生成与输入数据明显矛盾的输出（如基于下降营收得出激进扩张结论且无合理解释），自动锁定该工具体并通知系统管理员介入审查。

7. 领域专业知识嵌入
   7.1 领域事实库：
        - 每个战略工具体内置领域事实库，以JSON Schema或规则集形式存储经典管理理论的强制约束与关键指标。
        - 示例：PESTEL工具体需包含政治、经济、社会、技术、环境、法律各维度的典型指标列表；SWOT工具体必须遵守“优势/劣势属内部，机会/威胁属外部”的交叉校验规则。
   7.2 验证器强制遵守：
        - 工具体输出前必须经过领域验证器，确保分析结论不违背基础理论框架（如四象限归因正确、波特五力各维度评分范围合规）。

8. 服务契约与集成
   8.1 独立微服务封装：
        - 工具箱整体作为独立微服务部署，对外暴露REST/gRPC接口。
        - 接口定义遵循OpenAPI 3.1规范，输入为系统处理后的结构化JSON数据，输出为分析结果、结构化证据包、成本审计信息的三元组。
   8.2 与Agent模块集成：
        - 与CEO、CFO等6个核心AGENT通过服务调用集成，AGENT按需请求工具箱执行特定分析任务。
        - 工具箱无状态设计，支持水平扩展，调用链通过OpenTelemetry输出分布式Trace。

9. 工具箱标准工作流
   9.1 工作流阶段：
        [阶段1] 环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具体价值观模板（SOUL.md），实例化会话级沙箱与Hybrid Notepad（混合检索记忆容器）。
        [阶段2] 规划（Planner）：Planner LLM接收任务请求与数据摘要，将战略分析任务分解为子任务序列，并为每个子任务匹配合适的工具体（含版本），输出Markdown格式任务分解图。
        [阶段3] 执行（Executor）：Executor LLM严格依照计划顺序调用工具体。每个工具体执行遵循Think→Code→Observe→Write Memory原子循环；数值计算走沙箱/优化器路径，文本归纳由LLM完成。
        [阶段4] 验证与仲裁：每步输出经领域验证器与契约校验；最终多工具输出经一致性仲裁器检测逻辑冲突。
        [阶段5] 证据打包：系统自动汇集所有中间产物，生成结构化证据包。
        [阶段6] 成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
   9.2 中断与抢占：
        - 支持用户或高级Agent发送中断信号，暂停当前执行流，插入高优先级指令后恢复现场。

10. 提示词工程与演进
   [1]. 提示词工程：基于DSPy理念建立工具箱Prompt Optimizer。不依赖手动微调，而是建立“修正日志-样本转换”闭环，将用户对分析结果的修正操作转化为Few-Shot样本，定期运行优化器自动迭代工具的提示词参数，使工具逐步适配企业的特定战略偏好（如激进或保守）。
   [2]. 提示词自动演进
        (1).自动演进受控触发：DSPy 提示词自动演进不得常驻运行，仅允许在系统处于 SP/BP 阶段 Checkpoint 完成态，且在满足稳定性与一致性指标阈值的前提下被显式触发。任何非受控状态下的自动演进请求必须被系统拒绝。
        (2).多目标与否决约束并存：必须采用多目标优化机制，并同时受结构与一致性指标作为硬约束；战略方法论违规作为否决约束；行为与成本指标作为软约束。任何仅基于单一性能指标的自动演进不得执行。
        (3).方法论不变性：不得改变工具的分析步骤、战略模型定义或 SP/BP 阶段逻辑。必须对演进前后提示词执行结构化差异校验，检测到方法论变化的结果不得进入发布流程。
        (4).学习边界与样本抽象：用户或专家修正行为不得被直接用于提示词学习。所有修正数据必须先被抽象为 去业务化、去结论化的结构化表示，DSPy 自动演进仅允许基于该抽象层运行。
        (5).可审计回滚熔断：每次自动演进必须生成完整的审计记录，并支持影子发布与准入门禁；一键回滚至历史稳定版本；在异常行为出现时自动冻结后续演进。
   [3]. 提示词版本管理：每个工具的System Prompt、用户Few-shot示例均以文件形式纳入Git LFS管理，版本与系统发布号绑定。当AUD AGENT或SP/BP阶段Checkpoint人工反馈提示词效果下降时，管理员可一键回滚至历史稳定版本。


11. 测试与质量保证
    11.1 基准测试集（Strat-Bench）：
         - 构建战略分析专用基准测试集Strat-Bench V1.0，初期包含20个经专家标注的战略决策场景（覆盖17个工具的核心应用），每个场景包含输入数据、标准输出Schema、专家评分标准。
         - 后续逐步扩展至100+场景，作为工具箱版本迭代的准入测试。
    11.2 A/B测试框架：
         - 支持同一工具体多版本（Prompt模板、模型型号、DAG流程）并行运行，自动收集任务完成率、数值计算精度、幻觉率、Token成本等指标，择优晋级。
    11.3 健康度仪表盘：
         - 实时监控各工具体调用成功率、平均执行时长、用户修正频率、算力成本效益比。
         - 当某工具体用户修正频率超过阈值（默认15%），自动触发Skill Review流程，提示工程师介入优化或淘汰。

12. 性能监控与漂移检测
    12.1 定期评估工具检索准确率与生成质量，建立性能漂移检测机制，对比历史基线。
    12.2 所有评估结果可视化输出，纳入系统行为日志。

13. 成本熔断与契约测试质量门禁
   [1]. 契约测试强制嵌入CI/CD流水线：所有工具输出Schema必须通过Pydantic V2 + Instructor Patch的严格校验，任何字段名称、类型、嵌套结构的非向后兼容变更均触发构建失败。运行时输出若三次校验失败，则该工具进入熔断状态并通知管理员。
   [2]. 建立三级成本熔断机制：
       - 任务级熔断：单次战略分析任务（含多工具串联）累计消耗tokens超过预设阈值（默认5万tokens）或预估成本超过$0.5，强制终止执行并返回“成本超限”错误。
       - 会话级熔断：同一用户会话周期内总消耗tokens超过日配额，暂停工具箱服务直至配额重置。
       - 重试熔断：代码执行连续重试3次均失败（语法错误、求解器无解、网络异常），直接标记工具不可用并记录失败模式至性能看板。
   [3]. 建立战略分析专用基准测试集Strat-Bench V1.0，包含30个经专家标注的战略决策场景（覆盖17个工具典型使用路径），每次工具箱核心组件升级必须全量通过Strat-Bench，生成任务完成率、数值计算误差率、平均token消耗等指标，未通过则阻断发布。


G1
{
二、工具箱：构建基于“神经符号人工智能（Neuro-symbolic AI）”与“代码即推理（Code-as-Reasoning）”架构的确定性深度战略计算引擎。包含PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新模型（含破坏潜力评估/时间预测/投资决策）、BSC、战略地图、KPI以及变革管理模型共17个核心微型智能体（Micro-Agents）。

    1.数据输入与Schema契约：输入经系统处理结构化为JSON格式的数据，严格遵循“Schema-First”原则。所有工具的输入/输出必须通过Pydantic V2定义严格的数据模型契约，配合Instructor Patch确保LLM输出严格符合Schema结构的JSON数据，严禁输出非结构化的自由文本。建立逻辑校验器（Logic Validator），在输出前对业务逻辑（如市场份额总和≤100%、利润=收入-成本）进行硬性约束检查。

    2.工具箱基本工作流：采用“规划-执行-审计（Planner-Executor-Auditor）”架构。
        [1]. 规划：首先读取数据摘要，由Planner构建任务依赖有向无环图（DAG），明确工具执行的拓扑顺序（如先执行PESTEL生成宏观变量，再流转至SWOT）。
        [2]. 上下文：实例化持久化的Jupyter Kernel（Stateful Sandbox）作为共享内存，支持变量在不同工具间零损耗传递，确保分析过程的上下文连贯。
        [3]. 执行：Executor按DAG顺序调度工具，生成推理链与代码并在沙箱中执行。
        [4]. 审计：Auditor对最终结果进行Schema校验与逻辑核查，生成结构化证据包。

    3.对抗性反馈与修正：支持用户对工具分析结果的反馈与修正。在关键决策节点（如破坏性创新投资决策）引入“红蓝对抗（Red-Teaming）”机制，实例化批判者Agent对当前结论进行反事实推理与压力测试，强制执行者Agent进行辩论与修正，最终输出包含辩论摘要的决策报告。

    4.分级计算策略：严禁LLM直接进行任何形式的数值计算与统计预测，强制采用分级计算策略。
        [1]. 描述性统计与可视化：由LLM生成Python代码在隔离沙箱中执行。
        [2]. 运筹优化与复杂模拟：针对资源配置、风险模拟等问题，强制调用专业求解器（如OR-Tools、CVXPY），由LLM负责构建数学模型与约束条件，求解器输出最优解。

    5.全链路审计与溯源：输出结果必须包含完整的“推理证据链（Reasoning Trace）”，记录从原始数据切片、代码执行逻辑、求解器参数到最终结论的完整映射路径。同时实施Token经济审计，记录每个步骤的算力成本。

    6.提示词工程与演进：基于DSPy理念建立工具箱Prompt Optimizer。不依赖手动微调，而是建立“修正日志-样本转换”闭环，将用户对分析结果的修正操作转化为Few-Shot样本，定期运行优化器自动迭代工具的提示词参数，使工具逐步适配企业的特定战略偏好（如激进或保守）。

    7.可解释性可视化：支持工具分析过程的白盒化展示。不仅输出最终图表，需可视化展示决策树、变量依赖关系图以及关键假设的敏感性分析（如汇率波动对利润的影响幅度）。

    8.测试与监控：建立战略分析基准测试集（Strat-Bench），包含标准场景与边缘场景（如极端市场环境）。实施A/B测试框架对比不同Prompt策略的效果，并建立契约测试流水线，防止模型输出结构漂移导致系统崩溃。
}





二、工具箱：由17个核心战略模型构成，每个模型均封装为具备独立生命周期、领域知识图谱与契约化接口的智能工具。系统内置技能仓库，支持工具版本管理与渐进演化
    1.智能工具定义
        [1].建立工具标准：每个工具有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。工具初始化必须加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
        [2].构建中央技能仓库：实现工具的统一注册、版本控制、灰度发布与回滚机制。所有入库工具必须通过Pydantic V2契约测试，后续迭代需通过回归测试集。支持预定义模板与远期基于代码生成的技能动态扩展。
        [3].支持工具链编排与依赖调度：支持定义工具链有向无环图，实现上游工具输出结构化流转为下游输入。内置任务调度器，根据依赖关系自动并行执行无依赖子任务。

    2.工具箱标准工作流
        [1].环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具价值观模板（SOUL.md），实例化会话级沙箱与混合检索记忆容器。
        [2].感知与摘要：读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
        [3].规划：Planner依据用户自然语言指令及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、匹配工具映射、数据依赖边。
        [4].执行与验证：Executor按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe→writeback原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
        [5].证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
        [6].反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
        [7].成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
        [8].演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。

    3.数值计算与沙箱执行
        [1].强制代码执行隔离：严禁LLM直接进行数值计算、统计回归或现金流折现。所有量化分析强制通过两条路径执行：一是生成Python代码在沙箱执行；二是针对战略运筹问题（如线性规划），由LLM构建数学模型调用CVXPY、OR-Tools等优化器求解。
        [2].构建持久化计算沙箱：为每个任务会话实例化持久化Jupyter Kernel，支持跨工具、跨步骤的变量传递（如宏观经济变量在不同模型间共享），实现分析上下文连贯。任务结束后自动销毁内核并回收资源。
        [3].支持Validation Feedback闭环：自动捕获代码执行STDERR，检索错误案例库辅助LLM生成修复版本。设定最大重试次数（默认3次），失败则标记不可行并记录至演进日志。

    4.Schema强制与一致性校验
        [1].实施契约化输出：所有工具输入/输出严格遵循Pydantic V2定义的Data Model，利用Instructor Patch强制LLM输出符合Schema的结构化JSON，严禁输出自由文本。
        [2].建立一致性校验仲裁器：在多工具协作中，通过内置规则引擎及财务常识库检测逻辑冲突（如利润率与成本矛盾）。输出冲突检测报告，供用户或高级AGENT裁决。
        [3].自动化契约测试：在CI/CD流水线集成Schema契约测试，防止模型漂移导致结构崩溃。契约测试失败的任务强制终止。

    5.可解释性与决策溯源
        [1].输出结构化推理证据包：每次分析必须打包输出全量元数据，包括输入摘要哈希、Planner任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片ID）。
        [2].构建交互式溯源树：支持前端从结论反向逐层展开证据包，展示“数据来源→计算逻辑→中间结论→最终结论”的完整推理链。
        [3].支持红蓝辩论机制：针对高不确定性议题自动启动“红蓝对抗”，实例化激进派与保守派子智能体进行结构化辩论，输出包含共识与分歧区域的风险全景视图。

    6.提示词工程与演进
        [1].建立Prompt Optimizer：基于DSPy理念，建立“修正日志-样本转换”闭环，将用户修正转化为Few-Shot样本，自动迭代提示词参数以适配企业战略风格。
        [2].实施受控自动演进：仅在SP/BP Checkpoint完成态且满足一致性阈值时触发演进。必须采用多目标优化（结构、一致性、成本），严禁单一指标优化。
        [3].确保方法论不变性：演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
        [4].支持版本管理与回滚：所有Prompt纳入Git LFS管理，与系统版本绑定。支持一键回滚至历史稳定版本。

    7.用户反馈与闭环演进
        [1].支持Checkpoint人工介入：在关键决策节点或低置信度环节自动暂停，请求用户验证中间假设。支持用户修改参数或调整权重，系统记录修正前后的“专家修正轨迹”。
        [2].建立修正轨迹库与逆向传播：将用户修正快照匿名化存储，定期分析差异，推断输入权重或任务分解策略的优化方向。
        [3].实施用户反馈协议：将用户反馈转化为结构化修正日志，强制触发工具体“反思阶段”（ReflAct），并回传至技能演进日志。

    8.安全与防御机制
        [1].实施沙箱网络隔离：代码执行环境默认断网，仅允许通过白名单网关访问预设的可信财经API，严禁通用网页爬虫。
        [2].建立提示词注入防御：输入数据前置执行清洗与注入检测。将“注入防御测试”纳入基准测试集，定期验证工具体的抗干扰能力。
        [3].建立工具幻觉黑名单：自动监控工具体输出，若连续3次生成与输入矛盾的结论，自动锁定该工具体并报警。

    9.领域专业知识嵌入
        [1].内置领域事实库：以JSON Schema存储经典管理理论的强制约束（如SWOT内部/外部属性分类），确保分析逻辑符合理论框架。
        [2].部署领域验证器：工具体输出前必须经过验证器检查，确保结论不违背基础商业逻辑与领域常识。

    10.服务契约与集成
        [1].独立微服务封装：工具箱作为独立微服务对外暴露OpenAPI 3.1标准接口，输出分析结果、证据包与成本审计信息三元组。
        [2].支持Agent模块集成：与CEO、CFO等核心AGENT通过服务调用无缝集成，支持水平扩展与分布式Trace追踪。

    11.测试与质量保证
        [1].构建基准测试集（Strat-Bench）：建立包含20+专家标注场景的战略分析专用测试集，作为版本迭代的准入测试标准。
        [2].支持A/B测试框架：支持多版本策略并行运行，基于任务完成率、计算精度与成本指标择优晋级。
        [3].部署健康度仪表盘：实时监控工具成功率与修正率，修正率超标（默认15%）自动触发Skill Review。

    12.性能监控与漂移检测
        [1].建立性能漂移检测：定期评估检索准确率与生成质量，对比历史基线检测性能下降。
        [2].支持可视化日志：所有评估与运行结果纳入系统行为日志并可视化展示。

    13.成本熔断与契约测试质量门禁
        [1].CI/CD契约测试门禁：所有工具输出必须通过严格Schema校验，非兼容变更触发构建失败。运行时连续三次校验失败触发工具熔断。
        [2].实施三级成本熔断：支持任务级（单次Token/成本超限）、会话级（日配额超限）与重试级（连续代码执行失败）熔断机制，确保系统运行成本可控。





# TEMP - 重构AGENT

你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要重新设计提取[企业战略规划管理智库系统需求特性列表
]->[AGENT]需求特性。要求：
1、收集2024~2026年LLM、AGENT与AGI领域有重要影响力的优秀案例（如openclaw, claude code, SDD等），分析归纳它们的关键成功要素KSF1~n
2、对标优秀案例的关键成功要素，客观评估审核上面[AGENT]需求特性的优势与劣势，[逐条]AND[完整]列表分析并提出完善优化建议，表头：原有特性、KSF对标、优势、劣势、优化建议
3、根据上面优化建议完善[AGENT]需求特性
4、保留原来格式以txt代码片段形式输出完整的[AGENT]需求特性列表
----------------------
企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：支持基于网络LLM和先进Agent技术的智能分析工具集，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理（共计17个）。
    1.智能工具定义
        [1].建立工具标准：每个工具有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。工具初始化必须加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
        [2].构建中央技能仓库：实现工具的统一注册、版本控制、灰度发布与回滚机制。所有入库工具必须通过Pydantic V2契约测试，后续迭代需通过回归测试集。支持预定义模板与远期基于代码生成的技能动态扩展。
        [3].支持工具链编排与依赖调度：支持定义工具链有向无环图，实现上游工具输出结构化流转为下游输入。内置任务调度器，根据依赖关系自动并行执行无依赖子任务。
    2.工具箱标准工作流
        [1].环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具价值观模板（SOUL.md），实例化会话级沙箱与混合检索记忆容器。
        [2].感知与摘要：读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
        [3].规划：Planner依据用户自然语言指令及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、匹配工具映射、数据依赖边。
        [4].执行与验证：Executor按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe→writeback原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
        [5].证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
        [6].反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
        [7].成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
        [8].演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。
    3.数值计算与沙箱执行
        [1].强制代码执行隔离：严禁LLM直接进行数值计算、统计回归或现金流折现。所有量化分析强制通过两条路径执行：一是生成Python代码在沙箱执行；二是针对战略运筹问题（如线性规划），由LLM构建数学模型调用CVXPY、OR-Tools等优化器求解。
        [2].构建持久化计算沙箱：为每个任务会话实例化持久化Jupyter Kernel，支持跨工具、跨步骤的变量传递（如宏观经济变量在不同模型间共享），实现分析上下文连贯。任务结束后自动销毁内核并回收资源。
        [3].支持Validation Feedback闭环：自动捕获代码执行STDERR，检索错误案例库辅助LLM生成修复版本。设定最大重试次数（默认3次），失败则标记不可行并记录至演进日志。
    4.Schema强制与一致性校验
        [1].实施契约化输出：所有工具输入/输出严格遵循Pydantic V2定义的Data Model，利用Instructor Patch强制LLM输出符合Schema的结构化JSON，严禁输出自由文本。
        [2].建立一致性校验仲裁器：在多工具协作中，通过内置规则引擎及财务常识库检测逻辑冲突（如利润率与成本矛盾）。输出冲突检测报告，供用户或高级AGENT裁决。
        [3].自动化契约测试：在CI/CD流水线集成Schema契约测试，防止模型漂移导致结构崩溃。契约测试失败的任务强制终止。
    5.可解释性与决策溯源
        [1].输出结构化推理证据包：每次分析必须打包输出全量元数据，包括输入摘要哈希、Planner任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片ID）。
        [2].构建交互式溯源树：支持前端从结论反向逐层展开证据包，展示“数据来源→计算逻辑→中间结论→最终结论”的完整推理链。
        [3].支持红蓝辩论机制：针对高不确定性议题自动启动“红蓝对抗”，实例化激进派与保守派子智能体进行结构化辩论，输出包含共识与分歧区域的风险全景视图。
    6.提示词工程与演进
        [1].Prompt Optimizer：基于DSPy理念，建立“修正日志-样本转换”闭环，将用户修正转化为Few-Shot样本，自动迭代提示词参数以适配企业战略风格。
        [2].实施受控自动演进：仅在SP/BP Checkpoint完成态且满足一致性阈值时触发演进。必须采用多目标优化（结构、一致性、成本），严禁单一指标优化。
        [3].确保方法论不变性：演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
        [4].支持版本管理与回滚：所有Prompt纳入Git LFS管理，与系统版本绑定。支持一键回滚至历史稳定版本。
    7.用户反馈与闭环演进
        [1].支持Checkpoint人工介入：在关键决策节点或低置信度环节自动暂停，请求用户验证中间假设。支持用户修改参数或调整权重，系统记录修正前后的“专家修正轨迹”。
        [2].建立修正轨迹库与逆向传播：将用户修正快照匿名化存储，定期分析差异，推断输入权重或任务分解策略的优化方向。
        [3].实施用户反馈协议：将用户反馈转化为结构化修正日志，强制触发工具体“反思阶段”（ReflAct），并回传至技能演进日志。
    8.安全与防御机制
        [1].实施沙箱网络隔离：代码执行环境默认断网，仅允许通过白名单网关访问预设的可信财经API，严禁通用网页爬虫。
        [2].建立提示词注入防御：输入数据前置执行清洗与注入检测。将“注入防御测试”纳入基准测试集，定期验证工具体的抗干扰能力。
        [3].建立工具幻觉黑名单：自动监控工具体输出，若连续3次生成与输入矛盾的结论，自动锁定该工具体并报警。
    9.领域专业知识嵌入
        [1].内置领域事实库：以JSON Schema存储经典管理理论的强制约束（如SWOT内部/外部属性分类），确保分析逻辑符合理论框架。
        [2].部署领域验证器：工具体输出前必须经过验证器检查，确保结论不违背基础商业逻辑与领域常识。
    10.服务契约与集成
        [1].独立微服务封装：工具箱作为独立微服务对外暴露OpenAPI 3.1标准接口，输出分析结果、证据包与成本审计信息三元组。
        [2].支持Agent模块集成：与CEO、CFO等核心AGENT通过服务调用无缝集成，支持水平扩展与分布式Trace追踪。
    11.测试与质量保证
        [1].构建基准测试集（Strat-Bench）：建立包含20+专家标注场景的战略分析专用测试集，作为版本迭代的准入测试标准。
        [2].支持A/B测试框架：支持多版本策略并行运行，基于任务完成率、计算精度与成本指标择优晋级。
        [3].部署健康度仪表盘：实时监控工具成功率与修正率，修正率超标（默认15%）自动触发Skill Review。
    12.性能监控与漂移检测
        [1].建立性能漂移检测：定期评估检索准确率与生成质量，对比历史基线检测性能下降。
        [2].支持可视化日志：所有评估与运行结果纳入系统行为日志并可视化展示。
    13.成本熔断与契约测试质量门禁
        [1].CI/CD契约测试门禁：所有工具输出必须通过严格Schema校验，非兼容变更触发构建失败。运行时连续三次校验失败触发工具熔断。
        [2].实施三级成本熔断：支持任务级（单次Token/成本超限）、会话级（日配额超限）与重试级（连续代码执行失败）熔断机制，确保系统运行成本可控。

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节
    2.AGENT主要执行按角色定义自主分析数据、战略规划各阶段协作分析决策两大类任务，这两大类任务包括多种不同子类
    3.AGENT基本工作流：[感知]配置环境与上下文->管理会话生命周期->[思考]LLM推理->[执行]解析LLM输出决策任务完成或调用工具->[反射]回写工具输出到上下文并决策是否继续循环[思考]->构建输出并释放资源
    4.支持用户反馈与修正AGENT分析结果
    5.支持AGENT提示词生成与管理，按角色定义和分析框架生成适合LLM使用的提示词，基于DSPy理念建立AGENT Prompt Optimizer，根据AGENT分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数
    6.AGENT可按任务需求从工具箱中选择合适工具完成数据分析或战略规划任务
    7.支持企业利益最优的AGENT间协作，高级代理CEO AGENT拥有协作仲裁最终决定权，但用户可以观测和更改该决策
    8.支持视角隔离，AGENT仅从各自角色视角（如CFO仅关注资金风险）调用工具，严禁泛化讨论
    9.CEO AGENT内置"裁决状态机"作为仲裁器，设定最大辩论轮次，随着轮次增加，降低AGENT的Temperature Decay强制要求AGENT提出具体修改意见，而不是单纯否定，若未达成一致，生成Plan A（保守版）和Plan B（激进版）两套方案，CEO AGENT按偏好矩阵决策Plan A/B，同时暂停10秒提供用户介入裁决窗口，超时用户无动作则以 CEO AGENT 决策为准
    10.AUD AGENT采用"旁路监听+异步阻断"模式，不参与生成，只负责原文与生成内容的一致性检验，发现幻觉通知相关AGENT纠错，若3次重试失败，暂停任务人工纠错，之后从任务暂停点继续任务
    11.支持上下文压缩器与"公共黑板"机制，在多轮对话间仅传递关键结论而非全量历史，严格利用LangGraph的StateSnapshot机制配合Redis存储来管理公共黑板的更新与回溯
    12.支持Agent推理过程的可解释性可视化，包括决策依据、关键因素和置信度
    13.采用"思维链溯源树ToT"机制，只在SP/BP各阶段结束时启用ToT功能，当决策置信度低于阈值（默认0.6）触发ToT，通过回溯推理路径解决决策冲突，提高决策可解释性
    14.建立Agent性能监控与行为分析框架

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察(包括六个子步骤)、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据




## 20选5


你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。D1~5~是针对[企业战略规划管理智库系统需求特性列表]->[AGENT]需求特性的重构，请你以宗师级水准对标优秀案例，对这些需求列表内容进行评估。要求：
1、收集2024~2026年LLM、AGENT与AGI领域有重要影响力的优秀案例（如openclaw, claude code, SDD等），分析归纳它们的关键成功要素KSF1~n
2、对标优秀案例的关键成功要素，[逐条]AND[完整]列表分析评估审核上面[AGENT]需求特性
3、严格从[客观视角]进行评估
4、以表格形式呈现，表头：评估维度---D1评估---D2评估---D3评估---D4评估---D5评估(10分制)



你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要结合[企业战略规划管理智库系统需求特性列表]系统需求，从D/Q/G/O共计20个需求清单中选择最优的[AGENT]需求特性。要求： 
1、结合系统需求为每个列表逐个评分（10分制），最后求总分
2、评分维度：
   - 核心架构思想
   - 输入与感知
   - AGENT工作流
   - 用户反馈闭环
   - 提示词工程与演进
   - 系统自适应能力
   - 记忆与上下文
   - 工具调用与集成
   - 仲裁与决策机制
   - 审计与一致性
   - 创新前瞻性
   - 可观测性与可解释性
   - 安全与鲁棒性
   - 性能与监控
   - 成本效能
   - 企业级适用性
   - 工程可实施性
3、严格从[客观视角]进行评估，以表格形式呈现
---------------------
企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：支持基于网络LLM和先进Agent技术的智能分析工具集，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理（共计17个）。
    1.智能工具定义
        [1].建立工具标准：每个工具有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。工具初始化必须加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
        [2].构建中央技能仓库：实现工具的统一注册、版本控制、灰度发布与回滚机制。所有入库工具必须通过Pydantic V2契约测试，后续迭代需通过回归测试集。支持预定义模板与远期基于代码生成的技能动态扩展。
        [3].支持工具链编排与依赖调度：支持定义工具链有向无环图，实现上游工具输出结构化流转为下游输入。内置任务调度器，根据依赖关系自动并行执行无依赖子任务。
    2.工具箱标准工作流
        [1].环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具价值观模板（SOUL.md），实例化会话级沙箱与混合检索记忆容器。
        [2].感知与摘要：读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
        [3].规划：Planner依据用户自然语言指令及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、匹配工具映射、数据依赖边。
        [4].执行与验证：Executor按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe→writeback原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
        [5].证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
        [6].反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
        [7].成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
        [8].演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。
    3.数值计算与沙箱执行
        [1].强制代码执行隔离：严禁LLM直接进行数值计算、统计回归或现金流折现。所有量化分析强制通过两条路径执行：一是生成Python代码在沙箱执行；二是针对战略运筹问题（如线性规划），由LLM构建数学模型调用CVXPY、OR-Tools等优化器求解。
        [2].构建持久化计算沙箱：为每个任务会话实例化持久化Jupyter Kernel，支持跨工具、跨步骤的变量传递（如宏观经济变量在不同模型间共享），实现分析上下文连贯。任务结束后自动销毁内核并回收资源。
        [3].支持Validation Feedback闭环：自动捕获代码执行STDERR，检索错误案例库辅助LLM生成修复版本。设定最大重试次数（默认3次），失败则标记不可行并记录至演进日志。
    4.Schema强制与一致性校验
        [1].实施契约化输出：所有工具输入/输出严格遵循Pydantic V2定义的Data Model，利用Instructor Patch强制LLM输出符合Schema的结构化JSON，严禁输出自由文本。
        [2].建立一致性校验仲裁器：在多工具协作中，通过内置规则引擎及财务常识库检测逻辑冲突（如利润率与成本矛盾）。输出冲突检测报告，供用户或高级AGENT裁决。
        [3].自动化契约测试：在CI/CD流水线集成Schema契约测试，防止模型漂移导致结构崩溃。契约测试失败的任务强制终止。
    5.可解释性与决策溯源
        [1].输出结构化推理证据包：每次分析必须打包输出全量元数据，包括输入摘要哈希、Planner任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片ID）。
        [2].构建交互式溯源树：支持前端从结论反向逐层展开证据包，展示“数据来源→计算逻辑→中间结论→最终结论”的完整推理链。
        [3].支持红蓝辩论机制：针对高不确定性议题自动启动“红蓝对抗”，实例化激进派与保守派子智能体进行结构化辩论，输出包含共识与分歧区域的风险全景视图。
    6.提示词工程与演进
        [1].Prompt Optimizer：基于DSPy理念，建立“修正日志-样本转换”闭环，将用户修正转化为Few-Shot样本，自动迭代提示词参数以适配企业战略风格。
        [2].实施受控自动演进：仅在SP/BP Checkpoint完成态且满足一致性阈值时触发演进。必须采用多目标优化（结构、一致性、成本），严禁单一指标优化。
        [3].确保方法论不变性：演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
        [4].支持版本管理与回滚：所有Prompt纳入Git LFS管理，与系统版本绑定。支持一键回滚至历史稳定版本。
    7.用户反馈与闭环演进
        [1].支持Checkpoint人工介入：在关键决策节点或低置信度环节自动暂停，请求用户验证中间假设。支持用户修改参数或调整权重，系统记录修正前后的“专家修正轨迹”。
        [2].建立修正轨迹库与逆向传播：将用户修正快照匿名化存储，定期分析差异，推断输入权重或任务分解策略的优化方向。
        [3].实施用户反馈协议：将用户反馈转化为结构化修正日志，强制触发工具体“反思阶段”（ReflAct），并回传至技能演进日志。
    8.安全与防御机制
        [1].实施沙箱网络隔离：代码执行环境默认断网，仅允许通过白名单网关访问预设的可信财经API，严禁通用网页爬虫。
        [2].建立提示词注入防御：输入数据前置执行清洗与注入检测。将“注入防御测试”纳入基准测试集，定期验证工具体的抗干扰能力。
        [3].建立工具幻觉黑名单：自动监控工具体输出，若连续3次生成与输入矛盾的结论，自动锁定该工具体并报警。
    9.领域专业知识嵌入
        [1].内置领域事实库：以JSON Schema存储经典管理理论的强制约束（如SWOT内部/外部属性分类），确保分析逻辑符合理论框架。
        [2].部署领域验证器：工具体输出前必须经过验证器检查，确保结论不违背基础商业逻辑与领域常识。
    10.服务契约与集成
        [1].独立微服务封装：工具箱作为独立微服务对外暴露OpenAPI 3.1标准接口，输出分析结果、证据包与成本审计信息三元组。
        [2].支持Agent模块集成：与CEO、CFO等核心AGENT通过服务调用无缝集成，支持水平扩展与分布式Trace追踪。
    11.测试与质量保证
        [1].构建基准测试集（Strat-Bench）：建立包含20+专家标注场景的战略分析专用测试集，作为版本迭代的准入测试标准。
        [2].支持A/B测试框架：支持多版本策略并行运行，基于任务完成率、计算精度与成本指标择优晋级。
        [3].部署健康度仪表盘：实时监控工具成功率与修正率，修正率超标（默认15%）自动触发Skill Review。
    12.性能监控与漂移检测
        [1].建立性能漂移检测：定期评估检索准确率与生成质量，对比历史基线检测性能下降。
        [2].支持可视化日志：所有评估与运行结果纳入系统行为日志并可视化展示。
    13.成本熔断与契约测试质量门禁
        [1].CI/CD契约测试门禁：所有工具输出必须通过严格Schema校验，非兼容变更触发构建失败。运行时连续三次校验失败触发工具熔断。
        [2].实施三级成本熔断：支持任务级（单次Token/成本超限）、会话级（日配额超限）与重试级（连续代码执行失败）熔断机制，确保系统运行成本可控。

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节
    2.AGENT主要执行按角色定义自主分析数据、战略规划各阶段协作分析决策两大类任务，这两大类任务包括多种不同子类
    3.AGENT基本工作流：[感知]配置环境与上下文->管理会话生命周期->[思考]LLM推理->[执行]解析LLM输出决策任务完成或调用工具->[反射]回写工具输出到上下文并决策是否继续循环[思考]->构建输出并释放资源
    4.支持用户反馈与修正AGENT分析结果
    5.支持AGENT提示词生成与管理，按角色定义和分析框架生成适合LLM使用的提示词，基于DSPy理念建立AGENT Prompt Optimizer，根据AGENT分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数
    6.AGENT可按任务需求从工具箱中选择合适工具完成数据分析或战略规划任务
    7.支持企业利益最优的AGENT间协作，高级代理CEO AGENT拥有协作仲裁最终决定权，但用户可以观测和更改该决策
    8.支持视角隔离，AGENT仅从各自角色视角（如CFO仅关注资金风险）调用工具，严禁泛化讨论
    9.CEO AGENT内置"裁决状态机"作为仲裁器，设定最大辩论轮次，随着轮次增加，降低AGENT的Temperature Decay强制要求AGENT提出具体修改意见，而不是单纯否定，若未达成一致，生成Plan A（保守版）和Plan B（激进版）两套方案，CEO AGENT按偏好矩阵决策Plan A/B，同时暂停10秒提供用户介入裁决窗口，超时用户无动作则以 CEO AGENT 决策为准
    10.AUD AGENT采用"旁路监听+异步阻断"模式，不参与生成，只负责原文与生成内容的一致性检验，发现幻觉通知相关AGENT纠错，若3次重试失败，暂停任务人工纠错，之后从任务暂停点继续任务
    11.支持上下文压缩器与"公共黑板"机制，在多轮对话间仅传递关键结论而非全量历史，严格利用LangGraph的StateSnapshot机制配合Redis存储来管理公共黑板的更新与回溯
    12.支持Agent推理过程的可解释性可视化，包括决策依据、关键因素和置信度
    13.采用"思维链溯源树ToT"机制，只在SP/BP各阶段结束时启用ToT功能，当决策置信度低于阈值（默认0.6）触发ToT，通过回溯推理路径解决决策冲突，提高决策可解释性
    14.建立Agent性能监控与行为分析框架

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察(包括六个子步骤)、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据
---------------------
D1
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1. 输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节。支持加载用户或团队的“长期演化记忆”（见特性13），使AGENT具备跨会话的经验积累。
    2. AGENT主要执行按角色定义自主分析数据、战略规划各阶段协作分析决策两大类任务。任务启动时，必须首先生成“任务执行规格说明书 (Specification)”，明确子任务、依赖关系、预期输出和验收标准。
    3. AGENT基本工作流采用“规划-执行-反思”模式：
        [1]. [规划] (Plan)：Planner根据全景摘要、用户指令和长期记忆，生成机器可读的Specification。规划结果需经用户或AUD AGENT确认后方可执行。
        [2]. [执行] (Execute)：Executor按Specification严格调度工具或子AGENT，执行规划。所有关键步骤均需输出结构化推理链。
        [3]. [反思] (Reflect)：任务或关键子任务完成后，AGENT对比实际输出与Specification，形成结构化反思日志，存入长期演化记忆。
    4. 支持用户反馈与修正AGENT分析结果。系统记录每一次修正前后的差异，形成“专家修正轨迹”，作为系统演进的原材料。
    5. 支持基于“修正轨迹”和“反思日志”的双路演进机制：
        [1]. 记忆演进：高频出现的修正模式，通过反思被转化为长期记忆中的业务规则或约束条件，供Planner在生成新Specification时默认引用。
        [2]. 提示词微调 (受限演进)：仅当记忆演进无法覆盖，且经AUD AGENT和人工审批后，方可启用DSPy理念对特定角色的“规划”提示词进行受控微调。所有微调版本纳入Git LFS管理，支持一键回滚。
    6. AGENT能力扩展分为“内建能力”与“外部工具”：
        [1]. 内建能力：AGENT天然具备读写本地结构化数据（JSON）、操作文件系统（需用户粒度授权）、发送通知等基础能力。
        [2]. 外部工具：AGENT可按任务需求从“工具箱”中选择专业分析工具，并严格按照工具的输入/输出契约执行。所有外部工具调用均在安全沙箱中运行。
    7. 支持企业利益最优的AGENT间协作，高级代理CEO AGENT拥有协作仲裁最终决定权，但用户可以观测和更改该决策。
    8. 强制安全与鲁棒性控制：
        [1]. 视角隔离：AGENT仅从各自角色视角调用工具和讨论问题，严禁泛化讨论。
        [2]. 权限控制：任何对本地文件系统、敏感API的访问都需用户按最小权限原则（PoLP）授权，并可设定访问范围。提供类似Claude Code的`/permissions`命令管理权限。
        [3]. 鲁棒性训练：系统定期模拟工具调用超时、数据残缺等“环境噪声”进行压力测试，并将应对策略固化为AGENT的默认行为。
        [4]. 注入防御：对所有用户输入和外部数据源执行前置清洗与注入检测。
    9. CEO AGENT内置“裁决状态机”作为仲裁器，设定最大辩论轮次。辩论基于各AGENT提交的“子规划”而非结论。随着轮次增加，降低AGENT的Temperature Decay强制要求AGENT提出具体规划修改意见，而不是单纯否定。若未达成一致，生成Plan A（保守版）和Plan B（激进版）两套方案。CEO AGENT按偏好矩阵决策Plan A/B，同时暂停10秒提供用户介入裁决窗口，超时用户无动作则以CEO AGENT决策为准。
    10. AUD AGENT采用“旁路监听+异步阻断”模式，不参与生成。其核心职责是基于“任务执行规格说明书 (Specification)”进行审计，检测实际执行是否符合规划（检测“规划偏差”）。发现偏差通知相关AGENT纠错，若3次重试失败，暂停任务人工纠错，之后从任务暂停点继续任务。
    11. 支持双层记忆架构：
        [1]. 短期工作记忆：利用“公共黑板”机制（基于LangGraph StateSnapshot和Redis）管理当前会话内的上下文、中间变量和推理轨迹，支持严格回溯。
        [2]. 长期演化记忆：引入基于本地文件系统或向量数据库的持久化记忆模块，结构化存储反思日志、专家修正轨迹、成功/失败的任务Specification。
    12. 支持Agent推理过程的可解释性可视化。将结构化推理链（Chain-of-Thought）作为每次规划、执行、反思的标准输出。前端提供“决策路径图”，可动态展示决策依据、关键因素和置信度。
    13. 移除独立的ToT机制。可解释性由标准化的推理链保证。当决策置信度低于阈值（默认0.6）时，系统高亮显示推理链中的冲突节点或不确定性来源，并引导用户介入。
    14. 建立全方位的可观测性与成本效能监控体系，通过可视化仪表盘展示：
        [1]. 性能指标：任务完成率、平均规划-执行时间、工具调用成功率。
        [2]. 成本指标：按AGENT/按任务/按会话统计的Token消耗、API成本、沙箱计算资源消耗，联动工具箱的成本熔断机制。
        [3]. 质量指标：用户修正率、审计通过率、长期记忆命中率。
        [4]. 行为分析：展示AGENT的决策路径热点、常见错误类型、协作网络拓扑。
}

D2
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT

1. 输入与全景感知
    [1]. 输入经系统处理结构化为JSON格式的数据，首先构建标准全景视图（含关键指标趋势、异常波动、战略假设变量、关联图谱），再基于视图挖掘细节
    [2]. 支持动态视角切换机制：当细节挖掘发现与全景视图严重矛盾时，自动触发全景视图重构，重构过程需记录变更轨迹并提交AUD AGENT审核
    [3]. 建立输入数据质量预检：自动检测数据完整性、时效性、一致性，对低质量数据标注置信度衰减系数

2. 任务分解与协作架构
    [1]. 建立任务IR（中间表示）标准：每个任务包含任务ID、类型、输入/输出Schema、依赖任务列表、优先级、执行状态、超时策略
    [2]. 采用分层协作架构：CEO AGENT作为指挥官负责目标解析与全局任务分解，各专业Agent并行执行子任务，调度器负责资源分配与执行监控
    [3]. 自动生成协作依赖图：基于BLM/BEM阶段定义标准依赖关系，支持动态调整与优化，可视化展示任务间数据流与控制流
    [4]. 支持柔性视角隔离：定义核心视角（必须遵守）和扩展视角（可协商），对跨领域问题组建临时联合分析组，建立视角冲突解决协议

3. AGENT增强工作流
    [1]. 双阶段思考模式：阶段一并行识别关键约束（识别率>80%），阶段二逐步细化形成完整决策，整体采用“全局→局部”路径
    [2]. 工作流定义：[感知]环境初始化与上下文加载->[规划]生成任务执行图->[执行]按拓扑顺序调度子任务->[验证]结构化检查输出->[反射]错误分析与修正->[闭环]Early Stopping机制（置信度达标且目标达成时提前终止）
    [3]. 结构化反思机制：每次反思输出包含错误识别（类型/位置）、修正策略（具体修改方案）、预防措施（避免再次发生）、置信度评估
    [4]. 并行执行优化：无依赖子任务自动并行执行，依赖子任务采用流式处理，整体协作效率通过成本模型持续优化

4. 用户反馈闭环
    [1]. 建立反馈标准化协议：将用户反馈转化为结构化修正日志（修正前/后值、修正原因、修正类型、用户角色、时间戳）
    [2]. 实现即时反馈响应机制：设置专用反馈通道，承诺24小时内处理并回复，紧急反馈设置加急处理流程
    [3]. 反馈聚合分析引擎：定期挖掘高频反馈模式（每周/每月），自动生成优化建议并触发提示词微调，优化效果需通过A/B测试验证

5. 提示词工程与演进
    [1]. 采用双轨制演进策略：常规演进基于用户反馈自动微调（周频），重大演进必须通过战略方法论不变性校验（月频）
    [2]. 建立提示词版本控制：所有Prompt纳入Git LFS管理，与系统版本绑定，支持一键回滚至任一历史稳定版本
    [3]. 提示词契约测试：每次优化后自动运行Strat-Bench基准测试集，通过率<90%自动拒绝上线，并生成差异分析报告
    [4]. Prompt Optimizer基于DSPy理念，但优化目标必须多目标权衡（结构完整性、一致性、成本效率），严禁单一指标优化

6. 工具调用与集成
    [1]. 工具选择优化器：基于历史成功率（权重0.4）、Token成本（权重0.3）、执行时长（权重0.3）多目标优化选择工具，支持动态权重调整
    [2]. 工具组合推荐：针对17个战略工具预定义最佳组合模板（如SWOT+TOWS自动组合），支持用户自定义模板
    [3]. 工具调用轨迹分析：记录每次调用的成功/失败模式、参数设置、返回结果，持续优化选择策略，失败模式入库作为少样本示例
    [4]. 严格遵循工具契约：每个工具调用前验证输入Schema，调用后验证输出Schema，Schema违规模3次自动熔断该工具

7. 仲裁与决策机制
    [1]. CEO AGENT内置裁决状态机，设定最大辩论轮次（默认5轮），采用三阶段辩论：发散阶段（Temperature=0.8）、收敛阶段（Temperature=0.5）、裁决阶段（Temperature=0.2）
    [2]. 辩论规则：随着轮次增加强制要求提出具体修改意见而非单纯否定，第3轮后必须附带数据或逻辑支撑
    [3]. 多方案生成：若未达成一致，生成Plan A（保守版）、Plan B（激进版）和Plan C（AI融合版），CEO按偏好矩阵决策
    [4]. 用户介入窗口：暂停60秒提供用户裁决，窗口期可视化展示三套方案的差异对比（风险/收益/资源需求），超时默认采纳CEO决策
    [5]. 动态仲裁权转移：根据任务领域（如财务决策）自动将仲裁权临时转移给最相关专业Agent（CFO），CEO保留监督权和最终否决权

8. 审计与一致性保障
    [1]. AUD AGENT采用“旁路监听+异步阻断”模式，不参与生成，审计范围包括原文一致性检查、逻辑自洽性检查（跨工具结论矛盾）、商业常识验证
    [2]. 建立幻觉量化标准：定义置信度阈值（<0.6标记可疑）、引用缺失率（>30%标记警告）、逻辑矛盾指数（基于规则引擎检测），综合评分<0.7触发纠错
    [3]. 智能重试策略：首次失败通知纠错，二次失败自动切换验证方法或调整提示词，三次失败暂停任务人工介入，从暂停点恢复后继续执行
    [4]. 审计日志强制记录：所有审计发现、纠错过程、重试轨迹全量存储，作为模型性能分析的基础数据

9. 记忆与上下文管理
    [1]. 分层记忆架构：工作记忆（当前会话，有效期24h）、短期记忆（最近项目，有效期30天）、长期记忆（战略知识库，永久存储），各层有独立的存储引擎和检索策略
    [2]. 公共黑板机制：基于LangGraph的StateSnapshot + Redis存储，仅传递关键结论和状态哈希，完整推理轨迹归档至数据湖，支持按需解压缩回溯
    [3]. 关键结论提取标准：重要性评分（1-5分，基于对最终决策的影响度）、置信度（0-1）、引用源（文档切片ID）、时效性（有效日期范围），综合评分>3.5才写入公共黑板
    [4]. 长期记忆演化：将用户修正日志、决策复盘结果、执行结果差值反向固化为少样本提示词，形成动态演进的战略知识库，每季度触发知识蒸馏与去重

10. 可解释性与溯源
    [1]. 结构化证据包标准：每次分析打包输出全量元数据，包括输入摘要哈希、任务分解图、工具调用轨迹（含版本）、关键中间变量、置信度评分、引用源切片ID列表
    [2]. 交互式溯源树：支持从结论逐层展开至原始数据，展示“数据来源→计算逻辑→中间结论→最终结论”完整推理链，每个节点支持查看详细信息
    [3]. 反事实推理引擎：支持“如果改变某假设/权重/约束，结论会如何变化”的模拟分析，输出对比报告和风险提示
    [4]. 思维链溯源树（ToT）分级触发机制：低置信度（0.4-0.6）触发局部回溯（当前子任务），中置信度（0.3-0.4）触发全局回溯（整个阶段），低置信度（<0.3）触发人工介入

11. 性能监控与行为分析
    [1]. 建立三维度指标体系：效率维度（任务完成率、平均时长、Token消耗、工具调用成功率）、质量维度（修正率<15%、幻觉率<5%、一致性得分>0.8）、成本维度（单任务API成本、计算资源消耗）
    [2]. 构建战略规划专用基准测试集（Strat-Bench）：包含20+专家标注的战略分析场景，覆盖BLM/BEM全阶段，作为版本迭代的准入测试标准
    [3]. 三级预警机制：黄牌（指标下降15%触发分析）、红牌（下降30%触发人工review）、停用（下降50%且连续三天，自动回滚至前一稳定版本）
    [4]. 行为分析框架：定期（周/月）生成Agent行为报告，包含成功率趋势、高频错误类型、修正模式分析、成本分布，为系统优化提供决策依据

12. 主动式能力
    [1]. 引入心跳机制：支持周期性自动唤醒（可配置频率），主动检查战略档案库中的待办事项、偏差预警、周期性任务，无需用户提问即可主动沟通
    [2]. 战略偏差预警：实时监控实际执行数据与SP/BP规划的偏差，偏差超过阈值（默认10%）自动触发预警，生成偏差分析报告并推送相关AGENT
    [3]. 主动知识更新：当检测到行业报告、市场数据、政策法规更新时，自动触发电请相关AGENT评估影响，重要影响主动推送CEO

13. 安全与防御
    [1]. 视角越界检测：所有工具调用前验证是否符合角色视角定义，跨视角调用需经CEO批准并记录审计日志
    [2]. 辩论过热保护：自动检测辩论轮次>5或Token消耗超限，强制暂停并请求人工介入，避免无限循环
    [3]. 幻觉累积阻断：当同一任务连续3次生成低置信度结论，自动锁定相关AGENT并报警，人工解锁后方可继续
    [4]. 敏感数据脱敏：审计日志和反馈记录自动脱敏处理（个人可识别信息、商业机密），脱敏策略定期评审更新
}

D3
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT

    1. 输入与记忆管理
        [1]. 环境初始化模块统一加载经系统处理的结构化JSON数据，首先生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），存入公共黑板的“全景摘要区”。
        [2]. 摘要数据附带时效性标记与数据版本哈希，AGENT首次推理前必须读取；支持用户指令或数据更新事件触发摘要强制刷新。
        [3]. 构建会话级混合记忆容器：短期记忆（公共黑板+当前轮次上下文）与长期记忆（战略档案库RAG检索），AGENT可主动检索与任务相关的历史案例与修正轨迹。

    2. 任务分类与角色映射
        [1]. 明确定义四类原子任务：分析型（趋势判断、差距分析）、生成型（方案起草、报告撰写）、验证型（一致性检查、合规校验）、决策型（方案选择、资源分配）。
        [2]. 建立角色-任务匹配矩阵（示例：CFO不承担生成型任务，仅负责分析与验证；CEO负责决策型与仲裁型任务），矩阵存储于中央技能仓库，初始化时加载至各AGENT上下文。
        [3]. 任务由指挥官（Planner）进行拆解与分配，AGENT无权自主选择任务类型，确保分工明确与视角隔离。

    3. AGENT原子工作流（ReAct+Verify）
        [1]. 标准化执行循环：感知（读取黑板与记忆）→ 思考（LLM推理生成行动计划）→ 执行（调用工具或执行代码）→ 验证（调用AUD AGENT或规则引擎校验结果）→ 反射（分析验证反馈，决定继续、修正或终止）→ 写回（更新黑板与记忆）。
        [2]. 内置循环熔断机制：设定最大迭代次数（默认5次）与单任务Token预算上限，超限时强制终止，记录失败状态至演进日志。
        [3]. 每个循环步骤必须输出结构化日志，包含步骤ID、输入哈希、输出摘要、耗时、Token消耗，供审计与可视化使用。

    4. 用户反馈与闭环采集
        [1]. 实施三级反馈采集机制：
            a. 隐式反馈：自动记录用户采纳率、修改耗时、光标轨迹（前端埋点）；
            b. 显式反馈：提供点赞/点踩按钮，并增设“幻觉”（Pinocchio）专用反馈按钮；
            c. 结构化修正：支持用户在Checkpoint界面直接修改参数/Schema，系统记录修正前后差异快照。
        [2]. 所有反馈必须关联到具体执行步骤ID与数据版本，写入修正轨迹库，作为后续优化的训练样本。

    5. AGENT提示词与模型动态优化
        [1]. 区分两种优化模式：
            a. 离线测试时微调（TTFT）：定期（如每日）基于修正轨迹库合成训练数据，对轻量级AGENT专用模型（如7B级别）进行LoRA微调，实现针对企业特定风格的“敏捷适应”。
            b. 在线提示词优化：基于DSPy理念，从修正轨迹中提取Few-Shot样本，动态调整AGENT提示词中的示例与约束条件。
        [2]. 优化触发条件与置信度绑定：当任务置信度低于阈值（默认0.6）或用户修正率高于阈值（默认15%），自动将当前任务加入优化候选队列。
        [3]. 所有提示词版本纳入Git LFS管理，支持一键回滚至历史稳定版本，确保方法论不变性。

    6. 工具调用与权限管控
        [1]. 工具选择权收归Planner/指挥官，AGENT仅负责执行分配的工具（决策-执行分离），执行时必须携带任务ID与AGENT身份标识。
        [2]. 建立工具访问控制列表（ACL），与AGENT角色绑定（如CFO无权调用PESTEL分析工具，CMO无权调用现金流折现工具），初始化时加载至工具调用中间件。
        [3]. 每次工具调用必须记录输入参数摘要、输出结果哈希、执行状态，写入公共黑板的“工具调用日志区”。

    7. AGENT间协作与仲裁机制
        [1]. 协作模式采用指挥官-专家架构：CEO AGENT担任指挥官角色，负责意图拆解、任务分配与最终仲裁；其余AGENT为专家角色，负责专项执行。
        [2]. 企业利益最优决策由CEO基于预设的“决策偏好矩阵”（包含风险偏好系数、增长优先系数、成本敏感系数）输出，决策理由必须结构化呈现（引用矩阵维度）。
        [3]. 用户可观测决策过程，介入界面提供对比视图（Plan A vs Plan B），高亮显示差异点（风险值、预期收益、资源需求），支持用户一键覆盖CEO决策。

    8. 视角隔离强制实施
        [1]. Schema级隔离：每个AGENT的输入/输出Pydantic Schema仅包含其职责相关的字段，由契约测试门禁强制执行（Schema校验失败则任务终止）。
        [2]. 工具级隔离：基于ACL限制AGENT可调用的工具范围（详见第6条）。
        [3]. 数据级隔离：AGENT仅能访问经过行/列权限过滤的数据视图（如CFO可读财务数据但不可写，CHO可读人力数据但不可读市场数据）。

    9. CEO裁决状态机与动态辩论策略
        [1]. CEO内置裁决状态机，设定最大辩论轮次（默认3轮）。随着轮次增加，自动调整辩论策略：
            a. 第1轮：正常温度（如0.7），鼓励发散；
            b. 第2轮：若未达成一致，触发“观点对抗”，强制要求提出反对意见；
            c. 第3轮：若仍僵局，自动生成Plan A（基于保守系数）和Plan B（基于激进系数）。
        [2]. 双方案生成后，CEO按决策偏好矩阵输出推荐方案，并暂停（默认30秒，可配置）提供用户介入窗口。
        [3]. 用户超时无动作，则以CEO推荐方案为准；用户提前介入，立即终止状态机并应用用户选择。

    10. AUD AGENT旁路审计与三重验证
        [1]. AUD AGENT采用“旁路监听+异步阻断”模式，不参与主流程生成，通过订阅公共黑板事件进行实时审计。
        [2]. 实施三重验证：
            a. 事实一致性：校验生成内容与引用文档切片是否矛盾（溯源验证）；
            b. 逻辑自洽性：基于业务状态机（如战略规划阶段顺序）检测前后矛盾；
            c. 业务合规性：调用规则引擎（内置财务/合规知识库）检测是否违反企业红线。
        [3]. 发现违规时，AUD写入审计日志至公共黑板，并置位“需修正”标志。执行AGENT在反射阶段读取该标志，触发自我修正循环。
        [4]. 同一任务连续3次审计失败，暂停任务并通知用户人工介入；恢复后从暂停点继续执行（保留已通过步骤的中间产物）。

    11. 公共黑板与状态快照管理
        [1]. 定义公共黑板标准化Schema，必须包含：
            a. 全景摘要区（数据版本、关键指标）；
            b. 任务状态区（当前阶段、待办队列、已完成步骤ID）；
            c. 中间变量区（关键计算结果哈希值，具体值存Redis）；
            d. 审计日志区（AUD检测记录）；
            e. 工具调用日志区。
        [2]. 利用LangGraph的StateSnapshot机制，配合Redis持久化，每完成一个原子步骤自动创建快照。
        [3]. 支持回滚至任意历史快照：回滚后，系统自动从待办队列中恢复未完成任务，基于中间变量哈希值重建上下文（完整变量需从Redis加载），避免全量重算。

    12. 可解释性可视化与决策溯源
        [1]. 每次任务完成后，系统自动生成“决策溯源树”JSON，包含：
            a. 任务拆解节点（Planner输出）；
            b. 工具调用节点（输入/输出摘要、耗时）；
            c. 中间结论节点（置信度、依据文档ID）；
            d. 最终输出节点。
        [2]. 前端可视化接口支持交互式展开（Drill-down），点击任意节点可查看原始证据（文档片段截图/代码执行日志）。
        [3]. 溯源树与最终输出报告（PDF/HTML）绑定，确保结论可追溯至原始数据。

    13. 思维链溯源与置信度回溯
        [1]. 置信度计算下沉至每个子任务：Executor执行完成后立即计算置信度（基于AUD验证结果、数据完整度、模型自评）。
        [2]. 定义两级回溯机制：
            a. 局部回溯（置信度<0.6）：当前AGENT重新规划子任务执行路径，最多重试2次；
            b. 全局回溯（局部回溯失败或置信度<0.3）：指挥官介入，调整任务拆解方案，重新分配子任务。
        [3]. 所有回溯路径与决策点均记录至决策溯源树，确保过程透明。

    14. AGENT性能监控与行为分析
        [1]. 建立三层监控指标体系：
            a. 任务级：成功率、平均耗时、Token消耗、修正率、成本/任务；
            b. AGENT级：工具调用成功率、幻觉率（AUD检出次数/总调用）、平均响应时间、排队长度；
            c. 系统级：并发执行数、资源利用率、日成本总额。
        [2]. 基于历史数据构建动态性能基线，超出3σ阈值自动触发告警（钉钉/邮件），并支持自动弹性伸缩（增加AGENT实例）。
        [3]. 所有监控数据可视化展示于健康度仪表盘，支持按AGENT角色、任务类型、时间范围多维筛选分析。
}

D4
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT

    1.  **结构化全景感知**
        [1]. 输入为系统处理后的结构化JSON数据及历史战略档案库快照。
        [2]. AGENT初始化时，强制生成符合`PanoramaView` Schema的全景摘要，必须包含：当前任务目标、输入数据指纹（哈希值）、以及关联的“历史关键假设与实际执行偏差”摘要。此摘要作为后续所有分析和决策的基础上下文。

    2.  **规范驱动型任务执行 (SDD范式)**
        [1]. 废除单纯的“自主分析”，全面实施“计划-执行-验证”闭环。所有复杂任务（包括单角色分析和多角色协作）必须由CEO AGENT（或专门的Planner AGENT）首先生成一个机器可读的`TaskPlan`（包含子任务、工具映射、数据依赖、验收标准）。
        [2]. `TaskPlan`需经AUD AGENT进行初步的契约校验，并可选择性地提交用户确认后，方可进入执行阶段。

    3.  **增强型AGENT工作流 (L0范式)**
        [1]. 标准工作流升级为：**计划 -> 感知 -> 思考 -> 执行 -> 观察 -> 验证 -> 反射**。
        [2]. **强制验证闭环**：在执行工具或生成内容后，必须根据`TaskPlan`中的验收标准进行自我验证（如调用`validate_output`工具）。验证失败则触发“反射”环节，进行错误分析并尝试修正，记录失败模式至演进日志。
        [3]. 利用Notebook Agent (NB-Agent) 架构，支持Think-Code-Observe循环，通过Jupyter Kernel执行代码，并将关键变量与上下文双向绑定 [citation:1]。

    4.  **用户反馈驱动的知识固化 (CLAUDE.md机制)**
        [1]. 支持用户通过Checkpoint或命令行对AGENT的中间产物和最终结果提出修正。
        [2]. 系统自动记录“修正轨迹”（用户修正前后的状态差异Delta），并存入修正轨迹库。
        [3]. 建立全局性的、版本控制的**AGENT行为规范文件 (`CLAUDE.md`)**。通过分析高频修正模式，定期（在人类监督下）将团队共识、常见错误规避指南、代码风格等知识更新至`CLAUDE.md`，作为所有AGENT在规划和执行时的默认参考 [citation:2]。

    5.  **双轨演进系统 (Prompt & Knowledge)**
        [1]. **局部演进**：基于修正轨迹库，利用DSPy理念，为特定类型的任务动态优化Planner的少样本示例。
        [2]. **全局演进**：基于AUD AGENT的季度审计报告和用户高频修正数据分析，在人工审核通过后，触发对`CLAUDE.md`的更新。演进过程需通过多目标优化（准确性、一致性、成本），确保方法论不变性。

    6.  **工具-服务分离架构 (关注点分离)**
        [1]. AGENT通过**工具层（Tool Layer）**与系统交互。工具层是LLM的薄适配器，负责将LLM的简易指令转换为对下层**服务层（Service Layer）**的标准化调用 [citation:6]。
        [2]. **服务层**封装所有复杂的业务逻辑（如现金流折现、运筹优化、文件I/O），返回丰富的领域对象。服务层可被AGENT工具、CLI、测试脚本等多种客户端复用。
        [3]. 所有涉及系统权限的操作（文件读写、网络请求、外部命令执行）必须在服务层进行，并受安全边界`BOUNDARY.md`的严格约束。

    7.  **量化仲裁与辩论协议**
        [1]. 多AGENT协作时，CEO AGENT作为仲裁者，其决策必须基于对**量化仲裁器**的调用结果，例如“风险偏好评估器”、“财务影响模拟器”、“市场份额预测器”等。
        [2]. 当启用“红蓝辩论”时，超过设定轮次后，强制辩论双方按`StructuredArgument` Schema提交结构化论点，包含：核心主张、证据链（数据源引用）、量化影响预估、以及对对方主张的具体反驳点。
        [3]. CEO的最终决策、所调用的仲裁器输出、以及关键辩论片段，必须被打包进最终的证据包，实现完全可追溯的裁决。

    8.  **架构级视角隔离**
        [1]. 视角隔离不再仅依赖提示词，而是通过为每个角色的AGENT分配**职责受限的工具包**来实现。例如，CFO AGENT的工具包中不包含访问产品技术路线图的工具。
        [2]. 任何越权调用工具的尝试将被AUD AGENT拦截并记录为安全事件。

    9.  **结构化辩论与裁决状态机**
        [1]. CEO AGENT内置“裁决状态机”，设定最大辩论轮次。随着轮次增加，强制使用`StructuredArgument` Schema进行辩论，并应用Temperature Decay策略。
        [2]. 若无法达成一致，AGENT需生成Plan A（保守）和Plan B（激进）两套完整方案。CEO调用量化仲裁器，基于风险-收益偏好矩阵选择方案。
        [3]. 输出最终方案后，系统暂停并提供10秒用户介入窗口。超时则以CEO决策为准，并将整个辩论与裁决过程写入证据包。

    10. **增强型审计官 (AUD AGENT) 四重校验**
        [1]. AUD AGENT以“旁路监听+异步阻断”模式运行，不参与生成，但进行**四重强制校验**：
            a. **事实一致性**：校验生成内容是否忠实于检索到的文档。
            b. **Schema合规性**：校验输出是否符合Pydantic V2数据模型。
            c. **工具调用审计**：校验工具调用是否在白名单内、参数是否符合契约。
            d. **业务逻辑一致性**：调用一致性校验仲裁器，检查是否违背财务常识或领域事实库。
        [2]. 任意校验失败，AUD AGENT发送结构化错误报告给相关AGENT纠错。若连续3次重试失败，则暂停任务，请求人工介入，并从暂停点利用LangGraph的StateSnapshot恢复。

    11. **结构化公共黑板 (Blackboard)**
        [1]. 多AGENT协作通过“公共黑板”进行异步信息共享。黑板基于向量数据库和Redis StateSnapshot实现。
        [2]. 所有写入黑板的信息必须遵循`BlackboardEntry`数据模型：包含写入者、时间戳、数据类型（如`EVIDENCE`、`HYPOTHESIS`、`INTERIM_CONCLUSION`）、数据内容（文本或指向大对象的指针）以及置信度。
        [3]. 支持语义检索，AGENT可基于内容而非仅关键词查询黑板历史信息。

    12. **结构化推理轨迹与可解释性**
        [1]. 强制每个AGENT的核心行动（尤其是决策点）输出结构化的`ReasoningTrace`，包含：执行计划ID、调用的工具及其输入输出快照、关键中间思考链、置信度评分。
        [2]. 前端可视化引擎负责将这些轨迹渲染成交互式溯源树，用户可点击任一节点展开详细证据，实现“决策依据”的端到端追溯 [citation:5]。

    13. **决策回溯与辩解机制**
        [1]. 当关键决策的置信度低于阈值（默认0.6）或AUD AGENT校验失败时，系统自动从LangGraph的StateSnapshot加载历史状态。
        [2]. 触发一个独立的“反思AGENT”，其任务不是重新决策，而是分析失败原因。它输出一份“辩解报告”，阐述失败是由于数据不足、内部逻辑冲突、还是外部假设变化，并建议下一步行动（如请求新数据、人工介入、或启用备用方案）。

    14. **可观测性与行为分析**
        [1]. 建立统一的Agent可观测性框架，实时监控核心指标：**北极星指标 - “人工修正率”**；诊断指标 - 任务完成率、平均决策时长、Token消耗、工具调用成功率、AUD AGENT告警次数。
        [2]. 构建行为分析仪表盘，可视化各AGENT的协作网络、决策热点和常见错误模式。
        [3]. 建立“技能回顾”工作流，当某类任务的人工修正率连续超标（默认>15%）时，自动通知系统管理员，提示其审查相关`CLAUDE.md`规范、Planner示例或工具设计。
}

D5
{
三、AGENT：基于本地部署与群体智能的企业战略规划执行体
系统核心由6个核心角色AGENT（CEO/COO/CMO/CTO/CFO/CHO）及1个联席审计官AUD AGENT组成。AGENT体系设计哲学遵循“本地主权、规格驱动、并行协作、全局规划、韧性执行”五大原则。

1.  **环境与数据感知**
    [*]. 输入数据为经系统处理的结构化JSON格式。
    [*]. **构建动态全景视图**：初始化时，系统需生成包含关键指标趋势、数据质量热力图（标记缺失/异常值）、不确定性区间的动态摘要，并实时监听数据源更新，触发视图自动刷新，为AGENT提供具备“风险感知”的决策环境。

2.  **原子化任务与并行协作**
    [*]. AGENT任务分解为自主分析、协作决策两大类，并建立**原子化任务库**，将复杂指令拆解为最小可执行单元（如“计算ROI”、“提取竞品名单”），支持多AGENT并行领取与执行。
    [*]. **引入“舰队指挥官”协作模式**：CEO AGENT转型为指挥官，负责战略意图理解、宏观任务拆解与资源分配。它将独立子任务下发给相应AGENT后，转为异步监控状态。各AGENT并行工作，通过“心跳机制”向CEO汇报进度，仅在发生冲突或需要资源协调时，CEO才介入仲裁，彻底告别线性低效的会议式协作。

3.  **规格驱动的工作流 (SDD-React)**
    [*]. AGENT基本工作流升级为**SDD-React模式**：[感知] -> [规格规划] -> [审计校验] -> [执行] -> [反射]。
    [*]. **[规格规划]取代[思考]**：AGENT收到任务后，首先生成一份机器可读的规格文件（`PLAN.md`），明确任务拆解步骤、拟调用工具契约（输入/输出Schema）、关键假设和预期产出格式。
    [*]. **[审计校验]作为强制门禁**：规格文件需经AUD AGENT或内置验证器校验，确保逻辑完备、契约合规后，方可进入执行阶段。严禁无计划的“裸奔式思考”。

4.  **长效记忆与知识固化 (CLAUDE.md机制)**
    [*]. 支持用户对AGENT分析结果的反馈与修正。
    [*]. **建立组织级记忆库**：所有用户修正、审计发现、项目复盘教训，均通过结构化或自然语言方式，固化至全局共享的`STRATEGIC_KNOWLEDGE.md`文件。
    [*]. AGENT在执行任何任务前，必须自动加载并遵守该文件中的规则，实现“一次犯错，永不重犯”，使整个系统成为具备自我进化能力的有机体。

5.  **提示词与工作流双轨演进**
    [*]. 保留基于DSPy理念的**Prompt Optimizer**，根据反馈自动微调提示词参数，优化微观表达。
    [*]. **引入工作流进化器 (Workflow Optimizer)**：定期分析失败案例与用户修正轨迹，自动优化CEO的任务拆解策略、工具链的DAG编排，实现宏观规划逻辑的迭代升级。

6.  **本地化工具调用与物理权限 (Skills生态)**
    [*]. AGENT不仅可调用系统内置的17个战略分析工具箱，还可通过**MCP协议**发现并调用其运行环境中的**本地操作系统级工具**，包括但不限于：Shell命令、文件读写、Excel宏执行、浏览器自动化、本地编译器。
    [*]. **实施硬件级沙箱隔离**：为每个AGENT会话创建独立的轻量级容器或工作目录，严格执行视角隔离。AGENT对本地资源的访问权限，严格限制在该会话沙箱内，任务结束立即销毁，确保数据安全与系统稳定。

7.  **红蓝对抗与方案整合 (超越裁决)**
    [*]. CEO AGENT内置“冲突解决状态机”。当多AGENT协作无法达成一致时，不强求统一。
    [*]. CEO要求各方（如激进派与保守派）输出完整的**Plan A（激进版）** 和**Plan B（保守版）** 方案，并附带详细的风险评估报告。
    [*]. CEO或用户可根据偏好矩阵进行选择，或指令系统整合两方优点，生成**Plan C（融合版）**。系统保留10秒用户介入窗口，超时则以CEO决策为准。

8.  **首席审计与风险官 (超越幻觉检测)**
    [*]. AUD AGENT升级为首席审计与风险官，采用“旁路监听+异步阻断”模式。
    [*]. 其职责从“事实一致性校验”扩展为**“逻辑合理性校验”**，加载领域事实库和财务常识验证器。若发现利润率与成本矛盾等逻辑硬伤，强制阻断并通知AGENT纠错。
    [*]. 若发现输出质量偏低但无硬伤，则标记为“低置信度”，触发深度思考机制。3次重试失败后，暂停任务并请求人工介入。

9.  **结构化记忆与交互式决策沙盘**
    [*]. 利用上下文压缩器与“公共黑板”机制，在黑板上存储“推理指纹”（关键假设、弃用路径），而非全量历史。
    [*]. **提供交互式可视化接口**：用户可查看动态的决策树（ToT）。不仅支持结论溯源，更支持**“What-if”推演**。用户可在可视化界面上直接修改某个中间节点的假设（如“将市场增长率调高5%”），系统实时重算后续所有节点的结果，预览战略变化。

10. **重思考模式与路径预演 (全局规划)**
    [*]. 对于复杂战略决策（如新市场进入），在任务启动前，强制AGENT进入**“重思考模式”**。
    [*]. AGENT需并行探索多条潜在解决路径，通过模拟推演评估每条路径的成功率、资源消耗和潜在风险，最终选择全局最优路径作为主执行方案，从根本上避免“试错式”的低效回溯。

11. **多维性能监控与鲁棒性分析**
    [*]. 建立Agent性能监控与行为分析框架，指标需细化至：规划冗余度（平均工具调用次数）、修正率（按工具/AGENT维度）、路径效率（实际路径 vs. 最优路径比）、抗噪鲁棒性（面对错误指令/数据残缺的成功率），确保持续优化方向精准有效。
}

---------------------
Q1
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入处理与全景构建
        [1].输入数据经RAG系统结构化为JSON Schema，AGENT首先调用摘要服务生成多粒度摘要（粗粒度：关键指标趋势；细粒度：异常波动与关联事件）
        [2].实施摘要质量门禁：摘要置信度评分（基于RAG相关性+完整性检测）<0.7时，自动触发原始文档切片回溯机制，确保关键信息不丢失
        [3].构建跨文档关联知识图谱：识别实体关系与隐含逻辑链，支持"全景视图→细节挖掘"的双向导航
        [4].动态摘要粒度控制：SP阶段默认使用粗粒度摘要（降低Token消耗），BP阶段自动切换至细粒度摘要（提升执行精度）
    2.AGENT任务分类与能力边界
        [1].定义两大任务类型：①角色自主分析（基于本职能数据独立完成）；②跨角色协作决策（需多AGENT协同）
        [2].为每个AGENT建立能力矩阵（Capability Matrix）：明确可调用工具集、数据访问权限、决策阈值（如CFO单笔支出审批上限）
        [3].实施任务-角色匹配度评分：匹配度<0.6时自动触发跨角色协作流程，由CEO AGENT协调资源分配
        [4].建立角色冲突解决协议：决策优先级链（CEO > 职能AGENT > AUD），冲突时按优先级链仲裁
    3.AGENT核心工作流（增强版ReflAct）
        [1].感知（Perceive）：加载环境上下文（用户偏好/行业模板/历史修正轨迹）+ 初始化会话级沙箱
        [2].预反思（Pre-Reflect）：执行前风险评估，检索历史失败案例库，识别潜在陷阱并生成规避策略
        [3].思考（Think）：LLM推理生成候选行动，输出必须符合Pydantic V2 Schema（含置信度评分）
        [4].执行（Act）：解析LLM输出，决策任务完成或调用工具；工具调用前执行契约验证
        [5].观察（Observe）：捕获工具输出/代码执行结果，进行L1-L3幻觉检测（事实/逻辑/常识）
        [6].反思（Reflect）：评估行动效果，生成反思日志；反思质量评分<0.5时触发强制工具辅助修正
        [7].循环终止条件：①最大循环次数（默认5）；②置信度收敛（Δ<0.05持续2轮）；③用户手动终止
        [8].输出构建：打包证据包（含输入哈希/工具版本/中间变量/置信度/引用源）并释放资源
    4.用户反馈与修正闭环
        [1].反馈分类处理：A类（参数微调）→ 直接更新上下文；B类（约束增删）→ 触发Planner重规划；C类（逻辑重构）→ 启动ReflAct全流程
        [2].反馈价值评估：基于修正后任务成功率提升幅度计算价值分，高价值反馈（Δ成功率>15%）自动固化为Few-Shot样本
        [3].专家反馈加权：记录用户角色/资历，专家反馈权重×1.5，加速高质量知识沉淀
        [4].反馈溯源追踪：所有修正操作记录完整审计日志，支持"修正-结果"因果链回溯
    5.AGENT Prompt工程与自动优化
        [1].角色化Prompt模板：按AGENT职能预置Identity.md（角色定义）+ Boundary.md（安全边界）+ Tools.md（工具契约）
        [2].集成DSPy双优化器：①MIPROv2优化Few-Shot示例选择；②GEPA优化推理路径参数（Temperature/Top-p）
        [3].多目标帕累托优化：质量（置信度）> 成本（Token）> 速度（延迟），用户可调整权重
        [4].演进安全锁：Prompt变更必须通过Strat-Bench回归测试且修正率增幅<5%，否则自动回滚
        [5].版本管理：所有Prompt纳入Git LFS，与系统版本绑定，支持一键回滚至历史稳定版
    6.工具动态选择与组合优化
        [1].分层工具选择策略：①简单任务（单目标）→ 启发式规则映射；②复杂任务（多目标）→ Planner生成工具链DAG
        [2].工具组合成本-收益分析：评估工具链总Token消耗与预期置信度增益，收益/成本比<1.2时建议简化工具链
        [3].自动并行化：识别工具链中无依赖子任务，通过任务调度器并行执行，降低端到端延迟
        [4].工具选择日志：记录每次工具选择的决策依据（匹配度评分/历史成功率），用于优化选择策略
    7.AGENT间协作与仲裁机制
        [1].企业利益函数量化：E = α·股东价值 + β·员工福祉 + γ·社会影响（α+β+γ=1，可配置权重）
        [2].CEO仲裁透明度要求：所有仲裁决策必须输出利益函数各分量得分及权重依据
        [3].用户仲裁否决权：关键决策（如并购/重大投资）可配置"强制人工批准"开关，超时未响应按预设策略处理
        [4].协作冲突检测：监控多AGENT输出的一致性，冲突度>0.4时自动触发辩论流程
    8.视角隔离与受控信息共享
        [1].严格视角隔离：AGENT仅能访问本角色数据视图与工具集，调用非授权资源触发AUD告警
        [2].受控视角扩展机制：当任务需要跨视角数据时，触发"信息请求-审批"流程（基于预设规则自动审批或CEO人工审批）
        [3].视角污染检测：实时监控AGENT输出，污染度（非本角色观点占比）>15%时冻结AGENT并通知AUD
        [4].视角白名单：为每个角色预定义允许访问的跨域数据类型（如CFO可访问市场增长率用于现金流预测）
    9.CEO AGENT裁决状态机（增强版）
        [1].多轮辩论机制：初始Temperature=0.7，每轮基于辩论质量评分动态调整（高质量辩论维持高Temperature）
        [2].观点融合阶段：辩论后启动贝叶斯融合模型，生成Plan C（融合方案），非仅二选一
        [3].不确定性可视化：输出Plan A/B/C的置信区间、风险分布图及关键假设敏感性分析
        [4].用户介入窗口：生成多方案后暂停15秒（可配置），提供交互式决策界面；超时按企业利益函数自动选择
        [5].辩论质量监控：记录每轮新观点涌现数量，低质量辩论（连续2轮无新观点）强制终止并生成融合方案
    10.AUD AGENT幻觉检测与熔断
        [1].三层幻觉检测：L1（事实核查：对比战略档案库）；L2（逻辑一致性：规则引擎）；L3（常识验证：领域知识图谱）
        [2].动态重试策略：L1错误1次重试；L2错误2次重试+工具辅助；L3错误3次重试+启动红蓝辩论
        [3].幻觉根因分析：分类记录幻觉来源（RAG不足40%/工具错误30%/LLM偏差30%），驱动针对性优化
        [4].熔断机制：单任务幻觉率>30%或连续3任务幻觉率>20%时，自动冻结相关AGENT并告警
        [5].异步阻断优化：检测到高风险幻觉时，提前注入"验证提示"至AGENT上下文，预防性降低幻觉发生
    11.上下文管理与公共黑板
        [1].分层黑板架构：L1（实时变量：当前任务状态）；L2（阶段结论：SP/BP各阶段输出）；L3（长期记忆：战略档案库）
        [2].上下文压缩质量门禁：压缩后信息熵保留率>90%，关键实体/数字/因果链100%保留
        [3].StateSnapshot持久化：基于LangGraph机制，每阶段Checkpoint自动保存完整状态快照至Redis Cluster（非单点）
        [4].黑板版本控制：支持任意历史快照回滚，回滚操作记录完整审计轨迹
        [5].跨会话记忆同步：任务结束后，关键结论自动提取并固化至战略档案库（长期记忆）
    12.决策可解释性与可视化
        [1].分角色可视化视图：①高管视图（关键决策点+置信度+风险热力图）；②专家视图（完整推理链+工具输出）；③审计视图（全量元数据+时间戳）
        [2].交互式假设推演：用户可修改中间变量（如市场增长率±10%），实时观察对最终结论的影响
        [3].决策影响图（DID）：可视化展示关键因素权重及因果路径，支持"点击钻取"至原始数据源
        [4].置信度溯源：每个结论标注置信度来源（RAG支持度/工具计算精度/历史成功率）
    13.思维链溯源树ToT（渐进式）
        [1].置信度分层触发：0.7-0.8 → 轻量ToT（Beam Width=2，1轮反思）；0.6-0.7 → 标准ToT（Width=3，2轮）；<0.6 → 深度ToT（Width=5，3轮+红蓝辩论）
        [2].早停机制：当某分支置信度>0.85且领先第二名>0.2时，提前终止搜索并采纳该分支
        [3].ToT结果固化：成功ToT路径自动提取为"决策模式模板"，存入战略档案库供类似场景复用
        [4].计算成本控制：ToT总Token消耗设置硬上限（默认5000 tokens），超限时降级为单路径推理
    14.Agent性能监控与行为分析
        [1].核心KPI体系：①任务成功率；②平均置信度；③修正率；④Token效率（置信度/Token）；⑤协作冲突率
        [2].行为异常检测：基于历史基线，使用Isolation Forest检测异常模式（如某AGENT突然高频调用非本职工具）
        [3].性能-业务价值映射：将AGENT指标关联到战略输出质量（如BP可行性评分、执行偏差率）
        [4].自动Skill Review：修正率>15%持续3天或任务成功率下降>10%时，自动触发工具体重构流程
        [5].漂移检测：每周对比基线评估性能变化，检测到显著漂移（p<0.05）时告警并建议Prompt/工具链优化
    15.安全与合规增强（新增）
        [1].最小权限原则：每个AGENT的API调用权限按"需知原则"严格限制，网络访问仅允许白名单财经API
        [2].审计日志全链路：记录AGENT所有操作（输入/输出/工具调用/修正），日志加密存储且不可篡改
        [3].合规性检查：关键决策（如财务预测）自动附加合规声明（依据会计准则/行业规范）
        [4].数据脱敏：处理含PII/商业机密数据时，自动触发脱敏流程，脱敏规则可配置
    16.成本治理与熔断（新增）
        [1].三级熔断机制：①任务级（单次Token>10K或USD>$5）；②会话级（日配额>$50）；③重试级（连续失败>3次）
        [2].Token预算硬限：每个任务预分配Token预算，消耗>90%时触发预警，>100%强制终止
        [3].成本-质量优化：定期分析Token消耗与置信度关系，自动调整RAG检索深度/工具链复杂度
        [4].成本审计报告：任务结束后生成详细成本报告（Token分布/工具调用成本/优化建议）
}

Q2
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入处理与全景构建
        [1].输入数据经系统预处理为结构化JSON，AGENT首先调用摘要质量评估器（基于信息熵与关键实体覆盖率），若摘要质量评分<0.7则触发二次检索补充缺失维度
        [2].构建双层全景视图：宏观层（战略指标趋势/异常波动/关键假设）+ 微观层（文档切片引用索引），支持从微观证据反向修正宏观判断的DRIP式回溯机制
        [3].实施上下文窗口动态管理：基于任务复杂度自适应分配Token预算（基础任务512 tokens，复杂规划2048 tokens），超限时触发上下文压缩器
    
    2.任务类型与工具动态匹配
        [1].支持三类任务模式：①角色专业化自主分析（预定义17种战略工具） ②跨角色协作决策（SP/BP阶段） ③动态涌现任务（通过Tool RAG自动发现工具链）
        [2].实现Tool RAG机制：将工具能力描述（输入/输出Schema/领域约束）向量化存入专用向量库，支持"自然语言任务描述→工具能力语义匹配→DAG自动编排"
        [3].工具选择增加置信度评估：输出工具匹配度评分（0~1），<0.6时触发人工确认或启动多工具并行验证
    
    3.AGENT确定性工作流（基于LangGraph StateGraph）
        [1].状态Schema强制定义：采用Pydantic V2定义显式状态模型，包含session_id、current_phase、agent_role、working_memory（List[Message]）、long_term_memory_ref、tools_used、confidence_score、cost_accumulated
        [2].五阶段循环：[感知]环境初始化（加载SOUL.md价值观模板+BOUNDARY.md安全边界）→[规划]任务分解（输出机器可读DAG，含子任务依赖边与工具映射）→[执行]工具调用（沙盒隔离执行，STDERR自动捕获）→[验证]AUD旁路校验（一致性/幻觉/逻辑矛盾三重检测）→[反思]反向验证（用工具输出反向验证初始假设，差异>15%触发重规划）
        [3].中断安全：任意节点支持用户中断，自动保存StateSnapshot至Redis Cluster（三节点哨兵模式），恢复时加载完整上下文与工具状态
    
    4.用户反馈闭环演进
        [1].反馈分级处理：①专家修正（置信度>0.9）→直接触发Prompt Optimizer ②普通反馈→进入修正轨迹库 ③噪声反馈（与历史修正冲突）→自动过滤
        [2].Prompt Optimizer增强：基于DSPy+GEPA双引擎，①Few-shot样本自动生成（从修正轨迹提取正/负样本对） ②对抗样本挖掘（自动构造边界案例测试鲁棒性） ③多目标优化（准确性/成本/一致性加权）
        [3].演进触发双通道：①人工反馈通道（SP/BP Checkpoint） ②自动漂移检测通道（任务成功率周环比下降>10%自动触发）
    
    5.工具动态选择与组合
        [1].工具检索三模式：①规则映射（预定义17种战略工具） ②语义检索（Tool RAG） ③能力合成（多个原子工具自动编排为复合工具）
        [2].工具链DAG自动生成：基于任务依赖分析，输出有向无环图，支持并行执行无依赖子任务，执行失败时自动回溯重规划
        [3].工具调用强制沙盒化：所有工具执行在E2B沙盒中（启动延迟<500ms），网络访问仅限白名单API（财经数据/企业内部系统），代码执行结果经Validation Feedback闭环校验
    
    6.AGENT间协作与仲裁
        [1].协作三原则：①视角隔离（各AGENT仅访问角色相关数据视图） ②贡献度透明（Shapley值量化各AGENT决策影响力） ③冲突显性化（输出分歧热力图，标注分歧维度与强度）
        [2].CEO仲裁状态机增强：
            - 辩论轮次动态调整：基础轮次=3，每轮分歧收敛率<20%则+1轮（上限7轮）
            - Temperature Decay策略：轮次1→0.7, 轮次2→0.5, 轮次3+→0.3，强制具体化建议
            - 双方案生成规则：Plan A（保守参数集：风险权重×1.5，增长预期×0.8）；Plan B（激进参数集：风险权重×0.7，增长预期×1.3）
            - 用户介入窗口：30秒倒计时，支持"采纳A/B/混合方案/自定义"四选项，超时默认CEO决策
        [3].视角融合机制：在SP/BP各阶段Checkpoint，强制各AGENT输出视角摘要（300字），由CEO AGENT执行跨视角冲突检测（基于财务常识库+战略一致性规则）
    
    7.视角隔离与信息融合
        [1].严格视角隔离：各AGENT初始化时加载角色专属数据过滤器（如CFO仅见财务数据视图），工具调用增加角色权限校验
        [2].跨视角融合层：在战略意图/业务设计等关键节点，启动"融合会议"：各AGENT输出视角摘要→冲突检测→CEO主导融合→生成统一战略假设
        [3].视角偏离监控：实时检测AGENT行为偏离度（调用非角色工具次数/讨论非角色话题比例），偏离度>30%触发AUD告警
    
    8.CEO裁决状态机增强
        [1].辩论质量评估：每轮输出新信息增益率（本轮新增关键论据/历史论据总数）与重复率，增益率<10%且重复率>50%时强制终止辩论
        [2].Plan A/B生成透明化：输出参数调整明细（如Plan B将市场增长率假设从8%→12%，风险准备金比例从15%→10%）
        [3].用户裁决增强：提供"影响模拟器"，用户可滑动调整关键参数实时预览Plan A/B结果变化
    
    9.AUD旁路监听增强
        [1].四重检测维度：
            - 幻觉检测：生成内容与检索文档的语义相似度<0.4
            - 逻辑矛盾：违反Pydantic Schema约束或财务常识库规则（如利润率>100%）
            - 数据漂移：引用数据时效性>12个月且无明确标注
            - 视角违规：AGENT输出包含非角色职责内容
        [2].动态重试机制：基础重试次数=3，任务复杂度评分>0.7时+2次，每次重试强制更换工具或调整参数
        [3].熔断阈值：连续3次同类错误触发工具熔断，错误类型分布熵<0.3触发AGENT能力降级
    
    10.上下文管理与公共黑板
        [1].三层记忆架构：
            - 短期记忆：当前会话Working Memory（LangGraph State），Token窗口动态压缩（重要性评分+时间衰减双因子）
            - 长期记忆：战略档案库（向量+图数据库混合存储），支持跨SP/BP周期检索
            - 经验记忆：修正轨迹库（匿名化存储），用于Prompt Optimizer训练
        [2].公共黑板MVCC：基于Redis Cluster实现多版本并发控制，每次更新生成新版本快照，支持任意历史版本回溯与diff对比
        [3].黑板安全：写操作需经AUD校验，读操作按角色权限过滤，敏感操作（如删除）需CEO+用户双重确认
    
    11.可解释性与证据链
        [1].GAIA级证据包标准：每次任务输出包含①输入数据哈希 ②工具调用序列（含参数/输出/耗时） ③中间状态快照（关键决策点） ④置信度评分及依据 ⑤文档切片引用索引（支持点击溯源）
        [2].交互式溯源树：前端支持从结论节点逐层展开至原始数据，每层显示"数据来源→计算逻辑→中间结论"三元组
        [3].决策影响分析：量化各输入变量对最终决策的Shapley贡献值，高影响变量（>20%）自动高亮标注
    
    12.思维链溯源树ToT增强
        [1].实时置信度监控：每完成子任务即计算置信度（基于工具输出一致性/数据覆盖度/逻辑连贯性三维度加权），非仅阶段结束时评估
        [2].动态阈值机制：基础阈值=0.6，高风险任务（如资本支出决策）阈值提升至0.75，低风险任务（如市场趋势描述）阈值降至0.5
        [3].ToT节点强制推理模型：触发ToT时自动切换至o1-mini或Grok 3 Reasoning模型，执行深度回溯推理（最大深度=5层）
    
    13.Agent性能监控与行为分析
        [1].AgentBoard对标指标：
            - 任务成功率（阶段完成率）≥95%
            - 工具调用准确率（输出符合Schema）≥98%
            - 幻觉率（AUD拦截次数/总任务数）≤5%
            - 成本效率（Token消耗/任务复杂度）周环比波动<15%
        [2].行为分析维度：
            - 角色偏离度：非角色工具调用占比
            - 协作效率：跨AGENT消息轮次/任务复杂度
            - 决策质量：用户修正率（修正次数/总输出数）
        [3].自动根因分析：指标异常时自动关联日志，输出根因报告（如幻觉率上升→RAG检索质量下降→向量库漂移）
    
    14.安全与合规增强
        [1].沙盒三重隔离：①代码执行隔离（E2B容器） ②网络隔离（仅白名单API） ③数据隔离（角色视图过滤+敏感字段脱敏）
        [2].MCP 2025协议集成：工具调用遵循Model Context Protocol 2025规范，支持工具发现/授权/执行标准化
        [3].合规审计：所有AGENT操作记录至不可篡改日志（WORM存储），满足SOX/ISO27001审计要求，支持按时间/角色/任务类型多维检索
    
    15.成本控制三重熔断
        [1].任务级熔断：单次任务Token消耗>预设阈值（基础任务5K tokens，复杂规划50K tokens）立即终止
        [2].会话级熔断：日累计Token消耗>企业配额（默认100万tokens）暂停新任务，已启动任务完成
        [3].系统级熔断：连续3次工具执行失败或单任务成本>预算200%触发系统级告警，需管理员介入
        [4].成本预测：任务启动前基于历史相似任务预测Token消耗，偏差>50%时提示用户确认
}

Q3
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入处理与分层感知
        [1].输入数据经系统预处理为结构化JSON格式，采用MemoryOS四级分层架构：工作记忆(当前任务上下文)、情景记忆(本次会话历史)、语义记忆(企业知识库)、程序记忆(工具使用经验)
        [2].感知阶段强制执行"摘要优先"策略：首先调用摘要检索器构建全景视图(含关键指标趋势/异常波动/关联文档清单)，置信度≥0.7方可进入细节挖掘；置信度<0.7自动触发二次检索并扩大检索范围20%
        [3].集成Working Memory Compressor：基于关键实体抽取与HyDE算法，将长对话历史压缩为≤512 tokens的关键结论集，压缩率目标≥70%
    2.角色专业化与视角隔离
        [1].实施三重硬隔离机制：
            (1) Prompt隔离：每个AGENT加载专属IDENTITY.md，注入角色知识边界与决策约束
            (2) 工具隔离：基于BOUNDARY.md定义角色工具白名单，越权调用触发熔断
            (3) 数据隔离：字段级ACL控制，如CFO仅可访问财务相关字段(营收/成本/现金流)
        [2].部署Perspective Drift Detector：实时监控输出中跨角色关键词频率(如CFO输出"用户体验"触发预警)，超阈值(默认5%)自动熔断并记录至Agent Safety Log
        [3].角色能力边界采用Role Embedding向量量化：计算任务需求向量与角色能力向量的余弦相似度，<0.65时强制路由至更匹配角色
    3.AGENT基本工作流（ReAct+Reflection增强版）
        [1].感知(Perceive)：配置环境上下文→加载四级记忆→执行摘要优先策略→构建全景视图
        [2].思考(Think)：LLM推理生成结构化Plan(遵循Pydantic V2 Schema)→置信度评估(贝叶斯网络融合四维度)
        [3].执行(Act)：解析Plan→工具调用(经MCP Server授权)→沙箱执行(网络隔离+权限最小化)
        [4].反射(Reflect)：回写工具输出→置信度重评估→决策循环条件：置信度<0.65 AND 循环次数<5 →返回[2]；否则进入[5]
        [5].输出(Output)：构建结构化结果(JSON Schema)→释放资源(对接Kubernetes Agent Sandbox自动回收)→持久化Checkpoint(经LangGraph Checkpointer存入Redis Cluster)
    4.人在回路与反馈闭环
        [1].支持结构化用户反馈：定义Feedback Schema(JSON)包含{修正类型: enum[参数/权重/约束], 置信度变化: float, 影响因子: array[string]}
        [2].反馈自动触发ReflAct循环：修正轨迹→Few-Shot样本转换→Prompt Optimizer参数更新→A/B测试验证
        [3].实施修正影响传播分析：评估修正对下游任务的影响范围，高影响修正(>3个下游任务)强制生成Impact Report
        [4].Checkpoint人工介入点：业绩差距分析完成、市场洞察完成、战略意图确认、创新焦点决策、业务设计终稿、执行设计终稿共6个强制暂停点
    5.AGENT提示词工程与持续演进
        [1].基于DSPy 2.0构建Prompt Optimizer：输入=用户反馈+AUD一致性报告+SP/BP Checkpoint人工反馈；输出=优化后System Prompt+Few-Shot样本集
        [2].优化触发三条件(满足任一即触发)：
            (1) 修正率连续2个任务>15%
            (2) 一致性评分<0.85
            (3) 单任务成本超历史均值20%
        [3].实施Pareto多目标优化：结构完整性(40%权重)+逻辑一致性(35%权重)+成本效率(25%权重)，采用NSGA-II算法求解Pareto前沿
        [4].版本管理：集成MLflow实现Prompt自动版本注册，支持A/B测试(分流比例可配置)与一键回滚(保留最近10个稳定版本)
    6.工具动态选择与MCP标准化
        [1].工具选择采用MCP Server能力发现机制：AGENT查询MCP Registry获取工具能力描述(输入/输出Schema/版本/可靠性评分)
        [2].工具选择策略：首选(能力匹配度>0.85)→次选(0.7-0.85)→人工介入(<0.7)，匹配度=语义相似度(60%)+历史成功率(40%)
        [3].工具调用强制MCP OAuth 2.1授权：敏感操作(如财务数据导出)需二次人工确认，授权令牌有效期≤15分钟
        [4].工具失败Fallback Chain：首选工具失败→自动切换次选工具(最多2次)→生成Tool Failure Report→人工介入
    7.AGENT间协作与仲裁机制
        [1].协作原则：企业利益最优化(定义为长期股东价值最大化)，各AGENT贡献度按角色权重分配(CEO 30%, CFO 25%, CMO 15%, CTO 15%, COO 10%, CHO 5%)
        [2].CEO仲裁器输出Decision Rationale Report：包含{各AGENT建议权重, 关键分歧点, 风险评估矩阵, 备选方案对比}
        [3].高不确定性议题(置信度<0.6)自动触发红蓝辩论：实例化Red Team(激进派)与Blue Team(保守派)子AGENT，执行结构化辩论(最大3轮)，输出Consensus Zone与Divergence Zone报告
        [4].用户覆盖决策：用户可覆盖CEO仲裁结果，强制记录至战略档案库并标记"人工覆盖"，覆盖行为计入User Trust Score
    8.视角隔离技术实现
        [1].Context Isolation硬约束：每个AGENT的System Prompt注入角色专属知识边界(如CFO仅关注"资金风险/资本结构/现金流"三类实体)
        [2].工具调用前BOUNDARY.md校验：检查工具功能是否在角色权限范围内，越界调用返回Permission Denied并触发安全告警
        [3].Perspective Drift实时检测：基于BERT微调的分类器监控输出文本，跨角色关键词出现频率>5%触发熔断，熔断后需人工解锁
        [4].越界行为审计：所有越界尝试记录至Agent Safety Log，累计3次触发Agent Quarantine(隔离72小时)
    9.CEO裁决状态机增强版
        [1].辩论轮次控制：最大轮次=3+议题风险等级(1-3级)，风险等级基于影响范围/资金规模/战略重要性三维度评估
        [2].动态Temperature Decay：基础Temperature=0.7，每轮衰减ΔT=0.15×(1-风险等级/3)，强制AGENT从发散思维转向收敛决策
        [3].辩论质量评估：每轮输出经Debate Quality Scorer评分(论点新颖性30%+证据强度40%+逻辑一致性30%)，评分<0.6的AGENT强制重试
        [4].未达成一致处理：生成Plan A(保守版，风险权重0.7)与Plan B(激进版，风险权重0.3)两套方案，CEO按企业风险偏好矩阵决策，同时开启10秒用户介入窗口
    10.AUD AGENT增强审计机制
        [1].四维一致性检验：
            (1) 事实一致性：对比原始文档切片，检测事实扭曲
            (2) 逻辑一致性：规则引擎检测内部矛盾(如利润率>100%)
            (3) 数值一致性：沙箱重计算关键指标，误差>5%标记异常
            (4) 时效一致性：检查引用数据时效性，超12个月标记"数据陈旧"
        [2].分级熔断机制：
            L1(幻觉概率>0.8)：自动触发修正(调用Self-Correction Agent)
            L2(0.6-0.8)：通知相关AGENT重试(最多2次)
            L3(<0.6)：暂停任务并生成Hallucination Report，人工介入后从断点恢复
        [3].幻觉根因分析：构建Hallucination Taxonomy(数据缺失/逻辑跳跃/数值错误/时效过期)，支持针对性改进
    11.上下文压缩与公共黑板
        [1].关键结论提取双路算法：
            (1) 实体驱动：提取战略关键实体(市场/竞争者/财务指标)及其关系
            (2) 意图驱动：识别用户核心诉求与决策约束条件
        [2].公共黑板持久化：基于LangGraph StateSnapshot机制，状态快照存入Redis Cluster(3主3从)+AOF持久化，RPO<1秒
        [3].黑板冲突解决：采用Vector Clock记录更新顺序，冲突时应用Last-Writer-Win策略，冲突日志存入战略档案库供审计
        [4].时间旅行调试：支持从任意Checkpoint恢复执行，用于问题复现与根因分析
    12.决策可解释性增强
        [1].置信度量化模型：贝叶斯网络融合四维度
            数据质量(30%)：来源权威性+完整性+时效性
            工具可靠性(25%)：历史准确率+领域适配度
            逻辑一致性(25%)：规则引擎检测结果
            历史表现(20%)：同类任务成功率
        [2].Decision Evidence Graph输出：
            节点=关键决策因子(大小=影响权重)
            边=因子间因果关系(粗细=影响强度)
            颜色=风险等级(红>黄>绿)
        [3].SHAP值解释：量化各输入特征对最终决策的贡献度，支持"如果...会怎样"反事实推理
        [4].决策风险热力图：可视化展示各决策维度的风险分布(市场/财务/运营/合规)
    13.思维链溯源树(ToT)增强机制
        [1].动态触发阈值：基础阈值0.6，根据议题复杂度动态调整(±0.1)，复杂度=子任务数×工具调用数×数据源数
        [2].ToT搜索约束：
            最大深度：5层(根节点→叶节点)
            每层宽度：3个候选分支
            总token预算：5000 tokens
            超预算自动剪枝(保留Top-K路径)
        [3].ToT-AUD联动：每条推理路径独立经AUD四维检验，仅保留全部通过检验的路径，失败路径记录至Hallucination Log
        [4].ToT结果融合：采用加权投票机制(路径置信度加权)生成最终决策，输出完整推理树供审计
    14.Agent性能监控与漂移防御
        [1].三大核心指标监控：
            Task Success Rate：目标>92%，警戒线85%
            修正率：目标<10%，警戒线15%
            成本效率比：实际成本/基准成本，目标<0.8，警戒线1.2
        [2].漂移检测双算法：
            KS检验：检测输出分布偏移(月度基线对比)
            CUSUM：检测性能指标连续下降(滑动窗口7天)
        [3].自动修复机制：
            轻微漂移(指标偏离<10%)：自动触发Prompt微调
            中度漂移(10%-20%)：Skill Review+人工审核
            严重漂移(>20%)：Agent Quarantine+根因分析+全量回归测试
        [4].健康度仪表盘：实时可视化展示各AGENT的12项健康度指标，支持下钻分析与预警订阅
}

Q4
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入处理与三层记忆路由
        [1].输入数据经RAG系统结构化为JSON Schema，首先进入Token Memory（上下文窗口）构建全景视图
        [2].自动生成300字战略摘要存入Latent Memory（向量库），支持跨会话语义召回
        [3].关键指标（营收/利润率/市占率等）提取存入Parametric Memory（结构化DB），支持时序分析与趋势预测
        [4].实施动态记忆压缩：Token Memory超过8K tokens时，自动将低频信息迁移至Parametric Memory，保留高频决策因子在Token层
    
    2.任务分类与执行模式
        [1].内置任务复杂度评估器，基于三维度判定：①输入熵值（信息不确定性）②工具依赖深度（调用链长度）③跨角色耦合度（需协作AGENT数量）
        [2].简单任务（熵值<0.3且工具依赖≤2）：单AGENT自主执行，无需协作
        [3].复杂任务（任一维度超标）：自动触发Crew模式，由CEO AGENT协调多AGENT协作，生成协作拓扑图（DAG）并分配子任务
    
    3.AGENT核心工作流（Reflexion v2.1增强版）
        [1].感知（Perceive）：加载三层记忆（Token/Parametric/Latent）+ 配置角色沙箱（工具ACL+数据过滤器）+ 初始化会话生命周期（TTL=2h）
        [2].思考（Think）：LLM生成推理链，强制输出结构化JSON（Schema: {reasoning_steps[], confidence_score, tool_candidates[]})
        [3].执行（Act）：①工具调用前执行MCP Capability Check ②敏感操作（财务预测/资源分配）需AUD预审批 ③代码执行强制沙箱隔离（网络断连+资源限额）
        [4].观察（Observe）：捕获工具输出+STDERR，计算执行质量评分（0-1）
        [5].反思（Reflect）：①质量评分<0.7触发结构化反思 ②生成反思日志（错误类型/根因/修正策略）存入反思库 ③连续3次同类错误自动熔断该工具并告警
        [6].循环控制：置信度≥0.85且无未决依赖则终止循环；否则回写关键中间变量至Token Memory，进入下一轮思考
        [7].输出构建：封装Evidence Chain（输入溯源+工具轨迹+中间变量+置信度演化）+ 生成可点击引文索引的PDF报告
    
    4.Human-in-the-Loop主动介入机制
        [1].自动暂停触发条件：①决策置信度<0.6 ②资源分配变动>10% ③跨阶段关键假设变更 ④用户预设的Checkpoint节点
        [2].暂停界面提供：①当前推理链可视化 ②Plan A/B双方案对比（保守/激进）③修正参数滑块（权重/约束/阈值）
        [3].用户修正自动捕获：差异快照存入修正轨迹库，经脱敏后转化为Few-shot样本供Prompt Optimizer使用
        [4].支持修正回溯：用户可查看历史修正记录及对应的任务完成率变化
    
    5.AGENT提示词工程与持续演进
        [1].角色提示词模板：基于IDENTITY.md（角色定义）+ TOOLS.md（工具契约）+ BOUNDARY.md（安全边界）三文件生成System Prompt
        [2].Prompt Optimizer v2.0：①输入：修正轨迹库+任务完成率+用户满意度 ②优化目标：多目标Pareto最优（质量/成本/速度）③输出：更新后的System Prompt参数
        [3].演进触发三重门禁：①修正率<5%（质量稳定）②任务完成率>90%（效能达标）③用户满意度>4.5/5（体验验证），三者同时满足且处于SP/BP非关键阶段才允许自动演进
        [4].版本管理：所有Prompt变更纳入Git LFS，支持一键回滚至历史版本，回滚操作需AUD审计
    
    6.工具调用安全框架（MCP v2025集成）
        [1].工具选择：基于任务需求+角色ACL过滤工具候选集，输出Top-3工具推荐及理由
        [2].调用前安全检查：①Capability Check（验证AGENT权限）②Input Validation（防注入检测）③Cost Estimate（Token/USD预估）
        [3].敏感工具熔断：财务预测/并购分析等高风险工具，连续2次输出矛盾结论自动熔断并告警
        [4].完整审计轨迹：工具调用记录（时间戳/参数/输出/耗时/成本）存入战略档案库，支持按AGENT/工具/时间多维检索
    
    7.多AGENT协作与分层仲裁
        [1].协作启动：CEO AGENT接收复杂任务后，生成协作DAG（节点=子任务，边=数据依赖），分配AGENT并设置超时（默认15min/节点）
        [2].L1辩论层：冲突观点触发结构化辩论，最大3轮；每轮计算观点差异熵值，熵值<0.2视为收敛提前终止；连续2轮无新论据触发僵局检测
        [3].L2方案层：辩论未收敛时，CEO生成Plan A（保守，风险权重0.7）与Plan B（激进，创新权重0.7）双方案，附带风险-收益矩阵
        [4].L3裁决层：①CEO按企业偏好矩阵（风险/创新/执行难度加权）决策 ②暂停10秒提供用户介入窗口 ③超时无动作则以CEO决策为准，但标记"无用户确认"
    
    8.视角隔离三重保障
        [1].Prompt层隔离：System Prompt强制注入角色约束（如"CFO视角：仅关注现金流/资产负债/资本成本，禁止讨论技术架构"）
        [2].工具层隔离：工具注册时标记角色标签（finance/ops/marketing/tech/hr），AGENT调用时实施ACL过滤（CFO仅见finance标签工具）
        [3].数据层隔离：RAG检索添加角色过滤器，基于文档元数据（部门/密级/角色）动态过滤结果集，确保CHO仅见HR相关数据片段
    
    9.CEO AGENT裁决状态机增强版
        [1].状态定义：{INIT, DEBATE_ROUND_1..3, STALEMATE_DETECT, PLAN_GENERATION, USER_INTERVENTION, FINAL_DECISION}
        [2].Temperature Decay策略：辩论轮次1→0.7, 轮次2→0.5, 轮次3→0.3，强制AGENT提出具体修改意见而非单纯否定
        [3].僵局检测：连续2轮辩论中，新论据数量<2且观点相似度>0.85，自动跳转至PLAN_GENERATION状态
        [4].用户介入超时处理：10秒无操作视为用户授权CEO决策，但输出中明确标注"未经用户确认"并降低该决策置信度0.1
    
    10.AUD AGENT四级干预机制
        [1].L1监控（实时）：监听所有AGENT输出，计算置信度+一致性评分（基于领域事实库校验）
        [2].L2预警（置信度<0.7）：向相关AGENT发送修正建议（提供3个修正方向+参考文档片段）
        [3].L3阻断（置信度<0.5或3次L2失败）：强制任务暂停，锁定当前状态，要求AGENT重试（参数固化：temperature=0.2, max_tokens=500）
        [4].L4人工介入（3次L3失败）：生成审计报告（含错误模式分析+风险等级），暂停任务并通知用户，支持从暂停点恢复执行
    
    11.分层黑板架构（StateSnapshot增强）
        [1].公共黑板（Global Board）：只读，存储全局事实（市场数据/行业基准/企业历史），所有AGENT可读不可写
        [2].角色黑板（Role Board）：角色专属，存储私有推理中间结果，实施ACL（仅创建者+CEO可读）
        [3].临时黑板（Temp Board）：任务级，自动过期（TTL=2h），存储临时变量，任务结束自动清理
        [4].持久化机制：基于LangGraph Checkpoint + Redis Streams，支持状态快照回溯（时间旅行调试）与跨会话状态恢复
    
    12.全链路可解释性Evidence Chain
        [1].输入溯源层：文档切片ID + 内容哈希 + 相似度得分，支持点击跳转原文
        [2].工具调用层：工具名称 + 参数快照 + 执行耗时 + 输出摘要 + 异常日志（如有）
        [3].推理层：思维链节点（编号+内容+置信度）+ 节点间依赖关系 + 修正历史（原结论→修正后结论）
        [4].决策层：最终结论 + 关键支撑证据（Top-3文档切片+工具输出）+ 置信度演化曲线（从初始到最终）
        [5].可视化接口：前端支持从结论反向逐层展开，形成可交互的决策溯源树
    
    13.动态思维链溯源树（ToT v2.0）
        [1].基础模式（Always On）：每步推理自动记录节点，构建轻量级推理图（内存驻留）
        [2].增强模式（置信度<0.7触发）：自动展开子树，对低置信度节点进行多路径探索（Beam Search, width=3）
        [3].审计模式（用户触发）：点击任意结论实时回溯完整路径，高亮显示关键分歧点与修正节点
        [4].ToT存储：推理图序列化存入战略档案库，支持跨SP/BP周期对比分析（如"2025Q4 vs 2026Q1推理模式变化"）
    
    14.三维漂移检测与性能监控
        [1].数据漂移检测：①输入分布监控（KS检验）②文档类型比例变化（卡方检验）③阈值：p-value<0.05触发告警
        [2].概念漂移检测：①输出质量滑动窗口（最近100任务完成率）②工具调用成功率趋势 ③阈值：完成率下降>15%持续3天触发Skill Review
        [3].行为漂移检测：①工具调用模式异常（Isolation Forest）②反思频率突增（同比>50%）③阈值：异常分数>0.8触发人工审计
        [4].监控仪表盘：实时展示14项核心指标（任务完成率/平均置信度/修正率/Token成本/漂移状态等），超标指标自动标红并推送告警
        [5].自动修复机制：检测到漂移后，①回滚至最近稳定Prompt版本 ②切换至备用LLM API ③降级至规则引擎模式（基于历史成功案例）
}

Q5
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入处理与全景构建
        [1].输入数据经系统预处理为结构化JSON格式，强制包含元数据（来源/时间戳/完整性评分）
        [2].采用两级摘要机制：①自动生成300字战略摘要（关键指标覆盖率≥85%）；②用户可动态调整摘要粒度（宏观/中观/微观三级）
        [3].构建全景视图时执行SIM卡校验：摘要关键实体与原始文档的语义一致性≥0.85，不达标则触发重新摘要
        [4].细节挖掘阶段采用分层检索：先锁定相关文档集（Top-5），再检索具体切片，保留完整引文索引链
    
    2.任务分类与协作触发
        [1].定义两类核心任务：①角色自主分析（单Agent完成，复杂度<7分）；②跨职能协作决策（多Agent，复杂度≥7分）
        [2].任务复杂度评分器：基于5维度量化（数据异构性/工具依赖数/时间压力/风险等级/战略影响），0-10分制
        [3].协作触发条件：任务复杂度≥7分 或 用户显式指令 或 检测到跨职能依赖（如财务数据需CTO技术可行性验证）
        [4].角色切换协议：定义标准化上下文传递Schema（含角色状态/关键假设/待决议项），确保无缝交接
    
    3.AGENT核心工作流（ReAct+Reflection增强版）
        [1].感知（Perceive）：①加载会话级环境配置（风险偏好/时间压力/行业上下文）；②初始化混合检索记忆容器（短期+情景记忆）
        [2].思考（Reason）：①基于全景视图生成推理链；②置信度<0.7时自动触发Self-RAG补充检索；③最大推理步数限制（默认15步）
        [3].执行（Act）：①解析LLM输出为结构化决策（Pydantic V2 Schema强制校验）；②任务完成则构建输出；③需工具调用则进入工具调度器
        [4].反射（Reflect）：①触发条件：置信度<0.7 或 工具执行失败≥2次 或 用户反馈修正；②执行三层反思：任务级（参数调整）、会话级（策略优化）、系统级（Prompt演进）；③最大反射循环5次，超限触发人工介入
        [5].持久化与资源回收：①任务结束前自动保存StateSnapshot至Redis集群+战略档案库（双写）；②资源释放前验证无活跃依赖；③生成执行轨迹包（含Token消耗/成本/时长）
    
    4.用户反馈与闭环演进
        [1].反馈结构化：强制JSON Schema（修正点定位/原结论/新结论/置信度/修正理由），非结构化反馈自动转译
        [2].反馈时效管理：SP/BP关键Checkpoint设置反馈倒计时（默认24h），超时自动采用系统推荐方案并记录风险
        [3].修正日志处理：①匿名化存储至修正轨迹库；②自动关联至对应Prompt版本；③高频修正模式（出现≥5次）触发DSPy优化器
        [4].演进安全机制：①Prompt优化仅在SP/BP Checkpoint完成态触发；②优化前运行A/B测试（对比历史10个相似场景，成功率提升≥5%方可发布）；③所有变更纳入Git LFS版本管理，支持一键回滚
    
    5.Prompt工程与动态优化
        [1].角色Prompt生成：基于角色契约（IDENTITY.md）+ 分析框架（BLM/BEM阶段）+ 用户偏好动态组装
        [2].DSPy优化器配置：①优化目标：多目标函数（结构合规性0.4 + 一致性0.3 + 成本效率0.3）；②优化触发：修正率<15% 且 一致性阈值>0.85 且 Checkpoint完成态；③优化范围：仅调整Few-shot样本与推理链模板，禁止修改战略模型逻辑步骤
        [3].Prompt版本治理：①与系统版本/战略档案库快照三元绑定；②发布前通过Strat-Bench基准测试（20+专家场景）；③运行时监控修正率，超标（>18%）自动回滚
    
    6.工具动态选择与调度
        [1].工具选择决策树：输入任务特征（类型/数据模态/精度要求/成本约束）→ 输出候选工具集（Top-3）及推荐理由
        [2].成本透明化：工具调用前显示预估消耗（Token数/USD成本/执行时长），用户可手动覆盖选择
        [3].工具冲突仲裁：①检测多工具输出矛盾（基于领域事实库规则）；②启动加权投票（工具历史准确率×0.6 + 数据匹配度×0.4）；③输出冲突报告供用户裁决
        [4].工具链DAG编排：自动构建工具依赖图，无依赖子任务并行执行，支持进度实时可视化
    
    7.多AGENT协作与仲裁
        [1].企业利益量化模型：三维权重矩阵（财务回报40% + 风险控制30% + 战略契合度30%），支持用户动态调整
        [2].CEO仲裁机制：①输出结构化决策日志（含各Agent建议/权重/关键分歧点）；②用户可查看完整辩论记录并修改权重；③修改后自动运行影响模拟（展示对后续3阶段的影响）
        [3].协作安全边界：①禁止Agent直接修改其他Agent的角色状态；②跨Agent数据传递强制脱敏（移除角色专属敏感字段）；③所有协作行为记录至审计日志
    
    8.视角隔离与越界防护
        [1].技术实现：①角色Prompt注入视角过滤器（Role Lens），仅保留角色域内实体；②工具调用前执行域校验（输入数据90%+在角色域内）
        [2].越界检测：①实时监控输出中非角色关键词（如CFO输出"品牌调性"）；②连续3次越界触发熔断并告警；③熔断期间强制重置角色上下文
        [3].受限跨职能通道：①特定阶段（如创新焦点）临时开放视角共享；②共享内容经AUD AGENT脱敏处理；③共享行为计入Agent行为评分
    
    9.CEO裁决状态机（增强版）
        [1].辩论轮次控制：最大5轮，Temperature Decay函数 T(n)=max(0.1, 0.7-0.1×n)，n为当前轮次
        [2].强制建设性输出：第3轮起禁止单纯否定，必须提供具体修改方案（含数据依据/工具支持）
        [3].Plan A/B生成：①差异化约束：Plan A风险容忍度≤30%（保守），Plan B≥70%（激进）；②独立验证：两方案分别通过AUD AGENT一致性检验；③差异度要求：关键指标差异≥15%
        [4].用户介入窗口：动态时长 = 任务复杂度×5秒（最小15秒，最大60秒），倒计时可视化，超时采用CEO决策并记录风险
    
    10.AUD AGENT旁路审计（金融级增强）
        [1].一致性量化：BERTScore计算原文-生成内容相似度，阈值动态调整（SP阶段≥0.85，BP阶段≥0.80）
        [2].幻觉检测三重校验：①事实一致性（对比战略档案库）；②逻辑一致性（领域验证器规则引擎）；③数据溯源完整性（引文索引链断裂检测）
        [3].重试策略：①重试次数 = 4 - 任务关键性系数（SP关键阶段=2次，BP执行阶段=4次）；②每次重试强制更换工具或调整Prompt；③失败后生成根因报告（数据/工具/Prompt三类归因）
        [4].熔断机制：连续3次相同类型幻觉触发工具熔断，并更新幻觉黑名单
    
    11.上下文管理与公共黑板
        [1].关键结论提取：基于战略档案库实体识别模型，提取三类核心实体（战略目标/关键假设/风险因子）
        [2].StateSnapshot持久化：①Redis Cluster三节点部署（主从+哨兵）；②每10分钟快照至战略档案库；③支持按时间戳/任务ID/Agent角色多维检索
        [3].上下文压缩质量保障：①压缩率目标≤30%（保留70%+关键信息）；②解压还原后语义相似度≥0.9；③压缩失败自动降级至全量传递并告警
        [4].黑板访问控制：①读写权限按角色分级；②写操作强制版本号校验防冲突；③所有变更记录至操作日志
    
    12.可解释性与决策溯源
        [1].置信度计算引擎：贝叶斯网络融合三要素（数据质量权重0.3 + 工具可靠性0.3 + 历史准确率0.4），输出0-1概率值
        [2].三级可视化粒度：①高管层：关键结论+置信度+风险提示；②经理层：决策依据+工具链+关键数据点；③专家层：完整推理链+中间变量+引文索引
        [3].不确定性热力图：可视化展示决策链中各环节置信度衰减，高风险环节标红并建议人工复核
        [4].溯源树交互：支持从结论反向逐层展开，每层显示（数据来源→计算逻辑→中间结论），支持导出PDF审计报告
    
    13.思维链溯源树ToT（可控探索）
        [1].触发条件：①仅在SP/BP阶段结束时启用；②置信度阈值动态调整 = 行业基准×任务关键性系数（咨询业0.65×1.1，制造业0.55×0.9）
        [2].探索约束：①最大深度3层；②每层候选路径≤5个；③总Token消耗上限 = 基础推理×300%
        [3].成功标准：①最优路径置信度提升≥15%；②与次优路径置信度差距≥10%；③不满足则采用原方案并记录探索失败原因
        [4].ToT日志：完整记录所有探索路径及淘汰原因，存入战略档案库供后续分析
    
    14.Agent性能监控与自愈
        [1].核心监控指标：①任务完成率（≥92%）；②修正率（≤15%）；③平均置信度（≥0.75）；④Token效率（输出/输入比≥0.3）；⑤工具调用成功率（≥88%）
        [2].行为异常检测：①基于LSTM的时序异常检测（滑动窗口30天）；②偏离历史基线2σ触发三级告警（邮件/短信/人工介入）；③异常行为自动归类（数据漂移/模型退化/恶意攻击）
        [3].漂移自愈机制：①性能下降持续3天自动回滚至前一稳定版本；②启动根因分析流水线（数据质量检查→工具测试→Prompt验证）；③修复后需通过Strat-Bench全量测试方可重新上线
        [4].健康度仪表盘：实时展示各Agent核心指标趋势，支持按时间/任务类型/行业多维钻取，超标指标自动标红
}

---------------------
G1
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.数据注入与动态感知：输入经系统处理的结构化JSON数据，AGENT首先读取全局摘要视图，随后利用内置RAG工具，按需动态检索并加载支撑细节，避免超长上下文导致内存污染或Token溢出
    2.受控自治与SOP驱动：AGENT通过图谱约束（State Graph）执行基于角色的战略分析任务。明确定义感知、协作、输出三大类任务的执行状态图，限制AGENT在既定轨道内进行自主拆解与微观决策
    3.带熔断机制的MCP工作流：基于模型上下文协议（MCP）标准化工作流：[感知]配置环境->[思考]LLM推理->[执行]调用授权工具->[反射]校验输出。强制设定单节点最大循环深度（默认Max_Steps=5），超限则触发熔断，输出局部结论并请求人工介入
    4.状态快照与深度反馈干预：支持将AGENT执行路径的任意节点状态（StateSnapshot）持久化。用户不仅可以文字反馈结果，更可直接修改快照中的工作区变量、输入权重或中间假设，系统支持从修改后的图节点恢复执行
    5.受控提示词演化（DSPy）：基于DSPy理念建立AGENT Prompt Optimizer，收集用户修正轨迹与审计日志作为候选集。所有演化必须离线进行，微调后的参数必须通过战略基准测试集（Strat-Bench）验证，方可发布至生产环境
    6.基于角色的工具白名单：AGENT的工具调用权限受其角色契约（Boundary Contract）严格限制，系统基于角色身份动态下发工具白名单，防止越权分析与逻辑混乱
    7.多维量化仲裁协作：建立多智能体协作框架，由CEO AGENT基于量化评价矩阵（包括ROIC、风险系数、战略契合度等）对方案进行评估打分，拥有最终协作仲裁决定权；所有评分与权重向用户透明并支持覆写
    8.严格视角隔离（Perspective Isolation）：系统级强制拦截并过滤跨界信息，确保AGENT仅基于角色限定视角（如CFO严格绑定资金风险与财务建模）提取数据和分析，严禁泛化讨论，防止角色崩溃
    9.异步阻塞型裁决状态机：CEO AGENT内置"裁决状态机"。随辩论轮次增加，降低温度参数（Temperature Decay）强制输出建设性修改方案。若达最大轮次未共识，生成Plan A/B，并【异步挂起任务】，通过系统通知无限期等待用户介入裁决；用户亦可输入Plan C破局
    10.混合审计与原文溯源（AUD AGENT）：AUD AGENT采用"旁路监听+异步阻断"模式。综合运用Pydantic规则校验（Schema）与LLM语义校验，发现幻觉或数据矛盾即通知纠错；3次重试失败则阻断任务并抛出带详细错误堆栈、引文ID的异常报告供人工处理
    11.并发控制的公共黑板机制：采用局部工作区与全局黑板分离架构。在多轮对话间仅传递关键结论，结合LangGraph和Redis进行状态管理，公共黑板的写入强制增加角色签名与时间戳版本锁，防止并发信息污染
    12.强制结构化溯源引擎：所有关键推理与决策结论，必须在其输出Schema的元数据中包含确切的 evidence_source（引文片段ID/数据源图表ID）与 confidence_score（置信度），支持前端交互式可视化溯源
    13.受限思维树溯源（ToT）：将昂贵的ToT（思维树）机制作为低置信度防御底线。仅在SP/BP关键里程碑且决策置信度低于阈值（0.6）时触发，严格限制搜索深度（≤3）与广度（≤3），通过AUD前置裁剪无效分支，提升决策说服力
    14.细粒度监控与效能评估：建立行为与成本分析仪表盘，实时监控单任务Token消耗、工具调用成功率、审计拦截率、人工介入比例等核心指标，为系统迭代提供量化依据
}

G2
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT。系统全面引入规范驱动开发（SDD）架构与MCP（模型上下文协议），构建高可靠、强防御的智能体生态。
    1. 采用"全量加载+动态压缩（Compaction）"机制管理上下文。摒弃静态摘要，通过MCP协议直接挂载内外数据源，利用百万Token窗口构建全景视图，并在长时间复杂会话中动态折叠冗余信息以保留核心决策树。
    2. AGENT严格遵循SDD（规范驱动）模式：生成战略规范（Specify）->制定计划（Plan）->拆解子任务（Tasks）->执行（Implement）。针对深度分析（如全域竞品穿透），核心AGENT可动态孵化并调度并行子智能体（Sub-agents）组建临时微型团队，实现任务并发与自动回收。
    3. AGENT基本工作流升级为闭环契约引擎：[规范感知]获取可执行契约->[思考规划]多树分支推演->[并行执行]调度子智能体或MCP工具->[前置验证]沙箱模拟测试->[反射纠错]->[合并输出]并释放资源。
    4. 支持深度用户介入与契约修正。用户反馈不再是简单的文本追加，而是直接修改SDD中的"Spec（规范）"节点，系统根据Spec的变更自动逆向传播，触发后续规划与代码层面的确定性重构。
    5. 建立基于契约的Agent Prompt Optimizer。结合DSPy理念，将提示词转化为可执行契约。基于AUD的一致性检验、用户反馈及Strat-Bench基准测试集，多目标（结构、一致性、成本）自动演进契约参数，严禁未经沙箱验证的单点黑盒优化。
    6. 工具调用全面接入MCP（Model Context Protocol）开放标准。AGENT可跨越语言与平台壁垒，按需调用企业ERP、网络爬虫、代码沙箱等外部能力，实现工具链的标准化与无限扩展。
    7. 实施受控的协作与视角隔离。强制AGENT坚守角色边界（如CFO仅关注资金/风险控制），但引入"受控状态中间件"允许在边界内以只读形式共享关键变量。高级代理CEO AGENT拥有协作仲裁决定权，其决策过程必须生成结构化决策树供审查与覆写。
    8. 强化仲裁机制与红蓝对抗。CEO AGENT内置"裁决状态机"，在多轮辩论未达成一致时，不仅降低Temperature Decay强制收敛，更自动引入微型"红蓝军对抗"量化风险敞口。若僵持则生成Plan A（保守）和Plan B（激进）两套方案，提供10秒人工介入窗口，超时默认按CEO预设风险偏好执行。
    9. AUD AGENT实施"前置沙箱+旁路监听"的双重审计防御。前置拦截违背商业常识与权限越界的执行指令；后置旁路检验原文与生成内容的一致性。重试3次失败后，自动封锁该推理分支并挂起任务，防止幻觉污染全局黑板。
    10. 支持上下文压缩器与持久化"公共黑板"机制。利用LangGraph的StateSnapshot配合Redis管理状态回溯，不仅传递多轮对话的关键结论，更强制持久化完整思维链轨迹（Thought Trail），确保企业战略复盘具备绝对的可追溯性与合规性。
    11. 建立动态风险自适应的"思维链溯源树（ToT）"机制。在SP/BP关键节点，系统不再使用固定常数阈值，而是根据决策领域的风险敞口（Risk Exposure）动态计算置信度阈值（例如财务重组决策阈值高于常规营销规划）。低于阈值时强制展开ToT回溯推理路径，化解底层逻辑冲突。
    12. 实现推理过程的全息可视化，支持前端动态反演决策依据、关键驱动因素及置信度概率分布，将战略推演过程打造为完全白盒化的交互面板。
    13. 部署零信任安全隔离（Zero-Trust Sandboxing）。鉴于高阶Agent的强大外控权限，所有执行环境默认断网与权限最小化。实时监控并防御提示词注入、数据越权访问及恶意工具调用，触发异常立即硬熔断。
    14. 建立多维度的Agent性能监控与行为漂移分析框架。将资源消耗（Token/算力）、执行成功率、安全熔断率和契约达成率整体纳入健康度仪表盘，为模型降级、人工干预或架构优化提供量化支持。
}

G3
{
三、AGENT：支持网络调用LLM API（支持System 2长文本推理能力）驱动的企业战略规划AGENT集群执行战略规划任务，核心阵列包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT。

    1. 数据流与环境感知：采用“懒加载与按需下钻”机制。输入经系统处理结构化为JSON与知识图谱形态的数据，AGENT首先读取全景摘要，在推理过程中若发现信息盲区，自动生成子查询指令获取细节切片，确保上下文时效性与高保真。
    2. 意图驱动任务路由：AGENT主要执行自主分析与协作决策两大类任务。系统抛弃刚性流水线，采用意图驱动的动态拓扑结构：针对单一领域任务由对应角色独立处理；针对灰度交叉议题（如“技术演进对营销渠道的冲击”），由CEO AGENT动态拉起跨角色的临时“特遣任务组（Task Force）”进行并发研讨。
    3. 认知升级版工作流：集成System 2测试期计算（Test-Time Compute）。标准工作流升级为：[感知]配置环境与上下文 -> [慢思考]基于内部MCTS生成多条假设推演路径并自我剪枝 -> [规划]生成动作空间 -> [执行]调用工具或输出 -> [反射]评估执行结果，决定是否触发状态回滚或继续 -> [固化]构建输出。
    4. 轨迹模仿学习与反馈闭环：支持用户深度反馈。系统不仅接受对最终结果的修正，更通过“轨迹模仿学习（Trajectory Imitation）”，将用户在纠错过程中的逻辑链转化为少样本示例（Few-Shot），沉淀为企业独有的隐性战略知识库。
    5. 数据驱动的自动提词编译：基于DSPy理念建立AGENT Prompt Compiler。不再依赖人工撰写静态提示词，而是结合审计结果、用户修正轨迹以及LLM-as-a-Judge自动化评估管线，通过定义多维目标函数（逻辑一致性、成本约束、战略契合度），自动高频微调和编译各角色的指令集。
    6. 语义约束工具调用：AGENT按需从工具箱中调用工具。当工具箱扩容时，采用“语义工具检索”机制下发候选工具列表，并通过严格的Pydantic Schema前置校验工具输入参数，从根本上阻断工具调用幻觉。
    7. 加权共识与CEO仲裁机制：支持企业利益最优的协作。在多方意见冲突时，系统优先尝试计算加权共识得分以达成妥协；若僵持，CEO AGENT拥有最终仲裁权，但其决策逻辑必须经过结构化输出，用户可随时观测、干预或重定向该决策。
    8. 视角隔离与综合跨域：严格执行基础分析的视角隔离（如CFO仅关注资金与税务风险），严禁泛化讨论以避免模型漂移。但在SP/BP的融合阶段，系统开启受控的“跨域综合会议机制”，允许AGENT基于自身分析结果进行跨界碰撞。
    9. 动态温度衰减的裁决状态机：CEO AGENT内置高级“裁决状态机”。设定最大辩论轮次，随着轮次增加，采用指数衰减公式动态调低生成温度（Temperature Decay），强制AGENT收敛并发散提出具体修改参数而非单纯否定。若触发轮次上限未达成一致，输出包含共识置信度的Plan A（保守版）和Plan B（激进版）。CEO AGENT预选后提供10秒中断窗口，超时无干预则推进流转。
    10. 确定性回滚的审计阻断机制：AUD AGENT采用“旁路监听+确定性状态阻断”模式。不参与内容生成，专注事实一致性检验与合规审查。一旦发现严重幻觉或逻辑断裂，系统依托LangGraph的Checkpointer精确回滚至上一个健康状态树并通知纠错，3次重试失败则挂起任务等待人工介入。
    11. 分层记忆与公共黑板：采用短期高保真与长期图谱化的分层记忆机制。在多轮对话中，通过LangGraph StateSnapshot与Redis管理“公共黑板”，只同步关键结论与状态向量，彻底解决Token爆炸问题，并支持无损的状态时空穿梭（随时回溯历史论点）。
    12. 白盒化可解释性推理界面：支持Agent推理轨迹的深度可视化。将隐式的注意力权重与多路径搜索树转化为概率流向图，直观展示“数据->约束条件->各方博弈->最终决策”的置信度演变过程。
    13. 动态触发的思维树 (ToT/MCTS)：抛弃静态阶段末尾触发机制。在分析全周期的任何高风险决策点，只要内部置信度评分跌破动态阈值（根据议题重要性在0.6~0.85浮动），即刻触发多路径思维树扩展，通过回溯与交叉验证解决局部冲突。
    14. 自愈型性能监控基座：建立全天候模型漂移与行为健康度监测。当监测到某类任务的RAG检索准确率、工具调用成功率或逻辑一致性跌破设定基线时，系统自动触发熔断预警，并支持一键回滚至上一个稳定编译版本的Prompt与配置树。
}

G4
{
三、AGENT：支持网络调用LLM API（支持System 2深层推理模型与快速响应模型的混合调度）驱动的企业战略规划多智能体系统。核心AGENT包括首席执行官CEO、运营COO、营销CMO、技术CTO、财务CFO、人力CHO共6个业务AGENT，以及1个联席审计官AUD AGENT。

    1. 数据全景与假设驱动探索：输入经系统结构化处理的JSON数据，AGENT首先读取摘要构建初始全景视图，然后采用“假设驱动”机制挖掘细节，允许在挖掘过程中动态更新和修正全景摘要，而非单向死板读取。
    2. 任务流转与图状态管理：AGENT执行基于角色定义的自主分析与协作决策两大类任务。所有协作任务必须基于有向无环图（DAG）构建流转拓扑，确保多体协作具备确定性的状态机流转特征。
    3. 新一代状态图工作流：废弃传统线性ReAct，采用基于StateGraph的工作流：[环境渲染与快照加载]->[状态机调度]->[System 2深层推理与多路径评估]->[原子化执行与沙箱约束]->[微观自愈与重试]->[状态更新与Checkpointer持久化]。
    4. 异步无缝人机协同闭环：系统支持粒度极细的用户反馈。在关键流转节点或异常时，AGENT状态流自动挂起（Suspended），触发异步通知。用户不仅能“批准/拒绝”，还支持Time-travel能力，可直接修改公共黑板中的中间状态变量，并从修改点继续执行。
    5. 受控的提示词演进（DSPy）：基于DSPy理念建立AGENT Prompt Optimizer，收集AGENT运行的修正日志与AUD的一致性检验结果。自动微调的提示词参数必须进入“待审查版本库”，经人工专家（Strategy Reviewer）确认后方可推向生产环境。
    6. 基于契约的工具箱调用：AGENT按角色域权限从工具箱调用工具，强制执行Pydantic V2契约校验。严禁跨域调用工具（如CHO不能调用量化金融模型），工具输出异常时在毫秒级触发单节点的Reflection与自愈重试。
    7. 协作仲裁与人类一票否决：CEO AGENT具有业务层面的统筹与仲裁权，负责生成最终战略提案。系统杜绝“超时自动代理机制”，对于重大分歧或未决决策，CEO AGENT必须生成包含Plan A与Plan B的对比视图，强制流转至人工审批节点，等待真实高管签字确认。
    8. 动态的主视角隔离与跨界渗透：严格约束各AGENT的生成视角与评判标准（如CFO侧重ROI与现金流）。支持“跨域提案接口”，允许特定AGENT将突破性建议（如CMO提出的技术收购需求）以结构化假设的形式提交给CTO和CEO评估，防止过度隔离导致的战略短视。
    9. 动态辩论与温度衰减控制：CEO AGENT内置“辩论状态机”，控制分歧时的多轮磋商。随辩论轮次增加，系统自动降低LLM的Temperature值收敛发散度，并强制要求提交具体修改JSON补丁而非自然语言的单纯否定。若达最大轮次未一致，自动输出分歧报告供人工裁决。
    10. AUD旁路审计与分级阻断：AUD AGENT采用“旁路监听”不阻塞主流程。进行原文与生成内容的一致性校验。发现次要错误（如轻微事实偏差）标注预警标签后放行；发现致命冲突（如违背财务常识），则异步阻断相关AGENT并回滚状态，3次重试失败则挂起任务交由人工纠错。
    11. 状态压缩与全时回溯公共黑板：在多轮对话与复杂流转中，采用LangGraph的StateSnapshot机制结合Redis/PostgreSQL，通过Thread ID和Version ID管理“公共黑板”。严格管理状态版本，支持随时切回历史快照重新推演（Time-travel）。
    12. 交互式推理过程可视化：建立动态交互的“战略决策依据图谱”，前端可点击任意推理节点，查看当前节点的上下文快照、系统提示词、检索到的引用源文档切片及置信度评分。
    13. 常态化多路径推演（类似o1）：摒弃仅在最终阶段使用ToT的做法。在SP/BP的所有核心分叉点（如商业模式选择、核心竞争力定义），默认调用支持System 2深层推理的模型，并行生成多条思维链推演路径，通过内部验证打分选出最优解，极大提升战略输出的逻辑厚度。
    14. 综合效能与成本监控：建立涵盖行为分析、API Token消耗、执行耗时与修正召回率的多维度仪表盘，定期评估每位AGENT的任务ROI，触发系统级的健康度预警。
}

G5
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务。基于LangGraph等先进状态图框架构建，包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心业务AGENT，以及1个联席审计官AUD AGENT。
    1.支持分层数据感知与懒加载：首轮读取JSON格式全景摘要数据构建顶层视图；在具体分析环节，各AGENT基于任务需求动态调用RAG检索工具拉取细节切片，防止超长上下文带来的注意力丢失。
    2.执行契约化驱动的两大类任务：按角色自主分析数据与跨角色协作规划决策。所有任务流转必须遵循严格的 JSON Schema 契约输入与输出，确保系统的确定性。
    3.标准状态图工作流（State-Graph Workflow）：[感知]配置环境与上下文 -> [路由]管理会话生命周期与消息总线 -> [思考]LLM推理与局部ToT探索 -> [执行]工具调用或任务分解 -> [反思]状态校验与沙盒反馈 -> [产出]写入不可变日志并更新全局黑板。设定最大循环阈值（默认5次），防止陷入死循环。
    4.支持细粒度流式人机协作（HITL）：在状态图的任意关键路由节点，支持流式暂停（Streaming Interrupt）。用户不仅可反馈意见，还可直接通过状态注入（State Injection）修改Agent的当前工作内存变量。
    5.基于影子模式（Shadow Mode）的提示词演进：基于DSPy理念，根据分析结果修正、AUD校验日志与Checkpoint反馈，在后台生成候选提示词。候选提示词必须在基准测试集通过验证，并经人类专家签批后，方可替换线上版本。
    6.实施基于角色的工具权限控制（RBAC for Tools）：AGENT按需求从注册表中调用工具，系统硬性限制工具可见范围，确保AGENT仅能执行其角色职责内的操作。
    7.支持基于消息总线（Message Bus）的多AGENT协作：抛弃自由文本拼接，采用“提议-反驳-共识”的结构化 JSON 通信协议。CEO AGENT拥有最终仲裁权，用户拥有最高覆写权。
    8.实施受控的视角隔离与跨域请求：各AGENT仅从本职视角进行分析评估。当需要跨领域数据时，严禁自行脑补或泛化讨论，必须通过消息总线向对应AGENT发起标准化的“跨域数据请求（Cross-Domain Query）”。
    9.CEO AGENT智能裁决状态机：设定最大辩论轮次，引入温度衰减公式（Temperature Decay）强制收敛，要求AGENT随轮次增加必须提出建设性修改而非单纯否定。若达上限未一致，生成Plan A（保守版）和Plan B（激进版），系统自动挂起当前工作流并通过异步消息通知用户，等待用户在交互界面上明确裁决后恢复执行。
    10.AUD AGENT同步校验门禁机制：AUD不参与生成，负责在状态图流转的“写前验证（Pre-write Check）”环节进行原文与生成内容的一致性校验。若发现幻觉，同步拦截并打回重构；连续3次失败则触发熔断，挂起任务等待人工纠错。
    11.基于不可变日志与状态投影的公共黑板：在多轮对话间，使用LangGraph的StateSnapshot与Redis结合，底层采用不可变追加日志（Append-only Log）记录所有动作，表层维护一个高度压缩的关键结论投影（State Projection）供全局读取，确保上下文精简且可绝对回溯。
    12.流式溯源与可观测性（Telemetry）：对外暴露完整的决策依据、底层工具调用记录、引用源切片以及基于概率置信度区间（Confidence Interval）的可视化溯源图谱。
    13.动态触发的思维链溯源树（ToT）：不局限于阶段末尾，在任意复杂的路由节点或冲突评估点，若核心AGENT给出的置信度得分方差超出阈值或均值低于0.6，即时动态触发局部ToT，通过多路径探索解决冲突。
    14.多维效能与成本监控体系：建立行为分析框架，实时记录Token消耗、预估资金成本（USD）、推理时延、工具调用失败率及用户修正采纳率，自动生成系统的ROI与健康度评估报告。
}

---------------------
O1
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1. 输入与结构化契约
        1.1 所有输入必须经系统处理并结构化为 JSON 格式（Pydantic v2 schema），摘要与原文均需存储；摘要需带置信度评分并满足预设质量阈值（如 embedding 相似度/摘要覆盖率），低于阈值触发 Self-RAG 或返回“数据不足”状态。
        1.2 所有 Schema 均版本化并纳入 CI/CD 契约测试，非兼容变更将阻断发布（见三·13/四·13 中的 ToT 触发规则）。
    2. 角色与协议（Agent Protocol）
        2.1 AGENT 以“角色”为最小自治单元，每个 Agent 实例需实现 Agent Protocol：能力描述、输入/输出 Schema、权限范围、仲裁信号、证据包格式（Evidence Pack）与版本号。
        2.2 Agent Protocol 必须可机读（JSON Schema），并记录每次调用的证据包：输入摘要哈希、匹配文档片段ID、工具版本、置信度、推理路径摘要等。
    3. 任务类别与视角隔离（含受控交叉）
        3.1 AGENT 任务分为（A）角色内自主分析与（B）跨角色协作决策；两类任务均需遵守视角隔离原则，但高不确定性或高风险任务将自动触发“跨视角审查任务（Cross-Check）”，由指定 Auditor 汇总并给出评分。
        3.2 视角隔离通过权限/工具白名单实现，且支持受控的交叉审查（审计链与证据打包）。
    4. 基本工作流（契约化 + 回收）
        4.1 标准工作流：感知（读取结构化 JSON）-> 管理会话生命周期（Snapshot + TTL） -> [思考] LLM 推理（function-calling 或 schema 输出） -> 解析 LLM 输出（契约校验）-> 执行（工具/沙箱）-> Observe -> Writeback（持久化证据包）-> 循环/退出。
        4.2 任何 LLM 输出在进入执行前必须通过 Schema 校验 / Instructor Patch 强制格式化；若校验失败，则进入修正重试逻辑或人工检查（根据任务风险与 SLA）。
        4.3 会话必须实现 Snapshot 版本、会话 TTL 与自动资源回收，防止长期会话与内存泄漏。
    5. 用户反馈与修正闭环（反馈引擎）
        5.1 用户反馈被结构化打标签并存入修正轨迹库；高频反馈自动生成 Few-Shot 样本，供 Prompt Optimizer 使用。
        5.2 定义反馈优先级与回归验证规则：任何自动化修正需通过回归测试集（Strat-Bench）及契约测试才能在生产默认策略中生效。
    6. Agent 提示词管理与 Prompt Optimizer（门禁式演进）
        6.1 所有 Agent prompt 纳入版本控制（Git LFS），变更需关联回归测试结果与多目标评估（结构、一致性、成本）。
        6.2 Prompt 自动演进仅在 SP/BP Checkpoint 完成态并通过多指标评估时触发；演进必须可回滚并有审计记录。
    7. 工具箱调用与治理
        7.1 Agent 从中央技能仓库调用工具（工具需注册并提供 metadata：版本、权限、契约测试覆盖率、灰度状态、回退策略）。
        7.2 所有工具的输入/输出 Schema、验证器与演进日志需公开（系统内）；工具注册必须通过 Pydantic 校验与 CI 流水线的契约门禁。
        7.3 工具调用前进行权限校验、白名单审计与最小权限原则，执行类工具（发邮件、操作系统、网络）必须在沙箱/白名单网关下运行。
    8. 协作仲裁与 CEO Agent 裁决状态机（可配置超时策略）
        8.1 Agent 间协作以任务编排拓扑为准，高级代理（如 CEO AGENT）可发起仲裁，系统记录全部仲裁证据包与决策路径。
        8.2 CEO AGENT 内置裁决状态机：定义最大辩论轮次、可配置 Temperature Decay、每轮写入证据包；当未达成一致时生成 Plan A（保守）与 Plan B（激进），但默认**不得**在没有人工/二次授权的情况下对高风险决策自动生效。超时行为为默认保守且异步通知（可配置介入窗口）。
    9. AUD AGENT（联席审计官）：旁路监听 + 可切换阻断模式
        9.1 AUD AGENT 主要职责：实时/近实时一致性检验（原文 vs 生成）、幻觉检测、工具调用合规性检查、阻断触发与错误上报。
        9.2 AUD 模式支持“实时阻断”与“异步阻断”切换（由任务风险/SLAs 决定）；阻断与纠错记录必须带标准化证据包与建议修复代码/提示。
        9.3 若连续 N 次（可配置，默认 3）校验失败，触发人工介入并记录任务暂停点，纠错后可从暂停点恢复。
    10. 上下文压缩器、公共黑板與 StateSnapshot 管理
        10.1 支持上下文压缩器以降低上下文成本，但压缩必须**可解释**：保留被压缩内容的引用 ID、摘要置信度与关键事实哈希，便于回溯（候选回溯策略）。
        10.2 公共黑板（Public Blackboard）维护关键结论而非全量历史；使用 LangGraph StateSnapshot + Redis 管理，StateSnapshot 带版本、哈希与回溯链。
    11. 可解释性與决策溯源（Evidence Pack / ToT）
        11.1 每次 Agent 分析输出都必须打包 Evidence Pack：输入摘要哈希、引用切片 ID、工具版本、推理链摘要、置信度评分与证据级别（原文/推断/外部验证）。
        11.2 支持交互式溯源树（分层展示：结论 → 关键证据 → 原始片段），并将 ToT（Tree-of-Thought）溯源机制作为按需/动态触发功能（非仅在阶段结束时启用）。
        11.3 ToT 的触发器包括：阶段结束置信度阈值、工具冲突告警、异常变更或审计告警，支持增量 ToT（中期回溯）。
    12. 沙箱执行、数值计算与 Validation Feedback
        12.1 所有可执行代码与数值计算需在隔离沙箱或持久化 Jupyter Kernel 中运行；沙箱默认断网，仅允许通过白名单网关访问受控 API（财经/指标等）。
        12.2 Agent 对数值/优化问题（如线性规划）应生成代码并调用 CVXPY/OR-Tools 等求解器；所有执行捕获 STDOUT/STDERR，自动检索错误案例库并生成修复建议，设最大重试次数（默认 3）。
    13. Schema 强制、契约测试与一致性仲裁器
        13.1 所有工具输入/输出、Agent 输出都强制遵守契约（Pydantic v2）；通过 Instructor Patch 强制模型输出符合 Schema。
        13.2 设立一致性校验仲裁器：当多工具产出冲突时，通过规则引擎与财务/业务常识库进行冲突检测，输出冲突报告并给出仲裁建议（供 AUD 或 CEO AGENT 裁决）。
        13.3 在 CI/CD 中集成自动契约测试，契约测试失败将自动阻断该工具/Agent 的灰度发布（三级成本/质量门禁）。
    14. 性能监控、行为分析與熔断策略
        14.1 建立统一 Agent KPI：成功率、修正率（用户/系统修正）、幻觉率（输入-输出不一致）、平均响应时延、每任务成本（Token/USD）等；并定义默认报警阈值（例如修正率 > 15% 触发 Skill Review）。
        14.2 支持三层熔断策略：任务级（单次成本超限）、会话级（日配额超限）、重试级（连续代码执行失败）；熔断动作包括回退、降级模式与人工告警。
        14.3 所有监控与评估结果纳入可视化健康度仪表盘并保留历史以做漂移检测与回归分析。
    15. 决策产品化與合规
        15.1 任何由 Agent 生成并交付的“决策”均需封装 Evidence Pack 与操作清单，作为合规与审计资料；高风险决策需额外二次授权（人或高级 Agent）。
        15.2 支持将 Agent 输出封装为可发行的产品（带版本、计费、SLA 与审计日志），便于商业化与权限管理。
    16. 可扩展性與演进数据集
        16.1 匿名化执行轨迹与修正快照可注入工具箱演进数据集（Strat-Bench），用于后续 A/B 和回归训练。
        16.2 所有演进均需通过多目标评估（结构、一致性、成本）与回归测试方可纳入生产默认策略。
    17. 验收条件（示例）
        17.1 Schema 校验覆盖率达到 95%，合同化输出在 100% 的关键任务中被强制执行。
        17.2 AUD Agent 对高风险任务能在 < X 秒 内发出阻断指令（X 由 SLA 决定）；阻断成功率 100% 且具备证据包记录。
        17.3 系统对于连续三次工具输出冲突能在 1 小时内完成一次人工介入或自动回退（取决于任务级别）。

}

O2
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入与预处理
        1.1 输入必须经系统数据处理管线结构化为 JSON 格式，JSON 必须包含 schema_version、source_list（文档切片 ID 列表）、摘要(text)、摘要_confidence（0~1）、original_hash。
        1.2 系统在写入上下文前对输入执行契约校验（Pydantic V2 / JSON Schema）。契约不通过时触发自动纠正尝试（若可修复）或标记“数据不足”并生成人工介入任务。
        1.3 摘要与全景视图同时存储摘要元数据（生成模型、生成时间、置信度与来源切片），便于后续溯源与回归测试。

    2.AGENT 任务与角色定义
        2.1 AGENT 按角色（CEO/COO/CMO/CTO/CFO/CHO/AUD）执行任务，系统必须为每个角色维护“能力矩阵（Capability Matrix）”——列出可调用工具、可读写上下文字段、最大并发任务数、权限等级（read/write/execute）。
        2.2 角色能力矩阵为可配置实体，纳入审计与 CI；变更需触发契约测试通过。
        2.3 AGENT 的职责范围与“视角隔离”由能力矩阵强制执行：除明确白名单外，AGENT 无权访问其他角色私有上下文字段。

    3.AGENT 基本工作流（规范化 artifact）
        3.1 标准循环：感知（Perceive）-> 思考（Think）-> 执行（Execute）-> 反射（Reflect）-> writeback。每步必须产出结构化 artifact 并写回会话沙箱：
            - 感知产物：input_summary（含置信度, source_list）
            - 思考产物：planner_task_json（包含任务分解、所选工具列表与版本、依赖边）
            - 执行产物：tool_invocation_log（工具名、版本、入参、出参、exit_code、stderr/stdout 哈希）
            - 反射产物：reflection_log（是否继续循环、异常说明、修正建议）
        3.2 每个 artifact 都带有唯一 trace_id 与完整性哈希（用于 ToT / 溯源），并存入证据包（Evidence Bundle）。

    4.用户反馈与修正闭环
        4.1 支持结构化用户反馈 Schema（{path_to_change, old_value, new_value, reason, priority}），所有反馈写入修正轨迹库并可绑定到相关 artifact。
        4.2 系统支持“自动吸收规则”：当同类反馈频次超过 configurable_threshold 时，可触发自动生成 Prompt/Skill 更新草案并通过回归契约测试后上线；否则进入人工审查流。

    5.AGENT 提示词（Prompt）与 Prompt Optimizer 管理
        5.1 所有 Agent Prompt / Prompt-template 视作代码：纳入 Git（含改动原因、作者、时间）。每次变更触发 CI 中的契约测试（示例输入->期望输出结构）与回归集跑通。
        5.2 Prompt Optimizer 基于“修正日志→Few-Shot 样本”闭环自动生成候选 Prompt，候选变更需通过 A/B 测试与回归后才能在生产会话默认生效。
        5.3 Prompt 版本带有 meta（兼容模型、适用场景、回滚标签）。

    6.AGENT 与工具箱协作机制
        6.1 Planner 在任务编排中必须显式列出映射工具（含版本号）与输入/输出 schema，Executor 在调度前执行工具可用性与兼容性检查。
        6.2 工具注册到中央技能仓库需通过契约测试，并带入灰度发布/回滚机制。技能仓库记录工具演进日志与匿名化执行轨迹样本（用于演化训练）。
        6.3 工具选择与调用在执行日志中透明可查，并支持审计追溯。

    7.仲裁与协作
        7.1 支持 AGENT 之间利益最优的协作框架，高级代理（如 CEO AGENT）拥有仲裁能力，但仲裁必须输出**仲裁证据包**（包含对立观点、关键证据、置信度矩阵、最终判定理由）。
        7.2 仲裁状态机参数（最大辩论轮次、Temperature Decay 策略、超时行为）均为可配置策略，且每次仲裁需写入可回滚的证据与回滚窗口（configurable，默认 = 24 小时）。
        7.3 用户介入行为由策略控制：可设为“强制人工确认/可选人工确认/默认自动判定”，并在界面或 API 中清晰暴露。

    8.视角隔离與公共黑板
        8.1 实施“公共黑板”机制，但仅允许传递结构化结论字段（如：KPI_delta, recommended_action_id, confidence_vector），禁止直接传递原始敏感数据。
        8.2 任何跨视角数据交换都必须带上来源切片索引与最小化字段列表（并写入审计日志）。

    9.CEO AGENT 裁决细则（增强）
        9.1 CEO AGENT 裁决状态机包含：论证阶段、证据收集阶段、降温/收敛阶段（Temperature 调整）、决策发布阶段与回滚窗口。
        9.2 裁决发布后，若在回滚窗口内发现新证据或用户申请撤销，可触发“回滚/二次仲裁”流程；裁决始终保留可解释性证据包供审计。

    10.AUD AGENT（联席审计官）职责强化
        10.1 AUD AGENT 执行旁路监听并实时校验生成内容与来源一致性；对差异提供“自动修复建议（patch）”。若自动修复失败则生成暂停点并创建人工纠错任务。
        10.2 AUD AGENT 的报警策略支持可调阈值，误报率与漏报率纳入健康度指标，定期由红队与人类审计评估。

    11.上下文管理、压缩器与 StateSnapshot
        11.1 支持上下文压缩器，但压缩器须输出“摘要覆盖率（%）”与“保留证据索引”，并保证关键信息（被索引的证据切片）在压缩后仍可恢复定位。
        11.2 公共黑板/StateSnapshot 存储需包含版本号、完整性哈希与产生时间，且 Snapshot 支持定位到具体 artifact trace_id。

    12.可解释性与决策溯源
        12.1 每次关键决策输出必须打包“结构化推理证据包”，包含：输入摘要哈希、Planner 图、工具版本、关键中间变量、置信度评分、source_ids 列表。
        12.2 系统提供交互式溯源接口，允许从结论逐层展开至具体文档/切片与计算轨迹。

    13.ToT（思维链溯源树）与置信度策略
        13.1 ToT 机制在 SP/BP 关键节点默认启用；此外支持对中间关键子任务按“策略触发器”启用（如置信度阈值、KPI 风险暴露、人工标注请求）。
        13.2 置信度阈值可配置并支持动态学习调整（基于历史决策质量与修正率），默认阈值 0.6 仅为初始建议值。

    14.Agent 性能监控、质量门控與行为分析
        14.1 定义最少 KPI 集：检索准确率、工具成功率、修正率、平均响应时长、单次任务成本（USD/Token）、误报率。将 KPI 纳入仪表盘并支持告警规则。
        14.2 实施三级熔断机制（任务级/会话级/重试级），并将契约测试失败、修正率超阈等事件与工具熔断联动。
        14.3 建立行为分析框架，匿名化执行轨迹可用于 Skill 演化、Prompt 优化与 Strat-Bench 扩充样本。
}

O3
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT

总体约束与通用契约：
  - 所有 AGENT 输入/输出、工具接口、仲裁消息、证据包等均须采用 Pydantic V2 定义的 JSON Schema（下称“Agent Schema”），并在 CI/CD 中强制契约测试通过方可发布。
  - 每次 Agent 运行均生成不可篡改的证据包（Evidence Bundle），包含：输入摘要哈希、原始文档切片ID列表、Planner 任务分解图、工具/技能版本、关键中间变量及其哈希、置信度评分、执行时 Token/成本记录与最终输出指针。
  - 建立中央 Skill Registry（工具/技能仓库），工具元数据必须包含：接口 Schema、capabilities、cost_estimate、risk_level、permission_scope、version、回归测试标识。所有工具入库需通过契约与回归测试。
  - 强制实现最小权限与沙箱执行策略：代码执行环境默认断网，外部访问仅通过白名单网关；有权限的工具/Agent 才能调用敏感 API，所有调用纳入审计。
  - 设定监控与熔断策略：定义关键指标（幻觉率、工具成功率、回退率、平均延迟、Token/cost）、报警规则与三级成本熔断（任务级/会话级/重试级），超阈值自动触发回退或人为审计流程。

具体特性：
  1. 输入数据处理与摘要接入
     - AGENT 接收经系统处理并结构化为 Agent Input JSON 的数据。Input JSON 首先包括摘要字段（含摘要置信度与摘要 Schema），Agent 首先读取摘要构建全景视图，再按需检索原始切片挖掘细节。
     - 若摘要置信度低于阈值（默认0.6），触发人工 Checkpoint 或自动扩展检索策略（例如扩大检索范围或请求更多文档）后再继续分析。

  2. AGENT 任务分类
     - AGENT 分为两大任务类型：一）按角色定义的自主数据分析任务；二）跨角色/阶段协作的战略规划决策任务。每类任务均必须输出符合 Task-JSON 的结构化描述（Task schema 包含子任务、依赖数据、优先级、预算/成本预估、回退策略）。

  3. AGENT 基本工作流（Planner → Executor → Auditor → Reflect）
     - 感知：配置执行环境（会话权限、资源配额、风险偏好），载入上下文 Snapshot（StateSnapshot）并校验 Input Schema。
     - 思考（Planner）：Planner 输出机器可读的任务编排 JSON（Task-JSON），包含子任务列表、建议工具映射、数据依赖边与预估成本。
     - 执行（Executor）：Executor 按拓扑顺序调度子任务，针对每个子任务实行 Think→Code→Execute→Observe→Writeback 的原子循环；所有工具调用采用明确的 Function/Tool Call 协议（结构化 JSON），并记录调用元数据与返回。
     - 验证（Auditor）：每次输出经 Auditor 校验（一致性、契约合规、财务常识库检查等），通过则写入持久化沙箱内存变量，失败则按回退策略重试或触发人工审计。
     - 反射（Reflect）：Executor/Auditor 的输出被回写到会话上下文；Planner 判断是否继续循环或完成并输出最终证据包。

  4. 用户反馈与修正（HITL）
     - 系统支持 Checkpoint 人工介入：在关键决策节点或低置信度环节自动暂停并请求用户验证中间假设。用户的每次修正被记录为结构化修正日志（含修正原因、修改内容、决策前后对比）。
     - 用户修正可匿名化后进入“反馈-样本化”流水线，由 Prompt-Optimizer/Prompt 管理系统转换为 Few-Shot 样本或提示词参数更新候选，且必须通过回归测试/人工门控后方可部署。

  5. Prompt 管理与 Prompt-Optimizer
     - Prompt/Policy 均纳入版本管理（Git LFS），任何演进需满足多目标优化（结构一致性、一致性校验、成本限制）并通过契约测试与 A/B 试验。
     - Prompt 自动演化仅在 SP/BP Checkpoint 完成且通过一致性阈值时触发，且演进过程不得改变核心战略方法论逻辑；所有演化均支持一键回滚。

  6. 工具调用与 Skill Registry
     - Agent 按任务需求从 Skill Registry 中选择工具。工具具有 capability descriptor 与 permission scope；在运行时先做 capability/permission check。
     - 支持工具链编排（有向无环图）与依赖调度；内置任务调度器按依赖自动并行调度无依赖子任务并保证执行顺序的幂等性与可回放性。

  7. 数值计算与安全沙箱
     - 强制代码执行隔离：所有数值计算/统计/优化必须通过沙箱化执行（Jupyter Kernel 或等效隔离环境），LLM 不直接在主环境中运行数值计算。数值问题可由 LLM 生成 Python/CVXPY/OR-Tools 模型并提交沙箱执行，沙箱返回结构化结果。
     - 沙箱捕获 STDOUT/STDERR，并自动检索错误案例库辅助修复；设最大重试次数（默认3次），失败则标记不可行并写入演进日志。

  8. 契约输出与一致性校验仲裁器
     - 所有工具/Agent 输出必须遵循 Agent Output Schema（Pydantic V2）。系统内置一致性校验仲裁器：检测跨工具/跨变量逻辑冲突（如成本与利润矛盾），输出冲突检测报告并将争议提交高级 Agent（或人工）裁决。
     - 契约测试集与 CI/CD 强制门禁：契约测试失败则阻断任务并触发回退或人工干预。

  9. 可解释性、溯源与 ToT（Trace-of-Thought）
     - 输出结构化推理证据包（Evidence Bundle）：包含输入摘要哈希、Planner 任务分解图、工具版本、关键中间变量、置信度评分、引用源（文档切片 ID）等，支持前端逐层展开证据树（数据来源→计算逻辑→中间结论→最终结论）。
     - ToT 启用策略：默认仅在 SP/BP 阶段结束时生成完整 ToT 以节省成本；同时对中间关键步骤若置信度 < 可配置阈值则分层启用局部 ToT，用以早期错误检测与修正。ToT 输出必须可映射回具体证据包条目。

  10. 联席审计官 AUD AGENT 模式
     - AUD 采用“旁路监听 + 实时/异步阻断”模式：不参与生成决策，但持续校验生成内容与原始证据的一致性。当检测到冲突/幻觉时通知相关 Agent 纠错。
     - 明确自动阻断阈值（如连续3次一致性检验失败→自动阻断并上报人工审计），并在阻断时保留可回放日志与恢复点，以便人工介入后从暂停点恢复任务。

  11. 公共黑板与上下文压缩（StateSnapshot 管理）
     - 支持上下文压缩器与“公共黑板”机制，在多轮会话间只传递关键结论与必要上下文，而非全量历史。所有 StateSnapshot 都版本化、哈希，并存储在受控 Redis/持久层。
     - Snapshot 的访问受 RBAC 控制，每次更新记录差异索引，保证可回溯并可恢复到历史版本。

  12. 性能监控、行为分析与演进数据集
     - 建立 Agent 性能监控框架：指标包括检索准确率、摘要质量、工具成功率、修正率、幻觉率、Token/成本消耗等。监控面板支持实时可视化，并配置报警与自动熔断策略。
     - 匿名化执行轨迹入库作为工具/Skill 演进数据集（Strat-Bench 扩增样本），供 Prompt-Optimizer 与 Planner 拆解策略优化参考。

  13. Checkpoint、A/B 测试与决策质量评估
     - 支持 SP/BP 各阶段 Checkpoint 机制：阶段任务完成后暂停并输出阶段性结果，用户可反馈并修正后继续执行。Checkpoint 策略为可配置（即时/异步/人工触发）。
     - 支持 A/B 测试框架以并行比较策略效果；建立决策质量评估器（基于贝叶斯网络/置信度评分），用于判定方案可靠性与上线条件。

  14. 成本、合规与熔断
     - 记录每次任务 Token 消耗、预估 USD 成本与执行时长；引入三级成本熔断（任务级/会话级/重试级），连续超过阈值触发熔断与人工审计。
     - 所有外部调用与敏感数据操作需通过合规审计链与访问授权记录。

  15. Agent 行为合规与视角隔离
     - 强制视角隔离规则：Agent 仅在其 permission scope 内操作；跨视角信息共享需通过明确合约（字段级共享声明、共享理由与批准者）并受审计。
     - CEO Agent 作为仲裁器：实现“裁决状态机”，设置最大辩论轮次并结合证据置信度与风险评估动态调整 Temperature Decay 与仲裁策略；取消僵化的10秒自动采纳，改为可配置 Checkpoint 策略与超时后基于风险/证据等级的安全处理（例如暂时封存/人工审查/或有条件采纳）。

  16. 可视化与用户接口（命令行与审计视窗）
     - 提供决策过程可视化接口：展示关键决策路径、引用证据、工具版本、置信度与成本；支持从结论逐层展开溯源树与回放中间执行步骤。
     - 支持用户交互的 Checkpoint 命令：包括修改权重、调整约束、选择采纳或回退方案，并保存专家修正轨迹以供后续演进使用。

  17. 性能基准与回归测试
     - 构建 Strat-Bench（包含 20+ 专家标注场景）用于版本迭代准入测试；任何工具/Agent 的影响性变更必须通过基准与回归测试方可在生产灰度/全量发布。
     - 支持 A/B 框架，用任务完成率、计算精度、成本指标做优选晋级。

  18. 伦理、安全与审计
     - 建立工具幻觉黑名单与自动锁定机制：若某工具连续 N 次（默认3）生成与输入矛盾的结论，自动锁定并报警，且需人工复核后才解锁。
     - 所有自动化决策链在执行前须做隐私/合规评估（如涉及个人敏感信息），并自动化脱敏或拒绝执行不合规请求。
}

O4
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1. 输入与全景构建
        1). 系统接收经数据处理模块规范化并版本化的 JSON 格式数据；所有输入/摘要/全景视图必须遵循 Pydantic V2 定义的 Schema（含版本号），并在元数据库中记录 Schema 版本、摘要哈希、摘要置信度分数及来源文档切片ID。
        2). 摘要生成需附带质量评分（自动化评估指标）与哈希；当摘要质量低于设定阈值（可配置，默认 0.6）时，AGENT 在检索/生成阶段须回退到原始切片并标注“摘要置信度不足”以避免盲发生成。
    2. AGENT 任务类型与职责边界
        1). AGENT 执行两大类任务：角色定义的自主分析任务（角色内视角职责）与跨角色协作分析/决策任务；每个 AGENT 的能力、权限和工具调用范围通过中央技能仓库的权限标签（RBAC）与契约声明明确定义并版本化。
        2). 对跨角色协作任务，系统要求提供协作协议（consensus policy）：可配置为自动仲裁 / 多方投票 / 人工裁决 / 双签策略；任何最终决策均纳入证据包并可事后复核。
    3. AGENT 基本工作流（契约化）
        1). 工作流标准化为：感知（读取并校验 Schema）-> 管理会话生命周期（会话上下文、Snapshot）-> 思考（LLM 推理，强制契约化输出 JSON）-> 执行（调用工具/子 Agent /沙箱运行）-> 反射（回写工具输出并更新上下文）-> 决策是否继续循环或终止。
        2). 每一阶段必须暴露明确输入/输出契约与失败码。阶段超时、重试、回退与降级策略须可配置；所有阶段事件写入可审计事件流（含时间戳、实例ID、执行成本）。
    4. 用户反馈与修正闭环
        1). 支持用户对 AGENT 分析结果的交互式反馈；反馈以结构化修正日志保存，修正会触发自动化回归测试（若适用），并在通过后将修正规则、Few-Shot 示例或 Prompt 更新纳入版本控制（Git LFS），同时写入技能演进数据集。
    5. AGENT 提示词与 Prompt 管理
        1). AGENT 提示词/模板纳入 Prompt 管理仓库并版本化。Prompt Optimizer（基于 DSPy 思想）输出需通过安全扫描器、契约测试及小样本回归测试。Prompt 的更新必须记录变更理由与回滚入口。
        2). 在任意 Prompt 变更前，必须评估变更对安全/合规/成本的影响，重大变更需人工审批后发布。
    6. 工具调用与选择
        1). AGENT 按任务需求从工具箱中选择工具；每个工具在技能仓库中包含唯一标识、输入/输出 Schema、版本、权限标签、调用配额、成本估算及验证器。
        2). 工具调用遵循 RBAC 与调用配额限制；工具失败需支持幂等重试与明确回退策略，调用记录纳入证据包。
    7. AGENT 间协作与仲裁策略
        1). AGENT 间协作以显式 Planner 输出的任务编排 JSON 为依据（包含子任务、工具映射、数据依赖）；协调冲突时启用“协作仲裁协议”（可配置共识阈值或人工优先策略）。
        2). 高影响决策（阈值可配置）需采取更严格的仲裁策略（例如双签/人工确认/多方投票），并在决策生效前生成异议工单供审计与后验复核。
    8. 视角隔离与受控共享
        1). AGENT 默认在其角色视角内运行，数据访问受最小权限原则约束；任何跨视角数据共享须通过“受控跨视角桥接”流程（满足权限审计与合议条件）或通过专门的中介 Agent 转译/脱敏后共享。
    9. CEO AGENT 裁决状态机（改进）
        1). CEO AGENT 保留仲裁职能，但仲裁策略为**可配置**：支持自动仲裁、人工优先、投票或双签。仲裁触发条件和最大辩论轮次应结合“证据质量”与“影响度”而非仅轮次。
        2). 用户介入窗口为可配置超时（UI/CLI 显示与告警）；默认行为为“超时后记录审计决策并将异议工单入队列，决策可被事后复核或回滚”，而不是立即不可逆生效。
    10. AUD AGENT（联席审计官）职责与响应等级
        1). AUD AGENT 以旁路监听为主，负责实时一致性校验、幻觉检测与合规检查；在检测到不一致或高风险输出时按分级策略响应：告警（记录）、自动修正（若有高置信度补救）、阻断（暂停任务）或人工介入（创建纠错工单）。
        2). AUD 的干预规则、错误分级与阈值可配置；对同一工具连续异常应触发工具黑名单并自动进入人工复核流程。
    11. 上下文压缩器、公共黑板与状态快照
        1). 在多轮对话中采用“公共黑板 + 状态快照（StateSnapshot）”管理，会话关键结论与最小可追溯证据被保留；Snapshot schema、过期策略与版本管理必须明确定义，压缩策略须在保留可溯源性的前提下最大化效率。
        2). 支持对关键结论生成反向索引以便溯源（结论 → 证据片段）。
    12. 推理过程可解释性与置信度校准
        1). AGENT 输出必须包含解释性元数据（决策依据、关键因素、引用切片、置信度分数与不确定性说明）。置信度计算方法需标准化（例如校准后的概率/贝叶斯网络输出），并与 Checkpoint/ToT 逻辑联动。
    13. ToT（思维链溯源树）与动态触发机制
        1). ToT 不再仅在阶段完成时被动启用；系统支持“终期触发 + 中期动态触发”：当置信度低、证据不一致、或任务影响度高时可即时触发 ToT；阈值与触发策略可由 AUD/管理员配置。
        2). ToT 输出纳入证据包并支持前端逐层展开，供用户/审计查看。
    14. Agent 性能监控、行为分析与告警
        1). 建立完整的监控指标体系：任务成功率、工具成功率、修正率（人工与自动）、幻觉率、平均成本（Token/USD）、延迟、异常事件率等。系统支持可配置告警策略与三级成本熔断（任务级/会话级/系统级）。
        2). 所有监控数据与事件日志可视化并支持按任务回溯、按工具回溯与按 Agent 回溯的调查 playbook，以便快速定位与修复问题。
    15. 兼容性、测试与部署准入
        1). 所有 Agent、工具与 Prompt 变更必须通过自动化契约测试与 Strata-Bench（战略分析基准测试）准入；不兼容或未通过回归的变更禁止上线（CI/CD 门禁）。
    16. 成本与审计
        1). 每次 Agent 执行记录 Token 消耗、估算 USD 成本与执行时长；系统支持基于成本/效益的策略（例如低价值任务降级为摘要或人工提示），并对高成本行为自动触发成本熔断或人工审批。
    17. 隐私、安全与合规
        1). 默认网络隔离并采用白名单网关访问受信任外部 API；敏感数据访问与处理需满足企业合规策略（脱敏/最小化），并记录访问审计。
        2). 建立注入防御、工具幻觉黑名单与自动化安全回归测试，定期执行攻防与抗注入验证。
    18. 演进与学习闭环
        1). 匿名化的执行轨迹、修正轨迹与演化样本入工具箱演进数据集（Strat-Bench 扩展），用于 Planner 优化、Prompt 优化与技能升级；所有演进样本需先脱敏并通过隐私审计。
    19. 可扩展性与集成
        1). Agent 模块通过 OpenAPI/OpenTelemetry 标准与外部系统集成，支持水平扩展、分布式 Trace 与跨服务追踪。
    20. 文档与操作手册
        1). 每个 AGENT 与工具须附带机器可读的 IDENTITY.md、TOOLS.md、BOUNDARY.md（定义角色/契约/安全边界）与变更日志（演进日志）。
}

O5
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1. 输入与摘要契约化与可观测性
       1. 所有输入必须经系统处理并以 Pydantic V2 定义的 Input_Summary_Schema 与 Full_JSON_Schema 存储；每个摘要与原文均带版本号、摘要质量评分（自动/人工）与证据哈希。
       2. 当摘要或检索相关性低于设定阈值（可配置）时，触发“数据不足”路径并暂停自动生成，转入人工校验或替代流程（Self-RAG保留）。
       3. 所有输入/摘要版本与质量评估写入证据包并供后续回溯。

    2. 角色能力与权限矩阵（Capability & Least-Privilege）
       1. 为每个 AGENT 定义 capability tags、可调用工具白名单与字段级数据访问权限（视角化数据租户）。
       2. 工具注册需包含元数据：capabilities、risk_score、required_permissions、schema_in/out；Agent 调用前必须通过权限检查与风控评分。
       3. 所有工具调用与权限校验操作记录审计链（不可篡改标识）。

    3. AGENT 工作流契约化与幂等事务边界
       1. AGENT 基本工作流保留：感知（Perceive）-> 管理会话生命周期 -> 思考（LLM推理）-> 执行（工具/算子）-> 反射（回写）-> 释放资源。
       2. 每一步强制输出结构化 JSON schema、版本号、trace_id、时间戳，定义事务边界、最大重试次数与回滚语义以保证幂等性。
       3. 中间产物写入持久化沙箱/证据仓，并可按需冷存以备 ToT/审计回溯。

    4. 用户反馈与修正闭环（Feedback→演进）
       1. 统一反馈 schema（type, priority, delta, author, timestamp）；系统定期分析反馈热区。
       2. 高频修正样本自动打包为 Few-Shot/Spec 基准并进入演进灰度发布流，必须通过 CI 的契约回归测试后才能全量发布。
       3. 所有修正轨迹匿名化存储并作为 Prompt/Spec Optimizer 的训练/微调数据。

    5. AGENT Prompt / Spec 管理与 CI 流水线
       1. 所有 Prompt/Spec 文件纳入 Git（含 LFS）；演进必须满足一致性阈值、回归测试通过与成本约束（多目标）。
       2. Prompt Optimizer 基于“修正日志→样本转换”闭环自动生成 Few-Shot 样例；演进触发策略可配置且记录审计。

    6. 工具箱调用与编排
       1. AGENT 可按任务需求从工具箱选择合适工具；工具箱实现统一注册、版本控制、灰度发布与回滚。
       2. 支持工具链编排（DAG），上游输出结构化传递给下游；内置任务调度器支持并行执行无依赖子任务。
       3. 工具入库必须通过 Pydantic V2 契约化测试与安全审计（静态安全扫描与动态行为检测）。

    7. 仲裁器与人机在环（CEO AGENT 及仲裁机制）
       1. 支持企业利益最优的 AGENT 间协作；高级代理（如 CEO AGENT）可作为仲裁器，但仲裁策略参数化（仲裁模式：auto/human_first/hybrid、超时配置、最大辩论轮次）。
       2. 在仲裁过程中，随着轮次增加系统按预设 Decay 规则降低 Temperature 并要求提出具体修改意见；仲裁决策产生必须伴随置信度、风险矩阵与成本估算。
       3. 人类干预窗口为可配置（建议默认 5 分钟），支持多渠道通知；超时或未响应时根据配置进行审计型自动回退（例如保守方案执行或暂停并创建工单）。

    8. 视角隔离与数据最小化
       1. AGENT 严格从各自角色视角工作（如 CFO 仅关注资金/风险）；实现 Role-based view 与 Field-level masking，防止越权访问或泛化讨论。
       2. 调用工具时 Agent 必须声明视角与目的，并在证据包中记录该声明，便于后续审计。

    9. CEO AGENT 裁决状态机与多方案输出
       1. 内置裁决状态机，设定最大辩论轮次与 Temperature decay 策略；未达成一致时输出 Plan A（保守）/ Plan B（激进）以及可扩展的多方案集，每套方案附带置信度、风险矩阵、成本与执行路径。
       2. 停顿/用户介入窗口为可配置，提供 UI/API 回调机制；所有决策均写入不可篡改证据包。

    10. AUD AGENT（旁路监听 + 形式化审计）
       1. AUD AGENT 不参与生成，仅执行一致性校验：Schema 校验、引用溯源、事实核验与工具输出对照。
       2. 检测规则形式化且分级（warning -> block -> rollback）；若连续 N 次（可配置）校验失败，自动暂停任务并生成人工工单/疑难工单；所有检测与响应写入审计日志。
       3. AUD 可触发“异步阻断”或即时阻断，规则决策可在白名单/灰度策略内调整。

    11. 上下文压缩器与公共黑板（StateSnapshot）
       1. 支持上下文压缩策略：在多轮会话中仅传递关键结论，但压缩后必须保留 summary_hash、相关切片 ID 与 provenance_index，确保信息可逆及可回溯。
       2. 公共黑板采用事务式 StateSnapshot 管理（支持 optimistic locking），并将 full-history 写入冷存以供 ToT 与审计调用（不可变证据链）。
       3. 黑板更新与回退支持基于 snapshot 的回滚。

    12. Agent 推理可解释性与可视化
       1. 输出结构化推理证据包，包含输入摘要哈希、Planner 任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片ID）。
       2. 定义标准可视化 schema（decision_node, input_refs, compute_ops, confidence_score），支持前端从结论逐层展开证据树并导出 JSON/HTML。

    13. ToT（思维链溯源树）策略化触发
       1. ToT 在 SP/BP 各阶段作为可选且受控功能启用；触发由置信度阈值与任务风险等级联合决定（阈值参数化）。
       2. ToT 输出需绑定原始证据切片、每步推理节点与置信度，并纳入审计与纠错闭环。

    14. Agent 性能监控、行为分析与成本治理
       1. 建立指标体系（SLO）：成功率、修正率、平均响应时延、每任务成本（Token/USD）、工具成功率等；定义告警与自愈策略。
       2. 实施三级成本熔断（任务级/会话级/重试级）：当触及阈值自动熔断并触发人工复核机制。
       3. 将性能漂移检测、A/B 实验指标、修正率等纳入健康度仪表盘，并定期生成合规审计报告作为版本迭代准入条件。

    15. 测试、回归与演进保障（补充）
       1. 所有 Agent/工具/Prompt/Spec 的变更必须通过 Strat-Bench（含 20+ 专家标注场景）或等效回归测试，CI 不通过则阻断发布。
       2. 支持 A/B 测试框架、抽样审计与 Skill Review 流程（当修正率超阈值触发人工检视）。

    16. 合规与安全（补充）
       1. 强制实现最小权限、白名单网关与断网沙箱（默认断网，允许白名单访问可信 API）；代码执行沙箱化（持久化计算沙箱，任务结束后自动清理）。
       2. 建立提示词注入检测、工具幻觉黑名单与红蓝模拟（对抗测试）作为常态化安全评估。
}


D:D2 O3
Q:O3 Q2+O3
G:Q2 G4
O:Q3 Q3





## TEMP D2 Q2 Q3 G4 O3

你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。下面5个清单是针对[企业战略规划管理智库系统需求特性列表]->[AGENT]需求特性的重构，请你结合[企业战略规划管理智库系统需求特性列表]整体系统需求，对这些需求列表内容进行评估。要求：
1、逐条对比评估优劣，提出适合本系统的改进建议，给出该建议的必要性分析和可行性分析
2、评分维度：
   - 核心架构思想
   - 输入与感知
   - AGENT工作流
   - 用户反馈闭环
   - 提示词工程与演进
   - 系统自适应能力
   - 记忆与上下文
   - 工具调用与集成
   - 仲裁与决策机制
   - 审计与一致性
   - 创新前瞻性
   - 可观测性与可解释性
   - 安全与鲁棒性
   - 性能与监控
   - 成本效能
   - 企业级适用性
   - 工程可实施性
3、严格从[客观视角]进行评估
4、以表格形式呈现，表头：评估维度---D2评估---Q2评估---Q3评估---G4评估---改进建议 (适用于本系统)---必要性分析---必要性评分 (5分制)---可行性分析---可行性评分 (5分制)
-------------------
企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：支持基于网络LLM和先进Agent技术的智能分析工具集，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理（共计17个）。
    1.智能工具定义
        [1].建立工具标准：每个工具有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。工具初始化必须加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
        [2].构建中央技能仓库：实现工具的统一注册、版本控制、灰度发布与回滚机制。所有入库工具必须通过Pydantic V2契约测试，后续迭代需通过回归测试集。支持预定义模板与远期基于代码生成的技能动态扩展。
        [3].支持工具链编排与依赖调度：支持定义工具链有向无环图，实现上游工具输出结构化流转为下游输入。内置任务调度器，根据依赖关系自动并行执行无依赖子任务。
    2.工具箱标准工作流
        [1].环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具价值观模板（SOUL.md），实例化会话级沙箱与混合检索记忆容器。
        [2].感知与摘要：读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
        [3].规划：Planner依据用户自然语言指令及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、匹配工具映射、数据依赖边。
        [4].执行与验证：Executor按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe→writeback原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
        [5].证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
        [6].反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
        [7].成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
        [8].演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。
    3.数值计算与沙箱执行
        [1].强制代码执行隔离：严禁LLM直接进行数值计算、统计回归或现金流折现。所有量化分析强制通过两条路径执行：一是生成Python代码在沙箱执行；二是针对战略运筹问题（如线性规划），由LLM构建数学模型调用CVXPY、OR-Tools等优化器求解。
        [2].构建持久化计算沙箱：为每个任务会话实例化持久化Jupyter Kernel，支持跨工具、跨步骤的变量传递（如宏观经济变量在不同模型间共享），实现分析上下文连贯。任务结束后自动销毁内核并回收资源。
        [3].支持Validation Feedback闭环：自动捕获代码执行STDERR，检索错误案例库辅助LLM生成修复版本。设定最大重试次数（默认3次），失败则标记不可行并记录至演进日志。
    4.Schema强制与一致性校验
        [1].实施契约化输出：所有工具输入/输出严格遵循Pydantic V2定义的Data Model，利用Instructor Patch强制LLM输出符合Schema的结构化JSON，严禁输出自由文本。
        [2].建立一致性校验仲裁器：在多工具协作中，通过内置规则引擎及财务常识库检测逻辑冲突（如利润率与成本矛盾）。输出冲突检测报告，供用户或高级AGENT裁决。
        [3].自动化契约测试：在CI/CD流水线集成Schema契约测试，防止模型漂移导致结构崩溃。契约测试失败的任务强制终止。
    5.可解释性与决策溯源
        [1].输出结构化推理证据包：每次分析必须打包输出全量元数据，包括输入摘要哈希、Planner任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片ID）。
        [2].构建交互式溯源树：支持前端从结论反向逐层展开证据包，展示“数据来源→计算逻辑→中间结论→最终结论”的完整推理链。
        [3].支持红蓝辩论机制：针对高不确定性议题自动启动“红蓝对抗”，实例化激进派与保守派子智能体进行结构化辩论，输出包含共识与分歧区域的风险全景视图。
    6.提示词工程与演进
        [1].Prompt Optimizer：基于DSPy理念，建立“修正日志-样本转换”闭环，将用户修正转化为Few-Shot样本，自动迭代提示词参数以适配企业战略风格。
        [2].实施受控自动演进：仅在SP/BP Checkpoint完成态且满足一致性阈值时触发演进。必须采用多目标优化（结构、一致性、成本），严禁单一指标优化。
        [3].确保方法论不变性：演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
        [4].支持版本管理与回滚：所有Prompt纳入Git LFS管理，与系统版本绑定。支持一键回滚至历史稳定版本。
    7.用户反馈与闭环演进
        [1].支持Checkpoint人工介入：在关键决策节点或低置信度环节自动暂停，请求用户验证中间假设。支持用户修改参数或调整权重，系统记录修正前后的“专家修正轨迹”。
        [2].建立修正轨迹库与逆向传播：将用户修正快照匿名化存储，定期分析差异，推断输入权重或任务分解策略的优化方向。
        [3].实施用户反馈协议：将用户反馈转化为结构化修正日志，强制触发工具体“反思阶段”（ReflAct），并回传至技能演进日志。
    8.安全与防御机制
        [1].实施沙箱网络隔离：代码执行环境默认断网，仅允许通过白名单网关访问预设的可信财经API，严禁通用网页爬虫。
        [2].建立提示词注入防御：输入数据前置执行清洗与注入检测。将“注入防御测试”纳入基准测试集，定期验证工具体的抗干扰能力。
        [3].建立工具幻觉黑名单：自动监控工具体输出，若连续3次生成与输入矛盾的结论，自动锁定该工具体并报警。
    9.领域专业知识嵌入
        [1].内置领域事实库：以JSON Schema存储经典管理理论的强制约束（如SWOT内部/外部属性分类），确保分析逻辑符合理论框架。
        [2].部署领域验证器：工具体输出前必须经过验证器检查，确保结论不违背基础商业逻辑与领域常识。
    10.服务契约与集成
        [1].独立微服务封装：工具箱作为独立微服务对外暴露OpenAPI 3.1标准接口，输出分析结果、证据包与成本审计信息三元组。
        [2].支持Agent模块集成：与CEO、CFO等核心AGENT通过服务调用无缝集成，支持水平扩展与分布式Trace追踪。
    11.测试与质量保证
        [1].构建基准测试集（Strat-Bench）：建立包含20+专家标注场景的战略分析专用测试集，作为版本迭代的准入测试标准。
        [2].支持A/B测试框架：支持多版本策略并行运行，基于任务完成率、计算精度与成本指标择优晋级。
        [3].部署健康度仪表盘：实时监控工具成功率与修正率，修正率超标（默认15%）自动触发Skill Review。
    12.性能监控与漂移检测
        [1].建立性能漂移检测：定期评估检索准确率与生成质量，对比历史基线检测性能下降。
        [2].支持可视化日志：所有评估与运行结果纳入系统行为日志并可视化展示。
    13.成本熔断与契约测试质量门禁
        [1].CI/CD契约测试门禁：所有工具输出必须通过严格Schema校验，非兼容变更触发构建失败。运行时连续三次校验失败触发工具熔断。
        [2].实施三级成本熔断：支持任务级（单次Token/成本超限）、会话级（日配额超限）与重试级（连续代码执行失败）熔断机制，确保系统运行成本可控。

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节
    2.AGENT主要执行按角色定义自主分析数据、战略规划各阶段协作分析决策两大类任务，这两大类任务包括多种不同子类
    3.AGENT基本工作流：[感知]配置环境与上下文->管理会话生命周期->[思考]LLM推理->[执行]解析LLM输出决策任务完成或调用工具->[反射]回写工具输出到上下文并决策是否继续循环[思考]->构建输出并释放资源
    4.支持用户反馈与修正AGENT分析结果
    5.支持AGENT提示词生成与管理，按角色定义和分析框架生成适合LLM使用的提示词，基于DSPy理念建立AGENT Prompt Optimizer，根据AGENT分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数
    6.AGENT可按任务需求从工具箱中选择合适工具完成数据分析或战略规划任务
    7.支持企业利益最优的AGENT间协作，高级代理CEO AGENT拥有协作仲裁最终决定权，但用户可以观测和更改该决策
    8.支持视角隔离，AGENT仅从各自角色视角（如CFO仅关注资金风险）调用工具，严禁泛化讨论
    9.CEO AGENT内置"裁决状态机"作为仲裁器，设定最大辩论轮次，随着轮次增加，降低AGENT的Temperature Decay强制要求AGENT提出具体修改意见，而不是单纯否定，若未达成一致，生成Plan A（保守版）和Plan B（激进版）两套方案，CEO AGENT按偏好矩阵决策Plan A/B，同时暂停10秒提供用户介入裁决窗口，超时用户无动作则以 CEO AGENT 决策为准
    10.AUD AGENT采用"旁路监听+异步阻断"模式，不参与生成，只负责原文与生成内容的一致性检验，发现幻觉通知相关AGENT纠错，若3次重试失败，暂停任务人工纠错，之后从任务暂停点继续任务
    11.支持上下文压缩器与"公共黑板"机制，在多轮对话间仅传递关键结论而非全量历史，严格利用LangGraph的StateSnapshot机制配合Redis存储来管理公共黑板的更新与回溯
    12.支持Agent推理过程的可解释性可视化，包括决策依据、关键因素和置信度
    13.采用"思维链溯源树ToT"机制，只在SP/BP各阶段结束时启用ToT功能，当决策置信度低于阈值（默认0.6）触发ToT，通过回溯推理路径解决决策冲突，提高决策可解释性
    14.建立Agent性能监控与行为分析框架

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察(包括六个子步骤)、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据
-------------------
D2
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT

1. 输入与全景感知
    [1]. 输入经系统处理结构化为JSON格式的数据，首先构建标准全景视图（含关键指标趋势、异常波动、战略假设变量、关联图谱），再基于视图挖掘细节
    [2]. 支持动态视角切换机制：当细节挖掘发现与全景视图严重矛盾时，自动触发全景视图重构，重构过程需记录变更轨迹并提交AUD AGENT审核
    [3]. 建立输入数据质量预检：自动检测数据完整性、时效性、一致性，对低质量数据标注置信度衰减系数

2. 任务分解与协作架构
    [1]. 建立任务IR（中间表示）标准：每个任务包含任务ID、类型、输入/输出Schema、依赖任务列表、优先级、执行状态、超时策略
    [2]. 采用分层协作架构：CEO AGENT作为指挥官负责目标解析与全局任务分解，各专业Agent并行执行子任务，调度器负责资源分配与执行监控
    [3]. 自动生成协作依赖图：基于BLM/BEM阶段定义标准依赖关系，支持动态调整与优化，可视化展示任务间数据流与控制流
    [4]. 支持柔性视角隔离：定义核心视角（必须遵守）和扩展视角（可协商），对跨领域问题组建临时联合分析组，建立视角冲突解决协议

3. AGENT增强工作流
    [1]. 双阶段思考模式：阶段一并行识别关键约束（识别率>80%），阶段二逐步细化形成完整决策，整体采用“全局→局部”路径
    [2]. 工作流定义：[感知]环境初始化与上下文加载->[规划]生成任务执行图->[执行]按拓扑顺序调度子任务->[验证]结构化检查输出->[反射]错误分析与修正->[闭环]Early Stopping机制（置信度达标且目标达成时提前终止）
    [3]. 结构化反思机制：每次反思输出包含错误识别（类型/位置）、修正策略（具体修改方案）、预防措施（避免再次发生）、置信度评估
    [4]. 并行执行优化：无依赖子任务自动并行执行，依赖子任务采用流式处理，整体协作效率通过成本模型持续优化

4. 用户反馈闭环
    [1]. 建立反馈标准化协议：将用户反馈转化为结构化修正日志（修正前/后值、修正原因、修正类型、用户角色、时间戳）
    [2]. 实现即时反馈响应机制：设置专用反馈通道，承诺24小时内处理并回复，紧急反馈设置加急处理流程
    [3]. 反馈聚合分析引擎：定期挖掘高频反馈模式（每周/每月），自动生成优化建议并触发提示词微调，优化效果需通过A/B测试验证

5. 提示词工程与演进
    [1]. 采用双轨制演进策略：常规演进基于用户反馈自动微调（周频），重大演进必须通过战略方法论不变性校验（月频）
    [2]. 建立提示词版本控制：所有Prompt纳入Git LFS管理，与系统版本绑定，支持一键回滚至任一历史稳定版本
    [3]. 提示词契约测试：每次优化后自动运行Strat-Bench基准测试集，通过率<90%自动拒绝上线，并生成差异分析报告
    [4]. Prompt Optimizer基于DSPy理念，但优化目标必须多目标权衡（结构完整性、一致性、成本效率），严禁单一指标优化

6. 工具调用与集成
    [1]. 工具选择优化器：基于历史成功率（权重0.4）、Token成本（权重0.3）、执行时长（权重0.3）多目标优化选择工具，支持动态权重调整
    [2]. 工具组合推荐：针对17个战略工具预定义最佳组合模板（如SWOT+TOWS自动组合），支持用户自定义模板
    [3]. 工具调用轨迹分析：记录每次调用的成功/失败模式、参数设置、返回结果，持续优化选择策略，失败模式入库作为少样本示例
    [4]. 严格遵循工具契约：每个工具调用前验证输入Schema，调用后验证输出Schema，Schema违规模3次自动熔断该工具

7. 仲裁与决策机制
    [1]. CEO AGENT内置裁决状态机，设定最大辩论轮次（默认5轮），采用三阶段辩论：发散阶段（Temperature=0.8）、收敛阶段（Temperature=0.5）、裁决阶段（Temperature=0.2）
    [2]. 辩论规则：随着轮次增加强制要求提出具体修改意见而非单纯否定，第3轮后必须附带数据或逻辑支撑
    [3]. 多方案生成：若未达成一致，生成Plan A（保守版）、Plan B（激进版）和Plan C（AI融合版），CEO按偏好矩阵决策
    [4]. 用户介入窗口：暂停60秒提供用户裁决，窗口期可视化展示三套方案的差异对比（风险/收益/资源需求），超时默认采纳CEO决策
    [5]. 动态仲裁权转移：根据任务领域（如财务决策）自动将仲裁权临时转移给最相关专业Agent（CFO），CEO保留监督权和最终否决权

8. 审计与一致性保障
    [1]. AUD AGENT采用“旁路监听+异步阻断”模式，不参与生成，审计范围包括原文一致性检查、逻辑自洽性检查（跨工具结论矛盾）、商业常识验证
    [2]. 建立幻觉量化标准：定义置信度阈值（<0.6标记可疑）、引用缺失率（>30%标记警告）、逻辑矛盾指数（基于规则引擎检测），综合评分<0.7触发纠错
    [3]. 智能重试策略：首次失败通知纠错，二次失败自动切换验证方法或调整提示词，三次失败暂停任务人工介入，从暂停点恢复后继续执行
    [4]. 审计日志强制记录：所有审计发现、纠错过程、重试轨迹全量存储，作为模型性能分析的基础数据

9. 记忆与上下文管理
    [1]. 分层记忆架构：工作记忆（当前会话，有效期24h）、短期记忆（最近项目，有效期30天）、长期记忆（战略知识库，永久存储），各层有独立的存储引擎和检索策略
    [2]. 公共黑板机制：基于LangGraph的StateSnapshot + Redis存储，仅传递关键结论和状态哈希，完整推理轨迹归档至数据湖，支持按需解压缩回溯
    [3]. 关键结论提取标准：重要性评分（1-5分，基于对最终决策的影响度）、置信度（0-1）、引用源（文档切片ID）、时效性（有效日期范围），综合评分>3.5才写入公共黑板
    [4]. 长期记忆演化：将用户修正日志、决策复盘结果、执行结果差值反向固化为少样本提示词，形成动态演进的战略知识库，每季度触发知识蒸馏与去重

10. 可解释性与溯源
    [1]. 结构化证据包标准：每次分析打包输出全量元数据，包括输入摘要哈希、任务分解图、工具调用轨迹（含版本）、关键中间变量、置信度评分、引用源切片ID列表
    [2]. 交互式溯源树：支持从结论逐层展开至原始数据，展示“数据来源→计算逻辑→中间结论→最终结论”完整推理链，每个节点支持查看详细信息
    [3]. 反事实推理引擎：支持“如果改变某假设/权重/约束，结论会如何变化”的模拟分析，输出对比报告和风险提示
    [4]. 思维链溯源树（ToT）分级触发机制：低置信度（0.4-0.6）触发局部回溯（当前子任务），中置信度（0.3-0.4）触发全局回溯（整个阶段），低置信度（<0.3）触发人工介入

11. 性能监控与行为分析
    [1]. 建立三维度指标体系：效率维度（任务完成率、平均时长、Token消耗、工具调用成功率）、质量维度（修正率<15%、幻觉率<5%、一致性得分>0.8）、成本维度（单任务API成本、计算资源消耗）
    [2]. 构建战略规划专用基准测试集（Strat-Bench）：包含20+专家标注的战略分析场景，覆盖BLM/BEM全阶段，作为版本迭代的准入测试标准
    [3]. 三级预警机制：黄牌（指标下降15%触发分析）、红牌（下降30%触发人工review）、停用（下降50%且连续三天，自动回滚至前一稳定版本）
    [4]. 行为分析框架：定期（周/月）生成Agent行为报告，包含成功率趋势、高频错误类型、修正模式分析、成本分布，为系统优化提供决策依据

12. 主动式能力
    [1]. 引入心跳机制：支持周期性自动唤醒（可配置频率），主动检查战略档案库中的待办事项、偏差预警、周期性任务，无需用户提问即可主动沟通
    [2]. 战略偏差预警：实时监控实际执行数据与SP/BP规划的偏差，偏差超过阈值（默认10%）自动触发预警，生成偏差分析报告并推送相关AGENT
    [3]. 主动知识更新：当检测到行业报告、市场数据、政策法规更新时，自动触发电请相关AGENT评估影响，重要影响主动推送CEO

13. 安全与防御
    [1]. 视角越界检测：所有工具调用前验证是否符合角色视角定义，跨视角调用需经CEO批准并记录审计日志
    [2]. 辩论过热保护：自动检测辩论轮次>5或Token消耗超限，强制暂停并请求人工介入，避免无限循环
    [3]. 幻觉累积阻断：当同一任务连续3次生成低置信度结论，自动锁定相关AGENT并报警，人工解锁后方可继续
    [4]. 敏感数据脱敏：审计日志和反馈记录自动脱敏处理（个人可识别信息、商业机密），脱敏策略定期评审更新
}

Q2
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入处理与全景构建
        [1].输入数据经系统预处理为结构化JSON，AGENT首先调用摘要质量评估器（基于信息熵与关键实体覆盖率），若摘要质量评分<0.7则触发二次检索补充缺失维度
        [2].构建双层全景视图：宏观层（战略指标趋势/异常波动/关键假设）+ 微观层（文档切片引用索引），支持从微观证据反向修正宏观判断的DRIP式回溯机制
        [3].实施上下文窗口动态管理：基于任务复杂度自适应分配Token预算（基础任务512 tokens，复杂规划2048 tokens），超限时触发上下文压缩器
    
    2.任务类型与工具动态匹配
        [1].支持三类任务模式：①角色专业化自主分析（预定义17种战略工具） ②跨角色协作决策（SP/BP阶段） ③动态涌现任务（通过Tool RAG自动发现工具链）
        [2].实现Tool RAG机制：将工具能力描述（输入/输出Schema/领域约束）向量化存入专用向量库，支持"自然语言任务描述→工具能力语义匹配→DAG自动编排"
        [3].工具选择增加置信度评估：输出工具匹配度评分（0~1），<0.6时触发人工确认或启动多工具并行验证
    
    3.AGENT确定性工作流（基于LangGraph StateGraph）
        [1].状态Schema强制定义：采用Pydantic V2定义显式状态模型，包含session_id、current_phase、agent_role、working_memory（List[Message]）、long_term_memory_ref、tools_used、confidence_score、cost_accumulated
        [2].五阶段循环：[感知]环境初始化（加载SOUL.md价值观模板+BOUNDARY.md安全边界）→[规划]任务分解（输出机器可读DAG，含子任务依赖边与工具映射）→[执行]工具调用（沙盒隔离执行，STDERR自动捕获）→[验证]AUD旁路校验（一致性/幻觉/逻辑矛盾三重检测）→[反思]反向验证（用工具输出反向验证初始假设，差异>15%触发重规划）
        [3].中断安全：任意节点支持用户中断，自动保存StateSnapshot至Redis Cluster（三节点哨兵模式），恢复时加载完整上下文与工具状态
    
    4.用户反馈闭环演进
        [1].反馈分级处理：①专家修正（置信度>0.9）→直接触发Prompt Optimizer ②普通反馈→进入修正轨迹库 ③噪声反馈（与历史修正冲突）→自动过滤
        [2].Prompt Optimizer增强：基于DSPy+GEPA双引擎，①Few-shot样本自动生成（从修正轨迹提取正/负样本对） ②对抗样本挖掘（自动构造边界案例测试鲁棒性） ③多目标优化（准确性/成本/一致性加权）
        [3].演进触发双通道：①人工反馈通道（SP/BP Checkpoint） ②自动漂移检测通道（任务成功率周环比下降>10%自动触发）
    
    5.工具动态选择与组合
        [1].工具检索三模式：①规则映射（预定义17种战略工具） ②语义检索（Tool RAG） ③能力合成（多个原子工具自动编排为复合工具）
        [2].工具链DAG自动生成：基于任务依赖分析，输出有向无环图，支持并行执行无依赖子任务，执行失败时自动回溯重规划
        [3].工具调用强制沙盒化：所有工具执行在E2B沙盒中（启动延迟<500ms），网络访问仅限白名单API（财经数据/企业内部系统），代码执行结果经Validation Feedback闭环校验
    
    6.AGENT间协作与仲裁
        [1].协作三原则：①视角隔离（各AGENT仅访问角色相关数据视图） ②贡献度透明（Shapley值量化各AGENT决策影响力） ③冲突显性化（输出分歧热力图，标注分歧维度与强度）
        [2].CEO仲裁状态机增强：
            - 辩论轮次动态调整：基础轮次=3，每轮分歧收敛率<20%则+1轮（上限7轮）
            - Temperature Decay策略：轮次1→0.7, 轮次2→0.5, 轮次3+→0.3，强制具体化建议
            - 双方案生成规则：Plan A（保守参数集：风险权重×1.5，增长预期×0.8）；Plan B（激进参数集：风险权重×0.7，增长预期×1.3）
            - 用户介入窗口：30秒倒计时，支持"采纳A/B/混合方案/自定义"四选项，超时默认CEO决策
        [3].视角融合机制：在SP/BP各阶段Checkpoint，强制各AGENT输出视角摘要（300字），由CEO AGENT执行跨视角冲突检测（基于财务常识库+战略一致性规则）
    
    7.视角隔离与信息融合
        [1].严格视角隔离：各AGENT初始化时加载角色专属数据过滤器（如CFO仅见财务数据视图），工具调用增加角色权限校验
        [2].跨视角融合层：在战略意图/业务设计等关键节点，启动"融合会议"：各AGENT输出视角摘要→冲突检测→CEO主导融合→生成统一战略假设
        [3].视角偏离监控：实时检测AGENT行为偏离度（调用非角色工具次数/讨论非角色话题比例），偏离度>30%触发AUD告警
    
    8.CEO裁决状态机增强
        [1].辩论质量评估：每轮输出新信息增益率（本轮新增关键论据/历史论据总数）与重复率，增益率<10%且重复率>50%时强制终止辩论
        [2].Plan A/B生成透明化：输出参数调整明细（如Plan B将市场增长率假设从8%→12%，风险准备金比例从15%→10%）
        [3].用户裁决增强：提供"影响模拟器"，用户可滑动调整关键参数实时预览Plan A/B结果变化
    
    9.AUD旁路监听增强
        [1].四重检测维度：
            - 幻觉检测：生成内容与检索文档的语义相似度<0.4
            - 逻辑矛盾：违反Pydantic Schema约束或财务常识库规则（如利润率>100%）
            - 数据漂移：引用数据时效性>12个月且无明确标注
            - 视角违规：AGENT输出包含非角色职责内容
        [2].动态重试机制：基础重试次数=3，任务复杂度评分>0.7时+2次，每次重试强制更换工具或调整参数
        [3].熔断阈值：连续3次同类错误触发工具熔断，错误类型分布熵<0.3触发AGENT能力降级
    
    10.上下文管理与公共黑板
        [1].三层记忆架构：
            - 短期记忆：当前会话Working Memory（LangGraph State），Token窗口动态压缩（重要性评分+时间衰减双因子）
            - 长期记忆：战略档案库（向量+图数据库混合存储），支持跨SP/BP周期检索
            - 经验记忆：修正轨迹库（匿名化存储），用于Prompt Optimizer训练
        [2].公共黑板MVCC：基于Redis Cluster实现多版本并发控制，每次更新生成新版本快照，支持任意历史版本回溯与diff对比
        [3].黑板安全：写操作需经AUD校验，读操作按角色权限过滤，敏感操作（如删除）需CEO+用户双重确认
    
    11.可解释性与证据链
        [1].GAIA级证据包标准：每次任务输出包含①输入数据哈希 ②工具调用序列（含参数/输出/耗时） ③中间状态快照（关键决策点） ④置信度评分及依据 ⑤文档切片引用索引（支持点击溯源）
        [2].交互式溯源树：前端支持从结论节点逐层展开至原始数据，每层显示"数据来源→计算逻辑→中间结论"三元组
        [3].决策影响分析：量化各输入变量对最终决策的Shapley贡献值，高影响变量（>20%）自动高亮标注
    
    12.思维链溯源树ToT增强
        [1].实时置信度监控：每完成子任务即计算置信度（基于工具输出一致性/数据覆盖度/逻辑连贯性三维度加权），非仅阶段结束时评估
        [2].动态阈值机制：基础阈值=0.6，高风险任务（如资本支出决策）阈值提升至0.75，低风险任务（如市场趋势描述）阈值降至0.5
        [3].ToT节点强制推理模型：触发ToT时自动切换至o1-mini或Grok 3 Reasoning模型，执行深度回溯推理（最大深度=5层）
    
    13.Agent性能监控与行为分析
        [1].AgentBoard对标指标：
            - 任务成功率（阶段完成率）≥95%
            - 工具调用准确率（输出符合Schema）≥98%
            - 幻觉率（AUD拦截次数/总任务数）≤5%
            - 成本效率（Token消耗/任务复杂度）周环比波动<15%
        [2].行为分析维度：
            - 角色偏离度：非角色工具调用占比
            - 协作效率：跨AGENT消息轮次/任务复杂度
            - 决策质量：用户修正率（修正次数/总输出数）
        [3].自动根因分析：指标异常时自动关联日志，输出根因报告（如幻觉率上升→RAG检索质量下降→向量库漂移）
    
    14.安全与合规增强
        [1].沙盒三重隔离：①代码执行隔离（E2B容器） ②网络隔离（仅白名单API） ③数据隔离（角色视图过滤+敏感字段脱敏）
        [2].MCP 2025协议集成：工具调用遵循Model Context Protocol 2025规范，支持工具发现/授权/执行标准化
        [3].合规审计：所有AGENT操作记录至不可篡改日志（WORM存储），满足SOX/ISO27001审计要求，支持按时间/角色/任务类型多维检索
    
    15.成本控制三重熔断
        [1].任务级熔断：单次任务Token消耗>预设阈值（基础任务5K tokens，复杂规划50K tokens）立即终止
        [2].会话级熔断：日累计Token消耗>企业配额（默认100万tokens）暂停新任务，已启动任务完成
        [3].系统级熔断：连续3次工具执行失败或单任务成本>预算200%触发系统级告警，需管理员介入
        [4].成本预测：任务启动前基于历史相似任务预测Token消耗，偏差>50%时提示用户确认
}

Q3
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入处理与分层感知
        [1].输入数据经系统预处理为结构化JSON格式，采用MemoryOS四级分层架构：工作记忆(当前任务上下文)、情景记忆(本次会话历史)、语义记忆(企业知识库)、程序记忆(工具使用经验)
        [2].感知阶段强制执行"摘要优先"策略：首先调用摘要检索器构建全景视图(含关键指标趋势/异常波动/关联文档清单)，置信度≥0.7方可进入细节挖掘；置信度<0.7自动触发二次检索并扩大检索范围20%
        [3].集成Working Memory Compressor：基于关键实体抽取与HyDE算法，将长对话历史压缩为≤512 tokens的关键结论集，压缩率目标≥70%
    2.角色专业化与视角隔离
        [1].实施三重硬隔离机制：
            (1) Prompt隔离：每个AGENT加载专属IDENTITY.md，注入角色知识边界与决策约束
            (2) 工具隔离：基于BOUNDARY.md定义角色工具白名单，越权调用触发熔断
            (3) 数据隔离：字段级ACL控制，如CFO仅可访问财务相关字段(营收/成本/现金流)
        [2].部署Perspective Drift Detector：实时监控输出中跨角色关键词频率(如CFO输出"用户体验"触发预警)，超阈值(默认5%)自动熔断并记录至Agent Safety Log
        [3].角色能力边界采用Role Embedding向量量化：计算任务需求向量与角色能力向量的余弦相似度，<0.65时强制路由至更匹配角色
    3.AGENT基本工作流（ReAct+Reflection增强版）
        [1].感知(Perceive)：配置环境上下文→加载四级记忆→执行摘要优先策略→构建全景视图
        [2].思考(Think)：LLM推理生成结构化Plan(遵循Pydantic V2 Schema)→置信度评估(贝叶斯网络融合四维度)
        [3].执行(Act)：解析Plan→工具调用(经MCP Server授权)→沙箱执行(网络隔离+权限最小化)
        [4].反射(Reflect)：回写工具输出→置信度重评估→决策循环条件：置信度<0.65 AND 循环次数<5 →返回[2]；否则进入[5]
        [5].输出(Output)：构建结构化结果(JSON Schema)→释放资源(对接Kubernetes Agent Sandbox自动回收)→持久化Checkpoint(经LangGraph Checkpointer存入Redis Cluster)
    4.人在回路与反馈闭环
        [1].支持结构化用户反馈：定义Feedback Schema(JSON)包含{修正类型: enum[参数/权重/约束], 置信度变化: float, 影响因子: array[string]}
        [2].反馈自动触发ReflAct循环：修正轨迹→Few-Shot样本转换→Prompt Optimizer参数更新→A/B测试验证
        [3].实施修正影响传播分析：评估修正对下游任务的影响范围，高影响修正(>3个下游任务)强制生成Impact Report
        [4].Checkpoint人工介入点：业绩差距分析完成、市场洞察完成、战略意图确认、创新焦点决策、业务设计终稿、执行设计终稿共6个强制暂停点
    5.AGENT提示词工程与持续演进
        [1].基于DSPy 2.0构建Prompt Optimizer：输入=用户反馈+AUD一致性报告+SP/BP Checkpoint人工反馈；输出=优化后System Prompt+Few-Shot样本集
        [2].优化触发三条件(满足任一即触发)：
            (1) 修正率连续2个任务>15%
            (2) 一致性评分<0.85
            (3) 单任务成本超历史均值20%
        [3].实施Pareto多目标优化：结构完整性(40%权重)+逻辑一致性(35%权重)+成本效率(25%权重)，采用NSGA-II算法求解Pareto前沿
        [4].版本管理：集成MLflow实现Prompt自动版本注册，支持A/B测试(分流比例可配置)与一键回滚(保留最近10个稳定版本)
    6.工具动态选择与MCP标准化
        [1].工具选择采用MCP Server能力发现机制：AGENT查询MCP Registry获取工具能力描述(输入/输出Schema/版本/可靠性评分)
        [2].工具选择策略：首选(能力匹配度>0.85)→次选(0.7-0.85)→人工介入(<0.7)，匹配度=语义相似度(60%)+历史成功率(40%)
        [3].工具调用强制MCP OAuth 2.1授权：敏感操作(如财务数据导出)需二次人工确认，授权令牌有效期≤15分钟
        [4].工具失败Fallback Chain：首选工具失败→自动切换次选工具(最多2次)→生成Tool Failure Report→人工介入
    7.AGENT间协作与仲裁机制
        [1].协作原则：企业利益最优化(定义为长期股东价值最大化)，各AGENT贡献度按角色权重分配(CEO 30%, CFO 25%, CMO 15%, CTO 15%, COO 10%, CHO 5%)
        [2].CEO仲裁器输出Decision Rationale Report：包含{各AGENT建议权重, 关键分歧点, 风险评估矩阵, 备选方案对比}
        [3].高不确定性议题(置信度<0.6)自动触发红蓝辩论：实例化Red Team(激进派)与Blue Team(保守派)子AGENT，执行结构化辩论(最大3轮)，输出Consensus Zone与Divergence Zone报告
        [4].用户覆盖决策：用户可覆盖CEO仲裁结果，强制记录至战略档案库并标记"人工覆盖"，覆盖行为计入User Trust Score
    8.视角隔离技术实现
        [1].Context Isolation硬约束：每个AGENT的System Prompt注入角色专属知识边界(如CFO仅关注"资金风险/资本结构/现金流"三类实体)
        [2].工具调用前BOUNDARY.md校验：检查工具功能是否在角色权限范围内，越界调用返回Permission Denied并触发安全告警
        [3].Perspective Drift实时检测：基于BERT微调的分类器监控输出文本，跨角色关键词出现频率>5%触发熔断，熔断后需人工解锁
        [4].越界行为审计：所有越界尝试记录至Agent Safety Log，累计3次触发Agent Quarantine(隔离72小时)
    9.CEO裁决状态机增强版
        [1].辩论轮次控制：最大轮次=3+议题风险等级(1-3级)，风险等级基于影响范围/资金规模/战略重要性三维度评估
        [2].动态Temperature Decay：基础Temperature=0.7，每轮衰减ΔT=0.15×(1-风险等级/3)，强制AGENT从发散思维转向收敛决策
        [3].辩论质量评估：每轮输出经Debate Quality Scorer评分(论点新颖性30%+证据强度40%+逻辑一致性30%)，评分<0.6的AGENT强制重试
        [4].未达成一致处理：生成Plan A(保守版，风险权重0.7)与Plan B(激进版，风险权重0.3)两套方案，CEO按企业风险偏好矩阵决策，同时开启10秒用户介入窗口
    10.AUD AGENT增强审计机制
        [1].四维一致性检验：
            (1) 事实一致性：对比原始文档切片，检测事实扭曲
            (2) 逻辑一致性：规则引擎检测内部矛盾(如利润率>100%)
            (3) 数值一致性：沙箱重计算关键指标，误差>5%标记异常
            (4) 时效一致性：检查引用数据时效性，超12个月标记"数据陈旧"
        [2].分级熔断机制：
            L1(幻觉概率>0.8)：自动触发修正(调用Self-Correction Agent)
            L2(0.6-0.8)：通知相关AGENT重试(最多2次)
            L3(<0.6)：暂停任务并生成Hallucination Report，人工介入后从断点恢复
        [3].幻觉根因分析：构建Hallucination Taxonomy(数据缺失/逻辑跳跃/数值错误/时效过期)，支持针对性改进
    11.上下文压缩与公共黑板
        [1].关键结论提取双路算法：
            (1) 实体驱动：提取战略关键实体(市场/竞争者/财务指标)及其关系
            (2) 意图驱动：识别用户核心诉求与决策约束条件
        [2].公共黑板持久化：基于LangGraph StateSnapshot机制，状态快照存入Redis Cluster(3主3从)+AOF持久化，RPO<1秒
        [3].黑板冲突解决：采用Vector Clock记录更新顺序，冲突时应用Last-Writer-Win策略，冲突日志存入战略档案库供审计
        [4].时间旅行调试：支持从任意Checkpoint恢复执行，用于问题复现与根因分析
    12.决策可解释性增强
        [1].置信度量化模型：贝叶斯网络融合四维度
            数据质量(30%)：来源权威性+完整性+时效性
            工具可靠性(25%)：历史准确率+领域适配度
            逻辑一致性(25%)：规则引擎检测结果
            历史表现(20%)：同类任务成功率
        [2].Decision Evidence Graph输出：
            节点=关键决策因子(大小=影响权重)
            边=因子间因果关系(粗细=影响强度)
            颜色=风险等级(红>黄>绿)
        [3].SHAP值解释：量化各输入特征对最终决策的贡献度，支持"如果...会怎样"反事实推理
        [4].决策风险热力图：可视化展示各决策维度的风险分布(市场/财务/运营/合规)
    13.思维链溯源树(ToT)增强机制
        [1].动态触发阈值：基础阈值0.6，根据议题复杂度动态调整(±0.1)，复杂度=子任务数×工具调用数×数据源数
        [2].ToT搜索约束：
            最大深度：5层(根节点→叶节点)
            每层宽度：3个候选分支
            总token预算：5000 tokens
            超预算自动剪枝(保留Top-K路径)
        [3].ToT-AUD联动：每条推理路径独立经AUD四维检验，仅保留全部通过检验的路径，失败路径记录至Hallucination Log
        [4].ToT结果融合：采用加权投票机制(路径置信度加权)生成最终决策，输出完整推理树供审计
    14.Agent性能监控与漂移防御
        [1].三大核心指标监控：
            Task Success Rate：目标>92%，警戒线85%
            修正率：目标<10%，警戒线15%
            成本效率比：实际成本/基准成本，目标<0.8，警戒线1.2
        [2].漂移检测双算法：
            KS检验：检测输出分布偏移(月度基线对比)
            CUSUM：检测性能指标连续下降(滑动窗口7天)
        [3].自动修复机制：
            轻微漂移(指标偏离<10%)：自动触发Prompt微调
            中度漂移(10%-20%)：Skill Review+人工审核
            严重漂移(>20%)：Agent Quarantine+根因分析+全量回归测试
        [4].健康度仪表盘：实时可视化展示各AGENT的12项健康度指标，支持下钻分析与预警订阅
}

G4
{
三、AGENT：支持网络调用LLM API（支持System 2深层推理模型与快速响应模型的混合调度）驱动的企业战略规划多智能体系统。核心AGENT包括首席执行官CEO、运营COO、营销CMO、技术CTO、财务CFO、人力CHO共6个业务AGENT，以及1个联席审计官AUD AGENT。

    1. 数据全景与假设驱动探索：输入经系统结构化处理的JSON数据，AGENT首先读取摘要构建初始全景视图，然后采用“假设驱动”机制挖掘细节，允许在挖掘过程中动态更新和修正全景摘要，而非单向死板读取。
    2. 任务流转与图状态管理：AGENT执行基于角色定义的自主分析与协作决策两大类任务。所有协作任务必须基于有向无环图（DAG）构建流转拓扑，确保多体协作具备确定性的状态机流转特征。
    3. 新一代状态图工作流：废弃传统线性ReAct，采用基于StateGraph的工作流：[环境渲染与快照加载]->[状态机调度]->[System 2深层推理与多路径评估]->[原子化执行与沙箱约束]->[微观自愈与重试]->[状态更新与Checkpointer持久化]。
    4. 异步无缝人机协同闭环：系统支持粒度极细的用户反馈。在关键流转节点或异常时，AGENT状态流自动挂起（Suspended），触发异步通知。用户不仅能“批准/拒绝”，还支持Time-travel能力，可直接修改公共黑板中的中间状态变量，并从修改点继续执行。
    5. 受控的提示词演进（DSPy）：基于DSPy理念建立AGENT Prompt Optimizer，收集AGENT运行的修正日志与AUD的一致性检验结果。自动微调的提示词参数必须进入“待审查版本库”，经人工专家（Strategy Reviewer）确认后方可推向生产环境。
    6. 基于契约的工具箱调用：AGENT按角色域权限从工具箱调用工具，强制执行Pydantic V2契约校验。严禁跨域调用工具（如CHO不能调用量化金融模型），工具输出异常时在毫秒级触发单节点的Reflection与自愈重试。
    7. 协作仲裁与人类一票否决：CEO AGENT具有业务层面的统筹与仲裁权，负责生成最终战略提案。系统杜绝“超时自动代理机制”，对于重大分歧或未决决策，CEO AGENT必须生成包含Plan A与Plan B的对比视图，强制流转至人工审批节点，等待真实高管签字确认。
    8. 动态的主视角隔离与跨界渗透：严格约束各AGENT的生成视角与评判标准（如CFO侧重ROI与现金流）。支持“跨域提案接口”，允许特定AGENT将突破性建议（如CMO提出的技术收购需求）以结构化假设的形式提交给CTO和CEO评估，防止过度隔离导致的战略短视。
    9. 动态辩论与温度衰减控制：CEO AGENT内置“辩论状态机”，控制分歧时的多轮磋商。随辩论轮次增加，系统自动降低LLM的Temperature值收敛发散度，并强制要求提交具体修改JSON补丁而非自然语言的单纯否定。若达最大轮次未一致，自动输出分歧报告供人工裁决。
    10. AUD旁路审计与分级阻断：AUD AGENT采用“旁路监听”不阻塞主流程。进行原文与生成内容的一致性校验。发现次要错误（如轻微事实偏差）标注预警标签后放行；发现致命冲突（如违背财务常识），则异步阻断相关AGENT并回滚状态，3次重试失败则挂起任务交由人工纠错。
    11. 状态压缩与全时回溯公共黑板：在多轮对话与复杂流转中，采用LangGraph的StateSnapshot机制结合Redis/PostgreSQL，通过Thread ID和Version ID管理“公共黑板”。严格管理状态版本，支持随时切回历史快照重新推演（Time-travel）。
    12. 交互式推理过程可视化：建立动态交互的“战略决策依据图谱”，前端可点击任意推理节点，查看当前节点的上下文快照、系统提示词、检索到的引用源文档切片及置信度评分。
    13. 常态化多路径推演（类似o1）：摒弃仅在最终阶段使用ToT的做法。在SP/BP的所有核心分叉点（如商业模式选择、核心竞争力定义），默认调用支持System 2深层推理的模型，并行生成多条思维链推演路径，通过内部验证打分选出最优解，极大提升战略输出的逻辑厚度。
    14. 综合效能与成本监控：建立涵盖行为分析、API Token消耗、执行耗时与修正召回率的多维度仪表盘，定期评估每位AGENT的任务ROI，触发系统级的健康度预警。
}


根据上述建议列表优化重构[AGENT]需求特性。要求：
1、采纳并整合必要性>=9分，可行性>=7分以上的优化建议
2、符合需求特性阶段的描述，而不是更偏向系统设计
3、符合[企业战略规划管理智库系统需求特性列表]->[AGENT]需求特性的语言风格 
4、符合[企业战略规划管理智库系统需求特性列表]->[AGENT]需求特性的排版格式
5、以txt代码片段格式输出完整的[AGENT]需求特性



## 决胜

你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要结合[企业战略规划管理智库系统需求特性列表]系统需求，从下面需求清单中选择最优的[AGENT]需求特性。要求： 
1、结合系统需求为每个列表逐个评分（10分制），最后求总分
2、评分维度：
   - 核心架构思想
   - 输入与感知
   - AGENT工作流
   - 用户反馈闭环
   - 提示词工程与演进
   - 系统自适应能力
   - 记忆与上下文
   - 工具调用与集成
   - 仲裁与决策机制
   - 审计与一致性
   - 创新前瞻性
   - 可观测性与可解释性
   - 安全与鲁棒性
   - 性能与监控
   - 成本效能
   - 企业级适用性
   - 工程可实施性
3、严格从[客观视角]进行评估，以表格形式呈现
---------------------
企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：支持基于网络LLM和先进Agent技术的智能分析工具集，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理（共计17个）。
    1.智能工具定义
        [1].建立工具标准：每个工具有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。工具初始化必须加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
        [2].构建中央技能仓库：实现工具的统一注册、版本控制、灰度发布与回滚机制。所有入库工具必须通过Pydantic V2契约测试，后续迭代需通过回归测试集。支持预定义模板与远期基于代码生成的技能动态扩展。
        [3].支持工具链编排与依赖调度：支持定义工具链有向无环图，实现上游工具输出结构化流转为下游输入。内置任务调度器，根据依赖关系自动并行执行无依赖子任务。
    2.工具箱标准工作流
        [1].环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具价值观模板（SOUL.md），实例化会话级沙箱与混合检索记忆容器。
        [2].感知与摘要：读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
        [3].规划：Planner依据用户自然语言指令及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、匹配工具映射、数据依赖边。
        [4].执行与验证：Executor按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe→writeback原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
        [5].证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
        [6].反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
        [7].成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
        [8].演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。
    3.数值计算与沙箱执行
        [1].强制代码执行隔离：严禁LLM直接进行数值计算、统计回归或现金流折现。所有量化分析强制通过两条路径执行：一是生成Python代码在沙箱执行；二是针对战略运筹问题（如线性规划），由LLM构建数学模型调用CVXPY、OR-Tools等优化器求解。
        [2].构建持久化计算沙箱：为每个任务会话实例化持久化Jupyter Kernel，支持跨工具、跨步骤的变量传递（如宏观经济变量在不同模型间共享），实现分析上下文连贯。任务结束后自动销毁内核并回收资源。
        [3].支持Validation Feedback闭环：自动捕获代码执行STDERR，检索错误案例库辅助LLM生成修复版本。设定最大重试次数（默认3次），失败则标记不可行并记录至演进日志。
    4.Schema强制与一致性校验
        [1].实施契约化输出：所有工具输入/输出严格遵循Pydantic V2定义的Data Model，利用Instructor Patch强制LLM输出符合Schema的结构化JSON，严禁输出自由文本。
        [2].建立一致性校验仲裁器：在多工具协作中，通过内置规则引擎及财务常识库检测逻辑冲突（如利润率与成本矛盾）。输出冲突检测报告，供用户或高级AGENT裁决。
        [3].自动化契约测试：在CI/CD流水线集成Schema契约测试，防止模型漂移导致结构崩溃。契约测试失败的任务强制终止。
    5.可解释性与决策溯源
        [1].输出结构化推理证据包：每次分析必须打包输出全量元数据，包括输入摘要哈希、Planner任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片ID）。
        [2].构建交互式溯源树：支持前端从结论反向逐层展开证据包，展示“数据来源→计算逻辑→中间结论→最终结论”的完整推理链。
        [3].支持红蓝辩论机制：针对高不确定性议题自动启动“红蓝对抗”，实例化激进派与保守派子智能体进行结构化辩论，输出包含共识与分歧区域的风险全景视图。
    6.提示词工程与演进
        [1].Prompt Optimizer：基于DSPy理念，建立“修正日志-样本转换”闭环，将用户修正转化为Few-Shot样本，自动迭代提示词参数以适配企业战略风格。
        [2].实施受控自动演进：仅在SP/BP Checkpoint完成态且满足一致性阈值时触发演进。必须采用多目标优化（结构、一致性、成本），严禁单一指标优化。
        [3].确保方法论不变性：演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
        [4].支持版本管理与回滚：所有Prompt纳入Git LFS管理，与系统版本绑定。支持一键回滚至历史稳定版本。
    7.用户反馈与闭环演进
        [1].支持Checkpoint人工介入：在关键决策节点或低置信度环节自动暂停，请求用户验证中间假设。支持用户修改参数或调整权重，系统记录修正前后的“专家修正轨迹”。
        [2].建立修正轨迹库与逆向传播：将用户修正快照匿名化存储，定期分析差异，推断输入权重或任务分解策略的优化方向。
        [3].实施用户反馈协议：将用户反馈转化为结构化修正日志，强制触发工具体“反思阶段”（ReflAct），并回传至技能演进日志。
    8.安全与防御机制
        [1].实施沙箱网络隔离：代码执行环境默认断网，仅允许通过白名单网关访问预设的可信财经API，严禁通用网页爬虫。
        [2].建立提示词注入防御：输入数据前置执行清洗与注入检测。将“注入防御测试”纳入基准测试集，定期验证工具体的抗干扰能力。
        [3].建立工具幻觉黑名单：自动监控工具体输出，若连续3次生成与输入矛盾的结论，自动锁定该工具体并报警。
    9.领域专业知识嵌入
        [1].内置领域事实库：以JSON Schema存储经典管理理论的强制约束（如SWOT内部/外部属性分类），确保分析逻辑符合理论框架。
        [2].部署领域验证器：工具体输出前必须经过验证器检查，确保结论不违背基础商业逻辑与领域常识。
    10.服务契约与集成
        [1].独立微服务封装：工具箱作为独立微服务对外暴露OpenAPI 3.1标准接口，输出分析结果、证据包与成本审计信息三元组。
        [2].支持Agent模块集成：与CEO、CFO等核心AGENT通过服务调用无缝集成，支持水平扩展与分布式Trace追踪。
    11.测试与质量保证
        [1].构建基准测试集（Strat-Bench）：建立包含20+专家标注场景的战略分析专用测试集，作为版本迭代的准入测试标准。
        [2].支持A/B测试框架：支持多版本策略并行运行，基于任务完成率、计算精度与成本指标择优晋级。
        [3].部署健康度仪表盘：实时监控工具成功率与修正率，修正率超标（默认15%）自动触发Skill Review。
    12.性能监控与漂移检测
        [1].建立性能漂移检测：定期评估检索准确率与生成质量，对比历史基线检测性能下降。
        [2].支持可视化日志：所有评估与运行结果纳入系统行为日志并可视化展示。
    13.成本熔断与契约测试质量门禁
        [1].CI/CD契约测试门禁：所有工具输出必须通过严格Schema校验，非兼容变更触发构建失败。运行时连续三次校验失败触发工具熔断。
        [2].实施三级成本熔断：支持任务级（单次Token/成本超限）、会话级（日配额超限）与重试级（连续代码执行失败）熔断机制，确保系统运行成本可控。

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入经系统处理结构化为JSON格式的数据，首先读取摘要，构建全景视图，然后再挖掘细节
    2.AGENT主要执行按角色定义自主分析数据、战略规划各阶段协作分析决策两大类任务，这两大类任务包括多种不同子类
    3.AGENT基本工作流：[感知]配置环境与上下文->管理会话生命周期->[思考]LLM推理->[执行]解析LLM输出决策任务完成或调用工具->[反射]回写工具输出到上下文并决策是否继续循环[思考]->构建输出并释放资源
    4.支持用户反馈与修正AGENT分析结果
    5.支持AGENT提示词生成与管理，按角色定义和分析框架生成适合LLM使用的提示词，基于DSPy理念建立AGENT Prompt Optimizer，根据AGENT分析结果人工反馈、AUD AGENT的一致性检验与SP/BP各阶段Checkpoint人工反馈自动微调提示词参数
    6.AGENT可按任务需求从工具箱中选择合适工具完成数据分析或战略规划任务
    7.支持企业利益最优的AGENT间协作，高级代理CEO AGENT拥有协作仲裁最终决定权，但用户可以观测和更改该决策
    8.支持视角隔离，AGENT仅从各自角色视角（如CFO仅关注资金风险）调用工具，严禁泛化讨论
    9.CEO AGENT内置"裁决状态机"作为仲裁器，设定最大辩论轮次，随着轮次增加，降低AGENT的Temperature Decay强制要求AGENT提出具体修改意见，而不是单纯否定，若未达成一致，生成Plan A（保守版）和Plan B（激进版）两套方案，CEO AGENT按偏好矩阵决策Plan A/B，同时暂停10秒提供用户介入裁决窗口，超时用户无动作则以 CEO AGENT 决策为准
    10.AUD AGENT采用"旁路监听+异步阻断"模式，不参与生成，只负责原文与生成内容的一致性检验，发现幻觉通知相关AGENT纠错，若3次重试失败，暂停任务人工纠错，之后从任务暂停点继续任务
    11.支持上下文压缩器与"公共黑板"机制，在多轮对话间仅传递关键结论而非全量历史，严格利用LangGraph的StateSnapshot机制配合Redis存储来管理公共黑板的更新与回溯
    12.支持Agent推理过程的可解释性可视化，包括决策依据、关键因素和置信度
    13.采用"思维链溯源树ToT"机制，只在SP/BP各阶段结束时启用ToT功能，当决策置信度低于阈值（默认0.6）触发ToT，通过回溯推理路径解决决策冲突，提高决策可解释性
    14.建立Agent性能监控与行为分析框架

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察(包括六个子步骤)、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据
---------------------

D
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
1. 输入处理与全景构建
    [1]. 输入数据经系统预处理为结构化JSON格式，AGENT首先调用摘要质量评估器（基于信息熵与关键实体覆盖率），若摘要质量评分低于阈值（默认0.7）则自动触发二次检索补充缺失维度，确保全景视图的完整性。
    [2]. 构建动态全景视图：宏观层（战略指标趋势/异常波动/关键假设变量）与微观层（文档切片引用索引）双向关联，支持细节挖掘过程中发现与全景视图严重矛盾时自动触发视图重构，重构过程需记录变更轨迹并提交AUD AGENT审核。
    [3]. 实施上下文窗口动态管理：基于任务复杂度自适应分配Token预算，超限时触发上下文压缩器（基于关键实体抽取与HyDE算法），压缩率目标≥70%，确保关键信息不丢失。

2. 任务分解与协作架构
    [1]. 建立任务IR（中间表示）标准：每个任务包含任务ID、类型、输入/输出Schema、依赖任务列表、优先级、执行状态、超时策略。所有协作任务必须基于有向无环图（DAG）构建流转拓扑，确保多体协作具备确定性的状态机流转特征。
    [2]. 采用分层协作架构：CEO AGENT作为指挥官负责目标解析与全局任务分解，各专业Agent并行执行子任务，调度器负责资源分配与执行监控。自动生成协作依赖图，基于BLM/BEM阶段定义标准依赖关系，支持动态调整与优化，可视化展示任务间数据流与控制流。
    [3]. 支持柔性视角隔离：定义核心视角（必须遵守）和扩展视角（可协商），对跨领域问题组建临时联合分析组，建立视角冲突解决协议。视角隔离通过Prompt隔离、工具隔离、数据隔离三重硬约束实现。

3. AGENT确定性工作流
    [1]. 基于状态图的工作流引擎：采用StateGraph机制定义显式状态模型，包含session_id、current_phase、agent_role、working_memory、long_term_memory_ref、tools_used、confidence_score、cost_accumulated等字段。工作流循环定义为：[感知]环境初始化与上下文加载→[规划]生成任务执行DAG→[执行]按拓扑顺序调度子任务→[深度思考]（可选）在关键决策节点并行生成多条思维链推演路径→[验证]结构化检查输出→[反思]错误分析与修正→[闭环]Early Stopping机制（置信度达标且目标达成时提前终止）。
    [2]. 深度思考与多路径推演：在SP/BP所有核心分叉点（如商业模式选择、核心竞争力定义），默认调用支持深层推理的模型，并行生成多条思维链推演路径，通过内部验证打分选出最优解，极大提升战略输出的逻辑厚度。
    [3]. 中断安全与状态持久化：任意节点支持用户中断，自动保存StateSnapshot至Redis Cluster（三节点哨兵模式），恢复时加载完整上下文与工具状态，支持从任意历史快照恢复执行（Time-travel能力分步实现）。

4. 用户反馈闭环
    [1]. 建立反馈标准化协议：将用户反馈转化为结构化修正日志（包含修正类型enum[参数/权重/约束]、修正前/后值、置信度变化、影响因子、用户角色、时间戳）。
    [2]. 反馈分级处理与即时响应：①专家修正（置信度>0.9）直接触发Prompt Optimizer；②普通反馈进入修正轨迹库；③噪声反馈自动过滤。设置专用反馈通道，承诺24小时内处理并回复，紧急反馈设置加急处理流程。
    [3]. 修正影响传播分析：评估修正对下游任务的影响范围，高影响修正（>3个下游任务）强制生成Impact Report并通知相关AGENT。支持从任意Checkpoint恢复执行后修改中间状态变量（Time-travel第一阶段），并从修改点继续执行。

5. 提示词工程与演进
    [1]. 基于DSPy理念构建Prompt Optimizer：输入为用户反馈、AUD一致性报告、SP/BP Checkpoint人工反馈，输出优化后System Prompt与Few-Shot样本集。优化目标采用Pareto多目标权衡（结构完整性40% + 逻辑一致性35% + 成本效率25%），采用NSGA-II算法求解Pareto前沿。
    [2]. 受控演进机制：自动微调的提示词参数必须进入“待审查版本库”，经人工专家（首席战略官或授权代表）确认后方可推向生产环境。集成MLflow实现Prompt自动版本注册，支持A/B测试（分流比例可配置）与一键回滚（保留最近10个稳定版本）。
    [3]. 提示词契约测试：每次优化后自动运行Strat-Bench基准测试集，通过率<90%自动拒绝上线，并生成差异分析报告。演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。

6. 工具调用与集成
    [1]. 全面遵循MCP（Model Context Protocol）2025规范：工具箱作为独立微服务对外暴露符合MCP标准的能力发现接口，支持工具注册、版本控制、灰度发布与回滚。AGENT通过MCP Registry查询工具能力描述（输入/输出Schema、版本、可靠性评分）。
    [2]. 工具选择优化：基于语义匹配（60%）+历史成功率（40%）计算工具匹配度，匹配度<0.6时触发人工确认或启动多工具并行验证。预定义17种战略工具的最佳组合模板（如SWOT+TOWS自动组合），支持用户自定义模板。
    [3]. 强制契约化调用与沙箱执行：所有工具调用前验证输入Schema，调用后验证输出Schema，连续3次校验失败自动熔断该工具。代码执行在E2B沙盒中（网络隔离+权限最小化），执行结果经Validation Feedback闭环校验，最大重试次数3次。

7. 仲裁与决策机制
    [1]. CEO AGENT内置裁决状态机，设定最大辩论轮次（默认3+风险等级，上限7轮）。辩论采用三阶段：发散（Temperature=0.8）、收敛（Temperature=0.5）、裁决（Temperature=0.2），强制随轮次增加提交具体修改JSON补丁。
    [2]. 辩论质量评估：每轮计算新信息增益率与重复率，增益率<10%且重复率>50%时强制终止辩论。辩论过程记录各AGENT贡献度（基于Shapley值量化），分歧点生成热力图。
    [3]. 多方案生成与人类一票否决：若未达成一致，生成Plan A（保守参数集）、Plan B（激进参数集）和Plan C（AI融合版），输出参数调整明细。强制暂停60秒提供用户介入窗口，窗口期可视化展示三套方案的对比差异（风险/收益/资源需求）及影响模拟器（支持用户滑动调整关键参数实时预览结果）。超时无用户操作则按CEO决策执行，但所有人工覆盖决策强制记录至战略档案库并标记“人工覆盖”。

8. 审计与一致性保障
    [1]. AUD AGENT采用“旁路监听+异步阻断”模式，不参与生成。审计范围包括四维一致性检验：①事实一致性（对比原始文档切片）；②逻辑一致性（规则引擎检测内部矛盾）；③数值一致性（沙箱重计算关键指标，误差>5%标记异常）；④时效一致性（引用数据超12个月标记“数据陈旧”）。
    [2]. 分级熔断与根因分析：L1（幻觉概率>0.8）自动触发修正；L2（0.6-0.8）通知相关AGENT重试（最多2次）；L3（<0.6）暂停任务并生成Hallucination Report（包含错误类型分类：数据缺失/逻辑跳跃/数值错误/时效过期），人工介入后从断点恢复。
    [3]. 审计日志强制存储：所有审计发现、纠错过程、重试轨迹全量存储至不可篡改日志（WORM存储），满足SOX/ISO27001审计要求，支持按时间/角色/任务类型多维检索。

9. 记忆与上下文管理
    [1]. 分层记忆架构：工作记忆（当前会话，有效期24h）、短期记忆（最近项目，有效期30天）、长期记忆（战略知识库，永久存储），各层有独立的存储引擎和检索策略。长期记忆将用户修正日志、决策复盘结果、执行结果差值反向固化为少样本提示词，每季度触发知识蒸馏与去重。
    [2]. 公共黑板机制：基于LangGraph StateSnapshot + Redis Cluster管理状态版本，支持多版本并发控制（MVCC）。关键结论写入黑板时必须附带重要性评分（1-5）、置信度、引用源切片ID、时效性，综合评分>3.5才写入，便于上下文压缩器智能筛选。
    [3]. 时间旅行调试：支持从任意Checkpoint恢复执行，用于问题复现与根因分析。所有状态快照与完整推理轨迹归档至数据湖，可按需解压缩回溯。

10. 可解释性与溯源
    [1]. 结构化证据包标准：每次分析打包输出全量元数据，包括输入摘要哈希、任务分解图、工具调用轨迹（含版本）、关键中间变量快照、置信度评分及依据、引用源切片ID列表。
    [2]. 交互式溯源树：前端支持从结论节点逐层展开至原始数据，每层显示“数据来源→计算逻辑→中间结论”三元组，每个节点支持查看详细信息。
    [3]. 决策影响分析：量化各输入变量对最终决策的Shapley贡献值，高影响变量（>20%）自动高亮标注。支持反事实推理，输出“如果改变某假设，结论会如何变化”的对比报告和风险热力图。

11. 主动能力
    [1]. 引入心跳机制：支持周期性自动唤醒（可配置频率），主动检查战略档案库中的待办事项、偏差预警、周期性任务，无需用户提问即可主动沟通。
    [2]. 战略偏差预警：实时监控实际执行数据与SP/BP规划的偏差，偏差超过阈值（默认10%）自动触发预警，生成偏差分析报告并推送相关AGENT。
    [3]. 主动知识更新：当检测到行业报告、市场数据、政策法规更新时，自动触发电请相关AGENT评估影响，重要影响主动推送CEO AGENT。

12. 性能监控与行为分析
    [1]. 建立三维度指标体系：效率维度（任务完成率、平均时长、工具调用成功率）、质量维度（修正率<10%、幻觉率<5%、一致性得分>0.8）、成本维度（单任务API成本、Token消耗）。构建战略规划专用基准测试集（Strat-Bench）作为版本迭代的准入标准。
    [2]. 漂移检测双算法：KS检验检测输出分布偏移（月度基线对比），CUSUM检测性能指标连续下降（滑动窗口7天）。指标异常时启动自动根因分析，关联日志输出根因报告。
    [3]. 健康度仪表盘：实时可视化展示各AGENT的12项健康度指标，支持下钻分析与预警订阅。三级预警机制：黄牌（指标下降15%触发分析）、红牌（下降30%触发人工review）、停用（下降50%且连续三天，自动回滚至前一稳定版本）。

13. 安全与防御
    [1]. 纵深防御体系：
        - 第一层（隔离）：三重硬隔离（Prompt隔离、工具隔离、数据隔离），基于角色权限最小化。
        - 第二层（执行）：所有代码在E2B沙盒中执行，严格网络隔离（仅白名单API）。
        - 第三层（检测）：实时监控视角越界（跨角色关键词频率>5%触发熔断）、幻觉累积（连续3次低置信度结论锁定Agent）。
        - 第四层（审计）：所有操作记录至WORM存储。
        - 第五层（熔断）：辩论过热保护、成本熔断。
    [2]. 敏感数据脱敏：审计日志和反馈记录自动脱敏处理（个人可识别信息、商业机密），脱敏策略定期评审更新。

14. 成本控制
    [1]. 成本预测：任务启动前基于历史相似任务预测Token消耗与API成本，偏差>50%时提示用户确认。
    [2]. 三级熔断机制：
        - 任务级熔断：单次任务Token消耗超预设阈值（基础任务5K tokens，复杂规划50K tokens）立即终止。
        - 会话级熔断：日累计Token消耗超企业配额（默认100万tokens）暂停新任务，已启动任务完成。
        - 系统级熔断：连续3次工具执行失败或单任务成本超预算200%触发系统级告警，需管理员介入。
}

Q
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1.输入与全景感知
        [1].输入数据经系统预处理为结构化JSON格式，AGENT首先调用摘要质量评估器构建全景视图，摘要质量评分低于0.75时自动触发二次检索补充缺失维度
        [2].构建双层全景视图：宏观层（战略指标趋势/关键假设变量/异常波动）与微观层（文档切片引用索引），支持从微观证据反向修正宏观判断
        [3].实施阶段化感知策略：根据BLM/BEM不同规划阶段自动加载定制化摘要模板，确保感知焦点与阶段目标对齐
        [4].建立输入数据质量预检机制：自动检测数据完整性、时效性与一致性，对低质量数据标注置信度衰减系数并预警
    
    2.角色专业化与硬性视角隔离
        [1].实施三重硬隔离机制：Prompt隔离（专属角色定义文件）、工具隔离（角色工具白名单）、数据隔离（字段级访问控制），严禁跨角色越界操作
        [2].部署视角偏离实时监控：当AGENT输出中出现非角色职责关键词频率超过5%时，自动触发熔断并记录安全日志
        [3].建立跨域提案结构化接口：允许AGENT以标准化假设形式（含依据/影响范围/风险评估）向CEO提交跨域建议，禁止自由讨论式越界
    
    3.AGENT确定性工作流
        [1].采用基于状态图的工作流：环境初始化→任务规划（输出机器可读任务分解图）→工具执行→AUD校验→反思修正→输出构建，全程状态可追溯
        [2].强制checkpoint人工介入机制：在BLM/BEM各阶段关键节点（业绩差距分析完成、市场洞察完成、战略意图确认、创新焦点决策、业务设计终稿、执行设计终稿）自动暂停，等待用户审批后继续
        [3].禁用自动化终止机制：置信度仅作为预警指标，不得作为任务终止依据，确保战略决策全程保持人类监督
    
    4.用户反馈闭环
        [1].支持结构化用户反馈：定义标准反馈Schema（含修正类型/置信度变化/影响因子），确保反馈可机器解析与追踪
        [2].实施修正影响传播分析：当用户修正影响超过3个下游任务时，系统自动生成影响范围报告供决策参考
        [3].反馈仅在checkpoint节点生效：避免实时反馈破坏战略规划连贯性，确保修正经过充分评估
    
    5.提示词工程与受控演进
        [1].实施双轨制演进策略：常规演进基于用户反馈周频自动微调；重大演进（涉及战略方法论逻辑步骤变更）必须通过月度人工专家确认
        [2].建立Strat-Bench契约测试门禁：所有提示词优化必须通过20+专家标注场景的基准测试，通过率低于90%自动拒绝上线
        [3].所有提示词纳入版本管理：支持与系统版本绑定及一键回滚至历史稳定版本
    
    6.工具调用与集成
        [1].采用预定义工具映射表：根据BLM/BEM各阶段需求预配置战略工具组合（如市场洞察阶段绑定PESTEL+波特五力），禁止动态工具发现
        [2].强制执行输入/输出契约校验：所有工具调用前后必须验证数据结构符合预定义Schema，违规模3次自动熔断该工具
        [3].工具执行强制沙盒隔离：所有量化计算与代码执行在断网沙盒环境中完成，仅允许通过白名单访问可信财经API
    
    7.AGENT间协作与仲裁机制
        [1].确立CEO AGENT最终仲裁权：CEO拥有不可转移的最终决策权，负责生成最终战略提案并承担决策责任
        [2].实施双方案生成规则：当AGENT间分歧无法收敛时，自动生成Plan A（保守参数集：风险权重×1.5）与Plan B（激进参数集：风险权重×0.7）两套方案
        [3].重大分歧强制人工审批：涉及资本支出、市场进入等高风险决策的分歧，必须流转至企业高管电子审批流，获得签字确认后方可执行
    
    8.审计与一致性保障
        [1].实施四维一致性检验：AUD AGENT对每次生成内容执行事实一致性（对比原始文档）、逻辑一致性（规则引擎检测）、数值一致性（沙盒重计算）、时效一致性（数据新鲜度）四重校验
        [2].建立量化幻觉标准：综合置信度评分（<0.6标记可疑）、引用缺失率（>30%标记警告）、逻辑矛盾指数，评分低于0.7触发强制纠错
        [3].审计日志强制WORM存储：所有操作记录存入不可篡改存储介质，满足SOX/ISO27001合规审计要求
    
    9.记忆与上下文管理
        [1].采用三层记忆架构：会话记忆（当前规划任务）、项目记忆（单次SP/BP周期）、战略档案库（跨周期长期知识），各层独立管理
        [2].关键结论提取标准：仅当结论对最终决策影响度评分>3.5（5分制）且置信度>0.8时，才写入公共黑板供多AGENT共享
        [3].保留完整推理链：禁止高压缩率上下文压缩，确保所有中间推理步骤可完整溯源以满足审计要求
    
    10.可解释性与决策溯源
        [1].输出结构化证据包：每次分析必须包含输入数据哈希、任务分解图、工具调用序列、关键中间变量、置信度评分及文档切片引用索引
        [2].支持文本化溯源树：通过层级缩进文本展示"数据来源→计算逻辑→中间结论→最终结论"完整推理链，适配命令行接口
        [3].生成决策依据摘要：自动提炼300字内核心决策依据，包含关键假设、数据支撑及风险提示
    
    11.多路径推演机制
        [1].限定关键分叉点推演：仅在BLM模型的创新焦点（价值/模式创新选择）与业务设计（商业模式选择）两个关键分叉点启用多路径推演
        [2].参数范围硬性约束：反事实推理的参数调整范围限定在基准值±20%内，防止过度推测导致决策失真
        [3].推演结果强制对比输出：多路径推演必须生成对比报告，明确标注各路径的核心差异、风险分布及资源需求
    
    12.性能监控与漂移检测
        [1].建立三维度监控指标：效率维度（任务完成率/平均时长）、质量维度（修正率<15%/幻觉率<5%）、成本维度（单任务Token消耗）
        [2].实施三级预警机制：指标周环比下降15%触发黄牌分析，下降30%触发红牌人工Review，连续三天下降50%自动回滚至前一稳定版本
        [3].漂移检测周频执行：每周自动对比当前性能与历史基线，检测模型输出分布偏移与任务成功率下降
    
    13.成本控制与熔断机制
        [1].实施阶段化Token预算：根据BLM/BEM各阶段复杂度预设Token消耗阈值（如市场洞察阶段≤20K tokens，业务设计阶段≤50K tokens）
        [2].三级熔断机制：任务级（单次超阈值200%立即终止）、会话级（日累计超企业配额暂停新任务）、系统级（连续3次工具失败触发告警）
        [3].成本预测前置：任务启动前基于历史相似任务预测Token消耗，偏差超过50%时提示用户确认
    
    14.安全与鲁棒性
        [1].实施沙盒三重隔离：代码执行隔离、网络访问隔离（仅白名单API）、敏感数据字段脱敏
        [2].视角越界零容忍：任何跨角色工具调用或数据访问尝试均触发实时告警并记录安全日志
        [3].辩论过热保护：自动检测辩论轮次超过7轮或单任务Token消耗超阈值300%，强制暂停并请求人工介入
    
    15.企业级治理强化
        [1].强制高管电子审批流：SP/BP终稿必须经企业授权高管通过电子签名确认，审批记录存入战略档案库
        [2].季度战略复盘机制：每季度自动导入实际执行数据，计算与规划偏差并生成复盘报告，偏差超过10%触发战略校准流程
        [3].用户修正轨迹反向固化：高频用户修正模式自动转化为业务规则模板，在类似场景下供Planner默认引用，形成战略知识持续演进
}

G
{
三、AGENT：支持网络调用LLM API（支持System 2深层推理模型与快速响应模型的混合调度）驱动的企业战略规划多智能体系统，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT

    1.输入处理与全景构建：输入经系统处理结构化为JSON格式的数据。AGENT采用"摘要优先与假设驱动"策略，首先读取摘要构建包含关键指标与战略假设的初始全景视图，然后基于该视图进行细节挖掘。支持DRIP式回溯机制，当微观细节挖掘发现与宏观全景视图严重矛盾时，自动触发全景视图的修正与重构，而非单向死板读取。
    
    2.AGENT确定性工作流：采用基于StateGraph的确定性状态机流转架构。通过Pydantic V2定义显式的状态契约（包含会话ID、当前阶段、工作记忆、调用工具、置信度等）。基本工作流包含：[感知]环境渲染与快照加载->[规划]任务分解与状态机调度->[执行]工具调用与沙盒约束->[验证]AUD旁路校验->[反思]反向验证与状态更新 五个阶段的严格循环。
    
    3.工具调用与安全隔离：AGENT按角色域权限从工具箱调用工具，工具发现与集成需遵循MCP（Model Context Protocol）标准化协议。强制实施代码沙盒化隔离与网络白名单隔离，严禁跨角色越权调用（如CHO严禁调用财务量化模型）。工具输出异常时，要求在毫秒级触发单节点范围内的反思（Reflection）与自愈重试。
    
    4.异步人机协同与状态回溯：系统支持细粒度、非阻塞的用户反馈机制。在SP/BP关键流转节点或检测到高风险异常时，AGENT状态流自动挂起（Suspended），触发异步通知请求人工介入。支持Time-travel时空回溯能力，用户不仅能审批，还可直接修改"公共黑板"中的历史状态快照变量，并指定从修改点继续执行推演。
    
    5.提示词受控演进：基于DSPy理念建立AGENT Prompt Optimizer，收集AGENT运行的修正日志与AUD的一致性检验结果，自动生成优化建议。实施"强制人工审核门禁"，自动微调的提示词参数必须先进入"待审查版本库"，经人类战略专家评审确认并进行契约测试后，方可推向生产环境。
    
    6.协作仲裁与人类一票否决：支持企业利益最优的AGENT间协作。CEO AGENT具有业务统筹与仲裁权，内置"辩论状态机"控制多轮磋商，随辩论轮次增加动态降低Temperature值以收敛发散度，并强制要求提交具体结构化补丁而非单纯否定。系统杜绝"超时自动决策"，若未达成一致，CEO AGENT必须生成Plan A（保守版）与Plan B（激进版）的对比视图，强制流转至人工审批节点，等待人类高管裁决。
    
    7.联席审计与分级阻断：AUD AGENT采用"旁路监听"模式，不参与内容生成也不阻塞主流程。负责执行事实、逻辑、数值与时效四重一致性检验。实施分级阻断：发现次要错误（如轻微事实偏差）标注预警后放行；发现致命冲突（如违背财务常识），则异步阻断相关AGENT并回滚状态，3次自动重试失败则挂起任务交由人工纠错。
    
    8.上下文管理与公共黑板：采用多版本并发控制（MVCC）技术管理多轮对话与复杂流转间的"公共黑板"。在多轮对话间仅传递关键结论，状态快照（包含Thread ID和Version ID）需严格持久化至数据库。支持读写权限的角色隔离过滤，确保多智能体并发写入时的数据一致性与全时回溯能力。
    
    9.常态化多路径推演：在SP/BP的核心战略分叉点（如商业模式选择、核心竞争力定义），默认调度支持System 2深层推理的模型，并行生成多条思维链推演路径。通过内部规则引擎与验证器打分选出最优解，极大提升复杂战略输出的逻辑厚度，摒弃仅在阶段末尾由低置信度触发的被动ToT机制。
    
    10.可解释性与证据链溯源：建立GAIA级结构化证据包标准，每次任务输出必须包含：输入数据哈希、工具调用序列、关键中间状态快照、置信度评分及具体到文档切片ID的引用索引。支持前端提供决策过程可视化接口，构建交互式战略决策依据图谱，支持从最终结论逐层反向点击展开至原始数据源。
    
    11.成本熔断与合规审计：建立涵盖任务级（单次Token/成本超限）、会话级（日配额超限）、系统级（连续代码执行失败）的三级成本熔断机制。所有AGENT的操作轨迹、工具调用与越界尝试均需脱敏记录，并存入符合SOX/ISO27001审计标准的不可篡改日志（WORM存储）中，确保企业级合规。
}

O
{
三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
1. 输入与全景感知
    [1]. 输入格式与质量元数据：所有输入经系统预处理为结构化JSON，附带标准化元数据字段（来源、时间戳、摘要质量评分、置信度、可溯源引用清单）。
    [2]. 摘要优先与双层全景：AGENT应首先读取摘要构建双层全景视图——宏观层（关键战略指标趋势、关键假设、偏差信号）与微观层（文档切片引用索引）；摘要质量评分低于阈值时触发自动补检并在任务上下文中记录“数据不足”标记。
    [3]. 全景重构与变更轨迹：若细节挖掘发现与全景显著矛盾，系统应支持全景重构并记录变更轨迹，变更需纳入AUD审查流程并保留修正日志。

2. 任务分解、状态与协作规范
    [1]. 状态Schema与任务中间表示：任务必须具备标准IR（任务ID、类型、输入/输出Schema、依赖列表、优先级、超时策略、置信度与成本估算），并以StateSchema形式贯穿AGENT生命周期。
    [2]. 基于DAG的确定性流转：协作任务应以有向无环图（DAG）定义拓扑，支持自动并行执行无依赖子任务与可视化展示任务间数据流与控制流。
    [3]. Checkpoint与会话生命周期：在SP/BP关键阶段（参见战略规划模块的6个阶段）必须具备强制Checkpoint点，Checkpoint保存StateSnapshot并允许用户介入/回滚。

3. AGENT工作流（感知→规划→执行→验证→反思→输出）
    [1]. 五阶段通用工作流：AGENT工作流定义为[感知]环境渲染与上下文加载→[规划]任务分解与工具映射→[执行]按拓扑顺序调用工具/模型→[验证]AUD旁路一致性检测→[反思]错误分析与修正→[输出]结构化结果并释放资源。
    [2]. 多模型调度：工作流应支持快速响应模型（System-1）与深度推理模型（System-2）混合调度，并在SP/BP核心决策点或低置信度情形下触发System-2深推理或常态ToT并行推演。
    [3]. Early Stopping与循环终止：当任务达到预设置信度与目标时启用Early Stopping；若置信度低于阈值并且循环次数未达上限则继续反思循环。

4. 提示词工程与受控演进
    [1]. Prompt Optimizer闭环：将用户修正日志、AUD一致性报告、Checkpoint反馈作为输入，产出受控的System Prompt与Few-Shot样本；所有自动化候选须进入“待审查版本库”，经Strategy Reviewer或自动基准测试（Strat-Bench）通过后方可上线。
    [2]. 多目标优化与版本管理：Prompt演进必须以多目标（结构完整性、一致性、成本效率）为优化指标，并纳入版本控制与可回滚机制（保留历史稳定版本以便审计与回溯）。
    [3]. 演进触发策略：演进仅在满足触发条件时进行（例如修正率持续上升、一致性下降或成本异常），并保留演进审计记录。

5. 工具调用与能力治理
    [1]. 工具契约化：所有工具在工具库中需有唯一标识、输入/输出Schema、能力说明与验证器；调用前后必须进行Schema校验，违反契约触发熔断与回退策略。
    [2]. Tool RAG与工具选择器：系统应支持工具能力向量化检索（Tool-RAG），并基于历史成功率、Token成本与执行时长做多目标权衡推荐工具组合；低匹配度工具需人工确认。
    [3]. 沙箱执行与Fallback：所有工具执行应在受控沙箱内（代码/网络/数据权限最小化）完成；执行失败触发预定义Fallback Chain并记录失败模式用于技能演进。

6. 仲裁与决策机制
    [1]. 分级仲裁策略：采用分层仲裁——自动仲裁（高置信度、低风险）、结构化仲裁（中等风险，CEO仲裁并显示决策理由）、强制人工审批（高风险或合规模式）；决策必须产出Decision Rationale Report供审计。
    [2]. CEO裁决状态机：CEO AGENT应支持多轮辩论（发散→收敛→裁决）与Temperature Decay策略，并在未达成一致时生成多方案（至少Plan A/Plan B），同时提供用户介入窗口与影响模拟器供高管调整关键参数并作出最终裁决。
    [3]. 动态仲裁权转移：在专业性议题（如重大财务决策）可按规则将仲裁权临时移交相关专业Agent（如CFO），并记录权转轨迹与理由。

7. 审计、一致性与证据化输出
    [1]. AUD旁路监听：AUD AGENT以旁路监听模式进行一致性校验（事实/逻辑/数值/时效性），对可放行的轻微偏差发预警，对致命偏差异步阻断并触发人工纠错流程。
    [2]. 幻觉与一致性量化：定义统一的幻觉与一致性指标（如置信度阈值、引用缺失率、逻辑矛盾指数），低于阈值触发智能重试或任务暂停，并将纠错过程写入审计日志。
    [3]. GAIA证据包：每次重要分析输出必须打包证据（输入摘要哈希、工具调用序列与版本、关键中间状态、置信度评分、引用文档切片ID），并支持交互式溯源与不可篡改存储（供合规审计）。

8. 记忆与上下文管理
    [1]. 分层记忆：采用分层记忆（工作记忆/短期记忆/长期战略档案/经验记忆），各层有明确写入策略（基于重要性评分与置信度门限）与检索策略。
    [2]. 公共黑板与快照治理：公共黑板应以StateSnapshot机制管理版本（MVCC），仅写入关键结论并记录版本变更；支持按需回溯与Time-travel审查，但写操作需AUD校验与权限控制。
    [3]. 上下文压缩：对长会话应用上下文压缩器将历史压缩成关键结论集，压缩后的核心结论附带引用与置信度以便后续检索。

9. 用户反馈闭环与影响传播
    [1]. 结构化Feedback Schema：用户反馈采用标准JSON Schema（修正类型、修正前/后值、修正原因、用户角色、时间戳、置信度变化），并支持对影响范围进行自动化传播分析。
    [2]. 反馈处理分级：高价值/高置信度的专家修正可直接触发Prompt Optimizer；普通反馈被入库为修正轨迹；噪声反馈则由自动过滤机制识别并忽略。
    [3]. 修正影响报告：当修正影响下游任务或指标超阈值时自动生成Impact Report并在Checkpoint点强制人工确认。

10. 可解释性与决策可视化
    [1]. 决策证据图谱：输出Decision Evidence Graph与Decision Rationale Report，量化各输入因素对决策的贡献（如SHAP或Shapley度量），并提供反事实推理（如果改变关键假设结论如何变化）。
    [2]. 交互式溯源树：支持从结论逐层展开至原始数据的交互式界面（命令行导出可视化报告），每个节点显示来源、逻辑说明、置信度与引用切片ID。

11. ToT（思维链溯源树）触发策略
    [1]. 分级触发：ToT常态化触发于SP/BP核心分叉点与低置信度场景；触发条件基于置信度阈值、议题复杂度与潜在资金规模。
    [2]. 可审计路径保留：ToT生成的候选推理路径必须经AUD校验并保留通过检验的路径作为最终证据链，未通过路径写入Hallucination Log供后续改进。

12. 性能监控、漂移检测与行为分析
    [1]. 健康度面板（AgentBoard）：建立三维度监控（效率/质量/成本）并对关键指标（任务成功率、修正率、幻觉率、工具契约通过率）设定警戒值与三级预警策略（黄牌/红牌/停用）。
    [2]. 漂移检测与自动修复：集成统计检测（如KS/CUSUM等）判断输出分布或指标漂移；轻微漂移触发自动Prompt微调，中度/严重漂移触发Skill Review或Agent Quarantine。
    [3]. 根因分析与告警路由：指标异常时自动关联日志与证据包输出根因报告并路由至责任团队或触发自动回滚。

13. 主动能力与成本控制
    [1]. 主动唤醒与偏差预警：AGENT支持心跳机制，主动检查战略档案库中的偏差与待办事项并在偏差超过阈值时生成偏差分析报告推送相关AGENT/用户。
    [2]. 成本预测与三重熔断：任务启动前提供成本预估并需用户确认；实现任务级/会话级/系统级成本熔断策略以控制Token与算力成本。

14. 视角隔离与安全防护
    [1]. 角色视角硬隔离：每个AGENT加载角色IDENTITY与BOUNDARY，工具与数据的访问受最小权限控制，越权调用触发熔断与审计。
    [2]. 趋势漂移/视角偏离检测：实时监控AGENT输出的视角偏离度（关键词/主题偏移），超过阈值自动触发AUD告警并记录至Agent Safety Log。
    [3]. 沙箱与网络白名单：执行环境应具备代码/网络/数据三重隔离，并限制网络访问于可信白名单API，敏感操作需二次授权。

15. 合规、审计与企业接入
    [1]. 不可篡改审计与合规模块：关键操作与决策凭证写入WORM或等同不可篡改存储，并支持按时间/角色/任务多维检索以满足SOX/ISO等合规要求。
    [2]. 企业接入与部署模板：为不同规模企业提供部署模板（精简/标准/企业），并在命令行交互中提供结构化报告导出（含可点击引用索引）以便高管审阅与签字确认。

16. 性能目标与工程可交付性（需求约束）
    [1]. 可用性与恢复：支持任务中断后基于StateSnapshot恢复，并保证关键Checkpoint的RPO/RTO满足企业级SLA约定（在部署阶段明确化）。
    [2]. 渐进式交付策略：需求实现采用分阶段交付——MVP（核心RAG、契约工具、证据包、基础AGENT工作流）→ 增强（Prompt Optimizer、分层记忆、AgentBoard）→ 进阶（常态ToT、Time-travel、System-2池）。
    [3]. 交付与验收：所有新功能需通过Strat-Bench基准测试与回归契约测试后方可上线，并在上线后纳入A/B试运行窗口以验证业务价值。
}





# TEMP - 重构数据处理

你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要重新设计提取[企业战略规划管理智库系统需求特性列表
]->[数据处理]需求特性。要求：
1、收集2024~2026年RAG、LLM、AGENT与AGI领域有重要影响力的优秀案例（如openclaw, claude code, SDD等），分析归纳它们的关键成功要素KSF1~n
2、对标优秀案例的关键成功要素，客观评估审核上面[数据处理]需求特性的优势与劣势，[逐条]AND[完整]列表分析并提出完善优化建议，表头：原有特性、KSF对标、优势、劣势、优化建议
3、根据上面优化建议完善[数据处理]需求特性
4、以txt代码片段形式输出重构后完整的[数据处理]需求特性列表（保持排版格式不变）
----------------------
企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：支持基于网络LLM和先进Agent技术的智能分析工具集，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理（共计17个）。
    1.智能工具定义
        [1].建立工具标准：每个工具有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。工具初始化必须加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
        [2].构建中央技能仓库：实现工具的统一注册、版本控制、灰度发布与回滚机制。所有入库工具必须通过Pydantic V2契约测试，后续迭代需通过回归测试集。支持预定义模板与远期基于代码生成的技能动态扩展。
        [3].支持工具链编排与依赖调度：支持定义工具链有向无环图，实现上游工具输出结构化流转为下游输入。内置任务调度器，根据依赖关系自动并行执行无依赖子任务。
    2.工具箱标准工作流
        [1].环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具价值观模板（SOUL.md），实例化会话级沙箱与混合检索记忆容器。
        [2].感知与摘要：读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
        [3].规划：Planner依据用户自然语言指令及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、匹配工具映射、数据依赖边。
        [4].执行与验证：Executor按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe→writeback原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
        [5].证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
        [6].反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
        [7].成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
        [8].演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。
    3.数值计算与沙箱执行
        [1].强制代码执行隔离：严禁LLM直接进行数值计算、统计回归或现金流折现。所有量化分析强制通过两条路径执行：一是生成Python代码在沙箱执行；二是针对战略运筹问题（如线性规划），由LLM构建数学模型调用CVXPY、OR-Tools等优化器求解。
        [2].构建持久化计算沙箱：为每个任务会话实例化持久化Jupyter Kernel，支持跨工具、跨步骤的变量传递（如宏观经济变量在不同模型间共享），实现分析上下文连贯。任务结束后自动销毁内核并回收资源。
        [3].支持Validation Feedback闭环：自动捕获代码执行STDERR，检索错误案例库辅助LLM生成修复版本。设定最大重试次数（默认3次），失败则标记不可行并记录至演进日志。
    4.Schema强制与一致性校验
        [1].实施契约化输出：所有工具输入/输出严格遵循Pydantic V2定义的Data Model，利用Instructor Patch强制LLM输出符合Schema的结构化JSON，严禁输出自由文本。
        [2].建立一致性校验仲裁器：在多工具协作中，通过内置规则引擎及财务常识库检测逻辑冲突（如利润率与成本矛盾）。输出冲突检测报告，供用户或高级AGENT裁决。
        [3].自动化契约测试：在CI/CD流水线集成Schema契约测试，防止模型漂移导致结构崩溃。契约测试失败的任务强制终止。
    5.可解释性与决策溯源
        [1].输出结构化推理证据包：每次分析必须打包输出全量元数据，包括输入摘要哈希、Planner任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片ID）。
        [2].构建交互式溯源树：支持前端从结论反向逐层展开证据包，展示“数据来源→计算逻辑→中间结论→最终结论”的完整推理链。
        [3].支持红蓝辩论机制：针对高不确定性议题自动启动“红蓝对抗”，实例化激进派与保守派子智能体进行结构化辩论，输出包含共识与分歧区域的风险全景视图。
    6.提示词工程与演进
        [1].Prompt Optimizer：基于DSPy理念，建立“修正日志-样本转换”闭环，将用户修正转化为Few-Shot样本，自动迭代提示词参数以适配企业战略风格。
        [2].实施受控自动演进：仅在SP/BP Checkpoint完成态且满足一致性阈值时触发演进。必须采用多目标优化（结构、一致性、成本），严禁单一指标优化。
        [3].确保方法论不变性：演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
        [4].支持版本管理与回滚：所有Prompt纳入Git LFS管理，与系统版本绑定。支持一键回滚至历史稳定版本。
    7.用户反馈与闭环演进
        [1].支持Checkpoint人工介入：在关键决策节点或低置信度环节自动暂停，请求用户验证中间假设。支持用户修改参数或调整权重，系统记录修正前后的“专家修正轨迹”。
        [2].建立修正轨迹库与逆向传播：将用户修正快照匿名化存储，定期分析差异，推断输入权重或任务分解策略的优化方向。
        [3].实施用户反馈协议：将用户反馈转化为结构化修正日志，强制触发工具体“反思阶段”（ReflAct），并回传至技能演进日志。
    8.安全与防御机制
        [1].实施沙箱网络隔离：代码执行环境默认断网，仅允许通过白名单网关访问预设的可信财经API，严禁通用网页爬虫。
        [2].建立提示词注入防御：输入数据前置执行清洗与注入检测。将“注入防御测试”纳入基准测试集，定期验证工具体的抗干扰能力。
        [3].建立工具幻觉黑名单：自动监控工具体输出，若连续3次生成与输入矛盾的结论，自动锁定该工具体并报警。
    9.领域专业知识嵌入
        [1].内置领域事实库：以JSON Schema存储经典管理理论的强制约束（如SWOT内部/外部属性分类），确保分析逻辑符合理论框架。
        [2].部署领域验证器：工具体输出前必须经过验证器检查，确保结论不违背基础商业逻辑与领域常识。
    10.服务契约与集成
        [1].独立微服务封装：工具箱作为独立微服务对外暴露OpenAPI 3.1标准接口，输出分析结果、证据包与成本审计信息三元组。
        [2].支持Agent模块集成：与CEO、CFO等核心AGENT通过服务调用无缝集成，支持水平扩展与分布式Trace追踪。
    11.测试与质量保证
        [1].构建基准测试集（Strat-Bench）：建立包含20+专家标注场景的战略分析专用测试集，作为版本迭代的准入测试标准。
        [2].支持A/B测试框架：支持多版本策略并行运行，基于任务完成率、计算精度与成本指标择优晋级。
        [3].部署健康度仪表盘：实时监控工具成功率与修正率，修正率超标（默认15%）自动触发Skill Review。
    12.性能监控与漂移检测
        [1].建立性能漂移检测：定期评估检索准确率与生成质量，对比历史基线检测性能下降。
        [2].支持可视化日志：所有评估与运行结果纳入系统行为日志并可视化展示。
    13.成本熔断与契约测试质量门禁
        [1].CI/CD契约测试门禁：所有工具输出必须通过严格Schema校验，非兼容变更触发构建失败。运行时连续三次校验失败触发工具熔断。
        [2].实施三级成本熔断：支持任务级（单次Token/成本超限）、会话级（日配额超限）与重试级（连续代码执行失败）熔断机制，确保系统运行成本可控。

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1. 输入处理与全景构建
        [1]. 输入系统预处理的结构化JSON数据，AGENT首先调用摘要质量评估器（基于信息熵与关键实体覆盖率），若摘要质量评分低于阈值（默认0.7）则自动触发二次检索补充缺失维度，确保全景视图的完整性。
        [2]. 构建动态全景视图：宏观层（战略指标趋势/异常波动/关键假设变量）与微观层（文档切片引用索引）双向关联，支持细节挖掘过程中发现与全景视图严重矛盾时自动触发视图重构，重构过程需记录变更轨迹并提交AUD AGENT审核。
        [3]. 实施上下文窗口动态管理：基于任务复杂度自适应分配Token预算，超限时触发上下文压缩器（基于关键实体抽取与HyDE算法），压缩率目标≥70%，确保关键信息不丢失。
    2. 任务分解与协作架构
        [1]. 建立任务IR（中间表示）标准：每个任务包含任务ID、类型、输入/输出Schema、依赖任务列表、优先级、执行状态、超时策略。所有协作任务必须基于有向无环图（DAG）构建流转拓扑，确保多体协作具备确定性的状态机流转特征。
        [2]. 采用分层协作架构：CEO AGENT作为指挥官负责目标解析与全局任务分解，各专业Agent并行执行子任务，调度器负责资源分配与执行监控。自动生成协作依赖图，基于BLM/BEM阶段定义标准依赖关系，支持动态调整与优化，可视化展示任务间数据流与控制流。
        [3]. 支持柔性视角隔离：定义核心视角（必须遵守）和扩展视角（可协商），对跨领域问题组建临时联合分析组，建立视角冲突解决协议。视角隔离通过Prompt隔离、工具隔离、数据隔离三重硬约束实现。
    3. AGENT确定性工作流
        [1]. 基于状态图的工作流引擎：采用StateGraph机制定义显式状态模型，包含session_id、current_phase、agent_role、working_memory、long_term_memory_ref、tools_used、confidence_score、cost_accumulated等字段。工作流循环定义为：[感知]环境初始化与上下文加载→[规划]生成任务执行DAG→[执行]按拓扑顺序调度子任务→[深度思考]（可选）在关键决策节点并行生成多条思维链推演路径→[验证]结构化检查输出→[反思]错误分析与修正→[闭环]Early Stopping机制（置信度达标且目标达成时提前终止）。
        [2]. 深度思考与多路径推演：在SP/BP所有核心分叉点（如商业模式选择、核心竞争力定义），默认调用支持深层推理的模型，并行生成多条思维链推演路径，通过内部验证打分选出最优解，极大提升战略输出的逻辑厚度。
        [3]. 中断安全与状态持久化：任意节点支持用户中断，自动保存StateSnapshot至Redis Cluster（三节点哨兵模式），恢复时加载完整上下文与工具状态，支持从任意历史快照恢复执行（Time-travel能力分步实现）。
    4. 用户反馈闭环
        [1]. 建立反馈标准化协议：将用户反馈转化为结构化修正日志（包含修正类型enum[参数/权重/约束]、修正前/后值、置信度变化、影响因子、用户角色、时间戳）。
        [2]. 反馈分级处理与即时响应：①专家修正（置信度>0.9）直接触发Prompt Optimizer；②普通反馈进入修正轨迹库；③噪声反馈自动过滤。设置专用反馈通道，承诺24小时内处理并回复，紧急反馈设置加急处理流程。
        [3]. 修正影响传播分析：评估修正对下游任务的影响范围，高影响修正（>3个下游任务）强制生成Impact Report并通知相关AGENT。支持从任意Checkpoint恢复执行后修改中间状态变量（Time-travel第一阶段），并从修改点继续执行。
    5. 提示词工程与演进
        [1]. 基于DSPy理念构建Prompt Optimizer：输入为用户反馈、AUD一致性报告、SP/BP Checkpoint人工反馈，输出优化后System Prompt与Few-Shot样本集。优化目标采用Pareto多目标权衡（结构完整性40% + 逻辑一致性35% + 成本效率25%），采用NSGA-II算法求解Pareto前沿。
        [2]. 受控演进机制：自动微调的提示词参数必须进入“待审查版本库”，经人工专家（首席战略官或授权代表）确认后方可推向生产环境。集成MLflow实现Prompt自动版本注册，支持A/B测试（分流比例可配置）与一键回滚（保留最近10个稳定版本）。
        [3]. 提示词契约测试：每次优化后自动运行Strat-Bench基准测试集，通过率<90%自动拒绝上线，并生成差异分析报告。演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
    6. 工具调用与集成
        [1]. 全面遵循MCP（Model Context Protocol）2025规范：工具箱作为独立微服务对外暴露符合MCP标准的能力发现接口，支持工具注册、版本控制、灰度发布与回滚。AGENT通过MCP Registry查询工具能力描述（输入/输出Schema、版本、可靠性评分）。
        [2]. 工具选择优化：基于语义匹配（60%）+历史成功率（40%）计算工具匹配度，匹配度<0.6时触发人工确认或启动多工具并行验证。预定义17种战略工具的最佳组合模板（如SWOT+TOWS自动组合），支持用户自定义模板。
        [3]. 强制契约化调用与沙箱执行：所有工具调用前验证输入Schema，调用后验证输出Schema，连续3次校验失败自动熔断该工具。代码执行在E2B沙盒中（网络隔离+权限最小化），执行结果经Validation Feedback闭环校验，最大重试次数3次。
    7. 仲裁与决策机制
        [1]. CEO AGENT内置裁决状态机，设定最大辩论轮次（默认3+风险等级，上限7轮）。辩论采用三阶段：发散（Temperature=0.8）、收敛（Temperature=0.5）、裁决（Temperature=0.2），强制随轮次增加提交具体修改JSON补丁。
        [2]. 辩论质量评估：每轮计算新信息增益率与重复率，增益率<10%且重复率>50%时强制终止辩论。辩论过程记录各AGENT贡献度（基于Shapley值量化），分歧点生成热力图。
        [3]. 多方案生成与人类一票否决：若未达成一致，生成Plan A（保守参数集）、Plan B（激进参数集）和Plan C（AI融合版），输出参数调整明细。强制暂停60秒提供用户介入窗口，窗口期可视化展示三套方案的对比差异（风险/收益/资源需求）及影响模拟器（支持用户滑动调整关键参数实时预览结果）。超时无用户操作则按CEO决策执行，但所有人工覆盖决策强制记录至战略档案库并标记“人工覆盖”。
    8. 审计与一致性保障
        [1]. AUD AGENT采用“旁路监听+异步阻断”模式，不参与生成。审计范围包括四维一致性检验：①事实一致性（对比原始文档切片）；②逻辑一致性（规则引擎检测内部矛盾）；③数值一致性（沙箱重计算关键指标，误差>5%标记异常）；④时效一致性（引用数据超12个月标记“数据陈旧”）。
        [2]. 分级熔断与根因分析：L1（幻觉概率>0.8）自动触发修正；L2（0.6-0.8）通知相关AGENT重试（最多2次）；L3（<0.6）暂停任务并生成Hallucination Report（包含错误类型分类：数据缺失/逻辑跳跃/数值错误/时效过期），人工介入后从断点恢复。
        [3]. 审计日志强制存储：所有审计发现、纠错过程、重试轨迹全量存储至不可篡改日志（WORM存储），满足SOX/ISO27001审计要求，支持按时间/角色/任务类型多维检索。
    9. 记忆与上下文管理
        [1]. 分层记忆架构：工作记忆（当前会话，有效期24h）、短期记忆（最近项目，有效期30天）、长期记忆（战略知识库，永久存储），各层有独立的存储引擎和检索策略。长期记忆将用户修正日志、决策复盘结果、执行结果差值反向固化为少样本提示词，每季度触发知识蒸馏与去重。
        [2]. 公共黑板机制：基于LangGraph StateSnapshot + Redis Cluster管理状态版本，支持多版本并发控制（MVCC）。关键结论写入黑板时必须附带重要性评分（1-5）、置信度、引用源切片ID、时效性，综合评分>3.5才写入，便于上下文压缩器智能筛选。
        [3]. 时间旅行调试：支持从任意Checkpoint恢复执行，用于问题复现与根因分析。所有状态快照与完整推理轨迹归档至数据湖，可按需解压缩回溯。
    10. 可解释性与溯源
        [1]. 结构化证据包标准：每次分析打包输出全量元数据，包括输入摘要哈希、任务分解图、工具调用轨迹（含版本）、关键中间变量快照、置信度评分及依据、引用源切片ID列表。
        [2]. 交互式溯源树：前端支持从结论节点逐层展开至原始数据，每层显示“数据来源→计算逻辑→中间结论”三元组，每个节点支持查看详细信息。
        [3]. 决策影响分析：量化各输入变量对最终决策的Shapley贡献值，高影响变量（>20%）自动高亮标注。支持反事实推理，输出“如果改变某假设，结论会如何变化”的对比报告和风险热力图。
    11. 主动能力
        [1]. 引入心跳机制：支持周期性自动唤醒（可配置频率），主动检查战略档案库中的待办事项、偏差预警、周期性任务，无需用户提问即可主动沟通。
        [2]. 战略偏差预警：实时监控实际执行数据与SP/BP规划的偏差，偏差超过阈值（默认10%）自动触发预警，生成偏差分析报告并推送相关AGENT。
        [3]. 主动知识更新：当检测到行业报告、市场数据、政策法规更新时，自动触发电请相关AGENT评估影响，重要影响主动推送CEO AGENT。
    12. 性能监控与行为分析
        [1]. 建立三维度指标体系：效率维度（任务完成率、平均时长、工具调用成功率）、质量维度（修正率<10%、幻觉率<5%、一致性得分>0.8）、成本维度（单任务API成本、Token消耗）。构建战略规划专用基准测试集（Strat-Bench）作为版本迭代的准入标准。
        [2]. 漂移检测双算法：KS检验检测输出分布偏移（月度基线对比），CUSUM检测性能指标连续下降（滑动窗口7天）。指标异常时启动自动根因分析，关联日志输出根因报告。
        [3]. 健康度仪表盘：实时可视化展示各AGENT的12项健康度指标，支持下钻分析与预警订阅。三级预警机制：黄牌（指标下降15%触发分析）、红牌（下降30%触发人工review）、停用（下降50%且连续三天，自动回滚至前一稳定版本）。
    13. 安全与防御
        [1]. 纵深防御体系：
            - 第一层（隔离）：三重硬隔离（Prompt隔离、工具隔离、数据隔离），基于角色权限最小化。
            - 第二层（执行）：所有代码在E2B沙盒中执行，严格网络隔离（仅白名单API）。
            - 第三层（检测）：实时监控视角越界（跨角色关键词频率>5%触发熔断）、幻觉累积（连续3次低置信度结论锁定Agent）。
            - 第四层（审计）：所有操作记录至WORM存储。
            - 第五层（熔断）：辩论过热保护、成本熔断。
        [2]. 敏感数据脱敏：审计日志和反馈记录自动脱敏处理（个人可识别信息、商业机密），脱敏策略定期评审更新。
    14. 成本控制
        [1]. 成本预测：任务启动前基于历史相似任务预测Token消耗与API成本，偏差>50%时提示用户确认。
        [2]. 三级熔断机制：
            - 任务级熔断：单次任务Token消耗超预设阈值（基础任务5K tokens，复杂规划50K tokens）立即终止。
            - 会话级熔断：日累计Token消耗超企业配额（默认100万tokens）暂停新任务，已启动任务完成。
            - 系统级熔断：连续3次工具执行失败或单任务成本超预算200%触发系统级告警，需管理员介入。

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察(包括六个子步骤)、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据


## 归纳评估维度


你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。D1~5~是针对[企业战略规划管理智库系统需求特性列表]->[数据处理]需求特性的重构，请你结合系统整体需求，以宗师级水准对标优秀案例，对这些需求列表内容进行评估。要求：
1、收集2024~2026年RAG、LLM、AGENT与AGI领域有重要影响力的优秀案例（如openclaw, claude code, SDD等），分析归纳它们的关键成功要素KSF1~n
2、对标优秀案例的关键成功要素，[逐条]AND[完整]列表分析评估审核上面[数据处理]需求特性
3、严格从[客观视角]进行评估
4、以表格形式呈现，表头：评估维度---D1评估---D2评估---D3评估---D4评估---D5评估(10分制)

------------------

企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：支持基于网络LLM和先进Agent技术的智能分析工具集，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理（共计17个）。
    1.智能工具定义
        [1].建立工具标准：每个工具有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。工具初始化必须加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
        [2].构建中央技能仓库：实现工具的统一注册、版本控制、灰度发布与回滚机制。所有入库工具必须通过Pydantic V2契约测试，后续迭代需通过回归测试集。支持预定义模板与远期基于代码生成的技能动态扩展。
        [3].支持工具链编排与依赖调度：支持定义工具链有向无环图，实现上游工具输出结构化流转为下游输入。内置任务调度器，根据依赖关系自动并行执行无依赖子任务。
    2.工具箱标准工作流
        [1].环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具价值观模板（SOUL.md），实例化会话级沙箱与混合检索记忆容器。
        [2].感知与摘要：读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
        [3].规划：Planner依据用户自然语言指令及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、匹配工具映射、数据依赖边。
        [4].执行与验证：Executor按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe→writeback原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
        [5].证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
        [6].反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
        [7].成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
        [8].演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。
    3.数值计算与沙箱执行
        [1].强制代码执行隔离：严禁LLM直接进行数值计算、统计回归或现金流折现。所有量化分析强制通过两条路径执行：一是生成Python代码在沙箱执行；二是针对战略运筹问题（如线性规划），由LLM构建数学模型调用CVXPY、OR-Tools等优化器求解。
        [2].构建持久化计算沙箱：为每个任务会话实例化持久化Jupyter Kernel，支持跨工具、跨步骤的变量传递（如宏观经济变量在不同模型间共享），实现分析上下文连贯。任务结束后自动销毁内核并回收资源。
        [3].支持Validation Feedback闭环：自动捕获代码执行STDERR，检索错误案例库辅助LLM生成修复版本。设定最大重试次数（默认3次），失败则标记不可行并记录至演进日志。
    4.Schema强制与一致性校验
        [1].实施契约化输出：所有工具输入/输出严格遵循Pydantic V2定义的Data Model，利用Instructor Patch强制LLM输出符合Schema的结构化JSON，严禁输出自由文本。
        [2].建立一致性校验仲裁器：在多工具协作中，通过内置规则引擎及财务常识库检测逻辑冲突（如利润率与成本矛盾）。输出冲突检测报告，供用户或高级AGENT裁决。
        [3].自动化契约测试：在CI/CD流水线集成Schema契约测试，防止模型漂移导致结构崩溃。契约测试失败的任务强制终止。
    5.可解释性与决策溯源
        [1].输出结构化推理证据包：每次分析必须打包输出全量元数据，包括输入摘要哈希、Planner任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片ID）。
        [2].构建交互式溯源树：支持前端从结论反向逐层展开证据包，展示“数据来源→计算逻辑→中间结论→最终结论”的完整推理链。
        [3].支持红蓝辩论机制：针对高不确定性议题自动启动“红蓝对抗”，实例化激进派与保守派子智能体进行结构化辩论，输出包含共识与分歧区域的风险全景视图。
    6.提示词工程与演进
        [1].Prompt Optimizer：基于DSPy理念，建立“修正日志-样本转换”闭环，将用户修正转化为Few-Shot样本，自动迭代提示词参数以适配企业战略风格。
        [2].实施受控自动演进：仅在SP/BP Checkpoint完成态且满足一致性阈值时触发演进。必须采用多目标优化（结构、一致性、成本），严禁单一指标优化。
        [3].确保方法论不变性：演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
        [4].支持版本管理与回滚：所有Prompt纳入Git LFS管理，与系统版本绑定。支持一键回滚至历史稳定版本。
    7.用户反馈与闭环演进
        [1].支持Checkpoint人工介入：在关键决策节点或低置信度环节自动暂停，请求用户验证中间假设。支持用户修改参数或调整权重，系统记录修正前后的“专家修正轨迹”。
        [2].建立修正轨迹库与逆向传播：将用户修正快照匿名化存储，定期分析差异，推断输入权重或任务分解策略的优化方向。
        [3].实施用户反馈协议：将用户反馈转化为结构化修正日志，强制触发工具体“反思阶段”（ReflAct），并回传至技能演进日志。
    8.安全与防御机制
        [1].实施沙箱网络隔离：代码执行环境默认断网，仅允许通过白名单网关访问预设的可信财经API，严禁通用网页爬虫。
        [2].建立提示词注入防御：输入数据前置执行清洗与注入检测。将“注入防御测试”纳入基准测试集，定期验证工具体的抗干扰能力。
        [3].建立工具幻觉黑名单：自动监控工具体输出，若连续3次生成与输入矛盾的结论，自动锁定该工具体并报警。
    9.领域专业知识嵌入
        [1].内置领域事实库：以JSON Schema存储经典管理理论的强制约束（如SWOT内部/外部属性分类），确保分析逻辑符合理论框架。
        [2].部署领域验证器：工具体输出前必须经过验证器检查，确保结论不违背基础商业逻辑与领域常识。
    10.服务契约与集成
        [1].独立微服务封装：工具箱作为独立微服务对外暴露OpenAPI 3.1标准接口，输出分析结果、证据包与成本审计信息三元组。
        [2].支持Agent模块集成：与CEO、CFO等核心AGENT通过服务调用无缝集成，支持水平扩展与分布式Trace追踪。
    11.测试与质量保证
        [1].构建基准测试集（Strat-Bench）：建立包含20+专家标注场景的战略分析专用测试集，作为版本迭代的准入测试标准。
        [2].支持A/B测试框架：支持多版本策略并行运行，基于任务完成率、计算精度与成本指标择优晋级。
        [3].部署健康度仪表盘：实时监控工具成功率与修正率，修正率超标（默认15%）自动触发Skill Review。
    12.性能监控与漂移检测
        [1].建立性能漂移检测：定期评估检索准确率与生成质量，对比历史基线检测性能下降。
        [2].支持可视化日志：所有评估与运行结果纳入系统行为日志并可视化展示。
    13.成本熔断与契约测试质量门禁
        [1].CI/CD契约测试门禁：所有工具输出必须通过严格Schema校验，非兼容变更触发构建失败。运行时连续三次校验失败触发工具熔断。
        [2].实施三级成本熔断：支持任务级（单次Token/成本超限）、会话级（日配额超限）与重试级（连续代码执行失败）熔断机制，确保系统运行成本可控。

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1. 输入处理与全景构建
        [1]. 输入系统预处理的结构化JSON数据，AGENT首先调用摘要质量评估器（基于信息熵与关键实体覆盖率），若摘要质量评分低于阈值（默认0.7）则自动触发二次检索补充缺失维度，确保全景视图的完整性。
        [2]. 构建动态全景视图：宏观层（战略指标趋势/异常波动/关键假设变量）与微观层（文档切片引用索引）双向关联，支持细节挖掘过程中发现与全景视图严重矛盾时自动触发视图重构，重构过程需记录变更轨迹并提交AUD AGENT审核。
        [3]. 实施上下文窗口动态管理：基于任务复杂度自适应分配Token预算，超限时触发上下文压缩器（基于关键实体抽取与HyDE算法），压缩率目标≥70%，确保关键信息不丢失。
    2. 任务分解与协作架构
        [1]. 建立任务IR（中间表示）标准：每个任务包含任务ID、类型、输入/输出Schema、依赖任务列表、优先级、执行状态、超时策略。所有协作任务必须基于有向无环图（DAG）构建流转拓扑，确保多体协作具备确定性的状态机流转特征。
        [2]. 采用分层协作架构：CEO AGENT作为指挥官负责目标解析与全局任务分解，各专业Agent并行执行子任务，调度器负责资源分配与执行监控。自动生成协作依赖图，基于BLM/BEM阶段定义标准依赖关系，支持动态调整与优化，可视化展示任务间数据流与控制流。
        [3]. 支持柔性视角隔离：定义核心视角（必须遵守）和扩展视角（可协商），对跨领域问题组建临时联合分析组，建立视角冲突解决协议。视角隔离通过Prompt隔离、工具隔离、数据隔离三重硬约束实现。
    3. AGENT确定性工作流
        [1]. 基于状态图的工作流引擎：采用StateGraph机制定义显式状态模型，包含session_id、current_phase、agent_role、working_memory、long_term_memory_ref、tools_used、confidence_score、cost_accumulated等字段。工作流循环定义为：[感知]环境初始化与上下文加载→[规划]生成任务执行DAG→[执行]按拓扑顺序调度子任务→[深度思考]（可选）在关键决策节点并行生成多条思维链推演路径→[验证]结构化检查输出→[反思]错误分析与修正→[闭环]Early Stopping机制（置信度达标且目标达成时提前终止）。
        [2]. 深度思考与多路径推演：在SP/BP所有核心分叉点（如商业模式选择、核心竞争力定义），默认调用支持深层推理的模型，并行生成多条思维链推演路径，通过内部验证打分选出最优解，极大提升战略输出的逻辑厚度。
        [3]. 中断安全与状态持久化：任意节点支持用户中断，自动保存StateSnapshot至Redis Cluster（三节点哨兵模式），恢复时加载完整上下文与工具状态，支持从任意历史快照恢复执行（Time-travel能力分步实现）。
    4. 用户反馈闭环
        [1]. 建立反馈标准化协议：将用户反馈转化为结构化修正日志（包含修正类型enum[参数/权重/约束]、修正前/后值、置信度变化、影响因子、用户角色、时间戳）。
        [2]. 反馈分级处理与即时响应：①专家修正（置信度>0.9）直接触发Prompt Optimizer；②普通反馈进入修正轨迹库；③噪声反馈自动过滤。设置专用反馈通道，承诺24小时内处理并回复，紧急反馈设置加急处理流程。
        [3]. 修正影响传播分析：评估修正对下游任务的影响范围，高影响修正（>3个下游任务）强制生成Impact Report并通知相关AGENT。支持从任意Checkpoint恢复执行后修改中间状态变量（Time-travel第一阶段），并从修改点继续执行。
    5. 提示词工程与演进
        [1]. 基于DSPy理念构建Prompt Optimizer：输入为用户反馈、AUD一致性报告、SP/BP Checkpoint人工反馈，输出优化后System Prompt与Few-Shot样本集。优化目标采用Pareto多目标权衡（结构完整性40% + 逻辑一致性35% + 成本效率25%），采用NSGA-II算法求解Pareto前沿。
        [2]. 受控演进机制：自动微调的提示词参数必须进入“待审查版本库”，经人工专家（首席战略官或授权代表）确认后方可推向生产环境。集成MLflow实现Prompt自动版本注册，支持A/B测试（分流比例可配置）与一键回滚（保留最近10个稳定版本）。
        [3]. 提示词契约测试：每次优化后自动运行Strat-Bench基准测试集，通过率<90%自动拒绝上线，并生成差异分析报告。演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
    6. 工具调用与集成
        [1]. 全面遵循MCP（Model Context Protocol）2025规范：工具箱作为独立微服务对外暴露符合MCP标准的能力发现接口，支持工具注册、版本控制、灰度发布与回滚。AGENT通过MCP Registry查询工具能力描述（输入/输出Schema、版本、可靠性评分）。
        [2]. 工具选择优化：基于语义匹配（60%）+历史成功率（40%）计算工具匹配度，匹配度<0.6时触发人工确认或启动多工具并行验证。预定义17种战略工具的最佳组合模板（如SWOT+TOWS自动组合），支持用户自定义模板。
        [3]. 强制契约化调用与沙箱执行：所有工具调用前验证输入Schema，调用后验证输出Schema，连续3次校验失败自动熔断该工具。代码执行在E2B沙盒中（网络隔离+权限最小化），执行结果经Validation Feedback闭环校验，最大重试次数3次。
    7. 仲裁与决策机制
        [1]. CEO AGENT内置裁决状态机，设定最大辩论轮次（默认3+风险等级，上限7轮）。辩论采用三阶段：发散（Temperature=0.8）、收敛（Temperature=0.5）、裁决（Temperature=0.2），强制随轮次增加提交具体修改JSON补丁。
        [2]. 辩论质量评估：每轮计算新信息增益率与重复率，增益率<10%且重复率>50%时强制终止辩论。辩论过程记录各AGENT贡献度（基于Shapley值量化），分歧点生成热力图。
        [3]. 多方案生成与人类一票否决：若未达成一致，生成Plan A（保守参数集）、Plan B（激进参数集）和Plan C（AI融合版），输出参数调整明细。强制暂停60秒提供用户介入窗口，窗口期可视化展示三套方案的对比差异（风险/收益/资源需求）及影响模拟器（支持用户滑动调整关键参数实时预览结果）。超时无用户操作则按CEO决策执行，但所有人工覆盖决策强制记录至战略档案库并标记“人工覆盖”。
    8. 审计与一致性保障
        [1]. AUD AGENT采用“旁路监听+异步阻断”模式，不参与生成。审计范围包括四维一致性检验：①事实一致性（对比原始文档切片）；②逻辑一致性（规则引擎检测内部矛盾）；③数值一致性（沙箱重计算关键指标，误差>5%标记异常）；④时效一致性（引用数据超12个月标记“数据陈旧”）。
        [2]. 分级熔断与根因分析：L1（幻觉概率>0.8）自动触发修正；L2（0.6-0.8）通知相关AGENT重试（最多2次）；L3（<0.6）暂停任务并生成Hallucination Report（包含错误类型分类：数据缺失/逻辑跳跃/数值错误/时效过期），人工介入后从断点恢复。
        [3]. 审计日志强制存储：所有审计发现、纠错过程、重试轨迹全量存储至不可篡改日志（WORM存储），满足SOX/ISO27001审计要求，支持按时间/角色/任务类型多维检索。
    9. 记忆与上下文管理
        [1]. 分层记忆架构：工作记忆（当前会话，有效期24h）、短期记忆（最近项目，有效期30天）、长期记忆（战略知识库，永久存储），各层有独立的存储引擎和检索策略。长期记忆将用户修正日志、决策复盘结果、执行结果差值反向固化为少样本提示词，每季度触发知识蒸馏与去重。
        [2]. 公共黑板机制：基于LangGraph StateSnapshot + Redis Cluster管理状态版本，支持多版本并发控制（MVCC）。关键结论写入黑板时必须附带重要性评分（1-5）、置信度、引用源切片ID、时效性，综合评分>3.5才写入，便于上下文压缩器智能筛选。
        [3]. 时间旅行调试：支持从任意Checkpoint恢复执行，用于问题复现与根因分析。所有状态快照与完整推理轨迹归档至数据湖，可按需解压缩回溯。
    10. 可解释性与溯源
        [1]. 结构化证据包标准：每次分析打包输出全量元数据，包括输入摘要哈希、任务分解图、工具调用轨迹（含版本）、关键中间变量快照、置信度评分及依据、引用源切片ID列表。
        [2]. 交互式溯源树：前端支持从结论节点逐层展开至原始数据，每层显示“数据来源→计算逻辑→中间结论”三元组，每个节点支持查看详细信息。
        [3]. 决策影响分析：量化各输入变量对最终决策的Shapley贡献值，高影响变量（>20%）自动高亮标注。支持反事实推理，输出“如果改变某假设，结论会如何变化”的对比报告和风险热力图。
    11. 主动能力
        [1]. 引入心跳机制：支持周期性自动唤醒（可配置频率），主动检查战略档案库中的待办事项、偏差预警、周期性任务，无需用户提问即可主动沟通。
        [2]. 战略偏差预警：实时监控实际执行数据与SP/BP规划的偏差，偏差超过阈值（默认10%）自动触发预警，生成偏差分析报告并推送相关AGENT。
        [3]. 主动知识更新：当检测到行业报告、市场数据、政策法规更新时，自动触发电请相关AGENT评估影响，重要影响主动推送CEO AGENT。
    12. 性能监控与行为分析
        [1]. 建立三维度指标体系：效率维度（任务完成率、平均时长、工具调用成功率）、质量维度（修正率<10%、幻觉率<5%、一致性得分>0.8）、成本维度（单任务API成本、Token消耗）。构建战略规划专用基准测试集（Strat-Bench）作为版本迭代的准入标准。
        [2]. 漂移检测双算法：KS检验检测输出分布偏移（月度基线对比），CUSUM检测性能指标连续下降（滑动窗口7天）。指标异常时启动自动根因分析，关联日志输出根因报告。
        [3]. 健康度仪表盘：实时可视化展示各AGENT的12项健康度指标，支持下钻分析与预警订阅。三级预警机制：黄牌（指标下降15%触发分析）、红牌（下降30%触发人工review）、停用（下降50%且连续三天，自动回滚至前一稳定版本）。
    13. 安全与防御
        [1]. 纵深防御体系：
            - 第一层（隔离）：三重硬隔离（Prompt隔离、工具隔离、数据隔离），基于角色权限最小化。
            - 第二层（执行）：所有代码在E2B沙盒中执行，严格网络隔离（仅白名单API）。
            - 第三层（检测）：实时监控视角越界（跨角色关键词频率>5%触发熔断）、幻觉累积（连续3次低置信度结论锁定Agent）。
            - 第四层（审计）：所有操作记录至WORM存储。
            - 第五层（熔断）：辩论过热保护、成本熔断。
        [2]. 敏感数据脱敏：审计日志和反馈记录自动脱敏处理（个人可识别信息、商业机密），脱敏策略定期评审更新。
    14. 成本控制
        [1]. 成本预测：任务启动前基于历史相似任务预测Token消耗与API成本，偏差>50%时提示用户确认。
        [2]. 三级熔断机制：
            - 任务级熔断：单次任务Token消耗超预设阈值（基础任务5K tokens，复杂规划50K tokens）立即终止。
            - 会话级熔断：日累计Token消耗超企业配额（默认100万tokens）暂停新任务，已启动任务完成。
            - 系统级熔断：连续3次工具执行失败或单任务成本超预算200%触发系统级告警，需管理员介入。

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察(包括六个子步骤)、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据

------------------

D1
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据。核心设计理念是构建一个“本地优先、主动感知、自我进化”的数据操作系统。

    1. 多模态智能解析与格式支持
        [1]. 支持的原始格式包括但不限于：pdf（含扫描件）、txt、doc、docx、ppt、pptx、xls、xlsx、csv、jpeg、png、gif、json、log。
        [2]. 深度解析能力：对扫描件进行OCR文字识别；对图片和图表，调用多模态模型提取结构化描述（如趋势、数据点）并转化为可检索文本；对表格数据，保留其行列关系并转换为结构化元数据。
        [3]. 本地数据源挂载与同步：支持挂载本地文件夹、企业网盘、SharePoint等作为数据源，建立文件系统监听机制，对源文件的增删改操作进行实时同步与索引更新，实现数据随用随到。

    2. 数据契约与质量门禁
        [1]. 数据契约定义：为不同数据源（如年报、研报、财报）强制定义基于Pydantic的数据契约，明确输入Schema、完整性规则、新鲜度要求（如行业数据>3个月需打标）。
        [2]. 质量门禁：数据入库前必须通过契约校验，失败数据进入“待定区”并触发告警，无法通过修复流程的数据将被拒绝入库。
        [3]. 质量控制算子：内置格式验证、完整性检查、基于SIMHash的重复检测、时效性验证算子，作为预处理流水线的标准环节。

    3. 自适应RAG流水线
        [1]. 领域自适应嵌入模型：基于bge-m3等基座模型，利用企业历史战略文档进行领域自适应微调，并纳入CI/CD管理，实现模型版本的持续演进。
        [2]. 可编排的检索策略：支持通过声明式API配置检索算子链，包含【查询意图识别】→【查询改写】→【多路召回(BM25+Vector)】→【重排序(Rerank)】→【上下文压缩】。支持按文档类型（如法条用BM25优先，研报用向量优先）定制召回策略。
        [3]. 查询意图路由：在检索前置阶段部署意图分类器，将用户查询分为“概览”、“细节”、“对比”等类型，并据此动态选择摘要库或切片库作为优先检索目标。

    4. 契约化摘要与元数据管理
        [1]. 结构化摘要生成：强制LLM按照为各文档类型预定义的Pydantic Schema生成结构化摘要（如财报摘要必须包含“财务摘要表”和“风险因素”），存入元数据库。
        [2]. 摘要质量评估器：自动评估生成摘要的信息熵和关键实体覆盖率，评分低于阈值（默认0.7）则触发二次生成或标记人工审核。
        [3]. 精细化溯源与冲突检测：所有切片和摘要保留完整元数据（版本、置信度、时间戳）。最终输出时，自动检测引文间的逻辑冲突，并在证据包中标记争议点。

    5. 自我进化的战略档案库（长期记忆）
        [1]. 结构化偏差存储：除存储历史SP/BP数据外，重点结构化存储“关键假设变量”、“决策依据”、“实际执行结果”及其差值。
        [2]. 修正即代码：将用户对规划结果的每一次修正操作（参数调整、权重修改）转化为结构化事件日志，存入档案库。定期将这些日志**蒸馏**为可执行的规则或微调数据集，用于更新“偏差预测器”或形成类似Claude Code的 `STRATEGY.md` 自进化规则文件。
        [3]. 版本化规则管理：所有由修正固化的规则纳入Git LFS管理，支持版本控制、A/B测试与一键回滚。

    6. 主动感知与偏差预警
        [1]. 数据源主动监听：系统周期性（可配置）扫描已挂载的数据源和外部行业数据库，自动检测新版本报告、财报或政策更新。
        [2]. 战略偏差实时计算：实时比对最新经营复盘数据与战略档案库中的规划值，当偏差超过预设阈值（默认10%）时，自动生成包含根因分析的《偏差预警报告》。
        [3]. 信息缺口主动响应：当Self-RAG机制发现检索内容不足以支撑高置信度结论时，系统主动生成《信息缺口报告》，并基于缺口内容向用户推荐或请求补充相关数据。

    7. 全链路可观测性与漂移检测
        [1]. 算子级监控：监控粒度细化至每个检索算子（如Query Rewriter成功率、Rerank排序质量）和摘要生成算子的性能指标。
        [2]. 主动漂移检测：采用CUSUM等算法实时监测关键指标（如检索准确率、摘要相关性）的连续下降趋势，当检测到漂移时自动触发根因分析并告警。
        [3]. 红蓝对抗评估：构建覆盖复杂战略场景的对抗性评估集，定期进行新旧模型版本对比测试，主动发现能力退化点。
}

D2
{
一、数据处理：系统需具备将多源、异构的企业内外部数据，转化为支持高精度检索、推理与演进的战略级知识资产的能力。数据处理流水线遵循“规格驱动、本地优先、知识闭环”的设计原则。

    1. 多模态与多源数据接入
        [1]. 支持非结构化文档格式：pdf, txt, doc, docx, ppt, pptx, jpeg, png, gif（集成OCR 2.0，优化扫描件与图表提取）。
        [2]. 支持结构化数据源：xls, xlsx, csv，以及直连企业数据库（SQL/NoSQL）和数据湖（支持配置数据同步合约）。
        [3]. 支持半结构化数据：市场/咨询报告的JSON/XML格式导入。

    2. 知识资产管理
        [1]. 实施数据合约（Data Contract）：每个数据源入库前必须定义其Schema、质量SLA、更新频率、安全等级和有效期，不合规数据拒绝入库。
        [2]. 多租户与权限矩阵：基于RBAC/ABAC实现数据隔离，权限细化至行、列、文档片段。所有访问和操作记录不可篡改的审计日志（WORM存储）[citation:1]。
        [3]. 全生命周期管理：支持知识资产的注册、版本控制、归档与销毁，操作记录纳入审计。

    3. 可插拔的嵌入流水线与自动化评估
        [1]. 抽象嵌入模型接口，支持热插拔与A/B测试：默认支持本地bge-m3，并可无缝切换至更新的多模态模型或通过知识蒸馏训练的企业专属轻量模型[citation:2]。
        [2]. 流水线内置自动化评估：定期在验证集上计算检索命中率、MRR，检测性能漂移并自动报警。

    4. 符合MCP协议的知识检索技能 [citation:3][citation:10]
        [1]. 将RAG能力封装为符合MCP标准的企业级技能，供各Agent通过标准化接口调用。
        [2]. 智能查询路由：集成查询理解模块，自动选择关键词搜索（ES）、向量检索或混合检索策略，并支持基于业务上下文的检索结果重排（Rerank）[citation:8]。
        [3]. 支持元数据过滤、单文档/批量索引、语义/关键词混合查询，并提供检索性能统计信息。

    5. 规格驱动的结构化摘要生成
        [1]. 定义战略文档摘要Schema（如：{核心观点，关键假设变量列表，决策依据，时间范围，文档类型，置信度}），强制LLM按Schema输出结构化摘要[citation:7][citation:9]。
        [2]. 支持选择摘要生成引擎：默认高性能本地模型，高敏感文档强制本地处理；可选调用更强大的云端API进行辅助，但需经脱敏网关。
        [3]. 建立摘要-切片一致性校验机制，文档更新或发生语义漂移时，自动触发重新摘要。

    6. 精细化多模态溯源 [citation:1]
        [1]. 溯源颗粒度细化：支持精确引用至PDF页面区域、表格中特定单元格、PPT中的单页、图表中的数据点。
        [2]. 结构化证据封装：每个检索结果必须附带包含来源ID、具体位置、可信度评分、时效性标签的证据元数据。

    7. 动态阈值与主动知识缺口分析
        [1]. 基于查询复杂度与检索结果权威性，动态计算相关性阈值，替代固定阈值。
        [2]. 当检索质量低于动态阈值时，系统自动生成“知识缺口报告”，建议补充数据类型或信息来源，供后续行动参考。

    8. 构建战略知识图谱与长效记忆演化引擎（原战略档案库升级） [citation:2][citation:3]
        [1]. 将历史SP/BP的关键假设变量、决策依据、执行结果差值，以及用户修正日志，清洗并结构化存储，形成战略核心知识文档库。
        [2]. 基于上述知识库，利用“多教师蒸馏”技术，定期训练/微调一个专用于战略决策支持的“轻量级首席战略官辅助模型”，并将显性知识以知识图谱形式存储，实现系统长效记忆的动态演进。
        [3]. 将高频修正模式提炼为业务规则，纳入Planner的规划模板中，实现“从错误中学习”。

    9. 实时数据质量门禁 [citation:7][citation:8]
        [1]. 格式验证：严格校验上传文件格式与完整性。
        [2]. 内容校验：通过Pydantic V2契约测试，确保数据内容符合预定义的Schema。
        [3]. 时效性验证：根据文档类型（如年报、季报、新闻）和内容，自动评估其有效期限，过期数据标记降级。
        [4]. 重复检测：使用SIMHash等技术在文档级和切片级进行去重。

    10. “业务-技术”双维度可观测性 [citation:1][citation:9]
        [1]. 技术维度：持续监控RAG检索准确率、嵌入模型漂移（KS检验）、摘要生成质量。
        [2]. 业务维度：关联监控由数据质量引发的下游任务修正率、Agent一致性警告次数。建立数据质量根因分析链路，当业务指标异常时，能快速追溯至具体的数据处理环节。
}

D3
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据。数据处理层作为企业战略的长期记忆与知识基础，需具备多模态感知、混合检索、自省修正与动态演进能力。

    1.  **多模态数据源接入**：
        [1]. 支持非结构化文档格式：pdf, txt, doc, docx, ppt, pptx, jpeg, png, gif, mp3, wav, mp4, mov。
        [2]. 支持半/结构化数据源：xls, xlsx, csv, json, 以及通过连接器接入SQL/NoSQL数据库、数据仓库和API数据流。
        [3]. 支持文档/数据源的完整生命周期管理（上传、更新、删除、分类、统计），并具备变更监听与自动同步功能，当数据源更新时，自动触发向量库与知识图谱的索引重建。

    2.  **混合检索与GraphRAG增强的知识库**：
        [1]. 混合检索引擎：集成向量检索（使用本地bge-m3或更新型embedding模型）与全文检索（如Elasticsearch），并引入重排序模型对初步召回结果进行精排。
        [2]. 企业战略知识图谱：从文档中自动抽取核心实体（如公司、竞争对手、技术、产品）及其关系，构建GraphRAG知识图谱，支持“向量检索+图谱遍历”的混合查询。
        [3]. 高级检索策略：
            a. 父文档检索：采用“检索小块、返回大块”策略，在保证精度的同时提供完整上下文。
            b. PageIndex检索：对复杂战略问题，支持LLM驱动的、模拟人类阅读行为的目录树式搜索。
            c. 自适应路由：集成查询路由器，根据问题复杂度（事实性、总结性、推理性）自动选择最优检索路径（LLM直接回答、简单RAG、多步Agentic RAG）。

    3.  **契约驱动的知识结构化**：
        [1]. 结构化摘要生成：使用多模态模型（优先本地部署）生成文档的结构化摘要。摘要Schema由Pydantic V2严格定义，至少包含：文档主旨、核心假设、关键指标、时间范围、关联实体列表。
        [2]. 多模态数据解析：对PDF、图片中的图表进行专门的解析和文本化描述，形成“图表摘要”存入元数据库。
        [3]. 原子化溯源信息：在切片和索引过程中，保留并关联原子级引用信息（来源文档、版本号、页号、段落/行号），为结构化证据包提供基础。

    4.  **自省式RAG与自适应处理**：
        [1]. Self-RAG评估：在检索和生成环节，模型需生成反思令牌，自我评估检索内容的相关性与生成内容的真实性。
        [2]. Corrective-RAG修正：若评估发现检索质量低下（如相关性低于动态阈值），自动触发修正流程，包括查询词改写、调用备选数据源（如网络搜索、知识图谱推理）。
        [3]. “数据不足”声明：经过修正流程后仍无法找到可靠信息时，必须明确向用户声明“基于现有数据无法生成可靠答案”，严禁强行生成。

    5.  **动态演进的战略档案库（长期记忆）**：
        [1]. 结构化存储：除文档向量外，战略档案库需以知识图谱形式结构化存储历年SP/BP的关键假设变量、决策依据、内外部环境因子、实际执行结果及其差值。
        [2]. 反馈闭环与知识固化：将用户修正日志、Checkpoint人工介入、偏差分析结果作为反馈。这些反馈用于：
            a. 动态更新少样本提示词库。
            b. 生成高质量的微调数据，用于定期微调领域专家模型。
            c. 修正和丰富战略知识图谱中的实体关系与属性。
        [3]. 知识演进与版本管理：定期对战略档案库进行知识蒸馏与去重，提炼核心范式。对知识库的每一次重要变更进行版本标记，支持按时间点或版本号进行回滚。

    6.  **多层次数据质量门禁**：
        [1]. 基础验证：格式正确性校验、完整性检查。
        [2]. 高级验证：
            a. 时效性验证：标记超过特定阈值（如12个月）的财报数据或（如24个月）的市场报告为“数据陈旧”。
            b. 来源权威性打分：基于预定义的可信数据源列表，对文档进行权威性评分。
            c. 语义去重：结合SimHash和基于向量的语义相似度检测，识别并合并/标记重复或高度相似文档。
            d. 安全合规过滤：内置敏感信息（PII、商业机密）识别与脱敏模块，防止数据泄露。

    7.  **全链路性能监控与可观测性**：
        [1]. 多维度指标体系：建立涵盖数据质量（新鲜度、完整性）、检索性能（命中率、MRR、NDCG）、成本（存储、计算）的健康度仪表盘。
        [2]. 性能漂移检测：结合KS检验与CUSUM算法，定期评估检索准确率与生成质量的分布偏移和连续性能下降。
        [3]. 自动化根因分析：当关键指标发生漂移时，系统自动关联日志、追踪调用链、比对知识库版本，生成初步的根因分析报告。
}

D4
{
一、数据处理：系统需构建一个安全、可演进、高性能的企业级知识基座。核心目标是处理历史战略规划、业务计划、企业年报、市场报告、咨询报告、财务报表及内部业务数据，通过多策略数据流水线，将非结构化与半结构化数据转化为模型可理解、可追溯、可审计的结构化知识，并形成动态演进的战略档案库。

    1. 多模态文档解析与内容提取
        [1]. 支持多模态文档格式：pdf、txt、doc、docx、ppt、pptx、xls、xlsx、csv、jpeg、png、gif。
        [2]. 定义精细化内容提取策略：
            - PDF/图片：支持OCR并保留阅读顺序，提取段落、表格、图表标题，定位至页码及坐标区域。
            - Office文档：提取正文、备注、批注、单元格公式、幻灯片讲义，定位至工作表/页及具体单元格/文本框。
            - 富文本：保留Markdown等结构化标记，便于语义解析。
        [3]. 内容提取后统一封装为包含“原始内容”与“结构元数据”（坐标、层级、类型）的标准文档对象。

    2. 企业级文档生命周期管理与安全隔离
        [1]. 支持完整的文档操作功能：上传、更新、删除、分类管理、统计信息。
        [2]. 强制实施多租户数据隔离：文档从上传起即注入租户ID、数据主权域（如EU, US, CN）标签。所有操作与检索必须在租户权限和数据主权约束下进行，天然杜绝跨租户数据泄露[citation:8]。
        [3]. 文档版本管理：支持关键文档的版本控制，记录历史变更，便于回溯与审计。

    3. 可插拔嵌入框架与向量化
        [1]. 默认集成高性能本地嵌入模型（如bge-m3），确保核心数据不出域。
        [2]. 构建可插拔的嵌入模型管理框架：支持通过配置文件或遵循MCP协议动态切换/升级嵌入模型（包括云端模型），以适应未来技术演进。
        [3]. 实施嵌入模型性能监控，定期评估其在企业语料上的检索准确率与响应延迟。

    4. 高级RAG索引构建（支持父文档检索策略）
        [1]. 构建多知识库体系：支持为不同租户、不同项目、不同合规等级的数据创建独立的知识库，实现物理或逻辑隔离。
        [2]. 强制实施父文档检索策略：
            - 创建“子块”（chunk）：用于语义匹配，体积较小（如256-512 tokens）。
            - 保留“父块”（parent chunk）：提供完整上下文，体积较大（如1024-2048 tokens）。
            - 在元数据中建立父子块的双向索引。
        [3]. 元数据索引：为文档和切片注入丰富的元数据（文档ID、标题、作者、日期、类型、摘要、租户、主权域、文档坐标等），支持结构化过滤。

    5. 智能化摘要生成与质量评估
        [1]. 摘要生成采用“本地模型优先，云端模型辅助”的双轨制。常规文档由本地微调模型生成核心摘要，复杂文档或经管理员授权可调用网络大模型提升质量。
        [2]. 所有生成的摘要必须经过摘要质量评估器校验，评估维度包括关键实体覆盖率、信息熵、摘要-原文一致性评分。低于阈值（默认0.7）的摘要自动触发重生成或人工标记[citation:1]。
        [3]. 摘要作为核心元数据，与父块或文档主体关联存储，用于分层检索。

    6. 多级过滤与联邦检索策略
        [1]. 检索时实施三级过滤机制：
            - 第一级（元数据粗筛）：基于用户身份、租户权限、时间范围、文档类型等元数据，锁定可检索的知识库集合。
            - 第二级（摘要/实体检索）：在候选知识库内，检索文档摘要或关键实体，初步定位高价值文档。
            - 第三级（父文档检索）：在锁定文档内执行精细的语义检索，返回最相关的“子块”及其关联的“父块”。
        [2]. 支持联邦搜索：当用户权限涉及多个知识库时，系统自动执行跨库并发检索，并对结果进行归一化排序后返回[citation:8]。

    7. 精细化溯源与定位索引
        [1]. 所有索引切片必须包含精细化定位信息。引文元数据至少包括：文档ID、切片ID、文档内坐标（PDF页码/边界框、Excel工作表/单元格范围、PPT幻灯片编号/文本框索引）。
        [2]. 系统输出引用时，必须提供足以让前端高亮定位到原文具体位置的坐标数据。

    8. 多维度数据质量评估与自省（Self-RAG+）
        [1]. 检索结果后处理阶段，启动多维度数据充足性与质量评估器，综合判断：
            - 语义相关性得分（如>0.6）。
            - 数据时效性得分（根据文档时间戳与查询时间计算衰减）。
            - 事实一致性得分（通过多文档交叉验证或领域事实库校验）。
        [2]. 若综合评分低于动态阈值，则标记为“数据不足或质量存疑”，阻止模型强行生成，并返回评估明细供用户或Agent决策。

    9. 构建动态演进的战略档案库（长期记忆）
        [1]. 战略档案库不仅存储原始文档与向量，还结构化存储历史SP/BP的关键假设变量、决策依据、执行结果差值。
        [2]. 建立用户修正闭环：将用户对系统输出的所有修正操作记录为结构化的修正事务日志。
        [3]. 定期（如每季度）启动知识蒸馏任务，利用高级模型对累积的修正事务进行去重、泛化与冲突解决，将高频修正模式提炼为少样本提示词模板或业务规则，反向注入战略档案库，供未来规划任务使用[citation:2]。
        [4]. 知识蒸馏过程需由AUD AGENT或人工专家审计，确保知识库的纯净与演进方向的正确性。

    10. 闭环数据预处理流水线
        [1]. 建立数据预处理流水线，强制包含格式验证、内容完整性检查、SIMHash重复检测环节。
        [2]. 实现闭环质量控制：检测出的问题不仅记录日志，还需反馈至系统。例如，高频解析失败格式自动生成工单提示更新解析器；重复文档信息存入元数据库，优化后续检索展示。

    11. RAG系统性能监控与漂移检测
        [1]. 构建自动化评估Pipeline，利用Strat-Bench中预定义的检索与问答测试集，定期（如每周）评估RAG系统的检索准确率、生成质量、端到端延迟。
        [2]. 引入统计漂移检测算法（如KS检验），监控检索结果分布的长期变化[citation:2]。
        [3]. 建立可视化监控仪表盘，实时展示核心指标（平均相关性得分、检索耗时、无结果率等）。当指标发生显著漂移时，自动触发告警，并建议管理员进行模型微调或数据重索引。
}

D5
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据

    1. 多模态文档解析层
        [1]. 支持文档格式：pdf、txt、doc、docx、ppt、pptx、xls、xlsx、csv、jpeg、png、gif、mp3、mp4（音视频会议记录）
        [2]. 多模态智能解析：图片自动触发OCR+视觉语言模型双通道解析；表格提取结构化数据存入SQLite缓存层；音视频调用语音识别+摘要模型生成文字稿
        [3]. 统一输出Schema：所有文档解析后输出标准化JSON，包含内容块列表（类型text/table/image）、元数据（来源/页码/时间戳）、置信度评分

    2. 文档生命周期管理
        [1]. 文档操作：上传、更新、删除、分类管理、统计信息、版本控制
        [2]. 生命周期钩子：上传触发预处理流水线；更新触发向量库增量索引+元数据同步；删除触发级联清理（向量+缓存+摘要）
        [3]. 血缘追踪：记录每个文档片段的来源文件、处理历史、依赖关系，支持从结论追溯至原始文档的全路径

    3. 混合检索架构
        [1]. 多路召回引擎：BM25稀疏检索（精确匹配） + bge-m3密集检索（语义匹配） + ColBERT多向量检索（细粒度交互）
        [2]. 自适应路由：查询意图分类器（事实型/分析型/探索型）动态选择检索策略与深度，简单查询单路召回，复杂查询多路融合
        [3]. 父文档检索策略：用小块（256 tokens）匹配语义实现精准定位，用大块（1024 tokens或全文）提供上下文，检索噪声降低30%以上[citation:9]

    4. 智能索引与存储
        [1]. 混合索引结构：向量索引（HNSW）+ 关键词倒排索引 + 知识图谱索引（实体关系显式建模）
        [2]. 层次化存储：热数据（3个月内高频访问）存内存/SSD，温数据（历史战略档案）存对象存储，冷数据（超过5年）存归档存储
        [3]. 增量更新机制：文档变更时仅重新计算变更部分的向量与摘要，支持分钟级增量同步

    5. 结构化摘要生成
        [1]. 摘要Schema契约（Pydantic V2）：文档类型/核心观点/关键实体/时间范围/置信度/数据质量评分/适用场景标签
        [2]. 摘要质量门禁：基于信息熵+关键实体覆盖率的摘要质量评估器，低于阈值（默认0.75）自动触发重生成或人工标注
        [3]. 摘要-切片映射索引：每个切片存储父文档ID+摘要哈希+切片类型，检索时先匹配摘要锁定文档范围，再执行切片精准检索

    6. 精细化引文索引
        [1]. 精确位置存储：每个知识片段记录源文档的页码、段落号、行号、bbox坐标（PDF/图片）、表格单元格坐标
        [2]. 可交互溯源：前端支持从结论反向高亮源文档对应区域，点击跳转至文档阅读器对应位置
        [3]. 引文置信度：每个引用附带相关性得分和时效性标签，生成内容时优先采用高置信度引用

    7. 自适应检索增强生成
        [1]. 动态阈值机制：根据查询类型（战略规划/日常分析/事实核查）和领域风险等级动态调整相关性阈值（0.4-0.8可配置）
        [2]. 多源补充协议：相关性低于阈值时依次触发——知识图谱实体检索→同义词扩展查询→可信外部API（如财经数据库）→人工标注请求
        [3]. 缺失维度反馈：标记数据不足时，明确输出缺失的信息维度，指导后续数据采集

    8. 战略档案库（长期记忆模块）
        [1]. 结构化存储Schema：
            - 基础信息：SP/BP年度、版本号、创建时间、责任AGENT
            - 假设变量集：宏观经济假设/市场增长率假设/竞争假设/技术路线假设
            - 决策依据链：引用文档切片ID列表、工具调用轨迹、中间计算结果
            - 执行结果：实际经营数据、偏差分析报告、根因分类
            - 差值向量：计划vs实际的量化差异（数值型/类别型）
            - 修正轨迹：用户修正日志、修正前后对比、影响范围评估
        [2]. 知识蒸馏机制：每季度执行聚类分析，将相似修正日志转化为少样本提示词模板，存入战略知识库
        [3]. 动态遗忘策略：基于访问频次和时效性计算记忆权重，低频知识降级存储，高频知识自动强化索引

    9. 数据预处理质量门禁
        [1]. 质量检查流水线：
            - 格式验证：文件完整性、编码格式、损坏检测
            - 内容完整性检查：页数校验、关键章节覆盖率、表格解析成功率
            - 重复检测：SimHash粗筛 + Embedding相似度精排，阈值0.85以上标记重复
            - 敏感信息扫描：PII检测、商业机密关键词匹配
        [2]. 不合格品隔离区：质量评分低于0.6的文档进入隔离区，标注失败原因，支持人工修复后重入流水线
        [3]. 数据质量评分卡：每个文档生成质量评分（0-100），存入元数据供下游AGENT决策参考

    10. 全链路可观测性与漂移检测
        [1]. 监控指标体系：
            - 数据源健康度：采集成功率/解析失败率/延迟
            - 检索质量：召回率@5、MRR、NDCG@10、检索延迟P95
            - 摘要质量：信息熵/实体覆盖率/人工评分相关性
            - 系统资源：向量库查询QPS/CPU/内存/存储增长率
        [2]. 漂移检测双算法：
            - KS检验：对比月度分布检测检索质量偏移
            - CUSUM时序检测：识别性能指标的连续下降趋势
        [3]. 智能根因分析：漂移触发时自动关联数据源变更、模型版本升级、预处理逻辑调整，输出根因报告

    11. 数据治理与合规
        [1]. 数据分类分级：按保密等级（公开/内部/机密/绝密）实施访问控制，敏感数据自动脱敏
        [2]. 合规审计追踪：所有数据处理操作记录至WORM存储，满足SOX/ISO27001/欧盟AI法案要求[citation:4]
        [3]. 数据生命周期策略：按数据类型设定保留期限（战略档案永久保存，原始文档按合规要求销毁），过期自动归档或删除
}


- 多模态解析与原子化溯源
- 混合检索架构与图谱增强
- 强制契约化与数据质量门禁
- Agentic RAG与自适应纠偏
- 知识演进与反馈闭环
- 全链路可观测性与漂移检测
- 本地优先与数据主权隔离
- 企业级合规与审计追踪
- 互操作性与生态扩展
- 成本工程与性能优化
- 开发者体验与运维支持
- 创新前瞻性
- 安全与鲁棒性
- 性能与监控
- 企业级适用性
- 工程可实施性


## 20选5

你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要结合[企业战略规划管理智库系统需求特性列表]系统需求，从下面20个需求清单中选择最优的[数据处理]需求特性。要求： 
1、结合系统需求为每个列表逐个评分（10分制），最后求总分
2、评分维度：
   - 多模态解析与原子化溯源
   - 混合检索架构与图谱增强
   - 强制契约化与数据质量门禁
   - Agentic RAG与自适应纠偏
   - 知识演进与反馈闭环
   - 全链路可观测性与漂移检测
   - 本地优先与数据主权隔离
   - 企业级合规与审计追踪
   - 互操作性与生态扩展
   - 成本工程与性能优化
   - 开发者体验与运维支持
   - 创新前瞻性
   - 安全与鲁棒性
   - 性能与监控
   - 企业级适用性
   - 工程可实施性
3、严格从[客观视角]进行评估，以表格形式呈现

---------------------

企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：支持基于网络LLM和先进Agent技术的智能分析工具集，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理（共计17个）。
    1.智能工具定义
        [1].建立工具标准：每个工具有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。工具初始化必须加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
        [2].构建中央技能仓库：实现工具的统一注册、版本控制、灰度发布与回滚机制。所有入库工具必须通过Pydantic V2契约测试，后续迭代需通过回归测试集。支持预定义模板与远期基于代码生成的技能动态扩展。
        [3].支持工具链编排与依赖调度：支持定义工具链有向无环图，实现上游工具输出结构化流转为下游输入。内置任务调度器，根据依赖关系自动并行执行无依赖子任务。
    2.工具箱标准工作流
        [1].环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具价值观模板（SOUL.md），实例化会话级沙箱与混合检索记忆容器。
        [2].感知与摘要：读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
        [3].规划：Planner依据用户自然语言指令及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、匹配工具映射、数据依赖边。
        [4].执行与验证：Executor按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe→writeback原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
        [5].证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
        [6].反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
        [7].成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
        [8].演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。
    3.数值计算与沙箱执行
        [1].强制代码执行隔离：严禁LLM直接进行数值计算、统计回归或现金流折现。所有量化分析强制通过两条路径执行：一是生成Python代码在沙箱执行；二是针对战略运筹问题（如线性规划），由LLM构建数学模型调用CVXPY、OR-Tools等优化器求解。
        [2].构建持久化计算沙箱：为每个任务会话实例化持久化Jupyter Kernel，支持跨工具、跨步骤的变量传递（如宏观经济变量在不同模型间共享），实现分析上下文连贯。任务结束后自动销毁内核并回收资源。
        [3].支持Validation Feedback闭环：自动捕获代码执行STDERR，检索错误案例库辅助LLM生成修复版本。设定最大重试次数（默认3次），失败则标记不可行并记录至演进日志。
    4.Schema强制与一致性校验
        [1].实施契约化输出：所有工具输入/输出严格遵循Pydantic V2定义的Data Model，利用Instructor Patch强制LLM输出符合Schema的结构化JSON，严禁输出自由文本。
        [2].建立一致性校验仲裁器：在多工具协作中，通过内置规则引擎及财务常识库检测逻辑冲突（如利润率与成本矛盾）。输出冲突检测报告，供用户或高级AGENT裁决。
        [3].自动化契约测试：在CI/CD流水线集成Schema契约测试，防止模型漂移导致结构崩溃。契约测试失败的任务强制终止。
    5.可解释性与决策溯源
        [1].输出结构化推理证据包：每次分析必须打包输出全量元数据，包括输入摘要哈希、Planner任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片ID）。
        [2].构建交互式溯源树：支持前端从结论反向逐层展开证据包，展示“数据来源→计算逻辑→中间结论→最终结论”的完整推理链。
        [3].支持红蓝辩论机制：针对高不确定性议题自动启动“红蓝对抗”，实例化激进派与保守派子智能体进行结构化辩论，输出包含共识与分歧区域的风险全景视图。
    6.提示词工程与演进
        [1].Prompt Optimizer：基于DSPy理念，建立“修正日志-样本转换”闭环，将用户修正转化为Few-Shot样本，自动迭代提示词参数以适配企业战略风格。
        [2].实施受控自动演进：仅在SP/BP Checkpoint完成态且满足一致性阈值时触发演进。必须采用多目标优化（结构、一致性、成本），严禁单一指标优化。
        [3].确保方法论不变性：演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
        [4].支持版本管理与回滚：所有Prompt纳入Git LFS管理，与系统版本绑定。支持一键回滚至历史稳定版本。
    7.用户反馈与闭环演进
        [1].支持Checkpoint人工介入：在关键决策节点或低置信度环节自动暂停，请求用户验证中间假设。支持用户修改参数或调整权重，系统记录修正前后的“专家修正轨迹”。
        [2].建立修正轨迹库与逆向传播：将用户修正快照匿名化存储，定期分析差异，推断输入权重或任务分解策略的优化方向。
        [3].实施用户反馈协议：将用户反馈转化为结构化修正日志，强制触发工具体“反思阶段”（ReflAct），并回传至技能演进日志。
    8.安全与防御机制
        [1].实施沙箱网络隔离：代码执行环境默认断网，仅允许通过白名单网关访问预设的可信财经API，严禁通用网页爬虫。
        [2].建立提示词注入防御：输入数据前置执行清洗与注入检测。将“注入防御测试”纳入基准测试集，定期验证工具体的抗干扰能力。
        [3].建立工具幻觉黑名单：自动监控工具体输出，若连续3次生成与输入矛盾的结论，自动锁定该工具体并报警。
    9.领域专业知识嵌入
        [1].内置领域事实库：以JSON Schema存储经典管理理论的强制约束（如SWOT内部/外部属性分类），确保分析逻辑符合理论框架。
        [2].部署领域验证器：工具体输出前必须经过验证器检查，确保结论不违背基础商业逻辑与领域常识。
    10.服务契约与集成
        [1].独立微服务封装：工具箱作为独立微服务对外暴露OpenAPI 3.1标准接口，输出分析结果、证据包与成本审计信息三元组。
        [2].支持Agent模块集成：与CEO、CFO等核心AGENT通过服务调用无缝集成，支持水平扩展与分布式Trace追踪。
    11.测试与质量保证
        [1].构建基准测试集（Strat-Bench）：建立包含20+专家标注场景的战略分析专用测试集，作为版本迭代的准入测试标准。
        [2].支持A/B测试框架：支持多版本策略并行运行，基于任务完成率、计算精度与成本指标择优晋级。
        [3].部署健康度仪表盘：实时监控工具成功率与修正率，修正率超标（默认15%）自动触发Skill Review。
    12.性能监控与漂移检测
        [1].建立性能漂移检测：定期评估检索准确率与生成质量，对比历史基线检测性能下降。
        [2].支持可视化日志：所有评估与运行结果纳入系统行为日志并可视化展示。
    13.成本熔断与契约测试质量门禁
        [1].CI/CD契约测试门禁：所有工具输出必须通过严格Schema校验，非兼容变更触发构建失败。运行时连续三次校验失败触发工具熔断。
        [2].实施三级成本熔断：支持任务级（单次Token/成本超限）、会话级（日配额超限）与重试级（连续代码执行失败）熔断机制，确保系统运行成本可控。

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1. 输入处理与全景构建
        [1]. 输入系统预处理的结构化JSON数据，AGENT首先调用摘要质量评估器（基于信息熵与关键实体覆盖率），若摘要质量评分低于阈值（默认0.7）则自动触发二次检索补充缺失维度，确保全景视图的完整性。
        [2]. 构建动态全景视图：宏观层（战略指标趋势/异常波动/关键假设变量）与微观层（文档切片引用索引）双向关联，支持细节挖掘过程中发现与全景视图严重矛盾时自动触发视图重构，重构过程需记录变更轨迹并提交AUD AGENT审核。
        [3]. 实施上下文窗口动态管理：基于任务复杂度自适应分配Token预算，超限时触发上下文压缩器（基于关键实体抽取与HyDE算法），压缩率目标≥70%，确保关键信息不丢失。
    2. 任务分解与协作架构
        [1]. 建立任务IR（中间表示）标准：每个任务包含任务ID、类型、输入/输出Schema、依赖任务列表、优先级、执行状态、超时策略。所有协作任务必须基于有向无环图（DAG）构建流转拓扑，确保多体协作具备确定性的状态机流转特征。
        [2]. 采用分层协作架构：CEO AGENT作为指挥官负责目标解析与全局任务分解，各专业Agent并行执行子任务，调度器负责资源分配与执行监控。自动生成协作依赖图，基于BLM/BEM阶段定义标准依赖关系，支持动态调整与优化，可视化展示任务间数据流与控制流。
        [3]. 支持柔性视角隔离：定义核心视角（必须遵守）和扩展视角（可协商），对跨领域问题组建临时联合分析组，建立视角冲突解决协议。视角隔离通过Prompt隔离、工具隔离、数据隔离三重硬约束实现。
    3. AGENT确定性工作流
        [1]. 基于状态图的工作流引擎：采用StateGraph机制定义显式状态模型，包含session_id、current_phase、agent_role、working_memory、long_term_memory_ref、tools_used、confidence_score、cost_accumulated等字段。工作流循环定义为：[感知]环境初始化与上下文加载→[规划]生成任务执行DAG→[执行]按拓扑顺序调度子任务→[深度思考]（可选）在关键决策节点并行生成多条思维链推演路径→[验证]结构化检查输出→[反思]错误分析与修正→[闭环]Early Stopping机制（置信度达标且目标达成时提前终止）。
        [2]. 深度思考与多路径推演：在SP/BP所有核心分叉点（如商业模式选择、核心竞争力定义），默认调用支持深层推理的模型，并行生成多条思维链推演路径，通过内部验证打分选出最优解，极大提升战略输出的逻辑厚度。
        [3]. 中断安全与状态持久化：任意节点支持用户中断，自动保存StateSnapshot至Redis Cluster（三节点哨兵模式），恢复时加载完整上下文与工具状态，支持从任意历史快照恢复执行（Time-travel能力分步实现）。
    4. 用户反馈闭环
        [1]. 建立反馈标准化协议：将用户反馈转化为结构化修正日志（包含修正类型enum[参数/权重/约束]、修正前/后值、置信度变化、影响因子、用户角色、时间戳）。
        [2]. 反馈分级处理与即时响应：①专家修正（置信度>0.9）直接触发Prompt Optimizer；②普通反馈进入修正轨迹库；③噪声反馈自动过滤。设置专用反馈通道，承诺24小时内处理并回复，紧急反馈设置加急处理流程。
        [3]. 修正影响传播分析：评估修正对下游任务的影响范围，高影响修正（>3个下游任务）强制生成Impact Report并通知相关AGENT。支持从任意Checkpoint恢复执行后修改中间状态变量（Time-travel第一阶段），并从修改点继续执行。
    5. 提示词工程与演进
        [1]. 基于DSPy理念构建Prompt Optimizer：输入为用户反馈、AUD一致性报告、SP/BP Checkpoint人工反馈，输出优化后System Prompt与Few-Shot样本集。优化目标采用Pareto多目标权衡（结构完整性40% + 逻辑一致性35% + 成本效率25%），采用NSGA-II算法求解Pareto前沿。
        [2]. 受控演进机制：自动微调的提示词参数必须进入“待审查版本库”，经人工专家（首席战略官或授权代表）确认后方可推向生产环境。集成MLflow实现Prompt自动版本注册，支持A/B测试（分流比例可配置）与一键回滚（保留最近10个稳定版本）。
        [3]. 提示词契约测试：每次优化后自动运行Strat-Bench基准测试集，通过率<90%自动拒绝上线，并生成差异分析报告。演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
    6. 工具调用与集成
        [1]. 全面遵循MCP（Model Context Protocol）2025规范：工具箱作为独立微服务对外暴露符合MCP标准的能力发现接口，支持工具注册、版本控制、灰度发布与回滚。AGENT通过MCP Registry查询工具能力描述（输入/输出Schema、版本、可靠性评分）。
        [2]. 工具选择优化：基于语义匹配（60%）+历史成功率（40%）计算工具匹配度，匹配度<0.6时触发人工确认或启动多工具并行验证。预定义17种战略工具的最佳组合模板（如SWOT+TOWS自动组合），支持用户自定义模板。
        [3]. 强制契约化调用与沙箱执行：所有工具调用前验证输入Schema，调用后验证输出Schema，连续3次校验失败自动熔断该工具。代码执行在E2B沙盒中（网络隔离+权限最小化），执行结果经Validation Feedback闭环校验，最大重试次数3次。
    7. 仲裁与决策机制
        [1]. CEO AGENT内置裁决状态机，设定最大辩论轮次（默认3+风险等级，上限7轮）。辩论采用三阶段：发散（Temperature=0.8）、收敛（Temperature=0.5）、裁决（Temperature=0.2），强制随轮次增加提交具体修改JSON补丁。
        [2]. 辩论质量评估：每轮计算新信息增益率与重复率，增益率<10%且重复率>50%时强制终止辩论。辩论过程记录各AGENT贡献度（基于Shapley值量化），分歧点生成热力图。
        [3]. 多方案生成与人类一票否决：若未达成一致，生成Plan A（保守参数集）、Plan B（激进参数集）和Plan C（AI融合版），输出参数调整明细。强制暂停60秒提供用户介入窗口，窗口期可视化展示三套方案的对比差异（风险/收益/资源需求）及影响模拟器（支持用户滑动调整关键参数实时预览结果）。超时无用户操作则按CEO决策执行，但所有人工覆盖决策强制记录至战略档案库并标记“人工覆盖”。
    8. 审计与一致性保障
        [1]. AUD AGENT采用“旁路监听+异步阻断”模式，不参与生成。审计范围包括四维一致性检验：①事实一致性（对比原始文档切片）；②逻辑一致性（规则引擎检测内部矛盾）；③数值一致性（沙箱重计算关键指标，误差>5%标记异常）；④时效一致性（引用数据超12个月标记“数据陈旧”）。
        [2]. 分级熔断与根因分析：L1（幻觉概率>0.8）自动触发修正；L2（0.6-0.8）通知相关AGENT重试（最多2次）；L3（<0.6）暂停任务并生成Hallucination Report（包含错误类型分类：数据缺失/逻辑跳跃/数值错误/时效过期），人工介入后从断点恢复。
        [3]. 审计日志强制存储：所有审计发现、纠错过程、重试轨迹全量存储至不可篡改日志（WORM存储），满足SOX/ISO27001审计要求，支持按时间/角色/任务类型多维检索。
    9. 记忆与上下文管理
        [1]. 分层记忆架构：工作记忆（当前会话，有效期24h）、短期记忆（最近项目，有效期30天）、长期记忆（战略知识库，永久存储），各层有独立的存储引擎和检索策略。长期记忆将用户修正日志、决策复盘结果、执行结果差值反向固化为少样本提示词，每季度触发知识蒸馏与去重。
        [2]. 公共黑板机制：基于LangGraph StateSnapshot + Redis Cluster管理状态版本，支持多版本并发控制（MVCC）。关键结论写入黑板时必须附带重要性评分（1-5）、置信度、引用源切片ID、时效性，综合评分>3.5才写入，便于上下文压缩器智能筛选。
        [3]. 时间旅行调试：支持从任意Checkpoint恢复执行，用于问题复现与根因分析。所有状态快照与完整推理轨迹归档至数据湖，可按需解压缩回溯。
    10. 可解释性与溯源
        [1]. 结构化证据包标准：每次分析打包输出全量元数据，包括输入摘要哈希、任务分解图、工具调用轨迹（含版本）、关键中间变量快照、置信度评分及依据、引用源切片ID列表。
        [2]. 交互式溯源树：前端支持从结论节点逐层展开至原始数据，每层显示“数据来源→计算逻辑→中间结论”三元组，每个节点支持查看详细信息。
        [3]. 决策影响分析：量化各输入变量对最终决策的Shapley贡献值，高影响变量（>20%）自动高亮标注。支持反事实推理，输出“如果改变某假设，结论会如何变化”的对比报告和风险热力图。
    11. 主动能力
        [1]. 引入心跳机制：支持周期性自动唤醒（可配置频率），主动检查战略档案库中的待办事项、偏差预警、周期性任务，无需用户提问即可主动沟通。
        [2]. 战略偏差预警：实时监控实际执行数据与SP/BP规划的偏差，偏差超过阈值（默认10%）自动触发预警，生成偏差分析报告并推送相关AGENT。
        [3]. 主动知识更新：当检测到行业报告、市场数据、政策法规更新时，自动触发电请相关AGENT评估影响，重要影响主动推送CEO AGENT。
    12. 性能监控与行为分析
        [1]. 建立三维度指标体系：效率维度（任务完成率、平均时长、工具调用成功率）、质量维度（修正率<10%、幻觉率<5%、一致性得分>0.8）、成本维度（单任务API成本、Token消耗）。构建战略规划专用基准测试集（Strat-Bench）作为版本迭代的准入标准。
        [2]. 漂移检测双算法：KS检验检测输出分布偏移（月度基线对比），CUSUM检测性能指标连续下降（滑动窗口7天）。指标异常时启动自动根因分析，关联日志输出根因报告。
        [3]. 健康度仪表盘：实时可视化展示各AGENT的12项健康度指标，支持下钻分析与预警订阅。三级预警机制：黄牌（指标下降15%触发分析）、红牌（下降30%触发人工review）、停用（下降50%且连续三天，自动回滚至前一稳定版本）。
    13. 安全与防御
        [1]. 纵深防御体系：
            - 第一层（隔离）：三重硬隔离（Prompt隔离、工具隔离、数据隔离），基于角色权限最小化。
            - 第二层（执行）：所有代码在E2B沙盒中执行，严格网络隔离（仅白名单API）。
            - 第三层（检测）：实时监控视角越界（跨角色关键词频率>5%触发熔断）、幻觉累积（连续3次低置信度结论锁定Agent）。
            - 第四层（审计）：所有操作记录至WORM存储。
            - 第五层（熔断）：辩论过热保护、成本熔断。
        [2]. 敏感数据脱敏：审计日志和反馈记录自动脱敏处理（个人可识别信息、商业机密），脱敏策略定期评审更新。
    14. 成本控制
        [1]. 成本预测：任务启动前基于历史相似任务预测Token消耗与API成本，偏差>50%时提示用户确认。
        [2]. 三级熔断机制：
            - 任务级熔断：单次任务Token消耗超预设阈值（基础任务5K tokens，复杂规划50K tokens）立即终止。
            - 会话级熔断：日累计Token消耗超企业配额（默认100万tokens）暂停新任务，已启动任务完成。
            - 系统级熔断：连续3次工具执行失败或单任务成本超预算200%触发系统级告警，需管理员介入。

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察(包括六个子步骤)、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据

-------------------

D1
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化知识，并作为系统的长期记忆动态演进。
    1. 多模态文档智能解析：支持常见文档格式（pdf, docx, pptx, xlsx, jpeg, png, gif, txt, csv）。针对图片/扫描件，强制集成OCR引擎，并调用视觉语言模型生成图片内容的语义描述。针对复杂表格，利用表格结构识别模型解析为机器可读的结构化数据（如JSON），保留行列关系与数值逻辑。
    2. 知识生命周期治理：建立“知识责任者”元数据机制，强制关联上传人/部门。支持文档的细粒度版本控制与变更追踪，文档更新时自动识别变更内容并进行增量重索引。分类管理支持动态聚类建议与人工审核调整。
    3. 混合检索与语义重排序流水线：采用本地嵌入模型（如bge-m3）构建基础向量索引。强制集成语义重排序（Rerank）模型，对多路召回结果（向量+关键词+知识图谱）进行二次精准排序，提升抗“检索噪声”能力。
    4. 父文档检索策略：实施父文档检索，建立“子块-父块”映射。用小粒度块（~256 tokens）匹配语义，用大粒度父块（~1024 tokens）作为完整上下文提供给大模型，确保信息不碎片化。
    5. 结构化摘要生成与质量评估：调用网络大模型API为文档生成结构化摘要（含关键实体、事件、指标）。生成后，强制经过摘要质量评估器（评估事实一致性、实体覆盖率）校验，低于阈值（动态可配，默认0.7）则触发重新生成。
    6. 动态路由检索策略：引入检索路由器，根据查询意图（由小型分类模型判断）动态选择或组合最优检索路径，包括：摘要优先分层检索、全文切片直接检索、精确关键词检索或元数据过滤检索。
    7. 溯源信息强制绑定与传播：检索返回的每个文档切片必须绑定完整的溯源元数据（文档ID、版本号、页码、块ID）。该溯源信息作为系统内“一等公民”随数据流传递至下游所有Agent和分析工具，确保最终输出结论可一键溯源至原文。
    8. 上下文感知的“数据不足”动态阈值：采用Self-RAG机制，但相关性判定阈值根据查询的领域、用户风险偏好和任务类型动态调整。当判定数据不足时，系统自动生成“信息缺口报告”并触发数据采集请求或向用户提问。
    9. 构建动态演进的战略记忆库：
        [1]. 除基础向量库外，构建结构化长期记忆模块，存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值。
        [2]. 将用户修正日志、Checkpoint人工干预记录进行结构化清洗与验证，通过多教师知识蒸馏技术，一方面固化为少样本提示词，另一方面用于微调轻量级“决策适配器”模型或更新领域事实库中的规则。
        [3]. 建立“记忆清洁”机制，定期对长期记忆进行冲突检测、去重归纳（利用更先进的语义去重模型），并将超期数据归档或标记为“低时效”。
    10. 持续数据质量可观测性：建立数据质量监控仪表盘，实时监控数据源健康度（更新频率、空值率、格式错误）。质量控制环节扩展至语义级去重和时效性标记（如“数据陈旧”），并纳入数据可观测性体系。
    11. RAG系统性能监控与自动化回归测试：
        [1]. 引入RAGAs评估框架，建立包含“上下文相关性”、“答案忠实度”、“答案相关性”等指标的多维评估体系。
        [2]. 构建并持续扩充基于战略规划场景的“黄金问答测试集”。在知识库更新或模型升级时，自动触发CI/CD流水线中的RAG回归测试，性能指标（如忠实度）下降超过阈值（默认5%）则阻断发布并生成根因分析报告。
}
D2
{
一、数据处理：系统需具备将多源、异构的企业内外部数据，转化为支持高精度检索、推理与演进的战略级知识资产的能力。数据处理流水线遵循“规格驱动、本地优先、知识闭环”的设计原则。

    1. 多模态与多源数据接入
        [1]. 支持非结构化文档格式：pdf, txt, doc, docx, ppt, pptx, jpeg, png, gif（集成OCR 2.0，优化扫描件与图表提取）。
        [2]. 支持结构化数据源：xls, xlsx, csv，以及直连企业数据库（SQL/NoSQL）和数据湖（支持配置数据同步合约）。
        [3]. 支持半结构化数据：市场/咨询报告的JSON/XML格式导入。

    2. 知识资产管理
        [1]. 实施数据合约（Data Contract）：每个数据源入库前必须定义其Schema、质量SLA、更新频率、安全等级和有效期，不合规数据拒绝入库。
        [2]. 多租户与权限矩阵：基于RBAC/ABAC实现数据隔离，权限细化至行、列、文档片段。所有访问和操作记录不可篡改的审计日志（WORM存储）[citation:1]。
        [3]. 全生命周期管理：支持知识资产的注册、版本控制、归档与销毁，操作记录纳入审计。

    3. 可插拔的嵌入流水线与自动化评估
        [1]. 抽象嵌入模型接口，支持热插拔与A/B测试：默认支持本地bge-m3，并可无缝切换至更新的多模态模型或通过知识蒸馏训练的企业专属轻量模型[citation:2]。
        [2]. 流水线内置自动化评估：定期在验证集上计算检索命中率、MRR，检测性能漂移并自动报警。

    4. 符合MCP协议的知识检索技能 [citation:3][citation:10]
        [1]. 将RAG能力封装为符合MCP标准的企业级技能，供各Agent通过标准化接口调用。
        [2]. 智能查询路由：集成查询理解模块，自动选择关键词搜索（ES）、向量检索或混合检索策略，并支持基于业务上下文的检索结果重排（Rerank）[citation:8]。
        [3]. 支持元数据过滤、单文档/批量索引、语义/关键词混合查询，并提供检索性能统计信息。

    5. 规格驱动的结构化摘要生成
        [1]. 定义战略文档摘要Schema（如：{核心观点，关键假设变量列表，决策依据，时间范围，文档类型，置信度}），强制LLM按Schema输出结构化摘要[citation:7][citation:9]。
        [2]. 支持选择摘要生成引擎：默认高性能本地模型，高敏感文档强制本地处理；可选调用更强大的云端API进行辅助，但需经脱敏网关。
        [3]. 建立摘要-切片一致性校验机制，文档更新或发生语义漂移时，自动触发重新摘要。

    6. 精细化多模态溯源 [citation:1]
        [1]. 溯源颗粒度细化：支持精确引用至PDF页面区域、表格中特定单元格、PPT中的单页、图表中的数据点。
        [2]. 结构化证据封装：每个检索结果必须附带包含来源ID、具体位置、可信度评分、时效性标签的证据元数据。

    7. 动态阈值与主动知识缺口分析
        [1]. 基于查询复杂度与检索结果权威性，动态计算相关性阈值，替代固定阈值。
        [2]. 当检索质量低于动态阈值时，系统自动生成“知识缺口报告”，建议补充数据类型或信息来源，供后续行动参考。

    8. 构建战略知识图谱与长效记忆演化引擎（原战略档案库升级） [citation:2][citation:3]
        [1]. 将历史SP/BP的关键假设变量、决策依据、执行结果差值，以及用户修正日志，清洗并结构化存储，形成战略核心知识文档库。
        [2]. 基于上述知识库，利用“多教师蒸馏”技术，定期训练/微调一个专用于战略决策支持的“轻量级首席战略官辅助模型”，并将显性知识以知识图谱形式存储，实现系统长效记忆的动态演进。
        [3]. 将高频修正模式提炼为业务规则，纳入Planner的规划模板中，实现“从错误中学习”。

    9. 实时数据质量门禁 [citation:7][citation:8]
        [1]. 格式验证：严格校验上传文件格式与完整性。
        [2]. 内容校验：通过Pydantic V2契约测试，确保数据内容符合预定义的Schema。
        [3]. 时效性验证：根据文档类型（如年报、季报、新闻）和内容，自动评估其有效期限，过期数据标记降级。
        [4]. 重复检测：使用SIMHash等技术在文档级和切片级进行去重。

    10. “业务-技术”双维度可观测性 [citation:1][citation:9]
        [1]. 技术维度：持续监控RAG检索准确率、嵌入模型漂移（KS检验）、摘要生成质量。
        [2]. 业务维度：关联监控由数据质量引发的下游任务修正率、Agent一致性警告次数。建立数据质量根因分析链路，当业务指标异常时，能快速追溯至具体的数据处理环节。
}
D3
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据

1. 支持多格式文件处理，分类执行差异化解析策略：
    [1]. 文本类（pdf,txt,doc,docx,ppt,pptx）：执行深度语义解析，提取标题、章节结构、表格、图表标题
    [2]. 表格类（xls,xlsx,csv）：执行行列式结构化提取，保留公式依赖关系与数据透视关系
    [3]. 图像类（jpeg,png,gif）：集成多模态嵌入模型进行图表解析与OCR文本提取，解析结果存入元数据库，图像特征向量单独存储支持图文混合检索

2. 文档全生命周期管理（引入版本控制与质量仪表盘）：
    [1]. 支持文档上传、更新、删除、分类管理，每次更新强制生成新版本并保留历史轨迹（Git-LFS风格）
    [2]. 分类管理需绑定元数据Schema（文档类型：年报/咨询报告/财务报表；时效性：有效期/过期日期；部门归属：战略部/财务部/市场部）
    [3]. 统计信息升级为文档健康度仪表盘，实时展示：文档完整性评分、语义重复率、时效过期预警、各格式占比

3. 嵌入模型策略管理器（告别单一模型锁定）：
    [1]. 支持多嵌入模型注册（bge-m3, text-embedding-3-small, e5-mistral等），根据文档类型与查询场景动态选择最优模型
    [2]. 建立嵌入模型季度基准测试（测试集涵盖中英文年报、咨询报告、技术文档），评估召回率与延迟，支持模型热切换
    [3]. 所有嵌入向量存储时附带模型版本号，支持按版本回溯和跨版本检索

4. 增强型RAG流水线（检索+重排序+质量看板）：
    [1]. 索引管理：支持单文档索引、批量索引、增量索引，索引状态实时可视化
    [2]. 查询处理：集成HyDE（假设性文档嵌入）扩展查询语义，生成多个查询变体提升召回
    [3]. 检索执行：向量检索（Top-K=50）→ 重排序层（Rerank，采用bge-reranker或cohere rerank，取Top-20）→ 动态上下文组装（根据切片相关性得分和Token预算自适应组装）
    [4]. 元数据过滤：支持按时间范围、文档类型、部门归属、质量分级过滤
    [5]. 统计信息升级为检索质量看板：准确率、召回率、mAP、NDCG@10，支持按时间段和查询类型下钻分析

5. 文档摘要生成（自适应策略+质量评估+API熔断）：
    [1]. 自适应摘要策略：短文档（<10页）生成300-500字完整摘要；长文档（>50页）生成章节级摘要（每章100-200字）+全局概要（300字）
    [2]. 摘要质量评估器：计算关键实体覆盖率（与原文对比）和事实一致性得分（通过NLI模型验证），低于阈值（默认0.7）自动触发重生成或加入人工复核队列
    [3]. 网络API调用集成三级熔断：任务级（单次Token超限/超时熔断）、会话级（日累计Token超配额熔断）、连续失败级（连续3次失败锁定工具24h）

6. 三层分层检索策略（摘要→章节→切片）：
    [1]. 第一层：文档摘要检索，定位最相关的3-5篇文档
    [2]. 第二层：章节摘要检索，在锁定文档内定位相关模块（如"财务分析"章节、"市场竞争"章节）
    [3]. 第三层：切片级检索，在锁定模块内检索具体段落，支持跨文档的同主题切片聚合
    [4]. 对跨文档战略议题（如"未来三年技术趋势"），支持多文档联合检索与聚合切片排序

7. 不可变引文索引（确保溯源可信与审计合规）：
    [1]. 每个切片被引用时，同时保存：切片内容快照 + 元数据快照（文档标题/作者/时间/版本） + 版本哈希（SHA-256）
    [2]. 文档更新后，历史引文依然可访问快照版本，前端展示时标注"基于历史版本"并显示版本时间
    [3]. 引文索引附带置信度评分（基于语义相似度与rerank得分，0-1），低于0.5的引文前端置灰提示

8. 自适应Self-RAG机制（动态阈值+数据缺口报告）：
    [1]. 动态阈值策略：根据查询类型（事实型阈值0.7，分析型阈值0.5）和业务场景（常规分析阈值0.6，战略决策阈值0.8）自动调整
    [2]. 若检索得分低于动态阈值，自动执行扩展检索：放宽阈值（-0.1）、扩充查询词（生成2-3个同义变体）、检索外部知识库（需企业授权）
    [3]. 扩展检索仍不足时，生成数据缺口报告：明确缺失的信息维度（如"缺乏2025年Q3竞争对手市场份额数据"）、建议补充的数据来源、影响分析（对哪些战略决策可能产生影响）

9. 动态演进的战略知识库（记忆蒸馏+假设差值存储）：
    [1]. 构建战略记忆蒸馏流水线（季度触发）：
        - 原始修正日志采集 → 修正质量评估（AUD AGENT验证合理性）→ 高频模式挖掘（聚类相似修正场景）→ 少样本提示词生成（每场景3-5样本）→ 知识去重与版本化存储
    [2]. 结构化存储历年SP/BP的关键假设变量、决策依据、实际执行结果：
        - 标准化JSON Schema：{假设ID, 假设描述, 预期值, 实际值, 偏差率, 根因分析标签[市场变化/执行偏差/假设错误], 影响范围, 复盘时间}
        - 支持按时间、业务单元、偏差率范围多维检索
    [3]. 用户修正日志强制存储原始版本+修正版本，修正合理性评分>0.8的进入蒸馏候选池

10. 智能数据预处理流水线（验证+修复+分级）：
     [1]. 格式验证：检测文件完整性、编码格式、PDF文本层是否存在（若无则自动OCR）
     [2]. 语义重复检测：采用嵌入相似度+DBSCAN聚类，阈值0.85以上标记为重复，重复文档存入"归档库"不进入主检索库
     [3]. 质量分级（A/B/C/D四级）：
         - A级：OCR置信度>0.95，结构完整，无乱码
         - B级：OCR置信度0.8-0.95，少量格式问题
         - C级：OCR置信度<0.8，或大量扫描件需人工复核
         - D级：严重损坏，需重新上传
     [4]. 质量修复机制：C级文档自动触发增强OCR（更换引擎+后处理校正）；D级文档标记"不可用"并通知上传者
     [5]. 质量分级结果存入元数据，检索时支持按最低质量阈值过滤（默认仅检索A/B级）

11. 全维度性能监控与自动漂移恢复：
     [1]. 监控指标体系（实时计算+日报周报）：
         - 检索质量：准确率、召回率、NDCG@10、平均检索延迟
         - 生成质量：摘要事实一致性得分、关键实体覆盖率、ROUGE-L
         - 系统性能：TPS、P95延迟、错误率
         - 成本效率：每千次检索成本、每千字摘要生成成本
     [2]. 漂移检测双算法（每日运行）：
         - PSI（群体稳定性指标）：检测检索结果分布与历史基线的偏移，阈值0.2告警
         - CUSUM累积和：检测性能指标连续下降趋势，窗口期7天，连续下降3天告警
     [3]. 自动根因分析与恢复：
         - 检测到漂移后，自动关联时间轴：模型版本变更、数据分布变化、系统配置调整
         - 输出根因分析报告，并推荐恢复操作（如"建议回滚至bge-m3-v2版本，因text-embedding-3召回率下降8%"）
         - 支持一键执行推荐操作，恢复后持续监控48h确认稳定性
}
D4
{
一、数据处理：系统需构建一个安全、可演进、高性能的企业级知识基座。核心目标是处理历史战略规划、业务计划、企业年报、市场报告、咨询报告、财务报表及内部业务数据，通过多策略数据流水线，将非结构化与半结构化数据转化为模型可理解、可追溯、可审计的结构化知识，并形成动态演进的战略档案库。

    1. 多模态文档解析与内容提取
        [1]. 支持多模态文档格式：pdf、txt、doc、docx、ppt、pptx、xls、xlsx、csv、jpeg、png、gif。
        [2]. 定义精细化内容提取策略：
            - PDF/图片：支持OCR并保留阅读顺序，提取段落、表格、图表标题，定位至页码及坐标区域。
            - Office文档：提取正文、备注、批注、单元格公式、幻灯片讲义，定位至工作表/页及具体单元格/文本框。
            - 富文本：保留Markdown等结构化标记，便于语义解析。
        [3]. 内容提取后统一封装为包含“原始内容”与“结构元数据”（坐标、层级、类型）的标准文档对象。

    2. 企业级文档生命周期管理与安全隔离
        [1]. 支持完整的文档操作功能：上传、更新、删除、分类管理、统计信息。
        [2]. 强制实施多租户数据隔离：文档从上传起即注入租户ID、数据主权域（如EU, US, CN）标签。所有操作与检索必须在租户权限和数据主权约束下进行，天然杜绝跨租户数据泄露[citation:8]。
        [3]. 文档版本管理：支持关键文档的版本控制，记录历史变更，便于回溯与审计。

    3. 可插拔嵌入框架与向量化
        [1]. 默认集成高性能本地嵌入模型（如bge-m3），确保核心数据不出域。
        [2]. 构建可插拔的嵌入模型管理框架：支持通过配置文件或遵循MCP协议动态切换/升级嵌入模型（包括云端模型），以适应未来技术演进。
        [3]. 实施嵌入模型性能监控，定期评估其在企业语料上的检索准确率与响应延迟。

    4. 高级RAG索引构建（支持父文档检索策略）
        [1]. 构建多知识库体系：支持为不同租户、不同项目、不同合规等级的数据创建独立的知识库，实现物理或逻辑隔离。
        [2]. 强制实施父文档检索策略：
            - 创建“子块”（chunk）：用于语义匹配，体积较小（如256-512 tokens）。
            - 保留“父块”（parent chunk）：提供完整上下文，体积较大（如1024-2048 tokens）。
            - 在元数据中建立父子块的双向索引。
        [3]. 元数据索引：为文档和切片注入丰富的元数据（文档ID、标题、作者、日期、类型、摘要、租户、主权域、文档坐标等），支持结构化过滤。

    5. 智能化摘要生成与质量评估
        [1]. 摘要生成采用“本地模型优先，云端模型辅助”的双轨制。常规文档由本地微调模型生成核心摘要，复杂文档或经管理员授权可调用网络大模型提升质量。
        [2]. 所有生成的摘要必须经过摘要质量评估器校验，评估维度包括关键实体覆盖率、信息熵、摘要-原文一致性评分。低于阈值（默认0.7）的摘要自动触发重生成或人工标记[citation:1]。
        [3]. 摘要作为核心元数据，与父块或文档主体关联存储，用于分层检索。

    6. 多级过滤与联邦检索策略
        [1]. 检索时实施三级过滤机制：
            - 第一级（元数据粗筛）：基于用户身份、租户权限、时间范围、文档类型等元数据，锁定可检索的知识库集合。
            - 第二级（摘要/实体检索）：在候选知识库内，检索文档摘要或关键实体，初步定位高价值文档。
            - 第三级（父文档检索）：在锁定文档内执行精细的语义检索，返回最相关的“子块”及其关联的“父块”。
        [2]. 支持联邦搜索：当用户权限涉及多个知识库时，系统自动执行跨库并发检索，并对结果进行归一化排序后返回[citation:8]。

    7. 精细化溯源与定位索引
        [1]. 所有索引切片必须包含精细化定位信息。引文元数据至少包括：文档ID、切片ID、文档内坐标（PDF页码/边界框、Excel工作表/单元格范围、PPT幻灯片编号/文本框索引）。
        [2]. 系统输出引用时，必须提供足以让前端高亮定位到原文具体位置的坐标数据。

    8. 多维度数据质量评估与自省（Self-RAG+）
        [1]. 检索结果后处理阶段，启动多维度数据充足性与质量评估器，综合判断：
            - 语义相关性得分（如>0.6）。
            - 数据时效性得分（根据文档时间戳与查询时间计算衰减）。
            - 事实一致性得分（通过多文档交叉验证或领域事实库校验）。
        [2]. 若综合评分低于动态阈值，则标记为“数据不足或质量存疑”，阻止模型强行生成，并返回评估明细供用户或Agent决策。

    9. 构建动态演进的战略档案库（长期记忆）
        [1]. 战略档案库不仅存储原始文档与向量，还结构化存储历史SP/BP的关键假设变量、决策依据、执行结果差值。
        [2]. 建立用户修正闭环：将用户对系统输出的所有修正操作记录为结构化的修正事务日志。
        [3]. 定期（如每季度）启动知识蒸馏任务，利用高级模型对累积的修正事务进行去重、泛化与冲突解决，将高频修正模式提炼为少样本提示词模板或业务规则，反向注入战略档案库，供未来规划任务使用[citation:2]。
        [4]. 知识蒸馏过程需由AUD AGENT或人工专家审计，确保知识库的纯净与演进方向的正确性。

    10. 闭环数据预处理流水线
        [1]. 建立数据预处理流水线，强制包含格式验证、内容完整性检查、SIMHash重复检测环节。
        [2]. 实现闭环质量控制：检测出的问题不仅记录日志，还需反馈至系统。例如，高频解析失败格式自动生成工单提示更新解析器；重复文档信息存入元数据库，优化后续检索展示。

    11. RAG系统性能监控与漂移检测
        [1]. 构建自动化评估Pipeline，利用Strat-Bench中预定义的检索与问答测试集，定期（如每周）评估RAG系统的检索准确率、生成质量、端到端延迟。
        [2]. 引入统计漂移检测算法（如KS检验），监控检索结果分布的长期变化[citation:2]。
        [3]. 建立可视化监控仪表盘，实时展示核心指标（平均相关性得分、检索耗时、无结果率等）。当指标发生显著漂移时，自动触发告警，并建议管理员进行模型微调或数据重索引。
}
D5
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据

    1. 多模态文档解析层
        [1]. 支持文档格式：pdf、txt、doc、docx、ppt、pptx、xls、xlsx、csv、jpeg、png、gif、mp3、mp4（音视频会议记录）
        [2]. 多模态智能解析：图片自动触发OCR+视觉语言模型双通道解析；表格提取结构化数据存入SQLite缓存层；音视频调用语音识别+摘要模型生成文字稿
        [3]. 统一输出Schema：所有文档解析后输出标准化JSON，包含内容块列表（类型text/table/image）、元数据（来源/页码/时间戳）、置信度评分

    2. 文档生命周期管理
        [1]. 文档操作：上传、更新、删除、分类管理、统计信息、版本控制
        [2]. 生命周期钩子：上传触发预处理流水线；更新触发向量库增量索引+元数据同步；删除触发级联清理（向量+缓存+摘要）
        [3]. 血缘追踪：记录每个文档片段的来源文件、处理历史、依赖关系，支持从结论追溯至原始文档的全路径

    3. 混合检索架构
        [1]. 多路召回引擎：BM25稀疏检索（精确匹配） + bge-m3密集检索（语义匹配） + ColBERT多向量检索（细粒度交互）
        [2]. 自适应路由：查询意图分类器（事实型/分析型/探索型）动态选择检索策略与深度，简单查询单路召回，复杂查询多路融合
        [3]. 父文档检索策略：用小块（256 tokens）匹配语义实现精准定位，用大块（1024 tokens或全文）提供上下文，检索噪声降低30%以上[citation:9]

    4. 智能索引与存储
        [1]. 混合索引结构：向量索引（HNSW）+ 关键词倒排索引 + 知识图谱索引（实体关系显式建模）
        [2]. 层次化存储：热数据（3个月内高频访问）存内存/SSD，温数据（历史战略档案）存对象存储，冷数据（超过5年）存归档存储
        [3]. 增量更新机制：文档变更时仅重新计算变更部分的向量与摘要，支持分钟级增量同步

    5. 结构化摘要生成
        [1]. 摘要Schema契约（Pydantic V2）：文档类型/核心观点/关键实体/时间范围/置信度/数据质量评分/适用场景标签
        [2]. 摘要质量门禁：基于信息熵+关键实体覆盖率的摘要质量评估器，低于阈值（默认0.75）自动触发重生成或人工标注
        [3]. 摘要-切片映射索引：每个切片存储父文档ID+摘要哈希+切片类型，检索时先匹配摘要锁定文档范围，再执行切片精准检索

    6. 精细化引文索引
        [1]. 精确位置存储：每个知识片段记录源文档的页码、段落号、行号、bbox坐标（PDF/图片）、表格单元格坐标
        [2]. 可交互溯源：前端支持从结论反向高亮源文档对应区域，点击跳转至文档阅读器对应位置
        [3]. 引文置信度：每个引用附带相关性得分和时效性标签，生成内容时优先采用高置信度引用

    7. 自适应检索增强生成
        [1]. 动态阈值机制：根据查询类型（战略规划/日常分析/事实核查）和领域风险等级动态调整相关性阈值（0.4-0.8可配置）
        [2]. 多源补充协议：相关性低于阈值时依次触发——知识图谱实体检索→同义词扩展查询→可信外部API（如财经数据库）→人工标注请求
        [3]. 缺失维度反馈：标记数据不足时，明确输出缺失的信息维度，指导后续数据采集

    8. 战略档案库（长期记忆模块）
        [1]. 结构化存储Schema：
            - 基础信息：SP/BP年度、版本号、创建时间、责任AGENT
            - 假设变量集：宏观经济假设/市场增长率假设/竞争假设/技术路线假设
            - 决策依据链：引用文档切片ID列表、工具调用轨迹、中间计算结果
            - 执行结果：实际经营数据、偏差分析报告、根因分类
            - 差值向量：计划vs实际的量化差异（数值型/类别型）
            - 修正轨迹：用户修正日志、修正前后对比、影响范围评估
        [2]. 知识蒸馏机制：每季度执行聚类分析，将相似修正日志转化为少样本提示词模板，存入战略知识库
        [3]. 动态遗忘策略：基于访问频次和时效性计算记忆权重，低频知识降级存储，高频知识自动强化索引

    9. 数据预处理质量门禁
        [1]. 质量检查流水线：
            - 格式验证：文件完整性、编码格式、损坏检测
            - 内容完整性检查：页数校验、关键章节覆盖率、表格解析成功率
            - 重复检测：SimHash粗筛 + Embedding相似度精排，阈值0.85以上标记重复
            - 敏感信息扫描：PII检测、商业机密关键词匹配
        [2]. 不合格品隔离区：质量评分低于0.6的文档进入隔离区，标注失败原因，支持人工修复后重入流水线
        [3]. 数据质量评分卡：每个文档生成质量评分（0-100），存入元数据供下游AGENT决策参考

    10. 全链路可观测性与漂移检测
        [1]. 监控指标体系：
            - 数据源健康度：采集成功率/解析失败率/延迟
            - 检索质量：召回率@5、MRR、NDCG@10、检索延迟P95
            - 摘要质量：信息熵/实体覆盖率/人工评分相关性
            - 系统资源：向量库查询QPS/CPU/内存/存储增长率
        [2]. 漂移检测双算法：
            - KS检验：对比月度分布检测检索质量偏移
            - CUSUM时序检测：识别性能指标的连续下降趋势
        [3]. 智能根因分析：漂移触发时自动关联数据源变更、模型版本升级、预处理逻辑调整，输出根因报告

    11. 数据治理与合规
        [1]. 数据分类分级：按保密等级（公开/内部/机密/绝密）实施访问控制，敏感数据自动脱敏
        [2]. 合规审计追踪：所有数据处理操作记录至WORM存储，满足SOX/ISO27001/欧盟AI法案要求[citation:4]
        [3]. 数据生命周期策略：按数据类型设定保留期限（战略档案永久保存，原始文档按合规要求销毁），过期自动归档或删除
}

-------------------

Q1
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的多模态数据转化为模型可理解、可溯源、可演化的结构化知识表示
    1. 支持全格式文档智能解析：
       [1.1] 文本类（pdf/txt/doc/docx/ppt/pptx）：集成DocLayout-YOLO进行Layout Analysis，精准识别标题层级、段落边界、表格结构、图表位置，输出带语义标签的结构化JSON
       [1.2] 表格类（xls/xlsx/csv）：自动识别表头/数据区/合并单元格，转换为标准化Schema（含字段类型推断），支持跨表关联分析
       [1.3] 图像类（jpeg/png/gif）：调用VLM2Vec-v2提取视觉语义嵌入，OCR识别文字区域，图表自动分类（趋势图/饼图/散点图）并提取关键数据点
       [1.4] 扫描文档：集成Tesseract++ OCR引擎，手写体调用专用识别模型，输出置信度评分供质量门禁
    2. 文档全生命周期管理：
       [2.1] 版本化CRUD：每次更新生成Immutable Snapshot（SHA3-256哈希），支持任意历史版本差异对比与回滚
       [2.2] 元数据驱动分类：自动提取行业/部门/时间/密级/文档类型标签，支持多维交叉检索与权限隔离
       [2.3] 增强统计看板：文档质量评分（完整性/清晰度/时效性）、检索热度排名、引用频次分析、存储成本分布
    3. 多模态混合检索架构：
       [3.1] 三级嵌入体系：
           - 文本：bge-m3（1024维，支持8192 token上下文）
           - 视觉：VLM2Vec-v2（统一文本-图像嵌入空间）
           - 结构：表格Schema Embedding（字段关系图谱编码）
       [3.2] 三级检索流水线：
           - 第一级（召回）：BM25稀疏检索（关键词匹配，Recall@100>0.95）
           - 第二级（粗排）：bge-m3密集检索（语义相似度，Top-50）
           - 第三级（精排）：Jina-ColBERT-v2晚期交互（token级交互，Top-5）
       [3.3] 索引自适应策略：
           - 小规模（<10万文档）：HNSW（ef=200, M=16）
           - 中规模（10万-100万）：IVF-PQ（nlist=1024, m=8）
           - 超大规模（>100万）：DiskANN（内存映射+SSD缓存）
    4. 智能分块与多路召回：
       [4.1] 语义分块+Parent Context：基于句子边界+语义相似度聚类（阈值0.75），chunk_size=512±128，保留父级上下文（2级祖先）
       [4.2] 多查询生成：对复杂查询自动生成3个语义变体（同义替换/视角转换/分解子查询）
       [4.3] RRF融合：对多查询/多检索器结果应用Reciprocal Rank Fusion（k=60），抗单一检索偏差
       [4.4] Cross-Encoder重排序：初检Top-50输入bge-reranker-large，输出精筛Top-5（相关性>0.8）
    5. 动态摘要与HyDE增强：
       [5.1] 自适应摘要长度：基于文档信息熵动态调整（简单文档200字，中等400字，复杂800字）
       [5.2] 摘要质量门禁：调用RAGAS评估faithfulness>0.85且relevance>0.8才入库，否则触发人工审核队列
       [5.3] 摘要双路径存储：
           - 元数据库：结构化JSON（关键指标/趋势/异常点）
           - 向量库：bge-m3嵌入 + HyDE增强查询（将摘要作为假设文档生成查询嵌入）
    6. 三级分层检索策略：
       [6.1] 宏观层：战略档案库检索（基于时间/行业/战略主题过滤）
       [6.2] 中观层：文档摘要检索（锁定相关文档集合）
       [6.3] 微观层：切片级精检（返回具体证据片段）
       [6.4] 检索路径追踪：生成Retrieval Trace Graph（Query→Filter→Top-K→Score），供AUD审计与可解释性分析
    7. 全链路数据血缘：
       [7.1] 四级溯源体系：document_id → page_num/block_id → chunk_id → entity_mention/relation
       [7.2] 血缘图谱存储：使用属性图数据库（Neo4j）存储文档-切片-实体-关系全链路
       [7.3] 可视化溯源：前端支持点击引文展开"原始文档→解析结果→向量化→检索排名→生成依据"完整证据链
    8. 自适应检索熔断机制：
       [8.1] 动态置信度阈值：
           - 事实型查询：0.75（高精度要求）
           - 分析型查询：0.60（允许适度推断）
           - 创意型查询：0.50（鼓励发散）
       [8.2] 三重熔断保障：
           - 一级熔断：相关性<阈值 → 标记"数据不足"
           - 二级熔断：Self-RAG Critique Token判定低质量 → 触发重检索
           - 三级熔断：CRAG外部验证失败 → 调用可信财经API补充证据
       [8.3] 熔断日志：记录触发原因、原始查询、候选文档，供AUD分析与系统优化
    9. 分层记忆与知识蒸馏：
       [9.1] 三层记忆架构：
           - 工作记忆（24h）：当前会话StateSnapshot（LangGraph Checkpoint）
           - 短期记忆（30d）：项目级关键结论、工具链配置、用户偏好
           - 长期记忆（永久）：战略档案库（SP/BP历史+执行差值+修正日志）
       [9.2] 知识固化机制：
           - 用户修正日志 → 差异分析 → Few-Shot模板生成 → Prompt Optimizer
           - 高频修正模式（>5次/季度）自动转化为业务规则模板
       [9.3] 季度知识蒸馏：
           - 聚类相似记忆（Sentence-BERT + DBSCAN）
           - 保留核心模式，压缩冗余实例（压缩率≥60%）
           - 生成Memory Summary Report供人工审核
    10. 多维质量控制流水线：
        [10.1] 重复检测三重机制：
            - SIMHash（字面重复，阈值3）
            - Sentence-BERT（语义重复，余弦相似度>0.92）
            - MinHash LSH（大规模去重，Jaccard>0.85）
        [10.2] 文档质量评分体系：
            - 完整性（0-1）：关键章节缺失检测（基于模板匹配）
            - 清晰度（0-1）：OCR置信度均值/图像模糊度（Laplacian方差）
            - 时效性（0-1）：数据新鲜度衰减函数（e^(-λ·Δt), λ=0.02/月）
        [10.3] 质量门禁：综合评分<0.7触发人工审核，<0.5拒绝入库并告警
    11. 多维性能监控与漂移治理：
        [11.1] 评估指标体系：
            - 检索质量：Recall@5, Precision@5, MRR@10, NDCG@10
            - 生成质量：RAGAS Faithfulness, Answer Relevance, Context Utilization
            - 业务影响：决策置信度变化率、人工修正率、SP/BP关键节点风险等级
        [11.2] 漂移检测双算法：
            - KS检验：月度基线对比，p-value<0.05触发告警
            - CUSUM：滑动窗口7天，连续3天下降>5%触发根因分析
        [11.3] 根因归因引擎：
            - 模块级定位：解析/嵌入/检索/生成四层归因
            - 数据级定位：特定文档类型/行业/时间范围异常
            - 自动输出Root Cause Report + 修复建议（如重训嵌入模型/调整分块策略）
        [11.4] 业务影响映射：
            - L1漂移（性能下降>15%）：标记相关SP/BP输出为"需人工复核"
            - L2漂移（>30%）：暂停自动规划，转为辅助模式
            - L3漂移（>50%）：系统级熔断，回滚至前一稳定版本
}
Q2
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的多模态数据（文本/表格/图像/公式）转化为模型可理解和检索的结构化知识图谱
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif，且具备：
      [1].表格结构化重建：集成TableTransformer解析xlsx/csv/pdf表格为结构化JSON，保留行列语义
      [2].公式与手写体识别：通过LaTeX OCR与手写识别引擎提取数学表达式与手写批注
      [3].多模态对齐：构建CLIP+文本联合嵌入空间，支持"以图搜文"与"以文搜图"跨模态检索
    2.支持完整的文档生命周期管理功能，包括文档上传、版本快照、差异比对、安全删除、自动分类、统计信息，且具备：
      [1].文档版本控制：基于Git LFS实现版本快照，支持任意历史版本回滚与变更轨迹追溯
      [2].自动分类引擎：基于内容语义+元数据的多标签分类器（准确率>92%），支持人工修正反馈闭环
      [3].元数据标准化：强制校验ISO 15489合规元数据Schema（创建者/时间/密级/业务域），缺失关键字段阻断入库
    3.构建Hybrid RAG流水线，融合BM25关键词检索与bge-m3多模态向量检索，并引入重排序优化：
      [1].混合检索引擎：初检阶段并行执行BM25与bge-m3稠密检索，通过RRF算法融合结果
      [2].语义分块替代固定切片：基于sentence-transformers检测主题边界，动态生成语义连贯片段（平均长度300±100 tokens）
      [3].重排序层：集成ColBERT-v2晚期交互模型对Top-20初检结果二次精排，提升precision@5达40%+
    4.支持完整的RAG操作功能，包括结构化元数据、单文档索引、批量索引、混合查询（BM25/向量/混合）、上下文检索、质量监控仪表盘，且具备：
      [1].检索质量实时反馈：每检索请求返回precision@k/recall@k/confidence三元组，支持前端可视化
      [2].引文精细化：存储三元组[片段ID, 原始文本快照(512字符), 采集时间戳]，支持页码/段落级定位
      [3].时效性标记：自动标记引用数据时效状态（<6月"新鲜"/6-12月"一般"/>12月"陈旧"）
    5.支持领域自适应摘要生成，调用网络大模型API生成动态长度结构化摘要：
      [1].动态长度策略：基于文档类型与复杂度自适应（企业年报800字/市场简报200字/咨询报告500字）
      [2].领域模板：预置财务摘要/市场洞察/技术评估三类Schema，强制输出关键指标字段
      [3].质量门禁：部署摘要质量评估器（信息熵+关键实体覆盖率），评分<0.7触发重生成或人工审核
    6.实施四级分层检索策略，平衡检索效率与精度：
      [1].L1全局摘要层：跨文档社区摘要（GraphRAG生成），解决全局语义关联
      [2].L2文档摘要层：单文档结构化摘要，快速锁定相关文档集
      [3].L3文档层：基于元数据过滤缩小范围
      [4].L4语义切片层：精粒度片段检索
      [5].自适应跳过：当L2摘要置信度>0.85时直连L4，减少15%+检索延迟
    7.强制实施片段级引文溯源机制，确保生成内容可审计：
      [1].三元组存储：[片段ID, 原始文本快照, 采集时间戳]，文档变更后仍可追溯原始内容
      [2].时效性标注：引用数据自动标记时效状态，陈旧数据（>12月）生成时强制提示
      [3].差异高亮：当引用文档更新时，自动比对并高亮变更区域供用户确认
    8.集成Self-RAG自适应检索机制，动态决策检索行为与质量控制：
      [1].动态阈值：基于查询复杂度自适应（简单事实查询0.5/战略分析0.75/创新推演0.85）
      [2].多文档聚合评分：采用RRF融合多源检索结果，避免单点误判
      [3].主动补救：当"数据不足"触发时，自动扩展检索范围（时间/来源/模态）或生成用户补数据提示
    9.构建三级战略记忆架构，实现知识持续演进：
      [1].工作记忆（24h）：当前会话上下文，Redis Cluster持久化
      [2].项目记忆（30d）：单次SP/BP项目全周期数据，支持跨阶段关联
      [3].战略知识库（永久）：结构化存储历年SP/BP关键假设、决策依据、执行偏差、用户修正轨迹
      [4].自动蒸馏：每季度执行知识蒸馏，聚类高频修正模式→生成领域少样本提示模板
      [5].冲突检测：新旧假设矛盾时（如市场增长率方向相反）自动触发AUD人工审核
    10.实施四层数据质量控制流水线，阻断低质量数据入库：
      [1].格式验证：强制校验文档可解析性（PDF非扫描件/表格可提取结构）
      [2].完整性检查：元数据必填字段覆盖率<90%或正文长度<100字符阻断入库
      [3].重复检测：SIMHash(检测结构重复)+BERTScore(检测语义重复)双校验，相似度>0.95标记重复
      [4].质量指数：计算DQI=0.4*完整性+0.3*唯一性+0.3*时效性，DQI<0.6进入人工审核队列
    11.建立RAGOps持续监控体系，实现性能漂移自动检测与修复：
      [1].实时指标：每100次检索自动计算precision@5/recall@5/faithfulness，滑动窗口7天均值告警
      [2].双算法漂移检测：
          - KS检验：月度基线对比检测输出分布偏移（p<0.05触发告警）
          - CUSUM：连续7天性能指标下降>10%触发根因分析
      [3].业务影响关联：当检索质量下降>15%时自动暂停SP生成，推送根因报告至AUD AGENT
      [4].自动修复：检测到索引漂移时触发增量重索引，模型漂移时切换至备用嵌入模型
    12.集成GraphRAG知识图谱增强层，解决跨文档全局理解问题：
      [1].实体关系抽取：基于spaCy+领域词典识别战略实体（公司/产品/市场/技术）
      [2].图谱构建：自动构建实体-关系-事件三元组图谱，支持社区检测与全局摘要生成
      [3].图增强检索：复杂查询自动触发图遍历，串联跨文档证据链（如"竞对A的供应链风险"需关联多份报告）
    13.实施上下文压缩机制，突破Token预算限制：
      [1].HyDE生成：对复杂查询生成假设性文档扩展检索维度
      [2].关键实体压缩：基于TF-IDF+领域重要性评分提取核心实体，压缩率目标≥70%
      [3].自适应预算：根据任务复杂度动态分配Token（SP规划50K/简单查询5K），超限时触发压缩
    14.遵循MCP 2025规范实现工具安全集成：
      [1].能力注册：所有数据处理工具通过MCP Registry注册，暴露标准Schema与可靠性评分
      [2].动态发现：AGENT运行时通过MCP协议查询可用工具，支持版本协商与灰度切换
      [3].安全沙箱：工具执行强制网络隔离，仅白名单API可访问（如财经数据源）
}
Q3
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的多模态数据转化为模型可理解、可检索、可演化的结构化知识资产
    1. 支持全模态文档格式处理，包括：
       [1.1] 文本类：pdf（保留版式坐标）、txt、doc/docx（含修订痕迹）
       [1.2] 演示类：ppt/pptx（提取幻灯片层级结构+演讲者备注）
       [1.3] 表格类：xls/xlsx/csv（使用TableFormer结构化提取，输出JSON Schema）
       [1.4] 图像类：jpeg/png/gif（使用SigLIP 2嵌入，支持图表-正文关联检索）
       [1.5] 新增：PDF版式保留模式，记录图表相对位置坐标（x,y,width,height），支持"图表-正文"空间关联检索
    2. 支持企业级文档全生命周期管理，包括：
       [2.1] 基础操作：文档上传/更新/删除/分类/版本控制
       [2.2] 文档血缘追踪：记录每次更新的diff摘要、操作者、时间戳，支持版本影响分析
       [2.3] 文档质量评分：基于文本密度、图表占比、引用完整性自动评分（1-5分），低质量文档（<2.5分）触发人工审核
       [2.4] 战略价值统计：增加"关键假设密度"（每千字关键假设数）、"引用热度"（被AGENT调用频次/周）指标
    3. 构建多模态混合嵌入架构：
       [3.1] 分层嵌入策略：
           - 短文本（<1K tokens）：bge-m3 dense embedding
           - 长文档（1K-8K tokens）：bge-m3 multi-vector + 滑动窗口聚合（步长512，重叠256）
           - 超长文档（>8K tokens）：分段摘要→bge-m3嵌入 + Parent-Child索引
       [3.2] 多模态路由：
           - 文本→bge-m3
           - 图像→SigLIP 2（投影至bge-m3向量空间）
           - 表格→TaPEx（投影至bge-m3向量空间）
       [3.3] 集成ColBERTv2晚期交互重排序器：首轮bge-m3检索Top50→ColBERTv2精排Top10，提升MRR@10 ≥18%
    4. 构建三重融合检索引擎：
       [4.1] Dense检索：bge-m3语义相似度（权重0.5，可配置）
       [4.2] Sparse检索：BM25关键词匹配（权重0.3，可配置）
       [4.3] Graph检索：基于实体关系图谱的路径检索（权重0.2，可配置）
       [4.4] RRF融合排序：Reciprocal Rank Fusion算法动态融合三路结果
       [4.5] 自适应Query Routing：
           - 简单查询（<5 tokens）：仅Sparse检索
           - 中等查询（5-15 tokens）：Dense+Sparse融合
           - 复杂查询（>15 tokens/含多实体）：三重融合+图谱扩展
       [4.6] 元数据粒度细化：支持文档级/章节级（section_id）/段落级（paragraph_id）/图表级（figure_id）/关键假设标记（assumption_tag）五级索引
    5. 实施动态多视角摘要生成：
       [5.1] 动态长度策略：
           - SP文档：800字（覆盖战略意图/创新焦点/业务设计）
           - BP文档：500字（聚焦关键任务/衡量指标）
           - 市场报告：300字（提炼趋势/机会/威胁）
       [5.2] 多视角摘要：
           - Executive Summary（CEO视角：战略方向/资源分配）
           - Risk Summary（CFO视角：财务风险/现金流压力）
           - Innovation Summary（CTO视角：技术趋势/创新机会）
       [5.3] 摘要质量反馈闭环：基于用户点击率/引用频次自动优化生成Prompt，季度触发Prompt Optimizer
       [5.4] 摘要存储：结构化JSON存入元数据库，经bge-m3嵌入后存入向量库，建立与原始文档的双向索引
    6. 构建四级分层检索架构：
       [6.1] L1：文档级摘要（100%文档覆盖）
       [6.2] L2：章节级摘要（关键章节，覆盖率≥80%）
       [6.3] L3：段落级切片（Max-Min语义分块，平均长度300±50 tokens）
       [6.4] L4：实体级片段（关键数据点/假设/决策依据）
       [6.5] 动态层数决策器：基于查询信息熵自动选择检索深度（低熵→L1/L2，高熵→L1-L4全链路）
       [6.6] Parent-Child索引：L1→L2→L3→L4建立双向指针，支持"自顶向下"与"自底向上"双向检索
    7. 增强可解释性与溯源能力：
       [7.1] 片段级溯源：保留原始文档位置（file_id, page_num, paragraph_id, char_offset）
       [7.2] 跨文档证据融合：当结论依赖多文档时，自动生成"证据融合图"，标注各文档贡献度（Shapley值）
       [7.3] 反事实推理支持：对关键结论自动模拟"若关键假设变化±10%，结论置信度变化ΔX%"
       [7.4] 引文元数据增强：增加confidence_score（0-1）、timeliness_score（基于文档日期衰减）、source_authority（年报=1.0/咨询报告=0.8/网络=0.5）
    8. 增强Self-RAG机制：
       [8.1] 动态阈值策略：
           - 事实型查询：阈值0.75（高精度要求）
           - 推理型查询：阈值0.55（允许一定模糊性）
           - 创新型查询：阈值0.45（鼓励发散思维）
       [8.2] 迭代重检索机制：
           - 首次熔断→Query Expansion（同义词扩展/问题分解）→二次检索
           - 仍熔断→生成"数据缺口报告"（缺失维度/建议补充源）
       [8.3] 熔断类型区分：
           - Data Gap（数据缺失）：建议补充数据源
           - Data Conflict（数据冲突）：启动红蓝辩论机制
           - Low Confidence（低置信度）：标记结论并建议人工复核
    9. 构建三级分层战略记忆架构：
       [9.1] 工作记忆（Working Memory）：
           - 范围：当前SP/BP周期数据
           - 有效期：24小时（会话级）
           - 存储：Redis Cluster（三节点哨兵模式）
       [9.2] 短期记忆（Short-term Memory）：
           - 范围：近3年战略档案+用户修正日志
           - 有效期：30天活跃期，90天归档
           - 存储：向量数据库（带时效性标签）
       [9.3] 长期记忆（Long-term Memory）：
           - 范围：5年+历史数据+行业基准+知识蒸馏产物
           - 有效期：永久存储
           - 存储：分层存储（热数据SSD/冷数据对象存储）
           - 季度知识蒸馏流程：
               ① 聚类相似战略场景（DBSCAN，ε=0.35）
               ② 提取共性决策模式（Prompt Mining）
               ③ 生成行业模板库（脱敏后跨企业共享）
       [9.4] 战略档案结构化存储：
           - 关键假设变量（assumption_id, variable_name, baseline_value, actual_value, delta）
           - 决策依据（decision_id, evidence_sources[], confidence_score）
           - 执行偏差（plan_value, actual_value, gap_ratio, root_cause_tags[]）
           - 用户修正日志（修正类型enum[参数/权重/约束]、修正前后值、置信度变化）
    10. 构建全链路质量控制闭环：
        [10.1] 格式验证：
            - 文本类：编码检测（UTF-8/GBK）、乱码率<5%
            - 表格类：行列完整性、数值格式校验
            - 图像类：分辨率≥300dpi（战略图表）、OCR可读性评分
        [10.2] 重复检测三重机制：
            - 文本重复：SIMHash（阈值0.95）
            - 语义重复：bge-m3嵌入相似度（阈值0.85）
            - 结构重复：表格/图表结构哈希比对
        [10.3] 文档可信度评分：
            - 权威性权重：年报=1.0，审计财报=0.95，咨询报告=0.8，内部文档=0.7，网络=0.5
            - 时效性衰减：每6个月衰减0.1（max decay 0.5）
            - 完整性扣分：缺失关键章节（战略意图/执行设计）每项-0.2
        [10.4] CI/CD质量门禁：
            - 入库前自动触发质量检查流水线
            - 阻断规则：可信度<0.6 或 重复率>30% 或 乱码率>10%
            - 人工审核通道：低质量文档进入审核队列，48小时内处理
    11. 构建生产级可观测性体系：
        [11.1] 核心评估指标：
            - 检索层：MRR@10 ≥0.75、Recall@50 ≥0.85、NDCG@20 ≥0.80
            - 生成层：Faithfulness ≥0.90（FactScore）、Answer Relevancy ≥0.85
            - 端到端：Task Success Rate ≥85%（基于Strat-Bench 20+场景）
        [11.2] 双算法漂移检测：
            - KS检验：月度分布偏移检测（α=0.05，p<0.05触发告警）
            - CUSUM：连续7天性能下降趋势检测（阈值-5%，触发根因分析）
        [11.3] 自动根因分析引擎：
            - 输入：性能下降指标+操作日志+数据变更记录+模型版本
            - 输出：根因分类（数据漂移/模型退化/Prompt失效/工具故障）+修复建议优先级
        [11.4] 健康度仪表盘：
            - 实时展示12项核心指标（检索延迟P95<200ms、生成延迟P95<3s、幻觉率<5%）
            - 三级预警：黄牌（指标下降15%）、红牌（下降30%）、停用（下降50%连续3天）
            - 支持下钻分析：从系统级→模块级→文档级逐层定位问题
}
Q4
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的多模态数据转化为模型可理解、可溯源、可演化的结构化知识
    1. 支持全格式多模态文档解析
        [1]. 文本类：pdf,txt,doc,docx,ppt,pptx（保留标题层级与段落结构）
        [2]. 表格类：xls,xlsx,csv（解析为结构化JSON，保留行列关系与语义标签）
        [3]. 图像类：jpeg,png,gif（区分三类处理路径：a)图表→数据提取+自然语言描述双通道；b)流程图→节点关系图谱；c)照片→场景标签+关键实体识别）
        [4]. 集成DocLayNet文档布局分析模型，自动识别标题/正文/表格/图表区域，输出结构化文档对象模型（DOM）
        [5]. 定义跨模态检索融合策略：文本查询可触发图像/表格检索，通过CLIP-ViL实现跨模态语义对齐
    
    2. 文档全生命周期管理
        [1]. 基础操作：上传、更新、删除、分类、标签、统计信息
        [2]. 文档血缘追踪：记录文档来源、衍生关系（如"2025年报→2026Q1摘要"）、版本衍变历史
        [3]. 重要性自动评分：基于引用频率、用户反馈、战略相关性计算动态评分（1-5分），评分<2且6个月无访问自动归档
        [4]. 时效衰减机制：超12个月文档自动降权（检索权重×0.7），超24个月需人工确认是否保留
    
    3. 混合检索架构
        [1]. 三路召回引擎：
            a) 稀疏检索：BM25关键词匹配（处理专业术语/缩写）
            b) 密集检索：bge-m3向量检索（通用语义匹配）
            c) 元数据过滤：时效性、重要性、文档类型、战略阶段
        [2]. 嵌入模型路由：
            - 通用文本：bge-m3（8192 tokens）
            - 长文档重排：ColBERTv2（支持8000 tokens晚交互）
            - 战略专业术语：微调版bge-m3-strategy（基于BLM/BEM术语库）
        [3]. 结果融合：加权倒数秩融合（Weighted RRF），权重配置：BM25(0.3) + 向量(0.5) + 元数据(0.2)
        [4]. 嵌入漂移监控：月度计算MMD距离，>0.15触发嵌入模型增量微调或索引重建
    
    4. 两阶段检索流水线
        [1]. Stage1-搜索（Search）：基于文档摘要/元数据粗筛，返回Top 20文档
            - 查询扩展：同义词扩展（WordNet） + HyDE生成假设文档（3个变体）
            - 多路召回：向量检索(15) + BM25(10) + 时效过滤(最近24个月优先)
        [2]. Stage2-检索（Retrieve）：基于Stage1结果的细粒度切片检索
            - 分层分块：保留文档结构层次（章节→段落→句子），父块包含子块引用
            - 切片大小自适应：文本块300±50 tokens，表格块保留完整行列，图表块关联描述文本
            - 关联强度评分：摘要-切片关联度（0-1），<0.7时自动补充相邻切片
        [3]. 多跳推理支持：构建实体关系图谱（公司-产品-市场-技术），支持跨文档推理链
    
    5. 动态摘要生成与验证
        [1]. 摘要长度自适应：基于文档复杂度（页数×0.3 + 图表数×0.4 + 专业术语密度×0.3）动态调整200-800字
        [2]. 结构化摘要Schema：{核心结论, 关键数据, 趋势判断, 风险提示, 时效标签}
        [3]. 一致性校验：AUD AGENT验证关键事实失真率，>5%触发重生成并记录至漂移日志
        [4]. 摘要更新触发器：
            - 原文更新（内容变更>10%）
            - 时效超6个月且被高频检索
            - 用户反馈修正关键事实
    
    6. 分层检索与溯源增强
        [1]. 三级检索路径：元数据过滤 → 摘要检索 → 切片检索，每级设置置信度阈值
        [2]. 检索路径可视化：输出决策图谱，展示"用户查询→扩展查询→摘要匹配→切片召回"全链路
        [3]. 引文强制覆盖：生成内容中每个事实主张必须关联≥1个引文，覆盖率<90%触发重检索
        [4]. 引文质量评分：相关性(0-1) × 时效系数（12个月内=1.0，每超6个月衰减0.2），<0.6标记"弱证据"
        [5]. 引文冲突检测：同一主张关联矛盾引文时，触发AUD AGENT仲裁并生成冲突报告
    
    7. 自省式检索控制（Self-RAG增强版）
        [1]. 动态阈值机制：
            - 战略决策类任务：相关性阈值0.75
            - 市场洞察类任务：相关性阈值0.65
            - 创意生成类任务：相关性阈值0.55
        [2]. 检索诊断分类：
            - 数据缺失：库中无相关数据 → 触发外部数据采集（白名单API）
            - 检索失效：存在数据但未召回 → 优化查询策略并记录至Query Rewrite库
            - 证据不足：召回数据但质量低 → 标记"低置信度"并提示用户补充
        [3]. 主动补充策略：数据不足时，自动执行三步：a)扩展同义词检索；b)触发网络搜索（SEC/彭博/行业数据库）；c)生成数据需求清单供用户确认
    
    8. 战略档案库（长期记忆核心）
        [1]. 假设变量追踪Schema：
            {
                variable_name: "市场规模增长率",
                initial_value: "8%",
                actual_value: "5.2%",
                deviation_rate: "-35%",
                impact_factor: "营收预测偏差-12%",
                correction_suggestion: "采用保守增长率5%并增加敏感性分析",
                evidence_sources: ["2025年报P23", "IDC 2026Q1报告"]
            }
        [2]. 决策-结果闭环：结构化存储SP/BP关键假设、决策依据、实际执行结果、偏差分析
        [3]. 修正日志固化：用户修正自动转换为少样本提示词，按场景聚类（市场预测/竞争分析/财务建模）
        [4]. 知识蒸馏机制：季度执行，步骤：
            a) 聚类相似修正模式（DBSCAN，eps=0.3）
            b) 提取通用规则模板
            c) 冗余样本归档至冷存储
            d) 更新Prompt Optimizer的Few-Shot库
        [5]. 跨项目知识迁移：新项目启动时，自动匹配历史相似场景（余弦相似度>0.7）的假设-结果对，作为先验知识注入Planner
    
    9. 多维度质量控制
        [1]. 重复检测三重机制：
            - 字面重复：SIMHash（阈值<3）
            - 语义重复：Sentence-BERT聚类（余弦相似度>0.85）
            - 事实重复：知识图谱实体对齐（相同主体+相同事件+时间差<30天）
        [2]. 可信度评分体系：
            - 来源权威性：年报(1.0) > 咨询报告(0.85) > 行业白皮书(0.7) > 网络爬虫(0.4)
            - 时效性：12个月内(1.0)，每超6个月衰减0.2
            - 交叉验证：被≥2个独立来源验证(×1.2)，否则(×0.9)
        [3]. 质量分级路由：
            - 高可信度（≥0.8）：直接入库+高优先级索引
            - 中可信度（0.5-0.8）：进入人工审核队列，24小时内处理
            - 低可信度（<0.5）：隔离区待验证，禁止参与检索
    
    10. 漂移检测与自愈体系
        [1]. 量化监控指标：
            - 检索质量：Recall@5≥0.85, Precision@5≥0.75, MRR≥0.7
            - 生成质量：Faithfulness≥0.9（RAGAS评估）, Groundedness≥0.85
            - 系统健康度：平均响应时延<3s，错误率<2%
        [2]. 漂移检测三算法：
            - 数据分布偏移：KS检验（p<0.05触发告警）
            - 嵌入空间漂移：MMD距离（>0.15触发重训练）
            - 查询模式变化：Jensen-Shannon散度（滑动窗口7天，>0.2触发Query Rewrite库更新）
        [3]. 自愈策略矩阵：
            | 漂移类型 | 自动响应 | 人工介入阈值 |
            |----------|----------|--------------|
            | 轻度（指标下降10-15%） | 增量索引重建 | 无 |
            | 中度（下降15-30%） | 嵌入模型微调 + 查询改写 | 通知管理员 |
            | 重度（下降>30%） | 回滚至上一稳定版本 + 根因分析 | 强制人工review |
        [4]. 漂移根因分析报告：自动关联日志，输出"数据变更/模型更新/查询模式/外部事件"四维归因
    
    11. 审计与合规保障
        [1]. 全链路操作日志：文档操作、检索请求、生成内容、用户反馈均记录至WORM存储
        [2]. 引文溯源强制：PDF报告中所有数据主张必须包含可点击引文索引，格式：[DocID:切片ID@页码]
        [3]. 幻觉检测：基于FactScore量化幻觉率，>5%自动标记并触发AUD AGENT复核
        [4]. 合规性检查：自动识别PII/商业机密，脱敏后方可进入公共检索库
}
Q5
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的多模态数据转化为模型可理解、可推理、可溯源的结构化知识表示
    1. 支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif，采用双引擎解析策略：
       1.1 文本密集型PDF（年报/报告）优先使用PyMuPDF4LLM，保留表格结构与层级标题，输出Markdown+表格JSON双格式
       1.2 扫描件/图像型PDF使用Unstructured.io OCR，置信度<0.85的文本块标记"需人工复核"
       1.3 多模态联合索引：图像/图表自动生成alt-text描述并独立向量化，支持"图文语义联合检索"
    2. 支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息，并增强：
       2.1 文档血缘追踪：自动记录文档派生关系（如"2025年报→2026Q1经营简报"），构建文档演化图谱
       2.2 敏感信息自动检测：集成PII识别模块，自动标记财报中的商业机密字段（客户名单/成本结构），按脱敏级别分级存储
    3. 构建混合检索三级流水线（Hybrid Retrieval Pipeline）：
       3.1 稀疏检索层：BM25实现关键词精准匹配，解决专业术语召回问题
       3.2 稠密检索层：bge-m3实现8192-token长上下文语义匹配
       3.3 重排序层：bge-reranker-v2-m3对Top-50候选进行精细化排序，提升Top-5相关性30%+
       3.4 可选GraphRAG引擎：将SP/BP关键假设变量（市场增长率/竞争格局）构建成知识图谱，支持因果推理检索
    4. 支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息，并扩展元数据Schema：
       4.1 强制字段：valid_until（数据有效期，默认12个月）、source_confidence（来源可信度，0-1）、strategic_relevance（战略相关性，1-5分）
       4.2 检索质量实时反馈：每次检索返回RAG Triad三指标——Faithfulness(忠实度)、Answer Relevancy(答案相关性)、Contextual Precision(上下文精度)
    5. 支持Agentic摘要生成机制：
       5.1 动态长度控制：LLM自主决策摘要长度（战略文档>800字保留关键假设，市场快讯<200字提炼核心趋势）
       5.2 质量门禁：基于信息熵+关键实体覆盖率双阈值评估，综合评分<0.7自动触发二次生成
       5.3 摘要存入元数据库，经bge-m3词嵌入后存入向量库，同时构建摘要→原文片段的双向索引
    6. 支持Self-Routing RAG分层检索策略：
       6.1 动态路径决策：根据查询复杂度自动选择"摘要检索→切片检索→全文检索"路径
       6.2 置信度传播机制：摘要层检索置信度<0.6时跳过摘要直接切片检索，避免信息损失
       6.3 检索路径可解释：返回检索决策日志（如"跳过摘要层：因查询含具体财务指标"）
    7. 支持多级引文溯源体系：
       7.1 片段级溯源：保留原始文档切片位置（页码/段落）
       7.2 指标级溯源：关联关键财务/运营指标至数据源（如"营收增长率→2025年报P23"）
       7.3 假设级溯源：通过GraphRAG链接战略假设变量至支撑证据（如"市场增长率15%→IDC 2025Q4报告"）
       7.4 前端支持交互式溯源树：从结论逐层展开至原始数据，每层显示"数据来源→计算逻辑→中间结论"三元组
    8. 支持增强型Self-RAG机制：
       8.1 场景自适应阈值：战略规划类任务相关性阈值0.75，市场扫描类0.55，用户可配置
       8.2 补救工作流：触发"数据不足"时自动执行——①调用白名单财经API补充最新数据 ②提示用户上传补充材料 ③标记低置信度结论供AUD审核
       8.3 检索反思日志：记录每次"跳过生成"的决策依据，用于优化检索策略
    9. 构建时序知识图谱式战略档案库（Temporal Knowledge Graph Memory）：
       9.1 事实时效性管理：为每个存储事实标注valid_from/valid_until，超期事实自动衰减检索权重（线性衰减至0.3）
       9.2 结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，构建"假设-执行-偏差"三元组
       9.3 修正日志质量过滤：仅将置信度>0.85且被3+用户验证的修正模式固化为少样本提示词
       9.4 季度知识蒸馏：自动聚类相似修正模式，生成领域特定的提示词模板库
    10. 支持增强型数据预处理流水线质量控制：
        10.1 双模重复检测：SIMHash(字面重复)+bge-m3余弦相似度(语义重复)双校验，相似度>0.92标记重复
        10.2 多模态质检：图像OCR置信度<0.9标记"需人工复核"，表格结构完整性校验（行列对齐/合并单元格识别）
        10.3 格式验证增强：PDF解析失败率>5%自动切换备用解析引擎（PyMuPDF4LLM↔Unstructured.io）
    11. 建立RAG Triad全维度性能监控与漂移检测机制：
        11.1 三维度监控指标：
            - Faithfulness（忠实度）：生成内容与检索上下文的事实一致性，目标>0.85
            - Answer Relevancy（答案相关性）：回答与用户查询的语义匹配度，目标>0.80
            - Contextual Precision（上下文精度）：检索结果中相关片段占比，目标>0.70
        11.2 双算法漂移检测：
            - KS检验：月度基线对比检测输出分布偏移，p-value<0.05触发告警
            - CUSUM：7天滑动窗口检测性能指标连续下降，下降幅度>15%启动根因分析
        11.3 战略任务差异化监控：SP/BP规划类任务监控阈值比常规分析严格20%
        11.4 可视化健康度仪表盘：实时展示11项核心指标，支持按文档类型/时间范围下钻分析
    12. 【新增】支持知识感知查询扩展（Knowledge-Aware Query Expansion）：
        12.1 避免纯HyDE风险：不生成完整假设文档，仅扩展关键实体与关系（如"新能源汽车"→"动力电池/充电网络/政策补贴"）
        12.2 领域词典增强：集成战略管理术语库（BLM/BEM框架术语），自动识别并扩展专业概念
        12.3 扩展质量校验：扩展后查询与原查询语义相似度需>0.6，防止主题漂移
    13. 【新增】企业级安全与合规基线：
        13.1 敏感数据自动脱敏：审计日志和反馈记录自动脱敏处理（个人可识别信息、商业机密），脱敏策略季度评审
        13.2 访问控制增强：基于RBAC模型实现文档级权限控制（如CFO可访问财务数据，CMO仅市场数据）
        13.3 WORM存储：所有检索日志、生成内容、用户修正记录写入不可篡改存储，保留期≥7年满足SOX/ISO27001要求
    14. 【新增】全链路可观测性：
        14.1 检索轨迹追踪：记录每次检索的完整路径（查询→扩展→稀疏检索→稠密检索→重排序→结果）
        14.2 成本-质量权衡分析：可视化展示Token消耗与Faithfulness指标的帕累托前沿，支持成本优化决策
        14.3 根因分析自动化：性能下降时自动关联日志（文档更新/模型版本/查询模式变化），输出根因报告
}

-------------------

G1
{
企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将多模态、非结构化的数据转化为包含逻辑拓扑与语义向量的结构化知识网络，为系统提供高置信度的认知底座。
    1.支持多模态文档接入与版面感知解析
        [1].支持的文档格式包括pdf, txt, doc, docx, ppt, pptx, xls, xlsx, csv, jpeg, png, gif。
        [2].引入版面分析引擎与视觉大模型（VLM），精准识别复杂文档中的阅读顺序（Reading Order）、多层级标题、跨页表格与业务流程图。将非结构化图表转化为Markdown/JSON格式，保留空间布局与从属逻辑。
    2.支持全生命周期与数据血缘管理的文档操作功能
        [1].提供文档上传、增量更新、版本回滚、软删除、动态分类、统计信息等完整操作。
        [2].建立实体级数据血缘追踪（Data Lineage）。当源文档更新或删除时，自动标记下游派生的切片、摘要及战略假设为“失效/待复核”状态，触发级联更新。
    3.构建基于“知识图谱+双路向量”的混合检索引擎（GraphRAG + Hybrid Search）
        [1].底层采用本地词嵌入模型bge-m3（稠密语义特征）与BM25（稀疏关键词特征）双路召回。
        [2].在向量化前，调用信息抽取管道（LLM提取器）提取文本中的战略实体（如竞争对手、财务指标、宏观政策）及其关系，构建战略知识图谱，实现跨文档的逻辑链关联。
    4.支持动态语义切片与全面的RAG操作功能
        [1].废弃固定长度切片，采用基于文档大纲结构（Heading-based）与自然语义边界的动态切片（Semantic Chunking）技术，确保切片内容的业务完整性。
        [2].支持元数据管理、单文档/批量索引、语义查询、多跳图谱游走（Multi-hop Reasoning）、上下文窗口组装与统计信息。
    5.支持RAPTOR架构的递归抽象摘要树
        [1].抛弃单一摘要限制，自底向顶递归聚类并生成多层级摘要。底层聚焦财报明细与业务数据，高层聚合成宏观战略洞察与行业趋势。
        [2].将各层级摘要与其所关联的底层切片、知识图谱子图进行锚定，存入元数据库与向量库。
    6.支持“图-文”融合的分层检索策略
        [1].执行先全局后局部的漏斗策略：先检索高层抽象摘要和图谱中心节点锁定宏观范围，再沿树状结构下钻检索具体切片和数据表格，应对宏观战略提问与微观指标核查的双重需求。
    7.支持多模态细粒度引文索引与防篡改机制
        [1].检索处理时保留高精度引文索引（精确定位至原文页码、段落及图表Bounding Box）。
        [2].关键财务与战略指标建立密码学哈希校验，确保后续AGENT生成的内容100%可溯源至原始文档片段，从底层阻断数值级幻觉。
    8.支持带主动纠错机制的纠正型检索（CRAG）
        [1].若评估检索到的文档相关性得分或信息覆盖度低于设定阈值（默认0.6或缺乏关键实体），系统不仅标记"数据不足"，更自动触发知识路由器。
        [2].自动进行Query重写或调用内部可信API/白名单网络爬虫进行二次检索补充，构建“评估-补充-重试”的闭环。
    9.构建带时间衰减的战略档案库（长期记忆系统）
        [1].除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值。
        [2].引入时间衰减权重机制，对过期的宏观假设进行自动降权；将用户修正日志反向固化为少样本提示词（Few-shot Prompts），形成动态演进且自动清洁的战略知识库。
    10.支持高维数据预处理流水线与智能质量控制
        [1].执行格式验证、内容完整性检查、乱码清洗。
        [2].超越传统SIMHash查重，引入语义去重（Semantic Deduplication）机制。对于不同来源报告中的冲突数据（如不同投行对同一市场规模的预估），自动提取并触发审计标记，供系统后续引入多重视角对比。
    11.建立动态业务性能监控与漂移检测机制
        [1].定期评估系统在SP/BP真实规划任务中的检索召回率（Recall）与生成事实一致性。
        [2].结合用户（专家）在后续规划中的纠错反馈，逆向优化切片策略与图谱提取Prompt，形成与业务价值挂钩的数据处理自愈体系。
}
G2
{
企业战略规划管理智库系统需求特性列表
一、数据处理：构建基于多模态解析与混合检索架构（Hybrid GraphRAG）的智能数据底座。需要处理历史战略规划与业务计划、企业年报、市场研报、财务报表、内部业务数据及高管会议记录，将非结构化/半结构化多模态数据转化为大模型可推理、可溯源的知识图谱与向量网络。
    1.支持的多模态格式与版面解析：
        [1].支持格式包括pdf, txt, doc, docx, ppt, pptx, xls, xlsx, csv, jpeg, png, gif，以及音频格式(mp3, wav)的自动转写。
        [2].集成版面感知解析器（Layout-aware Parser），精准识别并提取PDF/PPT中的复杂表格结构、图表数据（图生文转化）、多级标题与脚注，确保财务数据与战略框架的物理结构不被破坏。
    2.支持全生命周期的文档与数据源管理：
        [1].提供文档上传、更新、删除、分类管理、版本控制与差异比对（Diff）功能。
        [2].支持内部业务数据库（如ERP/CRM）与外部API的定时/增量实时同步流控。
    3.构建“图向量混合”的双擎索引管线：
        [1].稠密检索底座：使用本地词嵌入模型（如bge-m3）处理多语言与长文本语义向量。
        [2].稀疏与图谱底座：集成BM25算法处理战略专有名词/代号，同时在数据入库时利用本地SLM（小语言模型）自动抽取“实体-关系-属性”，构建战略知识图谱（Property Graph）。
    4.自适应与上下文感知的RAG流水线：
        [1].废弃固定长度切片，采用基于文档大纲（AST结构）与语义边界的动态切片（Semantic Chunking）。
        [2].建立“父子文档层级树（Parent-Child Hierarchy）”，检索细粒度切片时自动召回父级上下文，防止战略背景丢失。
        [3].支持元数据过滤、单文档/批量索引、混合语义查询、统计信息实时大屏。
    5.动态结构化摘要与合成数据增强：
        [1].入库时自动生成基于信息熵弹性长度的结构化摘要，包含核心观点、数据指标和实体清单，存入元数据库。
        [2].自动利用文档内容生成高质量的“合成问答对（Synthetic QA）”并进行词嵌入，建立从“问题空间”到“文档空间”的映射，提升检索命中率。
    6.智能路由与多跳推理检索策略：
        [1].支持查询重写（Query Rewriting）与意图识别，将复杂战略问题拆解为多维子查询。
        [2].支持多跳检索（Multi-hop Retrieval），在图谱中穿梭关联多个独立报告（如将宏观PESTEL研报与内部财务表格跨文档关联分析）。
    7.不可篡改的区块级引文溯源机制：
        [1].检索与生成处理时严格保留细粒度引文索引（精确到页码、段落及图表ID）。
        [2].引入防篡改哈希签名机制（Merkle树），确保后续生成的每一条战略假设均可硬核溯源至原始文档片段，满足金融审计要求。
    8.自适应Self-RAG与主动补齐机制：
        [1].采用动态相关性阈值（基于任务复杂度自适应调整），对检索结果进行可信度评分。
        [2].若数据质量不达标，标记为"数据不足"拒绝强行生成，并自动生成“缺失信息采集清单”，反向触发上层Agent或提示用户补充数据。
    9.构建基于时序属性图谱（Temporal Graph）的战略档案库（长期记忆）：
        [1].基于时间序列存储历年SP/BP的关键假设变量、决策依据与实际执行结果。
        [2].结构化提炼用户修正日志与执行差值，作为动态节点更新至时序图谱中；对于高频修正模式，自动蒸馏为系统级或Agent级的少样本提示词（Few-shot Prompts）。
    10.AI驱动的数据预处理与质量控制门禁（Data Quality Gate）：
        [1].包含格式验证、内容完整性检查、基于向量的语义去重（识别不同研报对同一事件的重复表述）和逻辑矛盾检测。
        [2].引入LLM-as-a-Judge机制，对入库数据的可信度、时效性、相关性进行综合打分，低分数据需人工复核。
    11.实时遥测与模型漂移检测系统（Telemetry & Drift Monitoring）：
        [1].建立实时监控闭环，采集用户隐式反馈（如引用采纳率、修正频次）评估检索与生成质量。
        [2].定期执行基准测试对比，检测数据分布偏移与模型性能退化，自动触发索引重建或模型微调建议。
}
G3
{
一、数据处理：构建多模态、图向量混合的智能数据流水线。需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化与半结构化数据转化为模型可理解、可图谱化检索的结构化资产。
    1. 多模态深层解析引擎：
        [1]. 支持的文档格式包括 pdf, txt, doc, docx, ppt, pptx, xls, xlsx, csv, jpeg, png, gif，以及音视频会议记录（基于ASR转录）。
        [2]. 集成版面分析模型（如Docling/LayoutLM），强制将财报、研报中的多维表格、嵌套图表、页眉页脚分离，无损提取为结构化Markdown或JSON，消除视觉排版带来的语义割裂。
    2. 数据血缘与资产管理：
        [1]. 支持完整的文档操作功能，涵盖文档上传、增量更新、分类管理、版本控制（Versioning）。
        [2]. 建立严格的数据血缘（Data Lineage）追踪，确保每一个解析后的数据切片都能追溯到其导入批次、操作人及原始文件版本。
    3. 混合检索架构与向量库构建：
        [1]. 使用本地词嵌入模型 bge-m3 构建稠密向量索引，结合BM25算法构建稀疏索引。
        [2]. 引入知识图谱（Knowledge Graph）抽取流水线，识别战略文本中的实体（竞争对手、技术趋势、财务指标）及关系，构建图-向量混合检索（GraphRAG）底座。
    4. 智能体分块与RAG操作：
        [1]. 废弃固定字数切片，采用智能语义分块（Agentic Chunking），根据文档的逻辑段落、章节标题、表格边界进行动态切片，保证战略语境的完整性。
        [2]. 支持完整的RAG操作：多维元数据过滤、混合检索排序（Reciprocal Rank Fusion）、图谱关联展开、上下文窗口动态扩缩。
    5. 结构化摘要与RAPTOR层级：
        [1]. 调用网络大模型API对文档进行摘要提取，强制采用Pydantic校验输出JSON格式结构化摘要（包含核心指标、关键实体、结论基调）。
        [2]. 采用树状摘要聚合策略（类似RAPTOR机制），支持自下而上的各级摘要生成，锁定宏观问题边界后再下钻检索具体切片。
    6. 自反思与动态路由检索 (Active Self-RAG)：
        [1]. 建立Query理解与路由引擎，根据用户指令动态决定检索路径（走摘要树、密集向量或图谱关联）。
        [2]. 摒弃静态阈值，采用LLM-as-a-Judge进行检索质量实时评估。若检索上下文不足以回答问题，自动触发Query重写（Query Transformation）、条件放宽或调用外部工具检索，拒绝强行生成。
    7. 高保真视觉级引文溯源：
        [1]. 检索处理时不仅保留文本块切片ID，需强制映射原文档的物理视觉坐标（Bounding Box）。
        [2]. 确保后续Agent生成的内容可实现“点击溯源”，前端交互可直接高亮展示原始PDF或图像中的具体出处段落或表格单元。
    8. 时序动态战略档案库 (Temporal Strategic Memory)：
        [1]. 建立基于时间戳的长效记忆图谱，解决历年SP/BP数据冲突。明确隔离“假设变量”、“决策依据”与“实际结果”。
        [2]. 自动计算规划与执行的差值，将专家修正日志与差值复盘反向蒸馏为Few-shot提示词或业务规则，实现战略知识库的动态自我演进。
    9. 智能质量控制与数据清洗：
        [1]. 数据预处理流水线引入Agent评审员，进行格式验证、内容完整性检查。
        [2]. 采用语义级相似度去重（Semantic Deduplication）替代传统SIMHash，自动识别并预警不同咨询报告中对同一指标的冲突表述。
    10. 闭环性能监控与持续对齐：
        [1]. 建立RAG流水线的实时观测看板，监控指标包括：索引延迟、多路召回率、重排序耗时、摘要生成Token消耗。
        [2]. 建立性能漂移检测机制，收集终端用户对引用准确率的负反馈，自动触发评估基准测试集，诊断并调优词嵌入模型与重排序策略。
}
G4
{
企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的多模态数据转化为模型可深度理解、关联推理和精准检索的结构化与图谱化数据
    1.支持的多模态文档摄取
        [1].支持格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif。
        [2].集成多模态版面分析（Layout Parser）与Vision LLM，支持对复杂PDF中的财务数据表（Table-to-JSON）和战略图表（Chart-to-Markdown）进行高保真结构化提取。
    2.支持完整的文档生命周期与分类管理
        [1].支持文档上传、更新、删除、版本控制及多维度分类打标（按行业、年度、战略层级）。
        [2].引入时间衰减机制（Time-To-Live），支持对历史陈旧数据（如5年前市场报告）进行自动降权配置或归档冷存储。
    3.构建“向量+稀疏+图谱”的三路混合RAG底座
        [1].使用本地词嵌入模型bge-m3处理密集语义向量（Dense Retrieval）。
        [2].内置BM25算法处理特定战略术语与缩写的精准匹配（Sparse Retrieval）。
        [3].引入轻量级知识图谱抽取流水线，构建战略实体关系图（GraphRAG），支持宏观经济指标与企业内部业务逻辑的图结构化关联。
    4.支持高级RAG管道操作与自适应路由
        [1].支持元数据管理、单文档/批量索引、三路混合查询、上下文组装及统计信息。
        [2].内置Query Router（意图识别器），根据用户指令复杂度自动路由至不同检索策略（单一事实检索、多跳逻辑检索或图谱遍历）。
    5.支持多层级抽象与上下文感知切片（Contextual Chunking）
        [1].调用大模型API实施RAPTOR（递归抽象摘要）算法，生成文档树状层级摘要（全篇->章节->段落），提取结构化JSON元数据。
        [2].切片阶段执行“上下文注入”，在每个数据块（Chunk）前自动附加所属文档的核心摘要与实体属性，根除信息碎片化导致的语义歧义。
    6.支持分层检索与智能重排（Reranking）
        [1].执行“图谱摘要定位 -> 混合向量检索寻找切片”的分层过滤策略。
        [2].集成交叉编码器（Cross-Encoder）进行最终检索结果的语义重排，确保高价值战略上下文优先输入大模型窗口。
    7.支持细粒度、可视化的长效引文溯源
        [1].检索与生成处理中强制保留Block-level引文索引。
        [2].生成的报告内容需可反向追溯至具体文档切片，并支持前端基于原始PDF的物理坐标（Bounding Box）进行高亮定位渲染。
    8.支持纠错型检索机制（Corrective RAG）
        [1].系统对检索到的文档块进行相关性评分。若得分低于设定阈值（默认 0.6），自动触发Query Rewriter进行查询扩展与重写，执行二次检索。
        [2].若二次检索仍不达标，触发大网搜索Fallback机制（调用外部可信API）补充最新行业数据，最终若仍缺失则标记"数据不足"，阻断幻觉生成。
    9.构建动态演进的战略档案库（长期记忆）
        [1].除基于时间序列的元数据过滤外，需图谱化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值。
        [2].建立记忆蒸馏流水线：定期清洗、合并用户的“修正日志”，将其反向固化为高质量的Few-shot提示词样本集，利用DSPy框架动态注入模型推理上下文。
    10.支持高保真预处理流水线的质量控制
        [1].包含格式验证、内容完整性检查、重复检测（SIMHash结合LLM-as-a-Judge进行语义去重）。
        [2].建立自动化脱敏节点，对输入数据的敏感商业机密（如未公开的财务测算）进行动态掩码或匿名化处理。
    11.建立企业级性能监控与数据治理机制（LLMOps）
        [1].内嵌自动化评估框架（如RAGAS），实时计算并监控RAG流水线的检索精度（Precision/Recall）、上下文相关性以及生成的忠实度（Faithfulness）。
        [2].建立数据与模型漂移检测警报，支持系统行为与检索效能指标的可视化大盘展示。
}
G5
{
一、数据处理：构建基于多模态解析、混合检索与知识图谱增强（GraphRAG）的下一代动态数据底座。需要处理历史战略规划与业务计划、企业年报、市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化、半结构化数据转化为模型可理解、可推理、可溯源的结构化关联数据。
    1.支持原生多模态文档接入与视觉文档理解（VDU）
        [1].支持格式包括pdf, txt, doc, docx, ppt, pptx, xls, xlsx, csv, jpeg, png, gif。
        [2].引入先进版面分析模型（Layout Analysis），精准识别并剥离多栏文本、页眉页脚与水印。针对复杂财务报表与嵌套表格，强制采用视觉大模型或专门解析器转化为包含行列映射关系的Markdown或JSON结构，防止数据失真。
    2.支持高保真文档生命周期与血缘管理
        [1].支持包括文档上传、更新、删除、分类管理、版本控制与统计信息的完整操作。
        [2].建立严格的数据血缘追踪（Data Lineage），任何文档的更新或删除必须触发级联刷新，自动标记或更新下游受影响的切片、摘要与图谱节点。
    3.构建基于混合召回与知识图谱的RAG流水线（Graph-Enhanced Hybrid RAG）
        [1].使用本地词嵌入模型（如bge-m3）结合BM25算法，建立“语义+关键词”双路密集与稀疏检索。
        [2].引入图数据库构建动态知识图谱。在数据入库时，自动抽取文档中的关键实体（组织、人员、市场、财务指标等）及逻辑关系（因果、包含、影响等），构建战略实体关系网络。
    4.执行动态语义切片与自适应块管理
        [1].摒弃固定字数切块，采用基于文档结构（标题树）与上下文凝聚度（Semantic Cohesion）的自适应动态切片策略，确保每个Chunk具备独立完整语意。
        [2].支持完整的RAG基础操作，包含丰富元数据注入（时间戳、信源等级、作者等）、单/批量索引、统计信息管理。
    5.多维结构化提炼与认知锚点构建
        [1].支持调用大模型API按信息熵自适应生成结构化文档摘要，不仅存入元数据库，同时提取微观“三元组（实体-关系-实体）”写入知识图谱。
        [2].利用摘要构建认知锚点，大幅提升后续检索在宏观战略层面的对齐度。
    6.支持基于图谱与父子文档的立体检索策略
        [1].宏观问题优先触发图谱社区摘要（Community Summaries）检索；微观问题支持先检索文档摘要锁定范围，再下钻检索具体切片（Parent-Child Retriever）。
        [2].支持多跳推理检索，通过图谱关系补全向量检索无法发现的隐式战略传导链（如：宏观政策变动->行业原材料上涨->企业利润率预期影响）。
    7.强化级可溯源引文引擎
        [1].支持检索与生成过程保留极细粒度的引文索引（精确至Chunk ID与版面坐标）。
        [2].确保最终生成内容前端可直接溯源并高亮原始文档的具体片段或图表区域。
    8.部署Corrective RAG (CRAG) 与主动探究机制
        [1].系统对检索到的文档进行实时相关性与置信度评估（设定动态阈值）。
        [2].若评估为“数据不足”或存在“事实冲突”，不再仅是被动标记，而是自动触发Query重写机制，或向环境调度器申请调用内部SQL/外部Web Search工具进行动态数据补全，避免强行生成。
    9.构建四维战略时序档案库（长期记忆系统）
        [1].建立基于时间轴的四维记忆模块。除常规过滤外，强制结构化提取并对齐历年SP/BP的“关键假设变量、决策依据模型、实际执行结果的差值”。
        [2].通过时序图谱（Temporal Graph）追踪战略变量的演进轨迹。将高质量的用户修正日志经过清洗与一致性判定后，反向固化为Prompt Optimizer的Few-Shot样本集，实现系统认知的自我进化。
    10.高信噪比数据预处理与质量门禁
        [1].构建入库前清洗流水线，包括格式防腐验证、内容完整性检查、乱码剔除。
        [2].引入高级去重与偏见检测，从SIMHash文本去重升级至语义级排重；针对冲突研报数据，基于信源权重评级体系打标签，保留多方观点供Agent后续辩论裁决。
    11.实时运行观测与数据漂移自愈机制
        [1].建立查询意图难度评估与检索准确率（MRR/NDCG）的实时监控指标体系。
        [2].定期评估底层知识分布的漂移现象，对于高频长尾失败Query，自动生成诊断报告并推荐重索引或补充专项数据源。
}

-------------------

O1
{
企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
        [1]. 为每类格式定义专用解析器与处理策略：
            - PDF：基于页级与段级双通道解析（文本流 + 视觉布局），支持表格识别器（表格→结构化 CSV/JSON）、图表识别（图表元数据提取）、并记录解析版本号与置信度。
            - 图片（jpeg/png/gif）：OCR（可配置多引擎）→语言检测→视觉 embedding（可选）并保留图像指纹与原始图像哈希。
            - Office 文档（docx/pptx/xlsx）：结构化抽取（大纲、表格、注释、演讲稿）并保留原始文件元信息。
            - 文本/CSV：编码/分隔符验证与实体/时序字段检测。
            - 多语言与字符编码：统一使用 UTF-8 内部表示，支持显式语言标签与多语言分段。
            - 失败/降级：解析失败自动进入再处理队列（含人工复核标签），并记录失败原因与重试策略。
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
        [1]. 文档版本化与快照：每次上传/更新产出不可变快照（快照ID），并在元数据中记录 parse_version、embedding_version、source_hash、uploader。
        [2]. 删除策略：软删除 + WORM 审计条目（满足合规检索），并支持管理员触发的不可逆清理策略（受审批流程约束）。
        [3]. 分类管理：支持多维标签/领域 Taxonomy、PII 标记与自动/人工标签合并策略。
        [4]. 统计与健康度：统计项包括解析成功率、解析错误分布、重复率、低质量样本百分比、平均切片长度、索引延迟等。
        [5]. 并发与一致性：定义强一致性写入/并发冲突策略（乐观锁 + 合并策略）。
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
        [1]. 支持多 embedding 后端：bge-m3（本地）为默认，同时支持云端 embedding 作为可选后备并实现 embedding 版本化（embedding_version 元数据）。
        [2]. Embedding 策略：支持 ensemble / hybrid embeddings（对同一切片可存多版本 embedding 并在检索时选择最优或合并相似度评分）。
        [3]. 自动重嵌入与触发条件：当检索质量下降、语料量突增或 embedding 引擎升级时支持批量/增量重嵌入任务，并记录可回滚版本。
        [4]. 元数据要求：每个 embedding 记录 embedding_version、model_name、参数、生成时间戳与哈希。
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
        [1]. 索引与检索策略：默认 hybrid（sparse BM25 + dense vector）；支持可插拔的向量库（FAISS/Weaviate/Annoy/SQLite+vector）与 DB-adapter 抽象层。
        [2]. Chunking 策略：定义语义切片长度、边界保留与重叠率（默认 200–800 tokens，overlap 20%），并支持按文档类型差异化设置。
        [3]. 二级 reranker：在召回后可配置 cross-encoder rerank 以提高精确度；所有 rerank 结果写入统计以便长期评价。
        [4]. 批量索引：支持增量索引、幂等操作与并行批处理，并在元数据中写入索引作业 ID 与状态。
        [5]. 检索统计：记录召回率@k、平均相似度、rerank 精确率、查询延迟分布与索引延迟。
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
        [1]. 摘要契约：定义摘要 Schema（summary_text, key_entities[], topics[], confidence_score, model_meta{model, prompt_version}）。
        [2]. 摘要质量评估器：使用信息熵、关键实体覆盖率、QA-based fidelity 检测摘要质量；低质量摘要自动触发多模型投票、HyDE 或人工复核。
        [3]. 摘要存储与可追溯性：摘要写入元数据库同时保存源切片映射（slice_id 列表）、生成参数与时间戳，摘要亦参与向量索引并记录 embedding_version。
        [4]. 成本控制：摘要批量生成需遵循 token 预算与速率限制策略（任务级/日级阈值）。
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
        [1]. 两层检索流程定义：Stage-1 摘要/主题检索（快速），Stage-2 精细切片检索；在两层之间运行一致性仲裁器以校验摘要与切片一致性。
        [2]. 仲裁器策略：使用 cross-encoder/实体对齐检测摘要→切片冲突；冲突时触发多文档并行回溯或人工 Checkpoint。
        [3]. 局部回退机制：当 Stage-1 置信度低于任务敏感阈值时直接跳过 Stage-1 进入全量检索。
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
        [1]. Citation Schema：slice_id, doc_id, byte_range, parse_version, embedding_version, timestamp, confidence_score。
        [2]. 生成产物必须伴随引用映射表（可在报告/前端中点击回溯），并在证据包中包含原始片段高亮与置信度注释。
        [3]. 引文时效性检查：若引用数据距今超过设置时效（可配置，默认12个月）需标注“数据陈旧”。
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
        [1]. 阈值机制：支持任务敏感阈值映射表（不同任务/行业/语言可有不同阈值）；阈值基于历史召回分布自动校准。
        [2]. 数据不足时动作库：可选动作包括：① 扩展检索（增加外部数据源白名单）；② 发起数据补采任务并通知相关责任人；③ 返回带不确定性声明的草稿（并标注缺失维度）；④ 人工审阅队列。
        [3]. 事件记录：所有“数据不足”事件写入演进日志以供 Prompt Optimizer 与数据采集策略参考。
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
        [1]. StrategicArchive Schema：{ plan_id, period, key_assumptions[], decision_points[], kpi_baseline[], execution_results[], deviation_matrix[], correction_snapshots[] }；并对每条记录进行版本化与审计标记。
        [2]. 修正日志→Few-Shot 转化受控流程：自动提取高频修正模式，进入待审版本库，经人工/域验证器批准后写入 Prompt Library（并标注来源与生效版本）。
        [3]. 周期性蒸馏与去重：每季度触发知识蒸馏任务（去重、合并、优先级打标），并生成 Strat-Bench 的扩增样本。
        [4]. 隐私与合规：长期档案支持 PII 掩码/脱敏选项与访问控制审计。
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
        [1]. 数据质量指标集：解析成功率、OCR WER、关键实体覆盖率、语义重复率（embedding similarity clustering）、解析延迟等，并将分数写入元数据。
        [2]. 重复检测：结合 SimHash（字面重复）与 embedding-based clustering（语义重复），低置信重复进入人工合并队列。
        [3]. 质量分等级与阈值：自动分等级（A/B/C）并对 C 级样本做人工复核或直接隔离以免污染索引。
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量
        [1]. 监控指标：召回@k、rerank 精确度、摘要 QA-fidelity、生成事实一致率、平均响应延迟、索引延迟、embedding drift 指标。
        [2]. 漂移检测策略：月度 KS 检验（分布偏移）+ CUSUM（短期趋势），并在超过阈值时自动触发根因分析（关联日志、索引变更、模型更新）。
        [3]. 回归与门禁：维护回归测试集（Strat-Bench 子集），任何模型/embedding 升级必须通过契约测试（通过率≥预设阈值）方可上线；失败自动回滚并写入演进日志。
        [4]. 可视化与告警：支持仪表盘展示历史基线、漂移事件、并支持多级告警（黄/红/停用）与告警单追踪。
}
O2
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif；并扩展支持 eml/mbox, html, zip/tar（含内嵌文档解压与批量摄取）、json/xml/odt/ods、mp3/wav/ogg、mp4 等。必须具备自动语言/编码检测、多语言 OCR 回退链（本地 Tesseract 快速 OCR -> 商业 OCR 深度回退）与媒体关键帧提取能力。支持大文件流式分片上传与增量索引接口。
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息。并强制实现：
        [1].版本控制与差分记录（每次写入产生版本号 + 内容哈希 + 变更 delta），支持一键回滚与批量幂等操作；
        [2].分级删除策略（软删除->人工核验->硬删除）与不可篡改审计日志（WORM）；
        [3].细粒度访问控制与审计（RBAC/ABAC），并对外暴露批量运维 API（并行化上传/回滚/健康检查）。
    3.使用可插拔的词嵌入适配层构建完整的RAG流水线（默认可配置为 bge-m3 本地嵌入）。嵌入适配层必须支持：模型版本管理、混合/ensemble 嵌入、量化/压缩策略、异步批量嵌入、向量回放（re-embed）任务及嵌入权限/密钥管理。监控 embedding drift（cosine 分布监控与 KS-test），并支持自动回滚或触发重建索引流程。
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息。要求：
        [1].定义索引策略模板（chunk 长度/overlap/切片策略）并支持基于文档类型的自动策略选择；
        [2].向量存储抽象层（可切换 FAISS/HNSW/PGVector/Weaviate/Chroma 等），提供事务化批量索引、幂等性与检查点；
        [3].提供检索统计指标：P@k, recall@k, avg_score, latency，并定期回测索引质量。
    5.支持调用网络大模型 API 与本地模型混合生成结构化摘要（可配置摘要档位：短/中/长/结构化JSON），摘要须包含：引用片段ID列表、片段偏移、置信度与实体覆盖率指标。摘要写入元数据库后再进行词嵌入并保存向量库元数据。摘要生成流程须支持多模型 ensemble 策略、成本熔断与本地降级策略。
    6.支持分层检索策略（summary-first -> slice-second）：先检索文档摘要锁定候选集，再检索具体切片。需实现：
        [1].分层阈值与回退逻辑（若 summary 层得分/覆盖不足则自动扩展或触发人工补检索）；
        [2].在 summary 层与 slice 层之间保持一致的来源链路（summary->slice IDs->offsets）；
        [3].并行/异步检索机制与延迟/成本监控。
    7.支持检索处理时保留结构化引文索引，确保后续生成的内容可溯源至具体文档片段。引用元数据规范包括：doc_id, slice_id, byte_offset_start, byte_offset_end, sentence_hash, confidence。冲突来源需生成“证据差异摘要”并标注冲突级别。
    8.支持 Self-RAG 与质量门机制：检索相关性由“多维置信度”评估（语义相似度 + 覆盖率 + 摘要一致性）；若综合置信低于可配置阈值，则标记为 "数据不足" 并：
        [1].自动生成补检索建议（缺失文档类型/关键字）并支持一键扩展检索/人工上传；或
        [2].在用户授权下提供“灰度推测模式”（返回带显著低置信注记的推测结果），并把该交互作为训练样本写入修正轨迹。
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年 SP/BP 的关键假设变量、决策依据与实际执行结果的差值，要求：
        [1].定义 `Strategic_Record` Schema（假设变量、时间窗、证据片段ID、量化差值、修正轨迹）；
        [2].演进固化流程须包含：脱敏/审查/审批（知识治理）、去重、版本化与冷热分层（长期记忆 vs archive）；
        [3].将用户修正日志反向固化为少样本提示词时，必须对敏感信息进行掩码/差分隐私处理，并记录来源与审计条目。
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（保留 SIMHash 做快速近似检测），并扩展：
        [1].添加语义重复检测（embedding similarity + cluster），实体级去重（NER 哈希）与 OCR_confidence 校验；
        [2].为每个文档/切片生成 `quality_score`（解析成功、OCR_confidence、duplicate_flag、lang_confidence等）；低质量进入“疑难件工单队列”（人工或半自动修正）；
        [3].将 QC 指标纳入索引质量门，未通过门的内容禁止进入生产向量库或需人工放行。
    11.建立模型性能监控与漂移检测机制，定期评估 RAG 检索准确率和摘要生成质量。要求：
        [1].定义监控指标：检索 P@k、recall@k、avg_cosine、摘要覆盖率、实体错误率、响应延时、成本/Token 等；并支持自定义 SLO；
        [2].使用双算法漂移检测：embedding 分布 KS-test + 时间序列 CUSUM 对关键质量指标；定义三级响应（告警→自动回滚/触发重构索引/触发重训练→人工介入）；
        [3].集成回归测试（Strat-Bench）与 CI/CD 契约门禁，失败自动阻断上线并生成差异分析报告。
    12.工程与合规：
        [1].对所有存储数据实行加密（静态与传输中），并提供密钥管理（KMS）集成与审计日志；
        [2].支持私有化/本地部署与云部署两种模式，提供部署白皮书与安全硬化清单（网络隔离、白名单 API 网关）；
        [3].定义数据保留/删除策略并支持合规删除（如 GDPR）；对被固化为提示词或长期记忆的条目提供可追溯的审批/删除路径；
        [4].为运维和 SRE 提供健康度仪表盘（摄取延迟、解析错误率、索引失败率、检索质量）与报警订阅机制。
    13.交付与可观测性：
        [1].所有处理步骤（摄取/解析/摘要/嵌入/索引/检索/生成）必须产出可检索的证据包（含输入摘要哈希、处理链路、工具版本、关键中间变量、置信度评分与源切片 ID 列表）；
        [2].证据包应能与工具箱/AGENT 的溯源树对接，支持从结论向源数据逐层回溯（deep-link）。
    14.性能与成本控制：
        [1].在批处理与实时路径均实现成本预估（估算 Token/API 调用成本），并在任务启动时提示及支持用户确认；  
        [2].实现成本熔断（任务级/会话级/系统级），并将成本使用纳入任务证据包以便审计与优化。
}
O3
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
       - 扩展格式：同时支持 eml/mbox/html/json/xml/odt/rtf/odf、zip 包内批量导入、音频（mp3,wav）与视频（mp4）待转录轨迹，支持多语种与编码自动识别与规范化。
       - 每个上传文件须记录原始 MIME、来源 URL / 上传用户、文件哈希（sha256）、抓取/上传时间及版权/许可元字段（license, provider_id）。
       - 支持扫描 PDF 与图片的 OCR 抽取能力（可配置 OCR 引擎：如 Tesseract/商业 OCR），并记录 OCR 置信度；若置信度 < 0.85 则标记为“待人工校验”。
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
       - 强制版本化：文档每次更新保留不可篡改快照（snapshot），并存储 diff / 改动元数据；支持基于版本的回滚与审计。
       - 支持软删除/强删除策略与合规保留期（retain policy）；删除操作生成不可篡改的审计条目（WORM 或等效技术）。
       - 支持 RBAC（基于角色与数据标签的访问控制）与细粒度权限（读取/注释/导出/索引权）。
       - 支持文档分类与标签体系（可自定义 taxonomy），并提供文档质量统计（完好率、OCR 通过率、PII 检出率）。
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
       - 默认使用 bge-m3 作为首选 embedding，但实现“嵌入适配器”接口（pluggable embedding adapters），可无缝切换至其他 embedding 模型/版本（并记录模型哈希与版本）。
       - 为嵌入生成记录 embedding 参数（normalize/float32/int8）、向量维度、生成时间与模型版本；实现 embedding 版本回滚与 AB 测试能力。
       - 支持 embedding drift 监控（均值/方差/相似度分布）并提供自动化告警与回退策略。
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
       - 支持 hybrid search（vector + BM25）与可插拔 re-ranker（可配置 ranker pipeline）；支持 index atomic batch 操作与回滚。
       - 明确定义切片策略模板（chunk size, overlap, semantic boundary rules），并在索引元数据中持久化 chunk->doc 映射。
       - 提供检索和索引指标：recall@k、MRR、subquestion_coverage、latency_p99、index_size、index_build_time。
       - 索引治理：索引版本、回滚、差异化增量索引与索引一致性检查（index checksum）。
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
       - 摘要输出必须遵守契约化 Schema（JSON schema），包含字段：summary_text, summary_hash, key_entities[], coverage_score, fact_consistency_score, generation_model{vendor,version}, generation_cost_usd, timestamp。
       - 自动执行摘要质量评估器：信息熵、覆盖率、事实一致性检测（可配置阈值）；若质量低于阈值，触发二次生成/本地模型fallback或人工复核流程。
       - 摘要与原文切片建立可验证引用（docID, chunkID, char_offset, snippet_hash）并写入证据包（evidence bundle）。
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
       - 明确定义分层流程参数：摘要 top_N → 切片 top_M 映射规则（默认 top_N=10, top_M=50，可配置）；并持久化摘要→切片映射表用于溯源与再现。
       - 对分层检索链路计算联合置信度（摘要_confidence × slice_confidence），并基于联合置信度决定是否触发 Self-RAG 或人工介入。
       - 在分层检索中支持语义 re-ranking 与多策略并行（并行调用不同索引/向量库以提高覆盖）。
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
       - 引文粒度标准：docID + chunkID + char_offset + snippet_hash；输出引用需包含来源元信息（provider, license, fetch_date）与展示型引用片段（≤300字符）。
       - 若引用来源受版权/许可限制，自动标注并阻断直接复制；提供授权申请或人工替代方案（summary only）。
       - 输出端支持机器可校验的引用三元组（docID, chunkID, snippetHash）与可跳转的原文定位链接（若可用）。
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
       - 将单一阈值扩展为**多维置信度组合**（检索相似度、摘要覆盖率、事实一致性），并允许任务级/工具级自定义阈值策略。
       - 在"数据不足"情形提供多种响应策略：①提示用户补数据并建议优先补源；②自动触发 agent 补检（受白名单限制）；③允许生成草稿但强制显著标注“低置信度/仅供参考”；④记录拒绝原因到修正轨迹库并排序为数据采集优先级。
       - 所有“拒绝”与“低置信”决策写入证据包用于事后审计与模型改进。
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
       - 定义战略档案 Schema（fields: plan_id, period, key_assumptions[], decision_reasons[], execution_results{metrics,time_series}, deviation_analysis, correction_logs[]，confidence_score, reviewer_signoff）。
       - 修正固化策略：仅在满足“审阅签名”或达到高频修正模式阈值时自动固化；自动去重与知识蒸馏（quarterly distillation pipeline），并记录变更差异与方法论一致性校验。
       - 长期记忆支持版本化检索、权限控制与可删除（数据主体权利）处理；支持定期知识蒸馏与去重（quarterly）并保留审计证据。
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
       - 采用多层质量检查：文件哈希校验 + SIMHash 文本近似 + 向量相似度去重 + 表格/财务一致性校验（列/行/总计/单位一致性）。
       - 对 OCR/图片/表格输出记录置信度并在低置信度触发人工抽检；对表格抽取实现列类型推断与一致性规则验证（如金额列单位规范化）。
       - 对异常（截断/乱码/不完整）文件自动分类到“异常处理队列”并通知上传方或数据工程师。
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量
       - 指标集合与频率：检索（recall@k, MRR, latency_p95 daily）、摘要（coverage_score, fact_consistency_rate, rouge/bleu proxies weekly）、生成（hallucination_rate, correction_rate monthly）；embedding drift（distributional shift daily）。
       - 漂移检测算法：KS检验 + CUSUM（滑动窗口 7 天）并配套根因分析 playbook（自动收集失败 case，触发 A/B 回归测试或回滚）。
       - 当关键指标越过阈值时触发分级告警（黄/红/停用），并自动保存当时的 system prompt、model version、index snapshot 以便回溯与回滚。
       - 性能监控面板与可导出报告，支持按任务/客户/时间区间下钻。
    12.（附加）合规与隐私防护（横向补充）
       - 上传前执行 PII 探测与脱敏策略（可配置：mask/replace/redirect_to_human）；对敏感字段提供加密-at-rest 与字段级访问控制。
       - 所有生成结果与引用写入不可篡改审计日志（WORM），满足 SOX/ISO27001 风险合规需求；支持以审计友好格式导出（时间/角色/任务维度）。
    13.（附加）成本与版本治理（横向补充）
       - 在数据处理与摘要生成阶段记录预计/实际 API cost 与 token 消耗，任务完成后产出成本审计信息（cost_usd, tokens_used）。
       - 所有关键构件（embedding model、index、summarizer、re-ranker）均纳入版本管理并记录版本哈希、发布时间与回滚机制。
}
O4
{
企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1. 支持的文档格式包括 pdf, txt, doc, docx, ppt, pptx, xls, xlsx, csv, jpeg, png, gif（并预留扩展位：audio(m4a,wav), video(mp4)）
        - 为每一格式定义标准解析器（parser id/version），并记录解析元日志（parser_id, parser_version, parse_time, confidence）。
        - 图像/扫描件需走 OCR 子流水线并保存 OCR 置信度；表格类文件需走结构化表格抽取并产出表格 schema。
        - 支持语言检测与编码修复（utf-8 normalization），并对多语种文档走相应语言管线。
    2. 支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
        - 所有文档操作支持版本化（document_version）与事务一致性语义（写入→触发索引任务，保证幂等）。
        - 支持软删除与回滚；操作写入不可篡改审计日志（WORM），并记录变更快照（who/when/what）。
        - 并发冲突使用乐观锁/版本号或 MVCC 处理，支持按项目或租户隔离。
    3. Embedding 与 RAG 流水线采用可插拔模型策略层（默认本地 bge-m3），支持按文档类型/语言/任务路由到替代模型（私有云/公有 API）
        - 保存 embedding metadata（model_name, model_version, embedding_version, generation_time）。
        - 支持 embedding 的版本管理与回滚（vector_version），并提供 embedding drift 检测（平均相似度/语义距离趋势）。
        - 对外调用云端嵌入/摘要接口需走成本预算与熔断策略（缓存常见摘要与 embedding，避免重复调用）。
    4. 支持完整的 RAG 操作功能，包括元数据、单文档索引、增量索引与批量索引、语义查询、上下文检索、检索统计信息
        - 支持 Hybrid Search（向量相似度 + 关键词 + 元数据过滤），并支持多种检索器后端（Milvus/Qdrant/Pinecone/pgvector/Weaviate 等可配置）。
        - 定义索引分层策略（例如：摘要索引层、切片索引层、长期档案索引），并提供索引生命周期管理（冷存/热存/重建计划）。
        - 提供增量/重建 API：单文件增量索引、批量离线索引、并记录索引作业元数据（job_id, start/end, status, errors）。
    5. 支持调用网络大模型 API 接口生成结构化摘要（长度与风格根据文档类型/任务动态调整），摘要存入元数据库，词嵌入后再存入向量库元数据
        - 摘要生成采用“动态摘要策略”：根据文档长度/类型/用户指令动态决定摘要长度与模板（可配置  short/medium/long）。
        - 每次摘要生成必须经过质量评估器（信息熵、实体覆盖率、关键主题覆盖），低质量摘要标注并触发重生或人工校验。
        - 摘要保存需包含 provenance（原始doc_id, slice_id, model_name/version, prompt_hash, quality_score），摘要与原始切片保持双向引用。
    6. 支持分层检索策略（先检索文档摘要锁定范围，再检索具体切片），并实现置信度聚合与降级策略
        - 明确定义分层阈值与 Top-N 参数（第一层摘要 top-N、第二层切片 top-M）以及置信度计算方式。
        - 当第一层/第二层置信度低于阈值时，自动触发二次检索策略（query expansion/HyDE、外部白名单数据源）或进入人工验证队列。
        - 分层检索结果需返回整体置信度与每条证据的 provenance 信息，便于前端溯源展示。
    7. 支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
        - 证据指针采用统一规范（document_id:file_path:page:char_start:char_end 或 slice_id），并在向量元数据中保存 provenance 字段（source_hash, parser_version, ocr_confidence）。
        - 输出生成结果时必须附带引用清单（引用切片ID、置信度、原文片段预览、可点击回溯链接）。
        - 支持导出可审计的证据包（evidence_package）用于合规审计。
    8. 支持 Self-RAG 机制：检索相关性低于可配置阈值（默认 0.6）时标记为 "数据不足" 并触发降级流程，避免强行生成
        - 阈值为可配置且可按任务类型/领域自适应调整；记录每次触发原因与处理链路。
        - 降级流程包括：二次扩展检索、调用外部白名单源、人工审查/回退或明确返回“无法回答”并建议相关文档/行动。
        - 将 Self-RAG 的触发统计纳入监控面板，以评估数据覆盖率与知识缺口。
    9. 构建战略档案库作为长期记忆模块
        - 结构化存储历年 SP/BP 的关键假设变量、决策依据、执行结果与差值（schema 明确：assumption_id, assumption_text, decision_evidence, expected_metric, actual_metric, delta, timestamp）。
        - 用户修正日志（修正前/后、角色、置信度）可反向固化为少样本提示词（prompt snippets），进入受控演化流水线（离线验证→契约测试→人工批准→上线）。
        - 长期档案实现版本化管理、敏感信息分级并支持脱敏视图；每季度触发知识蒸馏/去重并产出演化报告。
    10. 支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（扩展 SimHash）
        - 预处理质量门包含：格式验证、语言检测、编码修复、OCR 置信度门、PII/敏感信息检测、内容完整性校验。
        - 重复检测采用多层方法：SimHash（快速）、MinHash（集合相似）、semantic similarity（embedding 距离）三数据合并判定，支持自定义阈值与人工复核。
        - 不合格文档进入异常队列并生成预处理报告供人工修正；保留处理轨迹用于改进 parser 与规则。
    11. 建立模型性能监控与漂移检测机制，定期评估 RAG 检索准确率和摘要生成质量
        - 指标体系：检索召回@K、检索精确率、平均 embedding 相似度、摘要覆盖率（关键实体覆盖%）、引用可追溯率、幻觉率（人工抽样标注）、处理时延与成本（API token/$）。
        - 漂移检测：KS/CUSUM 等算法检测分布与指标漂移；异常触发自动根因分析（关联日志/模型版本/数据来源变更）。
        - 建立自动化告警与治理：指标下降触发（1）模型回退，（2）索引重建，（3）拉取人工干预；并保留详细的根因与修复记录。
    12. （新增）安全、访问控制与合规要求嵌入数据处理模块
        - 数据入库与索引流程遵循最小权限原则（RBAC），敏感数据分类并自动脱敏或加密存储（at-rest 与 in-transit）。
        - 对外模型调用（cloud API）需通过白名单与审计；对高敏感内容强制本地处理或拒绝外发。
        - 所有数据处理操作与生成输出均写入不可篡改审计日志（支持按时间/角色/任务类型检索），满足合规审计需求（SOX/ISO27001 类）。
    13. （新增）可观测性、成本与运维接口
        - 暴露监控和度量 API（Prometheus / Grafana 指标），包含索引延迟、检索延迟、摘要生成成本、embedding 存储量等。
        - 成本控制策略：预估任务 Token/调用成本；超过预算触发熔断/人工确认；支持任务级/会话级/系统级成本熔断策略。
        - 提供运维工具：索引一致性检查、向量校验工具、索引重建向导与离线回放（replay）机制。
}
O5
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1. 支持的文档格式包括 pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
        [1]. 明确各格式的预处理链：文本格式直接解析；office 文档（docx/xlsx/pptx）使用结构化解析器保留段落/表格/图表元信息；xls/xlsx/csv 必须保留单元格坐标与数据类型；图片格式（jpeg/png/gif）通过指定 OCR 引擎（带语言/字体配置）抽取文本与表格。所有解析失败的文件应进入“人工 QA”队列并标注失败原因与 SLA（默认 24 小时）。
        [2]. 对大文件与二进制或加密文件定义退化策略（如拆分/转换/人工介入），并记录 immutable snapshot id（file_id, file_version）。
    2. 支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
        [1]. 所有文档操作具备版本化与事务保障：每次上传/更新生成不可变快照（snapshot_id），索引更新采用两阶段提交（先写元数据库，再更新向量库）；支持回滚接口与 WORM 审计日志（用于合规/法务）。
        [2]. 支持权限化访问控制与多租户隔离（RBAC），对敏感文档自动触发脱敏/差异化访问策略。
    3. 使用本地词嵌入模型 bge-m3 构建完整的 RAG 流水线
        [1]. 采用主 embedding=bge-m3 + 行业/语言 adapter 策略（多-encoder），并在 embedding 元数据保存 model_id、version、eval_score 与训练/校准时间。
        [2]. 定义 embedding 更新/回滚策略：每次模型替换需通过基准评估（Strat-Bench）并提供对照报告（前后差异、回归测试）。
    4. 支持完整的 RAG 操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
        [1]. 规范索引切片策略：默认切片长度 500–1000 tokens，overlap 50–200 tokens（可参数化）；存储 slice_id、slice_hash、byte_offset 与原 file_version。
        [2]. 实现检索评分标准与归一化方法（score normalization），并提供 Top-K、MMR（可选）等检索策略切换配置。支持检索缓存与冷热向量分层，定义向量库 SLA（99x% 查询成功率/延迟目标）。
    5. 支持调用网络大模型 API 接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
        [1]. 摘要生成必须走质量评估器（摘要_quality = f(information_entropy, entity_coverage, domain_key_items)）。若质量低于阈值（默认 0.7），启动本地回退模型或人工校对流程，并记录摘要生成 model_id 与 response_hash。
        [2]. 摘要元数据必须包含 source_slice_ids 列表、生成时间、model_id、confidence_score、evaluation_metrics。
    6. 支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
        [1]. 明确分层参数（摘要层 top-n、扩展阈值 delta）；同时实现“并行粗检+精检”合并策略以避免召回盲区。
        [2]. 添加分层检索的自动扩展逻辑：当摘要层覆盖率或召回低时，自动扩展到邻近摘要或直接全文浅检。
    7. 支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
        [1]. 引入标准化证据单元 schema：{file_id, file_version, slice_id, slice_hash, offset_start, offset_end, retrieval_score, confidence}；所有生成产出必须附带 evidence_list 并支持前端点击回溯到原始片段。
    8. 支持 Self-RAG 机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
        [1]. 阈值采用任务/领域自适化策略（基于历史性能），并定义补救路径：扩展检索、外部知识库联查、本地微调或人工路由；最终仍不足时返回标准化“数据不足”响应并给出建议动作（上传补充文件 / 人工评审）。
    9. 构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年 SP/BP 的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
        [1]. 长期记忆应具有去噪/精炼管线：周期性知识蒸馏（quarterly）、去重（content_hash dedupe）、可信度打分（仅 high-confidence/high-frequency 修正固化）；修正日志匿名化与敏感字段脱敏策略强制执行。
        [2]. 支持知识版本化、回溯对比（diff）与自动化定期审计（老旧假设超 12 个月自动标记“陈旧”）。
    10. 支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
        [1]. 建立数据质量 KPI 与仪表盘（parse_success_rate、duplicate_rate、schema_match_rate 等），并对异常样本自动打单、入 QA 队列；重复检测采用多算法级联（SIMHash + semantic dedupe）。
    11. 建立模型性能监控与漂移检测机制，定期评估 RAG 检索准确率和摘要生成质量
        [1]. 指标体系：precision@k、evidence_coverage、summary_entity_coverage、hallucination_rate；监控采样频率（默认每日样本 1k / 周回归测试）。采用 KS/CUSUM 等漂移检测算法，触发 root-cause pipeline（自动关联日志、样本保存、回滚或拉入人工审查）。
        [2]. 将评估结果纳入模型/embedding 的版本门禁（若新版本 regress 超阈则自动回滚并生成差异报告）。
}

-------------------


D Q2 Q3
Q O3 G5
G Q5 Q5
O G5 G3

Q2 Q3 Q5 G3 G5 O3


## 6融合Q2 Q3 Q5 G3 G5 O3


你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。下面5个清单是针对[企业战略规划管理智库系统需求特性列表]->[数据处理]需求特性的重构，请你结合[企业战略规划管理智库系统需求特性列表]整体系统需求，对这些需求列表内容进行评估。要求：
1、逐条对比评估优劣，提出适合本系统的改进建议，给出该建议的必要性分析和可行性分析
2、评分维度：
   - 多模态解析与原子化溯源
   - 混合检索架构与图谱增强
   - 强制契约化与数据质量门禁
   - Agentic RAG与自适应纠偏
   - 知识演进与反馈闭环
   - 全链路可观测性与漂移检测
   - 本地优先与数据主权隔离
   - 企业级合规与审计追踪
   - 互操作性与生态扩展
   - 成本工程与性能优化
   - 开发者体验与运维支持
   - 创新前瞻性
   - 安全与鲁棒性
   - 性能与监控
   - 企业级适用性
   - 工程可实施性
3、严格从[客观视角]进行评估
4、以表格形式呈现，表头：评估维度---Q2评估---Q3评估---Q5评估---G3评估---G5评估---O3评估---改进建议 (适用于本系统)---必要性分析---必要性评分 (5分制)---可行性分析---可行性评分 (5分制)

-------------------

企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：支持基于网络LLM和先进Agent技术的智能分析工具集，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理（共计17个）。
    1.智能工具定义
        [1].建立工具标准：每个工具有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。工具初始化必须加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
        [2].构建中央技能仓库：实现工具的统一注册、版本控制、灰度发布与回滚机制。所有入库工具必须通过Pydantic V2契约测试，后续迭代需通过回归测试集。支持预定义模板与远期基于代码生成的技能动态扩展。
        [3].支持工具链编排与依赖调度：支持定义工具链有向无环图，实现上游工具输出结构化流转为下游输入。内置任务调度器，根据依赖关系自动并行执行无依赖子任务。
    2.工具箱标准工作流
        [1].环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具价值观模板（SOUL.md），实例化会话级沙箱与混合检索记忆容器。
        [2].感知与摘要：读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
        [3].规划：Planner依据用户自然语言指令及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、匹配工具映射、数据依赖边。
        [4].执行与验证：Executor按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe→writeback原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
        [5].证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
        [6].反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
        [7].成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
        [8].演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。
    3.数值计算与沙箱执行
        [1].强制代码执行隔离：严禁LLM直接进行数值计算、统计回归或现金流折现。所有量化分析强制通过两条路径执行：一是生成Python代码在沙箱执行；二是针对战略运筹问题（如线性规划），由LLM构建数学模型调用CVXPY、OR-Tools等优化器求解。
        [2].构建持久化计算沙箱：为每个任务会话实例化持久化Jupyter Kernel，支持跨工具、跨步骤的变量传递（如宏观经济变量在不同模型间共享），实现分析上下文连贯。任务结束后自动销毁内核并回收资源。
        [3].支持Validation Feedback闭环：自动捕获代码执行STDERR，检索错误案例库辅助LLM生成修复版本。设定最大重试次数（默认3次），失败则标记不可行并记录至演进日志。
    4.Schema强制与一致性校验
        [1].实施契约化输出：所有工具输入/输出严格遵循Pydantic V2定义的Data Model，利用Instructor Patch强制LLM输出符合Schema的结构化JSON，严禁输出自由文本。
        [2].建立一致性校验仲裁器：在多工具协作中，通过内置规则引擎及财务常识库检测逻辑冲突（如利润率与成本矛盾）。输出冲突检测报告，供用户或高级AGENT裁决。
        [3].自动化契约测试：在CI/CD流水线集成Schema契约测试，防止模型漂移导致结构崩溃。契约测试失败的任务强制终止。
    5.可解释性与决策溯源
        [1].输出结构化推理证据包：每次分析必须打包输出全量元数据，包括输入摘要哈希、Planner任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片ID）。
        [2].构建交互式溯源树：支持前端从结论反向逐层展开证据包，展示“数据来源→计算逻辑→中间结论→最终结论”的完整推理链。
        [3].支持红蓝辩论机制：针对高不确定性议题自动启动“红蓝对抗”，实例化激进派与保守派子智能体进行结构化辩论，输出包含共识与分歧区域的风险全景视图。
    6.提示词工程与演进
        [1].Prompt Optimizer：基于DSPy理念，建立“修正日志-样本转换”闭环，将用户修正转化为Few-Shot样本，自动迭代提示词参数以适配企业战略风格。
        [2].实施受控自动演进：仅在SP/BP Checkpoint完成态且满足一致性阈值时触发演进。必须采用多目标优化（结构、一致性、成本），严禁单一指标优化。
        [3].确保方法论不变性：演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
        [4].支持版本管理与回滚：所有Prompt纳入Git LFS管理，与系统版本绑定。支持一键回滚至历史稳定版本。
    7.用户反馈与闭环演进
        [1].支持Checkpoint人工介入：在关键决策节点或低置信度环节自动暂停，请求用户验证中间假设。支持用户修改参数或调整权重，系统记录修正前后的“专家修正轨迹”。
        [2].建立修正轨迹库与逆向传播：将用户修正快照匿名化存储，定期分析差异，推断输入权重或任务分解策略的优化方向。
        [3].实施用户反馈协议：将用户反馈转化为结构化修正日志，强制触发工具体“反思阶段”（ReflAct），并回传至技能演进日志。
    8.安全与防御机制
        [1].实施沙箱网络隔离：代码执行环境默认断网，仅允许通过白名单网关访问预设的可信财经API，严禁通用网页爬虫。
        [2].建立提示词注入防御：输入数据前置执行清洗与注入检测。将“注入防御测试”纳入基准测试集，定期验证工具体的抗干扰能力。
        [3].建立工具幻觉黑名单：自动监控工具体输出，若连续3次生成与输入矛盾的结论，自动锁定该工具体并报警。
    9.领域专业知识嵌入
        [1].内置领域事实库：以JSON Schema存储经典管理理论的强制约束（如SWOT内部/外部属性分类），确保分析逻辑符合理论框架。
        [2].部署领域验证器：工具体输出前必须经过验证器检查，确保结论不违背基础商业逻辑与领域常识。
    10.服务契约与集成
        [1].独立微服务封装：工具箱作为独立微服务对外暴露OpenAPI 3.1标准接口，输出分析结果、证据包与成本审计信息三元组。
        [2].支持Agent模块集成：与CEO、CFO等核心AGENT通过服务调用无缝集成，支持水平扩展与分布式Trace追踪。
    11.测试与质量保证
        [1].构建基准测试集（Strat-Bench）：建立包含20+专家标注场景的战略分析专用测试集，作为版本迭代的准入测试标准。
        [2].支持A/B测试框架：支持多版本策略并行运行，基于任务完成率、计算精度与成本指标择优晋级。
        [3].部署健康度仪表盘：实时监控工具成功率与修正率，修正率超标（默认15%）自动触发Skill Review。
    12.性能监控与漂移检测
        [1].建立性能漂移检测：定期评估检索准确率与生成质量，对比历史基线检测性能下降。
        [2].支持可视化日志：所有评估与运行结果纳入系统行为日志并可视化展示。
    13.成本熔断与契约测试质量门禁
        [1].CI/CD契约测试门禁：所有工具输出必须通过严格Schema校验，非兼容变更触发构建失败。运行时连续三次校验失败触发工具熔断。
        [2].实施三级成本熔断：支持任务级（单次Token/成本超限）、会话级（日配额超限）与重试级（连续代码执行失败）熔断机制，确保系统运行成本可控。

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1. 输入处理与全景构建
        [1]. 输入系统预处理的结构化JSON数据，AGENT首先调用摘要质量评估器（基于信息熵与关键实体覆盖率），若摘要质量评分低于阈值（默认0.7）则自动触发二次检索补充缺失维度，确保全景视图的完整性。
        [2]. 构建动态全景视图：宏观层（战略指标趋势/异常波动/关键假设变量）与微观层（文档切片引用索引）双向关联，支持细节挖掘过程中发现与全景视图严重矛盾时自动触发视图重构，重构过程需记录变更轨迹并提交AUD AGENT审核。
        [3]. 实施上下文窗口动态管理：基于任务复杂度自适应分配Token预算，超限时触发上下文压缩器（基于关键实体抽取与HyDE算法），压缩率目标≥70%，确保关键信息不丢失。
    2. 任务分解与协作架构
        [1]. 建立任务IR（中间表示）标准：每个任务包含任务ID、类型、输入/输出Schema、依赖任务列表、优先级、执行状态、超时策略。所有协作任务必须基于有向无环图（DAG）构建流转拓扑，确保多体协作具备确定性的状态机流转特征。
        [2]. 采用分层协作架构：CEO AGENT作为指挥官负责目标解析与全局任务分解，各专业Agent并行执行子任务，调度器负责资源分配与执行监控。自动生成协作依赖图，基于BLM/BEM阶段定义标准依赖关系，支持动态调整与优化，可视化展示任务间数据流与控制流。
        [3]. 支持柔性视角隔离：定义核心视角（必须遵守）和扩展视角（可协商），对跨领域问题组建临时联合分析组，建立视角冲突解决协议。视角隔离通过Prompt隔离、工具隔离、数据隔离三重硬约束实现。
    3. AGENT确定性工作流
        [1]. 基于状态图的工作流引擎：采用StateGraph机制定义显式状态模型，包含session_id、current_phase、agent_role、working_memory、long_term_memory_ref、tools_used、confidence_score、cost_accumulated等字段。工作流循环定义为：[感知]环境初始化与上下文加载→[规划]生成任务执行DAG→[执行]按拓扑顺序调度子任务→[深度思考]（可选）在关键决策节点并行生成多条思维链推演路径→[验证]结构化检查输出→[反思]错误分析与修正→[闭环]Early Stopping机制（置信度达标且目标达成时提前终止）。
        [2]. 深度思考与多路径推演：在SP/BP所有核心分叉点（如商业模式选择、核心竞争力定义），默认调用支持深层推理的模型，并行生成多条思维链推演路径，通过内部验证打分选出最优解，极大提升战略输出的逻辑厚度。
        [3]. 中断安全与状态持久化：任意节点支持用户中断，自动保存StateSnapshot至Redis Cluster（三节点哨兵模式），恢复时加载完整上下文与工具状态，支持从任意历史快照恢复执行（Time-travel能力分步实现）。
    4. 用户反馈闭环
        [1]. 建立反馈标准化协议：将用户反馈转化为结构化修正日志（包含修正类型enum[参数/权重/约束]、修正前/后值、置信度变化、影响因子、用户角色、时间戳）。
        [2]. 反馈分级处理与即时响应：①专家修正（置信度>0.9）直接触发Prompt Optimizer；②普通反馈进入修正轨迹库；③噪声反馈自动过滤。设置专用反馈通道，承诺24小时内处理并回复，紧急反馈设置加急处理流程。
        [3]. 修正影响传播分析：评估修正对下游任务的影响范围，高影响修正（>3个下游任务）强制生成Impact Report并通知相关AGENT。支持从任意Checkpoint恢复执行后修改中间状态变量（Time-travel第一阶段），并从修改点继续执行。
    5. 提示词工程与演进
        [1]. 基于DSPy理念构建Prompt Optimizer：输入为用户反馈、AUD一致性报告、SP/BP Checkpoint人工反馈，输出优化后System Prompt与Few-Shot样本集。优化目标采用Pareto多目标权衡（结构完整性40% + 逻辑一致性35% + 成本效率25%），采用NSGA-II算法求解Pareto前沿。
        [2]. 受控演进机制：自动微调的提示词参数必须进入“待审查版本库”，经人工专家（首席战略官或授权代表）确认后方可推向生产环境。集成MLflow实现Prompt自动版本注册，支持A/B测试（分流比例可配置）与一键回滚（保留最近10个稳定版本）。
        [3]. 提示词契约测试：每次优化后自动运行Strat-Bench基准测试集，通过率<90%自动拒绝上线，并生成差异分析报告。演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
    6. 工具调用与集成
        [1]. 全面遵循MCP（Model Context Protocol）2025规范：工具箱作为独立微服务对外暴露符合MCP标准的能力发现接口，支持工具注册、版本控制、灰度发布与回滚。AGENT通过MCP Registry查询工具能力描述（输入/输出Schema、版本、可靠性评分）。
        [2]. 工具选择优化：基于语义匹配（60%）+历史成功率（40%）计算工具匹配度，匹配度<0.6时触发人工确认或启动多工具并行验证。预定义17种战略工具的最佳组合模板（如SWOT+TOWS自动组合），支持用户自定义模板。
        [3]. 强制契约化调用与沙箱执行：所有工具调用前验证输入Schema，调用后验证输出Schema，连续3次校验失败自动熔断该工具。代码执行在E2B沙盒中（网络隔离+权限最小化），执行结果经Validation Feedback闭环校验，最大重试次数3次。
    7. 仲裁与决策机制
        [1]. CEO AGENT内置裁决状态机，设定最大辩论轮次（默认3+风险等级，上限7轮）。辩论采用三阶段：发散（Temperature=0.8）、收敛（Temperature=0.5）、裁决（Temperature=0.2），强制随轮次增加提交具体修改JSON补丁。
        [2]. 辩论质量评估：每轮计算新信息增益率与重复率，增益率<10%且重复率>50%时强制终止辩论。辩论过程记录各AGENT贡献度（基于Shapley值量化），分歧点生成热力图。
        [3]. 多方案生成与人类一票否决：若未达成一致，生成Plan A（保守参数集）、Plan B（激进参数集）和Plan C（AI融合版），输出参数调整明细。强制暂停60秒提供用户介入窗口，窗口期可视化展示三套方案的对比差异（风险/收益/资源需求）及影响模拟器（支持用户滑动调整关键参数实时预览结果）。超时无用户操作则按CEO决策执行，但所有人工覆盖决策强制记录至战略档案库并标记“人工覆盖”。
    8. 审计与一致性保障
        [1]. AUD AGENT采用“旁路监听+异步阻断”模式，不参与生成。审计范围包括四维一致性检验：①事实一致性（对比原始文档切片）；②逻辑一致性（规则引擎检测内部矛盾）；③数值一致性（沙箱重计算关键指标，误差>5%标记异常）；④时效一致性（引用数据超12个月标记“数据陈旧”）。
        [2]. 分级熔断与根因分析：L1（幻觉概率>0.8）自动触发修正；L2（0.6-0.8）通知相关AGENT重试（最多2次）；L3（<0.6）暂停任务并生成Hallucination Report（包含错误类型分类：数据缺失/逻辑跳跃/数值错误/时效过期），人工介入后从断点恢复。
        [3]. 审计日志强制存储：所有审计发现、纠错过程、重试轨迹全量存储至不可篡改日志（WORM存储），满足SOX/ISO27001审计要求，支持按时间/角色/任务类型多维检索。
    9. 记忆与上下文管理
        [1]. 分层记忆架构：工作记忆（当前会话，有效期24h）、短期记忆（最近项目，有效期30天）、长期记忆（战略知识库，永久存储），各层有独立的存储引擎和检索策略。长期记忆将用户修正日志、决策复盘结果、执行结果差值反向固化为少样本提示词，每季度触发知识蒸馏与去重。
        [2]. 公共黑板机制：基于LangGraph StateSnapshot + Redis Cluster管理状态版本，支持多版本并发控制（MVCC）。关键结论写入黑板时必须附带重要性评分（1-5）、置信度、引用源切片ID、时效性，综合评分>3.5才写入，便于上下文压缩器智能筛选。
        [3]. 时间旅行调试：支持从任意Checkpoint恢复执行，用于问题复现与根因分析。所有状态快照与完整推理轨迹归档至数据湖，可按需解压缩回溯。
    10. 可解释性与溯源
        [1]. 结构化证据包标准：每次分析打包输出全量元数据，包括输入摘要哈希、任务分解图、工具调用轨迹（含版本）、关键中间变量快照、置信度评分及依据、引用源切片ID列表。
        [2]. 交互式溯源树：前端支持从结论节点逐层展开至原始数据，每层显示“数据来源→计算逻辑→中间结论”三元组，每个节点支持查看详细信息。
        [3]. 决策影响分析：量化各输入变量对最终决策的Shapley贡献值，高影响变量（>20%）自动高亮标注。支持反事实推理，输出“如果改变某假设，结论会如何变化”的对比报告和风险热力图。
    11. 主动能力
        [1]. 引入心跳机制：支持周期性自动唤醒（可配置频率），主动检查战略档案库中的待办事项、偏差预警、周期性任务，无需用户提问即可主动沟通。
        [2]. 战略偏差预警：实时监控实际执行数据与SP/BP规划的偏差，偏差超过阈值（默认10%）自动触发预警，生成偏差分析报告并推送相关AGENT。
        [3]. 主动知识更新：当检测到行业报告、市场数据、政策法规更新时，自动触发电请相关AGENT评估影响，重要影响主动推送CEO AGENT。
    12. 性能监控与行为分析
        [1]. 建立三维度指标体系：效率维度（任务完成率、平均时长、工具调用成功率）、质量维度（修正率<10%、幻觉率<5%、一致性得分>0.8）、成本维度（单任务API成本、Token消耗）。构建战略规划专用基准测试集（Strat-Bench）作为版本迭代的准入标准。
        [2]. 漂移检测双算法：KS检验检测输出分布偏移（月度基线对比），CUSUM检测性能指标连续下降（滑动窗口7天）。指标异常时启动自动根因分析，关联日志输出根因报告。
        [3]. 健康度仪表盘：实时可视化展示各AGENT的12项健康度指标，支持下钻分析与预警订阅。三级预警机制：黄牌（指标下降15%触发分析）、红牌（下降30%触发人工review）、停用（下降50%且连续三天，自动回滚至前一稳定版本）。
    13. 安全与防御
        [1]. 纵深防御体系：
            - 第一层（隔离）：三重硬隔离（Prompt隔离、工具隔离、数据隔离），基于角色权限最小化。
            - 第二层（执行）：所有代码在E2B沙盒中执行，严格网络隔离（仅白名单API）。
            - 第三层（检测）：实时监控视角越界（跨角色关键词频率>5%触发熔断）、幻觉累积（连续3次低置信度结论锁定Agent）。
            - 第四层（审计）：所有操作记录至WORM存储。
            - 第五层（熔断）：辩论过热保护、成本熔断。
        [2]. 敏感数据脱敏：审计日志和反馈记录自动脱敏处理（个人可识别信息、商业机密），脱敏策略定期评审更新。
    14. 成本控制
        [1]. 成本预测：任务启动前基于历史相似任务预测Token消耗与API成本，偏差>50%时提示用户确认。
        [2]. 三级熔断机制：
            - 任务级熔断：单次任务Token消耗超预设阈值（基础任务5K tokens，复杂规划50K tokens）立即终止。
            - 会话级熔断：日累计Token消耗超企业配额（默认100万tokens）暂停新任务，已启动任务完成。
            - 系统级熔断：连续3次工具执行失败或单任务成本超预算200%触发系统级告警，需管理员介入。

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察(包括六个子步骤)、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据
-------------------

Q2
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的多模态数据（文本/表格/图像/公式）转化为模型可理解和检索的结构化知识图谱
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif，且具备：
      [1].表格结构化重建：集成TableTransformer解析xlsx/csv/pdf表格为结构化JSON，保留行列语义
      [2].公式与手写体识别：通过LaTeX OCR与手写识别引擎提取数学表达式与手写批注
      [3].多模态对齐：构建CLIP+文本联合嵌入空间，支持"以图搜文"与"以文搜图"跨模态检索
    2.支持完整的文档生命周期管理功能，包括文档上传、版本快照、差异比对、安全删除、自动分类、统计信息，且具备：
      [1].文档版本控制：基于Git LFS实现版本快照，支持任意历史版本回滚与变更轨迹追溯
      [2].自动分类引擎：基于内容语义+元数据的多标签分类器（准确率>92%），支持人工修正反馈闭环
      [3].元数据标准化：强制校验ISO 15489合规元数据Schema（创建者/时间/密级/业务域），缺失关键字段阻断入库
    3.构建Hybrid RAG流水线，融合BM25关键词检索与bge-m3多模态向量检索，并引入重排序优化：
      [1].混合检索引擎：初检阶段并行执行BM25与bge-m3稠密检索，通过RRF算法融合结果
      [2].语义分块替代固定切片：基于sentence-transformers检测主题边界，动态生成语义连贯片段（平均长度300±100 tokens）
      [3].重排序层：集成ColBERT-v2晚期交互模型对Top-20初检结果二次精排，提升precision@5达40%+
    4.支持完整的RAG操作功能，包括结构化元数据、单文档索引、批量索引、混合查询（BM25/向量/混合）、上下文检索、质量监控仪表盘，且具备：
      [1].检索质量实时反馈：每检索请求返回precision@k/recall@k/confidence三元组，支持前端可视化
      [2].引文精细化：存储三元组[片段ID, 原始文本快照(512字符), 采集时间戳]，支持页码/段落级定位
      [3].时效性标记：自动标记引用数据时效状态（<6月"新鲜"/6-12月"一般"/>12月"陈旧"）
    5.支持领域自适应摘要生成，调用网络大模型API生成动态长度结构化摘要：
      [1].动态长度策略：基于文档类型与复杂度自适应（企业年报800字/市场简报200字/咨询报告500字）
      [2].领域模板：预置财务摘要/市场洞察/技术评估三类Schema，强制输出关键指标字段
      [3].质量门禁：部署摘要质量评估器（信息熵+关键实体覆盖率），评分<0.7触发重生成或人工审核
    6.实施四级分层检索策略，平衡检索效率与精度：
      [1].L1全局摘要层：跨文档社区摘要（GraphRAG生成），解决全局语义关联
      [2].L2文档摘要层：单文档结构化摘要，快速锁定相关文档集
      [3].L3文档层：基于元数据过滤缩小范围
      [4].L4语义切片层：精粒度片段检索
      [5].自适应跳过：当L2摘要置信度>0.85时直连L4，减少15%+检索延迟
    7.强制实施片段级引文溯源机制，确保生成内容可审计：
      [1].三元组存储：[片段ID, 原始文本快照, 采集时间戳]，文档变更后仍可追溯原始内容
      [2].时效性标注：引用数据自动标记时效状态，陈旧数据（>12月）生成时强制提示
      [3].差异高亮：当引用文档更新时，自动比对并高亮变更区域供用户确认
    8.集成Self-RAG自适应检索机制，动态决策检索行为与质量控制：
      [1].动态阈值：基于查询复杂度自适应（简单事实查询0.5/战略分析0.75/创新推演0.85）
      [2].多文档聚合评分：采用RRF融合多源检索结果，避免单点误判
      [3].主动补救：当"数据不足"触发时，自动扩展检索范围（时间/来源/模态）或生成用户补数据提示
    9.构建三级战略记忆架构，实现知识持续演进：
      [1].工作记忆（24h）：当前会话上下文，Redis Cluster持久化
      [2].项目记忆（30d）：单次SP/BP项目全周期数据，支持跨阶段关联
      [3].战略知识库（永久）：结构化存储历年SP/BP关键假设、决策依据、执行偏差、用户修正轨迹
      [4].自动蒸馏：每季度执行知识蒸馏，聚类高频修正模式→生成领域少样本提示模板
      [5].冲突检测：新旧假设矛盾时（如市场增长率方向相反）自动触发AUD人工审核
    10.实施四层数据质量控制流水线，阻断低质量数据入库：
      [1].格式验证：强制校验文档可解析性（PDF非扫描件/表格可提取结构）
      [2].完整性检查：元数据必填字段覆盖率<90%或正文长度<100字符阻断入库
      [3].重复检测：SIMHash(检测结构重复)+BERTScore(检测语义重复)双校验，相似度>0.95标记重复
      [4].质量指数：计算DQI=0.4*完整性+0.3*唯一性+0.3*时效性，DQI<0.6进入人工审核队列
    11.建立RAGOps持续监控体系，实现性能漂移自动检测与修复：
      [1].实时指标：每100次检索自动计算precision@5/recall@5/faithfulness，滑动窗口7天均值告警
      [2].双算法漂移检测：
          - KS检验：月度基线对比检测输出分布偏移（p<0.05触发告警）
          - CUSUM：连续7天性能指标下降>10%触发根因分析
      [3].业务影响关联：当检索质量下降>15%时自动暂停SP生成，推送根因报告至AUD AGENT
      [4].自动修复：检测到索引漂移时触发增量重索引，模型漂移时切换至备用嵌入模型
    12.集成GraphRAG知识图谱增强层，解决跨文档全局理解问题：
      [1].实体关系抽取：基于spaCy+领域词典识别战略实体（公司/产品/市场/技术）
      [2].图谱构建：自动构建实体-关系-事件三元组图谱，支持社区检测与全局摘要生成
      [3].图增强检索：复杂查询自动触发图遍历，串联跨文档证据链（如"竞对A的供应链风险"需关联多份报告）
    13.实施上下文压缩机制，突破Token预算限制：
      [1].HyDE生成：对复杂查询生成假设性文档扩展检索维度
      [2].关键实体压缩：基于TF-IDF+领域重要性评分提取核心实体，压缩率目标≥70%
      [3].自适应预算：根据任务复杂度动态分配Token（SP规划50K/简单查询5K），超限时触发压缩
    14.遵循MCP 2025规范实现工具安全集成：
      [1].能力注册：所有数据处理工具通过MCP Registry注册，暴露标准Schema与可靠性评分
      [2].动态发现：AGENT运行时通过MCP协议查询可用工具，支持版本协商与灰度切换
      [3].安全沙箱：工具执行强制网络隔离，仅白名单API可访问（如财经数据源）
}

Q3
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的多模态数据转化为模型可理解、可检索、可演化的结构化知识资产
    1. 支持全模态文档格式处理，包括：
       [1.1] 文本类：pdf（保留版式坐标）、txt、doc/docx（含修订痕迹）
       [1.2] 演示类：ppt/pptx（提取幻灯片层级结构+演讲者备注）
       [1.3] 表格类：xls/xlsx/csv（使用TableFormer结构化提取，输出JSON Schema）
       [1.4] 图像类：jpeg/png/gif（使用SigLIP 2嵌入，支持图表-正文关联检索）
       [1.5] 新增：PDF版式保留模式，记录图表相对位置坐标（x,y,width,height），支持"图表-正文"空间关联检索
    2. 支持企业级文档全生命周期管理，包括：
       [2.1] 基础操作：文档上传/更新/删除/分类/版本控制
       [2.2] 文档血缘追踪：记录每次更新的diff摘要、操作者、时间戳，支持版本影响分析
       [2.3] 文档质量评分：基于文本密度、图表占比、引用完整性自动评分（1-5分），低质量文档（<2.5分）触发人工审核
       [2.4] 战略价值统计：增加"关键假设密度"（每千字关键假设数）、"引用热度"（被AGENT调用频次/周）指标
    3. 构建多模态混合嵌入架构：
       [3.1] 分层嵌入策略：
           - 短文本（<1K tokens）：bge-m3 dense embedding
           - 长文档（1K-8K tokens）：bge-m3 multi-vector + 滑动窗口聚合（步长512，重叠256）
           - 超长文档（>8K tokens）：分段摘要→bge-m3嵌入 + Parent-Child索引
       [3.2] 多模态路由：
           - 文本→bge-m3
           - 图像→SigLIP 2（投影至bge-m3向量空间）
           - 表格→TaPEx（投影至bge-m3向量空间）
       [3.3] 集成ColBERTv2晚期交互重排序器：首轮bge-m3检索Top50→ColBERTv2精排Top10，提升MRR@10 ≥18%
    4. 构建三重融合检索引擎：
       [4.1] Dense检索：bge-m3语义相似度（权重0.5，可配置）
       [4.2] Sparse检索：BM25关键词匹配（权重0.3，可配置）
       [4.3] Graph检索：基于实体关系图谱的路径检索（权重0.2，可配置）
       [4.4] RRF融合排序：Reciprocal Rank Fusion算法动态融合三路结果
       [4.5] 自适应Query Routing：
           - 简单查询（<5 tokens）：仅Sparse检索
           - 中等查询（5-15 tokens）：Dense+Sparse融合
           - 复杂查询（>15 tokens/含多实体）：三重融合+图谱扩展
       [4.6] 元数据粒度细化：支持文档级/章节级（section_id）/段落级（paragraph_id）/图表级（figure_id）/关键假设标记（assumption_tag）五级索引
    5. 实施动态多视角摘要生成：
       [5.1] 动态长度策略：
           - SP文档：800字（覆盖战略意图/创新焦点/业务设计）
           - BP文档：500字（聚焦关键任务/衡量指标）
           - 市场报告：300字（提炼趋势/机会/威胁）
       [5.2] 多视角摘要：
           - Executive Summary（CEO视角：战略方向/资源分配）
           - Risk Summary（CFO视角：财务风险/现金流压力）
           - Innovation Summary（CTO视角：技术趋势/创新机会）
       [5.3] 摘要质量反馈闭环：基于用户点击率/引用频次自动优化生成Prompt，季度触发Prompt Optimizer
       [5.4] 摘要存储：结构化JSON存入元数据库，经bge-m3嵌入后存入向量库，建立与原始文档的双向索引
    6. 构建四级分层检索架构：
       [6.1] L1：文档级摘要（100%文档覆盖）
       [6.2] L2：章节级摘要（关键章节，覆盖率≥80%）
       [6.3] L3：段落级切片（Max-Min语义分块，平均长度300±50 tokens）
       [6.4] L4：实体级片段（关键数据点/假设/决策依据）
       [6.5] 动态层数决策器：基于查询信息熵自动选择检索深度（低熵→L1/L2，高熵→L1-L4全链路）
       [6.6] Parent-Child索引：L1→L2→L3→L4建立双向指针，支持"自顶向下"与"自底向上"双向检索
    7. 增强可解释性与溯源能力：
       [7.1] 片段级溯源：保留原始文档位置（file_id, page_num, paragraph_id, char_offset）
       [7.2] 跨文档证据融合：当结论依赖多文档时，自动生成"证据融合图"，标注各文档贡献度（Shapley值）
       [7.3] 反事实推理支持：对关键结论自动模拟"若关键假设变化±10%，结论置信度变化ΔX%"
       [7.4] 引文元数据增强：增加confidence_score（0-1）、timeliness_score（基于文档日期衰减）、source_authority（年报=1.0/咨询报告=0.8/网络=0.5）
    8. 增强Self-RAG机制：
       [8.1] 动态阈值策略：
           - 事实型查询：阈值0.75（高精度要求）
           - 推理型查询：阈值0.55（允许一定模糊性）
           - 创新型查询：阈值0.45（鼓励发散思维）
       [8.2] 迭代重检索机制：
           - 首次熔断→Query Expansion（同义词扩展/问题分解）→二次检索
           - 仍熔断→生成"数据缺口报告"（缺失维度/建议补充源）
       [8.3] 熔断类型区分：
           - Data Gap（数据缺失）：建议补充数据源
           - Data Conflict（数据冲突）：启动红蓝辩论机制
           - Low Confidence（低置信度）：标记结论并建议人工复核
    9. 构建三级分层战略记忆架构：
       [9.1] 工作记忆（Working Memory）：
           - 范围：当前SP/BP周期数据
           - 有效期：24小时（会话级）
           - 存储：Redis Cluster（三节点哨兵模式）
       [9.2] 短期记忆（Short-term Memory）：
           - 范围：近3年战略档案+用户修正日志
           - 有效期：30天活跃期，90天归档
           - 存储：向量数据库（带时效性标签）
       [9.3] 长期记忆（Long-term Memory）：
           - 范围：5年+历史数据+行业基准+知识蒸馏产物
           - 有效期：永久存储
           - 存储：分层存储（热数据SSD/冷数据对象存储）
           - 季度知识蒸馏流程：
               ① 聚类相似战略场景（DBSCAN，ε=0.35）
               ② 提取共性决策模式（Prompt Mining）
               ③ 生成行业模板库（脱敏后跨企业共享）
       [9.4] 战略档案结构化存储：
           - 关键假设变量（assumption_id, variable_name, baseline_value, actual_value, delta）
           - 决策依据（decision_id, evidence_sources[], confidence_score）
           - 执行偏差（plan_value, actual_value, gap_ratio, root_cause_tags[]）
           - 用户修正日志（修正类型enum[参数/权重/约束]、修正前后值、置信度变化）
    10. 构建全链路质量控制闭环：
        [10.1] 格式验证：
            - 文本类：编码检测（UTF-8/GBK）、乱码率<5%
            - 表格类：行列完整性、数值格式校验
            - 图像类：分辨率≥300dpi（战略图表）、OCR可读性评分
        [10.2] 重复检测三重机制：
            - 文本重复：SIMHash（阈值0.95）
            - 语义重复：bge-m3嵌入相似度（阈值0.85）
            - 结构重复：表格/图表结构哈希比对
        [10.3] 文档可信度评分：
            - 权威性权重：年报=1.0，审计财报=0.95，咨询报告=0.8，内部文档=0.7，网络=0.5
            - 时效性衰减：每6个月衰减0.1（max decay 0.5）
            - 完整性扣分：缺失关键章节（战略意图/执行设计）每项-0.2
        [10.4] CI/CD质量门禁：
            - 入库前自动触发质量检查流水线
            - 阻断规则：可信度<0.6 或 重复率>30% 或 乱码率>10%
            - 人工审核通道：低质量文档进入审核队列，48小时内处理
    11. 构建生产级可观测性体系：
        [11.1] 核心评估指标：
            - 检索层：MRR@10 ≥0.75、Recall@50 ≥0.85、NDCG@20 ≥0.80
            - 生成层：Faithfulness ≥0.90（FactScore）、Answer Relevancy ≥0.85
            - 端到端：Task Success Rate ≥85%（基于Strat-Bench 20+场景）
        [11.2] 双算法漂移检测：
            - KS检验：月度分布偏移检测（α=0.05，p<0.05触发告警）
            - CUSUM：连续7天性能下降趋势检测（阈值-5%，触发根因分析）
        [11.3] 自动根因分析引擎：
            - 输入：性能下降指标+操作日志+数据变更记录+模型版本
            - 输出：根因分类（数据漂移/模型退化/Prompt失效/工具故障）+修复建议优先级
        [11.4] 健康度仪表盘：
            - 实时展示12项核心指标（检索延迟P95<200ms、生成延迟P95<3s、幻觉率<5%）
            - 三级预警：黄牌（指标下降15%）、红牌（下降30%）、停用（下降50%连续3天）
            - 支持下钻分析：从系统级→模块级→文档级逐层定位问题
}

Q5
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的多模态数据转化为模型可理解、可推理、可溯源的结构化知识表示
    1. 支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif，采用双引擎解析策略：
       1.1 文本密集型PDF（年报/报告）优先使用PyMuPDF4LLM，保留表格结构与层级标题，输出Markdown+表格JSON双格式
       1.2 扫描件/图像型PDF使用Unstructured.io OCR，置信度<0.85的文本块标记"需人工复核"
       1.3 多模态联合索引：图像/图表自动生成alt-text描述并独立向量化，支持"图文语义联合检索"
    2. 支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息，并增强：
       2.1 文档血缘追踪：自动记录文档派生关系（如"2025年报→2026Q1经营简报"），构建文档演化图谱
       2.2 敏感信息自动检测：集成PII识别模块，自动标记财报中的商业机密字段（客户名单/成本结构），按脱敏级别分级存储
    3. 构建混合检索三级流水线（Hybrid Retrieval Pipeline）：
       3.1 稀疏检索层：BM25实现关键词精准匹配，解决专业术语召回问题
       3.2 稠密检索层：bge-m3实现8192-token长上下文语义匹配
       3.3 重排序层：bge-reranker-v2-m3对Top-50候选进行精细化排序，提升Top-5相关性30%+
       3.4 可选GraphRAG引擎：将SP/BP关键假设变量（市场增长率/竞争格局）构建成知识图谱，支持因果推理检索
    4. 支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息，并扩展元数据Schema：
       4.1 强制字段：valid_until（数据有效期，默认12个月）、source_confidence（来源可信度，0-1）、strategic_relevance（战略相关性，1-5分）
       4.2 检索质量实时反馈：每次检索返回RAG Triad三指标——Faithfulness(忠实度)、Answer Relevancy(答案相关性)、Contextual Precision(上下文精度)
    5. 支持Agentic摘要生成机制：
       5.1 动态长度控制：LLM自主决策摘要长度（战略文档>800字保留关键假设，市场快讯<200字提炼核心趋势）
       5.2 质量门禁：基于信息熵+关键实体覆盖率双阈值评估，综合评分<0.7自动触发二次生成
       5.3 摘要存入元数据库，经bge-m3词嵌入后存入向量库，同时构建摘要→原文片段的双向索引
    6. 支持Self-Routing RAG分层检索策略：
       6.1 动态路径决策：根据查询复杂度自动选择"摘要检索→切片检索→全文检索"路径
       6.2 置信度传播机制：摘要层检索置信度<0.6时跳过摘要直接切片检索，避免信息损失
       6.3 检索路径可解释：返回检索决策日志（如"跳过摘要层：因查询含具体财务指标"）
    7. 支持多级引文溯源体系：
       7.1 片段级溯源：保留原始文档切片位置（页码/段落）
       7.2 指标级溯源：关联关键财务/运营指标至数据源（如"营收增长率→2025年报P23"）
       7.3 假设级溯源：通过GraphRAG链接战略假设变量至支撑证据（如"市场增长率15%→IDC 2025Q4报告"）
       7.4 前端支持交互式溯源树：从结论逐层展开至原始数据，每层显示"数据来源→计算逻辑→中间结论"三元组
    8. 支持增强型Self-RAG机制：
       8.1 场景自适应阈值：战略规划类任务相关性阈值0.75，市场扫描类0.55，用户可配置
       8.2 补救工作流：触发"数据不足"时自动执行——①调用白名单财经API补充最新数据 ②提示用户上传补充材料 ③标记低置信度结论供AUD审核
       8.3 检索反思日志：记录每次"跳过生成"的决策依据，用于优化检索策略
    9. 构建时序知识图谱式战略档案库（Temporal Knowledge Graph Memory）：
       9.1 事实时效性管理：为每个存储事实标注valid_from/valid_until，超期事实自动衰减检索权重（线性衰减至0.3）
       9.2 结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，构建"假设-执行-偏差"三元组
       9.3 修正日志质量过滤：仅将置信度>0.85且被3+用户验证的修正模式固化为少样本提示词
       9.4 季度知识蒸馏：自动聚类相似修正模式，生成领域特定的提示词模板库
    10. 支持增强型数据预处理流水线质量控制：
        10.1 双模重复检测：SIMHash(字面重复)+bge-m3余弦相似度(语义重复)双校验，相似度>0.92标记重复
        10.2 多模态质检：图像OCR置信度<0.9标记"需人工复核"，表格结构完整性校验（行列对齐/合并单元格识别）
        10.3 格式验证增强：PDF解析失败率>5%自动切换备用解析引擎（PyMuPDF4LLM↔Unstructured.io）
    11. 建立RAG Triad全维度性能监控与漂移检测机制：
        11.1 三维度监控指标：
            - Faithfulness（忠实度）：生成内容与检索上下文的事实一致性，目标>0.85
            - Answer Relevancy（答案相关性）：回答与用户查询的语义匹配度，目标>0.80
            - Contextual Precision（上下文精度）：检索结果中相关片段占比，目标>0.70
        11.2 双算法漂移检测：
            - KS检验：月度基线对比检测输出分布偏移，p-value<0.05触发告警
            - CUSUM：7天滑动窗口检测性能指标连续下降，下降幅度>15%启动根因分析
        11.3 战略任务差异化监控：SP/BP规划类任务监控阈值比常规分析严格20%
        11.4 可视化健康度仪表盘：实时展示11项核心指标，支持按文档类型/时间范围下钻分析
    12. 支持知识感知查询扩展（Knowledge-Aware Query Expansion）：
        12.1 避免纯HyDE风险：不生成完整假设文档，仅扩展关键实体与关系（如"新能源汽车"→"动力电池/充电网络/政策补贴"）
        12.2 领域词典增强：集成战略管理术语库（BLM/BEM框架术语），自动识别并扩展专业概念
        12.3 扩展质量校验：扩展后查询与原查询语义相似度需>0.6，防止主题漂移
    13. 企业级安全与合规基线：
        13.1 敏感数据自动脱敏：审计日志和反馈记录自动脱敏处理（个人可识别信息、商业机密），脱敏策略季度评审
        13.2 访问控制增强：基于RBAC模型实现文档级权限控制（如CFO可访问财务数据，CMO仅市场数据）
        13.3 WORM存储：所有检索日志、生成内容、用户修正记录写入不可篡改存储，保留期≥7年满足SOX/ISO27001要求
    14. 全链路可观测性：
        14.1 检索轨迹追踪：记录每次检索的完整路径（查询→扩展→稀疏检索→稠密检索→重排序→结果）
        14.2 成本-质量权衡分析：可视化展示Token消耗与Faithfulness指标的帕累托前沿，支持成本优化决策
        14.3 根因分析自动化：性能下降时自动关联日志（文档更新/模型版本/查询模式变化），输出根因报告
}

G3
{
一、数据处理：构建多模态、图向量混合的智能数据流水线。需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化与半结构化数据转化为模型可理解、可图谱化检索的结构化资产。
    1. 多模态深层解析引擎：
        [1]. 支持的文档格式包括 pdf, txt, doc, docx, ppt, pptx, xls, xlsx, csv, jpeg, png, gif，以及音视频会议记录（基于ASR转录）。
        [2]. 集成版面分析模型（如Docling/LayoutLM），强制将财报、研报中的多维表格、嵌套图表、页眉页脚分离，无损提取为结构化Markdown或JSON，消除视觉排版带来的语义割裂。
    2. 数据血缘与资产管理：
        [1]. 支持完整的文档操作功能，涵盖文档上传、增量更新、分类管理、版本控制（Versioning）。
        [2]. 建立严格的数据血缘（Data Lineage）追踪，确保每一个解析后的数据切片都能追溯到其导入批次、操作人及原始文件版本。
    3. 混合检索架构与向量库构建：
        [1]. 使用本地词嵌入模型 bge-m3 构建稠密向量索引，结合BM25算法构建稀疏索引。
        [2]. 引入知识图谱（Knowledge Graph）抽取流水线，识别战略文本中的实体（竞争对手、技术趋势、财务指标）及关系，构建图-向量混合检索（GraphRAG）底座。
    4. 智能体分块与RAG操作：
        [1]. 废弃固定字数切片，采用智能语义分块（Agentic Chunking），根据文档的逻辑段落、章节标题、表格边界进行动态切片，保证战略语境的完整性。
        [2]. 支持完整的RAG操作：多维元数据过滤、混合检索排序（Reciprocal Rank Fusion）、图谱关联展开、上下文窗口动态扩缩。
    5. 结构化摘要与RAPTOR层级：
        [1]. 调用网络大模型API对文档进行摘要提取，强制采用Pydantic校验输出JSON格式结构化摘要（包含核心指标、关键实体、结论基调）。
        [2]. 采用树状摘要聚合策略（类似RAPTOR机制），支持自下而上的各级摘要生成，锁定宏观问题边界后再下钻检索具体切片。
    6. 自反思与动态路由检索 (Active Self-RAG)：
        [1]. 建立Query理解与路由引擎，根据用户指令动态决定检索路径（走摘要树、密集向量或图谱关联）。
        [2]. 摒弃静态阈值，采用LLM-as-a-Judge进行检索质量实时评估。若检索上下文不足以回答问题，自动触发Query重写（Query Transformation）、条件放宽或调用外部工具检索，拒绝强行生成。
    7. 高保真视觉级引文溯源：
        [1]. 检索处理时不仅保留文本块切片ID，需强制映射原文档的物理视觉坐标（Bounding Box）。
        [2]. 确保后续Agent生成的内容可实现“点击溯源”，前端交互可直接高亮展示原始PDF或图像中的具体出处段落或表格单元。
    8. 时序动态战略档案库 (Temporal Strategic Memory)：
        [1]. 建立基于时间戳的长效记忆图谱，解决历年SP/BP数据冲突。明确隔离“假设变量”、“决策依据”与“实际结果”。
        [2]. 自动计算规划与执行的差值，将专家修正日志与差值复盘反向蒸馏为Few-shot提示词或业务规则，实现战略知识库的动态自我演进。
    9. 智能质量控制与数据清洗：
        [1]. 数据预处理流水线引入Agent评审员，进行格式验证、内容完整性检查。
        [2]. 采用语义级相似度去重（Semantic Deduplication）替代传统SIMHash，自动识别并预警不同咨询报告中对同一指标的冲突表述。
    10. 闭环性能监控与持续对齐：
        [1]. 建立RAG流水线的实时观测看板，监控指标包括：索引延迟、多路召回率、重排序耗时、摘要生成Token消耗。
        [2]. 建立性能漂移检测机制，收集终端用户对引用准确率的负反馈，自动触发评估基准测试集，诊断并调优词嵌入模型与重排序策略。
}

G5
{
一、数据处理：构建基于多模态解析、混合检索与知识图谱增强（GraphRAG）的下一代动态数据底座。需要处理历史战略规划与业务计划、企业年报、市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化、半结构化数据转化为模型可理解、可推理、可溯源的结构化关联数据。
    1.支持原生多模态文档接入与视觉文档理解（VDU）
        [1].支持格式包括pdf, txt, doc, docx, ppt, pptx, xls, xlsx, csv, jpeg, png, gif。
        [2].引入先进版面分析模型（Layout Analysis），精准识别并剥离多栏文本、页眉页脚与水印。针对复杂财务报表与嵌套表格，强制采用视觉大模型或专门解析器转化为包含行列映射关系的Markdown或JSON结构，防止数据失真。
    2.支持高保真文档生命周期与血缘管理
        [1].支持包括文档上传、更新、删除、分类管理、版本控制与统计信息的完整操作。
        [2].建立严格的数据血缘追踪（Data Lineage），任何文档的更新或删除必须触发级联刷新，自动标记或更新下游受影响的切片、摘要与图谱节点。
    3.构建基于混合召回与知识图谱的RAG流水线（Graph-Enhanced Hybrid RAG）
        [1].使用本地词嵌入模型（如bge-m3）结合BM25算法，建立“语义+关键词”双路密集与稀疏检索。
        [2].引入图数据库构建动态知识图谱。在数据入库时，自动抽取文档中的关键实体（组织、人员、市场、财务指标等）及逻辑关系（因果、包含、影响等），构建战略实体关系网络。
    4.执行动态语义切片与自适应块管理
        [1].摒弃固定字数切块，采用基于文档结构（标题树）与上下文凝聚度（Semantic Cohesion）的自适应动态切片策略，确保每个Chunk具备独立完整语意。
        [2].支持完整的RAG基础操作，包含丰富元数据注入（时间戳、信源等级、作者等）、单/批量索引、统计信息管理。
    5.多维结构化提炼与认知锚点构建
        [1].支持调用大模型API按信息熵自适应生成结构化文档摘要，不仅存入元数据库，同时提取微观“三元组（实体-关系-实体）”写入知识图谱。
        [2].利用摘要构建认知锚点，大幅提升后续检索在宏观战略层面的对齐度。
    6.支持基于图谱与父子文档的立体检索策略
        [1].宏观问题优先触发图谱社区摘要（Community Summaries）检索；微观问题支持先检索文档摘要锁定范围，再下钻检索具体切片（Parent-Child Retriever）。
        [2].支持多跳推理检索，通过图谱关系补全向量检索无法发现的隐式战略传导链（如：宏观政策变动->行业原材料上涨->企业利润率预期影响）。
    7.强化级可溯源引文引擎
        [1].支持检索与生成过程保留极细粒度的引文索引（精确至Chunk ID与版面坐标）。
        [2].确保最终生成内容前端可直接溯源并高亮原始文档的具体片段或图表区域。
    8.部署Corrective RAG (CRAG) 与主动探究机制
        [1].系统对检索到的文档进行实时相关性与置信度评估（设定动态阈值）。
        [2].若评估为“数据不足”或存在“事实冲突”，不再仅是被动标记，而是自动触发Query重写机制，或向环境调度器申请调用内部SQL/外部Web Search工具进行动态数据补全，避免强行生成。
    9.构建四维战略时序档案库（长期记忆系统）
        [1].建立基于时间轴的四维记忆模块。除常规过滤外，强制结构化提取并对齐历年SP/BP的“关键假设变量、决策依据模型、实际执行结果的差值”。
        [2].通过时序图谱（Temporal Graph）追踪战略变量的演进轨迹。将高质量的用户修正日志经过清洗与一致性判定后，反向固化为Prompt Optimizer的Few-Shot样本集，实现系统认知的自我进化。
    10.高信噪比数据预处理与质量门禁
        [1].构建入库前清洗流水线，包括格式防腐验证、内容完整性检查、乱码剔除。
        [2].引入高级去重与偏见检测，从SIMHash文本去重升级至语义级排重；针对冲突研报数据，基于信源权重评级体系打标签，保留多方观点供Agent后续辩论裁决。
    11.实时运行观测与数据漂移自愈机制
        [1].建立查询意图难度评估与检索准确率（MRR/NDCG）的实时监控指标体系。
        [2].定期评估底层知识分布的漂移现象，对于高频长尾失败Query，自动生成诊断报告并推荐重索引或补充专项数据源。
}

O3
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
       - 扩展格式：同时支持 eml/mbox/html/json/xml/odt/rtf/odf、zip 包内批量导入、音频（mp3,wav）与视频（mp4）待转录轨迹，支持多语种与编码自动识别与规范化。
       - 每个上传文件须记录原始 MIME、来源 URL / 上传用户、文件哈希（sha256）、抓取/上传时间及版权/许可元字段（license, provider_id）。
       - 支持扫描 PDF 与图片的 OCR 抽取能力（可配置 OCR 引擎：如 Tesseract/商业 OCR），并记录 OCR 置信度；若置信度 < 0.85 则标记为“待人工校验”。
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
       - 强制版本化：文档每次更新保留不可篡改快照（snapshot），并存储 diff / 改动元数据；支持基于版本的回滚与审计。
       - 支持软删除/强删除策略与合规保留期（retain policy）；删除操作生成不可篡改的审计条目（WORM 或等效技术）。
       - 支持 RBAC（基于角色与数据标签的访问控制）与细粒度权限（读取/注释/导出/索引权）。
       - 支持文档分类与标签体系（可自定义 taxonomy），并提供文档质量统计（完好率、OCR 通过率、PII 检出率）。
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
       - 默认使用 bge-m3 作为首选 embedding，但实现“嵌入适配器”接口（pluggable embedding adapters），可无缝切换至其他 embedding 模型/版本（并记录模型哈希与版本）。
       - 为嵌入生成记录 embedding 参数（normalize/float32/int8）、向量维度、生成时间与模型版本；实现 embedding 版本回滚与 AB 测试能力。
       - 支持 embedding drift 监控（均值/方差/相似度分布）并提供自动化告警与回退策略。
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
       - 支持 hybrid search（vector + BM25）与可插拔 re-ranker（可配置 ranker pipeline）；支持 index atomic batch 操作与回滚。
       - 明确定义切片策略模板（chunk size, overlap, semantic boundary rules），并在索引元数据中持久化 chunk->doc 映射。
       - 提供检索和索引指标：recall@k、MRR、subquestion_coverage、latency_p99、index_size、index_build_time。
       - 索引治理：索引版本、回滚、差异化增量索引与索引一致性检查（index checksum）。
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
       - 摘要输出必须遵守契约化 Schema（JSON schema），包含字段：summary_text, summary_hash, key_entities[], coverage_score, fact_consistency_score, generation_model{vendor,version}, generation_cost_usd, timestamp。
       - 自动执行摘要质量评估器：信息熵、覆盖率、事实一致性检测（可配置阈值）；若质量低于阈值，触发二次生成/本地模型fallback或人工复核流程。
       - 摘要与原文切片建立可验证引用（docID, chunkID, char_offset, snippet_hash）并写入证据包（evidence bundle）。
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
       - 明确定义分层流程参数：摘要 top_N → 切片 top_M 映射规则（默认 top_N=10, top_M=50，可配置）；并持久化摘要→切片映射表用于溯源与再现。
       - 对分层检索链路计算联合置信度（摘要_confidence × slice_confidence），并基于联合置信度决定是否触发 Self-RAG 或人工介入。
       - 在分层检索中支持语义 re-ranking 与多策略并行（并行调用不同索引/向量库以提高覆盖）。
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
       - 引文粒度标准：docID + chunkID + char_offset + snippet_hash；输出引用需包含来源元信息（provider, license, fetch_date）与展示型引用片段（≤300字符）。
       - 若引用来源受版权/许可限制，自动标注并阻断直接复制；提供授权申请或人工替代方案（summary only）。
       - 输出端支持机器可校验的引用三元组（docID, chunkID, snippetHash）与可跳转的原文定位链接（若可用）。
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
       - 将单一阈值扩展为**多维置信度组合**（检索相似度、摘要覆盖率、事实一致性），并允许任务级/工具级自定义阈值策略。
       - 在"数据不足"情形提供多种响应策略：①提示用户补数据并建议优先补源；②自动触发 agent 补检（受白名单限制）；③允许生成草稿但强制显著标注“低置信度/仅供参考”；④记录拒绝原因到修正轨迹库并排序为数据采集优先级。
       - 所有“拒绝”与“低置信”决策写入证据包用于事后审计与模型改进。
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
       - 定义战略档案 Schema（fields: plan_id, period, key_assumptions[], decision_reasons[], execution_results{metrics,time_series}, deviation_analysis, correction_logs[]，confidence_score, reviewer_signoff）。
       - 修正固化策略：仅在满足“审阅签名”或达到高频修正模式阈值时自动固化；自动去重与知识蒸馏（quarterly distillation pipeline），并记录变更差异与方法论一致性校验。
       - 长期记忆支持版本化检索、权限控制与可删除（数据主体权利）处理；支持定期知识蒸馏与去重（quarterly）并保留审计证据。
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
       - 采用多层质量检查：文件哈希校验 + SIMHash 文本近似 + 向量相似度去重 + 表格/财务一致性校验（列/行/总计/单位一致性）。
       - 对 OCR/图片/表格输出记录置信度并在低置信度触发人工抽检；对表格抽取实现列类型推断与一致性规则验证（如金额列单位规范化）。
       - 对异常（截断/乱码/不完整）文件自动分类到“异常处理队列”并通知上传方或数据工程师。
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量
       - 指标集合与频率：检索（recall@k, MRR, latency_p95 daily）、摘要（coverage_score, fact_consistency_rate, rouge/bleu proxies weekly）、生成（hallucination_rate, correction_rate monthly）；embedding drift（distributional shift daily）。
       - 漂移检测算法：KS检验 + CUSUM（滑动窗口 7 天）并配套根因分析 playbook（自动收集失败 case，触发 A/B 回归测试或回滚）。
       - 当关键指标越过阈值时触发分级告警（黄/红/停用），并自动保存当时的 system prompt、model version、index snapshot 以便回溯与回滚。
       - 性能监控面板与可导出报告，支持按任务/客户/时间区间下钻。
    12.（附加）合规与隐私防护（横向补充）
       - 上传前执行 PII 探测与脱敏策略（可配置：mask/replace/redirect_to_human）；对敏感字段提供加密-at-rest 与字段级访问控制。
       - 所有生成结果与引用写入不可篡改审计日志（WORM），满足 SOX/ISO27001 风险合规需求；支持以审计友好格式导出（时间/角色/任务维度）。
    13.（附加）成本与版本治理（横向补充）
       - 在数据处理与摘要生成阶段记录预计/实际 API cost 与 token 消耗，任务完成后产出成本审计信息（cost_usd, tokens_used）。
       - 所有关键构件（embedding model、index、summarizer、re-ranker）均纳入版本管理并记录版本哈希、发布时间与回滚机制。
}





根据上述建议优化重构[数据处理]需求特性。要求：
1、符合需求特性阶段的描述，而不是更偏向系统设计
2、符合[企业战略规划管理智库系统需求特性列表]->[数据处理]需求特性的语言风格
3、符合[企业战略规划管理智库系统需求特性列表]->[数据处理]需求特性的排版风格
4、以txt代码片段格式输出完整的[数据处理]需求特性




## 决胜


你是一名宗师级程序设计大师，全球顶级人工智能科学家，同时还是一名全球知名的战略管理家。现在需要结合[企业战略规划管理智库系统需求特性列表]系统需求，从下面20个需求清单中选择最优的[数据处理]需求特性。要求： 
1、结合系统需求为每个列表逐个评分（10分制），最后求总分
2、评分维度：
   - 多模态解析与原子化溯源
   - 混合检索架构与图谱增强
   - 强制契约化与数据质量门禁
   - Agentic RAG与自适应纠偏
   - 知识演进与反馈闭环
   - 全链路可观测性与漂移检测
   - 本地优先与数据主权隔离
   - 企业级合规与审计追踪
   - 互操作性与生态扩展
   - 成本工程与性能优化
   - 开发者体验与运维支持
   - 创新前瞻性
   - 安全与鲁棒性
   - 性能与监控
   - 企业级适用性
   - 工程可实施性
3、严格从[客观视角]进行评估，以表格形式呈现

---------------------

企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的数据转化为模型可理解和检索的结构化数据
    1.支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif
    2.支持完整的文档操作功能，包括但不限于文档上传、文档更新、文档删除、分类管理、统计信息
    3.使用本地词嵌入模型bge-m3构建完整的RAG流水线
    4.支持完整的RAG操作功能，包括元数据、单文档索引、批量索引、语义查询、上下文检索、统计信息
    5.支持调用网络大模型API接口生成300-500字的结构化摘要，摘要存入元数据库，词嵌入后再存入向量库元数据
    6.支持分层检索策略，先检索文档摘要锁定范围，再检索具体切片
    7.支持检索处理时保留引文索引，确保后续生成的内容可溯源至具体文档片段
    8.支持Self-RAG机制，若检索到的文档相关性得分低于设定阈值（默认 0.6），则标记为"数据不足"，避免强行生成
    9.构建战略档案库作为长期记忆模块。除基于时间序列的元数据过滤外，需结构化存储历年SP/BP的关键假设变量、决策依据与实际执行结果的差值，将用户修正日志反向固化为少样本提示词，形成动态演进的战略知识库
    10.支持数据预处理流水线的质量控制环节，包括格式验证、内容完整性检查和重复检测（SIMHash）
    11.建立模型性能监控与漂移检测机制，定期评估RAG检索准确率和摘要生成质量

二、工具箱：支持基于网络LLM和先进Agent技术的智能分析工具集，包括PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS分析、GE-麦肯锡矩阵、SPACE矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI以及变革管理（共计17个）。
    1.智能工具定义
        [1].建立工具标准：每个工具有唯一标识、输入/输出Schema、执行逻辑模板、领域事实库、验证器及演进日志。工具初始化必须加载角色定义（IDENTITY.md）、工具契约（TOOLS.md）及安全边界（BOUNDARY.md），严禁越权操作。
        [2].构建中央技能仓库：实现工具的统一注册、版本控制、灰度发布与回滚机制。所有入库工具必须通过Pydantic V2契约测试，后续迭代需通过回归测试集。支持预定义模板与远期基于代码生成的技能动态扩展。
        [3].支持工具链编排与依赖调度：支持定义工具链有向无环图，实现上游工具输出结构化流转为下游输入。内置任务调度器，根据依赖关系自动并行执行无依赖子任务。
    2.工具箱标准工作流
        [1].环境初始化：根据用户偏好（风险偏好、时间压力、行业上下文）渲染工具价值观模板（SOUL.md），实例化会话级沙箱与混合检索记忆容器。
        [2].感知与摘要：读取经处理的结构化JSON数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
        [3].规划：Planner依据用户自然语言指令及数据摘要，输出机器可读的任务编排JSON，包含子任务列表、匹配工具映射、数据依赖边。
        [4].执行与验证：Executor按拓扑顺序调度子任务，每个子任务内执行Think→Code→Execute→Observe→writeback原子循环；每次输出均经Auditor校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
        [5].证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
        [6].反馈与固化：用户可通过命令行对分析结果提出修正（如调整权重、增减约束），系统记录修正前后差异，并将高频修正模式转化为业务规则模板，供Planner在类似场景下默认引用。
        [7].成本审计与持久化：记录本次任务Token消耗、预估USD成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
        [8].演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续Strat-Bench扩增样本或Planner拆解策略优化的输入源。
    3.数值计算与沙箱执行
        [1].强制代码执行隔离：严禁LLM直接进行数值计算、统计回归或现金流折现。所有量化分析强制通过两条路径执行：一是生成Python代码在沙箱执行；二是针对战略运筹问题（如线性规划），由LLM构建数学模型调用CVXPY、OR-Tools等优化器求解。
        [2].构建持久化计算沙箱：为每个任务会话实例化持久化Jupyter Kernel，支持跨工具、跨步骤的变量传递（如宏观经济变量在不同模型间共享），实现分析上下文连贯。任务结束后自动销毁内核并回收资源。
        [3].支持Validation Feedback闭环：自动捕获代码执行STDERR，检索错误案例库辅助LLM生成修复版本。设定最大重试次数（默认3次），失败则标记不可行并记录至演进日志。
    4.Schema强制与一致性校验
        [1].实施契约化输出：所有工具输入/输出严格遵循Pydantic V2定义的Data Model，利用Instructor Patch强制LLM输出符合Schema的结构化JSON，严禁输出自由文本。
        [2].建立一致性校验仲裁器：在多工具协作中，通过内置规则引擎及财务常识库检测逻辑冲突（如利润率与成本矛盾）。输出冲突检测报告，供用户或高级AGENT裁决。
        [3].自动化契约测试：在CI/CD流水线集成Schema契约测试，防止模型漂移导致结构崩溃。契约测试失败的任务强制终止。
    5.可解释性与决策溯源
        [1].输出结构化推理证据包：每次分析必须打包输出全量元数据，包括输入摘要哈希、Planner任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片ID）。
        [2].构建交互式溯源树：支持前端从结论反向逐层展开证据包，展示“数据来源→计算逻辑→中间结论→最终结论”的完整推理链。
        [3].支持红蓝辩论机制：针对高不确定性议题自动启动“红蓝对抗”，实例化激进派与保守派子智能体进行结构化辩论，输出包含共识与分歧区域的风险全景视图。
    6.提示词工程与演进
        [1].Prompt Optimizer：基于DSPy理念，建立“修正日志-样本转换”闭环，将用户修正转化为Few-Shot样本，自动迭代提示词参数以适配企业战略风格。
        [2].实施受控自动演进：仅在SP/BP Checkpoint完成态且满足一致性阈值时触发演进。必须采用多目标优化（结构、一致性、成本），严禁单一指标优化。
        [3].确保方法论不变性：演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
        [4].支持版本管理与回滚：所有Prompt纳入Git LFS管理，与系统版本绑定。支持一键回滚至历史稳定版本。
    7.用户反馈与闭环演进
        [1].支持Checkpoint人工介入：在关键决策节点或低置信度环节自动暂停，请求用户验证中间假设。支持用户修改参数或调整权重，系统记录修正前后的“专家修正轨迹”。
        [2].建立修正轨迹库与逆向传播：将用户修正快照匿名化存储，定期分析差异，推断输入权重或任务分解策略的优化方向。
        [3].实施用户反馈协议：将用户反馈转化为结构化修正日志，强制触发工具体“反思阶段”（ReflAct），并回传至技能演进日志。
    8.安全与防御机制
        [1].实施沙箱网络隔离：代码执行环境默认断网，仅允许通过白名单网关访问预设的可信财经API，严禁通用网页爬虫。
        [2].建立提示词注入防御：输入数据前置执行清洗与注入检测。将“注入防御测试”纳入基准测试集，定期验证工具体的抗干扰能力。
        [3].建立工具幻觉黑名单：自动监控工具体输出，若连续3次生成与输入矛盾的结论，自动锁定该工具体并报警。
    9.领域专业知识嵌入
        [1].内置领域事实库：以JSON Schema存储经典管理理论的强制约束（如SWOT内部/外部属性分类），确保分析逻辑符合理论框架。
        [2].部署领域验证器：工具体输出前必须经过验证器检查，确保结论不违背基础商业逻辑与领域常识。
    10.服务契约与集成
        [1].独立微服务封装：工具箱作为独立微服务对外暴露OpenAPI 3.1标准接口，输出分析结果、证据包与成本审计信息三元组。
        [2].支持Agent模块集成：与CEO、CFO等核心AGENT通过服务调用无缝集成，支持水平扩展与分布式Trace追踪。
    11.测试与质量保证
        [1].构建基准测试集（Strat-Bench）：建立包含20+专家标注场景的战略分析专用测试集，作为版本迭代的准入测试标准。
        [2].支持A/B测试框架：支持多版本策略并行运行，基于任务完成率、计算精度与成本指标择优晋级。
        [3].部署健康度仪表盘：实时监控工具成功率与修正率，修正率超标（默认15%）自动触发Skill Review。
    12.性能监控与漂移检测
        [1].建立性能漂移检测：定期评估检索准确率与生成质量，对比历史基线检测性能下降。
        [2].支持可视化日志：所有评估与运行结果纳入系统行为日志并可视化展示。
    13.成本熔断与契约测试质量门禁
        [1].CI/CD契约测试门禁：所有工具输出必须通过严格Schema校验，非兼容变更触发构建失败。运行时连续三次校验失败触发工具熔断。
        [2].实施三级成本熔断：支持任务级（单次Token/成本超限）、会话级（日配额超限）与重试级（连续代码执行失败）熔断机制，确保系统运行成本可控。

三、AGENT：支持网络调用LLM API驱动的企业战略规划AGENT执行战略规划任务，这些AGENT包括首席执行官CEO AGENT、首席运营官COO AGENT、首席营销官CMO AGENT、首席技术官CTO AGENT、首席财务官CFO AGENT、首席人力官CHO AGENT共6个核心AGENT，以及1个联席审计官AUD AGENT
    1. 输入处理与全景构建
        [1]. 输入系统预处理的结构化JSON数据，AGENT首先调用摘要质量评估器（基于信息熵与关键实体覆盖率），若摘要质量评分低于阈值（默认0.7）则自动触发二次检索补充缺失维度，确保全景视图的完整性。
        [2]. 构建动态全景视图：宏观层（战略指标趋势/异常波动/关键假设变量）与微观层（文档切片引用索引）双向关联，支持细节挖掘过程中发现与全景视图严重矛盾时自动触发视图重构，重构过程需记录变更轨迹并提交AUD AGENT审核。
        [3]. 实施上下文窗口动态管理：基于任务复杂度自适应分配Token预算，超限时触发上下文压缩器（基于关键实体抽取与HyDE算法），压缩率目标≥70%，确保关键信息不丢失。
    2. 任务分解与协作架构
        [1]. 建立任务IR（中间表示）标准：每个任务包含任务ID、类型、输入/输出Schema、依赖任务列表、优先级、执行状态、超时策略。所有协作任务必须基于有向无环图（DAG）构建流转拓扑，确保多体协作具备确定性的状态机流转特征。
        [2]. 采用分层协作架构：CEO AGENT作为指挥官负责目标解析与全局任务分解，各专业Agent并行执行子任务，调度器负责资源分配与执行监控。自动生成协作依赖图，基于BLM/BEM阶段定义标准依赖关系，支持动态调整与优化，可视化展示任务间数据流与控制流。
        [3]. 支持柔性视角隔离：定义核心视角（必须遵守）和扩展视角（可协商），对跨领域问题组建临时联合分析组，建立视角冲突解决协议。视角隔离通过Prompt隔离、工具隔离、数据隔离三重硬约束实现。
    3. AGENT确定性工作流
        [1]. 基于状态图的工作流引擎：采用StateGraph机制定义显式状态模型，包含session_id、current_phase、agent_role、working_memory、long_term_memory_ref、tools_used、confidence_score、cost_accumulated等字段。工作流循环定义为：[感知]环境初始化与上下文加载→[规划]生成任务执行DAG→[执行]按拓扑顺序调度子任务→[深度思考]（可选）在关键决策节点并行生成多条思维链推演路径→[验证]结构化检查输出→[反思]错误分析与修正→[闭环]Early Stopping机制（置信度达标且目标达成时提前终止）。
        [2]. 深度思考与多路径推演：在SP/BP所有核心分叉点（如商业模式选择、核心竞争力定义），默认调用支持深层推理的模型，并行生成多条思维链推演路径，通过内部验证打分选出最优解，极大提升战略输出的逻辑厚度。
        [3]. 中断安全与状态持久化：任意节点支持用户中断，自动保存StateSnapshot至Redis Cluster（三节点哨兵模式），恢复时加载完整上下文与工具状态，支持从任意历史快照恢复执行（Time-travel能力分步实现）。
    4. 用户反馈闭环
        [1]. 建立反馈标准化协议：将用户反馈转化为结构化修正日志（包含修正类型enum[参数/权重/约束]、修正前/后值、置信度变化、影响因子、用户角色、时间戳）。
        [2]. 反馈分级处理与即时响应：①专家修正（置信度>0.9）直接触发Prompt Optimizer；②普通反馈进入修正轨迹库；③噪声反馈自动过滤。设置专用反馈通道，承诺24小时内处理并回复，紧急反馈设置加急处理流程。
        [3]. 修正影响传播分析：评估修正对下游任务的影响范围，高影响修正（>3个下游任务）强制生成Impact Report并通知相关AGENT。支持从任意Checkpoint恢复执行后修改中间状态变量（Time-travel第一阶段），并从修改点继续执行。
    5. 提示词工程与演进
        [1]. 基于DSPy理念构建Prompt Optimizer：输入为用户反馈、AUD一致性报告、SP/BP Checkpoint人工反馈，输出优化后System Prompt与Few-Shot样本集。优化目标采用Pareto多目标权衡（结构完整性40% + 逻辑一致性35% + 成本效率25%），采用NSGA-II算法求解Pareto前沿。
        [2]. 受控演进机制：自动微调的提示词参数必须进入“待审查版本库”，经人工专家（首席战略官或授权代表）确认后方可推向生产环境。集成MLflow实现Prompt自动版本注册，支持A/B测试（分流比例可配置）与一键回滚（保留最近10个稳定版本）。
        [3]. 提示词契约测试：每次优化后自动运行Strat-Bench基准测试集，通过率<90%自动拒绝上线，并生成差异分析报告。演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
    6. 工具调用与集成
        [1]. 全面遵循MCP（Model Context Protocol）2025规范：工具箱作为独立微服务对外暴露符合MCP标准的能力发现接口，支持工具注册、版本控制、灰度发布与回滚。AGENT通过MCP Registry查询工具能力描述（输入/输出Schema、版本、可靠性评分）。
        [2]. 工具选择优化：基于语义匹配（60%）+历史成功率（40%）计算工具匹配度，匹配度<0.6时触发人工确认或启动多工具并行验证。预定义17种战略工具的最佳组合模板（如SWOT+TOWS自动组合），支持用户自定义模板。
        [3]. 强制契约化调用与沙箱执行：所有工具调用前验证输入Schema，调用后验证输出Schema，连续3次校验失败自动熔断该工具。代码执行在E2B沙盒中（网络隔离+权限最小化），执行结果经Validation Feedback闭环校验，最大重试次数3次。
    7. 仲裁与决策机制
        [1]. CEO AGENT内置裁决状态机，设定最大辩论轮次（默认3+风险等级，上限7轮）。辩论采用三阶段：发散（Temperature=0.8）、收敛（Temperature=0.5）、裁决（Temperature=0.2），强制随轮次增加提交具体修改JSON补丁。
        [2]. 辩论质量评估：每轮计算新信息增益率与重复率，增益率<10%且重复率>50%时强制终止辩论。辩论过程记录各AGENT贡献度（基于Shapley值量化），分歧点生成热力图。
        [3]. 多方案生成与人类一票否决：若未达成一致，生成Plan A（保守参数集）、Plan B（激进参数集）和Plan C（AI融合版），输出参数调整明细。强制暂停60秒提供用户介入窗口，窗口期可视化展示三套方案的对比差异（风险/收益/资源需求）及影响模拟器（支持用户滑动调整关键参数实时预览结果）。超时无用户操作则按CEO决策执行，但所有人工覆盖决策强制记录至战略档案库并标记“人工覆盖”。
    8. 审计与一致性保障
        [1]. AUD AGENT采用“旁路监听+异步阻断”模式，不参与生成。审计范围包括四维一致性检验：①事实一致性（对比原始文档切片）；②逻辑一致性（规则引擎检测内部矛盾）；③数值一致性（沙箱重计算关键指标，误差>5%标记异常）；④时效一致性（引用数据超12个月标记“数据陈旧”）。
        [2]. 分级熔断与根因分析：L1（幻觉概率>0.8）自动触发修正；L2（0.6-0.8）通知相关AGENT重试（最多2次）；L3（<0.6）暂停任务并生成Hallucination Report（包含错误类型分类：数据缺失/逻辑跳跃/数值错误/时效过期），人工介入后从断点恢复。
        [3]. 审计日志强制存储：所有审计发现、纠错过程、重试轨迹全量存储至不可篡改日志（WORM存储），满足SOX/ISO27001审计要求，支持按时间/角色/任务类型多维检索。
    9. 记忆与上下文管理
        [1]. 分层记忆架构：工作记忆（当前会话，有效期24h）、短期记忆（最近项目，有效期30天）、长期记忆（战略知识库，永久存储），各层有独立的存储引擎和检索策略。长期记忆将用户修正日志、决策复盘结果、执行结果差值反向固化为少样本提示词，每季度触发知识蒸馏与去重。
        [2]. 公共黑板机制：基于LangGraph StateSnapshot + Redis Cluster管理状态版本，支持多版本并发控制（MVCC）。关键结论写入黑板时必须附带重要性评分（1-5）、置信度、引用源切片ID、时效性，综合评分>3.5才写入，便于上下文压缩器智能筛选。
        [3]. 时间旅行调试：支持从任意Checkpoint恢复执行，用于问题复现与根因分析。所有状态快照与完整推理轨迹归档至数据湖，可按需解压缩回溯。
    10. 可解释性与溯源
        [1]. 结构化证据包标准：每次分析打包输出全量元数据，包括输入摘要哈希、任务分解图、工具调用轨迹（含版本）、关键中间变量快照、置信度评分及依据、引用源切片ID列表。
        [2]. 交互式溯源树：前端支持从结论节点逐层展开至原始数据，每层显示“数据来源→计算逻辑→中间结论”三元组，每个节点支持查看详细信息。
        [3]. 决策影响分析：量化各输入变量对最终决策的Shapley贡献值，高影响变量（>20%）自动高亮标注。支持反事实推理，输出“如果改变某假设，结论会如何变化”的对比报告和风险热力图。
    11. 主动能力
        [1]. 引入心跳机制：支持周期性自动唤醒（可配置频率），主动检查战略档案库中的待办事项、偏差预警、周期性任务，无需用户提问即可主动沟通。
        [2]. 战略偏差预警：实时监控实际执行数据与SP/BP规划的偏差，偏差超过阈值（默认10%）自动触发预警，生成偏差分析报告并推送相关AGENT。
        [3]. 主动知识更新：当检测到行业报告、市场数据、政策法规更新时，自动触发电请相关AGENT评估影响，重要影响主动推送CEO AGENT。
    12. 性能监控与行为分析
        [1]. 建立三维度指标体系：效率维度（任务完成率、平均时长、工具调用成功率）、质量维度（修正率<10%、幻觉率<5%、一致性得分>0.8）、成本维度（单任务API成本、Token消耗）。构建战略规划专用基准测试集（Strat-Bench）作为版本迭代的准入标准。
        [2]. 漂移检测双算法：KS检验检测输出分布偏移（月度基线对比），CUSUM检测性能指标连续下降（滑动窗口7天）。指标异常时启动自动根因分析，关联日志输出根因报告。
        [3]. 健康度仪表盘：实时可视化展示各AGENT的12项健康度指标，支持下钻分析与预警订阅。三级预警机制：黄牌（指标下降15%触发分析）、红牌（下降30%触发人工review）、停用（下降50%且连续三天，自动回滚至前一稳定版本）。
    13. 安全与防御
        [1]. 纵深防御体系：
            - 第一层（隔离）：三重硬隔离（Prompt隔离、工具隔离、数据隔离），基于角色权限最小化。
            - 第二层（执行）：所有代码在E2B沙盒中执行，严格网络隔离（仅白名单API）。
            - 第三层（检测）：实时监控视角越界（跨角色关键词频率>5%触发熔断）、幻觉累积（连续3次低置信度结论锁定Agent）。
            - 第四层（审计）：所有操作记录至WORM存储。
            - 第五层（熔断）：辩论过热保护、成本熔断。
        [2]. 敏感数据脱敏：审计日志和反馈记录自动脱敏处理（个人可识别信息、商业机密），脱敏策略定期评审更新。
    14. 成本控制
        [1]. 成本预测：任务启动前基于历史相似任务预测Token消耗与API成本，偏差>50%时提示用户确认。
        [2]. 三级熔断机制：
            - 任务级熔断：单次任务Token消耗超预设阈值（基础任务5K tokens，复杂规划50K tokens）立即终止。
            - 会话级熔断：日累计Token消耗超企业配额（默认100万tokens）暂停新任务，已启动任务完成。
            - 系统级熔断：连续3次工具执行失败或单任务成本超预算200%触发系统级告警，需管理员介入。

四、战略规划：系统核心功能是输出五年滚动战略规划（简称SP）和年度业务计划（简称BP），SP基于业务领先模型模型（简称BLM）制定，BP基于业务执行模型（简称BEM）制定。系统由LLM驱动，基于规划模型和方法论智能运行
    1.严格遵守BLM与BEM模型的规定流程与动作进行战略规划工作。SP输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定SP采用BLM模型，具体分为业绩差距分析、市场洞察(包括六个子步骤)、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [2].市场洞察
            (1).看趋势：建议工具PESTEL分析；（CEO/CMO/CTO AGENT负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS分析（内部部分）；（全部AGENT负责）
            (5).看机会：建议工具SWOT-TOWS分析、安索夫矩阵；（全部AGENT负责）
            (6).机会差距分析：建议工具SWOT-TOWS分析、GE-麦肯锡矩阵；（全部AGENT负责）
        [3].战略意图与目标：建议工具SWOT-TOWS分析、BSC（目标层面）；（全部AGENT负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE矩阵；（CEO/CMO/CTO AGENT负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具BSC、KPI；（全部AGENT负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如ADKAR）、BSC（学习与成长层面）；（全部AGENT负责）
    3.BP严格依赖SP的最终输出。构建战略解码器，实现从SP_Output_Schema到BP_Input_Schema的结构化映射转换。制定BP采用BEM模型，各阶段建议战略工具与AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具BSC（战略目标与主题澄清）；（全部AGENT负责）
        [2].导出中长期关键战略举措：建议工具SPACE矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT负责）
        [3].导出战略衡量指标：建议工具BSC、KPI；（全部AGENT负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具BSC、KPI（年度分解）、价值链分析（活动优化）；（全部AGENT负责）
        [5].分解关键措施和目标：建议工具BSC、KPI；（全部AGENT负责）
        [6].导出重点工作计划：建议工具BSC、KP行动方案；（全部AGENT负责）
    4.支持SP/BP各阶段Checkpoint机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP各个阶段，输出JSON思维链Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.支持A/B测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    7.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    8.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU驱动/CUDA版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个AGENT协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行SP与BP规划，生成PDF格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索RAG处理后的数据
    6.支持Checkpoint交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据

--------------

D
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的多模态数据转化为模型可理解、可检索、可推理、可演进的结构化知识资产
    1. 支持全模态文档格式与深层解析能力
        [1]. 支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif，以及音视频会议记录（基于ASR转录）
        [2]. 具备智能文档解析引擎，针对PDF采用双引擎策略（文本型优先PyMuPDF4LLM，扫描件/图像型使用OCR），OCR置信度<0.85的文本块需标记“待人工复核”
        [3]. 集成版面分析模型，精准识别多栏文本、页眉页脚、水印及嵌套表格，将复杂表格（财报/研报）转化为包含行列映射的结构化Markdown或JSON
        [4]. 支持图像/图表的多模态解析，自动生成描述性文本（alt-text）并独立向量化，实现图文语义联合检索
        [5]. 支持数学公式与手写体识别，通过LaTeX OCR及手写识别引擎提取关键公式与批注
    2. 支持企业级文档生命周期管理与血缘追踪
        [1]. 支持完整的文档操作功能：上传、更新、删除、分类管理、版本控制、统计信息
        [2]. 强制文档版本化，每次更新保留不可篡改快照，支持基于版本的回滚与变更轨迹追溯（血缘追踪）
        [3]. 支持软删除/硬删除策略及合规保留期，删除操作生成不可篡改的审计条目
        [4]. 基于RBAC实现文档级权限控制（读取/注释/导出/索引权），并支持敏感信息自动检测与脱敏（PII识别）
        [5]. 自动记录文档质量评分（文本密度、图表占比、引用完整性），低质量文档（<2.5分）触发人工审核
    3. 构建混合检索架构与图谱增强检索能力
        [1]. 使用本地词嵌入模型bge-m3构建稠密向量索引，结合BM25构建稀疏索引，支持双路召回与RRF融合排序
        [2]. 集成重排序模型（如bge-reranker-v2-m3）对初检结果进行精排，提升Top-5相关性
        [3]. 引入知识图谱抽取流水线，自动识别战略实体（公司/产品/市场/技术/财务指标）及关系，构建实体-关系-事件图谱，支持图-向量混合检索（GraphRAG）
        [4]. 支持多跳推理检索，通过图谱关系发现隐式战略传导链（如政策变动→原材料上涨→利润率影响）
        [5]. 支持动态语义分块，基于文档逻辑结构（标题/章节）与主题边界自适应生成语义连贯片段（平均长度300±50 tokens）
        [6]. 支持元数据五级索引：文档级/章节级/段落级/图表级/关键假设标记
    4. 实施分层检索策略与自适应查询路由
        [1]. 建立四级分层检索架构：
            L1：文档级摘要（全局语义）
            L2：章节级摘要
            L3：段落级切片
            L4：实体级片段（关键数据点/假设/决策依据）
        [2]. 构建查询理解与路由引擎，根据查询复杂度自动选择检索路径（简单查询仅BM25，复杂查询启动三重融合+图谱扩展）
        [3]. 支持Parent-Child索引，实现“自顶向下”与“自底向上”双向检索
    5. 支持结构化摘要生成与多视角提炼
        [1]. 调用网络大模型API生成动态长度结构化摘要（企业年报800字/市场简报200字/咨询报告500字），输出须遵循契约化JSON Schema（含核心指标、关键实体、结论基调）
        [2]. 支持多视角摘要生成：执行摘要（CEO视角）、风险摘要（CFO视角）、创新摘要（CTO视角）
        [3]. 部署摘要质量评估器（信息熵+关键实体覆盖率），评分<0.7触发二次生成或人工审核
        [4]. 摘要存入元数据库，经bge-m3嵌入后存入向量库，同时构建摘要→原文片段的双向索引
    6. 采用Agentic RAG与自适应纠偏机制
        [1]. 摒弃静态相关性阈值，采用LLM-as-a-Judge对检索结果进行实时多维评估（相关性、充足性、冲突性）
        [2]. 若评估为“数据不足”或“存在事实冲突”，自动触发补救工作流：查询重写、调用白名单财经API补充数据、或生成“数据缺口报告”供用户补充
        [3]. 支持Self-RAG机制，根据查询类型动态调整阈值（事实型0.75/推理型0.55/创新型0.45），低置信度结论强制标注并记录拒绝原因至证据包
    7. 强制细粒度引文溯源与可解释性支持
        [1]. 检索与生成过程保留原子化证据单元：片段ID、原始文本快照（512字符）、页码/段落/坐标、采集时间戳、来源可信度
        [2]. 支持指标级溯源：关键财务/运营指标直接关联数据源（如“营收增长率→2025年报P23”）
        [3]. 假设级溯源：通过GraphRAG链接战略假设变量至支撑证据，形成可追溯的推理链
        [4]. 前端支持交互式溯源树，从结论逐层展开至原始数据，每层显示“数据来源→计算逻辑→中间结论”三元组
    8. 构建三级分层战略记忆架构与知识演进闭环
        [1]. 工作记忆：当前SP/BP会话上下文，有效期24小时，基于Redis Cluster持久化
        [2]. 短期记忆：近3年战略档案+用户修正日志，有效期30天，带时效性标签存储于向量库
        [3]. 长期记忆（战略知识库）：永久存储历年SP/BP关键假设变量、决策依据、实际执行结果的差值、用户修正轨迹
        [4]. 战略档案结构化存储Schema：plan_id, period, key_assumptions[], decision_reasons[], execution_results{metrics, time_series}, deviation_analysis, correction_logs[]
        [5]. 季度知识蒸馏：聚类高频修正模式，生成领域少样本提示模板，固化前需通过一致性校验与审阅签名
    9. 实施全链路数据质量控制与门禁
        [1]. 数据预处理流水线包括：格式验证（编码/乱码率）、内容完整性检查（关键章节缺失）、重复检测（SIMHash字面重复+bge-m3语义重复双校验）
        [2]. 文档可信度评分：基于权威性（年报=1.0/咨询报告=0.8/网络=0.5）、时效性（每6个月衰减0.1）、完整性综合计算，可信度<0.6阻断入库
        [3]. 建立CI/CD质量门禁，入库前自动触发质量检查，低质量文档进入人工审核队列
    10. 建立全链路可观测性与性能漂移检测
        [1]. 核心评估指标：
            检索层：MRR@10 ≥0.75、Recall@50 ≥0.85、NDCG@20 ≥0.80
            生成层：Faithfulness ≥0.90、Answer Relevancy ≥0.85
            端到端：Task Success Rate ≥85%（基于Strat-Bench）
        [2]. 双算法漂移检测：KS检验（月度分布偏移） + CUSUM（连续7天性能下降）
        [3]. 自动根因分析引擎：关联日志、数据变更、模型版本，输出根因分类与修复建议
        [4]. 健康度仪表盘：实时展示12项核心指标，支持三级预警（黄牌/红牌/停用）与下钻分析
    11. 遵循企业级安全与合规基线
        [1]. 所有检索日志、生成内容、用户修正记录写入不可篡改存储（WORM），保留期≥7年满足SOX/ISO27001要求
        [2]. 上传前执行PII探测与脱敏，敏感字段实现加密-at-rest与字段级访问控制
        [3]. 数据主权隔离：所有模型（嵌入/重排序）均在本地部署，关键数据永不离开企业边界
    12. 支持成本与版本治理
        [1]. 记录每次数据处理任务的Token消耗与预估USD成本，产出成本审计信息
        [2]. 嵌入模型、索引、摘要生成器等核心组件均纳入版本管理，支持A/B测试与一键回滚
        [3]. 实现嵌入适配器接口，可无缝切换至不同本地模型（如bge-m3后继版本），确保技术架构的灵活性
}

Q
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化的多模态数据转化为模型可理解、可推理、可溯源的结构化知识资产，并建立企业级数据治理与合规基线
    1.支持原生多模态文档接入与高保真解析，包括 pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif 及音视频转录文本
        [1].引入视觉文档理解（VDU）与版面分析能力，精准识别多栏文本、页眉页脚、嵌套表格与图表，强制将复杂财务报表转化为包含行列映射关系的结构化数据，防止数据失真
        [2].支持扫描件与图片的 OCR 抽取，记录 OCR 置信度，若置信度低于阈值（默认 0.85）自动标记为“待人工校验”并阻断自动化流程
        [3].支持多语种与编码自动识别，记录原始文件哈希（SHA256）、MIME 类型、来源及版权许可元字段
    2.支持企业级文档全生命周期管理与血缘追踪，包括文档上传、更新、删除、分类、版本控制与统计信息
        [1].强制版本化管理：每次更新保留不可篡改快照，存储差异摘要（diff）与操作元数据，支持基于版本的回滚与审计
        [2].建立数据血缘追踪：确保每个解析后的数据切片可追溯至导入批次、操作人及原始文件版本，支持版本影响分析
        [3].实施细粒度权限控制（RBAC）：基于角色与数据标签控制读取、注释、导出与索引权限，支持软删除与合规保留策略
    3.构建混合检索架构与自适应嵌入流水线，默认使用本地词嵌入模型（如 bge-m3 标准）
        [1].实现嵌入适配器接口：支持无缝切换至其他嵌入模型或版本，记录模型哈希与版本，支持嵌入漂移监控与自动化回退
        [2].构建“语义 + 关键词 + 图谱”三重索引：结合稠密向量、BM25 稀疏检索与知识图谱实体关系，支持混合检索排序（RRF）
        [3].支持索引治理：包括索引版本管理、原子批量操作、回滚机制及索引一致性检查（checksum）
    4.支持完整的 RAG 操作功能与质量度量，包括元数据管理、单/批量索引、语义查询、上下文检索与统计信息
        [1].定义标准化切片策略：支持基于文档结构（标题树）与语义连贯性的动态分块，持久化 chunk 到 doc 的映射关系
        [2].提供检索质量实时反馈：每次检索返回 RAG Triad 指标（Faithfulness 忠实度、Answer Relevancy 相关性、Contextual Precision 精度）
        [3].支持检索指标监控：包括 recall@k、MRR、延迟（P95）、索引构建时间及存储大小
    5.支持契约化结构化摘要生成，调用大模型 API 生成动态长度摘要并存入元数据库
        [1].摘要输出强制遵守 JSON Schema 契约：包含摘要文本、哈希、关键实体、覆盖率评分、事实一致性评分及生成成本
        [2].实施摘要质量门禁：自动执行信息熵、覆盖率与事实一致性检测，若质量低于阈值触发二次生成、本地模型 fallback 或人工复核
        [3].建立摘要与原文切片的可验证引用：记录 docID、chunkID、字符偏移量及片段哈希，写入证据包
    6.支持分层检索策略与图谱增强推理，先检索文档摘要锁定范围，再检索具体切片
        [1].定义四级分层检索架构：L1 文档级摘要、L2 章节级摘要、L3 段落级切片、L4 实体级片段，支持基于查询复杂度的动态层数决策
        [2].支持图谱多跳推理：宏观问题优先触发图谱社区摘要检索，微观问题支持 Parent-Child 检索，通过图谱关系补全隐式战略传导链
        [3].支持语义重排序：在分层检索中集成重排序模型，基于联合置信度决定是否触发自我修正或人工介入
    7.支持高保真引文索引与交互式溯源，确保后续生成的内容可审计
        [1].引文粒度标准：精确至文档 ID、切片 ID、字符偏移量及片段哈希，输出引用需包含来源元信息与展示型引用片段
        [2].支持视觉级溯源：检索处理时映射原文档的物理视觉坐标（Bounding Box），前端交互可直接高亮展示原始 PDF 或图像中的具体出处
        [3].支持版权与许可控制：若引用来源受限制，自动标注并阻断直接复制，提供授权申请或摘要替代方案
    8.支持增强型 Self-RAG 机制与自适应纠偏，若检索到的文档相关性得分低于设定阈值，避免强行生成
        [1].实施多维置信度组合：结合检索相似度、摘要覆盖率与事实一致性，允许任务级自定义阈值策略
        [2].提供多种响应策略：包括提示用户补数据、自动触发受控补检、生成低置信度草稿并显著标注、记录拒绝原因至修正轨迹库
        [3].支持检索反思日志：记录每次“跳过生成”或“数据不足”的决策依据，用于优化检索策略与数据采集优先级
    9.构建时序知识图谱式战略档案库作为长期记忆模块，实现知识持续演进
        [1].结构化存储历年 SP/BP 的关键假设变量、决策依据与实际执行结果的差值，构建“假设 - 执行 - 偏差”三元组
        [2].实施事实时效性管理：为每个存储事实标注有效起止时间，超期事实自动衰减检索权重，支持基于时间轴的版本化检索
        [3].支持知识蒸馏与固化：将高频用户修正日志与偏差复盘反向固化为少样本提示词，经一致性校验后更新战略知识库
    10.支持全链路数据质量控制与预处理流水线，包括格式验证、内容完整性检查和重复检测
        [1].采用多层质量检查：文件哈希校验、SIMHash 文本近似、向量相似度去重及表格财务一致性校验
        [2].实施 CI/CD 质量门禁：入库前自动触发质量检查流水线，阻断低可信度、高重复率或高乱码率文档，支持人工审核通道
        [3].支持敏感信息检测：集成 PII 识别模块，自动标记商业机密字段，按脱敏级别分级存储或加密
    11.建立全维度性能监控与漂移检测机制，定期评估 RAG 检索准确率、摘要生成质量与系统成本
        [1].定义核心监控指标集：包括检索延迟、生成延迟、幻觉率、修正率、任务成功率及单任务 API 成本
        [2].实施双算法漂移检测：使用 KS 检验检测输出分布偏移，使用 CUSUM 检测性能指标连续下降，配套根因分析 playbook
        [3].支持成本审计与熔断：记录预计与实际 Token 消耗，任务级、会话级与系统级成本超限触发熔断告警
    12.建立企业级合规与安全基线，满足审计与数据主权要求
        [1].实施不可篡改审计日志：所有检索日志、生成内容、用户修正记录写入 WORM 存储，保留期满足 SOX/ISO27001 要求
        [2].支持数据主权隔离：核心数据默认本地化处理，支持嵌入模型与索引的本地部署，确保敏感数据不出域
        [3].支持合规导出：所有审计与性能监控数据支持以审计友好格式按时间、角色、任务维度导出
}

G
{
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化与半结构化的多模态数据转化为模型可理解、可推理、可溯源的结构化知识资产
    1. 支持原生多模态文档接入与视觉文档理解（VDU）解析
        [1]. 支持的文档格式包括pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif。
        [2]. 采用视觉文档理解大模型（如Docling）进行深层解析，精准剥离多栏文本、页眉页脚与水印。针对复杂财务报表与嵌套表格，强制保留行列映射关系并无损提取为结构化JSON或Markdown，杜绝关键财务数据失真。
    2. 支持企业级高保真文档生命周期管理与合规审计
        [1]. 支持完整的文档操作功能，包括文档上传、更新、删除、分类管理、版本控制与统计信息。建立严格的数据血缘追踪（Data Lineage），文档更新或强删除时自动触发级联刷新，保留不可篡改的变更快照。
        [2]. 实施合规防御层，强制执行入库前加密（加密-at-rest）与PII敏感数据（如客户名单/核心财务参数）动态脱敏。
        [3]. 所有变更轨迹与检索日志写入不可篡改存储（WORM），满足SOX/ISO27001审计要求，并支持按角色与维度的审计友好格式导出。
    3. 构建基于图谱增强与晚期交互的混合检索引擎（Graph-Enhanced Hybrid RAG）
        [1]. 使用本地词嵌入模型（如bge-m3）结合BM25算法，建立“语义+关键词”双路召回基座。
        [2]. 外挂重排序模型（如ColBERTv2）对候选切片进行晚期交互精细化排序，提升头部相关性。
        [3]. 引入知识图谱抽取流水线，自动识别战略文本中的核心实体（市场、竞对、财务指标）及逻辑关系，支持多跳推理检索，以补全纯向量检索无法发现的隐式战略传导链（如：宏观政策变动->行业原材料上涨->企业利润率预期影响）。
    4. 支持智能语义切片与带有权限隔离的RAG基础操作
        [1]. 摒弃固定字数切块，采用基于文档逻辑结构（标题树）与上下文凝聚度的智能语义分块（Agentic Chunking），确保切片语境完整。
        [2]. 支持完整的RAG操作：结构化元数据注入、单文档/批量索引、语义查询。
        [3]. 确立“信源分级权威权重”（如审计财报1.0，咨询报告0.8，网络数据0.5），并在检索端外挂RBAC（基于角色的访问控制），实施严格的视角与数据源隔离。
    5. 支持契约化多视角摘要生成与认知锚点构建
        [1]. 调用网络大模型API基于查询复杂度自适应生成结构化摘要，输出强制校验JSON Schema契约（含关键实体、信息熵、置信度等字段）。
        [2]. 摘要不仅存入元数据库并经词嵌入进入向量库，同时将提取的微观三元组写入知识图谱，构建宏观认知锚点。
        [3]. 部署摘要质量评估器，综合评分（信息熵+覆盖率+一致性）达标后方可入库，否则触发二次生成或人工复核。
    6. 支持基于图谱与父子层级的自适应路由检索策略
        [1]. 根据Query意图与复杂度实施自适应路由：宏观问题优先触发图谱社区摘要（Community Summaries）检索；微观问题优先检索文档摘要锁定范围，再下钻检索具体切片（Parent-Child Retriever）。
        [2]. 引入自适应跳过机制，当上层摘要联合置信度极高或极低时，动态调整下钻深度，优化检索算力分配。
    7. 支持高保真视觉级（Bounding Box）引文溯源机制
        [1]. 检索与生成处理时，强制保留引文的“三元组”特征（文档ID + 切片哈希Hash + 版面视觉坐标Bounding Box）。
        [2]. 确保后续Agent生成的内容具备极强的可审计性，前端交互可“点击溯源”，直接高亮展示原始PDF或图像中的具体出处段落或图表单元。
    8. 部署纠错型检索（CRAG）与多维置信度兜底拦截
        [1]. 废弃单一静态阈值，采用多维置信度组合（相关性、覆盖率、事实一致性）对检索结果进行实时评估（LLM-as-a-Judge）。
        [2]. 若触发“数据不足”或“事实冲突”熔断，系统坚决拦截强行生成（防幻觉），自动向环境调度器申请调用内部SQL或外部Web Search工具进行动态数据补全；若仍无法补全，则明确输出“数据缺口报告”请求用户补充。
    9. 构建基于“假设-执行-偏差”的时序战略档案库（长期记忆）
        [1]. 建立四维时序记忆模块。结构化存储历年SP/BP的“关键假设变量、决策依据模型、实际执行结果的差值”，形成演进轨迹。
        [2]. 建立专家签名（Reviewer Signoff）确认机制，将高质量的用户修正日志与差值复盘数据，经自动去重与季度知识蒸馏后，反向固化为Prompt Optimizer的少样本（Few-Shot）提示词，实现智库认知的自我进化。
    10. 实施多维度数据防腐预处理与质量门禁
        [1]. 构建入库前清洗流水线，涵盖格式防腐验证、内容完整性检查（缺失关键字段阻断入库）。
        [2]. 引入高级排重与一致性检测：采用语义级相似度去重替代传统字面去重；强制执行表格/财务一致性校验（行列总计、单位规范化），对于研报冲突数据打上多源对立标签供Agent裁决。
    11. 建立RAGOps全链路监控与双算法漂移检测机制
        [1]. 确立性能延迟红线（检索P95 < 300ms，生成P95 < 5s），并对核心战略任务执行更严苛的质量阈值。
        [2]. 实时监控RAG Triad（忠实度、答案相关性、上下文精度）指标。
        [3]. 运用KS检验（检测月度分布偏移）与CUSUM（检测连续下滑）双算法进行数据与模型漂移检测。指标越界自动触发分级告警，并生成包含根因分析、回滚建议的报告，同时触发测试基准集（Strat-Bench）回归。
    12. 确立MCP生态互操作接口与系统级成本熔断机制
        [1]. 采用模块化Adapter设计模式，核心数据处理组件对外暴露符合MCP（Model Context Protocol）标准规范的能力接口，便于未来无缝对接ERP、BI系统。
        [2]. 建立严格的成本工程管理，基于Query与文档量预估并记录每次任务的Token消耗与预估USD成本。实施任务级、会话级成本熔断机制，防范高并发API算力成本失控。
}

O
{
一、数据处理：处理历年战略规划/SP、年度业务计划/BP、企业年报、市场/咨询报告、财务报表及企业内部业务数据；将多模态非结构化/半结构化资料（文本/表格/图像/公式/音视频转录）转化为模型可理解、可检索、可推理、可溯源的结构化知识资产。

1. 支持文档格式与基础解析
   [1]. 支持主流文档与媒体格式：pdf, txt, doc, docx, ppt, pptx, xls, xlsx, csv, jpeg, png, gif；支持批量压缩包导入（zip/tar）。  
   [2]. 表格结构化重建：对xls/xlsx/csv及表格型PDF能提取行列语义并输出结构化JSON（包含表头/列类型）。  
   [3]. OCR与公式抽取：对扫描件或图像PDF进行OCR，提取置信度并标注；支持数学公式的LaTeX表达或结构化表示（若置信度<0.85则标注需人工复核）。  
2. 文档生命周期与基础治理
   [1]. 文档上传/更新/删除/分类与版本快照（不可篡改 snapshot）功能；每次更改记录操作者、时间戳与diff摘要。  
   [2]. 元数据标准化：入库强制校验最小元字段（creator, created_at, source, license, business_domain）；关键字段缺失阻断入库。  
3. 基本RAG流水线（混合检索）
   [1]. 支持Local dense embedding（默认bge-m3）与BM25稀疏检索并行初检，保留Top-N候选用于后续精排。  
   [2]. 语义分块：由语义边界而非固定字数切片，平均片段长度目标≈300 tokens（允许配置）。  
4. 引文溯源：对检索到的片段保存引用三元组（doc_id, chunk_id, snippet_text/字符范围）并暴露可跳转定位。  
5. 数据质量门禁：入库前进行格式验证、重复检测（SIMHash）和完整性检查；设定简单阈值（如正文长度>100字符、元数据完整率≥90%）。  
6. Self-RAG 基础策略：若检索相关性低于系统阈值（默认0.6），标注为“数据不足”并阻断直接生成；提供标准化“低置信提示”。  
7. 全模态高保真解析与版面保留
   [1]. 支持PDF版式保留模式（记录元素坐标 x,y,width,height），对图表/表格/注释保留视觉位置信息以支持“图表-正文”空间关联检索。  
   [2]. 多模态对齐：实现图文联合嵌入空间（CLIP-like 投影），支持“以图搜文/以文搜图”跨模态检索。  
8. 混合检索与重排序策略
   [1]. 三路融合策略（Dense bge-m3 + Sparse BM25 + Graph/metadata signals）并支持可配置权重与RRF融合；集成重排序器（ColBERT-v2 或等效）对Top-K进行精排。  
   [2]. 分层检索（L1-L4）：L1跨文档摘要→L2文档摘要→L3文档切片→L4实体级片段，并实现Parent-Child双向索引关系。  
9. 结构化摘要与质量门禁：领域模板化结构化摘要（财务/市场/技术）输出遵守JSON Schema，摘要质量评估器（信息熵+实体覆盖率）评分<0.7触发二次生成或人工审核。  
10. 时序战略档案库（长期记忆）：以结构化方式存储历年SP/BP的关键假设变量、决策依据与执行偏差，并对事实打上valid_from/valid_until标注。  
11. 多维数据质量控制流水线：格式验证、完整性检查、语义重复检测（bge-m3相似度）与可信度评分（DQI=0.4*完整性+0.3*唯一性+0.3*时效性），DQI<0.6进入人工审核。  
12. RAGOps与漂移检测：建立检索/生成/嵌入的监控与告警体系（关键指标：precision@k, recall@k, MRR, Faithfulness, embedding drift），并采用KS检验与CUSUM作为双重漂移检测算法（按日/周/月频率）。  
13. 引文精细化与时效性管理：引文元数据增强（confidence_score, timeliness_score, source_authority），并在引用陈旧（>12个月）时强制提示或降权。  
14. GraphRAG 与知识图谱增强检索：在入库时抽取实体-关系-事件三元组，生成可检索的知识图谱并支持图谱驱动的多跳推理检索（解决跨文档因果链路查询）。  
15. 多维证据包与可交互溯源：每次分析/输出打包证据包（包含input summary hash, task decomposition, tool versions, doc chunk IDs, bbox/visual coordinates, confidence metrics），支持前端逐层展开溯源树至原始文档坐标点。  
16. 自适应 Agentic RAG 与补救机制：LLM-as-a-Judge 或检索判定器驱动的动态阈值与自动补救：当“数据不足/冲突”被判定时，系统按策略自动扩展检索（时间/来源/模态）、调用白名单数据源或生成数据缺口报告并提示用户补充。  
17. 知识演进与受控固化：修正固化规范：仅当修正满足（置信度>0.85 且>=3用户确认 或 经审阅签名）方可固化为少样本提示词；并保留固化前后差异记录与审计证据。  
18. 可配置成本与熔断策略：在数据处理/摘要生成阶段记录预计/实际API成本与token消耗，支持任务级/会话级/系统级三级成本熔断并提供成本-质量权衡视图。  
19. 本地优先与数据主权：对敏感或受限数据默认采用本地优先策略（本地索引/本地模型处理），外部调用需经审计与白名单批准，并支持字段级加密与脱敏存储。  
20. 合规、审计与不可篡改日志：所有检索请求、生成结果、用户修正与系统决策记录写入可导出的不可篡改审计日志（WORM或等效技术），满足SOX/ISO27001类审计基本要求并支持导出。  
21. 开放性与适配性：定义Data Contract（文档/片段/摘要/embedding/graph）并在CI中加入契约测试；支持embedding adapter接口以便替换或回滚embedding模型版本。  
22. 可观测性与运维支持：提供端到端观测面板（检索延迟P95、生成延迟P95、precision/MRR、幻觉率、token消耗）并支持下钻至模块/文档级进行根因分析与回放失败case。  
}





















一、数据处理：处理历年战略规划/SP、年度业务计划/BP、企业年报、市场/咨询报告、财务报表及企业内部业务数据；将多模态非结构化/半结构化资料（文本/表格/图像/公式/音视频转录）转化为模型可理解、可检索、可推理、可溯源的结构化知识资产。
    1.支持全模态文档格式与深层解析能力
        [1].支持主流文档与媒体格式的接入：pdf, txt, doc, docx, ppt, pptx, xls, xlsx, csv, jpeg, png, gif；支持批量压缩包导入（zip/tar）。
        [2].全模态高保真解析与版面保留：支持PDF版式保留模式，记录元素视觉坐标（x,y,width,height），对图表/表格/注释保留视觉位置信息以支持“图表-正文”空间关联检索。实现图文联合嵌入空间（CLIP-like投影），支持“以图搜文/以文搜图”跨模态检索。
        [3].表格结构化重建与公式抽取：对xls/xlsx/csv及表格型PDF能精准提取行列语义并输出结构化JSON（包含表头/列类型）。对扫描件或图像PDF进行OCR抽取，支持数学公式的LaTeX表达或结构化表示（记录置信度，若置信度<0.85则标注需人工复核）。
    2.支持企业级文档生命周期管理与基础治理
        [1].支持完整的文档操作功能，包括上传、更新、删除、分类与版本快照（不可篡改snapshot）功能；每次更改强制记录操作者、时间戳与差异摘要（diff），支持血缘追踪。
        [2].元数据标准化：入库强制校验最小元字段（creator, created_at, source, license, business_domain）；关键字段缺失自动阻断入库。
    3.构建混合检索架构与图谱增强检索能力
        [1].使用本地词嵌入模型（默认bge-m3）与BM25稀疏检索构建并行初检，保留Top-N候选。支持三路融合策略（Dense + Sparse + Graph/metadata signals），支持可配置权重与RRF融合排序。
        [2].集成晚期交互重排序器（ColBERT-v2或等效模型）对Top-K进行精排，提升头部相关性。
        [3].引入GraphRAG知识图谱增强检索：在入库时自动抽取实体-关系-事件三元组，生成可检索的知识图谱并支持图谱驱动的多跳推理检索，以解决跨文档因果链路查询。
        [4].智能语义分块：由文档逻辑边界而非固定字数进行语义切片，平均片段长度目标约300 tokens（支持自定义配置）。
    4.实施分层检索策略与自适应查询路由
        [1].建立四级分层检索架构（L1-L4）：L1跨文档摘要 -> L2文档摘要 -> L3文档切片 -> L4实体级片段。
        [2].实现Parent-Child双向索引关系，支持查询意图自适应路由，实现“自顶向下”锁定范围与“自底向上”下钻细节的双向检索流转。
    5.支持结构化摘要生成与多视角提炼
        [1].基于领域模板（财务/市场/技术）调用大模型API生成多视角结构化摘要，输出严格遵守JSON Schema契约。
        [2].部署摘要质量评估器，综合评分（信息熵+实体覆盖率）<0.7时，自动触发二次生成或进入人工审核队列。
    6.采用Agentic RAG与自适应纠偏机制
        [1].建立Self-RAG基础策略：若检索相关性得分低于系统设定阈值（默认0.6），自动标注为“数据不足”并阻断模型强行生成，提供标准化的“低置信提示”。
        [2].LLM-as-a-Judge或检索判定器驱动的动态补救机制：当判定为“数据不足/事实冲突”时，系统按策略自动扩展检索范围（时间/来源/模态）、调用白名单数据源API补充，或生成数据缺口报告请求用户补充。
    7.强制细粒度引文溯源与可解释性支持
        [1].高保真引文三元组：对检索到的片段保存引文元数据（doc_id, chunk_id, snippet_text/字符范围），并与版面视觉坐标（Bounding Box）映射，前端暴露可交互的高亮跳转定位。
        [2].引文精细化与时效性管理：增强引文元数据（confidence_score, timeliness_score, source_authority），当引用数据陈旧（>12个月）时强制生成提示或进行降权处理。
        [3].输出多维证据包：每次分析打包证据包含输入摘要哈希、任务分解树、工具版本、文档切片ID、视觉坐标及置信度指标，支持前端逐层展开溯源树至原始数据源。
    8.构建时序战略档案库架构与知识演进闭环
        [1].构建长期记忆模块：以结构化Schema存储历年SP/BP的关键假设变量、决策依据与执行偏差差值，并对所有事实打上有效起止时间标注（valid_from/valid_until）。
        [2].受控知识演进与固化：用户修正轨迹的固化需遵守严格规范：仅当修正满足置信度>0.85且>=3次用户确认（或经专家审阅签名）后，方可固化为少样本提示词（Few-Shot），并永久保留固化前后的差异记录与审计证据。
    9.实施全链路数据质量控制与门禁
        [1].构建入库前多维质量控制流水线，包含格式验证、完整性检查及语义重复检测（基于bge-m3相似度与SIMHash算法）。
        [2].建立可信度评分门禁：基于公式计算DQI（Data Quality Index = 0.4*完整性 + 0.3*唯一性 + 0.3*时效性），当DQI<0.6时自动阻断入库并转入人工审核通道。
    10.建立全链路可观测性与性能漂移检测
        [1].提供RAGOps端到端观测面板：实时监控检索延迟P95、生成延迟P95、precision@k, recall@k, MRR, Faithfulness, 幻觉率及Token消耗等关键指标，支持下钻至模块/文档级进行根因分析与失败Case回放。
        [2].双重算法漂移检测：采用KS检验与CUSUM双算法，按日/周/月频率定期检测输出分布偏移、性能连续下降与Embedding Drift，异常情况自动触发分级告警。
    11.遵循企业级安全、合规与数据主权基线
        [1].本地优先与数据主权隔离：对核心敏感数据或受限数据默认采用本地优先策略（本地索引/本地模型基座处理），外部API调用需经审计与白名单批准。支持字段级加密与动态脱敏存储。
        [2].合规与不可篡改审计：所有检索请求、生成结果、用户修正轨迹与系统决策记录强制写入不可篡改审计日志（WORM或等效技术），满足SOX/ISO27001类审计基本要求，并支持按角色和维度的合规导出。
    12.支持成本、版本治理与开放适配
        [1].可配置成本与熔断策略：在数据处理与摘要生成阶段记录预计/实际API成本与Token消耗，支持任务级/会话级/系统级三级成本熔断机制，并提供“成本-质量权衡”可视化视图。
        [2].建立Data Contract（数据契约）：统一定义文档/片段/摘要/Embedding/Graph的结构化契约，并在CI/CD流水线中加入强制契约测试。
        [3].接口与版本解耦：提供Embedding Adapter适配器接口，便于无缝替换、平滑升级或一键回滚Embedding模型版本，保障技术底座的扩展性与稳定性。




一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将非结构化与半结构化的多模态数据转化为模型可理解、可推理、可溯源的结构化知识资产，并建立企业级数据治理与合规基线。
    1.支持全模态文档格式与高保真深层解析能力
        [1].支持主流文档与媒体格式批量导入：pdf,txt,doc,docx,ppt,pptx,xls,xlsx,csv,jpeg,png,gif，及音视频转录文本，支持批量压缩包导入（zip/tar）。
        [2].表格与版式结构化重建：对xls/xlsx/csv及表格型PDF精准提取行列语义并输出包含表头/列类型的结构化JSON；支持PDF版式保留模式，记录元素视觉坐标（x,y,width,height）以支持“图表-正文”空间关联检索。
        [3].OCR识别与公式抽取：对扫描件或图像PDF进行OCR解析，提取置信度并标注；支持数学公式的LaTeX表达或结构化表示，置信度<0.85的文本块强制标记待人工复核。
        [4].多模态语义对齐：实现图文联合嵌入空间，支持“以图搜文/以文搜图”的跨模态检索。
    2.支持企业级文档全生命周期管理与血缘追踪
        [1].支持完整的文档操作功能，包括文档上传、更新、删除、分类管理与版本快照（不可篡改 snapshot）；每次更改详细记录操作者、时间戳与差异摘要（diff）。
        [2].实施元数据标准化与入库治理：入库强制校验最小元字段集（creator, created_at, source, license, business_domain），关键字段缺失自动阻断入库。
        [3].建立细粒度数据血缘追踪：确保每个解析后的数据切片均可追溯至导入批次与原始文件版本，支持版本影响分析与回滚。
    3.构建混合检索架构与图谱增强检索（GraphRAG）能力
        [1].采用三路融合检索策略：结合本地稠密向量（默认bge-m3）、BM25稀疏检索与知识图谱实体关系信号，支持可配置权重与RRF融合排序；集成重排序器（如ColBERT-v2）对Top-K进行精排。
        [2].实施智能语义切片：由文档逻辑结构与语义边界驱动动态分块，摒弃固定字数切块，平均片段长度目标约300 tokens（支持配置），并持久化chunk到doc的映射关系。
        [3].图谱增强与多跳推理：文档入库时自动抽取实体-关系-事件三元组，生成可检索的知识图谱，支持图谱驱动的多跳推理检索以打通跨文档的因果传导链路。
    4.实施分层检索策略与自适应查询路由
        [1].定义四级分层检索架构：L1跨文档全局摘要 → L2单文档摘要 → L3文档段落切片 → L4实体级片段/关键指标。
        [2].实现自适应路由与双向索引：系统根据查询意图与复杂度动态决定检索层级，并支持Parent-Child双向索引关联，实现“自顶向下”与“自底向上”的灵活检索。
    5.支持契约化结构化摘要生成与质量评估
        [1].领域模板化摘要生成：调用大模型API生成财务、市场、技术等多视角摘要，输出强制遵守JSON Schema契约（包含核心指标、摘要文本、置信度等）。
        [2].部署摘要质量门禁：内置摘要质量评估器，综合计算信息熵与实体覆盖率，评分低于阈值（默认0.7）自动触发二次生成、本地模型回退或人工复核。
    6.采用Agentic RAG与自适应纠偏补救机制
        [1].摒弃静态检索策略，引入LLM-as-a-Judge与检索判定器对检索结果的相关性、充足性及冲突性进行实时评估。
        [2].自动化补救与扩展检索：若检索相关性低于阈值（默认0.6），判定为“数据不足/冲突”并阻断直接强行生成。系统按策略自动扩展检索范围（时间/来源/模态）、调用白名单外部数据源，或生成结构化“数据缺口报告”请求用户补充。
    7.强制高保真细粒度引文溯源与可解释性支持
        [1].建立原子化引文三元组：每次检索与生成强制保留（文档ID, 切片ID, 字符范围/片段哈希），并强化引文元数据（置信度、时效性得分、来源权威度）。
        [2].视觉级交互溯源：针对PDF和图像提取的内容，前端支持根据保留的视觉坐标（Bounding Box）进行交互式点击溯源，直接在原文档高亮出处。
        [3].多维证据包打包：每次分析任务打包完整的证据包（含输入摘要哈希、任务分解、工具版本、切片ID及视觉坐标），支持前端展开完整推理溯源树。
    8.构建时序战略档案库与知识受控演进闭环
        [1].构建长期记忆战略库：以结构化方式存储历年SP/BP的关键假设变量、决策依据模型与实际执行结果偏差，形成“假设-执行-偏差”演进轨迹。
        [2].实施事实时效性管理：为档案库中的每个事实打上有效起止时间（valid_from/valid_until）标签，超期事实自动降权或强制提示。
        [3].知识受控蒸馏与固化：高质量的用户修正日志与差值复盘数据，须满足严格条件（置信度>0.85且多用户确认或专家签名）后，方可反向固化为少样本（Few-Shot）提示词，并保留固化前后的审计证据。
    9.实施全链路数据防腐与质量控制门禁
        [1].构建多维数据预处理流水线：入库前自动执行格式验证、内容完整性检查（正文长度>100字符等）及乱码率检测。
        [2].重排与去重处理：采用SIMHash与向量相似度双重检测机制拦截字面与语义重复文档。
        [3].动态文档可信度评分（DQI）：基于完整性（40%）、唯一性（30%）与时效性（30%）综合计算DQI，评分<0.6的文档阻断入库并转入人工审核通道。
    10.建立RAGOps全链路可观测性与性能漂移检测
        [1].定义核心度量指标面板：实时监控检索延迟（P95）、生成延迟（P95）、Precision@k、Recall@k、MRR及Faithfulness幻觉率等端到端指标。
        [2].双重算法漂移检测：采用KS检验（检测分布偏移）与CUSUM算法（检测连续性能下降）按日/周/月频率监控数据与嵌入模型漂移情况。
        [3].根因分析与排错支持：提供观测面板下钻能力，支持从系统级下钻至模块/文档级进行根因分析，并支持回放失败的检索与生成Case。
    11.遵循企业级数据主权隔离与合规审计基线
        [1].实施本地优先策略：核心数据默认采用本地嵌入模型与本地向量库处理；对于受限或敏感数据，强制实施字段级加密（加密-at-rest）与脱敏存储。
        [2].不可篡改合规日志：所有的检索请求、生成结果、系统决策记录与用户修正轨迹均写入不可篡改的审计日志（WORM存储），全面满足SOX/ISO27001审计要求，并支持按时间/角色维度的合规导出。
    12.支持系统级成本治理与生态互操作适配
        [1].实施三级成本熔断机制：实时预估并记录每次API调用的Token消耗与预估成本。支持配置任务级、会话级、系统级成本阈值，超限自动触发熔断拦截，并提供直观的成本-质量权衡视图。
        [2].建立契约与适配器接口：定义系统级数据契约（Data Contract）并在CI/CD流水线中加入契约测试；实现嵌入适配器（Embedding Adapter）接口，确保可无缝替换或回滚不同版本的本地或云端嵌入模型。




一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将多模态非结构化/半结构化资料（文本/表格/图像/公式/音视频转录）转化为模型可理解、可检索、可推理、可溯源的结构化知识资产。
    1.支持原生全模态文档接入与高保真深层解析能力
        [1].支持主流文档与媒体格式批量导入（pdf, txt, doc, docx, ppt, pptx, xls, xlsx, csv, jpeg, png, gif及zip/tar压缩包），支持音视频转录文本接入。
        [2].引入视觉文档理解与版面保留模式，精确记录元素坐标（x, y, width, height）。对xls/xlsx/csv及表格型PDF提取行列语义，强制输出包含表头与列类型的结构化JSON，防止财务数据失真。
        [3].对扫描件或图像PDF进行OCR解析，提取置信度并标注。支持数学公式的LaTeX表达或结构化表示，置信度<0.85时自动标注为“待人工复核”。
        [4].实现图文联合嵌入空间（CLIP-like投影），支持“以图搜文/以文搜图”的跨模态检索与“图表-正文”空间关联检索。
    2.支持企业级文档全生命周期管理与基础数据治理
        [1].支持完整的文档操作功能，包括文档上传、更新、删除、分类管理与版本快照（不可篡改 snapshot）；每次更改详细记录操作者、时间戳与差异摘要（diff）。
        [2].实施元数据标准化与入库治理：入库强制校验最小元字段集（creator, created_at, source, license, business_domain），关键字段缺失自动阻断入库。
        [3].建立细粒度数据血缘追踪：确保每个解析后的数据切片均可追溯至导入批次与原始文件版本，支持版本影响分析与回滚。
    3.构建混合检索架构与知识图谱增强检索（GraphRAG）
        [1].支持本地稠密向量（默认bge-m3）与BM25稀疏检索并行初检，建立“语义+关键词”双路召回基座。
        [2].入库时抽取实体-关系-事件三元组，生成可检索的知识图谱，支持图谱驱动的多跳推理检索，以解决跨文档因果链路查询。
        [3].采用智能语义分块机制，由文档语义边界而非固定字数进行切片，平均片段长度目标≈300 tokens（允许配置）。
        [4].支持三路融合策略（Dense + Sparse + Graph/metadata signals）并支持可配置权重的RRF融合排序，集成重排序器（如ColBERT-v2）对Top-K候选切片进行精排。
    4.实施分层检索策略与自适应查询路由
        [1].构建四级分层检索架构（L1跨文档摘要→L2文档摘要→L3文档切片→L4实体级片段），并实现“自顶向下”与“自底向上”的Parent-Child双向索引关系。
        [2].构建查询理解与路由引擎，根据查询复杂度与意图自动路由至对应层级或图谱社区，优化检索算力分配。
    5.支持契约化结构化摘要生成与多视角提炼
        [1].调用大模型API生成领域模板化结构化摘要（如财务、市场、技术视角），输出强制遵守预定义的JSON Schema契约。
        [2].部署摘要质量评估器，综合信息熵与关键实体覆盖率进行评分。评分<0.7自动触发二次生成或转入人工审核队列。
    6.采用Agentic RAG与自适应纠偏补救机制
        [1].采用LLM-as-a-Judge或检索判定器对检索结果进行实时多维评估，实施动态阈值判定。
        [2].若检索相关性低于阈值（默认0.6）或被判定为“事实冲突”，系统阻断直接生成以防幻觉，标注为“数据不足”。
        [3].触发自动补救机制：按策略自动扩展检索范围（时间/来源/模态）、调用白名单外部数据源，或生成结构化“数据缺口报告”并提示用户补充。
    7.强制高保真引文溯源与可解释性多维证据包
        [1].检索与生成过程保留引文“三元组”特征（文档ID、切片ID、字符范围/文本快照），增强引文元数据（置信度、时效性评分、来源权威性）。
        [2].支持高保真视觉级（Bounding Box）溯源，前端支持交互式展开溯源树，可直接跳转并高亮原始文档坐标点（PDF或图像中的具体出处）。
        [3].实施时效性管理，当引用数据陈旧（如超12个月）时，强制在生成结果中提示或自动降权。
    8.构建时序战略档案库与受控知识演进闭环（长期记忆）
        [1].建立时序战略档案库，以结构化方式永久存储历年SP/BP的关键假设变量、决策依据与实际执行偏差。
        [2].实施事实有效期标签管理（valid_from/valid_until），构建随时间轴演进的动态知识网络。
        [3].规范修正受控固化机制：仅当用户修正满足条件（如置信度>0.85且多用户确认或经审阅签名）时，方可提取固化为Prompt Optimizer的少样本（Few-Shot）提示词，并保留固化前后的审计差异记录。
    9.实施全链路多维数据质量控制与门禁流水线
        [1].数据预处理流水线涵盖格式验证、内容完整性检查、语义重复检测（基于bge-m3相似度）与SIMHash字面去重。
        [2].建立复合数据质量基准（如设定正文长度>100字符），计算综合可信度评分（DQI = 0.4*完整性 + 0.3*唯一性 + 0.3*时效性）。
        [3].入库触发CI/CD质量门禁，当DQI < 0.6时自动阻断自动化流程，进入人工审核队列。
    10.建立RAGOps全链路可观测性与双算法性能漂移检测
        [1].建立端到端监控体系，涵盖关键指标（如precision@k, recall@k, MRR, Faithfulness, 检索延迟P95、生成延迟P95、Token消耗、幻觉率）。
        [2].提供端到端观测面板，支持下钻至模块级或文档级进行根因分析与失败Case回放。
        [3].部署双算法性能漂移检测：采用KS检验（检测分布偏移）与CUSUM算法（检测连续性能下降），支持按日/周/月频率定期评估告警。
    11.遵循企业级数据主权隔离与合规审计基线
        [1].对敏感或受限数据默认采用本地优先策略（本地索引/本地模型处理），外部网络调用需经审计与白名单批准。
        [2].实施细粒度访问控制（RBAC），支持按数据标签脱敏与字段级加密存储（加密-at-rest）。
        [3].所有检索请求、生成结果、系统决策记录与用户修正轨迹全量写入不可篡改审计日志（WORM存储），满足SOX/ISO27001类审计基线，并支持以审计友好格式导出。
    12.支持系统级成本熔断与开放生态治理
        [1].实施成本工程管理：在数据处理与摘要生成阶段精确记录预计/实际的API成本与Token消耗，提供成本-质量权衡视图。
        [2].建立三级成本熔断机制：支持任务级、会话级与系统级防超支拦截，确保并发与长上下文场景下成本可控。
        [3].定义数据契约（Data Contract）标准（涵盖文档/片段/摘要/向量/图谱），集成CI契约测试；实现嵌入适配器（Adapter）接口，确保可无缝替换或回滚本地Embedding模型版本。
