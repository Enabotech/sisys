# language: zh-CN
功能: RBAC 权限管理系统

  作为安全工程师
  我想要实现用户认证与 RBAC 权限管理
  以便系统支持细粒度访问控制，满足等保 2.0 三级合规要求

  背景:
    假如 系统已配置 AuthConfig
    并且 PostgreSQL 用户表已创建（Story 1.5）
   并且 数据库连接正常

  # =========================================================================
  # AC-1: 用户认证 (Authentication)
  # =========================================================================

  场景: 用户登录成功
    假如 数据库中存在用户 testuser（密码为 Test123!）
    当 用户提交登录请求（用户名: testuser，密码: Test123!）
    那么 系统验证用户凭证成功
    并且 返回 JWT 访问令牌
    并且 返回令牌类型为 "bearer"
    并且 返回过期时间（24小时）
    并且 返回用户信息（包含 ID、用户名、角色列表）

  场景: 用户登录失败 - 密码错误
    假如 数据库中存在用户 testuser（密码为 Test123!）
    当 用户提交登录请求（用户名: testuser，密码: WrongPassword!）
    那么 系统返回 401 Unauthorized
    并且 返回错误消息 "Invalid credentials"

  场景: 用户登录失败 - 用户不存在
    假如 数据库中不存在用户 nonexistentuser
    当 用户提交登录请求（用户名: nonexistentuser，密码: AnyPassword!）
    那么 系统返回 401 Unauthorized
    并且 返回错误消息 "Invalid credentials"

  场景: 账户锁定 - 连续5次失败
    假如 数据库中存在用户 testuser（密码为 Test123!）
    并且 用户已连续5次输入错误密码
    当 用户提交登录请求（用户名: testuser，密码: WrongPassword!）
    那么 系统返回 423 Locked
    并且 返回错误消息 "Account locked due to multiple failed attempts"
    并且 账户被锁定 30 分钟

  场景: JWT 令牌验证成功
    假如 用户已成功登录并获得访问令牌
    当 用户使用该令牌访问受保护资源
    那么 系统验证令牌成功
    并且 返回请求的资源

  场景: JWT 令牌已过期
    假如 用户持有过期令牌
    当 用户使用该令牌访问受保护资源
    那么 系统返回 401 Unauthorized
    并且 返回错误消息 "Token expired"

  场景: JWT 令牌刷新
    假如 用户持有有效的刷新令牌
    当 用户使用刷新令牌请求新访问令牌
    那么 系统返回新的访问令牌
    并且 返回新的刷新令牌

  # =========================================================================
  # AC-2: 角色管理 (Role Management)
  # =========================================================================

  场景: 管理员创建新角色
    假如 管理员用户已登录（角色: admin）
    当 管理员创建新角色（名称: analyst，描述: 分析师角色）
    那么 角色创建成功
    并且 返回角色信息（包含 ID、名称、描述）

  场景: 管理员获取角色列表
    假如 管理员用户已登录（角色: admin）
    当 管理员请求获取所有角色
    那么 返回角色列表
    并且 包含预定义角色: admin, analyst, viewer

  场景: 管理员修改角色
    假如 管理员用户已登录（角色: admin）
    并且 系统存在角色 analyst
    当 管理员修改角色 analyst（添加权限: tool:execute）
    那么 角色更新成功
    并且 新权限已添加到角色

  场景: 管理员删除角色（软删除）
    假如 管理员用户已登录（角色: admin）
    并且 系统存在角色 custom_role
    当 管理员删除角色 custom_role
    那么 角色标记为已删除（is_active=False）
    并且 用户仍保留该角色引用（软删除）

  场景: 非管理员用户无法创建角色
    假如 普通用户已登录（角色: viewer）
    当 普通用户尝试创建新角色
    那么 系统返回 403 Forbidden
    并且 返回错误消息 "Insufficient permissions"

  场景: 角色分配权限
    假如 管理员用户已登录（角色: admin）
    并且 系统存在角色 analyst
    当 管理员为角色 analyst 分配权限 document:write
    那么 权限分配成功
    并且 角色 analyst 现在拥有权限 document:write

  场景: 角色撤销权限
    假如 管理员用户已登录（角色: admin）
    并且 系统存在角色 analyst（拥有权限 document:write）
    当 管理员从角色 analyst 撤销权限 document:write
    那么 权限撤销成功
    并且 角色 analyst 不再拥有权限 document:write

  # =========================================================================
  # AC-3: 权限控制 (Permission Control)
  # =========================================================================

  场景: 用户访问受保护资源 - 有权限
    假如 用户已登录（角色: analyst）
    并且 用户拥有权限 document:read
    当 用户请求读取文档
    那么 系统允许访问
    并且 返回请求的文档

  场景: 用户访问受保护资源 - 无权限
    假如 用户已登录（角色: viewer）
    并且 用户只拥有权限 document:read
    当 用户请求删除文档
    那么 系统拒绝访问
    并且 返回 403 Forbidden
    并且 返回错误消息 "Insufficient permissions"

  场景: 管理员访问所有资源
    假如 用户已登录（角色: admin）
    当 用户请求任何资源操作
    那么 系统允许访问
    并且 返回请求的资源

  场景: 权限验证中间件正确拦截
    假如 用户已登录（角色: viewer）
    当 用户尝试访问需要 admin 权限的端点
    那么 系统拒绝访问
    并且 返回 403 Forbidden

  场景: 权限层级 - 通配符权限
    假如 用户已登录（角色: admin）
    当 admin 用户请求任何资源操作
    那么 系统允许访问（因为 admin 拥有 *:* 权限）

  场景: 权限层级 - 资源级通配符
    假如 用户已登录（角色: document_admin）
    并且 用户拥有权限 document:*
    当 用户请求对文档的任何操作
    那么 系统允许访问

  # =========================================================================
  # AC-4: 越权访问防护 (Privilege Escalation Prevention)
  # =========================================================================

  场景: 水平越权防护 - 用户间数据隔离
    假如 系统存在用户 Alice 和 Bob
   并且 Alice 拥有角色 viewer
    并且 Bob 拥有角色 viewer
    当 Alice 尝试访问 Bob 的私有资源
    那么 系统拒绝访问
    并且 返回 403 Forbidden

  场景: 垂直越权防护 - 低权限用户无法访问高权限资源
    假如 用户已登录（角色: viewer）
    当 用户尝试访问需要 admin 权限的管理端点
    那么 系统拒绝访问
    并且 返回 403 Forbidden
    并且 越权访问尝试被记录到审计日志

  场景: 垂直越权防护 - 权限提升尝试被拒绝
    假如 用户已登录（角色: viewer）
    当 用户尝试通过修改请求获取 admin 角色
    那么 系统拒绝请求
    并且 返回 403 Forbidden

  场景: SQL 注入防护
    假如 用户已登录（角色: viewer）
    当 用户提交恶意 SQL 输入尝试注入攻击
    那么 系统正确转义输入
    并且 返回正常的错误消息（而非 SQL 错误）

  场景: 越权访问尝试被记录
    假如 用户已登录（角色: viewer）
    并且 用户尝试访问管理员资源
    当 请求被系统拒绝
    那么 审计日志记录: 用户 ID、时间、资源、操作、结果（拒绝）

  # =========================================================================
  # AC-5: 等保 2.0 合规 (Deng Bao 2.0 Compliance)
  # =========================================================================

  场景: 密码复杂度要求
    当 用户尝试设置密码（长度 7 位，仅小写字母）
    那么 系统拒绝密码
    并且 返回错误消息 "Password does not meet complexity requirements"

  场景: 密码复杂度要求 - 有效密码
    当 用户尝试设置密码（长度 8 位，包含大小写字母、数字、特殊字符）
    那么 系统接受密码
    并且 密码被正确哈希存储

  场景: 认证失败锁定
    假如 用户已连续 4 次输入错误密码
    当 用户第 5 次输入错误密码
    那么 账户被锁定
    并且 锁定持续 30 分钟

  场景: 会话超时
    假如 用户已登录
    并且 用户 30 分钟无操作
    当 用户尝试继续使用会话
    那么 系统返回 401 Unauthorized
    并且 返回错误消息 "Session expired due to inactivity"

  场景: 最小权限原则 - 默认拒绝
    假如 用户已登录（角色: new_role，不包含任何权限）
    当 用户尝试访问任何资源
    那么 系统默认拒绝访问
    并且 返回 403 Forbidden

  场景: 敏感操作二次验证
    假如 用户已登录（角色: admin）
    当 用户尝试执行删除操作（高风险操作）
    那么 系统要求二次验证
    并且 用户通过验证后才执行操作

  场景: 安全审计 - 登录事件记录
    假如 用户成功登录系统
    当 登录事件完成
    那么 审计日志记录: 用户 ID、登录时间、IP地址、结果（成功）

  场景: 安全审计 - 权限变更事件记录
    假如 管理员修改了用户角色
    当 角色变更完成
    那么 审计日志记录: 管理员 ID、目标用户 ID、变更类型、变更内容、时间

  场景: 安全审计 - 越权访问事件记录
    假如 用户尝试越权访问
    当 访问被拒绝
    那么 审计日志记录: 用户 ID、时间、尝试访问的资源、尝试的操作、结果（拒绝）
