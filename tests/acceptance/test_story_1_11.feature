# language: zh-CN
功能: 数据主权隔离

  作为合规工程师
  我想要实现数据主权隔离（敏感数据本地优先，外部网络调用需审计与白名单批准）
  以便满足数据安全法和 PIPL 要求

  背景:
    假如 系统已配置 DataSovereigntyConfig
    并且 PostgreSQL 审计日志表已创建（Story 1.10）
    并且 数据库连接正常

  # =========================================================================
  # AC-1: 敏感数据识别与标记 (Sensitive Data Detection)
  # =========================================================================

  场景: 识别身份证号码
    假如 系统处理包含身份证号的数据
    当 数据进入系统
    那么 系统识别出身份证号
    并且 数据标记为 PII 类型（敏感类型）
    并且 识别置信度 ≥ 95%

  场景: 识别中国手机号码
    假如 系统处理包含手机号的数据
    当 数据被访问时
    那么 系统识别出手机号（1开头11位）
    并且 数据标记为 PII 类型

  场景: 识别商业秘密关键词
    假如 系统处理包含"机密"、"配方"等关键词的数据
    当 数据被访问时
    那么 系统识别出商业秘密关键词
    并且 数据标记为 TRADE_SECRET 类型

  场景: 识别金融账号
    假如 系统处理包含银行卡号的数据
    当 数据进入系统
    那么 系统识别出银行账号
    并且 数据标记为 FINANCIAL 类型

  场景: 识别生物识别数据
    假如 系统处理包含指纹、人脸特征的数据
    当 数据被访问时
    那么 系统识别出生物识别信息
    并且 数据标记为 BIOMETRIC 类型（PIPL 特殊保护）

  场景: 自定义敏感类型支持
    假如 系统配置了自定义敏感类型
    当 数据匹配自定义规则时
    那么 系统识别为对应自定义类型
    并且 应用相应保护策略

  场景: 敏感标签传播至下游
    假如 原始数据已被标记为敏感
    当 数据被复制或传输至下游系统
    那么 敏感标签随数据传播
    并且 下游系统识别相同敏感类型

  场景: 识别准确率验证
    假如 测试数据集包含100个样本
    当 执行敏感数据识别
    那么 识别准确率 ≥ 95%

  # =========================================================================
  # AC-2: 数据境内存储策略 (Data Residency)
  # =========================================================================

  场景: PII 数据默认境内存储
    假如 敏感数据已被标记为 PII 类型
    当 系统选择存储层
    那么 优先选择中国境内存储层
    并且 数据物理存储在中国大陆

  场景: 境外存储触发审批流程
    假如 敏感数据需要存储在境外
    当 存储请求被发起
    那么 系统触发跨境审批流程
    并且 审批通过前阻断存储操作

  场景: 境内优先路由决策
    假如 系统有多层存储可用
    当 需要选择存储位置时
    那么 系统优先选择境内存储层
    并且 记录路由决策原因

  场景: 数据主权配置验证
    假如 配置指定数据只能存储在中国大陆
    当 尝试将数据存储到境外
    那么 系统拒绝存储请求
    并且 记录违规事件

  场景: 存储层物理隔离
    假如 系统配置境内和境外存储层
    当 敏感数据存储时
    那么 境内数据与境外数据物理隔离

  场景: 跨境检测和告警
    假如 发生跨境数据传输
    当 传输完成时
    那么 系统生成跨境告警
    并且 告警记录包含源、目的、敏感数据类型

  场景: 数据境内存储 100% 验证
    假如 合规性测试执行
    当 验证数据境内存储要求
    那么 数据境内存储率 = 100%

  # =========================================================================
  # AC-3: 外部调用白名单机制 (External Call Whitelist)
  # =========================================================================

  场景: 白名单验证 - 授权端点
    假如 外部 API 端点已在白名单中（active 状态）
    当 系统调用该端点
    那么 白名单验证通过
    并且 允许调用执行

  场景: 白名单验证 - 未授权端点
    假如 外部 API 端点不在白名单中
    当 系统尝试调用该端点
    那么 白名单验证失败
    并且 调用被阻断
    并且 阻断事件记录至审计日志

  场景: 白名单规则动态添加
    假如 管理员添加新白名单规则
    当 规则状态为 active
    那么 新规则立即生效

  场景: 白名单规则过期自动失效
    假如 白名单规则已过期
    当 系统验证该规则时
    那么 规则状态变为 expired
    并且 验证失败

  场景: 白名单规则撤销
    假如 管理员撤销白名单规则
    当 后续调用使用该规则
    那么 调用被阻断
    并且 记录撤销原因

  场景: 白名单管理 - CLI 添加规则
    假如 管理员执行 sisys system whitelist add --endpoint https://api.example.com --provider ExampleAPI --purpose "数据同步"
    当 命令执行成功
    那么 白名单规则创建成功
    并且 规则状态为 pending

  场景: 白名单管理 - CLI 列出规则
    假如 系统存在多条白名单规则
    当 管理员执行 sisys system whitelist list --status active
    那么 返回所有 active 状态的规则
    并且 包含规则详情（endpoint, provider, risk_level）

  场景: 白名单管理 - API 端点
    假如 管理员调用 POST /api/v1/admin/whitelist
    当 请求包含规则信息
    那么 白名单规则创建成功
    并且 返回规则 ID

  场景: 白名单审计日志
    假如 系统调用外部 API
    当 调用完成时
    那么 审计日志记录：调用时间、端点、结果（成功/失败）

  场景: 白名单命中率 100% 验证
    假如 合规性测试执行
    当 验证白名单验证覆盖率
    那么 所有外部调用均经过白名单验证

  # =========================================================================
  # AC-4: 跨境传输审批流程 (Cross-Border Transfer Approval)
  # =========================================================================

  场景: 跨境传输审批请求创建
    假如 数据需要跨境传输
    当 传输请求被发起
    那么 审批请求创建成功
    并且 状态为 pending

  场景: 跨境传输审批 - 合规官批准
    假如 存在待审批的跨境传输请求
    当 合规官执行 approve 操作
    那么 请求状态变为 approved
    并且 记录批准人
    并且 允许跨境传输

  场景: 跨境传输审批 - 合规官拒绝
    假如 存在待审批的跨境传输请求
    当 合规官执行 reject 操作并提供原因
    那么 请求状态变为 rejected
    并且 记录拒绝原因
    并且 跨境传输被阻断

  场景: 审批 SLA 管理 - 超时告警
    假如 审批请求已超过 SLA 时限（48小时）
    当 系统检查待审批请求
    那么 生成 SLA 超时告警
    并且 告警通知相关合规官

  场景: 审批 SLA 管理 - 自动升级
    假如 审批请求超时 24 小时
    当 系统检测到超时
    那么 请求自动升级给上级合规官

  场景: 跨境传输阻断机制
    假如 跨境传输请求未获批准
    当 尝试执行跨境传输
    那么 传输被阻断
    并且 阻断日志记录

  场景: 审批历史记录
    假如 跨境传输请求已完成审批
    当 查询审批历史
    那么 返回完整审批记录（请求人、审批人、时间、结果）

  场景: CLI 审批命令 - 列出待审批
    假如 存在待审批的跨境传输请求
    当 管理员执行 sisys system approval list --status pending
    那么 返回所有 pending 状态的请求
    并且 包含详情（data_id, destination, purpose, requester）

  场景: CLI 审批命令 - 批准
    假如 存在待审批请求（request_id: xxx）
    当 管理员执行 sisys system approval approve --request-id xxx
    那么 请求状态变为 approved

  场景: CLI 审批命令 - 拒绝
    假如 存在待审批请求（request_id: xxx）
    当 管理员执行 sisys system approval reject --request-id xxx --reason "不合规"
    那么 请求状态变为 rejected
    并且 记录拒绝原因

  场景: 跨境传输审批率 100% 验证
    假如 合规性测试执行
    当 验证跨境传输审批率
    那么 所有跨境传输均经过审批

  # =========================================================================
  # AC-5: PIPL 合规 (Personal Information Protection Law)
  # =========================================================================

  场景: 个人信息处理记录
    假如 系统访问个人信息
    当 访问发生时
    那么 记录处理目的（purpose）
    并且 记录处理方式（legal_basis）
    并且 记录数据主体同意状态

  场景: PIPL 合法依据记录
    假如 系统处理个人信息
    当 处理发生时
    那么 记录合法依据（consent/contract/legal_obligation）

  场景: 数据主体同意验证
    假如 PIPL 配置要求同意
    当 个人信息被处理
    那么 验证数据主体已提供同意
    并且 拒绝未同意的处理请求

  场景: 个人信息访问权
    假如 数据主体请求查看其个人信息
    当 请求被处理
    那么 返回该主体的所有个人信息记录

  场景: 个人信息删除权
    假如 数据主体请求删除其个人信息
    当 请求被处理
    那么 标记个人信息为已删除
    并且 停止所有相关处理活动

  场景: 个人信息更正权
    假如 数据主体请求更正其个人信息
    当 请求被处理
    那么 更新相关信息
    并且 记录更正历史

  场景: 生物识别信息特殊保护
    假如 系统处理生物识别信息
    当 处理发生时
    那么 应用最严格的保护措施
    并且 禁止跨境传输

  场景: 未成年人信息特殊保护
    假如 系统处理14岁以下未成年人信息
    当 处理发生时
    那么 需要监护人额外同意
    并且 应用增强保护措施

  场景: PIPL 合规报告生成
    假如 合规审计需要报告
    当 报告生成请求
    那么 生成 PIPL 合规审计报告
    并且 包含所有个人信息处理记录

  场景: PIPL 合规审计通过
    假如 PIPL 合规测试执行
    当 所有测试项通过
    那么 系统满足 PIPL 合规要求

  # =========================================================================
  # AC-6: 合规性测试 (Compliance Testing)
  # =========================================================================

  场景: COMP-05 数据境内存储测试通过
    假如 执行 COMP-05 合规性测试
    当 测试完成
    那么 数据境内存储率 = 100%

  场景: 敏感数据识别准确率验证
    假如 执行敏感数据识别测试
    当 测试样本 ≥ 100
    那么 识别准确率 ≥ 95%

  场景: 白名单验证覆盖率 100%
    假如 执行白名单验证覆盖率测试
    当 测试外部调用样本
    那么 所有调用均经过白名单验证

  场景: 跨境传输审批率 100%
    假如 执行跨境传输审批率测试
    当 测试跨境传输请求
    那么 所有传输均经过审批流程
