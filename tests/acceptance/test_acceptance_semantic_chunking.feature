# language: zh-CN
功能: 语义分块
  作为 RAG 工程师
  我希望系统对文档进行语义分块（基于文档语义边界而非固定字数切片）
  以便检索结果更符合语义完整性

  背景:
    假如 语义分块器已就绪

  # =========================================================================
  # AC-1: 语义边界识别与切分
  # =========================================================================

  场景: 单段落短文档
    假如 一个包含单段落的短文档
    当 系统执行语义分块
    那么 生成 1 个分块
    并且 分块边界类型为 paragraph

  场景: 多段落文档
    假如 一个包含多个段落的文档
    当 系统执行语义分块
    那么 按段落边界切分
    并且 每个分块围绕 300 tokens 聚合

  场景: 章节标题边界
    假如 一个包含章节标题的文档
    当 系统执行语义分块
    那么 标题样式为 "h1"~"h6" 或 "Heading 1"~"Heading 9" 的元素触发新分块
    并且 分块边界类型为 section_header

  场景: 表格独立分块
    假如 一个包含表格的文档
    当 系统执行语义分块
    那么 每个表格成为一个独立分块
    并且 表格内容展平为 pipe-separated 结构化文本

  场景: 跨页边界
    假如 一个包含多页的文档
    当 系统执行语义分块
    那么 新页码必然创建新分块
    并且 分块边界类型为 page_break

  场景: 大段落超过 max_chunk_size_tokens
    假如 一个包含超长段落的文档
    当 系统执行语义分块
    那么 超过 8192 tokens 的段落按 token_limit 类型硬切分
    并且 分块边界类型为 token_limit

  场景: 空文档
    假如 一个空文档
    当 系统执行语义分块
    那么 返回空列表
    并且 不抛异常

  # =========================================================================
  # AC-2: Token 计数与分块大小控制
  # =========================================================================

  场景: 中英文混合 token 计数
    假如 包含中英文混合文本的文档
    当 系统估算 token 数
    那么 估算误差不超过 20%

  # =========================================================================
  # AC-3: 分块元数据完整性
  # =========================================================================

  场景: 分块元数据完整性
    假如 一个包含多元素的文档
    当 系统执行语义分块
    那么 每个分块包含 chunk_id、document_id、chunk_index、boundary_type、token_count、page_range、content_hash、metadata
    并且 content_hash 使用 SHA256 计算
    并且 分块可序列化为 JSON

  # =========================================================================
  # AC-4: 分块集成到文档流水线
  # =========================================================================

  场景: 分块结果持久化到文档元数据
    假如 文档解析完成并发布 DocumentProcessed 事件
    当 语义分块处理器接收事件
    那么 分块结果存入 document.metadata.chunks
    并且 发布 RAGIndexed 事件（含 chunk_count）

  # =========================================================================
  # AC-5: 分块文本格式化
  # =========================================================================

  场景: 空表格跳过
    假如 一个包含空表格的文档
    当 系统执行语义分块
    那么 空表格不产生分块

  场景: content_hash 一致性
    假如 相同内容文档
    当 系统执行语义分块
    那么 相同内容产生相同 content_hash
    并且 内容变更后哈希变化

  场景: Word Heading 样式归一化
    假如 一个包含 Word Heading 样式的文档
    当 系统执行语义分块
    那么 "Heading 1"~"Heading 9" 均识别为 SECTION_HEADER 边界
    并且 "Heading 1 Char" 等含 "heading" 子串的样式也识别为 SECTION_HEADER
