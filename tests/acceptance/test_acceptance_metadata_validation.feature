# language: zh-CN
功能: 文档元数据标准化校验

  作为数据治理工程师
  我希望系统校验入库文档的最小元字段集（creator/created_at/source/license/business_domain）
  以便确保文档元数据完整性和可追溯性

  背景:
    假如 元数据校验器已就绪

  # =========================================================================
  # AC-1: 最小元字段集校验
  # =========================================================================

  # 场景 1: 完整元数据上传
  场景: 完整元数据上传成功
    假如 一个包含完整元数据的文档准备入库
    并且 元数据包含 creator、created_at、source、license、business_domain 五个字段
    当 系统执行元数据校验
    那么 校验通过
    并且 返回缺失字段列表为空

  # 场景 2: 部分元数据 + 自动填充
  场景: 部分元数据自动填充后上传成功
    假如 一个文档仅提供 source、license、business_domain 三个字段
    当 系统执行元数据校验（含自动填充）
    那么 creator 自动填充为上传者
    并且 created_at 自动填充为当前 UTC 时间
    并且 校验通过

  # 场景 4: 空值阻断
  场景: 空字符串字段视为缺失
    假如 一个文档的 source 字段值为空字符串
    当 系统执行元数据校验
    那么 校验失败
    并且 缺失字段列表包含 source

  # 场景 5: 无 metadata 上传
  场景: 不传 metadata 时自动填充部分字段后阻断
    假如 一个文档不包含任何元数据
    当 系统执行元数据校验（含自动填充）
    那么 creator 自动填充为上传者
    并且 created_at 自动填充为当前 UTC 时间
    但是 source、license、business_domain 仍然缺失
    并且 校验失败
    并且 缺失字段列表包含 source、license、business_domain

  # =========================================================================
  # AC-2: 关键字段缺失自动阻断
  # =========================================================================

  # 场景 3: 元数据缺失阻断
  场景: 缺少 license 字段阻断上传
    假如 一个文档的元数据缺少 license 字段
    当 系统执行元数据校验
    那么 校验失败
    并且 抛出 MetadataValidationError 异常
    并且 异常编码为 EXCEPTION_217
    并且 异常上下文包含缺失字段列表
    并且 缺失字段列表包含 license
    并且 异常上下文包含 document_id 和 tenant_id

  # =========================================================================
  # AC-3: 上传流程集成
  # =========================================================================

  # 场景 6: 校验失败无 MinIO 残留（单文件上传）
  场景: 单文件上传校验失败时无 MinIO 残留
    假如 一个文档缺少 license 字段
    当 系统执行单文件上传（含元数据校验）
    那么 校验失败
    并且 MinIO 未存储该文档对象
    并且 PG 无该文档记录

  # 场景 7: 校验失败无 MinIO 残留（分片上传）
  场景: 分片上传校验失败时清理 MinIO 残留
    假如 一个分片上传的文档缺少 license 字段
    当 系统执行分片上传完成（含元数据校验）
    那么 校验失败
    并且 abort_multipart_upload 被调用清理 MinIO 对象

  # 场景 8: 校验失败无 PG 残留
  场景: 校验失败时 PG 无残留
    假如 一个文档缺少 source 字段
    当 系统执行上传（含元数据校验）
    那么 校验失败
    并且 PG 无该文档记录

  # 场景 9: 校验通过正常上传完整流程
  场景: 元数据校验通过后正常完成上传流程
    假如 一个包含完整元数据的文档
    当 系统执行上传流程（含元数据校验）
    那么 校验通过
    并且 文档存入 MinIO 存储
    并且 文档持久化到 PG
    并且 事件 DocumentUploaded 被发布

  # =========================================================================
  # AC-4: 元数据自动填充
  # =========================================================================

  # 场景 10: 批量上传 metadata_list 索引对齐
  场景: 批量上传 metadata_list 与文件索引对齐
    假如 3 个文件准备批量上传
    并且 每个文件分别传入不同的 metadata
    当 系统执行批量上传
    那么 每个文件的 metadata 与索引一一对应
    并且 索引 0 的 metadata 应用于文件 0
    并且 索引 1 的 metadata 应用于文件 1
    并且 索引 2 的 metadata 应用于文件 2

  # =========================================================================
  # Edge Cases
  # =========================================================================

  场景: created_at 非法格式拒绝
    假如 一个文档的 created_at 字段为 "2024/01/01"（非 ISO 8601 格式）
    当 系统执行元数据校验
    那么 校验失败
    并且 缺失字段列表包含 created_at

  场景: metadata 为 null 时自动填充后阻断
    假如 一个文档的 metadata 参数为 null
    当 系统执行元数据校验（含自动填充）
    那么 creator 自动填充为上传者
    并且 created_at 自动填充为当前 UTC 时间
    但是 source、license、business_domain 仍然缺失
    并且 校验失败

  场景: 跨租户数据隔离
    假如 租户 A 和租户 B 各自上传文档
    当 系统执行元数据校验
    那么 租户 A 的校验不影响租户 B
    并且 每个租户的 metadata 独立存储