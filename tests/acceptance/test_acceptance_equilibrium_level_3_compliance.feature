# language: zh-CN
功能: 等保2.0三级基础安全合规

  作为安全合规工程师
  我需要系统具备等保2.0三级要求的各项安全能力
  以确保信息系统满足国家等级保护三级标准

  背景:
    假如 系统已初始化完成
    并且 所有安全服务端口已注册

  # ==================== 入侵防范 ====================
  场景: 检测SQL注入攻击
    假如 入侵检测服务可用
    当 系统收到包含SQL注入特征的请求
    那么 入侵检测服务识别为SQL注入攻击
    并且 攻击被记录到审计日志

  场景: 检测XSS攻击
    假如 入侵检测服务可用
    当 系统收到包含XSS特征的请求
    那么 入侵检测服务识别为XSS攻击
    并且 攻击严重级别为HIGH

  场景: 检测暴力破解攻击
    假如 入侵检测服务可用
    当 同一IP地址在5分钟内失败登录超过10次
    那么 入侵检测服务识别为暴力破解攻击
    并且 攻击严重级别为MEDIUM

  场景: 封禁恶意IP地址
    假如 入侵检测服务可用
    当 管理员封禁恶意IP地址
    那么 该IP地址被封禁
    并且 封禁时长为24小时

  # ==================== 数据完整性 ====================
  场景: 计算数据校验和
    假如 数据完整性服务可用
    当 系统计算数据的SHA256校验和
    那么 返回64位十六进制哈希值

  场景: 验证数据完整性
    假如 数据完整性服务可用
    并且 数据已存储校验和
    当 系统验证数据完整性
    那么 校验和匹配验证通过

  场景: 检测数据篡改
    假如 数据完整性服务可用
    并且 数据已被篡改
    当 系统验证数据完整性
    那么 校验和不匹配验证失败
    并且 发布DataIntegrityViolationEvent事件

  # ==================== 备份恢复 ====================
  场景: 创建PostgreSQL备份
    假如 备份恢复服务可用
    当 管理员触发PostgreSQL备份
    那么 PostgreSQL数据库备份成功
    并且 备份结果包含校验和

  场景: 创建完整备份
    假如 备份恢复服务可用
    当 管理员触发完整备份
    那么 PostgreSQL数据库备份成功
    并且 MinIO对象存储备份成功
    并且 Redis缓存备份成功

  场景: 恢复备份
    假如 备份恢复服务可用
    并且 已存在有效备份
    当 管理员触发备份恢复
    那么 备份恢复成功
    并且 恢复结果包含已恢复项数

  场景: 验证备份完整性
    假如 备份恢复服务可用
    并且 已存在备份
    当 系统验证备份完整性
    那么 备份完整性验证通过

  # ==================== 入侵统计 ====================
  场景: 获取入侵检测统计
    假如 入侵检测服务可用
    当 系统请求过去24小时的入侵统计
    那么 返回攻击总数
    并且 返回按攻击类型统计
    并且 返回按严重级别统计
