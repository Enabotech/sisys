# language: zh-CN
功能: 战略工具注册
  作为工具工程师
  我希望系统注册 23 种战略工具
  以便 Agent 可以调用这些工具执行战略分析

  背景:
    假如 工具注册服务已初始化
    并且 已注册 23 种战略工具

  # =============================================================================
  # AC-1: 23 种战略工具全部注册
  # =============================================================================
  场景: AC-1 - 23 种战略工具全部注册成功
    当 执行工具注册引导流程
    那么 23 种战略工具全部注册成功
    并且 每种工具有唯一 tool_id
    并且 每种工具 name 非空且唯一
    并且 每种工具 category 属于 ToolCategory 枚举值
    并且 每种工具 input_schema 和 output_schema 为有效 JSON Schema 字典
    并且 所有工具 status 默认为 ACTIVE
    并且 所有工具 version 默认为 1.0.0

  # =============================================================================
  # AC-2: 按分类查询工具
  # =============================================================================
  场景: AC-2 - 按分类查询工具返回正确数量
    当 按分类 ENVIRONMENT_ANALYSIS 查询
    那么 返回 3 种工具
    当 按分类 COMPETITIVE_ANALYSIS 查询
    那么 返回 3 种工具
    当 按分类 STRATEGIC_SELECTION 查询
    那么 返回 6 种工具
    当 按分类 BUSINESS_MODEL 查询
    那么 返回 3 种工具
    当 按分类 EXECUTION_MANAGEMENT 查询
    那么 返回 8 种工具

  # =============================================================================
  # AC-3: 按 ID/名称查询工具
  # =============================================================================
  场景: AC-3a - 按 ID 查询工具
    当 按 tool_id 查询 PESTEL 分析
    那么 返回完整工具元数据
    并且 工具名称为 PESTEL 分析
    并且 工具分类为 ENVIRONMENT_ANALYSIS

  场景: AC-3b - 按名称查询工具
    当 按 name 查询 波特五力
    那么 返回完整工具元数据
    并且 工具名称为 波特五力

  场景: AC-3c - 查询不存在的工具抛出 ToolNotFoundError
    当 按 tool_id 查询不存在的工具
    那么 抛出 ToolNotFoundError

  场景: AC-3d - 查询不存在的工具名称抛出 ToolNotFoundError
    当 按 name 查询不存在的工具
    那么 抛出 ToolNotFoundError

  场景: AC-3e - 空参数查询工具抛出 EntityValidationError
    当 不传任何参数查询工具
    那么 抛出 EntityValidationError
    并且 错误码为 EXCEPTION_242

  # =============================================================================
  # AC-4: 端口注册与依赖注入
  # =============================================================================
  场景: AC-4 - 端口注册验证
    那么 tool_repository 端口已注册
    并且 tool_registry_service 端口已注册
    并且 PortRegistry.get("tool_repository") 返回非空 PortSpec
    并且 PortRegistry.get("tool_registry_service") 返回非空 PortSpec
