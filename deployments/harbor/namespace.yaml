---
# Harbor 命名空间配置
# Story 0.6: Harbor 镜像仓库部署

apiVersion: v1
kind: Namespace
metadata:
  name: harbor
  labels:
    name: harbor
    # 网络策略标签
    network-policy: enabled
  annotations:
    description: "Harbor 镜像仓库命名空间"
    story: "0.6-harbor-image-registry"
