企业战略规划管理智库系统需求特性列表
一、数据处理：需要处理历史战略规划与业务计划、企业年报，市场报告、咨询报告、财务报表与企业内部业务数据，将多模态非结构化或半结构化数据（文本/表格/图像/公式/音视频转录）转化为模型可理解、可检索、可推理、可溯源的结构化知识资产。
    1.支持原生全模态文档接入与高保真深层解析能力
        [1].支持主流文档与媒体格式批量导入（pdf, txt, doc, docx, ppt, pptx, xls, xlsx, csv, jpeg, png, gif 及 zip/tar 压缩包），支持音视频转录文本接入。
        [2].引入视觉文档理解与版面保留模式，精确记录元素坐标（x, y, width, height）。对 xls/xlsx/csv 及表格型 PDF 提取行列语义，强制输出包含表头与列类型的结构化 JSON，防止财务数据失真。
        [3].对扫描件或图像 PDF 进行 OCR 解析，提取置信度并标注。支持数学公式的 LaTeX 表达或结构化表示，置信度<0.85 时自动标注为"待人工复核"。
        [4].实现图文联合嵌入空间（CLIP-like 投影），支持"以图搜文/以文搜图"的跨模态检索与"图表 - 正文"空间关联检索。
    2.支持企业级文档全生命周期管理与基础数据治理
        [1].支持完整的文档操作功能，包括文档上传、更新、删除、分类管理与版本快照（不可篡改 snapshot）；每次更改详细记录操作者、时间戳与差异摘要（diff）。
        [2].实施元数据标准化与入库治理：入库强制校验最小元字段集（creator, created_at, source, license, business_domain），关键字段缺失自动阻断入库。
        [3].建立细粒度数据血缘追踪：确保每个解析后的数据切片均可追溯至导入批次与原始文件版本，支持版本影响分析与回滚。
    3.构建混合检索架构与知识图谱增强检索（GraphRAG）
        [1].支持本地稠密向量（默认 bge-m3）与 BM25 稀疏检索并行初检，建立"语义 + 关键词"双路召回基座。
        [2].采用 LLM+ 规则混合实体抽取策略：规则基抽取（领域词典 AC 自动机匹配、正则模式、依存句法分析）保障高准确率（目标≥90%），LLM 语义抽取（Few-Shot+CoT+Schema 约束）保障高召回率，冲突仲裁器基于置信度融合（规则权重 0.6、LLM 权重 0.4）输出最终三元组。
        [3].建立战略领域词典库（≥5000 词条，涵盖公司/产品/指标/模型/地区），支持词典热更新与版本管理，新词入库自动触发增量图谱构建。
        [4].实施实体对齐与消歧：基于编辑距离 + 语义相似度（bge-m3）双路匹配，阈值≥0.85 判定为同一实体，支持同义词归并（如"华为"/"Huawei"统一为"华为"）。
        [5].采用智能语义分块机制，由文档语义边界而非固定字数进行切片，平均片段长度目标≈300 tokens（允许配置）。
        [6].支持三路融合策略（Dense + Sparse + Graph/metadata signals）并支持可配置权重的 RRF 融合排序，集成重排序器（如 ColBERT-v2）对 Top-K 候选切片进行精排。
    4.实施分层检索策略与自适应查询路由
        [1].构建四级分层检索架构（L1 跨文档摘要→L2 文档摘要→L3 文档切片→L4 实体级片段），并实现"自顶向下"与"自底向上"的 Parent-Child 双向索引关系。
        [2].构建查询理解与路由引擎，根据查询复杂度与意图自动路由至对应层级或图谱社区，优化检索算力分配。
    5.支持契约化结构化摘要生成与多视角提炼
        [1].调用大模型 API 生成领域模板化结构化摘要（如财务、市场、技术视角），输出强制遵守预定义的 JSON Schema 契约。
        [2].部署摘要质量评估器，综合信息熵与关键实体覆盖率进行评分。评分<0.7 自动触发二次生成或转入人工审核队列。
    6.采用 Agentic RAG 与自适应纠偏补救机制
        [1].采用 LLM-as-a-Judge 或检索判定器对检索结果进行实时多维评估，实施动态阈值判定。
        [2].若检索相关性低于阈值（默认 0.6）或被判定为"事实冲突"，系统阻断直接生成以防幻觉，标注为"数据不足"。
        [3].触发自动补救机制：按策略自动扩展检索范围（时间/来源/模态）、调用白名单外部数据源，或生成结构化"数据缺口报告"并提示用户补充。
    7.强制高保真引文溯源与可解释性多维证据包
        [1].检索与生成过程保留引文"三元组"特征（文档 ID、切片 ID、字符范围/文本快照），增强引文元数据（置信度、时效性评分、来源权威性）。
        [2].支持高保真视觉级（Bounding Box）溯源，前端支持交互式展开溯源树，可直接跳转并高亮原始文档坐标点（PDF 或图像中的具体出处）。
        [3].实施时效性管理，当引用数据陈旧（如超 12 个月）时，强制在生成结果中提示或自动降权。
    8.构建时序战略档案库与受控知识演进闭环（长期记忆）
        [1].建立时序战略档案库，以结构化方式永久存储历年 SP/BP 的关键假设变量、决策依据与实际执行偏差。
        [2].实施事实有效期标签管理（valid_from/valid_until），构建随时间轴演进的动态知识网络。
        [3].建立三级修正分级体系：L1 操作级（参数/权重微调，影响≤1 任务，置信度≥0.85）自动固化；L2 专业级（约束变更，影响 2-3 任务，置信度≥0.75）专家确认（1 人，24 小时 SLA）；L3 战略级（假设/逻辑/战略修改，影响>3 任务）委员会审批（≥3 人，7 天 SLA）。
        [4].部署修正分级判定器：基于修正类型、置信度变化、影响范围、可逆性、领域关键度五维特征自动判定级别，判定准确率目标≥85%，路由决策写入审计日志。
        [5].自动固化流水线（L1 级）：生成 Few-Shot 样本→Strat-Bench 测试（通过率≥90%）→Git LFS 版本注册→WORM 存储，支持 24 小时内自动回滚。
        [6].统一演进日志 Schema（EvolutionLog）：记录修正分级判定证据、审批人信息、固化版本、WORM 存储引用，满足 SOX/ISO27001 审计要求，支持按时间/角色/任务多维检索。
    9.实施全链路多维数据质量控制与门禁流水线
        [1].数据预处理流水线涵盖格式验证、内容完整性检查、语义重复检测（基于 bge-m3 相似度）与 SIMHash 字面去重。
        [2].建立复合数据质量基准（如设定正文长度>100 字符），计算综合可信度评分（DQI = 0.4*完整性 + 0.3*唯一性 + 0.3*时效性）。
        [3].入库触发 CI/CD 质量门禁，当 DQI < 0.6 时自动阻断自动化流程，进入人工审核队列。
    10.建立 RAGOps 全链路可观测性与双算法性能漂移检测
        [1].建立端到端监控体系，涵盖关键指标（如 precision@k, recall@k, MRR, Faithfulness, 检索延迟 P95、生成延迟 P95、Token 消耗、幻觉率）。
        [2].提供端到端观测面板，支持下钻至模块级或文档级进行根因分析与失败 Case 回放。
        [3].性能漂移检测：CUSUM 算法（检测连续性能下降），支持按日/周/月频率定期评估告警。
    11.遵循企业级数据主权隔离与合规审计基线
        [1].对敏感或受限数据默认采用本地优先策略（本地索引/本地模型处理），外部网络调用需经审计与白名单批准。
        [2].实施细粒度访问控制（RBAC），支持按数据标签脱敏与字段级加密存储（加密-at-rest）。
        [3].所有检索请求、生成结果、系统决策记录与用户修正轨迹全量写入不可篡改审计日志（WORM 存储），满足 SOX/ISO27001 类审计基线，并支持以审计友好格式导出。
    12.支持系统级成本熔断与开放生态治理
        [1].实施成本工程管理：在数据处理与摘要生成阶段精确记录预计/实际的 API 成本与 Token 消耗，提供成本 - 质量权衡视图。
        [2].建立三级成本熔断机制：支持任务级、会话级与系统级防超支拦截，确保并发与长上下文场景下成本可控。
        [3].定义数据契约（Data Contract）标准（涵盖文档/片段/摘要/向量/图谱），集成 CI 契约测试；实现嵌入适配器（Adapter）接口，确保可无缝替换或回滚本地 Embedding 模型版本。

二、工具箱：支持基于网络 LLM 和先进 Agent 技术的智能分析工具集，包括 PESTEL、波特五力、$APPEALS、竞争对手分析、价值链分析、安索夫矩阵、SWOT-TOWS 分析、GE-麦肯锡矩阵、SPACE 矩阵、情景规划、价值主张画布、商业模式画布、破坏性创新（包括破坏潜力评估矩阵、破坏时间预测模型、破坏性创新投资决策框架）、BSC、战略地图、KPI 以及变革管理（共计 17 个）。这些工具可执行由AGENT通过协同机制指派的任务。
    1.工具定义
        [1].建立工具标准：每个工具有唯一标识、输入/输出 Schema、执行逻辑模板、领域事实库、验证器及演进日志。工具初始化必须加载工具定义（TOOL_DEFINE.md）、运行配置（TOOL_CONFIG.md）及安全边界（TOOL_BOUNDARY.md），严禁越权操作。
        [2].构建中央技能仓库：实现工具的统一注册、版本控制、灰度发布与回滚机制。所有入库工具必须通过 Pydantic V2 契约测试，后续迭代需通过回归测试集。支持预定义模板与远期基于代码生成的技能动态扩展。
        [3].支持工具链编排与依赖调度：支持定义工具链有向无环图，实现上游工具输出结构化流转为下游输入。内置任务调度器，根据依赖关系自动并行执行无依赖子任务。
    2.工具箱标准工作流
        [1].环境初始化：准备环境配置，实例化会话级沙箱与混合检索记忆容器。
        [2].感知与摘要：读取经处理的结构化 JSON 数据，自动生成全景数据摘要（含关键指标趋势、异常波动、关联文档清单），摘要存入临时上下文。
        [3].规划：Planner 依据 AGENT 委派的任务 IR 及数据摘要，输出机器可读的任务编排 JSON，包含子任务列表、匹配工具映射、数据依赖边。
        [4].执行与验证：Executor 按拓扑顺序调度子任务，每个子任务内执行 Think→Code→Execute→Observe→writeback 原子循环；每次输出均经 Auditor 校验，通过则写入持久化沙箱内存变量，失败则重试或回退；Executor 接收工具箱 DAG 调度指令，与 AGENT 调度器解耦。
        [5].证据打包：任务全部完成后，系统自动收集本次执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
        [6].反馈与固化：用户可通过命令行对分析结果提出修正，系统记录修正前后差异，通过修正分级判定器自动路由（L1 自动固化/L2 专家确认/L3 委员会审批），高频修正模式转化为业务规则模板。
        [7].成本审计与持久化：记录本次任务 Token 消耗、预估 USD 成本及执行时长；将任务轨迹匿名化存入技能演进数据集。
        [8].演化（可选）：匿名化执行轨迹存入工具箱演进数据集，作为后续 Strat-Bench 扩增样本或 Planner 拆解策略优化的输入源。
    3.数值计算与沙箱执行
        [1].强制代码执行隔离：严禁 LLM 直接进行数值计算、统计回归或现金流折现。所有量化分析强制通过两条路径执行：一是生成 Python 代码在自托管 gVisor沙箱执行；二是针对战略运筹问题（如线性规划），由 LLM 构建数学模型调用 CVXPY、OR-Tools 等优化器求解。
        [2].构建持久化计算沙箱：为每个任务会话实例化持久化 Jupyter Kernel，支持跨工具、跨步骤的变量传递（如宏观经济变量在不同模型间共享），实现分析上下文连贯。任务结束后自动销毁内核并回收资源。
        [3].支持 Validation Feedback 闭环：自动捕获代码执行 STDERR，检索错误案例库辅助 LLM 生成修复版本。设定最大重试次数（默认 3 次），失败则标记不可行并记录至演进日志。
    4.Schema 强制与一致性校验
        [1].实施契约化输出：所有工具输入/输出严格遵循 Pydantic V2 定义的 Data Model，利用 Instructor Patch 强制 LLM 输出符合 Schema 的结构化 JSON，严禁输出自由文本。
        [2].建立一致性校验仲裁器：在多工具协作中，通过内置规则引擎及财务常识库检测逻辑冲突（如利润率与成本矛盾）。输出冲突检测报告，供用户或高级 AGENT 裁决。
        [3].自动化契约测试：在 CI/CD 流水线集成 Schema 契约测试，防止模型漂移导致结构崩溃。契约测试失败的任务强制终止。
    5.可解释性与决策溯源
        [1].输出结构化推理证据包：每次分析必须打包输出全量元数据，包括输入摘要哈希、Planner 任务分解图、工具版本、关键中间变量、置信度评分及引用源（文档切片 ID）。
        [2].构建交互式溯源树：支持前端从结论反向逐层展开证据包，展示"数据来源→计算逻辑→中间结论→最终结论"的完整推理链。
        [3].支持红蓝辩论机制：针对高不确定性议题自动启动"红蓝对抗"，实例化激进派与保守派子智能体进行结构化辩论，输出包含共识与分歧区域的风险全景视图。
    6.提示词工程与演进
        [1].Prompt Optimizer：基于 DSPy 理念，建立"修正日志 - 样本转换"闭环，将用户修正转化为 Few-Shot 样本，自动迭代提示词参数以适配企业战略风格。
        [2].实施受控自动演进：L1 级修正自动固化，L2+ 级修正需人工确认。仅在 SP/BP Checkpoint 完成态且满足一致性阈值时触发演进。必须采用多目标优化（结构 40%、一致性 35%、成本 25%），严禁单一指标优化。
        [3].确保方法论不变性：演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验（Strat-Bench 通过率≥90%）。
        [4].支持版本管理与回滚：所有 Prompt 纳入 Git LFS 管理，与系统版本绑定。支持一键回滚至历史稳定版本（保留最近 10 个版本）。
    7.用户反馈与闭环演进
        [1].支持 Checkpoint 人工介入：在关键决策节点或低置信度环节自动暂停，请求用户验证中间假设。支持用户修改参数或调整权重，系统记录修正前后的"专家修正轨迹"。
        [2].建立修正轨迹库与逆向传播：将用户修正快照匿名化存储，定期分析差异，推断输入权重或任务分解策略的优化方向。
        [3].实施用户反馈协议：将用户反馈转化为结构化修正日志（修正类型 enum[参数/权重/约束/假设]、修正前/后值、置信度变化、影响因子、用户角色、时间戳），强制触发工具体"反思阶段"（ReflAct），回传至技能演进日志。
        [4].修正影响传播分析：评估修正对下游任务的影响范围，高影响修正（>3 个下游任务）强制生成 Impact Report 并通知相关 AGENT，支持从任意 Checkpoint 恢复执行后修改中间状态变量并从修改点继续执行。
    8.安全与防御机制
        [1].实施沙箱网络隔离：代码执行环境默认断网，仅允许通过白名单网关访问预设的可信财经 API，严禁通用网页爬虫。
        [2].建立提示词注入防御：输入数据前置执行清洗与注入检测。将"注入防御测试"纳入基准测试集，定期验证工具体的抗干扰能力。
        [3].建立工具幻觉黑名单：自动监控工具体输出，若连续 3 次生成与输入矛盾的结论，自动锁定该工具体并报警。
    9.领域专业知识嵌入
        [1].内置领域事实库：以 JSON Schema 存储经典管理理论的强制约束（如 SWOT 内部/外部属性分类），确保分析逻辑符合理论框架。
        [2].部署领域验证器：工具体输出前必须经过验证器检查，确保结论不违背基础商业逻辑与领域常识。
    10.服务契约与集成
        [1].独立微服务封装：工具箱作为独立微服务对外暴露 OpenAPI 3.1 标准接口，输出分析结果、证据包与成本审计信息三元组。
        [2].支持 Agent 模块集成：与 CEO、CFO 等核心 AGENT 通过服务调用无缝集成，支持水平扩展与分布式 Trace 追踪。
    11.测试与质量保证
        [1].构建基准测试集（Strat-Bench）：建立包含 20+ 专家标注场景的战略分析专用测试集，作为版本迭代的准入测试标准。
        [2].支持 A/B 测试框架：支持多版本策略并行运行，基于任务完成率、计算精度与成本指标择优晋级。
        [3].部署健康度仪表盘：实时监控工具成功率与修正率，修正率超标（默认 15%）自动触发 Skill Review。
    12.性能监控与漂移检测
        [1].建立性能漂移检测：定期评估检索准确率与生成质量，对比历史基线检测性能下降。
        [2].支持可视化日志：所有评估与运行结果纳入系统行为日志并可视化展示。
    13.成本熔断与契约测试质量门禁
        [1].CI/CD 契约测试门禁：所有工具输出必须通过严格 Schema 校验，非兼容变更触发构建失败。运行时连续三次校验失败触发工具熔断。
        [2].实施三级成本熔断：支持任务级（单次 Token/成本超限）、会话级（日配额超限）与重试级（连续代码执行失败）熔断机制，确保系统运行成本可控。

三、AGENT：支持网络调用 LLM API 驱动的企业战略规划 AGENT 执行战略规划任务，这些 AGENT 包括首席执行官 CEO AGENT、首席运营官 COO AGENT、首席营销官 CMO AGENT、首席技术官 CTO AGENT、首席财务官 CFO AGENT、首席人力官 CHO AGENT 共 6 个核心 AGENT，以及 1 个联席审计官 AUD AGENT。这些AGENT可通过协同机制调用工具或工具组合执行任务。
    1. AGENT定义
        [1]. AGENT 有唯一标识、角色定义、权责边界、领域专业知识与技能、验证器及演进日志。AGENT 实例化启动时加载身份档案（IDENTITY.md）、行为规范（CODE.md）、人格定义（SOUL.md）、工具集（TOOLS.md）、用户信息（USER.md）、长期记忆（MEMORY.md）、心跳任务配置（HEARTBEAT.md）
    2. 输入处理与全景构建
        [1]. 输入系统预处理的结构化 JSON 数据，AGENT 首先调用摘要质量评估器（基于信息熵与关键实体覆盖率），若摘要质量评分低于阈值（默认 0.7）则自动触发二次检索补充缺失维度，确保全景视图的完整性。
        [2]. 构建动态全景视图：宏观层（战略指标趋势/异常波动/关键假设变量）与微观层（文档切片引用索引）双向关联，支持细节挖掘过程中发现与全景视图严重矛盾时自动触发视图重构，重构过程需记录变更轨迹并提交 AUD AGENT 审核。
        [3]. 实施上下文窗口动态管理：基于任务复杂度自适应分配 Token 预算，超限时触发上下文压缩器（基于关键实体抽取与 HyDE 算法），压缩率目标≥70%，确保关键信息不丢失。
    3. 任务分解与协作架构
        [1]. 建立任务 IR（中间表示）标准：每个任务包含任务 ID、类型、输入/输出 Schema、依赖任务列表、优先级、执行状态、超时策略。所有协作任务必须基于有向无环图（DAG）构建流转拓扑，确保多体协作具备确定性的状态机流转特征；CEO AGENT 仅定义 required_tools 列表，工具依赖由工具箱 DAG 自主推导。
        [2]. 采用分层协作架构：CEO AGENT 作为指挥官负责目标解析与全局任务分解，各专业 Agent 并行执行子任务，调度器负责资源分配与执行监控。自动生成协作依赖图，基于 BLM/BEM 阶段定义标准依赖关系，支持动态调整与优化，可视化展示任务间数据流与控制流。
        [3]. 支持柔性视角隔离：定义核心视角（必须遵守）和扩展视角（可协商），对跨领域问题组建临时联合分析组，建立视角冲突解决协议（触发条件：跨角色关键词频率>5%；仲裁流程：CEO AGENT 裁决）。视角隔离通过 Prompt 隔离、工具隔离、数据隔离三重硬约束实现。
    4. AGENT 确定性工作流
        [1]. 基于状态图的工作流引擎：采用 StateGraph 机制定义显式状态模型，包含 session_id、current_phase、agent_role、working_memory、long_term_memory_ref、tools_used、confidence_score、cost_accumulated 等字段。
        [2]. AGENT 标准工作流：
            (1).[初始化]加载环境配置并初始化，根据用户偏好（风险偏好、时间压力、任务上下文）渲染价值观模板（SOUL.md），实例化会话级沙箱与混合检索记忆容器。
            (2).[感知] 读取经处理的结构化 JSON 数据，自动生成全景数据摘要，生成并加载任务上下文。
            (3).[规划] 生成任务执行 DAG，匹配工具映射
            (4).[执行] 按拓扑顺序调度子任务，每个子任务内执行 Think→Code→Execute→Observe→writeback 原子循环；每次输出均经审计校验，通过则写入持久化沙箱内存变量，失败则重试或回退。
            (5).[深度思考]（可选）在关键决策节点并行生成多条思维链推演路径
            (6).[验证] 结构化检查输出。支持置信度达标且目标达成时提前终止
            (7).[反思] 错误分析与修正
            (8).[证据打包] 任务全部完成后，系统自动收集本次任务执行全量元数据、中间产物、最终输出，封装为证据包并写入战略档案库。
            (9).[演化]（可选）匿名化执行轨迹存入工具箱演进数据集，作为后续 Strat-Bench 扩增样本或任务拆解策略优化的输入源。
        [3]. 深度思考与多路径推演：在 SP/BP 所有核心分叉点（如商业模式选择、核心竞争力定义），默认调用支持深层推理的模型，并行生成多条思维链推演路径，通过内部验证打分选出最优解，极大提升战略输出的逻辑厚度。
        [4]. 中断安全与状态持久化：任意节点支持用户中断，自动保存 StateSnapshot 至 Redis Cluster（三节点哨兵模式），恢复时加载完整上下文与工具状态，支持从任意历史快照恢复执行（Time-travel 能力分步实现）。
    4. 用户反馈闭环
        [1]. 建立反馈标准化协议：将用户反馈转化为结构化修正日志（包含修正类型 enum[参数/权重/约束/假设]、修正前/后值、置信度变化、影响因子、用户角色、时间戳）。
        [2]. 反馈分级处理与即时响应：通过修正分级判定器自动路由——L1 级（置信度≥0.85，影响≤1 任务）自动固化；L2 级（置信度≥0.75，影响 2-3 任务）专家确认（1 人 24 小时 SLA）；L3 级（影响>3 任务）委员会审批（7 天 SLA）。噪声反馈自动过滤。设置专用反馈通道，承诺 24 小时内处理并回复，紧急反馈设置加急处理流程。
        [3]. 修正影响传播分析：评估修正对下游任务的影响范围，高影响修正（>3 个下游任务）强制生成 Impact Report 并通知相关 AGENT。支持从任意 Checkpoint 恢复执行后修改中间状态变量（Time-travel 第一阶段），并从修改点继续执行。
    5. 提示词工程与演进
        [1]. 基于 DSPy 理念构建 Prompt Optimizer：输入为用户反馈、AUD 一致性报告、SP/BP Checkpoint 人工反馈、EvolutionLog，输出优化后 System Prompt 与 Few-Shot 样本集。优化目标采用 Pareto 多目标权衡（结构完整性 40% + 逻辑一致性 35% + 成本效率 25%），采用 NSGA-II 算法求解 Pareto 前沿。
        [2]. 受控演进机制：L1 级修正自动固化，L2+ 级修正需人工确认。自动微调的提示词参数必须进入"待审查版本库"，经人工专家（首席战略官或授权代表）确认后方可推向生产环境。集成 MLflow 实现 Prompt 自动版本注册，支持 A/B 测试（分流比例可配置）与一键回滚（保留最近 10 个稳定版本）。
        [3]. 提示词契约测试：每次优化后自动运行 Strat-Bench 基准测试集，通过率<90% 自动拒绝上线，并生成差异分析报告。演进过程不得改变战略模型定义的逻辑步骤，必须通过结构化差异校验。
    6. 工具调用与集成
        [1]. 全面遵循 MCP（Model Context Protocol）2025 规范：工具箱作为独立微服务对外暴露符合 MCP 标准的能力发现接口，支持工具注册、版本控制、灰度发布与回滚。AGENT 通过 MCP Registry 查询工具能力描述（输入/输出 Schema、版本、可靠性评分）。
        [2]. 工具选择优化：基于语义匹配（60%）+ 历史成功率（40%）计算工具匹配度，匹配度<0.6 时触发人工确认或启动多工具并行验证。预定义 17 种战略工具的最佳组合模板（如 SWOT+TOWS 自动组合），支持用户自定义模板。
        [3]. 强制契约化调用与沙箱执行：所有工具调用前验证输入 Schema，调用后验证输出 Schema，连续 3 次校验失败自动熔断该工具。代码执行在自托管 gVisor 沙盒中（网络隔离 + 权限最小化），执行结果经 Validation Feedback 闭环校验，最大重试次数 3 次。
        [4]. 编排分层：AGENT DAG 与工具箱 DAG 遵循 4.6 协同协议
    7. 仲裁与决策机制
        [1]. CEO AGENT 内置裁决状态机，设定最大辩论轮次（默认 3+ 风险等级，上限 7 轮）。辩论采用三阶段：发散（Temperature=0.8）、收敛（Temperature=0.5）、裁决（Temperature=0.2），强制随轮次增加提交具体修改 JSON 补丁。
        [2]. 辩论质量评估：每轮计算新信息增益率与重复率，增益率<10% 且重复率>50% 时强制终止辩论。辩论过程记录各 AGENT 贡献度（基于 Shapley 值量化），分歧点生成热力图。
        [3]. 多方案生成与人类一票否决：若未达成一致，生成 Plan A（保守参数集）、Plan B（激进参数集）和 Plan C（AI 融合版），输出参数调整明细。强制暂停 60 秒提供用户介入窗口，窗口期可视化展示三套方案的对比差异（风险/收益/资源需求）及影响模拟器（支持用户滑动调整关键参数实时预览结果）。超时无用户操作则按 CEO 决策执行，但所有人工覆盖决策强制记录至战略档案库并标记"人工覆盖"。
    8. 审计与一致性保障
        [1]. AUD AGENT 采用"旁路监听 + 异步阻断"模式，不参与生成。审计范围包括四维一致性检验：①事实一致性（对比原始文档切片）；②逻辑一致性（规则引擎检测内部矛盾）；③数值一致性（沙箱重计算关键指标，误差>5% 标记异常）；④时效一致性（引用数据超 12 个月标记"数据陈旧"）。
        [2]. 分级熔断与根因分析：L1（幻觉概率>0.8）自动触发修正；L2（0.6-0.8）通知相关 AGENT 重试（最多 2 次）；L3（<0.6）暂停任务并生成 Hallucination Report（包含错误类型分类：数据缺失/逻辑跳跃/数值错误/时效过期），人工介入后从断点恢复。
        [3]. 审计日志强制存储：所有审计发现、纠错过程、重试轨迹全量存储至不可篡改日志（WORM 存储），满足 SOX/ISO27001 审计要求，支持按时间/角色/任务类型多维检索。
    9. 记忆与上下文管理
        [1]. 分层记忆架构：工作记忆（当前会话，有效期 24h）、短期记忆（最近项目，有效期 30 天）、长期记忆（战略知识库，永久存储），各层有独立的存储引擎和检索策略。长期记忆将用户修正日志、决策复盘结果、执行结果差值反向固化为少样本提示词，每季度触发知识蒸馏与去重。
        [2]. 公共黑板机制：基于 LangGraph StateSnapshot + Redis Cluster 管理状态版本，支持多版本并发控制（MVCC）。关键结论写入黑板时必须附带重要性评分（1-5）、置信度、引用源切片 ID、时效性，综合评分>3.5 才写入，便于上下文压缩器智能筛选。
        [3]. 时间旅行调试：支持从任意 Checkpoint 恢复执行，用于问题复现与根因分析。所有状态快照与完整推理轨迹归档至数据湖，可按需解压缩回溯。
    10. 可解释性与溯源
        [1]. 结构化证据包标准：每次分析打包输出全量元数据，包括输入摘要哈希、任务分解图、工具调用轨迹（含版本）、关键中间变量快照、置信度评分及依据、引用源切片 ID 列表。
        [2]. 交互式溯源树：前端支持从结论节点逐层展开至原始数据，每层显示"数据来源→计算逻辑→中间结论"三元组，每个节点支持查看详细信息。
        [3]. 决策影响分析：量化各输入变量对最终决策的 Shapley 贡献值，高影响变量（>20%）自动高亮标注。支持反事实推理，输出"如果改变某假设，结论会如何变化"的对比报告和风险热力图。
    11. 主动能力
        [1]. 引入心跳机制：支持周期性自动唤醒（可配置频率），主动检查战略档案库中的待办事项、偏差预警、周期性任务，无需用户提问即可主动沟通。心跳检查时要进行成本预算前置校验。
        [2]. 战略偏差预警：实时监控实际执行数据与 SP/BP 规划的偏差，偏差超过阈值（默认 10%）自动触发预警，生成偏差分析报告并推送相关 AGENT。
        [3]. 主动知识更新：当检测到行业报告、市场数据、政策法规更新时，自动触发电请相关 AGENT 评估影响，重要影响主动推送 CEO AGENT。
    12. 性能监控与行为分析
        [1]. 建立三维度指标体系：效率维度（任务完成率、平均时长、工具调用成功率）、质量维度（修正率<10%、幻觉率<5%、一致性得分>0.8）、成本维度（单任务 API 成本、Token 消耗）。构建战略规划专用基准测试集（Strat-Bench）作为版本迭代的准入标准。
        [2]. 漂移检测算法：CUSUM 检测性能指标连续下降（滑动窗口 7 天）。指标异常时启动自动根因分析，关联日志输出根因报告。
        [3]. 健康度仪表盘：实时可视化展示各 AGENT 的 12 项健康度指标，支持下钻分析与预警订阅。三级预警机制：黄牌（指标下降 15% 触发分析）、红牌（下降 30% 触发人工 review）、停用（下降 50% 且连续三天，自动回滚至前一稳定版本）。
    13. 安全与防御
        [1]. 纵深防御体系：
            - 第一层（隔离）：三重硬隔离（Prompt 隔离、工具隔离、数据隔离），基于角色权限最小化。
            - 第二层（执行）：所有代码在自托管 gVisor 沙盒中执行，严格网络隔离（仅白名单 API）。
            - 第三层（检测）：实时监控视角越界（跨角色关键词频率>5% 触发熔断）、幻觉累积（连续 3 次低置信度结论锁定 Agent）。
            - 第四层（审计）：所有操作记录至 WORM 存储。
            - 第五层（熔断）：辩论过热保护、成本熔断。
        [2]. 敏感数据脱敏：审计日志和反馈记录自动脱敏处理（个人可识别信息、商业机密），脱敏策略定期评审更新。
    14. 成本控制
        [1]. 成本预测：记录每次任务 Token 消耗、预估成本及执行时长，任务启动前基于历史相似任务预测 Token 消耗与 API 成本，偏差>50% 时提示用户确认。
        [2]. 三级熔断机制：
            - 任务级熔断：单次任务 Token 消耗超预设阈值（基础任务 5K tokens，复杂规划 50K tokens）立即终止。
            - 会话级熔断：日累计 Token 消耗超企业配额（默认 100 万 tokens）暂停新任务，已启动任务完成。
            - 系统级熔断：连续 3 次工具执行失败或单任务成本超预算 200% 触发系统级告警，需管理员介入。

四、战略规划：系统核心功能是输出五年滚动战略规划（简称 SP）和年度业务计划（简称 BP），SP 基于业务领先模型模型（简称 BLM）制定，BP 基于业务执行模型（简称 BEM）制定。系统由先进 LLM 和 Agent 驱动，基于规划模型和方法论智能运行
    1.严格遵守 BLM 与 BEM 模型的规定流程与动作进行战略规划工作。SP 输入数据包括：上期战略规划/业务计划/财务报表/运营数据/企业年报，本期市场报告/咨询报告/财务报表/运营数据
    2.制定 SP 采用 BLM 模型，具体分为业绩差距分析、市场洞察 (包括六个子步骤)、战略意图与目标、创新焦点、业务设计、执行设计六个阶段。各规划阶段建议战略工具与 AGENT（需严格执行视角隔离）组合如下
        [1].业绩差距分析：建议工具 SWOT-TOWS 分析、GE-麦肯锡矩阵；（全部 AGENT 负责）
        [2].市场洞察
            (1).看趋势：建议工具 PESTEL 分析；（CEO/CMO/CTO AGENT 负责）
            (2).看市场与客户：建议工具暂无；（CEO/CMO/CTO AGENT 负责）
            (3).看竞争：建议工具波特五力分析；（CEO/CMO/CTO AGENT 负责）
            (4).看自己：建议工具价值链分析、SWOT-TOWS 分析（内部部分）；（全部 AGENT 负责）
            (5).看机会：建议工具 SWOT-TOWS 分析、安索夫矩阵；（全部 AGENT 负责）
            (6).机会差距分析：建议工具 SWOT-TOWS 分析、GE-麦肯锡矩阵；（全部 AGENT 负责）
        [3].战略意图与目标：建议工具 SWOT-TOWS 分析、BSC（目标层面）；（全部 AGENT 负责）
        [4].创新焦点
            (1).价值创新：建议工具破坏性创新模型；（CEO/CMO/CTO AGENT 负责）
            (2).模式创新：建议工具商业模式画布、安索夫矩阵（新市场/新产品）；（CEO/CMO/CTO AGENT 负责）
        [5].业务设计：建议工具商业模式画布、GE-麦肯锡矩阵、SPACE 矩阵；（CEO/CMO/CTO AGENT 负责）
        [6].执行设计
            (1).关键任务与依赖关系：建议工具 BSC、KPI；（全部 AGENT 负责）
            (2).人力/组织/文化策略：建议工具变革管理模型（如 ADKAR）、BSC（学习与成长层面）；（全部 AGENT 负责）
    3.BP 严格依赖 SP 的最终输出。构建战略解码器，实现从 SP_Output_Schema 到 BP_Input_Schema 的结构化映射转换。制定 BP 采用 BEM 模型，各阶段建议战略工具与 AGENT（需严格执行视角隔离）组合如下
        [1].澄清战略方向与运营定义：建议工具 BSC（战略目标与主题澄清）；（全部 AGENT 负责）
        [2].导出中长期关键战略举措：建议工具 SPACE 矩阵、安索夫矩阵；（CEO/CMO/CTO AGENT 负责）
        [3].导出战略衡量指标：建议工具 BSC、KPI；（全部 AGENT 负责）
        [4].确定年度业务关键措施并导出具体目标：建议工具 BSC、KPI（年度分解）、价值链分析（活动优化）；（全部 AGENT 负责）
        [5].分解关键措施和目标：建议工具 BSC、KPI；（全部 AGENT 负责）
        [6].导出重点工作计划：建议工具 BSC、KP 行动方案；（全部 AGENT 负责）
    4.支持 SP/BP 各阶段 Checkpoint 机制，各个阶段任务完成后暂停并输出阶段性结果，用户可反馈与修正该结果，然后继续执行后续任务
    5.SP/BP 各个阶段，输出 JSON 思维链 Input -> <Reflection>...</Reflection> <Tools_Used>...</Tools_Used> <Constraints_Check>...</Constraints_Check> -> JSON
    6.工具箱和 AGENT 协同机制
        [1].编排分层原则
            (1).AGENT DAG 负责"做什么"(What): 战略任务分解、AGENT 分配、跨任务依赖
            (2).工具箱 DAG 负责"怎么做"(How): 工具选择、工具间依赖、执行优化
            (3).禁止跨层依赖引用 (AGENT 不可直接引用工具节点，工具不可感知 AGENT)
        [2].任务委派协议
            (1).AGENT DAG 完成任务分解后，向工具箱发送 TaskRequest {
                task_id: UUID,
                task_type: Enum[业绩差距分析，市场洞察，...],
                input_data: JSON (符合 input_schema),
                required_tools: List[tool_id] (可选，AGENT 可建议工具),
                output_schema: Pydantic Schema,
                deadline_ms: Int,
                cost_budget: {max_tokens, max_usd}
            }
            (2).工具箱返回 TaskAck {
                ack_id: UUID,
                estimated_duration_ms: Int,
                tool_dag_hash: SHA256 (用于追溯),
                cost_estimate: {tokens, usd}
            }
        [3].依赖边界规则
            (1).AGENT DAG 依赖：仅能引用其他 AGENT 任务的输出
            (2).工具箱 DAG 依赖：仅能引用同一 TaskRequest 内的工具输出
            (3).跨 TaskRequest 的数据传递必须通过战略档案库 (写回→读取模式)
        [4].状态同步机制
            (1).工具箱执行状态 (例如Waiting/Running/Completed/Failed等) 实时推送至 AGENT DAG
            (2).AGENT DAG 状态变更 (例如Paused/Resumed/Cancelled等) 广播至相关 TaskRequest
            (3).统一状态存储：Redis Cluster (扩展 3.9.2 公共黑板机制)
        [5].错误传播规则
            (1).工具级错误 (Schema 校验失败、沙箱执行异常) → 工具箱内部重试 (≤3 次)
            (2).任务级错误 (重试耗尽、成本超预算) → 返回 TaskResponse {status: Failed, error_code, partial_results}
            (3).AGENT 级错误 (工具连续失败≥3 个) → 触发 CEO AGENT 裁决 (见 3.7)
        [6].成本归属原则
            (1).工具箱核算单 TaskRequest 的实际成本 (tokens, usd)
            (2).AGENT DAG 聚合所属 TaskRequest 成本，计入任务总成本
            (3).成本熔断：工具箱执行任务级熔断，AGENT 执行会话级熔断
    7.支持 A/B 测试框架，对比不同策略效果，并建立基准测试集验证系统性能
    8.建立决策质量评估器，基于贝叶斯网络计算决策置信度
    9.建立性能漂移检测机制，定期评估模型性能并检测性能下降

五、用户接口：系统不实现复杂的用户接口，只需要以下命令行接口
    1.用户通过用户名密码远程登录服务器。登录时自动执行环境自检（GPU 驱动/CUDA 版本/内存），若环境异常则报警
    2.批量输入数据供系统分析处理，输出系统处理后的结构化数据
    3.指定一个或多个 AGENT 协作分析输入的数据，并输出结构化结果
    4.指定系统根据输入数据执行 SP 与 BP 规划，生成 PDF 格式的报告。报告中包含可点击的引文索引，支持溯源
    5.用户可检索 RAG 处理后的数据
    6.支持 Checkpoint 交互，查看当前阶段分析摘要，修正关键参数后恢复运行
    7.支持导入季度/年度经营复盘数据（实际执行结果），用于计算与规划的偏差，并反馈至战略档案库以实现闭环演进
    8.支持系统行为日志记录，用于后续分析和优化
    9.提供决策过程可视化接口，展示关键决策路径和依据
